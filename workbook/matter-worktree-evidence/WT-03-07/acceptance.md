# WT-03-07 진행률·상태 표시

- 상태: PASS (source/test/build/render).
- server projection owns cancelled exclusion and progress math.
- UI exposes done/total and semantic `progressbar` values.
- blocked and overdue use icon plus Korean text and count, never color alone.
- 실제 화면에서 `1/2 완료`, 차단 1, 기한 초과 1과 semantic progressbar를 확인했다.
