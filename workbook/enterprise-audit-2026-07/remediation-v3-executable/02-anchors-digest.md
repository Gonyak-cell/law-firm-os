

############ bridge-header ############
VERDICT: confirmed — Every load-bearing element was verified directly in /Users/jws/lawos-diag-wt at 169484912. (1) Re-ran node --test apps/api/test/matter-vault-bridge-api.test.js: 0/6 failing, requests dying 401 before handler auth — collision mechanism confirmed. (2) server.js anchors exact: session gate :779 with requireSessionToken:true, isAuthPath :772-777, pathname trailing-slash normalization :706, CORS allow-headers :417-426, isMatterPath dispatch :858; parsePermissionContext missing from the :40 import at HEAD but present at 6c83a2d5c^:40 and used at :698; sendJson/readRequestBody/hasJsonRequestBody/quer
CORRECTED: Three non-blocking corrections to fold into the plan: (1) add scripts/run-lcx-vltui-closeout-proof.mjs to the Playwright proof-gate rerun list (it mocks the bridge routes in-page like the others); (2) narrow the external amic-vault swap list to the three real Bearer call sites — matter-app-canonical-upsert-sync.mjs:215, matter-app-identity-preflight.mjs:112, onedrive-client-matter-write.mjs:173 (matter-app-migration-db-linkage-closeout.mjs uses cookie auth against the Vault app and sends no bridge Bearer; the preflight spec only references paths); (3) validator route strings are at scripts/validate-lcx-vltui-bridge-contract.mjs:48-52. The Lambda-pinned-at-3d7d9da36 premise is corroborated by docs/desktop/matter-desktop-v0.1.5-current-clients-matter-codes-release-notes-2026-07-01.md:57 with no newer deploy receipt, but was not live-verified (AWS query denied by permission policy) — recommend one aws lambda get-function-configuration check of LAWOS_DEPLOYMENT_COMMIT before relying on the clean-cut ordering.
SUMMARY: Move the 5 matter vault-bridge machine routes off the Authorization header onto a dedicated x-lawos-vault-bridge-token header, and exempt exactly those method+path pairs from the server.js session gate. Collision verified live in the worktree: node --test apps/api/test/matter-vault-bridge-api.test.js at HEAD 169484912 fails 0/6 — every request dies at the session gate (server.js:779, requireSessionToken:true) with 401 AUTH_SESSION_REQUIRED/AUTH_SESSION_INVALID before matter-runtime-context.js:456 can compare the bearer against LAWOS_VAULT_BRIDGE_TOKEN. Target taxonomy (unchanged from contract fixture): no LAWOS_VAULT_BRIDGE_TOKEN configured → 503 MATTER_VAULT_BRIDGE_REQUIRED; header missing/wrong (including a valid session token in Authorization without the bridge header) → 403 MATTER_VAULT_BRIDGE_BLOCKED; valid header → 200/201 (handler-level 400/409 unchanged). Web UI keeps session Authorization and needs zero component changes (UI never holds a bridge token; guarded 403/503 states are already rendered). Clean cut is safe for deployed surfaces: production Lambda matter-lawos-api-prod is pinned at pre-gate commit 3d7d9da36 (2026-07-01; gate landed 6c83a2d5c 2026-07-03), so no deployed server enforces the gate yet; the only out-of-repo callers are operator-run migration tools in /Users/jws/Projects/amic-vault (not deployed services) which need a coordinated one-line header swap before their next run.

-- INVENTORY --
1. Bridge auth check reads Authorization bearer and compares vs LAWOS_VAULT_BRIDGE_TOKEN with timing-safe sha256 compare
   EV: apps/api/src/matter-runtime-context.js:125-128 (configuredVaultBridgeToken reads process.env.LAWOS_VAULT_BRIDGE_TOKEN), :130-135 (bearerToken parses headers.authorization — bearerToken has NO other consumer in the file), :137-146 (tokenDigest sha256 + timingSafeEqual in tokenMatches — timing-safe compare IS present, keep), :451-460 (validateVaultBridgeAuth: no env → 503 vault_bridge_required at :453-454; mismatch → 403 vault_bridge_blocked at :456-457). Error codes at :40-41 (MATTER_VAULT_BRIDGE_REQUIRED / MATTER_VAULT_BRIDGE_BLOCKED).
2. The exact 5 bridge routes and their handlers (dispatch is exact pathname+method equality inside handleMatterApiRequest, no regex)
   EV: apps/api/src/matter-runtime-context.js:3225-3239 — GET /api/matters/vault-bridge/status → handleVaultBridgeStatus (:471), GET /api/matters/vault-bridge/matter-lookup → handleVaultBridgeMatterLookup (:513, uses routeGate+context), POST /api/matters/vault-bridge/upload-preflight → handleVaultBridgeUploadPreflight (:613, uses routeGate+context), POST /api/matters/vault-bridge/clients/upsert → handleVaultBridgeClientUpsert (:703), POST /api/matters/vault-bridge/matters/upsert → handleVaultBridgeMatterUpsert (:745). Each calls validateVaultBridgeAuth first (:472,:514,:615,:704,:746). Route lists also at apps/api/src/routes/matters.js:9-16 and MATTER_BOUNDED_CONTEXT apps/api/src/matter-runtime-context.js:84-88.
3. server.js routing: bridge paths are matched only by the generic /api/matters prefix; session gate consumes Authorization before dispatch; raw req.headers forwarded to matter handler
   EV: apps/api/src/server.js:706 (pathname trailing-slash normalize), :719 (isMatterPath = pathname.startsWith("/api/matters") — no bridge-specific predicate exists), :779-791 (sessionAuth.resolvePermissionContextFromHeaders(req.headers,{requireSessionToken:true}) → 401 on missing/non-session bearer per apps/api/src/session-auth.js:406-421, TOKEN_PREFIX "lawos_session_v1" at :38), :793-803 (requestHeaders() overwrites x-lawos-tenant-id/actor/role/scopes from session principal — used by HRX only), :858-873 (isMatterPath dispatch; :866 passes RAW req.headers, :859 passes context = sessionContext.context).
4. Live proof of collision: full bridge test suite fails at HEAD
   EV: [직접 재실행] node --test apps/api/test/matter-vault-bridge-api.test.js in /Users/jws/lawos-diag-wt → 0 pass / 6 fail, e.g. test at :316 expects 403 got 401 (session gate). Pre-gate server (git show 6c83a2d5c^:apps/api/src/server.js:692-698) made the session optional and fell back to parsePermissionContext(req.headers["x-lawos-permission-context"]) — that is the behavior to restore for bridge paths only.
5. The 6 test cases and their assertions
   EV: apps/api/test/matter-vault-bridge-api.test.js — (1) :119-126 no env+no header → 503 [MATTER_VAULT_BRIDGE_REQUIRED]; (2) :128-161 'Vault bridge rejects missing bearer auth and invalid canonical upsert payloads': no-auth status → 403 BLOCKED, invalid client payload w/ token → 400 MATTER_API_VALIDATION_ERROR, valid client upsert → 201, mismatched matterCode → 400; (3) :163-204 status 200 (source_mode matter_app_api, runtime_write_ready), client 201, matter 201, idempotent replay 200 action=skipped_idempotent; (4) :206-252 lookup: missing bearer → 403 BLOCKED items:[], UUID q → 400, positive lookup → 200 with reference-only items (no document_bytes/storage_pointer); (5) :254-314 preflight: missing bearer → 403, vault_projection_only → 409 MATTER_VAULT_UPLOAD_PREFLIGHT_SOURCE_BLOCKED, positive → 200 preflight_passed with vault_document_write_enabled=false; (6) :316-325 wrong bearer value → 403 BLOCKED. Token helper authHeaders at :35-37; permission context header helper at :39-51. Tests intended taxonomy = 503/403/200/201 exactly as ratified; the OLD contract never said 401 anywhere — 401 only appears as today's collision symptom from the session gate.
6. Web call sites: apiClient bridge helpers and their two component consumers; the session header overwrite bug
   EV: apps/web/src/data/apiClient.js:2861-2911 fetchVaultBridgeStatus (:2864 if (bridgeToken) headers.Authorization=...), :2913-2969 fetchVaultMatterLookup (:2916), :2971-3045 fetchVaultUploadPreflight (:2979). All go through apiFetch (:262-267) → sessionAuthorizedHeaders (:247-260) which setHeader()s authorization to the stored session token (:258, setHeader overwrite semantics at :160-163) — so today bridgeToken can NEVER reach the server when a session exists (silently clobbered). Component callers never pass bridgeToken: apps/web/src/components/VaultSurface.jsx:846 (fetchVaultBridgeStatus({ctx:liveCtx})), :890 (fetchVaultMatterLookup({ctx:liveCtx,query})), :912 (fetchVaultUploadPreflight); apps/web/src/components/MatterVaultPanel.jsx:263, :333. Enforced by apps/web/test/ui-regression.test.mjs:861 (VaultSurface must NOT match /bridgeToken/) and :889 (apiClient MUST match /bridgeToken/), plus scripts/validate-lcx-vltui-vault-status-panel.mjs:40-41, scripts/validate-lcx-vltui-matter-picker.mjs:64-65, scripts/validate-lcx-vltui-action-boundaries.mjs:62. UI obtains NO bridge token today (no config/env/vite/user-input source anywhere in apps/web/src) — the guarded 403/503 states are the designed UI outcome (VaultSurface.jsx:59-62,:181-190 map MATTER_VAULT_BRIDGE_REQUIRED/BLOCKED to Korean safe labels).
7. scripts/ callers sending the bridge token via Authorization
   EV: scripts/run-current-matter-codes-production-bridge-upsert.mjs:48-50 (authHeaders(token) → {authorization: Bearer}), :216 (token from env or Lambda), :242,:245,:264 (upserts against BASE_URL https://d2mthcc8vp3cr2.cloudfront.net per :7); scripts/run-lcx-vltui-production-smoke.mjs:116-119 (bridgeAuthHeaders → {authorization: Bearer BRIDGE_TOKEN}), :309-351 (5 bridge checks; :333-336 merges bridge auth + x-lawos-permission-context); scripts/lib/aws-sso-lambda-env.mjs:26-33,:82-119 (resolves LAWOS_VAULT_BRIDGE_TOKEN from Lambda matter-lawos-api-prod env — transport-agnostic, no change needed); scripts/test/aws-sso-lambda-env.test.mjs:19,:36 (env resolution only). Playwright proof scripts (run-lcx-vltui-*proof.mjs, run-lcx-vltui-browser-qa.mjs:185-195, run-lcx-full-*browser-proof.mjs) mock the bridge routes via page.route — auth-agnostic, no header change needed.
8. Gate/validator scripts coupled to the bridge contract wording
   EV: scripts/validate-lcx-vltui-bridge-contract.mjs:49-53 (5 route strings), :122 (forbids literal 'Bearer ' inside the contract fixture), :127 (REQUIRES the exact test title 'Vault bridge rejects missing bearer auth and invalid canonical upsert payloads' to exist in apps/api/test/matter-vault-bridge-api.test.js), :128 (MATTER_VAULT_BRIDGE_BLOCKED marker); scripts/validate-lcx-vltui-matter-picker.mjs:49-65; scripts/validate-lcx-vltui-vault-status-panel.mjs:35-44; scripts/validate-lcx-vltui-upload-preflight.mjs (no bearer-wording asserts); scripts/validate-lcx-vltui-session-principal.mjs:108.
9. Docs stating the old Authorization-bearer contract
   EV: docs/lazycodex/lcx-vault-app-current-ui-source-register-2026-06-29.json:76,83,90,97,104 ('auth': 'Authorization: Bearer <LAWOS_VAULT_BRIDGE_TOKEN>' for all 5 Law-side endpoints; Vault-app implementation path recorded as /Users/jws/Projects/amic-vault at :~110); docs/lazycodex/lcx-vltui-01-vault-bridge-contract-2026-06-29.json:25 (required env), :151-163 (negative cases: 503 MATTER_VAULT_BRIDGE_REQUIRED, 403 MATTER_VAULT_BRIDGE_BLOCKED, id 'law_bridge_bearer_missing_or_wrong'). Dated receipts/evidence (docs/lazycodex/*receipt*, docs/lazycodex/evidence/**, docs/desktop/*2026-07-01*) are historical proof artifacts — do not rewrite.
10. Backward-compat: deployed and external callers
   EV: Production Lambda matter-lawos-api-prod redeployed to commit 3d7d9da36 on 2026-07-01 (docs/desktop/matter-desktop-v0.1.5-current-clients-matter-codes-release-notes-2026-07-01.md:57; git log confirms 3d7d9da36 2026-07-01, ancestor of HEAD). Session gate landed later in 6c83a2d5c 2026-07-03 ('Close Wave 1 remediation and release candidate', via git log -S 'requireSessionToken: true'). So the DEPLOYED server still accepts bare Authorization-bearer bridge calls today, and deploying HEAD without this fix would 401 the production bridge. Out-of-repo callers: /Users/jws/Projects/amic-vault/tools/migration/{matter-app-canonical-upsert-sync.mjs (bridge paths at :17-19, 'authorization: Bearer ${args.matterAppApiToken}' at :215), matter-app-identity-preflight.mjs, matter-app-identity-preflight.spec.mjs, matter-app-migration-db-linkage-closeout.mjs, onedrive-client-matter-write.mjs} — all operator-run migration tools, NOT deployed services. apps/desktop and apps/addin: zero grep hits for LAWOS_VAULT_BRIDGE_TOKEN|vault-bridge|bridgeToken (desktop wraps the web bundle → inherits apiClient). No un-updatable deployed external caller exists → clean cut is safe, with one coordinated header swap in the amic-vault repo before its tools are next run.
11. CORS preflight allow-list does not include the new header
   EV: apps/api/src/server.js:415-427 CORS_BASE_HEADERS access-control-allow-headers (:417) currently lists authorization, content-type, x-lawos-permission-context, x-lawos-tenant-id/actor-id/actor-role/hrx-scopes, x-lawos-hrx-step-up — x-lawos-vault-bridge-token must be appended or browser preflights will block any future UI/browser-based bridge caller.

-- CHANGE PLAN --
1. FILE: apps/api/src/matter-runtime-context.js
   CHANGE: (a) Export the new surface constants near the top: export const VAULT_BRIDGE_TOKEN_HEADER = "x-lawos-vault-bridge-token"; and export const MATTER_VAULT_BRIDGE_ROUTES = Object.freeze(["GET /api/matters/vault-bridge/status", "GET /api/matters/vault-bridge/matter-lookup", "POST /api/matters/vault-bridge/upload-preflight", "POST /api/matters/vault-bridge/clients/upsert", "POST /api/matters/vault-bridge/matters/upsert"]) — string values must mirror the dispatch at :3225-3239 and routes/matters.js:9-16. (b) Add function vaultBridgeHeaderToken(headers = {}) that reads headers[VAULT_BRIDGE_TOKEN_HEADER] with a case-insensitive fallback scan (node lowercases incoming HTTP header names, but handlers are also invoked directly in unit contexts), returning the trimmed value or null. (c) In validateVaultBridgeAuth (:451-460) replace bearerToken(headers) at :456 with vaultBridgeHeaderToken(headers); keep the 503-when-unconfigured branch (:453-454), the 403-on-mismatch branch (:457), and the timing-safe tokenMatches compare (:137-146) exactly as-is. (d) Delete the now-orphaned bearerToken function (:130-135) — verified single consumer — so no future code silently reverts to Authorization.
   RISK: low — auth source swap only; taxonomy, handlers, and timing-safe compare untouched
2. FILE: apps/api/src/server.js
   CHANGE: (a) :40 add parsePermissionContext to the permission-gate import (present pre-gate at 6c83a2d5c^, dropped at HEAD). (b) Import MATTER_VAULT_BRIDGE_ROUTES from ./matter-runtime-context.js in the import block ending :47. (c) Insert the gate exemption AFTER the isAuthPath block (:772-777) and BEFORE the session gate (:779): if (MATTER_VAULT_BRIDGE_ROUTES.includes(`${req.method} ${pathname}`)) { const body = hasJsonRequestBody(req.method) ? await readRequestBody(req) : {}; const context = parsePermissionContext(req.headers[PERMISSION_CONTEXT_HEADER]); const result = await handleMatterApiRequest({ pathname, method: req.method, query, body, headers: req.headers, context, requestId, runtime: matterRuntime }); sendJson(req, res, result.status, result.body); return; } — MUST match method+path exactly (not startsWith) so unknown /api/matters/vault-bridge/* subpaths and wrong-method hits still fall through to the session gate, keeping the unauthenticated surface to exactly the 5 ratified routes. pathname is already trailing-slash-normalized at :706. This restores the pre-gate context model (6c83a2d5c^ server.js:698) for bridge paths only: permission scoping via x-lawos-permission-context, real authentication via the bridge header inside validateVaultBridgeAuth. (d) :417-425 append "x-lawos-vault-bridge-token" to the access-control-allow-headers list. (e) Leave the isMatterPath dispatch (:858-873) untouched — bridge requests return before it.
   RISK: medium — this is the security-sensitive hunk; exact-match set + fail-closed 503/403 inside the handlers is the mitigation; body is read pre-auth for POST bridge routes (same as pre-gate behavior)
3. FILE: apps/web/src/data/apiClient.js
   CHANGE: At :2864, :2916, :2979 replace `if (bridgeToken) headers.Authorization = \`Bearer ${bridgeToken}\`;` with `if (bridgeToken) headers["x-lawos-vault-bridge-token"] = bridgeToken;`. KEEP the bridgeToken parameter names (ui-regression.test.mjs:889 asserts the literal appears in apiClient source; :861 forbids it in VaultSurface). Do NOT touch sessionAuthorizedHeaders/apiFetch (:247-267): the session Authorization keeps flowing on every bridge call, and the custom bridge header now passes through untouched (only the 4 x-lawos identity headers are stripped at :249-256) — this ends the silent clobber where setHeader (:160-163) overwrote the caller's Authorization with the session token. After the change the three call sites send: (1) fetchVaultBridgeStatus — session Authorization + x-lawos-permission-context, NO bridge header (UI holds no token) → 503 REQUIRED unconfigured / 403 BLOCKED configured, both already-rendered guarded states; (2) fetchVaultMatterLookup — same headers + tenant/permission_ref/audit_hint_ref/q query params; (3) fetchVaultUploadPreflight — same headers + content-type + JSON payload. VaultSurface.jsx:846/:890/:912 and MatterVaultPanel.jsx:263/:333 need ZERO edits.
   RISK: low — dead-parameter transport swap; UI behavior states unchanged
4. FILE: apps/api/test/matter-vault-bridge-api.test.js
   CHANGE: (a) Replace authHeaders (:35-37) with bridgeHeaders(token = TOKEN) returning { "x-lawos-vault-bridge-token": token } and update all 16 usages (:138,:146,:153,:167,:175,:185,:197,:218,:227,:233,:239,:269,:282,:288,:295,:320). (b) Test 1 (:119-126) stays byte-identical — it is the required 'no header + no config → 503 MATTER_VAULT_BRIDGE_REQUIRED' case. (c) Test 2: KEEP the exact title 'Vault bridge rejects missing bearer auth and invalid canonical upsert payloads' (scripts/validate-lcx-vltui-bridge-contract.mjs:127 greps for it) or update that validator string in the same commit; its first assertion (:131-134, no headers → 403 BLOCKED) now exercises missing-header-with-config. (d) Test 6 (:316-325) becomes wrong-value-in-new-header → 403 BLOCKED. (e) ADD one new test 'Vault bridge ignores session Authorization without the bridge header': set LAWOS_VAULT_BRIDGE_TOKEN, POST /api/auth/login with a roster credential (copy the login helper pattern from apps/api/test/session-auth-api.test.js:48; roster seed docs/reorganization/client-matter-os/matter-vault-r4/launch/matter-vault-user-registration-seed.json, password form local-dev-only:<email>), then GET /api/matters/vault-bridge/status with only { authorization: `Bearer ${session_token}` } → assert 403 and safe_error_codes ["MATTER_VAULT_BRIDGE_BLOCKED"]. Acceptance: suite goes from 0/6 (verified failing at HEAD) to 7/7.
   RISK: low — but the title marker coupling to validate-lcx-vltui-bridge-contract.mjs:127 will break CI if renamed without the lockstep edit
5. FILE: scripts/run-current-matter-codes-production-bridge-upsert.mjs
   CHANGE: Change authHeaders (:48-50) to return { "x-lawos-vault-bridge-token": token }; consumed at :242 and used for both upsert loops (:245, :264). Token resolution (:216, env-or-Lambda via scripts/lib/aws-sso-lambda-env.mjs) is transport-agnostic — unchanged. NOTE in the script header comment that it targets deployments at/after the dedicated-header contract; running it against the current pre-gate Lambda (3d7d9da36, Bearer-only) will 403 after the client-side swap, so it must only run post-redeploy.
   RISK: medium — deployment-ordering dependent; also the readback GET /api/matters at :149 sends only permissionHeaders() and will 401 against any post-gate deployment regardless of this change (pre-existing, out of bridge scope — flagged in open_issues)
6. FILE: scripts/run-lcx-vltui-production-smoke.mjs
   CHANGE: Change bridgeAuthHeaders (:116-119) to return { "x-lawos-vault-bridge-token": BRIDGE_TOKEN }; the merged bridgePermissionHeaders (:333-336) and the 5 bridge checks (:309-351) then carry the new header automatically. All BRIDGE_TOKEN resolution/BLOCKED reporting (:25-45,:216-221) is unchanged. Same deployment-ordering note as the upsert script; same pre-existing caveat that the non-bridge smoke checks (:265 profile, :274-276 crm, :290-307 vault documents) send only x-lawos-permission-context and will 401 once a post-gate commit is deployed.
   RISK: medium — same ordering constraint; smoke must run with the script version matching the deployed commit
7. FILE: apps/api/src/routes/matters.js
   CHANGE: No functional change required (MATTER_VAULT_MATTER_ROUTES :1-22 is a documentation list with the bridge routes at :9-11,:15-16 and has no server.js consumer). Optional: add a comment pointing to MATTER_VAULT_BRIDGE_ROUTES in matter-runtime-context.js as the authoritative gate-exemption list, so the two lists don't drift.
   RISK: none
8. FILE: docs/lazycodex/lcx-vault-app-current-ui-source-register-2026-06-29.json
   CHANGE: Update the five Law-side bridge_endpoints auth fields (:76,:83,:90,:97,:104) from 'Authorization: Bearer <LAWOS_VAULT_BRIDGE_TOKEN>' to 'x-lawos-vault-bridge-token: <LAWOS_VAULT_BRIDGE_TOKEN>'. Leave the Vault-app-owned /v1/integrations/* entries untouched.
   RISK: low
9. FILE: docs/lazycodex/lcx-vltui-01-vault-bridge-contract-2026-06-29.json
   CHANGE: Update the transport wording only: negative case id/description 'law_bridge_bearer_missing_or_wrong' (:158) → header-based wording (e.g. 'law_bridge_header_missing_or_wrong'); statuses stay 503 (:153, MATTER_VAULT_BRIDGE_REQUIRED) and 403 (:160, MATTER_VAULT_BRIDGE_BLOCKED). Constraint: the fixture must NOT contain the literal 'Bearer ' (validate-lcx-vltui-bridge-contract.mjs:122 forbids it) — the new wording naturally satisfies this. If validate-lcx-vltui-bridge-contract.mjs pins the old negative-case id, update it in lockstep. Do NOT rewrite dated receipts/evidence under docs/lazycodex/evidence/** or docs/desktop/** — they are historical proof; add one new dated ADR/receipt documenting the header cut instead.
   RISK: low — validator lockstep required
10. FILE: /Users/jws/Projects/amic-vault/tools/migration/* (EXTERNAL REPO — coordinated follow-up, not part of this change set)
   CHANGE: Swap 'authorization: Bearer ${args.matterAppApiToken}' (matter-app-canonical-upsert-sync.mjs:215; bridge paths declared at :17-19) to the x-lawos-vault-bridge-token header in matter-app-canonical-upsert-sync.mjs, matter-app-identity-preflight.mjs, matter-app-identity-preflight.spec.mjs, matter-app-migration-db-linkage-closeout.mjs, onedrive-client-matter-write.mjs. These are operator-run migration tools, not deployed services, so the swap only needs to land before their next invocation against a post-cut deployment.
   RISK: medium — out-of-repo coordination; if forgotten, next migration run 403s loudly (fail-closed, no data risk)

-- AFFECTED TESTS/GATES --
* node --test apps/api/test/matter-vault-bridge-api.test.js — primary gate; verified 0/6 failing at HEAD 169484912 (all 401 at session gate); must become 7/7 after change
* node --test apps/api/test/session-auth-api.test.js — regression guard that the session gate itself is unchanged for non-bridge routes
* node --test apps/web/test/ui-regression.test.mjs — :861 (VaultSurface must NOT contain bridgeToken), :889 (apiClient MUST contain bridgeToken), :885-887 (bridge paths present in apiClient); all stay green if the param name is kept
* node scripts/validate-lcx-vltui-bridge-contract.mjs — :127 exact test-title marker, :122 forbidden 'Bearer ' marker in contract fixture, :49-53 route strings
* node scripts/validate-lcx-vltui-matter-picker.mjs (:49-65), scripts/validate-lcx-vltui-vault-status-panel.mjs (:35-44), scripts/validate-lcx-vltui-action-boundaries.mjs (:62), scripts/validate-lcx-vltui-upload-preflight.mjs, scripts/validate-lcx-vltui-session-principal.mjs (:108)
* node --test scripts/test/aws-sso-lambda-env.test.mjs — unchanged behavior, re-run as smoke
* Playwright proof gates that mock bridge routes (auth-agnostic mocks but expected statuses embedded): scripts/run-lcx-vltui-vault-status-panel-proof.mjs, run-lcx-vltui-matter-picker-proof.mjs, run-lcx-vltui-upload-preflight-proof.mjs, run-lcx-vltui-browser-qa.mjs, run-lcx-vltui-session-principal-proof.mjs, run-lcx-vltui-matter-document-workspace-proof.mjs, run-lcx-vltui-matter-sections-proof.mjs, run-lcx-vltui-profile-audit-proof.mjs, run-lcx-full-{baseline,guarded-ui,approval,provider}-browser-proof.mjs (guarded-ui :29 expects status→403 — re-verify against local env where LAWOS_VAULT_BRIDGE_TOKEN may be unset → 503)
* npm run lcx:vltui:production-smoke (scripts/run-lcx-vltui-production-smoke.mjs) — run ONLY after the Lambda matter-lawos-api-prod is redeployed with the new contract; script version must match deployed commit
* node --test packages/matter/test/runtime-services.test.js apps/api/test/cmp-r4-g4-matter.test.js — the historical 26/26 bundle cited in docs/desktop release notes :43 that includes the bridge suite

-- RISKS --
! Deployment-ordering cliff: production Lambda (matter-lawos-api-prod, CloudFront d2mthcc8vp3cr2.cloudfront.net) is at pre-gate commit 3d7d9da36 accepting Authorization-bearer only. Old server + new scripts → 403; new server + old scripts/amic-vault tools → 403. Land server + in-repo scripts + tests in one commit, redeploy Lambda, then run the production smoke with that same commit; swap amic-vault tools before their next run. (Deploying HEAD-without-fix would 401 the whole production bridge — the fix is also the unblocker for any Lambda redeploy.)
! Gate-exemption surface: exempting by prefix (startsWith) instead of exact method+path would let unknown /api/matters/vault-bridge/* requests reach matter fall-through handlers unauthenticated. The plan mandates exact `${method} ${pathname}` membership against a frozen 5-entry list mirroring matter-runtime-context.js:3225-3239.
! Bridge lookup/upload-preflight permission context reverts to client-supplied x-lawos-permission-context on bridge paths (pre-gate trust model, 6c83a2d5c^ server.js:698): the bridge token is the sole authentication; the permission context only scopes tenant/rules. This is exactly what tests (:39-51) and production smoke (:333-336) assume, but it is a deliberate machine-surface trust decision worth one line in the ADR.
! validate-lcx-vltui-bridge-contract.mjs:127 hard-couples to the test title containing 'bearer' — renaming the test without the lockstep validator edit breaks the gate; conversely keeping the stale title is cosmetically wrong. Pick one and do it atomically.
! ui-regression.test.mjs:889 requires the literal 'bridgeToken' in apiClient.js — removing the now-mostly-unused parameter (instead of re-pointing it at the new header) breaks that gate.
! POST bridge routes now read the request body before authentication (readRequestBody pre-gate) — same exposure as the pre-2026-07-03 server; bounded by node:http defaults, acceptable for a token-gated machine surface.
! Header name canonicalization: node lowercases incoming header names, but direct handler invocation (unit tests, other runtimes) may pass mixed case — vaultBridgeHeaderToken must handle both, mirroring the existing authorization ?? Authorization pattern at matter-runtime-context.js:131.

-- OPEN ISSUES --
? Post-gate production scripts beyond the bridge: run-current-matter-codes-production-bridge-upsert.mjs:149 (readback GET /api/matters with only permissionHeaders) and run-lcx-vltui-production-smoke.mjs non-bridge checks (:265 profile, :274-276 crm, :290-307 vault documents) will 401 against any deployment that includes the session gate. They need session-login support (POST /api/auth/login with a roster credential) — separate task, not part of the bridge header cut.
? run-lcx-full-guarded-ui-browser-proof.mjs:29 expects GET /api/matters/vault-bridge/status → 403 in the guarded browser proof; in a local env without LAWOS_VAULT_BRIDGE_TOKEN the post-change status is 503. Verify which env that proof runs under and adjust the expectation if needed.
? amic-vault repo update (5 migration tool files) is out-of-repo — needs its own commit/receipt in /Users/jws/Projects/amic-vault; this repo's change should reference it in the ADR so the cut is traceable from both sides.
? Header naming: the ratified working name x-lawos-vault-bridge-token fits the existing x-lawos-* namespace (cf. server.js:417-425); if Codex prefers a value format with a scheme (e.g. token=...), the tests/scripts/docs must all change together — the plan specifies raw trimmed token value, no prefix.
? MATTER_BOUNDED_CONTEXT endpoint strings (matter-runtime-context.js:84-88) and routes/matters.js:9-16 duplicate the route list a third time; the new MATTER_VAULT_BRIDGE_ROUTES constant makes it four. A follow-up could derive all from one source, but do not refactor inside this security cut.


############ tenant-b-seed ############
VERDICT: confirmed — Independently verified in /Users/jws/lawos-diag-wt @ 169484912. (1) Session pinning: apps/api/src/session-auth.js anchors exact — LAWOS_RUNTIME_TENANT_IDS 43-59 with exactly 15 entries; subjectsFromSeed 152-164 pins roleAssignment to seed.tenant_id; login user lookup at 368, deriveServerPrincipal 376-381 with factory trustedTenantId; verifyToken tenant check 315-317 before user lookup 319-322, re-derivation 326-331; packages/runtime-auth/src/principal.js:48 sets tenant_id=trustedTenantId; membership.js returns tenant_membership_missing, so a seed-only change indeed cannot mint a tenant_b princ
SUMMARY: Ratified design: add QA account qa.tenant-b@amic.kr with home tenant "tenant_b_qa_synthetic" (a NEW dedicated tenant, deliberately NOT added to LAWOS_RUNTIME_TENANT_IDS) plus 4 surgical session-auth.js changes. A seed-only change CANNOT produce a tenant-b principal: the session pipeline pins every principal to seed.tenant_id at three points — subjectsFromSeed (session-auth.js:152-164 builds each subject's memberships pinned to seed.tenant_id, ignoring the user's own tenant_memberships), login (session-auth.js:376-381 passes factory trustedTenantId into deriveServerPrincipal, which sets principal.tenant_id=trustedTenantId at packages/runtime-auth/src/principal.js:48), and verifyToken (session-auth.js:315-317 rejects payload.tenant_id !== trustedTenantId with 403). LAWOS_RUNTIME_TENANT_IDS (session-auth.js:43-59; 15 entries) does NOT gate login/verify — its ONLY consumer is permissionContextFromPrincipal (session-auth.js:121), where it becomes principal.tenant_ids and (proven live on port 4292) grants every signed-in account cross-tenant ALIAS access into all 15 tenants via trust-context.js:41-49: an amic session read 25 matters in tenant_rp05_synthetic (200), while out-of-list tenant_b_qa_synthetic got 403 MATTER_UNAUTHORIZED_OMISSION (cross_tenant_deny). Therefore the QA principal must get tenant_ids scoped to [own tenant], and tenant_b must stay OUT of the constant — otherwise the QA account would alias into every tenant (deny tests vacuous) and every amic account could read tenant_b. Reusing one of the 14 existing synthetic tenants was rejected: ALL 14 lack HRX seeds (HRX_DEFAULT_SEED_TENANT_IDS at hrx-runtime-context.js:81-83 seeds only tenant_amic_matter_vault and "tenant-a"), but each of the 14 is in-list and hence alias-authorized for all principals, so matter cross-tenant DENY could never be demonstrated against them — and tenant_rp05_synthetic actually hosts the 25-record matter fixture (matter-runtime-context.js:110). HRX isolation then needs zero data changes: server.js:793-803 spreads req.headers THEN overwrites x-lawos-tenant-id with principal.tenant_id (client-forged header proven ignored live), HRX reads only that header (middleware/tenant-context.js:7-23), query tenant_id is hard-rejected 400 (hrx-authz.js:58-63, proven live), and tenant_b has no HRX rows so GET /api/hrx/employees returns [] for an elevated-read role. Blast radius when one account is added: 1 validator script, 1 receipt doc, 1 runtime-auth test, 1 desktop-lambda test, the role registry, and session-auth.js; the 9-count HRX member-roster gates (hrx-runtime-api.test.js:115/143/185, validate-lcx-hrx-sft-roster-source.mjs:37) and session-auth-api.test.js:177 stay untouched IF the QA account is kept out of hrx-member-roster-source-of-truth.json and out of LAWOS_INTERNAL_ROLE_ASSIGNMENTS (use a separate const like the DESKTOP_QA_ASSIGNMENT precedent).

-- INVENTORY --
1. Seed schema, full per-account entry shape (10 users, all tenant_amic_matter_vault): user_id, email, display_name, english_name, source_title, status:'active', registration_state:'registered_seed', mfa_required:true, highest_privilege:false, privilege_rank, role_ids[], group_ids[], scopes[], assurance_level:'mfa', local_dev:{synthetic_only:true, synthetic_token:'local-dev-only:<email>'}, tenant_memberships:[{tenant_id, role_ids, group_ids, scopes}]
   EV: docs/reorganization/client-matter-os/matter-vault-r4/launch/matter-vault-user-registration-seed.json:5 (tenant_id), :12 (source.account_count 10), :22-32 (role_rank incl matter_vault_user:100), :39-66 (ytkim entry), :327-354 (QA precedent user_amic_matter_desktop_qa)
2. Seed loading: packaged copy next to src preferred over repo path (desktop builds bundle it); MATTER_VAULT_REGISTERED_TENANT_ID = seed.tenant_id; listRegisteredAccounts maps ALL users with no tenant filtering
   EV: apps/api/src/matter-vault-account-registry.js:6-13 (path preference), :17 (tenant const), :41-48 (list/findByEmail unfiltered)
3. Role registry: exactly 9 internal assignments in LAWOS_INTERNAL_ROLE_ASSIGNMENTS; DESKTOP_QA_ASSIGNMENT is a SEPARATE const merged only into ASSIGNMENTS_BY_USER_ID — the precedent that keeps session-auth-api.test.js:177 (length===9) green; unmapped users fall back to lawos_staff self-service; lawos_hr_operations (tryoon: role_ids [lawos_hr,lawos_staff], hrx_scopes HRX_HR_SCOPES) is the profile to mirror because lawos_hr is in HRX_ELEVATED_READ_ROLES
   EV: apps/api/src/lawos-role-registry.js:5-52 (HRX scope tiers), :71-144 (9 assignments), :128-135 (lawos_hr_operations), :146-157 (DESKTOP_QA + map), :169-178 (fallbackAssignment), :184-191 (resolve, tenant pinned to passed tenantId)
4. Session single-tenant pinning — the three gates a tenant-b account hits today: subjectsFromSeed pins the single membership to seed.tenant_id; login derives with factory trustedTenantId (default MATTER_VAULT_REGISTERED_TENANT_ID); verifyToken rejects payload.tenant_id !== trustedTenantId (403 AUTH_SESSION_TENANT_DENIED) BEFORE user lookup; permissionContextFromPrincipal gives EVERY principal tenant_ids = full 15-tenant LAWOS_RUNTIME_TENANT_IDS list; no test anywhere pins AUTH_SESSION_TENANT_DENIED (repo grep hits only the source line)
   EV: apps/api/src/session-auth.js:43-59 (list: tenant_amic_matter_vault, tenant_rp04/rp05_synthetic, tenant_cmp_g6..g12_synthetic, tenant_sf_b_w06/w07_synthetic, tenant_outlook_addin_test, tenant_upl_c09_c12_outlook, matter-runtime-tenant), :117-128 (tenant_ids injection at :121), :152-164 (subjectsFromSeed), :172 (default), :269-284 (token embeds principal.tenant_id), :312-331 (verifyToken order), :368-385 (login)
5. deriveServerPrincipal sets principal.tenant_id = trustedTenantId and requires a membership for exactly that tenant; forged trust headers/query keys throw — applies only to the internal synthetic-auth sub-request, not business-route headers
   EV: packages/runtime-auth/src/principal.js:4-27 (forged-trust guard), :29-56 (:36-38 membership check via membership.js:1-8 'tenant_membership_missing' 403; :48 tenant_id=trustedTenantId)
6. Server session gate + downstream tenant injection: all business routes require a signed session; requestHeaders() spreads req.headers THEN overwrites x-lawos-tenant-id=principal.tenant_id, x-lawos-actor-id, x-lawos-actor-role, x-lawos-hrx-scopes=principal.scopes, x-lawos-session-bound:'signed' — client-supplied tenant header can never survive; sessionAuth constructed with defaults
   EV: apps/api/src/server.js:779-791 (gate), :793-803 (injection), :805-817 (HRX authz wiring), :1110 and :1225 (createApiSessionAuth defaults)
7. HRX tenant/authz chain: query tenant_id/actor_id hard-rejected 400 HRX_QUERY_CONTEXT_FORBIDDEN; tenant read ONLY from x-lawos-tenant-id header; elevated read is ROLE-based via HRX_ELEVATED_READ_ROLES={security_admin,hr_admin,people_ops,hr_manager,hr_reviewer,lawos_admin,lawos_hr}; GET /api/hrx/employees: elevated role → all rows for the tenant, else self-service link filter
   EV: apps/api/src/middleware/hrx-authz.js:58-63 (query forbid), :65 + apps/api/src/middleware/tenant-context.js:1-23 (header tenant); apps/api/src/hrx-runtime-context.js:92 (role set), :569-574 (actorHasElevatedHrxRead), :1936-1950 (employees handler)
8. HRX seed tenants — of the 15 LAWOS_RUNTIME_TENANT_IDS entries, ONLY tenant_amic_matter_vault has HRX data; the other 14 have zero HRX rows (seed list is [tenant_amic_matter_vault,'tenant-a'], and 'tenant-a' is not in the runtime list); employee rows come from the 9-member hrx-member-roster-source-of-truth.json joined to accounts — an account absent from that roster (matter.desktop.qa precedent) produces NO employee row
   EV: apps/api/src/hrx-runtime-context.js:78-83 (HRX_DEFAULT_SEED_TENANT_IDS), :673-680 (resolveSeedTenantIds defaults), :1791-1793 (seedHrxDurableRuntimeStore called optionless from server.js:191), :1809-1815 (in-memory same), :222-243 (registeredRosterMembers roster-driven)
9. Matter tenant + cross-tenant alias mechanics: matter routes take client-supplied query.tenant_id but gate via evaluatePermission whose FIRST step is validateTenantBoundary; principal.tenant_id != resource tenant is ALLOWED iff resource tenant ∈ principal.tenant_ids (tenant_alias_authorized), else cross_tenant_deny; validateCommonQuery additionally requires permission_ref and audit_hint_ref; matter fixture data (25 records) lives in tenant_rp05_synthetic, NOT tenant_amic (0 rows)
   EV: apps/api/src/matter-runtime-context.js:110 (DEFAULT_TENANT tenant_rp05_synthetic), :789-796 (validateCommonQuery), :1002-1016 (routeGate), :1151-1173 (handleMatterList); packages/authz/src/evaluate.js:4-7; packages/authz/src/trust-context.js:40-55 (alias allow / cross_tenant_deny)
10. BOOT-AND-PROVE on port 4292 (LAWOS_API_PORT=4292 node apps/api/src/server.js, PID 92325; killed by PID, /api/health confirmed unreachable after): (a) login jwsuh → 200, session.tenant_id=tenant_amic_matter_vault, role_ids=[lawos_admin,lawos_partner,lawos_attorney,security_admin] proving the ROLE REGISTRY (not seed role_ids) drives session principals; (b) GET /api/hrx/employees → 200, 9 rows all tenant_amic; (c) same with forged 'x-lawos-tenant-id: tenant_b_forged' → identical 200/9/amic (override proven); (d) ?tenant_id query → 400 HRX_QUERY_CONTEXT_FORBIDDEN; (e) GET /api/matters?tenant_id=tenant_rp05_synthetic&permission_ref&audit_hint_ref → 200, 25 items (in-list alias); (f) tenant_id=tenant_b_qa_synthetic → 403 MATTER_UNAUTHORIZED_OMISSION, ui_state=denied (out-of-list cross_tenant_deny — the exact deny the QA tests need is already live); (g) amic home-tenant matters → 200, 0 items; (h) login qa.tenant-b@amic.kr before roster change → 401 AUTH_CREDENTIAL_INVALID (rejecting check = roster email lookup)
   EV: [직접 재실행] live curl transcript this session against worktree commit 169484912; rejecting code paths: session-auth.js:368-371 (unknown email), trust-context.js:51-54 (deny), server.js:797 (override)
11. Roster-count assertion census (every file that pins roster contents/count, re-grepped this worktree): seed validator expectedEmails of all 10 + users.length + membership-tenant==seed.tenant_id + token format + receipt phrase 'Registered account count: 10'; runtime-auth seed test iterates ALL users with trustedTenantId=seed.tenant_id; desktop lambda test registered_account_count===10 (source seed.users.length); HRX 9-counts are MEMBER-ROSTER counts, not account counts (current: 10 accounts vs 9 employees); session-auth test pins role-assignment array length 9; admin-security test has NO count assertions; web app/tests have ZERO amic.kr or roster references; desktop build bundles the seed; protected-reset lib names jwsuh (protected) and matter.desktop.qa (QA reset) only
   EV: scripts/validate-matter-vault-user-registration-seed.mjs:16-27,55,63,85,86-90,102-116,132; packages/runtime-auth/test/matter-vault-user-registration-seed.test.js:29-47,49-64; apps/api/test/matter-temp-desktop-runtime-lambda.test.js:110 (+ src matter-temp-desktop-runtime-lambda.mjs:933,970); apps/api/test/hrx-runtime-api.test.js:115,143,185; scripts/validate-lcx-hrx-sft-roster-source.mjs:37; apps/api/test/session-auth-api.test.js:177; apps/api/test/admin-security-api.test.js (grep verified, presence-only); apps/web grep empty; scripts/build-matter-desktop-mac.mjs:151-156; scripts/lib/protected-reset-accounts.mjs:1-2; docs .../matter-vault-user-registration-receipt.md:12; package.json:220 (validator npm gate), :153 (roster gate)

-- CHANGE PLAN --
1. FILE: docs/reorganization/client-matter-os/matter-vault-r4/launch/matter-vault-user-registration-seed.json
   CHANGE: Append to users[] (after user_amic_matter_desktop_qa, line 354) the exact entry: { "user_id": "user_qa_tenant_b", "email": "qa.tenant-b@amic.kr", "display_name": "Tenant-B QA", "english_name": "Tenant B QA", "source_title": "QA 전용 계정 (교차 테넌트 격리 검증)", "status": "active", "registration_state": "registered_seed", "mfa_required": true, "highest_privilege": false, "privilege_rank": 100, "role_ids": ["matter_vault_user"], "group_ids": ["group_qa_isolation"], "scopes": ["matter.read", "vault.read"], "assurance_level": "mfa", "local_dev": { "synthetic_only": true, "synthetic_token": "local-dev-only:qa.tenant-b@amic.kr" }, "tenant_memberships": [ { "tenant_id": "tenant_b_qa_synthetic", "role_ids": ["matter_vault_user"], "group_ids": ["group_qa_isolation"], "scopes": ["matter.read", "vault.read"] } ] }. Keep source.account_count at 10 (it describes the imported workbook rows; nothing ties it to users.length). role_rank needs no addition (matter_vault_user already ranks 100, seed line 31). tenant_memberships[0].tenant_id is the home-tenant source of truth consumed by the session-auth change below.
   RISK: Validator line 87 currently REQUIRES membership tenant == seed.tenant_id — seed and validator must land together or the gate fails; credential format 'local-dev-only:<email>' is enforced at validator line 85 and consumed verbatim by subjectsFromSeed
2. FILE: apps/api/src/lawos-role-registry.js
   CHANGE: Mirror the DESKTOP_QA_ASSIGNMENT pattern (lines 146-157): add const TENANT_B_QA_ASSIGNMENT = assignment({ user_id: "user_qa_tenant_b", role_profile_id: "lawos_qa_tenant_b", role_ids: ["lawos_hr", "lawos_staff"], group_ids: ["group_qa_isolation"], scopes: ["matter.read", "vault.read"], hrx_scopes: HRX_HR_SCOPES }); include it in the ASSIGNMENTS_BY_USER_ID map (155-157). Do NOT append to LAWOS_INTERNAL_ROLE_ASSIGNMENTS (would break session-auth-api.test.js:177 length===9). Rationale: lawos_hr is in HRX_ELEVATED_READ_ROLES (hrx-runtime-context.js:92), so GET /api/hrx/employees returns the plain tenant row list (empty for tenant_b) instead of a self-service-filtered response — 'zero rows' is then provably tenant isolation; HRX_HR_SCOPES mirrors the reviewed lawos_hr_operations profile (128-135) and includes hrx.employee.read required by the route policy.
   RISK: If the assignment is omitted, fallbackAssignment (169-178) silently yields lawos_staff/self-service — tests still see empty lists but for the wrong reason (scope filtering, not tenant isolation)
3. FILE: apps/api/src/session-auth.js
   CHANGE: Four changes; LAWOS_RUNTIME_TENANT_IDS (43-59) UNCHANGED: (1) add helper homeTenantIdForUser(user, seed) => user.tenant_memberships?.[0]?.tenant_id ?? seed.tenant_id; (2) subjectsFromSeed (152-164): resolve roleAssignment with { tenantId: homeTenantIdForUser(user, seed) } instead of seed.tenant_id so the local-dev provider carries the QA subject's tenant_b membership; (3) login (376-381) and verifyToken's re-derivation (326-331): pass trustedTenantId: homeTenantIdForUser(user, seed) (user already resolved at 368 / 319); in verifyToken MOVE the tenant check (currently 315-317, before user lookup) to AFTER the user lookup at 319-322 and compare payload.tenant_id !== homeTenantIdForUser(user, seed), keeping 403 AUTH_SESSION_TENANT_DENIED (signature check at 300-304 already authenticated the token, so reordering stays fail-closed; no test pins this error code); (4) permissionContextFromPrincipal (117-128): tenant_ids: principal.tenant_id === MATTER_VAULT_REGISTERED_TENANT_ID ? LAWOS_RUNTIME_TENANT_IDS : Object.freeze([principal.tenant_id]) — amic-homed principals keep the 15-tenant alias list (the web app depends on it: matter data lives in tenant_rp05_synthetic), the QA principal is confined to tenant_b_qa_synthetic.
   RISK: Highest-risk file: every business route flows through verifyToken. The tenant_ids scoping is what makes the cross-tenant DENY real — if Codex instead adds tenant_b_qa_synthetic to LAWOS_RUNTIME_TENANT_IDS, every account gains alias read into tenant_b AND the QA account gains read into ALL 15 tenants, inverting the isolation property (proven live: in-list tenant → 200/25 items, out-of-list → 403)
4. FILE: scripts/validate-matter-vault-user-registration-seed.mjs
   CHANGE: (a) add "qa.tenant-b@amic.kr" to expectedEmails (16-27) — line 63 count follows to 11; (b) relax the membership-tenant assert (86-87) to allow the declared QA exception, e.g. allowedMembershipTenants = new Set([seed.tenant_id, "tenant_b_qa_synthetic"]) or special-case user_qa_tenant_b; (c) update required receipt phrase (104) to "Registered account count: 11"; (d) update the final console.log (132) to registered_account_count: 11. Lines 79-96 need no change (QA entry satisfies active/mfa/token-format/mirror/rank<1000/no-super-admin).
   RISK: Gate runs via npm run matter-vault:user-registration:validate (package.json:220); the phrase check couples it to the receipt file — ship both together
5. FILE: docs/reorganization/client-matter-os/matter-vault-r4/launch/matter-vault-user-registration-receipt.md
   CHANGE: Line 12: 'Registered account count: 10' → 'Registered account count: 11'; add a line documenting qa.tenant-b@amic.kr as a synthetic-only cross-tenant-isolation QA account (home tenant tenant_b_qa_synthetic; not part of the M365 workbook import; API-level QA only).
   RISK: Low; only the validator phrase list reads it
6. FILE: packages/runtime-auth/test/matter-vault-user-registration-seed.test.js
   CHANGE: First test (29-47) derives a principal for EVERY seed user with trustedTenantId: seed.tenant_id and asserts principal.tenant_id === seed.tenant_id — it WILL FAIL for the QA entry (tenant_membership_missing → principal.ok false). Change to per-user home tenant: const homeTenant = user.tenant_memberships[0].tenant_id; deriveServerPrincipal({ ..., trustedTenantId: homeTenant }); assert principal.tenant_id === homeTenant. Tests 2 and 3 (49-74) pass unchanged with the designed entry.
   RISK: Low; pure test-shape update mirroring the runtime change
7. FILE: apps/api/test/matter-temp-desktop-runtime-lambda.test.js
   CHANGE: Line 110: assert.equal(body.registered_account_count, 10) → 11 (source: apps/api/src/matter-temp-desktop-runtime-lambda.mjs:933 seed.users.length; also surfaces at :970).
   RISK: Behavioral note: the QA account becomes a login-capable row on the desktop temp runtime and ships in packaged desktop builds (build-matter-desktop-mac.mjs:151-156 copies the seed to the packaged path preferred by matter-vault-account-registry.js:13) — acceptable for a synthetic-only account; state it in the commit message
8. FILE: apps/api/test/tenant-isolation-api.test.js (NEW)
   CHANGE: Isolation-test usage pattern (startApiServer({port:0}) like session-auth-api.test.js): (1) POST /api/auth/login {email:"qa.tenant-b@amic.kr", password:"local-dev-only:qa.tenant-b@amic.kr"} → 200; assert session.tenant_id === "tenant_b_qa_synthetic" and role_ids includes "lawos_hr"; (2) HRX empty-tenant proof: GET /api/hrx/employees with only the QA Bearer (server injects tenant/actor/scope headers) → 200 and body.employees.length === 0, and assert permission_summary is ABSENT (pins the elevated-read path) — zero tenant_amic rows leak because injected x-lawos-tenant-id is tenant_b and HRX seeds exist only for tenant_amic_matter_vault/tenant-a; (3) matter cross-tenant deny: GET /api/matters?tenant_id=tenant_rp05_synthetic&permission_ref=perm:qa-isolation&audit_hint_ref=audit:qa-isolation with QA Bearer → 403 safe_error_codes ["MATTER_UNAUTHORIZED_OMISSION"] (rp05 hosts the 25 matter fixtures — the amic home tenant has 0, so rp05 is the meaningful deny target; also assert 403 for tenant_id=tenant_amic_matter_vault); (4) reverse deny: login jwsuh/ytkim → GET /api/matters?tenant_id=tenant_b_qa_synthetic&permission_ref&audit_hint_ref → 403 (tenant_b not in LAWOS_RUNTIME_TENANT_IDS — already true today, proven live); (5) regression guard: amic session still gets 200/9 rows from /api/hrx/employees and 200-with-items from /api/matters?tenant_id=tenant_rp05_synthetic (alias list preserved for amic principals).
   RISK: Assertion (2) depends on the lawos_hr elevated-read role from the registry entry; if that entry is dropped the response gains permission_summary.self_service_filtered=true and the test correctly fails loudly
9. FILE: DO-NOT-TOUCH guard rails
   CHANGE: Leave unchanged: docs/.../hrx-member-roster-source-of-truth.json, scripts/validate-lcx-hrx-sft-roster-source.mjs (asserts 9), apps/api/test/hrx-runtime-api.test.js:115/143/185 (9-count member-roster gates), LAWOS_INTERNAL_ROLE_ASSIGNMENTS array (session-auth-api.test.js:177 asserts 9), LAWOS_RUNTIME_TENANT_IDS (session-auth.js:43-59), scripts/lib/protected-reset-accounts.mjs (desktop reset scoping; qa.tenant-b is not a desktop QA reset account).
   RISK: Adding the QA account to the HRX member roster by mistake breaks three 9-count assertions AND creates an employee row that defeats the empty-tenant proof

-- AFFECTED TESTS/GATES --
* scripts/validate-matter-vault-user-registration-seed.mjs (npm run matter-vault:user-registration:validate, package.json:220) — expectedEmails 16-27, count 63, membership-tenant 86-87, receipt phrase 104, summary print 132 [MUST CHANGE]
* docs/reorganization/client-matter-os/matter-vault-r4/launch/matter-vault-user-registration-receipt.md:12 'Registered account count: 10' [MUST CHANGE]
* packages/runtime-auth/test/matter-vault-user-registration-seed.test.js:29-47 per-user trustedTenantId [MUST CHANGE]
* apps/api/test/matter-temp-desktop-runtime-lambda.test.js:110 registered_account_count 10→11 [MUST CHANGE]
* apps/api/test/session-auth-api.test.js:177 listLawosInternalRoleAssignments().length===9 [UNCHANGED if new assignment kept as separate const per lawos-role-registry.js:146-157 precedent]
* apps/api/test/hrx-runtime-api.test.js:115 (roster.length===9), :143 and :185 (employees.length===9) — member-roster gates [UNCHANGED if hrx-member-roster-source-of-truth.json untouched]
* scripts/validate-lcx-hrx-sft-roster-source.mjs:37 rosterRows.length===9 (npm lcx:hrx-sft:roster:validate, package.json:153) [UNCHANGED]
* apps/api/test/admin-security-api.test.js — /api/admin/security/users gains an 11th row; verified NO count assertions, presence-only [NO CHANGE]
* apps/api/test/hrx/route-authz.test.js:54 — uses matter.desktop.qa as the no-HRX-scope account [NO CHANGE; do not reuse that role for qa.tenant-b]
* scripts/smoke-matter-desktop-aws-runtime.mjs:46,82 — non-strict (>0 / reporting only) [NO CHANGE]
* scripts/test/protected-reset-accounts.test.mjs + scripts/lib/protected-reset-accounts.mjs:1-2 — jwsuh protected, matter.desktop.qa desktop QA reset target [NO CHANGE]
* scripts/build-matter-desktop-mac.mjs:151-156 — bundles the seed into packaged desktop apps; new account auto-included via packagedSeedPath preference (matter-vault-account-registry.js:13) [NO CODE CHANGE; release-notes awareness]
* NEW gate to add: apps/api/test/tenant-isolation-api.test.js — HRX empty-tenant proof + bidirectional matter cross-tenant deny + amic regression guard

-- RISKS --
! Scope creep on tenant_ids: if permissionContextFromPrincipal keeps handing the QA principal the global LAWOS_RUNTIME_TENANT_IDS list, the QA account is alias-authorized into ALL 15 tenants (trust-context.js:41-49) and every cross-tenant deny test becomes vacuous — the per-principal scoping in session-auth.js:117-128 is the load-bearing change, not the seed entry
! Conversely, adding tenant_b_qa_synthetic to LAWOS_RUNTIME_TENANT_IDS would grant every existing account alias access into the QA tenant and must NOT be done; keeping it out is what makes the reverse deny (amic → tenant_b → 403) hold, proven live on port 4292
! verifyToken reorder (tenant check after user lookup) changes error precedence for the edge case 'valid signature + unknown user + wrong tenant' from 403 to 401 AUTH_SESSION_UNKNOWN_USER — no test pins either code on that path, but keep signature verification first to stay fail-closed
! The seed's per-user role_ids/scopes are decorative for sessions (proven live: jwsuh's session roles were the registry profile lawos_admin/..., not the seed's system_super_admin list) — isolation behavior lives in lawos-role-registry.js; forgetting TENANT_B_QA_ASSIGNMENT silently degrades to fallback lawos_staff self-service and weakens the proof without failing loudly (mitigate: the new test asserts role_ids includes lawos_hr)
! Matter deny test must target tenant_rp05_synthetic (where the 25 fixture matters live); targeting only the amic home tenant proves deny against an EMPTY tenant (0 rows), a weaker claim
! The new account ships in packaged desktop builds (packaged seed copy preferred over repo path, matter-vault-account-registry.js:6-13) and becomes a desktop-lambda login row (matter-temp-desktop-runtime-lambda.mjs:933) — synthetic-token-only so no credential exposure, but it widens the desktop login surface and changes registered_account_count surfaced by run-lcx-full-release-preflight-proof.mjs:99 (reporting-only, no assert)
! HRX durable-store persistence: seeding never writes tenant_b rows (resolveSeedTenantIds defaults, hrx-runtime-context.js:673-680/1791-1793), but any future proof script calling seedHrxDurableRuntimeStore({tenant_ids:[...]}) with tenant_b would silently break the empty-tenant assertion — pin the isolation test to a fresh ephemeral store (startApiServer default), as session-auth-api.test.js does
! Validator/receipt/tests are mutually coupled ('Registered account count: 11' checked in both directions) — land seed, validator, receipt, and both test updates in ONE commit or the gates flap

-- OPEN ISSUES --
? Product decision (low stakes): hrx_scopes for the QA assignment — HRX_HR_SCOPES (recommended: mirrors the reviewed lawos_hr_operations profile; write scopes act on an empty tenant) vs a hand-built read-only subset (stricter least-privilege but a novel scope combination no existing profile uses)
? Prose docs outside docs/reorganization/.../launch/ that narrate '10 registered accounts' were not exhaustively read (grep found the count asserted only in the validator, receipt, and lambda test); cheap closure after the change: npm run matter-vault:user-registration:validate plus node --test apps/api/test packages/runtime-auth/test
? Web login screen verified free of hardcoded rosters (no amic.kr in apps/web/src or apps/web/test), but a human logging into the WEB UI as qa.tenant-b will see empty states everywhere — intended for an API-level QA account; note it in the receipt so it is not mistaken for a regression


############ artifact-hygiene ############
SUMMARY: Root `npm test` includes 8 scripts/test/*.test.mjs files that regenerate proof artifacts into tracked artifacts/manual-qa paths, producing exactly the 13 perpetually-dirty files. Dirt comes from TWO layers: (1) tests execFileSync the run-*-proof.mjs runners, and (2) five of the "validators" (a06, a08, b13-withholding, e06, e10) re-run the same runner AGAIN via inline spawn or scripts/lib/upl-proof-runner.mjs assertNodeProofPass, plus validate-upl-c09-outlook-external-receipt.mjs itself writes the tracked readiness JSON/MD (it already supports env-var output redirection, used by the second wave1 test). All ten consumer validators are otherwise read-only, none checks generated_at freshness, and I empirically confirmed (from a scratch root seeded with the committed artifacts) that both wave1 ledger regenerators AND all read-only validators pass against the committed artifacts — so skipping upstream regeneration cannot break the wave1 chain. Recommended uniform pattern: one env switch LAWOS_REGEN_PROOFS — tests skip runner invocations (with explicit t.skip reasons) unless it is "1", tests spawn guarded validators with LAWOS_REGEN_PROOFS=0 so the shared helper/inline spawns become no-ops, and the c09 validator is redirected to a temp dir in default mode; direct manual invocation of any validator or runner (no env) keeps today's strict re-run behavior, so no gate is weakened. This also fixes the current cross-machine failure where UPL-B-13 fails (runner exits 1 with BLOCKED_POPBILL_CREDENTIALS_MISSING and clobbers the tracked artifact) on any machine without .env.popbill.local.

-- INVENTORY --
1. Full dirty-file mapping confirmed = exactly the 13 known files. test -> runner -> tracked artifacts written: (1) upl-a06 test -> run-upl-a06-all-domain-durable-roundtrip-proof.mjs -> upl-a06-...-proof.json+.md (json dirty: generated_at+tmpdir store_paths; md byte-stable). (2) upl-a08 test -> run-upl-a08-...-proof.mjs -> a08 json+md BOTH dirty (md embeds tmpdir store paths). (3) upl-a12 test -> run-upl-a12-...-proof.mjs -> a12 json+md BOTH dirty (md embeds per-call request/response hashes). (4) upl-b13 test#1 -> run-upl-b13-withholding-proof.mjs -> b13-withholding json dirty (md stable). (5) upl-b13 test#2 -> run-upl-b13-tax-invoice-sandbox-proof.mjs -> b13-popbill json+md BOTH dirty (mgtKey=Date.now + writeDate change request_hash/mgt_key_hash). (6) upl-e06 test -> run-upl-e06-notification-firing-proof.mjs -> e06 json dirty. (7) upl-e10 test -> run-upl-e10-wave1-hygiene-proof.mjs -> e10 json dirty. (8) wave1-external test step1 -> validate-upl-c09-outlook-external-receipt.mjs at DEFAULT paths -> upl-c09-outlook-external-receipt-readiness.json dirty (md stable, template only written if missing); step2 -> run-wave1-external-receipt-readiness.mjs -> wave1-external json dirty (md stable). (9) wave1-remediation test -> run-wave1-remediation-strict-verification-proof.mjs -> wave1-remediation json dirty (writes JSON only). All artifact paths are TRACKED (git ls-files artifacts/manual-qa, 124 files, includes every path above).
   EV: scripts/test/upl-a06-all-domain-durable-roundtrip.test.mjs:5,9; scripts/run-upl-a06-all-domain-durable-roundtrip-proof.mjs:21-22,338,353-355; scripts/test/upl-a08-packaged-desktop-restart.test.mjs:5,9; scripts/run-upl-a08-packaged-desktop-restart-proof.mjs:13-14,201,245-247,260-261; scripts/test/upl-a12-local-model-gateway.test.mjs:14-27; scripts/run-upl-a12-local-model-gateway-proof.mjs:9-10,113,177-195; scripts/test/upl-b13-withholding.test.mjs:5,16; scripts/run-upl-b13-withholding-proof.mjs:10-11,48,63-65; scripts/run-upl-b13-tax-invoice-sandbox-proof.mjs:10-11,228,190-216,246; scripts/test/upl-e06-notification-firing.test.mjs:5; scripts/run-upl-e06-notification-firing-proof.mjs:9-10,81,98-100; scripts/test/upl-e10-wave1-hygiene.test.mjs:5; scripts/run-upl-e10-wave1-hygiene-proof.mjs:7-8,124,154-156; scripts/test/wave1-external-receipt-readiness.test.mjs:9,13,17; scripts/run-wave1-external-receipt-readiness.mjs:6-7,308,351-353; scripts/test/wave1-remediation-strict-verification.test.mjs:5; scripts/run-wave1-remediation-strict-verification-proof.mjs:6,130,174-175; validate-upl-c09 default write paths scripts/validate-upl-c09-outlook-external-receipt.mjs:7-22,278-304,316
2. Hidden second regeneration layer: five validators re-run their runner unconditionally, so guarding only the tests is NOT enough. validate-upl-a06 (inline spawn), validate-upl-e06 (inline spawn), validate-upl-a08 / validate-upl-b13-withholding / validate-upl-e10-wave1-hygiene (via assertNodeProofPass in scripts/lib/upl-proof-runner.mjs). 16 more validate-upl-b1x/c0x validators use the same helper but are NOT invoked by npm test (they are manual/pack gates).
   EV: scripts/validate-upl-a06-all-domain-durable-roundtrip.mjs:20-21; scripts/validate-upl-e06-notification-firing.mjs:20-21; scripts/validate-upl-a08-packaged-desktop-restart.mjs:8 (await assertNodeProofPass("scripts/run-upl-a08-packaged-desktop-restart-proof.mjs")); scripts/validate-upl-b13-withholding.mjs:5; scripts/validate-upl-e10-wave1-hygiene.mjs:14; helper scripts/lib/upl-proof-runner.mjs:28-34; full user list from grep -rln assertNodeProofPass scripts/ (20 validators)
3. External conditions + pass/fail at snapshot per runner: a06 hermetic, PASS ([직접 재실행] exit 0 from scratch cwd). a08 hermetic (boots desktop-local API twice in-process), PASS (exit 0 verified). a12 requires Ollama at 127.0.0.1:11434 with model gemma4:12b (test has fetch-guard skip at upl-a12 test:14-18; runner env overrides LAWOS_OLLAMA_URL/LAWOS_MODEL_GATEWAY_MODEL) — gemma4:12b IS installed on this machine (verified /api/tags), so on the owner machine the guard passes and the artifact rewrites; on machines without gemma the test skips cleanly. b13-withholding hermetic, PASS (exit 0 verified). b13-popbill requires POPBILL_LINK_ID/POPBILL_SECRET_KEY/POPBILL_CORP_NUM/POPBILL_TEST_MODE from env or .env.popbill.local (gitignored, absent in fresh worktrees) and POPBILL_ALLOW_SANDBOX_ISSUE=1 for the real roundtrip: without creds it writes BLOCKED_POPBILL_CREDENTIALS_MISSING and exits 1 ([직접 재실행] verified) -> the UPL-B-13 test FAILS and the tracked artifact is clobbered away from its committed READY_NEEDS_SANDBOX_ISSUE_APPROVAL state (which carries credential fingerprints), which then breaks validate-wave1-remediation (requires exactly that status at :156) — matches 'everything except UPL-B-13 passes' from the prior diagnosis; on the owner machine with creds staged and allowIssue unset it exits 0 and just rewrites hashes. e06 hermetic, PASS (exit 0 verified). e10 requires python3 + machine-local /Users/jws/Applications/ai-slop-taxonomy/scripts/sloplint.py (exists here, verified) + a git repo for --changed + apps/web ui-regression; PASS on owner machine only. wave1-external + wave1-remediation regenerators read committed upstream artifacts and matrix — both PASS against pristine committed artifacts ([직접 재실행] exit 0 from scratch root seeded with committed artifacts/manual-qa + workbook + symlinked docs/packages/apps).
   EV: runner exits recorded in this session from scratchpad cwd: b13w=0, e06=0, a06=0, a08=0, b13p-nocreds=1 (status BLOCKED_POPBILL_CREDENTIALS_MISSING), w1e=0, w1r=0; Ollama tags show gemma4:12b; committed statuses: a06/a08/a12/e10 PASS, b13w LOCAL_WITHHOLDING_PASS_VENDOR_BLOCKED, b13p READY_NEEDS_SANDBOX_ISSUE_APPROVAL (cred_fp 6506d5ea00bb/d2fc1fbbd0a1), c09 READY_NEEDS_OUTLOOK_EXTERNAL_RECEIPT, w1e PASS_EXTERNAL_RECEIPT_READINESS_LEDGER, w1r PARTIAL_EXTERNAL_BLOCKED; runner gates scripts/run-upl-b13-tax-invoice-sandbox-proof.mjs:218-303; sloplint const scripts/run-upl-e10-wave1-hygiene-proof.mjs:9
4. Consumers of the 13 artifacts — the design must preserve these: validate-upl-a06/a08/a12/b13-withholding/b13-tax-invoice-sandbox/e06/e10 read their own artifact + wave1-70-tuw matrix; validate-wave1-external-receipt-readiness reads wave1-external json + matrix + c09 readiness json; validate-wave1-remediation-strict-verification reads remediation md+json, matrix, wave1-external json, c09 readiness json, b13-popbill json, e10 json and asserts b13Popbill.status===READY_NEEDS_SANDBOX_ISSUE_APPROVAL and c09.status===READY_NEEDS_OUTLOOK_EXTERNAL_RECEIPT; run-wave1-external-receipt-readiness reads a12/c09-readiness/b13-withholding/b13-popbill artifacts (:98-109) and run-wave1-remediation reads c09/b13-popbill/e10/wave1-external (:11-14). NO GitHub workflow, package.json script (other than upl:b13:popbill:* at package.json:373-374), or apps/packages code reads these files. No validator checks generated_at freshness — all pass against committed artifacts ([직접 재실행]: a12, b13-sandbox, wave1-external, wave1-remediation validators exit 0 in scratch root; c09 validator exit 0 with temp redirect producing READY_NEEDS_OUTLOOK_EXTERNAL_RECEIPT; a06/a08/b13w/e06/e10 validators only failed in scratch because their runner re-run couldn't resolve scripts/ there — their assert sections are read-only).
   EV: scripts/validate-wave1-remediation-strict-verification.mjs:46-54,149-159,156; scripts/validate-wave1-external-receipt-readiness.mjs:7-9,30-47,85; scripts/run-wave1-external-receipt-readiness.mjs:98-109; scripts/run-wave1-remediation-strict-verification-proof.mjs:7-15; grep over .github/workflows (only hrx-production-rollout.yml, production-smoke.yml; zero manual-qa references); package.json:373-374
5. Existing redirect mechanism to reuse: validate-upl-c09-outlook-external-receipt.mjs honors UPL_C09_OUTLOOK_EXTERNAL_RECEIPT_PATH / ..._READINESS_PATH / ..._READINESS_MD_PATH / ..._TEMPLATE_PATH env overrides; the second test in wave1-external-receipt-readiness.test.mjs already uses them with mkdtempSync and stays clean. No runner has a dry-run/no-write flag or output-path override (all use hardcoded join(ROOT, "artifacts/manual-qa/...") with ROOT=process.cwd()), so option (c) dry-mode does not exist today; option (b) temp-redirect exists ONLY for the c09 validator.
   EV: scripts/validate-upl-c09-outlook-external-receipt.mjs:7-22; scripts/test/wave1-external-receipt-readiness.test.mjs:24-94; ARTIFACT_* consts hardcoded in all 9 run-* scripts (e.g. run-upl-a06...:21-22, run-wave1-external...:6-7)
6. Non-suspects cleared: the other 6 scripts/test files (aws-sso-lambda-env, closeout-pack-claude-review-hardening, hrx-weighted-ledger, matter-vault-backup-restore, protected-reset-accounts, ui-tooling-smoke) write only to os.tmpdir() or spawn node --check; no packages/*/test or apps/*/test writes into artifacts/manual-qa. The a09-* tracked artifacts are not rewritten by npm test (matter-vault-backup-restore.test.mjs uses mkdtemp only).
   EV: scripts/test/matter-vault-backup-restore.test.mjs:15 (mkdtemp(join(tmpdir(),"lawos-a09-runtime-backup-"))); scripts/test/ui-tooling-smoke.test.mjs:28 (node --check only); grep for writeFileSync+artifacts/manual-qa across packages/*/test apps/*/test returned nothing

-- CHANGE PLAN --
1. FILE: scripts/lib/upl-proof-runner.mjs
   CHANGE: In assertNodeProofPass (lines 28-34), add a guard as the first statement: if (process.env.LAWOS_REGEN_PROOFS === "0") { console.log(`[skip] proof regeneration disabled for ${scriptPath} (LAWOS_REGEN_PROOFS=0); validating committed artifact`); return { status: 0, signal: null, stdout: "", stderr: "" }; }. IMPORTANT: guard fires ONLY on the explicit string "0" — unset env keeps today's strict re-run behavior for the 20 manual/pack validators that use this helper.
   RISK: If the guard fired on unset env it would silently weaken 16 closeout-pack gate validators (b10-b17, c01-c09-c12) — the explicit-"0"-only condition avoids that; verify with: node scripts/validate-upl-b12-trust-ledger.mjs still regenerates.
2. FILE: scripts/lib/proof-regen-guard.mjs (new)
   CHANGE: Tiny shared helper for the 8 tests: export const proofRegenEnabled = process.env.LAWOS_REGEN_PROOFS === "1"; export const REGEN_SKIP_REASON = "proof artifact regeneration is disabled during npm test; set LAWOS_REGEN_PROOFS=1 to regenerate committed proof artifacts"; export function validatorEnv() { return proofRegenEnabled ? process.env : { ...process.env, LAWOS_REGEN_PROOFS: "0" }; }
   RISK: None; pure additive.
3. FILE: scripts/test/upl-a06-all-domain-durable-roundtrip.test.mjs
   CHANGE: Wrap the runner execFileSync at line 5 in if (proofRegenEnabled) {...} else t.diagnostic(REGEN_SKIP_REASON); always run the validator (line 9) but with env: validatorEnv(). Keep a single test (skip only the runner step, not the validator) so the committed-artifact gate still executes every npm test.
   RISK: Validator asserts committed artifact content (status PASS, 13 domains) — passes today; would correctly fail if someone commits a broken artifact.
4. FILE: scripts/validate-upl-a06-all-domain-durable-roundtrip.mjs
   CHANGE: Replace the unconditional inline spawn at lines 20-21 with the shared helper: await assertNodeProofPass("scripts/run-upl-a06-all-domain-durable-roundtrip-proof.mjs") (import from ./lib/upl-proof-runner.mjs), so the LAWOS_REGEN_PROOFS=0 guard applies. All asserts from line 22 down stay unchanged.
   RISK: Behavior under manual invocation (no env) is identical (still re-runs); only the test-injected "0" changes anything.
5. FILE: scripts/test/upl-a08-packaged-desktop-restart.test.mjs
   CHANGE: Same pattern as a06: guard runner call (line 5) behind proofRegenEnabled; run validator (line 9) with env: validatorEnv(). No validator edit needed — validate-upl-a08-packaged-desktop-restart.mjs:8 already goes through assertNodeProofPass which gains the guard.
   RISK: None beyond helper-guard risk above.
6. FILE: scripts/test/upl-a12-local-model-gateway.test.mjs
   CHANGE: Extend the existing guard (lines 14-18): run the runner (line 19) only when proofRegenEnabled AND await hasLocalGemma(); when regen is enabled but Gemma missing, keep the existing t.skip("gemma4:12b is not installed in local Ollama"); when regen is disabled, do NOT skip the whole test — skip only the runner step and still run the validator (line 23, read-only, no env change needed). This closes the current hole where a present Gemma rewrites the artifact on every npm test.
   RISK: Validator asserts committed artifact only (status PASS, hashes present) — no Ollama needed, so this test finally passes deterministically on all machines.
7. FILE: scripts/test/upl-b13-withholding.test.mjs
   CHANGE: Test 1 (line 4): guard runner (line 5) behind proofRegenEnabled; run validate-upl-b13-withholding (line 9) with env: validatorEnv() (its :5 assertNodeProofPass gets the guard). Test 2 (line 15): guard the popbill runner (line 16) behind proofRegenEnabled AND presence of POPBILL_LINK_ID+POPBILL_SECRET_KEY (check process.env then parse .env.popbill.local exactly like scripts/run-upl-b13-tax-invoice-sandbox-proof.mjs:17-33); when creds are absent in regen mode, t.skip("POPBILL_LINK_ID/POPBILL_SECRET_KEY not staged (.env.popbill.local)"). Always run validate-upl-b13-tax-invoice-sandbox (line 20) — it is read-only and its allowedStatuses set (validate-upl-b13-tax-invoice-sandbox.mjs:69-75) accepts the committed READY_NEEDS_SANDBOX_ISSUE_APPROVAL.
   RISK: This is the change that fixes the current hard failure on cred-less machines (runner exit 1 + artifact clobbered to BLOCKED_POPBILL_CREDENTIALS_MISSING, which cascades into validate-wave1-remediation:156). Do NOT let the runner execute without creds in default mode under any refactor.
8. FILE: scripts/test/upl-e06-notification-firing.test.mjs
   CHANGE: Same pattern as a06: guard runner (line 5); run validator (line 9) with env: validatorEnv().
   RISK: None beyond a06-style risk.
9. FILE: scripts/validate-upl-e06-notification-firing.mjs
   CHANGE: Replace the unconditional inline spawn at lines 20-21 with await assertNodeProofPass("scripts/run-upl-e06-notification-firing-proof.mjs") from ./lib/upl-proof-runner.mjs so the guard applies; keep every assert below unchanged.
   RISK: Same as a06 validator.
10. FILE: scripts/test/upl-e10-wave1-hygiene.test.mjs
   CHANGE: Guard runner (line 5) behind proofRegenEnabled; optionally also skip-with-reason in regen mode when /Users/jws/Applications/ai-slop-taxonomy/scripts/sloplint.py (const at run-upl-e10-wave1-hygiene-proof.mjs:9) is missing, mirroring the runner's hard dependency. Run validate-upl-e10-wave1-hygiene (line 9) with env: validatorEnv() (its :14 assertNodeProofPass gets the guard).
   RISK: e10's sloplint path is machine-local to the owner; in default mode the validator now passes everywhere against the committed artifact, in regen mode it remains owner-machine-only — intended.
11. FILE: scripts/test/wave1-external-receipt-readiness.test.mjs
   CHANGE: Test 1 only (test 2 already temp-redirects and needs no change). Step 1 (line 9, validate-upl-c09-outlook-external-receipt): when NOT proofRegenEnabled, pass env redirecting outputs to a mkdtempSync dir via the EXISTING vars UPL_C09_OUTLOOK_EXTERNAL_RECEIPT_READINESS_PATH / _READINESS_MD_PATH / _TEMPLATE_PATH (leave UPL_C09_OUTLOOK_EXTERNAL_RECEIPT_PATH at default so the committed absent-receipt state is what gets validated), and optionally assert the redirected readiness.status === "READY_NEEDS_OUTLOOK_EXTERNAL_RECEIPT" ([직접 재실행] verified exit 0 with that status); when regen enabled, run at default tracked paths as today. Step 2 (line 13, run-wave1-external-receipt-readiness.mjs): guard behind proofRegenEnabled. Step 3 (line 17, validate-wave1-external-receipt-readiness.mjs): always run — read-only, and verified to pass against the committed ledger.
   RISK: The wave1 ledger regenerator was verified to pass against committed upstream artifacts, so regen mode stays green whether or not upstream runners also ran; keep the c09 receipt-path env at default in both modes or the validator would validate a nonexistent custom path.
12. FILE: scripts/test/wave1-remediation-strict-verification.test.mjs
   CHANGE: Guard runner (line 5, run-wave1-remediation-strict-verification-proof.mjs) behind proofRegenEnabled; always run validate-wave1-remediation-strict-verification.mjs (line 9) — read-only, verified passing against committed artifacts.
   RISK: validator:156 requires b13Popbill.status===READY_NEEDS_SANDBOX_ISSUE_APPROVAL — safe because default mode no longer rewrites that artifact.
13. FILE: package.json
   CHANGE: Optional but recommended: add "proofs:regen": "LAWOS_REGEN_PROOFS=1 node --test --test-concurrency=1 scripts/test/upl-a06-all-domain-durable-roundtrip.test.mjs scripts/test/upl-a08-packaged-desktop-restart.test.mjs scripts/test/upl-a12-local-model-gateway.test.mjs scripts/test/upl-b13-withholding.test.mjs scripts/test/upl-e06-notification-firing.test.mjs scripts/test/upl-e10-wave1-hygiene.test.mjs scripts/test/wave1-external-receipt-readiness.test.mjs scripts/test/wave1-remediation-strict-verification.test.mjs" next to the existing upl:b13:popbill:* scripts (package.json:373-374). Serial concurrency matters in regen mode because upl-b13 rewrites the popbill artifact that the wave1 tests read.
   RISK: npm-script env assignment is POSIX-shell syntax; fine on the owner's darwin environment where regen runs.

-- AFFECTED TESTS/GATES --
* scripts/test/upl-a06-all-domain-durable-roundtrip.test.mjs (runner step becomes regen-gated; validator still runs)
* scripts/test/upl-a08-packaged-desktop-restart.test.mjs (same)
* scripts/test/upl-a12-local-model-gateway.test.mjs (Ollama guard extended with regen gate; validator now always runs)
* scripts/test/upl-b13-withholding.test.mjs (both tests; popbill test gains cred-presence skip; fixes current FAIL on cred-less machines)
* scripts/test/upl-e06-notification-firing.test.mjs (same as a06)
* scripts/test/upl-e10-wave1-hygiene.test.mjs (regen-gated; sloplint machine dependency isolated to regen mode)
* scripts/test/wave1-external-receipt-readiness.test.mjs (test 1: c09 validator temp-redirect + regen-gated ledger runner; test 2 unchanged)
* scripts/test/wave1-remediation-strict-verification.test.mjs (regen-gated runner; validator unchanged)
* scripts/lib/upl-proof-runner.mjs assertNodeProofPass — guard must be explicit-"0"-only so the 16 non-npm-test validators (validate-upl-b10..b17, c01..c09-c12) keep strict re-run behavior
* scripts/validate-upl-a06-all-domain-durable-roundtrip.mjs, scripts/validate-upl-e06-notification-firing.mjs — inline spawns converted to the shared guarded helper; assert sections untouched
* Gates that must stay intact (verified read-only, no freshness checks): validate-upl-a12-local-model-gateway.mjs, validate-upl-b13-tax-invoice-sandbox.mjs, validate-upl-c09-outlook-external-receipt.mjs (strict operator path via default paths unchanged), validate-wave1-external-receipt-readiness.mjs, validate-wave1-remediation-strict-verification.mjs, npm run upl:b13:popbill:sandbox / upl:b13:popbill:validate (package.json:373-374)
* ACCEPTANCE CHECKLIST for Codex: (1) npm test with no special env -> exit 0, then `git status --porcelain -- artifacts/` empty; (2) run npm test a SECOND time immediately -> exit 0 and tree still clean (catches any residual writer); (3) repeat (1) on a machine/worktree WITHOUT .env.popbill.local, without sloplint, with Ollama stopped -> still exit 0 + clean (this currently fails at UPL-B-13); (4) strict mode: LAWOS_REGEN_PROOFS=1 npm run proofs:regen on the owner machine (gemma4:12b + .env.popbill.local + sloplint present) -> exit 0 and the artifacts are intentionally rewritten (operator may commit refreshed proofs); (5) gate preservation: node scripts/validate-upl-a08-packaged-desktop-restart.mjs with NO env still re-runs its runner (strict), node scripts/validate-upl-b12-trust-ledger.mjs unchanged, node scripts/validate-upl-c09-outlook-external-receipt.mjs with no env still writes the tracked readiness intake for operators; (6) node scripts/validate-wave1-remediation-strict-verification.mjs passes against the committed tree both before and after (1).

-- RISKS --
! Direction-of-flag hazard: if assertNodeProofPass skips on UNSET env instead of only on the literal "0", 16 closeout-pack validators silently stop re-proving — that weakens gates and violates the standing production_ready-gate rule. The tri-state design (unset=strict, 0=validate-only, 1=regen) must be implemented exactly.
! In regen mode, node --test runs the 8 files concurrently while upl-b13 rewrites upl-b13-popbill-sandbox-proof.json and the wave1 tests read it — a read-during-write JSON parse race. Pre-existing today, but the new proofs:regen script should pin --test-concurrency=1.
! If POPBILL_ALLOW_SANDBOX_ISSUE=1 ever lands in the owner's .env.popbill.local, a regen run would attempt the real Popbill sandbox issue and, on failure, commit-candidate the artifact as BLOCKED_POPBILL_SANDBOX_ISSUE_FAILED, which validate-wave1-remediation:156 rejects — regen runs must be reviewed before committing refreshed artifacts.
! Default npm test no longer re-proves runtime behavior for these 9 proofs (validators only assert committed artifact contents); this is the ratified trade-off — the strict path moves to the explicit regen command and the untouched manual validators.
! The committed artifacts become load-bearing test fixtures: any future commit that regenerates them into a different status (e.g. c09 receipt intake succeeding -> PASS_C09_OUTLOOK_EXTERNAL_RECEIPT) will fail validate-wave1-remediation/-external until the wave1 ledgers and matrix are regenerated together — document that regen must be all-or-nothing per wave1 chain.
! Diagnosis-environment caveat: pass/fail labels marked [직접 재실행] were verified by running runners from a scratch cwd on this machine (which has gemma4:12b and sloplint but no .env.popbill.local); e10 and a12 full-runner PASS on the owner main checkout is inferred from the committed PASS artifacts dated 2026-07-03 plus tool presence, not re-executed here ([에이전트 추론·미재검증]).

-- OPEN ISSUES --
? Naming: LAWOS_REGEN_PROOFS is proposed; confirm no collision (grep found no existing LAWOS_REGEN_* / LAWOS_SKIP_* variables).
? The diag worktree shows a pre-existing package-lock.json modification from dependency install; the acceptance 'clean tree' check should be scoped to `git status --porcelain -- artifacts/` or run after `git checkout -- package-lock.json` so lockfile noise doesn't mask the signal.
? Whether to also assert in the wave1-external test that the temp-redirected c09 readiness JSON equals the committed one field-for-field (minus generated_at) — cheap extra drift guard, left to Codex's judgment.
? upl-e10 runner hardcodes the machine-local sloplint path (scripts/run-upl-e10-wave1-hygiene-proof.mjs:9); out of scope for this decision but a candidate for an env override in a later pack.


############ misc-anchors ############
SUMMARY: Pinned all remaining implementation anchors in /Users/jws/lawos-diag-wt at 169484912. (F) /api/health is built inline at apps/api/src/server.js:768 by spreading the frozen SERVICE_DESCRIPTOR (defined :374-412); health returns at :767-770 BEFORE the session gate at :779, so it stays ungated; no test deep-equals the whole payload, so adding runtime_profile + synthetic_login_enabled is additive. (E) The 5 surfaces (Content/Dashboards/Experiments/Theme/AdminSurface) have zero importers; the only non-self reference is scripts/validate-sf-client-matter-parity-crosswalk.mjs:1430-1433 (4 lines to delete). The i18n file is apps/web/src/i18n.js (NOT data/i18n.js); 13 keys x 2 locale blocks (26 lines) are verifiably dead after deletion. (H) "Sign up now" is AuthSurface.jsx:91; the styled reusable notice convention is GuardedStateNotice/GuardedStatusBadge in components/GuardedState.js (live-data-state CSS exists incl. forest skin; there is no `.status-label` CSS). (Stage-7) GlobalUtilitySurface UtilityDetail (:102-161) is the single non-live-embed render path for messages/notifications/esign/calendar sections — one conditional GuardedStateNotice there covers all four. (C) apps/web has typescript ^5.8.3 (deps, resolves to hoisted 5.9.3), no typecheck script, no tsconfig; vite.config.js sets only base+server.proxy (no esbuild/jsx conflicts); the 7 initial errors were REPRODUCED here with a lenient trial config. (notifications) root test glob lacks packages/notifications; insertion point confirmed after the packages/hrx glob; the 2 tests pass standalone.

-- INVENTORY --
1. Health payload construction site: single inline literal `sendJson(req, res, 200, { status: "ok", time: new Date().toISOString(), ...SERVICE_DESCRIPTOR })`
   EV: apps/api/src/server.js:767-770; SERVICE_DESCRIPTOR frozen at :374-412 with top-level keys service, version, bounded_contexts, permission_gate, enrichment, synthetic_only, uses_real_client_data
2. Health is exempt from the session gate: handled and returns before `sessionAuth.resolvePermissionContextFromHeaders(..., { requireSessionToken: true })`
   EV: apps/api/src/server.js:767 (health return) vs :779 (gate); principal header overwrite at :793-806
3. Health shape assertions are field-level only (no whole-payload deepEqual) — additive fields are safe
   EV: apps/api/test/master-data-api.test.js:42-55 (asserts status/service/synthetic_only/uses_real_client_data/permission_gate.*/bounded_contexts[0]); apps/api/test/session-auth-api.test.js:77-85 (api-auth descriptor endpoints); ~15 other apps/api/test/*.test.js hit /api/health but check status/context membership; scripts/run-lcx-vltui-production-smoke.mjs:257-263 checks bounded_contexts membership; scripts/smoke-hrx-production.mjs:103-104 checks status only
4. No existing runtime_profile / synthetic_login_enabled / LAWOS_RUNTIME_PROFILE anywhere — both fields and any env var are net-new
   EV: grep across apps/api/src, apps/api/test, scripts: zero hits
5. Synthetic login provider is unconditional (roster-seeded local-dev provider is the only login path), so synthetic_login_enabled is currently always true
   EV: apps/api/src/session-auth.js:180 `createLocalDevAuthProvider({ subjects: subjectsFromSeed(seed) })`; credential accepted at :350; 'local-dev-only' literal only in the seed JSON and scripts/validate-matter-vault-user-registration-seed.mjs
6. Zero importers for all 5 orphan surfaces at this snapshot; only cross-reference is the parity-crosswalk validator reading AdminSurface.jsx
   EV: grep of each name across apps/, scripts/, packages/ matches only the file itself, plus scripts/validate-sf-client-matter-parity-crosswalk.mjs for AdminSurface; apps/web/test/*.mjs: zero matches for any of the 5 names
7. AdminSurface read-block to delete (exactly 4 lines): `const adminSurface = read("apps/web/src/components/AdminSurface.jsx");` / `forbid(adminSurface, permissionAdminUiPattern, "adminSurface:no-permission-admin-ui-before-routes");` / `forbid(adminSurface, dataCloudEnrichmentUiPattern, "adminSurface:no-data-cloud-enrichment-ui-before-routes");` / `forbid(adminSurface, reportingBuilderUiPattern, "adminSurface:no-report-builder-client-profitability-ui-before-routes");`
   EV: scripts/validate-sf-client-matter-parity-crosswalk.mjs:1430-1433
8. i18n file is apps/web/src/i18n.js (apps/web/src/data/i18n.js does not exist); consumed only via App.jsx:3 `import { copy }` -> App.jsx:72 `const labels = copy[locale]` -> `labels.<key>` props; only dynamic lookup is Shell.jsx:657 `labels[id]` over navItems ids home/clients/matters/people/vault/portal (apps/web/src/data/nav.js:11-16)
   EV: apps/web/src/i18n.js:1-150; App.jsx:3,72; Shell.jsx:657; nav.js:11-16
9. Verified dead i18n keys (13 keys, 26 lines; used only by the 5 orphans or by nothing): content (ko:24/en:97), dashboards (36/109), experiments (38/111), admin (39/112), save (16/89), templates (53/126), allContent (60/133), resources (69/142), flags (70/143), experimentTitle (72/145), adminTitle (73/146), billingTitle (74/147), themeTitle (75/148). KEEP: invite (Shell.jsx:174), saveChart (MatterModal.jsx:36), askTitle/builderTitle/resultTable/eventStream/rawEvent/profileTitle (used by Ask/Analytics/ProfilesSurface — out-of-scope orphans), project (ui-regression asserts the 'Client Matter People Vault' string), home/clients/matters/people/vault/portal (dynamic labels[id])
   EV: exhaustive `labels\.<key>` + `labels[` grep across apps/web/src excluding the 5 orphans: zero hits for all 13; orphan-side usage confirmed (e.g. ContentSurface.jsx:9,25,31,42 uses allContent/resources/flags)
10. Gates that read i18n.js: apps/web/test/ui-regression.test.mjs:116,129-131 (must keep PRODUCT_BRAND + 'Client Matter People Vault', must not add 'Project Atlas') and :1171-1186 product-copy scan (i18n.js + all src/components files matched against forbiddenVisibleCopy regex)
   EV: apps/web/test/ui-regression.test.mjs:110-131, 1170-1186
11. Sign up now anchor: AuthSurface.jsx:91 (button inside login heading block :88-94, login screen section :82-104). Existing preview/notice conventions: GuardedStateNotice/GuardedStatusBadge in apps/web/src/components/GuardedState.js:17-44 (classes `live-data-state live-data-<tone>` styled at styles.css:6961+ incl. forest skin :11069; `.status-label` has NO CSS rule); established Korean preview word is 미리보기; data-attr convention `data-signup-*` / `data-login-*` already in AuthSurface (:198,213,227,258-261) and `data-global-*` in GlobalUtilitySurface
   EV: apps/web/src/components/AuthSurface.jsx:82-104; GuardedState.js:17-44; styles.css:6961,11069; no 'Sign up now' assertion in any test/script (grep: zero hits)
12. GlobalUtility preview marker attach point: UtilityDetail (GlobalUtilitySurface.jsx:102-161) is the only render path for sections WITHOUT live embeds — live embeds are only policies-employment-contracts (:205-209) and settings-permissions (:210-213). Utility ids confirmed in apps/web/src/data/globalUtilities.js: messages (:24, sections :32-61), notifications (:68, notifications-center :76 already has its own note at surface :150-155), esign (:295, sections :303-324), calendar (:336, sections :345-358)
   EV: GlobalUtilitySurface.jsx:102-161,205-216; globalUtilities.js:24-61,68-90,295-324,336-358
13. Gates on GlobalUtilitySurface that must keep passing: validate-lcx-full-guarded-ui.mjs:32-38 requires the strings 'GuardedStateNotice', 'data-global-audit-required', 'data-global-decision-required' in the file (all preserved by an additive notice); validate-hrx-ui-api-backed.mjs:133-136 requires data-global-live-hr-documents + catalog strings; validate-lcx-global-ia.mjs:37-40 asserts utility id arrays (untouched)
   EV: scripts/validate-lcx-full-guarded-ui.mjs:26-56; scripts/validate-hrx-ui-api-backed.mjs:133-136; scripts/validate-lcx-global-ia.mjs:23-40
14. typecheck wiring (C): apps/web/package.json has typescript ^5.8.3 in dependencies, scripts only dev/build/preview/test:ui (no typecheck); no tsconfig anywhere under apps/web; vite config is apps/web/vite.config.js exporting only { base: "./", server: { strictPort, proxy } } — no esbuild/jsx options to fight; ambient decls already exist at apps/web/src/react-jsx.d.ts (stubs react, react/jsx-runtime, '*.jsx', JSX namespace — compile substrate since @types/react is NOT installed); hoisted tsc is 5.9.3
   EV: apps/web/package.json:6-19; apps/web/vite.config.js (whole file); apps/web/src/react-jsx.d.ts:1-29; node_modules/@types has no react; node_modules/.bin/tsc --version = 5.9.3
15. 7 initial typecheck errors REPRODUCED here [직접 재실행] with lenient config (strict:false, allowImportingTsExtensions:true, skipLibCheck:true, types:[], jsx react-jsx, moduleResolution bundler, include src/**/*.ts+tsx): EmployeeList.tsx(5) TS2305 useSkin, EmployeeList.tsx(23) TS2339 length-on-unknown, PeopleWorkforceDirectory.tsx(19) TS2305 useSkin, PeopleWorkforceDirectory.tsx(438) TS2698 spread-of-unknown, LegalPeopleWorkspace.tsx(5) TS2305 useSkin, PeopleHome.tsx(4) TS2305 ForestHero, PeopleHome.tsx(20) TS2305 PermissionAdminPanel. With strict:true the count explodes (100+), so the tsconfig knobs are load-bearing
   EV: trial run: /Users/jws/lawos-diag-wt/node_modules/.bin/tsc -p <scratchpad tsconfig> -> exactly 7 'error TS' lines; strict variant -> TS5097/TS7006/TS2345 flood
16. notifications glob: root package.json test script (line 10) lists 36 package globs ending '...packages/commercial/test/*.test.js packages/hrx/test/*.test.js packages/persistence/test/*.test.js ... scripts/test/*.test.mjs' and lacks packages/notifications; packages/notifications/test/service.test.js exists and passes standalone (2/2). Other unglobbed packages (data-cloud, import-data, record-actions, reports) have NO test dirs, so notifications is the only real gap
   EV: package.json:10; `node --test packages/notifications/test/service.test.js` -> pass 2 fail 0 [직접 재실행]

-- CHANGE PLAN --
1. FILE: apps/api/src/server.js
   CHANGE: Decision F: at line 768 expand the health literal to `sendJson(req, res, 200, { status: "ok", time: new Date().toISOString(), ...SERVICE_DESCRIPTOR, runtime_profile: process.env.LAWOS_RUNTIME_PROFILE ?? "local_dev", synthetic_login_enabled: true })`. Place the new fields AFTER the spread so precedence is explicit (SERVICE_DESCRIPTOR :374-412 has neither key, so this is purely additive). Do NOT add them inside the frozen SERVICE_DESCRIPTOR (module-scope freeze at import time; tests spawn in-process servers with per-test env). synthetic_login_enabled is honestly `true` today because session-auth.js:180 wires createLocalDevAuthProvider unconditionally — if Codex prefers env-driven, derive both from the same new env var, but do not invent a disabled state that the code cannot actually reach.
   RISK: low — no test/validator deep-equals the health payload; the ungated path (:767 before gate :779) is unchanged
2. FILE: apps/web/src/components/ContentSurface.jsx
   CHANGE: Decision E: delete file (zero importers).
   RISK: low
3. FILE: apps/web/src/components/DashboardsSurface.jsx
   CHANGE: Decision E: delete file (zero importers).
   RISK: low
4. FILE: apps/web/src/components/ExperimentsSurface.jsx
   CHANGE: Decision E: delete file (zero importers).
   RISK: low
5. FILE: apps/web/src/components/ThemeSurface.jsx
   CHANGE: Decision E: delete file (zero importers).
   RISK: low
6. FILE: apps/web/src/components/AdminSurface.jsx
   CHANGE: Decision E: delete file (zero importers) — MUST land in the same commit as the validator edit below, else `npm run` of the parity crosswalk fails on read().
   RISK: medium — ordering coupling with the validator
7. FILE: scripts/validate-sf-client-matter-parity-crosswalk.mjs
   CHANGE: Delete exactly lines 1430-1433 (the `const adminSurface = read(...)` line and the three `forbid(adminSurface, ...)` lines quoted in inventory). Nothing else in the script references adminSurface.
   RISK: low — grep confirms single block
8. FILE: apps/web/src/i18n.js
   CHANGE: Prune 13 dead keys from BOTH locale blocks (26 lines): ko lines 16 (save), 24 (content), 36 (dashboards), 38 (experiments), 39 (admin), 53 (templates), 60 (allContent), 69 (resources), 70 (flags), 72 (experimentTitle), 73 (adminTitle), 74 (billingTitle), 75 (themeTitle); en duplicates at 89, 97, 109, 111, 112, 126, 133, 142, 143, 145, 146, 147, 148. After removing themeTitle (current last entry, no trailing comma) fix the comma on the new last entry. KEEP: project (ui-regression.test.mjs:130 asserts 'Client Matter People Vault' in this file), invite, saveChart, askTitle, builderTitle, resultTable, eventStream, rawEvent, profileTitle, home/clients/matters/people/vault/portal.
   RISK: low — no script parses i18n keys; only ui-regression string assertions apply
9. FILE: apps/web/src/i18n.js
   CHANGE: Decision H (same edit pass): add one new key to BOTH ko and en blocks, e.g. `signupPreviewNotice: "가입 절차는 미리보기 화면입니다. 계정 등록은 관리자 승인으로 처리됩니다."` — copy must not match ui-regression forbiddenVisibleCopy (:1181) nor the AuthSurface-specific forbid list (:1167: no 'Start Workspace', 'Sign up with Google', 'Data Storage Location', 'Work Areas', 'Client Portal', 'Matter Graph', 'DMS', 'Billing').
   RISK: low
10. FILE: apps/web/src/components/AuthSurface.jsx
   CHANGE: Decision H: minimal labeling at the login screen heading block (:88-94) — directly after the `Sign up now` button (:90-92) add `<small className="matter-login-preview-note" data-signup-preview="true">{labels.signupPreviewNotice}</small>`; AND in the auth-panel card render path (:149-155) add, for the signup step only, `{authStep === "signup" && (<GuardedStateNotice state="ready_read_only" title={labels.signupPreviewNotice} dataAttrs={{ "data-signup-preview": "true" }} />)}` reusing the shared notice (import { GuardedStateNotice } from "./GuardedState.js"). GuardedStateNotice is the only styled notice convention (`live-data-state`, styles.css:6961 + forest :11069); `.status-label` has no CSS so don't rely on it alone.
   RISK: low — no gate asserts 'Sign up now' or the login block copy; ui-regression :1166-1168 forbid-list must be respected in the new copy
11. FILE: apps/web/src/components/GlobalUtilitySurface.jsx
   CHANGE: Stage-7 preview markers: inside UtilityDetail (:102-161), before the closing </Panel> (:159), add one conditional shared notice: `{["messages", "notifications", "esign", "calendar"].includes(utility.id) && (<GuardedStateNotice state="ready_read_only" title="미리보기 화면입니다." dataAttrs={{ "data-global-preview-section": section.id }}>이 항목은 화면 구성 미리보기이며, 실행은 연결된 업무 화면에서 처리합니다.</GuardedStateNotice>)}` — GuardedStateNotice is already imported (:5). This covers every messages-*/notifications-*/esign-*/calendar-* section because live embeds bypass UtilityDetail only for policies-employment-contracts (:205-209) and settings-permissions (:210-213). Keep the existing notifications-center note (:150-155) and the required strings 'data-global-audit-required'/'data-global-decision-required' (:141,:146) untouched — validate-lcx-full-guarded-ui.mjs:32-38 greps for them.
   RISK: low — additive; validators are includes-based
12. FILE: apps/web/tsconfig.json
   CHANGE: Decision C: create with EXACTLY the reproduced-7-errors knobs: compilerOptions { target ES2022, module ESNext, moduleResolution bundler, jsx react-jsx, allowJs false, checkJs false, strict false, noEmit true, skipLibCheck true, isolatedModules true, esModuleInterop true, allowImportingTsExtensions true, types [] }, include ["src/**/*.ts", "src/**/*.tsx"]. types:[] + the existing src/react-jsx.d.ts stubs are what make this compile without @types/react — do not add @types/react in this pass. strict:true changes the baseline from 7 errors to 100+.
   RISK: medium — knob drift silently changes the agreed 7-error baseline
13. FILE: apps/web/package.json
   CHANGE: Add to scripts (:6-11): `"typecheck": "tsc -p tsconfig.json"` (tsc resolves to hoisted 5.9.3; the ^5.8.3 dependency already satisfies it).
   RISK: low
14. FILE: package.json
   CHANGE: (1) Add root script `"typecheck:web": "npm --workspace apps/web run typecheck"` (matches the existing `npm --workspace apps/web run dev` convention at :8-9). (2) In the test script (:10), insert `packages/notifications/test/*.test.js ` immediately after `packages/hrx/test/*.test.js ` and before `packages/persistence/test/*.test.js`.
   RISK: low — notifications tests verified green standalone (2/2)

-- AFFECTED TESTS/GATES --
* apps/api/test/master-data-api.test.js:42-55 — health field assertions (additive fields pass; re-run to confirm)
* apps/api/test/session-auth-api.test.js:77-85 — api-auth health descriptor (unaffected, re-run)
* ~15 other apps/api/test/*.test.js files that GET /api/health for liveness only (cmp-r4-g4..g12, sf-b-w02..w08, profile-api) — status-only assertions
* scripts/run-lcx-vltui-production-smoke.mjs:257-263 and scripts/smoke-hrx-production.mjs:103-104 — health status/context-membership checks (unaffected)
* npm run (whatever wraps) scripts/validate-sf-client-matter-parity-crosswalk.mjs — will FAIL between AdminSurface deletion and the :1430-1433 edit unless same commit
* apps/web/test/ui-regression.test.mjs — :116,129-131 i18n string assertions (keep project key); :1166-1168 AuthSurface forbidden-copy list; :1171-1186 product-copy scan covers i18n.js + all remaining src/components files including new preview copy
* scripts/validate-lcx-full-guarded-ui.mjs:26-56 + apps/web/test/lcx-full-guarded-ui.test.mjs — require GuardedStateNotice/data-global-audit-required/data-global-decision-required strings to remain in GlobalUtilitySurface.jsx
* scripts/validate-hrx-ui-api-backed.mjs:133-136 — data-global-live-hr-documents and catalog strings must remain
* scripts/validate-lcx-global-ia.mjs:23-60 — utility id arrays and section labels (untouched by the UtilityDetail addition)
* root `npm test` (package.json:10) — gains packages/notifications/test/service.test.js (verified 2/2 pass standalone)
* apps/web `npm run test:ui` (node --test test/*.test.mjs) — covers ui-regression and guarded-ui tests above
* new `typecheck:web` — expected initial baseline: exactly 7 errors with the specified tsconfig (verified by direct trial run in this worktree)

-- RISKS --
! Commit-ordering coupling: deleting AdminSurface.jsx without the same-commit removal of validate-sf-client-matter-parity-crosswalk.mjs:1430-1433 makes that validator throw on read().
! tsconfig knob drift: the 7-error baseline holds ONLY with strict:false + allowImportingTsExtensions:true + types:[] + skipLibCheck:true + checkJs:false; strict:true produces 100+ errors (TS5097/TS7006/TS2345 flood). The baseline also rests on the hand-rolled apps/web/src/react-jsx.d.ts stubs since @types/react is absent.
! New visible copy (signup preview note, global utility preview notice) is scanned by ui-regression.test.mjs forbiddenVisibleCopy (:1181) and the AuthSurface-specific forbid list (:1167) — Korean 미리보기 wording is safe; avoid English terms like 'demo', 'API', 'preview_ready', 'Client Portal', 'Billing', 'DMS'.
! runtime_profile has no existing source of truth (no env var, no config); the default string ('local_dev' vs other naming) should be ratified once and mirrored anywhere docs/ops reference it — grep confirms nothing else consumes it yet.
! i18n comma hygiene: themeTitle is currently the last entry of each locale block with no trailing comma; after pruning, the new last entry keeps a trailing comma (valid JS) or Codex trims it — either fine, but a syntax slip here breaks every web test since App.jsx imports i18n at module top.
! The health handler runs BEFORE the 405 method guard only for GET-shaped flow; the new fields are computed per-request via process.env — negligible cost, but keep the fields out of the frozen SERVICE_DESCRIPTOR to avoid freezing env state at import time in tests.

-- OPEN ISSUES --
? Three MORE zero-importer orphan surfaces exist outside decision E scope: apps/web/src/components/AskSurface.jsx, AnalyticsSurface.jsx, ProfilesSurface.jsx (no importers anywhere in apps/web/src). Their i18n keys (askTitle, builderTitle, resultTable, eventStream, rawEvent, profileTitle) must be KEPT in this pass; a follow-up decision could delete them and prune ~7 more keys.
? The `ask` key (i18n.js ko:37/en:110) is used nowhere at all but nominally belongs to the out-of-scope AskSurface family — left out of the prune list for scope discipline; include it if the AskSurface follow-up lands.
? Several never-used legacy keys unrelated to any surface remain (recent, favorites, spaces, upgrade, auth, readiness, ops, intake, finance, profiles, analytics, dark, theme?) — not audited to completion here because they are outside the ratified decision; a dedicated dead-key sweep would need the same labels-grep method.
? packages/data-cloud, import-data, record-actions, reports have src but no test directories — not a glob gap today (node --test would error on empty globs), but a coverage gap worth a future pack.