# 07. 통합 실행 체크리스트 — 4개 워크스트림 합본

실행 주체: Codex 세션(또는 개발팀). 라이브 큐(CP 팩 진행)와 병행한다 — 본 패키지의 신규 경로는 전부 validator 비독해 안전 지대이므로 큐와 충돌하지 않지만, **각 커밋은 quiet window(Close CP00-xxx 직후)에 배치**한다(UI 트랙 규약과 동일 원칙).

## 0. 실행 전 필수 확인 (매 세션)

- [ ] `docs/closeout-pack-plan/next-pack-queue.json` + `git log --oneline -5`로 라이브 커서 재확인 — 본 패키지 내 모든 커서 수치는 스냅샷이다 (설계 시점 CP00-323 → 검증 시점 이미 CP00-326/RP10.P04, 하루 ~3팩 속도). **모든 경계 상수·스냅샷은 커밋 직전 라이브 값으로 재해석** (검증 반영)
- [ ] `git status` 클린 여부 — dirty worktree 프로토콜 준수
- [ ] 본 패키지 문서 00 §3의 불가침 8항 숙지

## 1. 권고 랜딩 순서 (검증 반영 — 임의 병렬 금지)

통합 검증이 워크스트림 간 의존(문서 11 이중 추출, 런타임 어휘 공유, 공유 파일 3종)을 적발해 **랜딩 순서를 명문화**했다:

| 순서 | 작업 | 선결 조건 | 출처 |
|---|---|---|---|
| 0 | MAT-DEC-05(소스 동결) 소유자 확인 — 전 트랙 공통 | — | 문서 06 |
| 1 | **마스터 트랙 Phase 0~4**: 용어집 → 통합 계획 → 소스 인덱스 → 요구 추출 → 커버리지·앵커맵 → 5축 adjudication·overlay | 결정 불요 | 문서 02 A-0~A-7 |
| 2 | 마스터 트랙 Phase 5~6: C-MPACK-01~04 리뷰 + 리스크 레지스터·전략 선언문 | Phase 4 완료 | 문서 02 A-8·B·C |
| 3 | **게이트 레이어** 전체 (③~⑧) | **MAT-DEC-07 결정 기록 동반**(착수 승인 = 결정) | 문서 05 |
| 4 | **M365** Stage 1~2 (마스터 트랙 부속 모듈 형태, `MAT-REQ-M365-*`) + C-MPACK-M365-01/02 | 순서 1 완료(추출 단일화) | 문서 04 |
| 5 | **HRX planning** W1~W2 + C-HRX-01~04 | **마스터 Phase 4 완료**(문서 11 이중 판정 방지) | 문서 03 |
| 6 | C-MPACK-05(최종 정합 리뷰) + 거버넌스 가산 개정 | 1~5 완료 | 문서 02 Phase 7 |

**공유 파일 3종 단일 편집 규칙 (검증 반영)**: `risk-classification-rules.md`(HRX 3건+마스터 3건 append), `latest-total-closeout-execution-plan.md`(HRX 단락+마스터 절+게이트 레이어 §3.3 단락), `scripts/progress-control-room-data.mjs`(HRX 분기+게이트 레이어 블록) — 위 랜딩 순서대로만 편집하고, 각 편집 전 직전 트랙의 머지 완료를 확인한다.

## 2. 결정 관문 후 작업

| 관문 | 해제되는 작업 |
|---|---|
| MAT-DEC-02 + MAT-DEC-06(a~c) | HRX W4·W5: 요구 ledger 동결 → weighted ledger(901 검산) → plan 편입 → 재생성 → 전수 sweep → 커밋 2 |
| MAT-DEC-07 | 게이트 레이어 ⑥~⑧: 기존 검증기 가산 확장(전/후 sweep diff=0 증명) → 대시보드 → 커밋. 이후 경계 뒤 첫 팩부터 `implementation_layer` 기입 |
| MAT-DEC-03 (+01·08) | M365 admission boolean 갱신 커밋 → (미래) 승인 확장 팩에서 런타임 착수 |
| C-MPACK-05 PASS | 흡수 트랙 Phase 7: risk rules·execution plan 가산 개정 + 최종 커밋 |

## 3. 전체 게이트 목록 (커밋 전 전부 green)

기존 (무변화 통과가 비약화 증명):
- [ ] `npm test` (11개 패키지 + scripts/test)
- [ ] `npm run validate` / `npm run contracts:validate`
- [ ] `npm run closeout-pack:validate` **전수 sweep — 확장 전/후 출력 diff 0** (게이트 레이어 ⑥)
- [ ] `npm run closeout-pack-plan:validate`
- [ ] 전 `rpXX:*:validate` (rp00~rp10 현행분)
- [ ] `goal1:foundation:validate` (54355/227 불변 자동 확인)

신규 (해당 워크스트림 완료 시):
- [ ] `hrx:requirements:validate` / `hrx:weighted:validate` (901 정확 일치) / `rp30:validate`
- [ ] `rp08:m365-runtime:validate` (기존 no-write 파수꾼 겸직)
- [ ] `runtime-readiness:contract:validate` / `implementation-layer:validate` / `runtime-readiness:validate`
- [ ] Claude 리뷰 전건 PASS/PASS_WITH_FINDINGS + p0/p1 finding 0 (무효 시도는 invalid-attempt로 영구 보존)

## 4. 커밋 시퀀스 (검증 반영 랜딩 순서, 전부 quiet window)

1. `Add matter-pack planning gate artifacts (planning-only)` — 마스터 트랙 A 전체
2. `docs(planning): unified product risk register and strategy declaration (planning-only)`
3. — [MAT-DEC-07 결정 기록 동반] `docs(governance): runtime gate layer policy and CG-to-RTG translation (additive)`
4. `feat(governance): runtime-readiness contract and implementation-layer ledger (derived, no closed-pack edits)`
5. `feat(governance): runtime-readiness validator; boundary-gated additive checks in validate-closeout-pack (pre/post full-sweep diff clean)` + `chore(repo): register runtime-readiness and implementation-layer npm scripts; control-room layer split`
6. `docs(planning): matter-pack m365 overlay artifacts for rp08 runtime decisions (planning-only)` — `docs/matter-pack-integration/m365/`
7. `Add email-dms m365 runtime decision contract artifacts (decision-record-only, additive)`
8. `Add hrx integration planning gate artifacts` — 마스터 Phase 4 이후
9. — [MAT-DEC-02·06 결정 후] `Add RP30 HRX people-hr-evidence source ledgers and extend closeout pack plan (additive, 55256 unchanged)`
10. `docs(governance): additive matter-pack overlay clauses in risk rules and execution plan` — 공유 파일 마지막 단일 편집

공통 규칙: `Close CP00-xxx` 형식은 팩 닫기에만 — 본 패키지 커밋에 사용 금지. 본문에 승인 주체·검증 커맨드 목록·"closed packs untouched" 명기. `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

## 5. 금지사항 (요약 — 상세는 문서 00 §3)

- 기존 검증기의 검사 삭제·완화, plan_totals·boundary·enum·경계 상수 변경
- 닫힌 팩 manifest 수정, 팩 리넘버링
- `contracts/email-dms-core-contract.json` 등 기존 계약·deepEqual 쌍·rpXX 마이크로페이즈 수정
- LDIP/V2 동결 산출물 수정
- 메인 ledger(weighted/spec/full-spec)에 HRX 합산 (별도 파일로만)
- MAT-DEC 결정을 코드·문서에서 기본값으로 선결하는 서술
- Claude를 최종 승인자로 만드는 어떤 구조 (리뷰는 read-only 게이트)

## 6. 완료 정의

- **planning gate passed**: 트랙 산출물 전체 + 리뷰 전건 PASS — 사람 결정 없이 도달 가능
- **registration complete**: HRX 901이 plan에 편입되고 전 게이트 green (MAT-DEC-02·06 후)
- **runtime layer live**: 경계 이후 신규 팩이 implementation_layer를 기입하기 시작 (MAT-DEC-07 후)
- **implementation admitted**: M365 등 런타임 구현 착수 — MAT-DEC-01·03 + 사용자 승인 ledger/pack 확장 후에만. **이 패키지의 범위는 여기까지이며 구현 자체는 일반 CP 팩 사이클로 진행한다.**
