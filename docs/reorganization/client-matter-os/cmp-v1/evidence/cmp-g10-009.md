# CMP-G10-W10-T009 Evidence - DataRoom projection sync

Status: implemented

Runtime evidence:
- `syncDataRoomProjection` writes metadata-only `DataRoomProjection` rows.
- Projection sync requires inherited DMS ACL and external ACL application.
- API tests verify projection persistence and audit after restart.

Boundary:
- Projection sync does not expose document bytes.
