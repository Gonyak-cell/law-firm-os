# CMP-G10-W10-T015 Evidence - G10 validator

Status: implemented

Runtime evidence:
- `scripts/validate-cmp-r4-g10.mjs` checks runtime source, API, UI, tests, evidence, and no premature production claim.
- `package.json` exposes `client-matter:cmp-v1:g10:validate`.

Boundary:
- Validator pass is implementation evidence only.
