# CMP-G10-W10-T014 Evidence - Data room UI

Status: implemented

Runtime evidence:
- `PortalSurface` renders `/api/data-room/projections`.
- Projection rows show document bytes as omitted.
- UI regression tests assert `fetchDataRoomProjections` usage.

Boundary:
- Data room UI does not expose object storage bytes.
