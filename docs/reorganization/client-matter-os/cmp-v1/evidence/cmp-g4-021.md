# CMP-G4-W04-T021 Evidence

Status: runtime-slice-implemented

## Task

G4 R4 validator

## Evidence

- `scripts/validate-cmp-r4-g4.mjs` checks required G4 runtime target files, focused tests, evidence artifacts, MatterTeam employee-id guard markers, idempotency, audit, API route markers, UI live markers, and claim-boundary markers.
- `package.json` exposes `client-matter:cmp-v1:g4:validate`.

## Verification

- `npm run client-matter:cmp-v1:g4:validate`

## Claim Boundary

CMP-G4 slice evidence only. The validator does not claim whole-package R4 readiness.
