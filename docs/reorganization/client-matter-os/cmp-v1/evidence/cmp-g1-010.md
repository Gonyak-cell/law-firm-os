# CMP-G1-W01-T010 Evidence

Status: runtime-slice-implemented
Source package date: 2026-06-20
Current repo ref: codex/hrx-release-go-no-go-package

## Task

Durable audit store

## Acceptance Boundary

Durable audit events are written to the file-backed SQL-style HRX store, tenant exports are scoped, and hash-chain verification detects tampered evidence rows. This evidence does not claim enterprise audit readiness across every CMP bounded context.

## Implementation Evidence

- `packages/audit/src/durable-audit-store.js` wraps the SQL HRX audit store with `exportTenant` and `verifyTenant`.
- `packages/audit/src/index.js` exports the durable audit store.
- `apps/api/src/hrx-runtime-context.js` uses `createDurableAuditStore({ store })` whenever the HRX runtime is store-backed.
- `scripts/audit-cmp-r4-blockers.mjs` now checks for the durable audit store facade and hash-chain verification capability.

## Test Evidence

- `node --test packages/audit/test/durable-audit-store.test.js packages/audit/test/hrx-event-store-sql.test.js`
- `node --test apps/api/test/hrx-durable-runtime.test.js apps/api/test/hrx-runtime-api.test.js apps/api/test/hrx/tenant-isolation.test.js`
- `node --test apps/api/test/cmp-r4-g1-trust.test.js`
- `npm run client-matter:cmp-v1:g1:validate`
- `npm run client-matter:cmp-v1:blockers`

## Claim Boundary

- R4 runtime-write-ready for the whole CMP R4 package: not claimed.
- G1 TUW evidence-linked: yes.
- R5/R6 owner-decision-ready: not claimed.
- Go-live or production-ready: blocked until owner approval and release gates pass.
