# CMP-G1-W01-T014 Evidence - Idempotency Store Runtime

Status: runtime-slice-implemented
Source package date: 2026-06-20
Current repo ref: codex/hrx-release-go-no-go-package

## Task

Idempotency store runtime

## Acceptance Boundary

Runtime writes can store and retrieve tenant-scoped idempotency keys through a durable trust store adapter. This evidence covers the G1 idempotency runtime slice only.

## Implementation Evidence

- `packages/platform/src/persistence/idempotency-store.js` exposes `createIdempotencyStore`.
- The store delegates to `recordIdempotency` and `getIdempotency` from the trust runtime store.
- `packages/authz/test/trust-runtime-stores.test.js` verifies idempotency save/get behavior.

## Test Evidence

- `node --test packages/authz/test/trust-runtime-stores.test.js`
- `npm run client-matter:cmp-v1:g1:validate`

## Claim Boundary

- G1 TUW evidence-linked: yes.
- R4 runtime-write-ready for the whole CMP R4 package: not claimed.
- R5/R6 owner-decision-ready: not claimed.
- Go-live or production-ready: blocked until owner approval and release gates pass.
