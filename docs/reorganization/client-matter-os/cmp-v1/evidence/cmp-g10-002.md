# CMP-G10-W10-T002 Evidence - Portal projection runtime

Status: implemented

Runtime evidence:
- `createPortalProjection` writes `PortalProjection` rows with visible object references.
- Projection writes require inherited DMS ACL.
- `/api/portal/projections` exposes permission-gated projection reads.

Boundary:
- Projection metadata is runtime-ready; external go-live remains blocked.
