# CMP-G10-W10-T008 Evidence - DataRoom runtime

Status: implemented

Runtime evidence:
- `packages/data-room/src/data-room-runtime-service.js` writes `DataRoom` records.
- Data rooms require an external ACL boundary.
- `/api/data-room/rooms` exposes permission-gated reads.

Boundary:
- Data rooms remain synthetic until external owner approval.
