# CMP-G1-W01-T002 Evidence - Tenant Context Middleware

Status: runtime-slice-implemented
Source package date: 2026-06-20
Current repo ref: codex/hrx-release-go-no-go-package

## Task

Tenant context middleware

## Acceptance Boundary

Tenant context is parsed from API headers, missing tenant context fails closed, and downstream request context cannot be built without a tenant. This evidence covers the G1 tenant boundary slice only.

## Implementation Evidence

- `apps/api/src/middleware/tenant-context.js` defines `parseTenantContext` and `requireTenantContext`.
- Missing tenant context returns `fail_closed: true` with a safe error code.
- `apps/api/test/cmp-r4-g1-trust.test.js` asserts fail-closed behavior and valid tenant composition.

## Test Evidence

- `node --test apps/api/test/cmp-r4-g1-trust.test.js`
- `npm run client-matter:cmp-v1:g1:validate`

## Claim Boundary

- G1 TUW evidence-linked: yes.
- R4 runtime-write-ready for the whole CMP R4 package: not claimed.
- R5/R6 owner-decision-ready: not claimed.
- Go-live or production-ready: blocked until owner approval and release gates pass.
