# CMP-G4-W04-T018 Evidence

Status: runtime-slice-implemented

## Task

Matter home UI

## Evidence

- `apps/web/src/components/MattersSurface.jsx` adds a routed Matter home with `data-cmp-g4-live-matters="true"`.
- The table displays Matter, status, documents, team count, WIP, risk, and release-gate status from `/api/matters`.
- `apps/web/test/ui-regression.test.mjs` checks routing and live Matter Core API markers.

## Verification

- `npm --workspace apps/web run test:ui`
- `npm --workspace apps/web run build`

## Claim Boundary

CMP-G4 slice evidence only. The UI explicitly keeps production-ready claim gated.
