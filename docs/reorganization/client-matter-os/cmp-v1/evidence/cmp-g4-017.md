# CMP-G4-W04-T017 Evidence

Status: runtime-slice-implemented

## Task

Matter API contracts

## Evidence

- `apps/api/src/matter-runtime-context.js` implements `/api/matters`, `/api/matters/:matter_id`, `/api/matters/openings`, `/api/matters/:matter_id/team-members`, and `/api/matters/audit`.
- `apps/api/src/server.js` exposes the Matter bounded context and routes Matter requests through the permission context header.
- `apps/api/test/cmp-r4-g4-matter.test.js` covers list, denied, review, write, idempotent replay, persistence restart, team write, and audit behavior.

## Verification

- `node --test apps/api/test/cmp-r4-g4-matter.test.js`
- `npm run api:test`

## Claim Boundary

CMP-G4 slice evidence only. API runtime-write-ready is limited to G4 and does not claim production readiness.
