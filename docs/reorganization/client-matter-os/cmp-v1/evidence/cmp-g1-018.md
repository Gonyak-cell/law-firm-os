# CMP-G1-W01-T018 Evidence - Audit Verify Job

Status: runtime-slice-implemented
Source package date: 2026-06-20
Current repo ref: codex/hrx-release-go-no-go-package

## Task

Audit verify job

## Acceptance Boundary

Audit verification runs tenant-scoped hash-chain verification and normalizes boolean verifier output into evidence fields. This evidence covers the G1 audit-verify job slice only.

## Implementation Evidence

- `packages/audit/src/verify-job.js` exposes `runAuditVerifyJob`.
- The job calls `auditStore.verifyTenant({ tenant_id })` when available and returns `audit_verify_job: true`.
- `apps/api/test/cmp-r4-g1-trust.test.js` verifies tenant-scoped invocation and boolean normalization.

## Test Evidence

- `node --test apps/api/test/cmp-r4-g1-trust.test.js`
- `npm run client-matter:cmp-v1:g1:validate`

## Claim Boundary

- G1 TUW evidence-linked: yes.
- R4 runtime-write-ready for the whole CMP R4 package: not claimed.
- R5/R6 owner-decision-ready: not claimed.
- Go-live or production-ready: blocked until owner approval and release gates pass.
