# CMP-G6-W06-T020 Evidence

- TUW: Intake/conflict UI.
- UI: `apps/web/src/components/IntakeSurface.jsx` renders live opportunity and intake queues.
- API-backed: `fetchCrmOpportunities` and `fetchIntakeRequests` call relative API routes with permission context.
- Tests: UI regression asserts route, component, and no mock fallback.
- Claim boundary: UI states R4 runtime write-ready only.
