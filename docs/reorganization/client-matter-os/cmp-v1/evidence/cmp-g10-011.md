# CMP-G10-W10-T011 Evidence - Client dashboard projection

Status: implemented

Runtime evidence:
- `createPortalDashboardProjection` writes aggregate-only dashboard projections.
- `/api/portal/dashboard` returns metadata-only aggregate rows.
- `PortalSurface` renders the dashboard from the API.

Boundary:
- Dashboard projection is safe aggregate evidence only.
