# CMP-G1-W01-T001 Evidence

Status: runtime-slice-implemented
Source package date: 2026-06-20
Current repo ref: codex/hrx-release-go-no-go-package

## Task

Persistence substrate repository interface

## Acceptance Boundary

DB/file-backed adapter exists; runtime gate rejects Map/non-durable stores; API default HRX runtime uses durable store, migrations, and seed before accepting write routes. This evidence does not close the full G1 gate or claim R4 product readiness.

## Implementation Evidence

- `packages/platform/src/persistence/store-port.js` defines the platform runtime persistence port and rejects Map/non-durable stores.
- `packages/platform/src/index.js` exports the persistence port.
- `apps/api/src/server.js` creates the default HRX runtime through `createFileHrxStore`, `runHrxMigrations`, `assertRuntimePersistenceStore`, `seedHrxDurableRuntimeStore`, then `createHrxRuntimeContext({ store })`.
- `apps/api/src/hrx-runtime-context.js` exposes idempotent durable seed rows for Employee, EmploymentProfile, EmployeeUserLink, HRDocument metadata, LeaveBalanceEntry, and LeaveRequest.

## Test Evidence

- `node --test packages/platform/test/persistence-store-port.test.js`
- `node --test apps/api/test/hrx-durable-runtime.test.js apps/api/test/hrx-runtime-api.test.js apps/api/test/hrx/tenant-isolation.test.js`
- `node --test packages/authz/test/trust-runtime-stores.test.js`
- `npm run client-matter:cmp-v1:g1:validate`
- `npm run client-matter:cmp-v1:validate`
- `npm run client-matter:cmp-v1:blockers`

## Claim Boundary

- R4 runtime-write-ready for the whole CMP R4 package: not claimed.
- G1 TUW evidence-linked: yes.
- R5/R6 owner-decision-ready: not claimed.
- Go-live or production-ready: blocked until owner approval and release gates pass.
