# CMP-G7-W07-T025 Evidence

- TUW: Finance UI live screens.
- UI: `apps/web/src/components/FinanceSurface.jsx`.
- Coverage: live time, invoice, AR aging data from `/api/finance/*`.
- Tests: UI regression asserts route, API client, and no mock fallback.
- Claim boundary: UI states runtime only, not go-live.
