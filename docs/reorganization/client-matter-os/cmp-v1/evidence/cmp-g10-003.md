# CMP-G10-W10-T003 Evidence - External ACL runtime

Status: implemented

Runtime evidence:
- `createExternalAcl` writes `ExternalAcl` rows.
- Cross-tenant external access is blocked.
- Runtime tests reject external ACL drift.

Boundary:
- ACL evidence is local R4 evidence, not an external penetration approval.
