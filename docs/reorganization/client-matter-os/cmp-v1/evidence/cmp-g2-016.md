# CMP-G2-W02-T016 Evidence

Status: runtime-slice-implemented

## Task

Party UI live integration

## Evidence

- `apps/web/src/components/ClientsSurface.jsx` adds a routed Clients surface with `data-cmp-g2-live-clients="true"` for live mode.
- Live mode calls `fetchMasterDataRecords({ modelType: "ClientGroup" })` against `/master-data/records` and renders loading, empty, denied, and review-required states without a local fallback.
- `apps/web/src/data/apiClient.js` now supports `model_type`, filters, permission refs, audit hint refs, and fail-closed unexpected response handling.
- `apps/web/src/App.jsx`, `apps/web/src/data/nav.js`, `apps/web/src/i18n.js`, and `apps/web/src/components/Shell.jsx` route the Clients surface into the app shell.
- `apps/web/test/ui-regression.test.mjs` asserts the Clients route, live API binding, no-fallback contract, and required UI state markers.

## Verification

- `npm --workspace apps/web run test:ui`
- `npm --workspace apps/web run build`
- `npm run client-matter:cmp-v1:g2:validate`

## Claim Boundary

G2 runtime slices are closed for this package level only. Whole-package R4 runtime-write-ready, R5/R6 owner-decision-ready, go-live, and production-ready status are not claimed.
