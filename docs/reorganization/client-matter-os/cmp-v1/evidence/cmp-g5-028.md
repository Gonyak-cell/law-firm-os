# CMP-G5-W05-T028 Evidence

Status: runtime-slice-implemented

## Task

Matter vault UI

## Evidence

- `apps/web/src/components/VaultSurface.jsx` adds `data-cmp-g5-vault-surface="true"` and reads live `/api/vault/documents`.
- The UI displays legal hold and privilege metadata while keeping raw storage hidden.

## Verification

- `npm --workspace apps/web run test:ui`
- `npm --workspace apps/web run build`

## Claim Boundary

CMP-G5 UI evidence only. No production-ready claim is made.
