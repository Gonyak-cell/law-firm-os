# CMP-G10-W10-T013 Evidence - Portal UI live screens

Status: implemented

Runtime evidence:
- `apps/web/src/components/PortalSurface.jsx` renders API-backed dashboard, RFI, and boundary panels.
- App routing, navigation, i18n, and sidebar include `portal`.
- UI regression tests assert the route and API client usage.

Boundary:
- UI screen is local runtime evidence, not external client availability.
