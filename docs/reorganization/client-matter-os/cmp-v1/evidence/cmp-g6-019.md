# CMP-G6-W06-T019 Evidence

- TUW: CRM/Intake API contracts.
- API: `apps/api/src/crm-intake-runtime-context.js` implements `/api/crm/*` and `/api/intake/*` handlers.
- Permission: all handlers require tenant, permission ref, audit hint, and permission-context header.
- Tests: API tests cover health, list, write, handoff, conflict, token, audit, and denied states.
- Claim boundary: no production-ready claim in API responses.
