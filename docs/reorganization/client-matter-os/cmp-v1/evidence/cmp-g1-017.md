# CMP-G1-W01-T017 Evidence - Audit Export API

Status: runtime-slice-implemented
Source package date: 2026-06-20
Current repo ref: codex/hrx-release-go-no-go-package

## Task

Audit export API

## Acceptance Boundary

Tenant-scoped audit exports use the durable audit-store API shape and exclude customer payload material. This evidence covers the G1 audit-export helper slice only.

## Implementation Evidence

- `apps/api/src/routes/audit.js` exposes `createAuditExportResponse`.
- The helper calls `auditStore.exportTenant({ tenant_id })` when available and records `customer_payload_included: false`.
- `apps/api/test/cmp-r4-g1-trust.test.js` verifies tenant-scoped export invocation and claim-boundary flags.

## Test Evidence

- `node --test apps/api/test/cmp-r4-g1-trust.test.js`
- `npm run client-matter:cmp-v1:g1:validate`

## Claim Boundary

- G1 TUW evidence-linked: yes.
- R4 runtime-write-ready for the whole CMP R4 package: not claimed.
- R5/R6 owner-decision-ready: not claimed.
- Go-live or production-ready: blocked until owner approval and release gates pass.
