# CMP-G3-W03-T021 Evidence

Status: runtime-slice-implemented

## Task

People UI live screens

## Evidence

- `apps/web/src/people/PeopleHome.tsx` is marked `data-hrx-api-backed="true"` and renders API-backed HRX modules without a static fallback.
- `apps/web/src/people/hrxApiClient.ts` calls `/api/hrx/*` routes.
- `apps/web/test/ui-regression.test.mjs` asserts the People runtime route and API-backed contract.

## Verification

- `npm --workspace apps/web run test:ui`
- `npm --workspace apps/web run build`

## Claim Boundary

CMP-G3 slice evidence only. Whole-package R4 runtime-write-ready, R5/R6 owner-decision-ready, go-live, and production-ready status are not claimed.
