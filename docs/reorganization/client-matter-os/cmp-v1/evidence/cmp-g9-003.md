# CMP-G9-W09-T003 Evidence - Permission-before-AI service

Status: implemented

Runtime evidence:
- `assertPermissionBeforeAi` excludes unauthorized documents before retrieval is persisted.
- Privileged source documents must carry inherited privilege labels before entering AI retrieval.
- API tests verify unauthorized omission and count-leak-safe blocked responses.

Boundary:
- Permission-before-AI is enforced at runtime; owner approval remains required before launch claims.
