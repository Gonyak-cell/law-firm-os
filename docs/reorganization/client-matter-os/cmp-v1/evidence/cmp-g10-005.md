# CMP-G10-W10-T005 Evidence - RFIResponse upload flow

Status: implemented

Runtime evidence:
- `createRfiResponse` writes `RfiResponse` rows.
- Uploads require malware scan pass and inherited DMS ACL.
- API tests verify metadata-only RFI response persistence across restart.

Boundary:
- File bytes and storage pointers are omitted from API responses.
