# CMP-G1-W01-T003 Evidence - Actor Context Middleware

Status: runtime-slice-implemented
Source package date: 2026-06-20
Current repo ref: codex/hrx-release-go-no-go-package

## Task

Actor context middleware

## Acceptance Boundary

Actor identity is parsed from API headers, missing actor context fails closed, and tenant plus actor are composed before protected runtime operations. This evidence covers the G1 actor boundary slice only.

## Implementation Evidence

- `apps/api/src/middleware/actor-context.js` defines `parseActorContext`, `requireActorContext`, and `buildHrxRequestContext`.
- Missing actor context emits `HRX_ACTOR_CONTEXT_REQUIRED` and `fail_closed: true`.
- `apps/api/test/cmp-r4-g1-trust.test.js` asserts actor fail-closed behavior and composed request context.

## Test Evidence

- `node --test apps/api/test/cmp-r4-g1-trust.test.js`
- `npm run client-matter:cmp-v1:g1:validate`

## Claim Boundary

- G1 TUW evidence-linked: yes.
- R4 runtime-write-ready for the whole CMP R4 package: not claimed.
- R5/R6 owner-decision-ready: not claimed.
- Go-live or production-ready: blocked until owner approval and release gates pass.
