# CMP-G4-W04-T020 Evidence

Status: runtime-slice-implemented

## Task

Matter team UI

## Evidence

- `apps/web/src/components/MatterTeamRoster.jsx` adds `data-cmp-g4-team-roster="true"` and posts employee-backed team members through `addMatterTeamMember`.
- The UI carries `employee_id`, `user_id`, role, permission ref, audit hint ref, and actor id.
- API tests verify user_id-only members are blocked and valid employee-backed members are audited.

## Verification

- `npm --workspace apps/web run test:ui`
- `npm --workspace apps/web run build`
- `node --test apps/api/test/cmp-r4-g4-matter.test.js`

## Claim Boundary

CMP-G4 slice evidence only. No production-ready claim is made.
