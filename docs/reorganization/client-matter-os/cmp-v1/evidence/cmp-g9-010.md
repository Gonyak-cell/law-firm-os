# CMP-G9-W09-T010 Evidence - Workflow builder UI

Status: implemented

Runtime evidence:
- `apps/web/src/components/AskSurface.jsx` includes an AI runtime panel for review queue visibility.
- UI regression tests assert the AI panel is API-backed through `fetchAiReviewQueue`.
- The panel labels permission-before-AI and human-review safety boundaries.

Boundary:
- The R4 UI exposes review state; full workflow authoring remains owner-decision-ready for R5/R6.
