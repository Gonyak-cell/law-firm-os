# CMP-G9-W09-T002 Evidence - RetrievalRequest runtime

Status: implemented

Runtime evidence:
- `packages/ai-governance/src/retrieval-service.js` writes `RetrievalRequest` records with source refs and retrieved document ids.
- `apps/api/src/ai-runtime-context.js` exposes `POST /api/ai/retrieval`.
- API and runtime tests cover persistence, idempotency, and audit event creation.

Boundary:
- Retrieval remains permission-scoped and synthetic in this R4 package.
