# CMP-G4-W04-T019 Evidence

Status: runtime-slice-implemented

## Task

Matter opening wizard UI

## Evidence

- `apps/web/src/components/MatterOpeningWizard.jsx` adds `data-cmp-g4-opening-wizard="true"` and posts to `/api/matters/openings` through `createMatterOpening`.
- The request carries clearance token evidence, permission ref, audit hint ref, actor id, idempotency key, Matter number seed, and Matter payload.
- UI regression tests verify the wizard is API-backed.

## Verification

- `npm --workspace apps/web run test:ui`
- `npm --workspace apps/web run build`

## Claim Boundary

CMP-G4 slice evidence only. Owner approval and release gates remain outside this UI claim.
