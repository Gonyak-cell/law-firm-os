# CMP-G1-W01-T005 Evidence - Policy Store Runtime

Status: runtime-slice-implemented
Source package date: 2026-06-20
Current repo ref: codex/hrx-release-go-no-go-package

## Task

Policy store runtime

## Acceptance Boundary

Policies are written to durable trust runtime storage with audit and idempotency metadata. This evidence covers the G1 policy-store slice only.

## Implementation Evidence

- `packages/authz/src/policy-store.js` exposes `createPolicyStore` and `savePolicy`.
- Policy records are normalized to `record_type: "Policy"` and persisted via the trust runtime store.
- `packages/authz/test/trust-runtime-stores.test.js` verifies policy writes include product-state, audit, and claim-boundary flags.

## Test Evidence

- `node --test packages/authz/test/trust-runtime-stores.test.js`
- `npm run client-matter:cmp-v1:g1:validate`

## Claim Boundary

- G1 TUW evidence-linked: yes.
- R4 runtime-write-ready for the whole CMP R4 package: not claimed.
- R5/R6 owner-decision-ready: not claimed.
- Go-live or production-ready: blocked until owner approval and release gates pass.
