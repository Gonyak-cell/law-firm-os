# CMP-G10-W10-T004 Evidence - RFIRequest runtime

Status: implemented

Runtime evidence:
- `createRfiRequest` writes `RfiRequest` rows for external users and matters.
- `GET /api/portal/rfi` returns permission-gated RFI rows.
- Seeded G10 RFI data powers the portal UI.

Boundary:
- RFI runtime is synthetic and not production-client enabled.
