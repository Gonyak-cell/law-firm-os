# CMP-G2-W02-T001 Evidence

Status: runtime-slice-implemented
Source package date: 2026-06-20
Current repo ref: codex/hrx-release-go-no-go-package

## Task

Party repository and migrations

## Acceptance Boundary

Party/Master Data create, read, and update operations persist through a file-backed repository; API reads come from repository runtime state instead of direct fixture globals. This evidence covers the G2 repository substrate only and does not close the full Party & Client Master Runtime gate.

## Implementation Evidence

- `packages/master-data/src/repository.js` implements `createMasterDataRepository`, durable JSON file persistence, create/read/update/list, idempotent seed, and snapshot/close.
- `packages/master-data/src/migrations/index.js` publishes the repository schema migration manifest.
- `packages/master-data/src/index.js` exports the repository and migration manifest.
- `apps/api/src/master-data-context.js` now exposes `createMasterDataRuntimeContext` and reads records/client groups through `runtime.repository`.
- `apps/api/src/server.js` creates the default Master Data runtime with `createMasterDataRepository({ filePath, seedRecords })` and supports `LAWOS_MASTER_DATA_STORE_PATH`.
- `scripts/audit-cmp-r4-blockers.mjs` now positively checks repository-backed Master Data runtime reads and fails on descriptor/synthetic markers in the API context.

## Test Evidence

- `npm --workspace packages/master-data run test`
- `node --test apps/api/test/master-data-runtime.test.js apps/api/test/master-data-api.test.js`
- `npm run api:test`
- `npm run client-matter:cmp-v1:blockers`
- `npm run client-matter:cmp-v1:validate`

## Claim Boundary

- G2 full closeout: not claimed.
- R4 runtime-write-ready for the whole CMP R4 package: not claimed.
- R5/R6 owner-decision-ready: not claimed.
- Go-live or production-ready: blocked until owner approval and release gates pass.
