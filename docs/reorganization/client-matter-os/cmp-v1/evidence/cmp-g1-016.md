# CMP-G1-W01-T016 Evidence - Permission Simulator Read-Only

Status: runtime-slice-implemented
Source package date: 2026-06-20
Current repo ref: codex/hrx-release-go-no-go-package

## Task

Permission simulator read-only

## Acceptance Boundary

Permission simulation evaluates real permission rules without writing product state. This evidence covers the G1 permission-simulator slice only.

## Implementation Evidence

- `apps/api/src/routes/permission-simulator.js` exposes `simulatePermissionReadOnly`.
- The helper delegates to `evaluatePermission` and returns `read_only: true` plus `writes_product_state: false`.
- `apps/api/test/cmp-r4-g1-trust.test.js` asserts simulator allow evaluation without product-state writes.

## Test Evidence

- `node --test apps/api/test/cmp-r4-g1-trust.test.js`
- `npm run client-matter:cmp-v1:g1:validate`

## Claim Boundary

- G1 TUW evidence-linked: yes.
- R4 runtime-write-ready for the whole CMP R4 package: not claimed.
- R5/R6 owner-decision-ready: not claimed.
- Go-live or production-ready: blocked until owner approval and release gates pass.
