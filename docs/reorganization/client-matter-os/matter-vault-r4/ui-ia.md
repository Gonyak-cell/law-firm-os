# Matter-Vault UI IA

Primary surface: Matter command center embeds MatterVaultPanel.

Required states: loading, empty, denied, review_required, error, metadata list, document detail, version history, email filing, legal hold, privilege label, and audit evidence.

The UI must use live API fetches and must not render raw storage paths, credential material, or document bytes.
