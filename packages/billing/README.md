# @law-firm-os/billing

RP12 "Billing And Invoicing" descriptor-only program package (Proforma, Invoice, TaxInvoice, write-down, write-off). Gates H12/C12, upstream RP11. Every pack in this package is descriptor-only: no billing, proforma, invoice, or write-down runtime, no runtime permission or audit evaluation, no state writes, no object storage, no Hermes runtime receipts, no real client data, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-364

CP00-364 bootstraps the RP12 program: the program contract (contracts/billing-core-contract.json), this package, and the rp12:billing-core:validate gate, covering the full RP12.P00 scope foundation cycle (M00-M10: scope inventory, acceptance gate definition, non-goal boundary, target file map, contract schema outline, ownership note, matter-first trace note, permission baseline note, audit baseline note, synthetic data policy, risk register row, blocked-claim rule, Hermes preflight fields, Claude review prompts, human approval note, closeout handoff, dependency list, downstream RP routing, command matrix, receipt shape) and the RP12.P01 model foundation head (M00-M02: package directory layout, primary entity identifier, tenant scope field, matter trace reference, lifecycle status enum, ownership metadata, reference relationship map, required/optional field registries, state transition map) as fourteen descriptor-only sections over 150 planned rows.

## CP00-365

CP00-365 implements descriptor-only coverage of the RP12.P01 model foundation slice: the M02 Type And Shape Definition tail (optional field registry, state transition map, validation helper, fixture model, serialization shape, public export, model unit/invalid-reference/ownership-drift tests, Hermes model summary, Claude model review prompt, closeout handoff), the full M03 Primary Implementation Slice model cycle (the twenty model rows plus documentation entry and index export check), and the M04 Secondary Workflow Slice head (package directory layout through ownership metadata) as three descriptor-only sections over 40 planned rows. The pack hands off to RP12.P01.M04.S07, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-366

CP00-366 implements descriptor-only coverage of the RP12.P01 workflow permission slice: the M04 Secondary Workflow Slice tail (reference relationship map through closeout handoff), the full M05 Permission And Audit Binding model cycle (the twenty model rows plus documentation entry and index export check), and the M06 Synthetic Fixture Set head (package directory layout through matter trace reference) as three descriptor-only sections over 40 planned rows. The pack hands off to RP12.P01.M06.S05, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-367

CP00-367 implements descriptor-only coverage that closes the RP12.P01 model foundation phase (M06 Synthetic Fixture Set tail, M07 Test And Golden Case Set, M08 Hermes Evidence Packet, M09 Claude Review Packet, M10 Closeout And Next Handoff) and opens the RP12.P02 foundation (M00 Scope Inventory, M01 Contract Draft, M02 Type And Shape Definition) as eight descriptor-only sections over 150 planned rows. The pack hands off to RP12.P02.M03.S01, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-368

CP00-368 implements descriptor-only coverage of the RP12.P02 implementation slice: the full M03 Primary Implementation Slice model cycle (the twenty model rows plus documentation entry and index export check) and the M04 Secondary Workflow Slice head (package directory layout through Hermes model summary) as two descriptor-only sections over 40 planned rows. The pack hands off to RP12.P02.M04.S19, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-369

CP00-369 implements descriptor-only coverage of the RP12.P02 permission fixture slice: the M04 Secondary Workflow Slice test tail (happy/denied/review unit tests, integration smoke case), the full M05 Permission And Audit Binding service cycle (service entrypoint contract, request normalization, tenant boundary precheck, matter trace precheck, permission precheck, and the remaining service rows), and the M06 Synthetic Fixture Set head (the first fourteen service rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP12.P02.M06.S15, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-370

CP00-370 implements descriptor-only coverage of the RP12.P02 fixture test slice: the M06 Synthetic Fixture Set service tail (the eight remaining service rows) and the M07 Test And Golden Case Set head (the first two service rows) as two descriptor-only sections over 10 planned rows. The pack hands off to RP12.P02.M07.S03, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-371

CP00-371 implements descriptor-only coverage of the RP12.P02 test slice: ten consecutive M07 Test And Golden Case Set service rows as a single descriptor-only section over 10 planned rows. The pack hands off to RP12.P02.M07.S13, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-372

CP00-372 implements descriptor-only coverage of the RP12.P02 test hermes slice: the M07 Test And Golden Case Set service tail (the ten remaining service rows), the full M08 Hermes Evidence Packet service cycle (twenty-two service rows), and the M09 Claude Review Packet head (the first eight service rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP12.P02.M09.S09, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-373

CP00-373 implements descriptor-only coverage that closes the RP12.P02 phase (M09 Claude Review Packet service tail, M10 Closeout And Next Handoff) and opens the RP12.P03 foundation (M00 Scope Inventory, M01 Contract Draft, M02 Type And Shape Definition, M03 Primary Implementation Slice, M04 Secondary Workflow Slice, M05 Permission And Audit Binding, M06 Synthetic Fixture Set head) as nine descriptor-only sections over 150 planned rows. The pack hands off to RP12.P03.M06.S13, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-374

CP00-374 implements descriptor-only coverage that closes the RP12.P03 phase (M06 Synthetic Fixture Set service tail, M07 Test And Golden Case Set, M08 Hermes Evidence Packet, M09 Claude Review Packet, M10 Closeout And Next Handoff) and opens the RP12.P04 foundation (M00 Scope Inventory, M01 Contract Draft, M02 Type And Shape Definition, M03 Primary Implementation Slice head) as nine descriptor-only sections over 150 planned rows. The pack hands off to RP12.P04.M03.S21, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-375

CP00-375 implements descriptor-only coverage of the RP12.P04 workflow permission slice: the M03 Primary Implementation Slice tail (the two remaining rows), the full M04 Secondary Workflow Slice cycle (twenty-two rows), and the M05 Permission And Audit Binding head (the first sixteen rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP12.P04.M05.S17, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-376

CP00-376 implements descriptor-only coverage of the RP12.P04 permission fixture slice: the M05 Permission And Audit Binding tail (the six remaining rows) and the M06 Synthetic Fixture Set head (the first four rows) as two descriptor-only sections over 10 planned rows. The pack hands off to RP12.P04.M06.S05, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-377

CP00-377 implements descriptor-only coverage that closes the RP12.P04 phase (M06 Synthetic Fixture Set tail, M07 Test And Golden Case Set, M08 Hermes Evidence Packet, M09 Claude Review Packet, M10 Closeout And Next Handoff) and opens the RP12.P05 foundation (M00 Scope Inventory, M01 Contract Draft, M02 Type And Shape Definition, M03 Primary Implementation Slice head) as nine descriptor-only sections over 150 planned rows. The pack hands off to RP12.P05.M03.S09, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-378

CP00-378 implements descriptor-only coverage of the RP12.P05 implementation slice: ten consecutive M03 Primary Implementation Slice rows as a single descriptor-only section over 10 planned rows. The pack hands off to RP12.P05.M03.S19, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-379

CP00-379 implements descriptor-only coverage that closes the RP12.P05 phase (M03 Primary Implementation Slice tail, M04 Secondary Workflow Slice, M05 Permission And Audit Binding, M06 Synthetic Fixture Set, M07 Test And Golden Case Set, M08 Hermes Evidence Packet, M09 Claude Review Packet, M10 Closeout And Next Handoff) and opens the RP12.P06 foundation (M00 Scope Inventory head) as nine descriptor-only sections over 150 planned rows. The pack hands off to RP12.P06.M00.S07, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-380

CP00-380 implements descriptor-only coverage of the RP12.P06 foundation slice: the M00 Scope Inventory tail, the full M01 Contract Draft, M02 Type And Shape Definition, M03 Primary Implementation Slice, M04 Secondary Workflow Slice, M05 Permission And Audit Binding, and M06 Synthetic Fixture Set cycles, and the M07 Test And Golden Case Set head as eight descriptor-only sections over 150 planned rows. The pack hands off to RP12.P06.M07.S07, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-381

CP00-381 implements descriptor-only coverage of the RP12.P06 test hermes slice: the M07 Test And Golden Case Set tail (the sixteen remaining rows), the full M08 Hermes Evidence Packet cycle (twenty-two rows), and the M09 Claude Review Packet head (the first two rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP12.P06.M09.S03, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-382

CP00-382 implements descriptor-only coverage that closes the RP12.P06 phase (M09 Claude Review Packet tail, M10 Closeout And Next Handoff) and opens the RP12.P07 foundation (M00 Scope Inventory, M01 Contract Draft, M02 Type And Shape Definition, M03 Primary Implementation Slice, M04 Secondary Workflow Slice, M05 Permission And Audit Binding head) as eight descriptor-only sections over 150 planned rows. The pack hands off to RP12.P07.M05.S05, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-383

CP00-383 implements descriptor-only coverage of the RP12.P07 permission slice: ten consecutive M05 Permission And Audit Binding rows as a single descriptor-only section over 10 planned rows. The pack hands off to RP12.P07.M05.S15, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-384

CP00-384 implements descriptor-only coverage of the RP12.P07 permission fixture slice: the M05 Permission And Audit Binding tail (the eight remaining rows) and the M06 Synthetic Fixture Set head (the first two rows) as two descriptor-only sections over 10 planned rows. The pack hands off to RP12.P07.M06.S03, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-385

CP00-385 implements descriptor-only coverage that closes the RP12.P07 phase (M06 Synthetic Fixture Set tail, M07 Test And Golden Case Set, M08 Hermes Evidence Packet, M09 Claude Review Packet, M10 Closeout And Next Handoff) and opens the RP12.P08 foundation (M00 Scope Inventory, M01 Contract Draft, M02 Type And Shape Definition head) as eight descriptor-only sections over 150 planned rows. The pack hands off to RP12.P08.M02.S15, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-386

CP00-386 implements descriptor-only coverage of the RP12.P08 implementation slice: the M02 Type And Shape Definition tail (the six remaining rows), the full M03 Primary Implementation Slice cycle (twenty-two rows), and the M04 Secondary Workflow Slice head (the first twelve rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP12.P08.M04.S13, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-387

CP00-387 implements descriptor-only coverage of the RP12.P08 workflow permission slice: the M04 Secondary Workflow Slice tail (the ten remaining rows), the full M05 Permission And Audit Binding cycle (twenty-two rows), and the M06 Synthetic Fixture Set head (the first eight rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP12.P08.M06.S09, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-388

CP00-388 implements descriptor-only coverage that closes the RP12.P08 phase (M06 Synthetic Fixture Set tail, M07 Test And Golden Case Set, M08 Hermes Evidence Packet, M09 Claude Review Packet, M10 Closeout And Next Handoff) and opens the RP12.P09 foundation (M00 Scope Inventory, M01 Contract Draft, M02 Type And Shape Definition, M03 Primary Implementation Slice) as nine descriptor-only sections over 150 planned rows. The pack hands off to RP12.P09.M04.S01, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-389

CP00-389 implements descriptor-only coverage of the RP12.P09 workflow permission slice: the full M04 Secondary Workflow Slice cycle (twenty rows) and the M05 Permission And Audit Binding head (the first twenty rows) as two descriptor-only sections over 40 planned rows. The pack hands off to RP12.P09.M05.S21, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-390

CP00-390 implements descriptor-only coverage of the RP12.P09 permission fixture slice: the M05 Permission And Audit Binding tail (the two remaining rows) and the M06 Synthetic Fixture Set head (the first eight rows) as two descriptor-only sections over 10 planned rows. The pack hands off to RP12.P09.M06.S09, with no billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-391

CP00-391 implements descriptor-only coverage of the final RP12 closeout slice: the M06 Synthetic Fixture Set tail (twelve rows), the full M07 Test And Golden Case Set cycle (twenty-two rows), the M08 Hermes Evidence Packet cycle (twenty rows), the M09 Claude Review Packet cycle (twenty rows), and the M10 Closeout And Next Handoff cycle (ten rows) as five descriptor-only sections over 84 planned rows, closing the RP12 Billing And Invoicing descriptor program and handing off to RP13.P00.M00.S01. No billing, proforma, invoice, or write-down runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.
