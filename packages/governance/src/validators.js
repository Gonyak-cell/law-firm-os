import {
  GOVERNANCE_CORE_CP480_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP480_PACK_BINDING,
  GOVERNANCE_CORE_CP480_REQUIREMENTS,
  GOVERNANCE_CORE_CP481_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP481_PACK_BINDING,
  GOVERNANCE_CORE_CP481_REQUIREMENTS,
  GOVERNANCE_CORE_CP482_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP482_PACK_BINDING,
  GOVERNANCE_CORE_CP482_REQUIREMENTS,
  GOVERNANCE_CORE_CP483_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP483_PACK_BINDING,
  GOVERNANCE_CORE_CP483_REQUIREMENTS,
  GOVERNANCE_CORE_CP484_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP484_PACK_BINDING,
  GOVERNANCE_CORE_CP484_REQUIREMENTS,
  GOVERNANCE_CORE_CP485_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP485_PACK_BINDING,
  GOVERNANCE_CORE_CP485_REQUIREMENTS,
  GOVERNANCE_CORE_CP486_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP486_PACK_BINDING,
  GOVERNANCE_CORE_CP486_REQUIREMENTS,
  GOVERNANCE_CORE_CP487_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP487_PACK_BINDING,
  GOVERNANCE_CORE_CP487_REQUIREMENTS,
  GOVERNANCE_CORE_CP488_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP488_PACK_BINDING,
  GOVERNANCE_CORE_CP488_REQUIREMENTS,
  GOVERNANCE_CORE_CP489_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP489_PACK_BINDING,
  GOVERNANCE_CORE_CP489_REQUIREMENTS,
  GOVERNANCE_CORE_CP490_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP490_PACK_BINDING,
  GOVERNANCE_CORE_CP490_REQUIREMENTS,
  GOVERNANCE_CORE_CP491_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP491_PACK_BINDING,
  GOVERNANCE_CORE_CP491_REQUIREMENTS,
  GOVERNANCE_CORE_CP492_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP492_PACK_BINDING,
  GOVERNANCE_CORE_CP492_REQUIREMENTS,
  GOVERNANCE_CORE_CP493_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP493_PACK_BINDING,
  GOVERNANCE_CORE_CP493_REQUIREMENTS,
  GOVERNANCE_CORE_CP494_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP494_PACK_BINDING,
  GOVERNANCE_CORE_CP494_REQUIREMENTS,
  GOVERNANCE_CORE_CP495_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP495_PACK_BINDING,
  GOVERNANCE_CORE_CP495_REQUIREMENTS,
  GOVERNANCE_CORE_CP496_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP496_PACK_BINDING,
  GOVERNANCE_CORE_CP496_REQUIREMENTS,
  GOVERNANCE_CORE_CP497_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP497_PACK_BINDING,
  GOVERNANCE_CORE_CP497_REQUIREMENTS,
  GOVERNANCE_CORE_CP498_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP498_PACK_BINDING,
  GOVERNANCE_CORE_CP498_REQUIREMENTS,
  GOVERNANCE_CORE_CP499_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP499_PACK_BINDING,
  GOVERNANCE_CORE_CP499_REQUIREMENTS,
  GOVERNANCE_CORE_CP500_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP500_PACK_BINDING,
  GOVERNANCE_CORE_CP500_REQUIREMENTS,
  GOVERNANCE_CORE_CP501_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP501_PACK_BINDING,
  GOVERNANCE_CORE_CP501_REQUIREMENTS,
  GOVERNANCE_CORE_CP502_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP502_PACK_BINDING,
  GOVERNANCE_CORE_CP502_REQUIREMENTS,
  GOVERNANCE_CORE_CP503_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP503_PACK_BINDING,
  GOVERNANCE_CORE_CP503_REQUIREMENTS,
  GOVERNANCE_CORE_CP504_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP504_PACK_BINDING,
  GOVERNANCE_CORE_CP504_REQUIREMENTS,
  GOVERNANCE_CORE_CP505_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP505_PACK_BINDING,
  GOVERNANCE_CORE_CP505_REQUIREMENTS,
  GOVERNANCE_CORE_CP506_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP506_PACK_BINDING,
  GOVERNANCE_CORE_CP506_REQUIREMENTS,
  GOVERNANCE_CORE_CP507_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP507_PACK_BINDING,
  GOVERNANCE_CORE_CP507_REQUIREMENTS,
  GOVERNANCE_CORE_CP508_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP508_PACK_BINDING,
  GOVERNANCE_CORE_CP508_REQUIREMENTS,
  GOVERNANCE_CORE_CP509_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP509_PACK_BINDING,
  GOVERNANCE_CORE_CP509_REQUIREMENTS,
  GOVERNANCE_CORE_CP510_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP510_PACK_BINDING,
  GOVERNANCE_CORE_CP510_REQUIREMENTS,
  GOVERNANCE_CORE_CP511_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP511_PACK_BINDING,
  GOVERNANCE_CORE_CP511_REQUIREMENTS,
  GOVERNANCE_CORE_CP512_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP512_PACK_BINDING,
  GOVERNANCE_CORE_CP512_REQUIREMENTS,
  GOVERNANCE_CORE_CP513_NO_WRITE_ATTESTATION,
  GOVERNANCE_CORE_CP513_PACK_BINDING,
  GOVERNANCE_CORE_CP513_REQUIREMENTS,
} from "./registry.js";
import {
  createGovernanceCoreCp480ScopeContractFoundationCaseSet,
  createGovernanceCoreCp480ScopeContractFoundationDescriptor,
  createGovernanceCoreCp481ModelFoundationSliceCaseSet,
  createGovernanceCoreCp481ModelFoundationSliceDescriptor,
  createGovernanceCoreCp482WorkflowPermissionSliceCaseSet,
  createGovernanceCoreCp482WorkflowPermissionSliceDescriptor,
  createGovernanceCoreCp483P01CloseoutP02FoundationCaseSet,
  createGovernanceCoreCp483P01CloseoutP02FoundationDescriptor,
  createGovernanceCoreCp484P02ImplementationSliceCaseSet,
  createGovernanceCoreCp484P02ImplementationSliceDescriptor,
  createGovernanceCoreCp485P02WorkflowPermissionSliceCaseSet,
  createGovernanceCoreCp485P02WorkflowPermissionSliceDescriptor,
  createGovernanceCoreCp486P02FixtureTestSliceCaseSet,
  createGovernanceCoreCp486P02FixtureTestSliceDescriptor,
  createGovernanceCoreCp487P02TestSliceCaseSet,
  createGovernanceCoreCp487P02TestSliceDescriptor,
  createGovernanceCoreCp488P02TestHermesSliceCaseSet,
  createGovernanceCoreCp488P02TestHermesSliceDescriptor,
  createGovernanceCoreCp489P02CloseoutP03FoundationCaseSet,
  createGovernanceCoreCp489P02CloseoutP03FoundationDescriptor,
  createGovernanceCoreCp490P03CloseoutP04FoundationCaseSet,
  createGovernanceCoreCp490P03CloseoutP04FoundationDescriptor,
  createGovernanceCoreCp491P04WorkflowPermissionSliceCaseSet,
  createGovernanceCoreCp491P04WorkflowPermissionSliceDescriptor,
  createGovernanceCoreCp492P04PermissionFixtureSliceCaseSet,
  createGovernanceCoreCp492P04PermissionFixtureSliceDescriptor,
  createGovernanceCoreCp493P04CloseoutP05FoundationCaseSet,
  createGovernanceCoreCp493P04CloseoutP05FoundationDescriptor,
  createGovernanceCoreCp494P05ImplementationWorkflowSliceCaseSet,
  createGovernanceCoreCp494P05ImplementationWorkflowSliceDescriptor,
  createGovernanceCoreCp495P05PermissionSliceCaseSet,
  createGovernanceCoreCp495P05PermissionSliceDescriptor,
  createGovernanceCoreCp496P05AuditBindingSliceCaseSet,
  createGovernanceCoreCp496P05AuditBindingSliceDescriptor,
  createGovernanceCoreCp497P05FixtureSliceCaseSet,
  createGovernanceCoreCp497P05FixtureSliceDescriptor,
  createGovernanceCoreCp498P05SyntheticFixtureSliceCaseSet,
  createGovernanceCoreCp498P05SyntheticFixtureSliceDescriptor,
  createGovernanceCoreCp499P05CloseoutP06FoundationCaseSet,
  createGovernanceCoreCp499P05CloseoutP06FoundationDescriptor,
  createGovernanceCoreCp500P06ImplementationWorkflowSliceCaseSet,
  createGovernanceCoreCp500P06ImplementationWorkflowSliceDescriptor,
  createGovernanceCoreCp501P06PermissionSliceCaseSet,
  createGovernanceCoreCp501P06PermissionSliceDescriptor,
  createGovernanceCoreCp502P06PermissionFixtureSliceCaseSet,
  createGovernanceCoreCp502P06PermissionFixtureSliceDescriptor,
  createGovernanceCoreCp503P06CloseoutP07FoundationCaseSet,
  createGovernanceCoreCp503P06CloseoutP07FoundationDescriptor,
  createGovernanceCoreCp504P07FoundationSliceCaseSet,
  createGovernanceCoreCp504P07FoundationSliceDescriptor,
  createGovernanceCoreCp505P07ImplementationSliceCaseSet,
  createGovernanceCoreCp505P07ImplementationSliceDescriptor,
  createGovernanceCoreCp506P07WorkflowPermissionSliceCaseSet,
  createGovernanceCoreCp506P07WorkflowPermissionSliceDescriptor,
  createGovernanceCoreCp507P07CloseoutP08FoundationCaseSet,
  createGovernanceCoreCp507P07CloseoutP08FoundationDescriptor,
  createGovernanceCoreCp508P08ImplementationSliceCaseSet,
  createGovernanceCoreCp508P08ImplementationSliceDescriptor,
  createGovernanceCoreCp509P08WorkflowPermissionSliceCaseSet,
  createGovernanceCoreCp509P08WorkflowPermissionSliceDescriptor,
  createGovernanceCoreCp510P08CloseoutP09FoundationCaseSet,
  createGovernanceCoreCp510P08CloseoutP09FoundationDescriptor,
  createGovernanceCoreCp511P09WorkflowPermissionSliceCaseSet,
  createGovernanceCoreCp511P09WorkflowPermissionSliceDescriptor,
  createGovernanceCoreCp512P09FixtureSliceCaseSet,
  createGovernanceCoreCp512P09FixtureSliceDescriptor,
  createGovernanceCoreCp513P09CloseoutHandoffSliceCaseSet,
  createGovernanceCoreCp513P09CloseoutHandoffSliceDescriptor,
  governanceCoreRowKey,
} from "./service.js";

function countBy(units, field) {
  const counts = {};
  for (const unit of units) {
    const key = unit?.[field];
    if (key === undefined) continue;
    counts[key] = (counts[key] ?? 0) + 1;
  }
  return Object.freeze(counts);
}

function freezeCp480Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP480_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP480_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp480CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp480Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp480Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-480 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP480_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-480");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP480_PACK_BINDING.risk_class) errors.push("CP00-480 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP480_PACK_BINDING.unit_count) errors.push("CP00-480 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP480_PACK_BINDING.first_unit_id) errors.push("CP00-480 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP480_PACK_BINDING.last_unit_id) errors.push("CP00-480 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-480 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-480 must only include RP16 units");
  const summary = createGovernanceCoreCp480CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP480_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-480 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP480_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-480 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP480_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-480 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP480_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-480 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP480_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-480 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-480 ${microId} missing row ${title}`);
    }
  }
  return freezeCp480Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp480ScopeContractFoundationDescriptor(
  descriptor = createGovernanceCoreCp480ScopeContractFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.scope_contract_foundation_case_set ?? createGovernanceCoreCp480ScopeContractFoundationCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp480ScopeContractFoundationDescriptor") errors.push("CP00-480 descriptor type drift");
  if (descriptor.program_contract?.program_id !== "RP16") errors.push("CP00-480 program id drift");
  if (descriptor.program_contract?.upstream_program_id !== "RP15") errors.push("CP00-480 upstream program drift");
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP480_REQUIREMENTS.required_section_rows).length) errors.push("CP00-480 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP480_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-480 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-480 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-480 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-480 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-480 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-480 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-480 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(GOVERNANCE_CORE_CP480_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.non_goal_boundary && rows.non_goal_boundary.governance_runtime_opened !== false) {
      errors.push(`CP00-480 ${microId} must not open governance runtime`);
    }
    if (rows.permission_baseline_note && rows.permission_baseline_note.permission_decision_detail_included !== false) {
      errors.push(`CP00-480 ${microId} permission baseline must not expose decisions`);
    }
    if (rows.permission_baseline_note && rows.permission_baseline_note.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-480 ${microId} permission baseline must deny cross-tenant access`);
    }
    if (rows.audit_baseline_note && rows.audit_baseline_note.audit_event_body_included !== false) {
      errors.push(`CP00-480 ${microId} audit baseline must not expose event bodies`);
    }
    if (rows.synthetic_data_policy && rows.synthetic_data_policy.real_client_data_loaded !== false) {
      errors.push(`CP00-480 ${microId} synthetic data policy drift`);
    }
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-480 ${microId} tenant scope must deny cross-tenant access`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-480 ${microId} matter trace must be required`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-480 ${microId} state transition map must not write state`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-480 ${microId} fixture model must not load real data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-480 ${microId} must not execute test runtime`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-480 ${microId} must not emit Hermes receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-480 ${microId} must not claim Claude final approval`);
    }
  }
  if (GOVERNANCE_CORE_CP480_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-480 must not promote Claude");
  if (GOVERNANCE_CORE_CP480_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-480 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-480 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-480 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP480_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP481_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP482_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP483_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP484_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP485_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP486_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP487_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP488_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP489_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-480 contract current_pack drift");
  }
  if (
    contractProjection?.scope_contract_foundation_descriptor?.descriptor &&
    contractProjection.scope_contract_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-480 contract scope_contract_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP480_PACK_BINDING.next_pack_id) errors.push("CP00-480 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP480_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-480 next subphase drift");
  }
  return freezeCp480Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp480HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp480ScopeContractFoundationDescriptor(),
) {
  const coverage = validateGovernanceCoreCp480Coverage(planPack);
  const foundation = validateGovernanceCoreCp480ScopeContractFoundationDescriptor(descriptor, contractProjection);
  return freezeCp480Validation({
    evidence_packet: "H16.CP00-480.governance_core_scope_contract_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP480_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    scope_contract_foundation_valid: foundation.valid,
    no_real_data: true,
    source_analytics_core_pack_id: GOVERNANCE_CORE_CP480_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP480_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP480_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP480_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createGovernanceCoreCp480ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp480Coverage(planPack);
  const foundation = validateGovernanceCoreCp480ScopeContractFoundationDescriptor(createGovernanceCoreCp480ScopeContractFoundationDescriptor(), {});
  return freezeCp480Validation({
    review_packet: "C16.CP00-480.governance_core_scope_contract_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP480_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP480_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    scope_contract_foundation_valid: foundation.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP480_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-480 cover exactly the 150 planned RP16 units from RP16.P00.M00.S01 through RP16.P01.M02.S08?",
      "Do the twenty-one descriptor sections (P00 M00-M10 scope foundation cycle and P01 M00-M08 model foundation cycle with package directory layout, primary entity identifier, tenant scope field, matter trace reference, lifecycle status enum, ownership metadata, reference relationship map, field registries, state transition map, validation helper, fixture model, serialization shape, public export, model tests, Hermes model summary, Claude model review prompt, and closeout handoff) cover every planned row title as descriptor-only rows?",
      "Does CP00-480 avoid governance/dlp/legal-hold/retention runtime, runtime permission evaluation, audit event writes, state writes, object storage, command runtime execution, Hermes runtime receipts, real data, permission decision leaks, audit event body leaks, fixture payload leaks, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp480CloseoutHandoff() {
  return freezeCp480Validation({
    handoff_id: "CP00-480-to-CP00-481",
    from_pack_id: GOVERNANCE_CORE_CP480_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP480_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP480_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP480_PACK_BINDING.range,
    open_scope: "Continue RP16.P01.M02.S09 onward with the remaining model foundation rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP480_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp481Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP481_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP481_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp481CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp481Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp481Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-481 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP481_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-481");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP481_PACK_BINDING.risk_class) errors.push("CP00-481 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP481_PACK_BINDING.unit_count) errors.push("CP00-481 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP481_PACK_BINDING.first_unit_id) errors.push("CP00-481 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP481_PACK_BINDING.last_unit_id) errors.push("CP00-481 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-481 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-481 must only include RP16 units");
  const summary = createGovernanceCoreCp481CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP481_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-481 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP481_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-481 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP481_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-481 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP481_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-481 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP481_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-481 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-481 ${microId} missing row ${title}`);
    }
  }
  return freezeCp481Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp481ModelFoundationSliceDescriptor(
  descriptor = createGovernanceCoreCp481ModelFoundationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.model_foundation_slice_case_set ?? createGovernanceCoreCp481ModelFoundationSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp481ModelFoundationSliceDescriptor") errors.push("CP00-481 descriptor type drift");
  if (descriptor.source_scope_contract_foundation_descriptor !== "GovernanceCoreCp480ScopeContractFoundationDescriptor") {
    errors.push("CP00-481 source scope contract foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP481_REQUIREMENTS.required_section_rows).length) errors.push("CP00-481 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP481_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-481 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-481 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-481 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-481 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-481 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-481 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-481 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(GOVERNANCE_CORE_CP481_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-481 ${microId} must not allow cross-tenant access`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-481 ${microId} must not write state transitions`);
    }
    if (rows.validation_helper && rows.validation_helper.validation_error_detail_included !== false) {
      errors.push(`CP00-481 ${microId} must not expose validation error details`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-481 ${microId} must not load real fixture data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-481 ${microId} must not execute test runtime`);
    }
    if (rows.invalid_reference_test && rows.invalid_reference_test.expected_outcome !== "rejected_customer_safe") {
      errors.push(`CP00-481 ${microId} invalid reference outcome drift`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-481 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-481 ${microId} must not claim Claude final approval`);
    }
  }
  if (GOVERNANCE_CORE_CP481_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-481 must not promote Claude");
  if (GOVERNANCE_CORE_CP481_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-481 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-481 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-481 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP481_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP482_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP483_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP484_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP485_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP486_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP487_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP488_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP489_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-481 contract current_pack drift");
  }
  if (
    contractProjection?.model_foundation_slice_descriptor?.descriptor &&
    contractProjection.model_foundation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-481 contract model_foundation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP481_PACK_BINDING.next_pack_id) errors.push("CP00-481 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP481_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-481 next subphase drift");
  }
  return freezeCp481Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp481HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp481ModelFoundationSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp481Coverage(planPack);
  const slice = validateGovernanceCoreCp481ModelFoundationSliceDescriptor(descriptor, contractProjection);
  return freezeCp481Validation({
    evidence_packet: "H16.CP00-481.governance_core_model_foundation_slice_descriptor",
    gate: GOVERNANCE_CORE_CP481_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    model_foundation_slice_valid: slice.valid,
    no_real_data: true,
    source_scope_contract_foundation_pack_id: GOVERNANCE_CORE_CP481_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP481_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP481_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP481_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp481ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp481Coverage(planPack);
  const slice = validateGovernanceCoreCp481ModelFoundationSliceDescriptor(createGovernanceCoreCp481ModelFoundationSliceDescriptor(), {});
  return freezeCp481Validation({
    review_packet: "C16.CP00-481.governance_core_model_foundation_slice_descriptor",
    gate: GOVERNANCE_CORE_CP481_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP481_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    model_foundation_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP481_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-481 cover exactly the 40 planned RP16 units from RP16.P01.M02.S09 through RP16.P01.M04.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-481 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp481CloseoutHandoff() {
  return freezeCp481Validation({
    handoff_id: "CP00-481-to-CP00-482",
    from_pack_id: GOVERNANCE_CORE_CP481_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP481_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP481_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP481_PACK_BINDING.range,
    open_scope: "Continue RP16.P01.M04.S07 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP481_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp482Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP482_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP482_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp482CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp482Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp482Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-482 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP482_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-482");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP482_PACK_BINDING.risk_class) errors.push("CP00-482 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP482_PACK_BINDING.unit_count) errors.push("CP00-482 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP482_PACK_BINDING.first_unit_id) errors.push("CP00-482 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP482_PACK_BINDING.last_unit_id) errors.push("CP00-482 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-482 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-482 must only include RP16 units");
  const summary = createGovernanceCoreCp482CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP482_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-482 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP482_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-482 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP482_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-482 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP482_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-482 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP482_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-482 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-482 ${microId} missing row ${title}`);
    }
  }
  return freezeCp482Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp482WorkflowPermissionSliceDescriptor(
  descriptor = createGovernanceCoreCp482WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.workflow_permission_slice_case_set ?? createGovernanceCoreCp482WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp482WorkflowPermissionSliceDescriptor") errors.push("CP00-482 descriptor type drift");
  if (descriptor.source_model_foundation_slice_descriptor !== "GovernanceCoreCp481ModelFoundationSliceDescriptor") {
    errors.push("CP00-482 source model foundation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP482_REQUIREMENTS.required_section_rows).length) errors.push("CP00-482 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP482_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-482 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-482 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-482 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-482 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-482 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-482 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-482 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(GOVERNANCE_CORE_CP482_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-482 ${microId} must not allow cross-tenant access`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-482 ${microId} must not write state transitions`);
    }
    if (rows.validation_helper && rows.validation_helper.validation_error_detail_included !== false) {
      errors.push(`CP00-482 ${microId} must not expose validation error details`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-482 ${microId} must not load real fixture data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-482 ${microId} must not execute test runtime`);
    }
    if (rows.invalid_reference_test && rows.invalid_reference_test.expected_outcome !== "rejected_customer_safe") {
      errors.push(`CP00-482 ${microId} invalid reference outcome drift`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-482 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-482 ${microId} must not claim Claude final approval`);
    }
  }
  if (GOVERNANCE_CORE_CP482_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-482 must not promote Claude");
  if (GOVERNANCE_CORE_CP482_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-482 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-482 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-482 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP482_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP483_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP484_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP485_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP486_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP487_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP488_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP489_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-482 contract current_pack drift");
  }
  if (
    contractProjection?.workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-482 contract workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP482_PACK_BINDING.next_pack_id) errors.push("CP00-482 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP482_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-482 next subphase drift");
  }
  return freezeCp482Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp482HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp482WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp482Coverage(planPack);
  const slice = validateGovernanceCoreCp482WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp482Validation({
    evidence_packet: "H16.CP00-482.governance_core_workflow_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP482_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_model_foundation_slice_pack_id: GOVERNANCE_CORE_CP482_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP482_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP482_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP482_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp482ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp482Coverage(planPack);
  const slice = validateGovernanceCoreCp482WorkflowPermissionSliceDescriptor(createGovernanceCoreCp482WorkflowPermissionSliceDescriptor(), {});
  return freezeCp482Validation({
    review_packet: "C16.CP00-482.governance_core_workflow_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP482_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP482_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP482_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-482 cover exactly the 40 planned RP16 units from RP16.P01.M04.S07 through RP16.P01.M06.S04?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-482 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp482CloseoutHandoff() {
  return freezeCp482Validation({
    handoff_id: "CP00-482-to-CP00-483",
    from_pack_id: GOVERNANCE_CORE_CP482_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP482_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP482_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP482_PACK_BINDING.range,
    open_scope: "Continue RP16.P01.M06.S05 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP482_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp483Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP483_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP483_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp483CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp483Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp483Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-483 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP483_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-483");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP483_PACK_BINDING.risk_class) errors.push("CP00-483 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP483_PACK_BINDING.unit_count) errors.push("CP00-483 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP483_PACK_BINDING.first_unit_id) errors.push("CP00-483 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP483_PACK_BINDING.last_unit_id) errors.push("CP00-483 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-483 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-483 must only include RP16 units");
  const summary = createGovernanceCoreCp483CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP483_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-483 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP483_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-483 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP483_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-483 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP483_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-483 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP483_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-483 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-483 ${microId} missing row ${title}`);
    }
  }
  return freezeCp483Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp483P01CloseoutP02FoundationDescriptor(
  descriptor = createGovernanceCoreCp483P01CloseoutP02FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p01_closeout_p02_foundation_case_set ?? createGovernanceCoreCp483P01CloseoutP02FoundationCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp483P01CloseoutP02FoundationDescriptor") errors.push("CP00-483 descriptor type drift");
  if (descriptor.source_workflow_permission_slice_descriptor !== "GovernanceCoreCp482WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-483 source workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP483_REQUIREMENTS.required_section_rows).length) errors.push("CP00-483 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP483_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-483 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-483 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-483 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-483 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-483 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-483 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-483 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(GOVERNANCE_CORE_CP483_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-483 ${microId} must not allow cross-tenant access`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-483 ${microId} must not write state transitions`);
    }
    if (rows.validation_helper && rows.validation_helper.validation_error_detail_included !== false) {
      errors.push(`CP00-483 ${microId} must not expose validation error details`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-483 ${microId} must not load real fixture data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-483 ${microId} must not execute test runtime`);
    }
    if (rows.invalid_reference_test && rows.invalid_reference_test.expected_outcome !== "rejected_customer_safe") {
      errors.push(`CP00-483 ${microId} invalid reference outcome drift`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-483 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-483 ${microId} must not claim Claude final approval`);
    }
  }
  if (GOVERNANCE_CORE_CP483_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-483 must not promote Claude");
  if (GOVERNANCE_CORE_CP483_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-483 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-483 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-483 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP483_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP484_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP485_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP486_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP487_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP488_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP489_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-483 contract current_pack drift");
  }
  if (
    contractProjection?.p01_closeout_p02_foundation_descriptor?.descriptor &&
    contractProjection.p01_closeout_p02_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-483 contract p01_closeout_p02_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP483_PACK_BINDING.next_pack_id) errors.push("CP00-483 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP483_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-483 next subphase drift");
  }
  return freezeCp483Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp483HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp483P01CloseoutP02FoundationDescriptor(),
) {
  const coverage = validateGovernanceCoreCp483Coverage(planPack);
  const slice = validateGovernanceCoreCp483P01CloseoutP02FoundationDescriptor(descriptor, contractProjection);
  return freezeCp483Validation({
    evidence_packet: "H16.CP00-483.governance_core_p01_closeout_p02_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP483_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p01_closeout_p02_foundation_valid: slice.valid,
    no_real_data: true,
    source_workflow_permission_slice_pack_id: GOVERNANCE_CORE_CP483_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP483_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP483_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP483_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp483ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp483Coverage(planPack);
  const slice = validateGovernanceCoreCp483P01CloseoutP02FoundationDescriptor(createGovernanceCoreCp483P01CloseoutP02FoundationDescriptor(), {});
  return freezeCp483Validation({
    review_packet: "C16.CP00-483.governance_core_p01_closeout_p02_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP483_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP483_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p01_closeout_p02_foundation_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP483_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-483 cover exactly the 150 planned RP16 units from RP16.P01.M06.S05 through RP16.P02.M02.S22?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-483 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp483CloseoutHandoff() {
  return freezeCp483Validation({
    handoff_id: "CP00-483-to-CP00-484",
    from_pack_id: GOVERNANCE_CORE_CP483_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP483_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP483_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP483_PACK_BINDING.range,
    open_scope: "Continue RP16.P02.M03.S01 onward with the P02 primary implementation rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP483_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp484Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP484_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP484_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp484CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp484Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp484Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-484 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP484_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-484");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP484_PACK_BINDING.risk_class) errors.push("CP00-484 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP484_PACK_BINDING.unit_count) errors.push("CP00-484 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP484_PACK_BINDING.first_unit_id) errors.push("CP00-484 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP484_PACK_BINDING.last_unit_id) errors.push("CP00-484 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-484 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-484 must only include RP16 units");
  const summary = createGovernanceCoreCp484CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP484_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-484 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP484_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-484 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP484_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-484 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP484_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-484 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP484_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-484 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-484 ${microId} missing row ${title}`);
    }
  }
  return freezeCp484Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp484P02ImplementationSliceDescriptor(
  descriptor = createGovernanceCoreCp484P02ImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_implementation_slice_case_set ?? createGovernanceCoreCp484P02ImplementationSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp484P02ImplementationSliceDescriptor") errors.push("CP00-484 descriptor type drift");
  if (descriptor.source_p01_closeout_p02_foundation_descriptor !== "GovernanceCoreCp483P01CloseoutP02FoundationDescriptor") {
    errors.push("CP00-484 source p01 closeout p02 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP484_REQUIREMENTS.required_section_rows).length) errors.push("CP00-484 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP484_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-484 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-484 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-484 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-484 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-484 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-484 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-484 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP484_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-484 must not promote Claude");
  if (GOVERNANCE_CORE_CP484_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-484 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-484 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-484 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP484_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP485_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP486_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP487_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP488_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP489_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-484 contract current_pack drift");
  }
  if (
    contractProjection?.p02_implementation_slice_descriptor?.descriptor &&
    contractProjection.p02_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-484 contract p02_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP484_PACK_BINDING.next_pack_id) errors.push("CP00-484 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP484_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-484 next subphase drift");
  }
  return freezeCp484Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp484HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp484P02ImplementationSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp484Coverage(planPack);
  const slice = validateGovernanceCoreCp484P02ImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp484Validation({
    evidence_packet: "H16.CP00-484.governance_core_p02_implementation_slice_descriptor",
    gate: GOVERNANCE_CORE_CP484_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_p01_closeout_p02_foundation_pack_id: GOVERNANCE_CORE_CP484_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP484_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP484_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP484_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp484ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp484Coverage(planPack);
  const slice = validateGovernanceCoreCp484P02ImplementationSliceDescriptor(createGovernanceCoreCp484P02ImplementationSliceDescriptor(), {});
  return freezeCp484Validation({
    review_packet: "C16.CP00-484.governance_core_p02_implementation_slice_descriptor",
    gate: GOVERNANCE_CORE_CP484_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP484_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_implementation_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP484_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-484 cover exactly the 40 planned RP16 units from RP16.P02.M03.S01 through RP16.P02.M04.S18?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-484 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp484CloseoutHandoff() {
  return freezeCp484Validation({
    handoff_id: "CP00-484-to-CP00-485",
    from_pack_id: GOVERNANCE_CORE_CP484_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP484_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP484_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP484_PACK_BINDING.range,
    open_scope: "Continue RP16.P02.M04.S19 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP484_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp485Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP485_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP485_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp485CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp485Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp485Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-485 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP485_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-485");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP485_PACK_BINDING.risk_class) errors.push("CP00-485 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP485_PACK_BINDING.unit_count) errors.push("CP00-485 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP485_PACK_BINDING.first_unit_id) errors.push("CP00-485 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP485_PACK_BINDING.last_unit_id) errors.push("CP00-485 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-485 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-485 must only include RP16 units");
  const summary = createGovernanceCoreCp485CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP485_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-485 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP485_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-485 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP485_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-485 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP485_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-485 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP485_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-485 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-485 ${microId} missing row ${title}`);
    }
  }
  return freezeCp485Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp485P02WorkflowPermissionSliceDescriptor(
  descriptor = createGovernanceCoreCp485P02WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_workflow_permission_slice_case_set ?? createGovernanceCoreCp485P02WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp485P02WorkflowPermissionSliceDescriptor") errors.push("CP00-485 descriptor type drift");
  if (descriptor.source_p02_implementation_slice_descriptor !== "GovernanceCoreCp484P02ImplementationSliceDescriptor") {
    errors.push("CP00-485 source p02 implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP485_REQUIREMENTS.required_section_rows).length) errors.push("CP00-485 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP485_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-485 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-485 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-485 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-485 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-485 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-485 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-485 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP485_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-485 must not promote Claude");
  if (GOVERNANCE_CORE_CP485_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-485 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-485 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-485 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP485_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP486_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP487_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP488_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP489_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-485 contract current_pack drift");
  }
  if (
    contractProjection?.p02_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p02_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-485 contract p02_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP485_PACK_BINDING.next_pack_id) errors.push("CP00-485 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP485_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-485 next subphase drift");
  }
  return freezeCp485Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp485HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp485P02WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp485Coverage(planPack);
  const slice = validateGovernanceCoreCp485P02WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp485Validation({
    evidence_packet: "H16.CP00-485.governance_core_p02_workflow_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP485_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_implementation_slice_pack_id: GOVERNANCE_CORE_CP485_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP485_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP485_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP485_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp485ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp485Coverage(planPack);
  const slice = validateGovernanceCoreCp485P02WorkflowPermissionSliceDescriptor(createGovernanceCoreCp485P02WorkflowPermissionSliceDescriptor(), {});
  return freezeCp485Validation({
    review_packet: "C16.CP00-485.governance_core_p02_workflow_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP485_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP485_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP485_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-485 cover exactly the 40 planned RP16 units from RP16.P02.M04.S19 through RP16.P02.M06.S14?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-485 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp485CloseoutHandoff() {
  return freezeCp485Validation({
    handoff_id: "CP00-485-to-CP00-486",
    from_pack_id: GOVERNANCE_CORE_CP485_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP485_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP485_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP485_PACK_BINDING.range,
    open_scope: "Continue RP16.P02.M06.S15 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP485_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp486Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP486_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP486_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp486CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp486Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp486Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-486 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP486_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-486");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP486_PACK_BINDING.risk_class) errors.push("CP00-486 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP486_PACK_BINDING.unit_count) errors.push("CP00-486 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP486_PACK_BINDING.first_unit_id) errors.push("CP00-486 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP486_PACK_BINDING.last_unit_id) errors.push("CP00-486 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-486 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-486 must only include RP16 units");
  const summary = createGovernanceCoreCp486CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP486_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-486 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP486_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-486 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP486_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-486 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP486_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-486 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP486_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-486 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-486 ${microId} missing row ${title}`);
    }
  }
  return freezeCp486Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp486P02FixtureTestSliceDescriptor(
  descriptor = createGovernanceCoreCp486P02FixtureTestSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_fixture_test_slice_case_set ?? createGovernanceCoreCp486P02FixtureTestSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp486P02FixtureTestSliceDescriptor") errors.push("CP00-486 descriptor type drift");
  if (descriptor.source_p02_workflow_permission_slice_descriptor !== "GovernanceCoreCp485P02WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-486 source p02 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP486_REQUIREMENTS.required_section_rows).length) errors.push("CP00-486 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP486_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-486 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-486 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-486 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-486 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-486 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-486 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-486 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP486_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-486 must not promote Claude");
  if (GOVERNANCE_CORE_CP486_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-486 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-486 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-486 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP486_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP487_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP488_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP489_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-486 contract current_pack drift");
  }
  if (
    contractProjection?.p02_fixture_test_slice_descriptor?.descriptor &&
    contractProjection.p02_fixture_test_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-486 contract p02_fixture_test_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP486_PACK_BINDING.next_pack_id) errors.push("CP00-486 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP486_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-486 next subphase drift");
  }
  return freezeCp486Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp486HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp486P02FixtureTestSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp486Coverage(planPack);
  const slice = validateGovernanceCoreCp486P02FixtureTestSliceDescriptor(descriptor, contractProjection);
  return freezeCp486Validation({
    evidence_packet: "H16.CP00-486.governance_core_p02_fixture_test_slice_descriptor",
    gate: GOVERNANCE_CORE_CP486_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_fixture_test_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_workflow_permission_slice_pack_id: GOVERNANCE_CORE_CP486_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP486_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP486_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP486_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp486ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp486Coverage(planPack);
  const slice = validateGovernanceCoreCp486P02FixtureTestSliceDescriptor(createGovernanceCoreCp486P02FixtureTestSliceDescriptor(), {});
  return freezeCp486Validation({
    review_packet: "C16.CP00-486.governance_core_p02_fixture_test_slice_descriptor",
    gate: GOVERNANCE_CORE_CP486_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP486_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_fixture_test_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP486_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-486 cover exactly the 10 planned RP16 units from RP16.P02.M06.S15 through RP16.P02.M07.S02?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-486 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp486CloseoutHandoff() {
  return freezeCp486Validation({
    handoff_id: "CP00-486-to-CP00-487",
    from_pack_id: GOVERNANCE_CORE_CP486_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP486_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP486_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP486_PACK_BINDING.range,
    open_scope: "Continue RP16.P02.M07.S03 onward with the remaining test and golden case rows while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP486_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp487Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP487_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP487_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp487CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp487Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp487Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-487 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP487_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-487");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP487_PACK_BINDING.risk_class) errors.push("CP00-487 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP487_PACK_BINDING.unit_count) errors.push("CP00-487 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP487_PACK_BINDING.first_unit_id) errors.push("CP00-487 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP487_PACK_BINDING.last_unit_id) errors.push("CP00-487 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-487 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-487 must only include RP16 units");
  const summary = createGovernanceCoreCp487CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP487_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-487 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP487_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-487 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP487_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-487 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP487_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-487 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP487_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-487 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-487 ${microId} missing row ${title}`);
    }
  }
  return freezeCp487Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp487P02TestSliceDescriptor(
  descriptor = createGovernanceCoreCp487P02TestSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_test_slice_case_set ?? createGovernanceCoreCp487P02TestSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp487P02TestSliceDescriptor") errors.push("CP00-487 descriptor type drift");
  if (descriptor.source_p02_fixture_test_slice_descriptor !== "GovernanceCoreCp486P02FixtureTestSliceDescriptor") {
    errors.push("CP00-487 source p02 fixture test slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP487_REQUIREMENTS.required_section_rows).length) errors.push("CP00-487 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP487_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-487 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-487 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-487 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-487 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-487 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-487 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-487 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP487_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-487 must not promote Claude");
  if (GOVERNANCE_CORE_CP487_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-487 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-487 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-487 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP487_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP488_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP489_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-487 contract current_pack drift");
  }
  if (
    contractProjection?.p02_test_slice_descriptor?.descriptor &&
    contractProjection.p02_test_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-487 contract p02_test_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP487_PACK_BINDING.next_pack_id) errors.push("CP00-487 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP487_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-487 next subphase drift");
  }
  return freezeCp487Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp487HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp487P02TestSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp487Coverage(planPack);
  const slice = validateGovernanceCoreCp487P02TestSliceDescriptor(descriptor, contractProjection);
  return freezeCp487Validation({
    evidence_packet: "H16.CP00-487.governance_core_p02_test_slice_descriptor",
    gate: GOVERNANCE_CORE_CP487_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_test_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_fixture_test_slice_pack_id: GOVERNANCE_CORE_CP487_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP487_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP487_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP487_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp487ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp487Coverage(planPack);
  const slice = validateGovernanceCoreCp487P02TestSliceDescriptor(createGovernanceCoreCp487P02TestSliceDescriptor(), {});
  return freezeCp487Validation({
    review_packet: "C16.CP00-487.governance_core_p02_test_slice_descriptor",
    gate: GOVERNANCE_CORE_CP487_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP487_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_test_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP487_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-487 cover exactly the 10 planned RP16 units from RP16.P02.M07.S03 through RP16.P02.M07.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-487 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp487CloseoutHandoff() {
  return freezeCp487Validation({
    handoff_id: "CP00-487-to-CP00-488",
    from_pack_id: GOVERNANCE_CORE_CP487_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP487_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP487_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP487_PACK_BINDING.range,
    open_scope: "Continue RP16.P02.M07.S13 onward with the remaining test and golden case rows and the downstream evidence micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP487_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp488Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP488_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP488_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp488CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp488Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp488Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-488 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP488_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-488");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP488_PACK_BINDING.risk_class) errors.push("CP00-488 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP488_PACK_BINDING.unit_count) errors.push("CP00-488 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP488_PACK_BINDING.first_unit_id) errors.push("CP00-488 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP488_PACK_BINDING.last_unit_id) errors.push("CP00-488 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-488 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-488 must only include RP16 units");
  const summary = createGovernanceCoreCp488CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP488_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-488 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP488_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-488 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP488_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-488 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP488_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-488 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP488_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-488 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-488 ${microId} missing row ${title}`);
    }
  }
  return freezeCp488Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp488P02TestHermesSliceDescriptor(
  descriptor = createGovernanceCoreCp488P02TestHermesSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_test_hermes_slice_case_set ?? createGovernanceCoreCp488P02TestHermesSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp488P02TestHermesSliceDescriptor") errors.push("CP00-488 descriptor type drift");
  if (descriptor.source_p02_test_slice_descriptor !== "GovernanceCoreCp487P02TestSliceDescriptor") {
    errors.push("CP00-488 source p02 test slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP488_REQUIREMENTS.required_section_rows).length) errors.push("CP00-488 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP488_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-488 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-488 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-488 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-488 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-488 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-488 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-488 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP488_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-488 must not promote Claude");
  if (GOVERNANCE_CORE_CP488_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-488 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-488 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-488 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP488_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP489_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-488 contract current_pack drift");
  }
  if (
    contractProjection?.p02_test_hermes_slice_descriptor?.descriptor &&
    contractProjection.p02_test_hermes_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-488 contract p02_test_hermes_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP488_PACK_BINDING.next_pack_id) errors.push("CP00-488 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP488_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-488 next subphase drift");
  }
  return freezeCp488Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp488HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp488P02TestHermesSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp488Coverage(planPack);
  const slice = validateGovernanceCoreCp488P02TestHermesSliceDescriptor(descriptor, contractProjection);
  return freezeCp488Validation({
    evidence_packet: "H16.CP00-488.governance_core_p02_test_hermes_slice_descriptor",
    gate: GOVERNANCE_CORE_CP488_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_test_hermes_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_test_slice_pack_id: GOVERNANCE_CORE_CP488_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP488_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP488_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP488_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp488ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp488Coverage(planPack);
  const slice = validateGovernanceCoreCp488P02TestHermesSliceDescriptor(createGovernanceCoreCp488P02TestHermesSliceDescriptor(), {});
  return freezeCp488Validation({
    review_packet: "C16.CP00-488.governance_core_p02_test_hermes_slice_descriptor",
    gate: GOVERNANCE_CORE_CP488_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP488_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_test_hermes_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP488_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-488 cover exactly the 40 planned RP16 units from RP16.P02.M07.S13 through RP16.P02.M09.S08?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-488 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp488CloseoutHandoff() {
  return freezeCp488Validation({
    handoff_id: "CP00-488-to-CP00-489",
    from_pack_id: GOVERNANCE_CORE_CP488_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP488_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP488_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP488_PACK_BINDING.range,
    open_scope: "Continue RP16.P02.M09.S09 onward with the remaining review packet rows and the closeout micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP488_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp489Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP489_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP489_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp489CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp489Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp489Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-489 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP489_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-489");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP489_PACK_BINDING.risk_class) errors.push("CP00-489 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP489_PACK_BINDING.unit_count) errors.push("CP00-489 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP489_PACK_BINDING.first_unit_id) errors.push("CP00-489 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP489_PACK_BINDING.last_unit_id) errors.push("CP00-489 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-489 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-489 must only include RP16 units");
  const summary = createGovernanceCoreCp489CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP489_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-489 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP489_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-489 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP489_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-489 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP489_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-489 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP489_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-489 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-489 ${microId} missing row ${title}`);
    }
  }
  return freezeCp489Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp489P02CloseoutP03FoundationDescriptor(
  descriptor = createGovernanceCoreCp489P02CloseoutP03FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_closeout_p03_foundation_case_set ?? createGovernanceCoreCp489P02CloseoutP03FoundationCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp489P02CloseoutP03FoundationDescriptor") errors.push("CP00-489 descriptor type drift");
  if (descriptor.source_p02_test_hermes_slice_descriptor !== "GovernanceCoreCp488P02TestHermesSliceDescriptor") {
    errors.push("CP00-489 source p02 test hermes slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP489_REQUIREMENTS.required_section_rows).length) errors.push("CP00-489 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP489_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-489 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-489 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-489 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-489 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-489 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-489 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-489 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP489_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-489 must not promote Claude");
  if (GOVERNANCE_CORE_CP489_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-489 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-489 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-489 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP489_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-489 contract current_pack drift");
  }
  if (
    contractProjection?.p02_closeout_p03_foundation_descriptor?.descriptor &&
    contractProjection.p02_closeout_p03_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-489 contract p02_closeout_p03_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP489_PACK_BINDING.next_pack_id) errors.push("CP00-489 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP489_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-489 next subphase drift");
  }
  return freezeCp489Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp489HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp489P02CloseoutP03FoundationDescriptor(),
) {
  const coverage = validateGovernanceCoreCp489Coverage(planPack);
  const slice = validateGovernanceCoreCp489P02CloseoutP03FoundationDescriptor(descriptor, contractProjection);
  return freezeCp489Validation({
    evidence_packet: "H16.CP00-489.governance_core_p02_closeout_p03_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP489_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_closeout_p03_foundation_valid: slice.valid,
    no_real_data: true,
    source_p02_test_hermes_slice_pack_id: GOVERNANCE_CORE_CP489_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP489_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP489_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP489_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp489ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp489Coverage(planPack);
  const slice = validateGovernanceCoreCp489P02CloseoutP03FoundationDescriptor(createGovernanceCoreCp489P02CloseoutP03FoundationDescriptor(), {});
  return freezeCp489Validation({
    review_packet: "C16.CP00-489.governance_core_p02_closeout_p03_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP489_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP489_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_closeout_p03_foundation_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP489_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-489 cover exactly the 150 planned RP16 units from RP16.P02.M09.S09 through RP16.P03.M06.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-489 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp489CloseoutHandoff() {
  return freezeCp489Validation({
    handoff_id: "CP00-489-to-CP00-490",
    from_pack_id: GOVERNANCE_CORE_CP489_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP489_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP489_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP489_PACK_BINDING.range,
    open_scope: "Continue RP16.P03.M06.S13 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP489_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp490Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP490_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp490CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp490Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp490Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-490 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-490");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP490_PACK_BINDING.risk_class) errors.push("CP00-490 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP490_PACK_BINDING.unit_count) errors.push("CP00-490 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP490_PACK_BINDING.first_unit_id) errors.push("CP00-490 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP490_PACK_BINDING.last_unit_id) errors.push("CP00-490 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-490 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-490 must only include RP16 units");
  const summary = createGovernanceCoreCp490CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP490_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-490 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP490_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-490 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP490_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-490 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP490_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-490 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP490_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-490 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-490 ${microId} missing row ${title}`);
    }
  }
  return freezeCp490Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp490P03CloseoutP04FoundationDescriptor(
  descriptor = createGovernanceCoreCp490P03CloseoutP04FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_closeout_p04_foundation_case_set ?? createGovernanceCoreCp490P03CloseoutP04FoundationCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp490P03CloseoutP04FoundationDescriptor") errors.push("CP00-490 descriptor type drift");
  if (descriptor.source_p02_closeout_p03_foundation_descriptor !== "GovernanceCoreCp489P02CloseoutP03FoundationDescriptor") {
    errors.push("CP00-490 source p02 closeout p03 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP490_REQUIREMENTS.required_section_rows).length) errors.push("CP00-490 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP490_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-490 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-490 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-490 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-490 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-490 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-490 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-490 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP490_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-490 must not promote Claude");
  if (GOVERNANCE_CORE_CP490_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-490 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-490 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-490 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-490 contract current_pack drift");
  }
  if (
    contractProjection?.p03_closeout_p04_foundation_descriptor?.descriptor &&
    contractProjection.p03_closeout_p04_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-490 contract p03_closeout_p04_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP490_PACK_BINDING.next_pack_id) errors.push("CP00-490 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP490_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-490 next subphase drift");
  }
  return freezeCp490Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp490HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp490P03CloseoutP04FoundationDescriptor(),
) {
  const coverage = validateGovernanceCoreCp490Coverage(planPack);
  const slice = validateGovernanceCoreCp490P03CloseoutP04FoundationDescriptor(descriptor, contractProjection);
  return freezeCp490Validation({
    evidence_packet: "H16.CP00-490.governance_core_p03_closeout_p04_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP490_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_closeout_p04_foundation_valid: slice.valid,
    no_real_data: true,
    source_p02_closeout_p03_foundation_pack_id: GOVERNANCE_CORE_CP490_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP490_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP490_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP490_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp490ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp490Coverage(planPack);
  const slice = validateGovernanceCoreCp490P03CloseoutP04FoundationDescriptor(createGovernanceCoreCp490P03CloseoutP04FoundationDescriptor(), {});
  return freezeCp490Validation({
    review_packet: "C16.CP00-490.governance_core_p03_closeout_p04_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP490_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP490_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_closeout_p04_foundation_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP490_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-490 cover exactly the 150 planned RP16 units from RP16.P03.M06.S13 through RP16.P04.M03.S20?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-490 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp490CloseoutHandoff() {
  return freezeCp490Validation({
    handoff_id: "CP00-490-to-CP00-491",
    from_pack_id: GOVERNANCE_CORE_CP490_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP490_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP490_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP490_PACK_BINDING.range,
    open_scope: "Continue RP16.P04.M03.S21 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP490_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp491Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP491_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp491CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp491Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp491Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-491 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-491");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP491_PACK_BINDING.risk_class) errors.push("CP00-491 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP491_PACK_BINDING.unit_count) errors.push("CP00-491 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP491_PACK_BINDING.first_unit_id) errors.push("CP00-491 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP491_PACK_BINDING.last_unit_id) errors.push("CP00-491 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-491 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-491 must only include RP16 units");
  const summary = createGovernanceCoreCp491CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP491_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-491 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP491_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-491 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP491_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-491 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP491_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-491 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP491_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-491 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-491 ${microId} missing row ${title}`);
    }
  }
  return freezeCp491Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp491P04WorkflowPermissionSliceDescriptor(
  descriptor = createGovernanceCoreCp491P04WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_workflow_permission_slice_case_set ?? createGovernanceCoreCp491P04WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp491P04WorkflowPermissionSliceDescriptor") errors.push("CP00-491 descriptor type drift");
  if (descriptor.source_p03_closeout_p04_foundation_descriptor !== "GovernanceCoreCp490P03CloseoutP04FoundationDescriptor") {
    errors.push("CP00-491 source p03 closeout p04 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP491_REQUIREMENTS.required_section_rows).length) errors.push("CP00-491 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP491_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-491 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-491 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-491 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-491 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-491 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-491 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-491 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP491_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-491 must not promote Claude");
  if (GOVERNANCE_CORE_CP491_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-491 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-491 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-491 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-491 contract current_pack drift");
  }
  if (
    contractProjection?.p04_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p04_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-491 contract p04_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP491_PACK_BINDING.next_pack_id) errors.push("CP00-491 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP491_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-491 next subphase drift");
  }
  return freezeCp491Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp491HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp491P04WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp491Coverage(planPack);
  const slice = validateGovernanceCoreCp491P04WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp491Validation({
    evidence_packet: "H16.CP00-491.governance_core_p04_workflow_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP491_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p03_closeout_p04_foundation_pack_id: GOVERNANCE_CORE_CP491_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP491_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP491_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP491_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp491ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp491Coverage(planPack);
  const slice = validateGovernanceCoreCp491P04WorkflowPermissionSliceDescriptor(createGovernanceCoreCp491P04WorkflowPermissionSliceDescriptor(), {});
  return freezeCp491Validation({
    review_packet: "C16.CP00-491.governance_core_p04_workflow_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP491_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP491_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP491_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-491 cover exactly the 40 planned RP16 units from RP16.P04.M03.S21 through RP16.P04.M05.S16?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-491 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp491CloseoutHandoff() {
  return freezeCp491Validation({
    handoff_id: "CP00-491-to-CP00-492",
    from_pack_id: GOVERNANCE_CORE_CP491_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP491_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP491_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP491_PACK_BINDING.range,
    open_scope: "Continue RP16.P04.M05.S17 onward with the remaining permission and audit binding rows and the synthetic fixture micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP491_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp492Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP492_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp492CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp492Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp492Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-492 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-492");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP492_PACK_BINDING.risk_class) errors.push("CP00-492 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP492_PACK_BINDING.unit_count) errors.push("CP00-492 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP492_PACK_BINDING.first_unit_id) errors.push("CP00-492 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP492_PACK_BINDING.last_unit_id) errors.push("CP00-492 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-492 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-492 must only include RP16 units");
  const summary = createGovernanceCoreCp492CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP492_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-492 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP492_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-492 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP492_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-492 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP492_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-492 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP492_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-492 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-492 ${microId} missing row ${title}`);
    }
  }
  return freezeCp492Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp492P04PermissionFixtureSliceDescriptor(
  descriptor = createGovernanceCoreCp492P04PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_permission_fixture_slice_case_set ?? createGovernanceCoreCp492P04PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp492P04PermissionFixtureSliceDescriptor") errors.push("CP00-492 descriptor type drift");
  if (descriptor.source_p04_workflow_permission_slice_descriptor !== "GovernanceCoreCp491P04WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-492 source p04 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP492_REQUIREMENTS.required_section_rows).length) errors.push("CP00-492 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP492_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-492 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-492 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-492 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-492 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-492 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-492 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-492 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP492_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-492 must not promote Claude");
  if (GOVERNANCE_CORE_CP492_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-492 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-492 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-492 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-492 contract current_pack drift");
  }
  if (
    contractProjection?.p04_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p04_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-492 contract p04_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP492_PACK_BINDING.next_pack_id) errors.push("CP00-492 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP492_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-492 next subphase drift");
  }
  return freezeCp492Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp492HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp492P04PermissionFixtureSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp492Coverage(planPack);
  const slice = validateGovernanceCoreCp492P04PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp492Validation({
    evidence_packet: "H16.CP00-492.governance_core_p04_permission_fixture_slice_descriptor",
    gate: GOVERNANCE_CORE_CP492_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p04_workflow_permission_slice_pack_id: GOVERNANCE_CORE_CP492_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP492_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP492_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP492_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp492ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp492Coverage(planPack);
  const slice = validateGovernanceCoreCp492P04PermissionFixtureSliceDescriptor(createGovernanceCoreCp492P04PermissionFixtureSliceDescriptor(), {});
  return freezeCp492Validation({
    review_packet: "C16.CP00-492.governance_core_p04_permission_fixture_slice_descriptor",
    gate: GOVERNANCE_CORE_CP492_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP492_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP492_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-492 cover exactly the 10 planned RP16 units from RP16.P04.M05.S17 through RP16.P04.M06.S04?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-492 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp492CloseoutHandoff() {
  return freezeCp492Validation({
    handoff_id: "CP00-492-to-CP00-493",
    from_pack_id: GOVERNANCE_CORE_CP492_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP492_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP492_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP492_PACK_BINDING.range,
    open_scope: "Continue RP16.P04.M06.S05 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP492_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp493Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP493_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp493CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp493Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp493Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-493 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-493");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP493_PACK_BINDING.risk_class) errors.push("CP00-493 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP493_PACK_BINDING.unit_count) errors.push("CP00-493 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP493_PACK_BINDING.first_unit_id) errors.push("CP00-493 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP493_PACK_BINDING.last_unit_id) errors.push("CP00-493 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-493 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-493 must only include RP16 units");
  const summary = createGovernanceCoreCp493CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP493_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-493 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP493_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-493 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP493_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-493 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP493_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-493 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP493_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-493 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-493 ${microId} missing row ${title}`);
    }
  }
  return freezeCp493Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp493P04CloseoutP05FoundationDescriptor(
  descriptor = createGovernanceCoreCp493P04CloseoutP05FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_closeout_p05_foundation_case_set ?? createGovernanceCoreCp493P04CloseoutP05FoundationCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp493P04CloseoutP05FoundationDescriptor") errors.push("CP00-493 descriptor type drift");
  if (descriptor.source_p04_permission_fixture_slice_descriptor !== "GovernanceCoreCp492P04PermissionFixtureSliceDescriptor") {
    errors.push("CP00-493 source p04 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP493_REQUIREMENTS.required_section_rows).length) errors.push("CP00-493 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP493_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-493 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-493 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-493 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-493 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-493 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-493 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-493 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP493_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-493 must not promote Claude");
  if (GOVERNANCE_CORE_CP493_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-493 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-493 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-493 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-493 contract current_pack drift");
  }
  if (
    contractProjection?.p04_closeout_p05_foundation_descriptor?.descriptor &&
    contractProjection.p04_closeout_p05_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-493 contract p04_closeout_p05_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP493_PACK_BINDING.next_pack_id) errors.push("CP00-493 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP493_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-493 next subphase drift");
  }
  return freezeCp493Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp493HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp493P04CloseoutP05FoundationDescriptor(),
) {
  const coverage = validateGovernanceCoreCp493Coverage(planPack);
  const slice = validateGovernanceCoreCp493P04CloseoutP05FoundationDescriptor(descriptor, contractProjection);
  return freezeCp493Validation({
    evidence_packet: "H16.CP00-493.governance_core_p04_closeout_p05_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP493_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_closeout_p05_foundation_valid: slice.valid,
    no_real_data: true,
    source_p04_permission_fixture_slice_pack_id: GOVERNANCE_CORE_CP493_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP493_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP493_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP493_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp493ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp493Coverage(planPack);
  const slice = validateGovernanceCoreCp493P04CloseoutP05FoundationDescriptor(createGovernanceCoreCp493P04CloseoutP05FoundationDescriptor(), {});
  return freezeCp493Validation({
    review_packet: "C16.CP00-493.governance_core_p04_closeout_p05_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP493_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP493_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_closeout_p05_foundation_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP493_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-493 cover exactly the 150 planned RP16 units from RP16.P04.M06.S05 through RP16.P05.M03.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-493 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp493CloseoutHandoff() {
  return freezeCp493Validation({
    handoff_id: "CP00-493-to-CP00-494",
    from_pack_id: GOVERNANCE_CORE_CP493_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP493_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP493_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP493_PACK_BINDING.range,
    open_scope: "Continue RP16.P05.M03.S07 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP493_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp494Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP494_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp494CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp494Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp494Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-494 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-494");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP494_PACK_BINDING.risk_class) errors.push("CP00-494 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP494_PACK_BINDING.unit_count) errors.push("CP00-494 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP494_PACK_BINDING.first_unit_id) errors.push("CP00-494 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP494_PACK_BINDING.last_unit_id) errors.push("CP00-494 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-494 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-494 must only include RP16 units");
  const summary = createGovernanceCoreCp494CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP494_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-494 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP494_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-494 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP494_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-494 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP494_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-494 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP494_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-494 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-494 ${microId} missing row ${title}`);
    }
  }
  return freezeCp494Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp494P05ImplementationWorkflowSliceDescriptor(
  descriptor = createGovernanceCoreCp494P05ImplementationWorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_implementation_workflow_slice_case_set ?? createGovernanceCoreCp494P05ImplementationWorkflowSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp494P05ImplementationWorkflowSliceDescriptor") errors.push("CP00-494 descriptor type drift");
  if (descriptor.source_p04_closeout_p05_foundation_descriptor !== "GovernanceCoreCp493P04CloseoutP05FoundationDescriptor") {
    errors.push("CP00-494 source p04 closeout p05 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP494_REQUIREMENTS.required_section_rows).length) errors.push("CP00-494 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP494_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-494 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-494 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-494 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-494 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-494 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-494 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-494 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP494_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-494 must not promote Claude");
  if (GOVERNANCE_CORE_CP494_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-494 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-494 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-494 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-494 contract current_pack drift");
  }
  if (
    contractProjection?.p05_implementation_workflow_slice_descriptor?.descriptor &&
    contractProjection.p05_implementation_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-494 contract p05_implementation_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP494_PACK_BINDING.next_pack_id) errors.push("CP00-494 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP494_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-494 next subphase drift");
  }
  return freezeCp494Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp494HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp494P05ImplementationWorkflowSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp494Coverage(planPack);
  const slice = validateGovernanceCoreCp494P05ImplementationWorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp494Validation({
    evidence_packet: "H16.CP00-494.governance_core_p05_implementation_workflow_slice_descriptor",
    gate: GOVERNANCE_CORE_CP494_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_implementation_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_p04_closeout_p05_foundation_pack_id: GOVERNANCE_CORE_CP494_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP494_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP494_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP494_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp494ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp494Coverage(planPack);
  const slice = validateGovernanceCoreCp494P05ImplementationWorkflowSliceDescriptor(createGovernanceCoreCp494P05ImplementationWorkflowSliceDescriptor(), {});
  return freezeCp494Validation({
    review_packet: "C16.CP00-494.governance_core_p05_implementation_workflow_slice_descriptor",
    gate: GOVERNANCE_CORE_CP494_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP494_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_implementation_workflow_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP494_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-494 cover exactly the 40 planned RP16 units from RP16.P05.M03.S07 through RP16.P05.M05.S02?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-494 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp494CloseoutHandoff() {
  return freezeCp494Validation({
    handoff_id: "CP00-494-to-CP00-495",
    from_pack_id: GOVERNANCE_CORE_CP494_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP494_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP494_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP494_PACK_BINDING.range,
    open_scope: "Continue RP16.P05.M05.S03 onward with the remaining permission and audit binding rows while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP494_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp495Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP495_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp495CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp495Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp495Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-495 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-495");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP495_PACK_BINDING.risk_class) errors.push("CP00-495 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP495_PACK_BINDING.unit_count) errors.push("CP00-495 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP495_PACK_BINDING.first_unit_id) errors.push("CP00-495 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP495_PACK_BINDING.last_unit_id) errors.push("CP00-495 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-495 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-495 must only include RP16 units");
  const summary = createGovernanceCoreCp495CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP495_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-495 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP495_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-495 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP495_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-495 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP495_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-495 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP495_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-495 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-495 ${microId} missing row ${title}`);
    }
  }
  return freezeCp495Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp495P05PermissionSliceDescriptor(
  descriptor = createGovernanceCoreCp495P05PermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_permission_slice_case_set ?? createGovernanceCoreCp495P05PermissionSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp495P05PermissionSliceDescriptor") errors.push("CP00-495 descriptor type drift");
  if (descriptor.source_p05_implementation_workflow_slice_descriptor !== "GovernanceCoreCp494P05ImplementationWorkflowSliceDescriptor") {
    errors.push("CP00-495 source p05 implementation workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP495_REQUIREMENTS.required_section_rows).length) errors.push("CP00-495 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP495_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-495 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-495 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-495 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-495 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-495 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-495 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-495 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP495_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-495 must not promote Claude");
  if (GOVERNANCE_CORE_CP495_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-495 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-495 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-495 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-495 contract current_pack drift");
  }
  if (
    contractProjection?.p05_permission_slice_descriptor?.descriptor &&
    contractProjection.p05_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-495 contract p05_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP495_PACK_BINDING.next_pack_id) errors.push("CP00-495 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP495_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-495 next subphase drift");
  }
  return freezeCp495Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp495HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp495P05PermissionSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp495Coverage(planPack);
  const slice = validateGovernanceCoreCp495P05PermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp495Validation({
    evidence_packet: "H16.CP00-495.governance_core_p05_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP495_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_implementation_workflow_slice_pack_id: GOVERNANCE_CORE_CP495_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP495_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP495_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP495_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp495ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp495Coverage(planPack);
  const slice = validateGovernanceCoreCp495P05PermissionSliceDescriptor(createGovernanceCoreCp495P05PermissionSliceDescriptor(), {});
  return freezeCp495Validation({
    review_packet: "C16.CP00-495.governance_core_p05_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP495_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP495_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_permission_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP495_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-495 cover exactly the 10 planned RP16 units from RP16.P05.M05.S03 through RP16.P05.M05.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-495 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp495CloseoutHandoff() {
  return freezeCp495Validation({
    handoff_id: "CP00-495-to-CP00-496",
    from_pack_id: GOVERNANCE_CORE_CP495_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP495_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP495_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP495_PACK_BINDING.range,
    open_scope: "Continue RP16.P05.M05.S13 onward with the remaining permission and audit binding rows while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP495_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp496Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP496_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp496CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp496Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp496Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-496 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-496");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP496_PACK_BINDING.risk_class) errors.push("CP00-496 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP496_PACK_BINDING.unit_count) errors.push("CP00-496 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP496_PACK_BINDING.first_unit_id) errors.push("CP00-496 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP496_PACK_BINDING.last_unit_id) errors.push("CP00-496 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-496 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-496 must only include RP16 units");
  const summary = createGovernanceCoreCp496CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP496_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-496 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP496_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-496 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP496_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-496 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP496_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-496 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP496_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-496 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-496 ${microId} missing row ${title}`);
    }
  }
  return freezeCp496Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp496P05AuditBindingSliceDescriptor(
  descriptor = createGovernanceCoreCp496P05AuditBindingSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_audit_binding_slice_case_set ?? createGovernanceCoreCp496P05AuditBindingSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp496P05AuditBindingSliceDescriptor") errors.push("CP00-496 descriptor type drift");
  if (descriptor.source_p05_permission_slice_descriptor !== "GovernanceCoreCp495P05PermissionSliceDescriptor") {
    errors.push("CP00-496 source p05 permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP496_REQUIREMENTS.required_section_rows).length) errors.push("CP00-496 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP496_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-496 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-496 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-496 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-496 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-496 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-496 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-496 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP496_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-496 must not promote Claude");
  if (GOVERNANCE_CORE_CP496_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-496 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-496 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-496 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-496 contract current_pack drift");
  }
  if (
    contractProjection?.p05_audit_binding_slice_descriptor?.descriptor &&
    contractProjection.p05_audit_binding_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-496 contract p05_audit_binding_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP496_PACK_BINDING.next_pack_id) errors.push("CP00-496 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP496_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-496 next subphase drift");
  }
  return freezeCp496Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp496HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp496P05AuditBindingSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp496Coverage(planPack);
  const slice = validateGovernanceCoreCp496P05AuditBindingSliceDescriptor(descriptor, contractProjection);
  return freezeCp496Validation({
    evidence_packet: "H16.CP00-496.governance_core_p05_audit_binding_slice_descriptor",
    gate: GOVERNANCE_CORE_CP496_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_audit_binding_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_permission_slice_pack_id: GOVERNANCE_CORE_CP496_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP496_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP496_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP496_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp496ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp496Coverage(planPack);
  const slice = validateGovernanceCoreCp496P05AuditBindingSliceDescriptor(createGovernanceCoreCp496P05AuditBindingSliceDescriptor(), {});
  return freezeCp496Validation({
    review_packet: "C16.CP00-496.governance_core_p05_audit_binding_slice_descriptor",
    gate: GOVERNANCE_CORE_CP496_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP496_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_audit_binding_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP496_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-496 cover exactly the 10 planned RP16 units from RP16.P05.M05.S13 through RP16.P05.M05.S22?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-496 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp496CloseoutHandoff() {
  return freezeCp496Validation({
    handoff_id: "CP00-496-to-CP00-497",
    from_pack_id: GOVERNANCE_CORE_CP496_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP496_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP496_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP496_PACK_BINDING.range,
    open_scope: "Continue RP16.P05.M06.S01 onward with the synthetic fixture rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP496_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp497Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP497_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp497CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp497Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp497Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-497 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-497");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP497_PACK_BINDING.risk_class) errors.push("CP00-497 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP497_PACK_BINDING.unit_count) errors.push("CP00-497 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP497_PACK_BINDING.first_unit_id) errors.push("CP00-497 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP497_PACK_BINDING.last_unit_id) errors.push("CP00-497 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-497 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-497 must only include RP16 units");
  const summary = createGovernanceCoreCp497CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP497_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-497 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP497_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-497 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP497_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-497 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP497_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-497 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP497_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-497 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-497 ${microId} missing row ${title}`);
    }
  }
  return freezeCp497Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp497P05FixtureSliceDescriptor(
  descriptor = createGovernanceCoreCp497P05FixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_fixture_slice_case_set ?? createGovernanceCoreCp497P05FixtureSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp497P05FixtureSliceDescriptor") errors.push("CP00-497 descriptor type drift");
  if (descriptor.source_p05_audit_binding_slice_descriptor !== "GovernanceCoreCp496P05AuditBindingSliceDescriptor") {
    errors.push("CP00-497 source p05 audit binding slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP497_REQUIREMENTS.required_section_rows).length) errors.push("CP00-497 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP497_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-497 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-497 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-497 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-497 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-497 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-497 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-497 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP497_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-497 must not promote Claude");
  if (GOVERNANCE_CORE_CP497_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-497 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-497 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-497 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-497 contract current_pack drift");
  }
  if (
    contractProjection?.p05_fixture_slice_descriptor?.descriptor &&
    contractProjection.p05_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-497 contract p05_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP497_PACK_BINDING.next_pack_id) errors.push("CP00-497 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP497_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-497 next subphase drift");
  }
  return freezeCp497Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp497HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp497P05FixtureSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp497Coverage(planPack);
  const slice = validateGovernanceCoreCp497P05FixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp497Validation({
    evidence_packet: "H16.CP00-497.governance_core_p05_fixture_slice_descriptor",
    gate: GOVERNANCE_CORE_CP497_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_audit_binding_slice_pack_id: GOVERNANCE_CORE_CP497_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP497_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP497_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP497_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp497ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp497Coverage(planPack);
  const slice = validateGovernanceCoreCp497P05FixtureSliceDescriptor(createGovernanceCoreCp497P05FixtureSliceDescriptor(), {});
  return freezeCp497Validation({
    review_packet: "C16.CP00-497.governance_core_p05_fixture_slice_descriptor",
    gate: GOVERNANCE_CORE_CP497_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP497_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_fixture_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP497_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-497 cover exactly the 10 planned RP16 units from RP16.P05.M06.S01 through RP16.P05.M06.S10?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-497 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp497CloseoutHandoff() {
  return freezeCp497Validation({
    handoff_id: "CP00-497-to-CP00-498",
    from_pack_id: GOVERNANCE_CORE_CP497_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP497_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP497_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP497_PACK_BINDING.range,
    open_scope: "Continue RP16.P05.M06.S11 onward with the remaining synthetic fixture rows while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP497_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp498Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP498_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp498CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp498Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp498Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-498 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-498");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP498_PACK_BINDING.risk_class) errors.push("CP00-498 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP498_PACK_BINDING.unit_count) errors.push("CP00-498 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP498_PACK_BINDING.first_unit_id) errors.push("CP00-498 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP498_PACK_BINDING.last_unit_id) errors.push("CP00-498 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-498 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-498 must only include RP16 units");
  const summary = createGovernanceCoreCp498CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP498_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-498 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP498_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-498 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP498_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-498 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP498_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-498 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP498_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-498 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-498 ${microId} missing row ${title}`);
    }
  }
  return freezeCp498Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp498P05SyntheticFixtureSliceDescriptor(
  descriptor = createGovernanceCoreCp498P05SyntheticFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_synthetic_fixture_slice_case_set ?? createGovernanceCoreCp498P05SyntheticFixtureSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp498P05SyntheticFixtureSliceDescriptor") errors.push("CP00-498 descriptor type drift");
  if (descriptor.source_p05_fixture_slice_descriptor !== "GovernanceCoreCp497P05FixtureSliceDescriptor") {
    errors.push("CP00-498 source p05 fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP498_REQUIREMENTS.required_section_rows).length) errors.push("CP00-498 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP498_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-498 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-498 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-498 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-498 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-498 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-498 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-498 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP498_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-498 must not promote Claude");
  if (GOVERNANCE_CORE_CP498_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-498 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-498 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-498 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-498 contract current_pack drift");
  }
  if (
    contractProjection?.p05_synthetic_fixture_slice_descriptor?.descriptor &&
    contractProjection.p05_synthetic_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-498 contract p05_synthetic_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP498_PACK_BINDING.next_pack_id) errors.push("CP00-498 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP498_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-498 next subphase drift");
  }
  return freezeCp498Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp498HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp498P05SyntheticFixtureSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp498Coverage(planPack);
  const slice = validateGovernanceCoreCp498P05SyntheticFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp498Validation({
    evidence_packet: "H16.CP00-498.governance_core_p05_synthetic_fixture_slice_descriptor",
    gate: GOVERNANCE_CORE_CP498_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_synthetic_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_fixture_slice_pack_id: GOVERNANCE_CORE_CP498_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP498_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP498_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP498_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp498ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp498Coverage(planPack);
  const slice = validateGovernanceCoreCp498P05SyntheticFixtureSliceDescriptor(createGovernanceCoreCp498P05SyntheticFixtureSliceDescriptor(), {});
  return freezeCp498Validation({
    review_packet: "C16.CP00-498.governance_core_p05_synthetic_fixture_slice_descriptor",
    gate: GOVERNANCE_CORE_CP498_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP498_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_synthetic_fixture_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP498_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-498 cover exactly the 10 planned RP16 units from RP16.P05.M06.S11 through RP16.P05.M06.S20?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-498 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp498CloseoutHandoff() {
  return freezeCp498Validation({
    handoff_id: "CP00-498-to-CP00-499",
    from_pack_id: GOVERNANCE_CORE_CP498_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP498_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP498_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP498_PACK_BINDING.range,
    open_scope: "Continue RP16.P05.M06.S21 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP498_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp499Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP499_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp499CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp499Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp499Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-499 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-499");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP499_PACK_BINDING.risk_class) errors.push("CP00-499 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP499_PACK_BINDING.unit_count) errors.push("CP00-499 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP499_PACK_BINDING.first_unit_id) errors.push("CP00-499 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP499_PACK_BINDING.last_unit_id) errors.push("CP00-499 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-499 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-499 must only include RP16 units");
  const summary = createGovernanceCoreCp499CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP499_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-499 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP499_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-499 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP499_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-499 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP499_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-499 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP499_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-499 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-499 ${microId} missing row ${title}`);
    }
  }
  return freezeCp499Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp499P05CloseoutP06FoundationDescriptor(
  descriptor = createGovernanceCoreCp499P05CloseoutP06FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_closeout_p06_foundation_case_set ?? createGovernanceCoreCp499P05CloseoutP06FoundationCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp499P05CloseoutP06FoundationDescriptor") errors.push("CP00-499 descriptor type drift");
  if (descriptor.source_p05_synthetic_fixture_slice_descriptor !== "GovernanceCoreCp498P05SyntheticFixtureSliceDescriptor") {
    errors.push("CP00-499 source p05 synthetic fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP499_REQUIREMENTS.required_section_rows).length) errors.push("CP00-499 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP499_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-499 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-499 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-499 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-499 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-499 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-499 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-499 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP499_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-499 must not promote Claude");
  if (GOVERNANCE_CORE_CP499_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-499 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-499 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-499 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-499 contract current_pack drift");
  }
  if (
    contractProjection?.p05_closeout_p06_foundation_descriptor?.descriptor &&
    contractProjection.p05_closeout_p06_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-499 contract p05_closeout_p06_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP499_PACK_BINDING.next_pack_id) errors.push("CP00-499 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP499_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-499 next subphase drift");
  }
  return freezeCp499Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp499HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp499P05CloseoutP06FoundationDescriptor(),
) {
  const coverage = validateGovernanceCoreCp499Coverage(planPack);
  const slice = validateGovernanceCoreCp499P05CloseoutP06FoundationDescriptor(descriptor, contractProjection);
  return freezeCp499Validation({
    evidence_packet: "H16.CP00-499.governance_core_p05_closeout_p06_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP499_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_closeout_p06_foundation_valid: slice.valid,
    no_real_data: true,
    source_p05_synthetic_fixture_slice_pack_id: GOVERNANCE_CORE_CP499_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP499_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP499_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP499_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp499ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp499Coverage(planPack);
  const slice = validateGovernanceCoreCp499P05CloseoutP06FoundationDescriptor(createGovernanceCoreCp499P05CloseoutP06FoundationDescriptor(), {});
  return freezeCp499Validation({
    review_packet: "C16.CP00-499.governance_core_p05_closeout_p06_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP499_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP499_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_closeout_p06_foundation_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP499_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-499 cover exactly the 150 planned RP16 units from RP16.P05.M06.S21 through RP16.P06.M03.S10?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-499 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp499CloseoutHandoff() {
  return freezeCp499Validation({
    handoff_id: "CP00-499-to-CP00-500",
    from_pack_id: GOVERNANCE_CORE_CP499_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP499_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP499_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP499_PACK_BINDING.range,
    open_scope: "Continue RP16.P06.M03.S11 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP499_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp500Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP500_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp500CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp500Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp500Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-500 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-500");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP500_PACK_BINDING.risk_class) errors.push("CP00-500 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP500_PACK_BINDING.unit_count) errors.push("CP00-500 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP500_PACK_BINDING.first_unit_id) errors.push("CP00-500 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP500_PACK_BINDING.last_unit_id) errors.push("CP00-500 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-500 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-500 must only include RP16 units");
  const summary = createGovernanceCoreCp500CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP500_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-500 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP500_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-500 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP500_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-500 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP500_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-500 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP500_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-500 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-500 ${microId} missing row ${title}`);
    }
  }
  return freezeCp500Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp500P06ImplementationWorkflowSliceDescriptor(
  descriptor = createGovernanceCoreCp500P06ImplementationWorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_implementation_workflow_slice_case_set ?? createGovernanceCoreCp500P06ImplementationWorkflowSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp500P06ImplementationWorkflowSliceDescriptor") errors.push("CP00-500 descriptor type drift");
  if (descriptor.source_p05_closeout_p06_foundation_descriptor !== "GovernanceCoreCp499P05CloseoutP06FoundationDescriptor") {
    errors.push("CP00-500 source p05 closeout p06 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP500_REQUIREMENTS.required_section_rows).length) errors.push("CP00-500 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP500_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-500 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-500 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-500 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-500 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-500 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-500 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-500 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP500_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-500 must not promote Claude");
  if (GOVERNANCE_CORE_CP500_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-500 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-500 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-500 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-500 contract current_pack drift");
  }
  if (
    contractProjection?.p06_implementation_workflow_slice_descriptor?.descriptor &&
    contractProjection.p06_implementation_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-500 contract p06_implementation_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP500_PACK_BINDING.next_pack_id) errors.push("CP00-500 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP500_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-500 next subphase drift");
  }
  return freezeCp500Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp500HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp500P06ImplementationWorkflowSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp500Coverage(planPack);
  const slice = validateGovernanceCoreCp500P06ImplementationWorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp500Validation({
    evidence_packet: "H16.CP00-500.governance_core_p06_implementation_workflow_slice_descriptor",
    gate: GOVERNANCE_CORE_CP500_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_implementation_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_closeout_p06_foundation_pack_id: GOVERNANCE_CORE_CP500_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP500_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP500_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP500_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp500ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp500Coverage(planPack);
  const slice = validateGovernanceCoreCp500P06ImplementationWorkflowSliceDescriptor(createGovernanceCoreCp500P06ImplementationWorkflowSliceDescriptor(), {});
  return freezeCp500Validation({
    review_packet: "C16.CP00-500.governance_core_p06_implementation_workflow_slice_descriptor",
    gate: GOVERNANCE_CORE_CP500_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP500_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_implementation_workflow_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP500_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-500 cover exactly the 40 planned RP16 units from RP16.P06.M03.S11 through RP16.P06.M05.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-500 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp500CloseoutHandoff() {
  return freezeCp500Validation({
    handoff_id: "CP00-500-to-CP00-501",
    from_pack_id: GOVERNANCE_CORE_CP500_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP500_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP500_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP500_PACK_BINDING.range,
    open_scope: "Continue RP16.P06.M05.S07 onward with the remaining permission and audit binding rows while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP500_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp501Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP501_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp501CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp501Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp501Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-501 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-501");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP501_PACK_BINDING.risk_class) errors.push("CP00-501 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP501_PACK_BINDING.unit_count) errors.push("CP00-501 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP501_PACK_BINDING.first_unit_id) errors.push("CP00-501 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP501_PACK_BINDING.last_unit_id) errors.push("CP00-501 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-501 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-501 must only include RP16 units");
  const summary = createGovernanceCoreCp501CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP501_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-501 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP501_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-501 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP501_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-501 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP501_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-501 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP501_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-501 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-501 ${microId} missing row ${title}`);
    }
  }
  return freezeCp501Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp501P06PermissionSliceDescriptor(
  descriptor = createGovernanceCoreCp501P06PermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_permission_slice_case_set ?? createGovernanceCoreCp501P06PermissionSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp501P06PermissionSliceDescriptor") errors.push("CP00-501 descriptor type drift");
  if (descriptor.source_p06_implementation_workflow_slice_descriptor !== "GovernanceCoreCp500P06ImplementationWorkflowSliceDescriptor") {
    errors.push("CP00-501 source p06 implementation workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP501_REQUIREMENTS.required_section_rows).length) errors.push("CP00-501 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP501_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-501 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-501 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-501 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-501 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-501 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-501 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-501 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP501_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-501 must not promote Claude");
  if (GOVERNANCE_CORE_CP501_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-501 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-501 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-501 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-501 contract current_pack drift");
  }
  if (
    contractProjection?.p06_permission_slice_descriptor?.descriptor &&
    contractProjection.p06_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-501 contract p06_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP501_PACK_BINDING.next_pack_id) errors.push("CP00-501 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP501_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-501 next subphase drift");
  }
  return freezeCp501Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp501HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp501P06PermissionSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp501Coverage(planPack);
  const slice = validateGovernanceCoreCp501P06PermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp501Validation({
    evidence_packet: "H16.CP00-501.governance_core_p06_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP501_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_implementation_workflow_slice_pack_id: GOVERNANCE_CORE_CP501_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP501_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP501_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP501_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp501ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp501Coverage(planPack);
  const slice = validateGovernanceCoreCp501P06PermissionSliceDescriptor(createGovernanceCoreCp501P06PermissionSliceDescriptor(), {});
  return freezeCp501Validation({
    review_packet: "C16.CP00-501.governance_core_p06_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP501_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP501_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_permission_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP501_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-501 cover exactly the 10 planned RP16 units from RP16.P06.M05.S07 through RP16.P06.M05.S16?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-501 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp501CloseoutHandoff() {
  return freezeCp501Validation({
    handoff_id: "CP00-501-to-CP00-502",
    from_pack_id: GOVERNANCE_CORE_CP501_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP501_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP501_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP501_PACK_BINDING.range,
    open_scope: "Continue RP16.P06.M05.S17 onward with the remaining permission and audit binding rows and the synthetic fixture micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP501_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp502Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP502_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp502CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp502Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp502Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-502 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-502");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP502_PACK_BINDING.risk_class) errors.push("CP00-502 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP502_PACK_BINDING.unit_count) errors.push("CP00-502 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP502_PACK_BINDING.first_unit_id) errors.push("CP00-502 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP502_PACK_BINDING.last_unit_id) errors.push("CP00-502 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-502 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-502 must only include RP16 units");
  const summary = createGovernanceCoreCp502CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP502_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-502 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP502_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-502 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP502_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-502 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP502_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-502 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP502_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-502 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-502 ${microId} missing row ${title}`);
    }
  }
  return freezeCp502Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp502P06PermissionFixtureSliceDescriptor(
  descriptor = createGovernanceCoreCp502P06PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_permission_fixture_slice_case_set ?? createGovernanceCoreCp502P06PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp502P06PermissionFixtureSliceDescriptor") errors.push("CP00-502 descriptor type drift");
  if (descriptor.source_p06_permission_slice_descriptor !== "GovernanceCoreCp501P06PermissionSliceDescriptor") {
    errors.push("CP00-502 source p06 permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP502_REQUIREMENTS.required_section_rows).length) errors.push("CP00-502 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP502_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-502 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-502 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-502 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-502 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-502 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-502 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-502 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP502_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-502 must not promote Claude");
  if (GOVERNANCE_CORE_CP502_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-502 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-502 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-502 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-502 contract current_pack drift");
  }
  if (
    contractProjection?.p06_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p06_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-502 contract p06_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP502_PACK_BINDING.next_pack_id) errors.push("CP00-502 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP502_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-502 next subphase drift");
  }
  return freezeCp502Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp502HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp502P06PermissionFixtureSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp502Coverage(planPack);
  const slice = validateGovernanceCoreCp502P06PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp502Validation({
    evidence_packet: "H16.CP00-502.governance_core_p06_permission_fixture_slice_descriptor",
    gate: GOVERNANCE_CORE_CP502_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_permission_slice_pack_id: GOVERNANCE_CORE_CP502_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP502_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP502_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP502_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp502ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp502Coverage(planPack);
  const slice = validateGovernanceCoreCp502P06PermissionFixtureSliceDescriptor(createGovernanceCoreCp502P06PermissionFixtureSliceDescriptor(), {});
  return freezeCp502Validation({
    review_packet: "C16.CP00-502.governance_core_p06_permission_fixture_slice_descriptor",
    gate: GOVERNANCE_CORE_CP502_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP502_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP502_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-502 cover exactly the 10 planned RP16 units from RP16.P06.M05.S17 through RP16.P06.M06.S04?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-502 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp502CloseoutHandoff() {
  return freezeCp502Validation({
    handoff_id: "CP00-502-to-CP00-503",
    from_pack_id: GOVERNANCE_CORE_CP502_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP502_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP502_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP502_PACK_BINDING.range,
    open_scope: "Continue RP16.P06.M06.S05 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP502_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp503Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP503_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp503CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp503Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp503Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-503 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-503");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP503_PACK_BINDING.risk_class) errors.push("CP00-503 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP503_PACK_BINDING.unit_count) errors.push("CP00-503 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP503_PACK_BINDING.first_unit_id) errors.push("CP00-503 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP503_PACK_BINDING.last_unit_id) errors.push("CP00-503 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-503 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-503 must only include RP16 units");
  const summary = createGovernanceCoreCp503CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP503_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-503 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP503_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-503 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP503_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-503 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP503_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-503 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP503_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-503 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-503 ${microId} missing row ${title}`);
    }
  }
  return freezeCp503Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp503P06CloseoutP07FoundationDescriptor(
  descriptor = createGovernanceCoreCp503P06CloseoutP07FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_closeout_p07_foundation_case_set ?? createGovernanceCoreCp503P06CloseoutP07FoundationCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp503P06CloseoutP07FoundationDescriptor") errors.push("CP00-503 descriptor type drift");
  if (descriptor.source_p06_permission_fixture_slice_descriptor !== "GovernanceCoreCp502P06PermissionFixtureSliceDescriptor") {
    errors.push("CP00-503 source p06 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP503_REQUIREMENTS.required_section_rows).length) errors.push("CP00-503 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP503_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-503 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-503 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-503 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-503 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-503 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-503 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-503 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP503_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-503 must not promote Claude");
  if (GOVERNANCE_CORE_CP503_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-503 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-503 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-503 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-503 contract current_pack drift");
  }
  if (
    contractProjection?.p06_closeout_p07_foundation_descriptor?.descriptor &&
    contractProjection.p06_closeout_p07_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-503 contract p06_closeout_p07_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP503_PACK_BINDING.next_pack_id) errors.push("CP00-503 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP503_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-503 next subphase drift");
  }
  return freezeCp503Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp503HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp503P06CloseoutP07FoundationDescriptor(),
) {
  const coverage = validateGovernanceCoreCp503Coverage(planPack);
  const slice = validateGovernanceCoreCp503P06CloseoutP07FoundationDescriptor(descriptor, contractProjection);
  return freezeCp503Validation({
    evidence_packet: "H16.CP00-503.governance_core_p06_closeout_p07_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP503_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_closeout_p07_foundation_valid: slice.valid,
    no_real_data: true,
    source_p06_permission_fixture_slice_pack_id: GOVERNANCE_CORE_CP503_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP503_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP503_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP503_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp503ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp503Coverage(planPack);
  const slice = validateGovernanceCoreCp503P06CloseoutP07FoundationDescriptor(createGovernanceCoreCp503P06CloseoutP07FoundationDescriptor(), {});
  return freezeCp503Validation({
    review_packet: "C16.CP00-503.governance_core_p06_closeout_p07_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP503_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP503_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_closeout_p07_foundation_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP503_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-503 cover exactly the 150 planned RP16 units from RP16.P06.M06.S05 through RP16.P07.M02.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-503 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp503CloseoutHandoff() {
  return freezeCp503Validation({
    handoff_id: "CP00-503-to-CP00-504",
    from_pack_id: GOVERNANCE_CORE_CP503_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP503_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP503_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP503_PACK_BINDING.range,
    open_scope: "Continue RP16.P07.M02.S07 onward with the remaining type and shape rows while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP503_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp504Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP504_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp504CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp504Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp504Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-504 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-504");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP504_PACK_BINDING.risk_class) errors.push("CP00-504 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP504_PACK_BINDING.unit_count) errors.push("CP00-504 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP504_PACK_BINDING.first_unit_id) errors.push("CP00-504 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP504_PACK_BINDING.last_unit_id) errors.push("CP00-504 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-504 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-504 must only include RP16 units");
  const summary = createGovernanceCoreCp504CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP504_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-504 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP504_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-504 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP504_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-504 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP504_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-504 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP504_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-504 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-504 ${microId} missing row ${title}`);
    }
  }
  return freezeCp504Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp504P07FoundationSliceDescriptor(
  descriptor = createGovernanceCoreCp504P07FoundationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_foundation_slice_case_set ?? createGovernanceCoreCp504P07FoundationSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp504P07FoundationSliceDescriptor") errors.push("CP00-504 descriptor type drift");
  if (descriptor.source_p06_closeout_p07_foundation_descriptor !== "GovernanceCoreCp503P06CloseoutP07FoundationDescriptor") {
    errors.push("CP00-504 source p06 closeout p07 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP504_REQUIREMENTS.required_section_rows).length) errors.push("CP00-504 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP504_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-504 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-504 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-504 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-504 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-504 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-504 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-504 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP504_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-504 must not promote Claude");
  if (GOVERNANCE_CORE_CP504_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-504 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-504 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-504 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-504 contract current_pack drift");
  }
  if (
    contractProjection?.p07_foundation_slice_descriptor?.descriptor &&
    contractProjection.p07_foundation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-504 contract p07_foundation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP504_PACK_BINDING.next_pack_id) errors.push("CP00-504 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP504_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-504 next subphase drift");
  }
  return freezeCp504Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp504HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp504P07FoundationSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp504Coverage(planPack);
  const slice = validateGovernanceCoreCp504P07FoundationSliceDescriptor(descriptor, contractProjection);
  return freezeCp504Validation({
    evidence_packet: "H16.CP00-504.governance_core_p07_foundation_slice_descriptor",
    gate: GOVERNANCE_CORE_CP504_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_foundation_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_closeout_p07_foundation_pack_id: GOVERNANCE_CORE_CP504_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP504_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP504_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP504_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp504ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp504Coverage(planPack);
  const slice = validateGovernanceCoreCp504P07FoundationSliceDescriptor(createGovernanceCoreCp504P07FoundationSliceDescriptor(), {});
  return freezeCp504Validation({
    review_packet: "C16.CP00-504.governance_core_p07_foundation_slice_descriptor",
    gate: GOVERNANCE_CORE_CP504_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP504_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_foundation_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP504_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-504 cover exactly the 10 planned RP16 units from RP16.P07.M02.S07 through RP16.P07.M02.S16?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-504 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp504CloseoutHandoff() {
  return freezeCp504Validation({
    handoff_id: "CP00-504-to-CP00-505",
    from_pack_id: GOVERNANCE_CORE_CP504_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP504_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP504_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP504_PACK_BINDING.range,
    open_scope: "Continue RP16.P07.M02.S17 onward with the remaining type and shape rows and the primary implementation micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP504_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp505Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP505_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp505CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp505Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp505Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-505 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-505");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP505_PACK_BINDING.risk_class) errors.push("CP00-505 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP505_PACK_BINDING.unit_count) errors.push("CP00-505 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP505_PACK_BINDING.first_unit_id) errors.push("CP00-505 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP505_PACK_BINDING.last_unit_id) errors.push("CP00-505 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-505 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-505 must only include RP16 units");
  const summary = createGovernanceCoreCp505CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP505_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-505 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP505_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-505 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP505_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-505 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP505_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-505 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP505_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-505 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-505 ${microId} missing row ${title}`);
    }
  }
  return freezeCp505Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp505P07ImplementationSliceDescriptor(
  descriptor = createGovernanceCoreCp505P07ImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_implementation_slice_case_set ?? createGovernanceCoreCp505P07ImplementationSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp505P07ImplementationSliceDescriptor") errors.push("CP00-505 descriptor type drift");
  if (descriptor.source_p07_foundation_slice_descriptor !== "GovernanceCoreCp504P07FoundationSliceDescriptor") {
    errors.push("CP00-505 source p07 foundation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP505_REQUIREMENTS.required_section_rows).length) errors.push("CP00-505 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP505_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-505 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-505 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-505 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-505 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-505 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-505 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-505 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP505_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-505 must not promote Claude");
  if (GOVERNANCE_CORE_CP505_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-505 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-505 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-505 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-505 contract current_pack drift");
  }
  if (
    contractProjection?.p07_implementation_slice_descriptor?.descriptor &&
    contractProjection.p07_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-505 contract p07_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP505_PACK_BINDING.next_pack_id) errors.push("CP00-505 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP505_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-505 next subphase drift");
  }
  return freezeCp505Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp505HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp505P07ImplementationSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp505Coverage(planPack);
  const slice = validateGovernanceCoreCp505P07ImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp505Validation({
    evidence_packet: "H16.CP00-505.governance_core_p07_implementation_slice_descriptor",
    gate: GOVERNANCE_CORE_CP505_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_foundation_slice_pack_id: GOVERNANCE_CORE_CP505_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP505_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP505_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP505_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp505ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp505Coverage(planPack);
  const slice = validateGovernanceCoreCp505P07ImplementationSliceDescriptor(createGovernanceCoreCp505P07ImplementationSliceDescriptor(), {});
  return freezeCp505Validation({
    review_packet: "C16.CP00-505.governance_core_p07_implementation_slice_descriptor",
    gate: GOVERNANCE_CORE_CP505_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP505_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_implementation_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP505_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-505 cover exactly the 40 planned RP16 units from RP16.P07.M02.S17 through RP16.P07.M04.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-505 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp505CloseoutHandoff() {
  return freezeCp505Validation({
    handoff_id: "CP00-505-to-CP00-506",
    from_pack_id: GOVERNANCE_CORE_CP505_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP505_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP505_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP505_PACK_BINDING.range,
    open_scope: "Continue RP16.P07.M04.S13 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP505_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp506Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP506_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp506CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp506Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp506Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-506 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-506");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP506_PACK_BINDING.risk_class) errors.push("CP00-506 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP506_PACK_BINDING.unit_count) errors.push("CP00-506 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP506_PACK_BINDING.first_unit_id) errors.push("CP00-506 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP506_PACK_BINDING.last_unit_id) errors.push("CP00-506 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-506 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-506 must only include RP16 units");
  const summary = createGovernanceCoreCp506CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP506_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-506 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP506_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-506 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP506_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-506 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP506_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-506 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP506_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-506 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-506 ${microId} missing row ${title}`);
    }
  }
  return freezeCp506Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp506P07WorkflowPermissionSliceDescriptor(
  descriptor = createGovernanceCoreCp506P07WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_workflow_permission_slice_case_set ?? createGovernanceCoreCp506P07WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp506P07WorkflowPermissionSliceDescriptor") errors.push("CP00-506 descriptor type drift");
  if (descriptor.source_p07_implementation_slice_descriptor !== "GovernanceCoreCp505P07ImplementationSliceDescriptor") {
    errors.push("CP00-506 source p07 implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP506_REQUIREMENTS.required_section_rows).length) errors.push("CP00-506 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP506_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-506 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-506 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-506 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-506 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-506 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-506 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-506 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP506_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-506 must not promote Claude");
  if (GOVERNANCE_CORE_CP506_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-506 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-506 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-506 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-506 contract current_pack drift");
  }
  if (
    contractProjection?.p07_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p07_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-506 contract p07_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP506_PACK_BINDING.next_pack_id) errors.push("CP00-506 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP506_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-506 next subphase drift");
  }
  return freezeCp506Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp506HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp506P07WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp506Coverage(planPack);
  const slice = validateGovernanceCoreCp506P07WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp506Validation({
    evidence_packet: "H16.CP00-506.governance_core_p07_workflow_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP506_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_implementation_slice_pack_id: GOVERNANCE_CORE_CP506_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP506_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP506_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP506_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp506ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp506Coverage(planPack);
  const slice = validateGovernanceCoreCp506P07WorkflowPermissionSliceDescriptor(createGovernanceCoreCp506P07WorkflowPermissionSliceDescriptor(), {});
  return freezeCp506Validation({
    review_packet: "C16.CP00-506.governance_core_p07_workflow_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP506_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP506_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP506_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-506 cover exactly the 40 planned RP16 units from RP16.P07.M04.S13 through RP16.P07.M06.S08?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-506 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp506CloseoutHandoff() {
  return freezeCp506Validation({
    handoff_id: "CP00-506-to-CP00-507",
    from_pack_id: GOVERNANCE_CORE_CP506_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP506_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP506_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP506_PACK_BINDING.range,
    open_scope: "Continue RP16.P07.M06.S09 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP506_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp507Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP507_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp507CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp507Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp507Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-507 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-507");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP507_PACK_BINDING.risk_class) errors.push("CP00-507 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP507_PACK_BINDING.unit_count) errors.push("CP00-507 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP507_PACK_BINDING.first_unit_id) errors.push("CP00-507 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP507_PACK_BINDING.last_unit_id) errors.push("CP00-507 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-507 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-507 must only include RP16 units");
  const summary = createGovernanceCoreCp507CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP507_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-507 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP507_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-507 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP507_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-507 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP507_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-507 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP507_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-507 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-507 ${microId} missing row ${title}`);
    }
  }
  return freezeCp507Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp507P07CloseoutP08FoundationDescriptor(
  descriptor = createGovernanceCoreCp507P07CloseoutP08FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_closeout_p08_foundation_case_set ?? createGovernanceCoreCp507P07CloseoutP08FoundationCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp507P07CloseoutP08FoundationDescriptor") errors.push("CP00-507 descriptor type drift");
  if (descriptor.source_p07_workflow_permission_slice_descriptor !== "GovernanceCoreCp506P07WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-507 source p07 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP507_REQUIREMENTS.required_section_rows).length) errors.push("CP00-507 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP507_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-507 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-507 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-507 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-507 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-507 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-507 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-507 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP507_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-507 must not promote Claude");
  if (GOVERNANCE_CORE_CP507_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-507 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-507 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-507 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-507 contract current_pack drift");
  }
  if (
    contractProjection?.p07_closeout_p08_foundation_descriptor?.descriptor &&
    contractProjection.p07_closeout_p08_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-507 contract p07_closeout_p08_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP507_PACK_BINDING.next_pack_id) errors.push("CP00-507 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP507_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-507 next subphase drift");
  }
  return freezeCp507Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp507HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp507P07CloseoutP08FoundationDescriptor(),
) {
  const coverage = validateGovernanceCoreCp507Coverage(planPack);
  const slice = validateGovernanceCoreCp507P07CloseoutP08FoundationDescriptor(descriptor, contractProjection);
  return freezeCp507Validation({
    evidence_packet: "H16.CP00-507.governance_core_p07_closeout_p08_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP507_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_closeout_p08_foundation_valid: slice.valid,
    no_real_data: true,
    source_p07_workflow_permission_slice_pack_id: GOVERNANCE_CORE_CP507_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP507_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP507_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP507_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp507ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp507Coverage(planPack);
  const slice = validateGovernanceCoreCp507P07CloseoutP08FoundationDescriptor(createGovernanceCoreCp507P07CloseoutP08FoundationDescriptor(), {});
  return freezeCp507Validation({
    review_packet: "C16.CP00-507.governance_core_p07_closeout_p08_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP507_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP507_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_closeout_p08_foundation_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP507_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-507 cover exactly the 150 planned RP16 units from RP16.P07.M06.S09 through RP16.P08.M02.S20?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-507 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp507CloseoutHandoff() {
  return freezeCp507Validation({
    handoff_id: "CP00-507-to-CP00-508",
    from_pack_id: GOVERNANCE_CORE_CP507_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP507_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP507_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP507_PACK_BINDING.range,
    open_scope: "Continue RP16.P08.M03.S01 onward with the primary implementation rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP507_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp508Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP508_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp508CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp508Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp508Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-508 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-508");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP508_PACK_BINDING.risk_class) errors.push("CP00-508 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP508_PACK_BINDING.unit_count) errors.push("CP00-508 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP508_PACK_BINDING.first_unit_id) errors.push("CP00-508 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP508_PACK_BINDING.last_unit_id) errors.push("CP00-508 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-508 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-508 must only include RP16 units");
  const summary = createGovernanceCoreCp508CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP508_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-508 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP508_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-508 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP508_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-508 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP508_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-508 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP508_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-508 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-508 ${microId} missing row ${title}`);
    }
  }
  return freezeCp508Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp508P08ImplementationSliceDescriptor(
  descriptor = createGovernanceCoreCp508P08ImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_implementation_slice_case_set ?? createGovernanceCoreCp508P08ImplementationSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp508P08ImplementationSliceDescriptor") errors.push("CP00-508 descriptor type drift");
  if (descriptor.source_p07_closeout_p08_foundation_descriptor !== "GovernanceCoreCp507P07CloseoutP08FoundationDescriptor") {
    errors.push("CP00-508 source p07 closeout p08 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP508_REQUIREMENTS.required_section_rows).length) errors.push("CP00-508 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP508_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-508 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-508 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-508 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-508 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-508 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-508 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-508 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP508_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-508 must not promote Claude");
  if (GOVERNANCE_CORE_CP508_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-508 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-508 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-508 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-508 contract current_pack drift");
  }
  if (
    contractProjection?.p08_implementation_slice_descriptor?.descriptor &&
    contractProjection.p08_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-508 contract p08_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP508_PACK_BINDING.next_pack_id) errors.push("CP00-508 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP508_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-508 next subphase drift");
  }
  return freezeCp508Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp508HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp508P08ImplementationSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp508Coverage(planPack);
  const slice = validateGovernanceCoreCp508P08ImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp508Validation({
    evidence_packet: "H16.CP00-508.governance_core_p08_implementation_slice_descriptor",
    gate: GOVERNANCE_CORE_CP508_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_closeout_p08_foundation_pack_id: GOVERNANCE_CORE_CP508_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP508_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP508_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP508_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp508ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp508Coverage(planPack);
  const slice = validateGovernanceCoreCp508P08ImplementationSliceDescriptor(createGovernanceCoreCp508P08ImplementationSliceDescriptor(), {});
  return freezeCp508Validation({
    review_packet: "C16.CP00-508.governance_core_p08_implementation_slice_descriptor",
    gate: GOVERNANCE_CORE_CP508_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP508_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_implementation_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP508_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-508 cover exactly the 40 planned RP16 units from RP16.P08.M03.S01 through RP16.P08.M04.S18?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-508 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp508CloseoutHandoff() {
  return freezeCp508Validation({
    handoff_id: "CP00-508-to-CP00-509",
    from_pack_id: GOVERNANCE_CORE_CP508_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP508_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP508_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP508_PACK_BINDING.range,
    open_scope: "Continue RP16.P08.M04.S19 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP508_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp509Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP509_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp509CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp509Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp509Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-509 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-509");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP509_PACK_BINDING.risk_class) errors.push("CP00-509 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP509_PACK_BINDING.unit_count) errors.push("CP00-509 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP509_PACK_BINDING.first_unit_id) errors.push("CP00-509 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP509_PACK_BINDING.last_unit_id) errors.push("CP00-509 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-509 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-509 must only include RP16 units");
  const summary = createGovernanceCoreCp509CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP509_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-509 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP509_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-509 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP509_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-509 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP509_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-509 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP509_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-509 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-509 ${microId} missing row ${title}`);
    }
  }
  return freezeCp509Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp509P08WorkflowPermissionSliceDescriptor(
  descriptor = createGovernanceCoreCp509P08WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_workflow_permission_slice_case_set ?? createGovernanceCoreCp509P08WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp509P08WorkflowPermissionSliceDescriptor") errors.push("CP00-509 descriptor type drift");
  if (descriptor.source_p08_implementation_slice_descriptor !== "GovernanceCoreCp508P08ImplementationSliceDescriptor") {
    errors.push("CP00-509 source p08 implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP509_REQUIREMENTS.required_section_rows).length) errors.push("CP00-509 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP509_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-509 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-509 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-509 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-509 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-509 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-509 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-509 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP509_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-509 must not promote Claude");
  if (GOVERNANCE_CORE_CP509_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-509 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-509 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-509 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-509 contract current_pack drift");
  }
  if (
    contractProjection?.p08_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p08_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-509 contract p08_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP509_PACK_BINDING.next_pack_id) errors.push("CP00-509 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP509_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-509 next subphase drift");
  }
  return freezeCp509Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp509HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp509P08WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp509Coverage(planPack);
  const slice = validateGovernanceCoreCp509P08WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp509Validation({
    evidence_packet: "H16.CP00-509.governance_core_p08_workflow_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP509_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p08_implementation_slice_pack_id: GOVERNANCE_CORE_CP509_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP509_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP509_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP509_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp509ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp509Coverage(planPack);
  const slice = validateGovernanceCoreCp509P08WorkflowPermissionSliceDescriptor(createGovernanceCoreCp509P08WorkflowPermissionSliceDescriptor(), {});
  return freezeCp509Validation({
    review_packet: "C16.CP00-509.governance_core_p08_workflow_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP509_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP509_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP509_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-509 cover exactly the 40 planned RP16 units from RP16.P08.M04.S19 through RP16.P08.M06.S14?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-509 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp509CloseoutHandoff() {
  return freezeCp509Validation({
    handoff_id: "CP00-509-to-CP00-510",
    from_pack_id: GOVERNANCE_CORE_CP509_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP509_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP509_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP509_PACK_BINDING.range,
    open_scope: "Continue RP16.P08.M06.S15 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP509_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp510Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP510_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp510CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp510Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp510Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-510 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-510");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP510_PACK_BINDING.risk_class) errors.push("CP00-510 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP510_PACK_BINDING.unit_count) errors.push("CP00-510 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP510_PACK_BINDING.first_unit_id) errors.push("CP00-510 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP510_PACK_BINDING.last_unit_id) errors.push("CP00-510 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-510 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-510 must only include RP16 units");
  const summary = createGovernanceCoreCp510CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP510_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-510 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP510_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-510 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP510_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-510 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP510_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-510 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP510_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-510 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-510 ${microId} missing row ${title}`);
    }
  }
  return freezeCp510Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp510P08CloseoutP09FoundationDescriptor(
  descriptor = createGovernanceCoreCp510P08CloseoutP09FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_closeout_p09_foundation_case_set ?? createGovernanceCoreCp510P08CloseoutP09FoundationCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp510P08CloseoutP09FoundationDescriptor") errors.push("CP00-510 descriptor type drift");
  if (descriptor.source_p08_workflow_permission_slice_descriptor !== "GovernanceCoreCp509P08WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-510 source p08 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP510_REQUIREMENTS.required_section_rows).length) errors.push("CP00-510 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP510_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-510 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-510 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-510 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-510 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-510 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-510 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-510 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP510_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-510 must not promote Claude");
  if (GOVERNANCE_CORE_CP510_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-510 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-510 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-510 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-510 contract current_pack drift");
  }
  if (
    contractProjection?.p08_closeout_p09_foundation_descriptor?.descriptor &&
    contractProjection.p08_closeout_p09_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-510 contract p08_closeout_p09_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP510_PACK_BINDING.next_pack_id) errors.push("CP00-510 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP510_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-510 next subphase drift");
  }
  return freezeCp510Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp510HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp510P08CloseoutP09FoundationDescriptor(),
) {
  const coverage = validateGovernanceCoreCp510Coverage(planPack);
  const slice = validateGovernanceCoreCp510P08CloseoutP09FoundationDescriptor(descriptor, contractProjection);
  return freezeCp510Validation({
    evidence_packet: "H16.CP00-510.governance_core_p08_closeout_p09_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP510_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_closeout_p09_foundation_valid: slice.valid,
    no_real_data: true,
    source_p08_workflow_permission_slice_pack_id: GOVERNANCE_CORE_CP510_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP510_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP510_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP510_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp510ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp510Coverage(planPack);
  const slice = validateGovernanceCoreCp510P08CloseoutP09FoundationDescriptor(createGovernanceCoreCp510P08CloseoutP09FoundationDescriptor(), {});
  return freezeCp510Validation({
    review_packet: "C16.CP00-510.governance_core_p08_closeout_p09_foundation_descriptor",
    gate: GOVERNANCE_CORE_CP510_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP510_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_closeout_p09_foundation_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP510_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-510 cover exactly the 150 planned RP16 units from RP16.P08.M06.S15 through RP16.P09.M04.S04?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-510 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp510CloseoutHandoff() {
  return freezeCp510Validation({
    handoff_id: "CP00-510-to-CP00-511",
    from_pack_id: GOVERNANCE_CORE_CP510_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP510_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP510_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP510_PACK_BINDING.range,
    open_scope: "Continue RP16.P09.M04.S05 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP510_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp511Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP511_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp511CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp511Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp511Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-511 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-511");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP511_PACK_BINDING.risk_class) errors.push("CP00-511 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP511_PACK_BINDING.unit_count) errors.push("CP00-511 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP511_PACK_BINDING.first_unit_id) errors.push("CP00-511 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP511_PACK_BINDING.last_unit_id) errors.push("CP00-511 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-511 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-511 must only include RP16 units");
  const summary = createGovernanceCoreCp511CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP511_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-511 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP511_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-511 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP511_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-511 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP511_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-511 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP511_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-511 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-511 ${microId} missing row ${title}`);
    }
  }
  return freezeCp511Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp511P09WorkflowPermissionSliceDescriptor(
  descriptor = createGovernanceCoreCp511P09WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_workflow_permission_slice_case_set ?? createGovernanceCoreCp511P09WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp511P09WorkflowPermissionSliceDescriptor") errors.push("CP00-511 descriptor type drift");
  if (descriptor.source_p08_closeout_p09_foundation_descriptor !== "GovernanceCoreCp510P08CloseoutP09FoundationDescriptor") {
    errors.push("CP00-511 source p08 closeout p09 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP511_REQUIREMENTS.required_section_rows).length) errors.push("CP00-511 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP511_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-511 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-511 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-511 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-511 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-511 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-511 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-511 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP511_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-511 must not promote Claude");
  if (GOVERNANCE_CORE_CP511_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-511 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-511 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-511 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-511 contract current_pack drift");
  }
  if (
    contractProjection?.p09_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p09_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-511 contract p09_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP511_PACK_BINDING.next_pack_id) errors.push("CP00-511 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP511_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-511 next subphase drift");
  }
  return freezeCp511Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp511HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp511P09WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp511Coverage(planPack);
  const slice = validateGovernanceCoreCp511P09WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp511Validation({
    evidence_packet: "H16.CP00-511.governance_core_p09_workflow_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP511_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p08_closeout_p09_foundation_pack_id: GOVERNANCE_CORE_CP511_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP511_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP511_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP511_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp511ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp511Coverage(planPack);
  const slice = validateGovernanceCoreCp511P09WorkflowPermissionSliceDescriptor(createGovernanceCoreCp511P09WorkflowPermissionSliceDescriptor(), {});
  return freezeCp511Validation({
    review_packet: "C16.CP00-511.governance_core_p09_workflow_permission_slice_descriptor",
    gate: GOVERNANCE_CORE_CP511_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP511_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP511_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-511 cover exactly the 40 planned RP16 units from RP16.P09.M04.S05 through RP16.P09.M06.S02?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-511 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp511CloseoutHandoff() {
  return freezeCp511Validation({
    handoff_id: "CP00-511-to-CP00-512",
    from_pack_id: GOVERNANCE_CORE_CP511_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP511_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP511_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP511_PACK_BINDING.range,
    open_scope: "Continue RP16.P09.M06.S03 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP511_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp512Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP512_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp512CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp512Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp512Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-512 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-512");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP512_PACK_BINDING.risk_class) errors.push("CP00-512 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP512_PACK_BINDING.unit_count) errors.push("CP00-512 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP512_PACK_BINDING.first_unit_id) errors.push("CP00-512 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP512_PACK_BINDING.last_unit_id) errors.push("CP00-512 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-512 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-512 must only include RP16 units");
  const summary = createGovernanceCoreCp512CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP512_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-512 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP512_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-512 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP512_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-512 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP512_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-512 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP512_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-512 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-512 ${microId} missing row ${title}`);
    }
  }
  return freezeCp512Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp512P09FixtureSliceDescriptor(
  descriptor = createGovernanceCoreCp512P09FixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_fixture_slice_case_set ?? createGovernanceCoreCp512P09FixtureSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp512P09FixtureSliceDescriptor") errors.push("CP00-512 descriptor type drift");
  if (descriptor.source_p09_workflow_permission_slice_descriptor !== "GovernanceCoreCp511P09WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-512 source p09 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP512_REQUIREMENTS.required_section_rows).length) errors.push("CP00-512 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP512_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-512 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-512 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-512 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-512 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-512 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-512 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-512 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP512_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-512 must not promote Claude");
  if (GOVERNANCE_CORE_CP512_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-512 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-512 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-512 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id, GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-512 contract current_pack drift");
  }
  if (
    contractProjection?.p09_fixture_slice_descriptor?.descriptor &&
    contractProjection.p09_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-512 contract p09_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP512_PACK_BINDING.next_pack_id) errors.push("CP00-512 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP512_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-512 next subphase drift");
  }
  return freezeCp512Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp512HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp512P09FixtureSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp512Coverage(planPack);
  const slice = validateGovernanceCoreCp512P09FixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp512Validation({
    evidence_packet: "H16.CP00-512.governance_core_p09_fixture_slice_descriptor",
    gate: GOVERNANCE_CORE_CP512_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_workflow_permission_slice_pack_id: GOVERNANCE_CORE_CP512_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP512_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP512_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP512_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp512ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp512Coverage(planPack);
  const slice = validateGovernanceCoreCp512P09FixtureSliceDescriptor(createGovernanceCoreCp512P09FixtureSliceDescriptor(), {});
  return freezeCp512Validation({
    review_packet: "C16.CP00-512.governance_core_p09_fixture_slice_descriptor",
    gate: GOVERNANCE_CORE_CP512_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP512_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_fixture_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP512_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-512 cover exactly the 10 planned RP16 units from RP16.P09.M06.S03 through RP16.P09.M06.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-512 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp512CloseoutHandoff() {
  return freezeCp512Validation({
    handoff_id: "CP00-512-to-CP00-513",
    from_pack_id: GOVERNANCE_CORE_CP512_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP512_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP512_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP512_PACK_BINDING.range,
    open_scope: "Continue RP16.P09.M06.S13 onward with the remaining synthetic fixture rows and the closeout micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP512_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp513Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id,
    no_write_attestation: GOVERNANCE_CORE_CP513_NO_WRITE_ATTESTATION,
  });
}

export function createGovernanceCoreCp513CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp513Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateGovernanceCoreCp513Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-513 plan pack is required");
  if (planPack?.pack_id !== GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-513");
  if (planPack?.risk_class !== GOVERNANCE_CORE_CP513_PACK_BINDING.risk_class) errors.push("CP00-513 risk class drift");
  if (planPack?.unit_count !== GOVERNANCE_CORE_CP513_PACK_BINDING.unit_count) errors.push("CP00-513 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== GOVERNANCE_CORE_CP513_PACK_BINDING.first_unit_id) errors.push("CP00-513 first unit drift");
  if (unitIds.at(-1) !== GOVERNANCE_CORE_CP513_PACK_BINDING.last_unit_id) errors.push("CP00-513 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-513 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP16")) errors.push("CP00-513 must only include RP16 units");
  const summary = createGovernanceCoreCp513CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(GOVERNANCE_CORE_CP513_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-513 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(GOVERNANCE_CORE_CP513_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-513 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(GOVERNANCE_CORE_CP513_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-513 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(GOVERNANCE_CORE_CP513_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-513 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP513_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-513 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-513 ${microId} missing row ${title}`);
    }
  }
  return freezeCp513Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateGovernanceCoreCp513P09CloseoutHandoffSliceDescriptor(
  descriptor = createGovernanceCoreCp513P09CloseoutHandoffSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_closeout_handoff_slice_case_set ?? createGovernanceCoreCp513P09CloseoutHandoffSliceCaseSet();
  if (descriptor.descriptor !== "GovernanceCoreCp513P09CloseoutHandoffSliceDescriptor") errors.push("CP00-513 descriptor type drift");
  if (descriptor.source_p09_fixture_slice_descriptor !== "GovernanceCoreCp512P09FixtureSliceDescriptor") {
    errors.push("CP00-513 source p09 fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(GOVERNANCE_CORE_CP513_REQUIREMENTS.required_section_rows).length) errors.push("CP00-513 section count drift");
  for (const [microId, titles] of Object.entries(GOVERNANCE_CORE_CP513_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-513 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-513 ${microId} row count drift`);
    for (const title of titles) {
      const key = governanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-513 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-513 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-513 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-513 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-513 ${microId} ${key} must not include raw payload`);
    }
  }
  if (GOVERNANCE_CORE_CP513_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-513 must not promote Claude");
  if (GOVERNANCE_CORE_CP513_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-513 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-513 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-513 local validation trust boundary drift");
  }
  if (contractProjection?.current_pack?.pack_id && contractProjection.current_pack.pack_id !== GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id) {
    errors.push("CP00-513 contract current_pack drift");
  }
  if (
    contractProjection?.p09_closeout_handoff_slice_descriptor?.descriptor &&
    contractProjection.p09_closeout_handoff_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-513 contract p09_closeout_handoff_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== GOVERNANCE_CORE_CP513_PACK_BINDING.next_pack_id) errors.push("CP00-513 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== GOVERNANCE_CORE_CP513_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-513 next subphase drift");
  }
  return freezeCp513Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createGovernanceCoreCp513HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createGovernanceCoreCp513P09CloseoutHandoffSliceDescriptor(),
) {
  const coverage = validateGovernanceCoreCp513Coverage(planPack);
  const slice = validateGovernanceCoreCp513P09CloseoutHandoffSliceDescriptor(descriptor, contractProjection);
  return freezeCp513Validation({
    evidence_packet: "H16.CP00-513.governance_core_p09_closeout_handoff_slice_descriptor",
    gate: GOVERNANCE_CORE_CP513_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_closeout_handoff_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_fixture_slice_pack_id: GOVERNANCE_CORE_CP513_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: GOVERNANCE_CORE_CP513_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: GOVERNANCE_CORE_CP513_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP513_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createGovernanceCoreCp513ClaudeReviewPacket(planPack) {
  const coverage = validateGovernanceCoreCp513Coverage(planPack);
  const slice = validateGovernanceCoreCp513P09CloseoutHandoffSliceDescriptor(createGovernanceCoreCp513P09CloseoutHandoffSliceDescriptor(), {});
  return freezeCp513Validation({
    review_packet: "C16.CP00-513.governance_core_p09_closeout_handoff_slice_descriptor",
    gate: GOVERNANCE_CORE_CP513_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: GOVERNANCE_CORE_CP513_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_closeout_handoff_slice_valid: slice.valid,
    invalid_review_blockers: GOVERNANCE_CORE_CP513_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-513 cover exactly the 80 planned RP16 units from RP16.P09.M06.S13 through RP16.P09.M10.S10?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared governance guards applied?",
      "Does CP00-513 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createGovernanceCoreCp513CloseoutHandoff() {
  return freezeCp513Validation({
    handoff_id: "CP00-513-to-CP00-514",
    from_pack_id: GOVERNANCE_CORE_CP513_PACK_BINDING.pack_id,
    to_pack_id: GOVERNANCE_CORE_CP513_PACK_BINDING.next_pack_id,
    next_subphase_id: GOVERNANCE_CORE_CP513_PACK_BINDING.next_subphase_id,
    closed_scope: GOVERNANCE_CORE_CP513_PACK_BINDING.range,
    open_scope: "Continue RP17.P00.M00.S01 onward with the RP17 AI Governance scope inventory micros while preserving descriptor-only governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: GOVERNANCE_CORE_CP513_PACK_BINDING.production_ready_flag,
  });
}
