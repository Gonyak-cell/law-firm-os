import {
  AI_LEGAL_WORKFLOWS_CORE_CP551_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP551_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP552_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP552_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP553_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP553_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP554_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP554_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP555_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP555_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP556_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP556_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP557_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP557_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP558_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP558_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP559_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP559_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP560_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP560_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP561_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP561_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP562_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP562_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP563_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP563_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP564_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP564_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP565_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP565_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP566_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP566_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP567_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP567_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP568_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP568_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP569_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP569_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP570_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP570_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP571_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP571_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP572_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP572_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP573_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP573_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP574_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP574_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP575_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP575_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP576_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP576_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP577_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP577_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP578_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP578_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP579_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP579_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP580_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP580_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP581_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP581_REQUIREMENTS,
  AI_LEGAL_WORKFLOWS_CORE_CP582_NO_WRITE_ATTESTATION,
  AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING,
  AI_LEGAL_WORKFLOWS_CORE_CP582_REQUIREMENTS,
} from "./registry.js";
import {
  createAiLegalWorkflowsCoreCp551ScopeContractFoundationCaseSet,
  createAiLegalWorkflowsCoreCp551ScopeContractFoundationDescriptor,
  createAiLegalWorkflowsCoreCp552P01ImplementationSliceCaseSet,
  createAiLegalWorkflowsCoreCp552P01ImplementationSliceDescriptor,
  createAiLegalWorkflowsCoreCp553P01WorkflowPermissionSliceCaseSet,
  createAiLegalWorkflowsCoreCp553P01WorkflowPermissionSliceDescriptor,
  createAiLegalWorkflowsCoreCp554P01CloseoutP02FoundationCaseSet,
  createAiLegalWorkflowsCoreCp554P01CloseoutP02FoundationDescriptor,
  createAiLegalWorkflowsCoreCp555P02ImplementationSliceCaseSet,
  createAiLegalWorkflowsCoreCp555P02ImplementationSliceDescriptor,
  createAiLegalWorkflowsCoreCp556P02WorkflowSliceCaseSet,
  createAiLegalWorkflowsCoreCp556P02WorkflowSliceDescriptor,
  createAiLegalWorkflowsCoreCp557P02WorkflowPermissionSliceCaseSet,
  createAiLegalWorkflowsCoreCp557P02WorkflowPermissionSliceDescriptor,
  createAiLegalWorkflowsCoreCp558P02CloseoutP03FoundationCaseSet,
  createAiLegalWorkflowsCoreCp558P02CloseoutP03FoundationDescriptor,
  createAiLegalWorkflowsCoreCp559P03ImplementationWorkflowSliceCaseSet,
  createAiLegalWorkflowsCoreCp559P03ImplementationWorkflowSliceDescriptor,
  createAiLegalWorkflowsCoreCp560P03WorkflowPermissionSliceCaseSet,
  createAiLegalWorkflowsCoreCp560P03WorkflowPermissionSliceDescriptor,
  createAiLegalWorkflowsCoreCp561P03CloseoutP04FoundationCaseSet,
  createAiLegalWorkflowsCoreCp561P03CloseoutP04FoundationDescriptor,
  createAiLegalWorkflowsCoreCp562P04WorkflowPermissionSliceCaseSet,
  createAiLegalWorkflowsCoreCp562P04WorkflowPermissionSliceDescriptor,
  createAiLegalWorkflowsCoreCp563P04PermissionFixtureSliceCaseSet,
  createAiLegalWorkflowsCoreCp563P04PermissionFixtureSliceDescriptor,
  createAiLegalWorkflowsCoreCp564P04CloseoutP05FoundationCaseSet,
  createAiLegalWorkflowsCoreCp564P04CloseoutP05FoundationDescriptor,
  createAiLegalWorkflowsCoreCp565P05ImplementationWorkflowSliceCaseSet,
  createAiLegalWorkflowsCoreCp565P05ImplementationWorkflowSliceDescriptor,
  createAiLegalWorkflowsCoreCp566P05WorkflowPermissionSliceCaseSet,
  createAiLegalWorkflowsCoreCp566P05WorkflowPermissionSliceDescriptor,
  createAiLegalWorkflowsCoreCp567P05FixtureSliceCaseSet,
  createAiLegalWorkflowsCoreCp567P05FixtureSliceDescriptor,
  createAiLegalWorkflowsCoreCp568P05CloseoutP06FoundationCaseSet,
  createAiLegalWorkflowsCoreCp568P05CloseoutP06FoundationDescriptor,
  createAiLegalWorkflowsCoreCp569P06FoundationSliceCaseSet,
  createAiLegalWorkflowsCoreCp569P06FoundationSliceDescriptor,
  createAiLegalWorkflowsCoreCp570P06ImplementationSliceCaseSet,
  createAiLegalWorkflowsCoreCp570P06ImplementationSliceDescriptor,
  createAiLegalWorkflowsCoreCp571P06WorkflowPermissionSliceCaseSet,
  createAiLegalWorkflowsCoreCp571P06WorkflowPermissionSliceDescriptor,
  createAiLegalWorkflowsCoreCp572P06PermissionFixtureSliceCaseSet,
  createAiLegalWorkflowsCoreCp572P06PermissionFixtureSliceDescriptor,
  createAiLegalWorkflowsCoreCp573P06CloseoutP07FoundationCaseSet,
  createAiLegalWorkflowsCoreCp573P06CloseoutP07FoundationDescriptor,
  createAiLegalWorkflowsCoreCp574P07ContractImplementationSliceCaseSet,
  createAiLegalWorkflowsCoreCp574P07ContractImplementationSliceDescriptor,
  createAiLegalWorkflowsCoreCp575P07ImplementationWorkflowSliceCaseSet,
  createAiLegalWorkflowsCoreCp575P07ImplementationWorkflowSliceDescriptor,
  createAiLegalWorkflowsCoreCp576P07PermissionSliceCaseSet,
  createAiLegalWorkflowsCoreCp576P07PermissionSliceDescriptor,
  createAiLegalWorkflowsCoreCp577P07AuditBindingSliceCaseSet,
  createAiLegalWorkflowsCoreCp577P07AuditBindingSliceDescriptor,
  createAiLegalWorkflowsCoreCp578P07PermissionAuditSliceCaseSet,
  createAiLegalWorkflowsCoreCp578P07PermissionAuditSliceDescriptor,
  createAiLegalWorkflowsCoreCp579P07CloseoutP08FoundationCaseSet,
  createAiLegalWorkflowsCoreCp579P07CloseoutP08FoundationDescriptor,
  createAiLegalWorkflowsCoreCp580P08ImplementationReviewSliceCaseSet,
  createAiLegalWorkflowsCoreCp580P08ImplementationReviewSliceDescriptor,
  createAiLegalWorkflowsCoreCp581P08CloseoutP09FoundationCaseSet,
  createAiLegalWorkflowsCoreCp581P08CloseoutP09FoundationDescriptor,
  createAiLegalWorkflowsCoreCp582P09CloseoutHandoffSliceCaseSet,
  createAiLegalWorkflowsCoreCp582P09CloseoutHandoffSliceDescriptor,
  aiLegalWorkflowsCoreRowKey,
} from "./service.js";

function countBy(units, field) {
  const counts = {};
  for (const unit of units) {
    const key = unit?.[field];
    if (key === undefined) continue;
    counts[key] = (counts[key] ?? 0) + 1;
  }
  return Object.freeze(counts);
}

function freezeCp551Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP551_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp551CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp551Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp551Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-551 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-551");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.risk_class) errors.push("CP00-551 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.unit_count) errors.push("CP00-551 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.first_unit_id) errors.push("CP00-551 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.last_unit_id) errors.push("CP00-551 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-551 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-551 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp551CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP551_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-551 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP551_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-551 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP551_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-551 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP551_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-551 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP551_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-551 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-551 ${microId} missing row ${title}`);
    }
  }
  return freezeCp551Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp551ScopeContractFoundationDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp551ScopeContractFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.scope_contract_foundation_case_set ?? createAiLegalWorkflowsCoreCp551ScopeContractFoundationCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp551ScopeContractFoundationDescriptor") errors.push("CP00-551 descriptor type drift");
  if (descriptor.program_contract?.program_id !== "RP18") errors.push("CP00-551 program id drift");
  if (descriptor.program_contract?.upstream_program_id !== "RP17") errors.push("CP00-551 upstream program drift");
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP551_REQUIREMENTS.required_section_rows).length) errors.push("CP00-551 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP551_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-551 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-551 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-551 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-551 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-551 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-551 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-551 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP551_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.non_goal_boundary && rows.non_goal_boundary.ai_legal_workflows_runtime_opened !== false) {
      errors.push(`CP00-551 ${microId} must not open AI legal workflows runtime`);
    }
    if (rows.permission_baseline_note && rows.permission_baseline_note.permission_decision_detail_included !== false) {
      errors.push(`CP00-551 ${microId} permission baseline must not expose decisions`);
    }
    if (rows.permission_baseline_note && rows.permission_baseline_note.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-551 ${microId} permission baseline must deny cross-tenant access`);
    }
    if (rows.audit_baseline_note && rows.audit_baseline_note.audit_event_body_included !== false) {
      errors.push(`CP00-551 ${microId} audit baseline must not expose event bodies`);
    }
    if (rows.synthetic_data_policy && rows.synthetic_data_policy.real_client_data_loaded !== false) {
      errors.push(`CP00-551 ${microId} synthetic data policy drift`);
    }
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-551 ${microId} tenant scope must deny cross-tenant access`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-551 ${microId} matter trace must be required`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-551 ${microId} state transition map must not write state`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-551 ${microId} fixture model must not load real data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-551 ${microId} must not execute test runtime`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-551 ${microId} must not emit Hermes receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-551 ${microId} must not claim Claude final approval`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP551_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-551 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP551_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-551 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-551 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-551 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-551 contract current_pack drift");
  }
  if (
    contractProjection?.scope_contract_foundation_descriptor?.descriptor &&
    contractProjection.scope_contract_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-551 contract scope_contract_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.next_pack_id) errors.push("CP00-551 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-551 next subphase drift");
  }
  return freezeCp551Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp551HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp551ScopeContractFoundationDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp551Coverage(planPack);
  const foundation = validateAiLegalWorkflowsCoreCp551ScopeContractFoundationDescriptor(descriptor, contractProjection);
  return freezeCp551Validation({
    evidence_packet: "H18.CP00-551.ai_legal_workflows_core_scope_contract_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    scope_contract_foundation_valid: foundation.valid,
    no_real_data: true,
    source_ai_governance_core_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP551_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createAiLegalWorkflowsCoreCp551ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp551Coverage(planPack);
  const foundation = validateAiLegalWorkflowsCoreCp551ScopeContractFoundationDescriptor(createAiLegalWorkflowsCoreCp551ScopeContractFoundationDescriptor(), {});
  return freezeCp551Validation({
    review_packet: "C18.CP00-551.ai_legal_workflows_core_scope_contract_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP551_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    scope_contract_foundation_valid: foundation.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP551_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-551 cover exactly the 150 planned RP18 units from RP18.P00.M00.S01 through RP18.P01.M02.S08?",
      "Do the twenty-one descriptor sections (P00 M00-M10 scope foundation cycle and P01 M00-M08 model foundation cycle with package directory layout, primary entity identifier, tenant scope field, matter trace reference, lifecycle status enum, ownership metadata, reference relationship map, field registries, state transition map, validation helper, fixture model, serialization shape, public export, model tests, Hermes model summary, Claude model review prompt, and closeout handoff) cover every planned row title as descriptor-only rows?",
      "Does CP00-551 avoid ai-legal-workflows/precedent/clause-markup/dd-extraction runtime, runtime permission evaluation, audit event writes, state writes, object storage, command runtime execution, Hermes runtime receipts, real data, permission decision leaks, audit event body leaks, fixture payload leaks, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp551CloseoutHandoff() {
  return freezeCp551Validation({
    handoff_id: "CP00-551-to-CP00-552",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.range,
    open_scope: "Continue RP18.P01.M02.S09 onward with the remaining model foundation rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP551_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp552Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP552_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp552CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp552Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp552Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-552 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-552");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.risk_class) errors.push("CP00-552 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.unit_count) errors.push("CP00-552 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.first_unit_id) errors.push("CP00-552 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.last_unit_id) errors.push("CP00-552 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-552 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-552 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp552CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP552_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-552 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP552_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-552 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP552_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-552 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP552_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-552 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP552_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-552 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-552 ${microId} missing row ${title}`);
    }
  }
  return freezeCp552Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp552P01ImplementationSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp552P01ImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p01_implementation_slice_case_set ?? createAiLegalWorkflowsCoreCp552P01ImplementationSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp552P01ImplementationSliceDescriptor") errors.push("CP00-552 descriptor type drift");
  if (descriptor.source_scope_contract_foundation_descriptor !== "AiLegalWorkflowsCoreCp551ScopeContractFoundationDescriptor") {
    errors.push("CP00-552 source scope contract foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP552_REQUIREMENTS.required_section_rows).length) errors.push("CP00-552 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP552_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-552 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-552 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-552 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-552 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-552 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-552 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-552 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP552_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-552 ${microId} must not allow cross-tenant access`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-552 ${microId} matter trace requirement drift`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-552 ${microId} must not write state transitions`);
    }
    if (rows.validation_helper && rows.validation_helper.validation_error_detail_included !== false) {
      errors.push(`CP00-552 ${microId} must not expose validation error details`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-552 ${microId} must not load real fixture data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-552 ${microId} must not execute test runtime`);
    }
    if (rows.invalid_reference_test && rows.invalid_reference_test.expected_outcome !== "rejected_customer_safe") {
      errors.push(`CP00-552 ${microId} invalid reference outcome drift`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-552 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-552 ${microId} must not claim Claude final approval`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP552_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-552 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP552_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-552 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-552 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-552 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-552 contract current_pack drift");
  }
  if (
    contractProjection?.p01_implementation_slice_descriptor?.descriptor &&
    contractProjection.p01_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-552 contract p01_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.next_pack_id) errors.push("CP00-552 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-552 next subphase drift");
  }
  return freezeCp552Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp552HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp552P01ImplementationSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp552Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp552P01ImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp552Validation({
    evidence_packet: "H18.CP00-552.ai_legal_workflows_core_p01_implementation_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p01_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_scope_contract_foundation_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP552_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp552ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp552Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp552P01ImplementationSliceDescriptor(createAiLegalWorkflowsCoreCp552P01ImplementationSliceDescriptor(), {});
  return freezeCp552Validation({
    review_packet: "C18.CP00-552.ai_legal_workflows_core_p01_implementation_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP552_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p01_implementation_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP552_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-552 cover exactly the 40 planned RP18 units from RP18.P01.M02.S09 through RP18.P01.M04.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-552 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp552CloseoutHandoff() {
  return freezeCp552Validation({
    handoff_id: "CP00-552-to-CP00-553",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.range,
    open_scope: "Continue RP18.P01.M04.S07 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP552_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp553Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP553_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp553CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp553Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp553Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-553 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-553");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.risk_class) errors.push("CP00-553 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.unit_count) errors.push("CP00-553 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.first_unit_id) errors.push("CP00-553 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.last_unit_id) errors.push("CP00-553 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-553 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-553 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp553CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP553_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-553 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP553_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-553 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP553_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-553 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP553_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-553 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP553_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-553 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-553 ${microId} missing row ${title}`);
    }
  }
  return freezeCp553Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp553P01WorkflowPermissionSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp553P01WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p01_workflow_permission_slice_case_set ?? createAiLegalWorkflowsCoreCp553P01WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp553P01WorkflowPermissionSliceDescriptor") errors.push("CP00-553 descriptor type drift");
  if (descriptor.source_p01_implementation_slice_descriptor !== "AiLegalWorkflowsCoreCp552P01ImplementationSliceDescriptor") {
    errors.push("CP00-553 source p01 implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP553_REQUIREMENTS.required_section_rows).length) errors.push("CP00-553 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP553_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-553 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-553 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-553 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-553 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-553 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-553 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-553 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP553_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-553 ${microId} must not allow cross-tenant access`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-553 ${microId} matter trace requirement drift`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-553 ${microId} must not write state transitions`);
    }
    if (rows.validation_helper && rows.validation_helper.validation_error_detail_included !== false) {
      errors.push(`CP00-553 ${microId} must not expose validation error details`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-553 ${microId} must not load real fixture data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-553 ${microId} must not execute test runtime`);
    }
    if (rows.invalid_reference_test && rows.invalid_reference_test.expected_outcome !== "rejected_customer_safe") {
      errors.push(`CP00-553 ${microId} invalid reference outcome drift`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-553 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-553 ${microId} must not claim Claude final approval`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP553_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-553 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP553_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-553 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-553 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-553 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-553 contract current_pack drift");
  }
  if (
    contractProjection?.p01_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p01_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-553 contract p01_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.next_pack_id) errors.push("CP00-553 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-553 next subphase drift");
  }
  return freezeCp553Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp553HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp553P01WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp553Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp553P01WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp553Validation({
    evidence_packet: "H18.CP00-553.ai_legal_workflows_core_p01_workflow_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p01_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p01_implementation_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP553_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp553ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp553Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp553P01WorkflowPermissionSliceDescriptor(createAiLegalWorkflowsCoreCp553P01WorkflowPermissionSliceDescriptor(), {});
  return freezeCp553Validation({
    review_packet: "C18.CP00-553.ai_legal_workflows_core_p01_workflow_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP553_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p01_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP553_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-553 cover exactly the 40 planned RP18 units from RP18.P01.M04.S07 through RP18.P01.M06.S04?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-553 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp553CloseoutHandoff() {
  return freezeCp553Validation({
    handoff_id: "CP00-553-to-CP00-554",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.range,
    open_scope: "Continue RP18.P01.M06.S05 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP553_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp554Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP554_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp554CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp554Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp554Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-554 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-554");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.risk_class) errors.push("CP00-554 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.unit_count) errors.push("CP00-554 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.first_unit_id) errors.push("CP00-554 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.last_unit_id) errors.push("CP00-554 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-554 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-554 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp554CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP554_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-554 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP554_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-554 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP554_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-554 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP554_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-554 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP554_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-554 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-554 ${microId} missing row ${title}`);
    }
  }
  return freezeCp554Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp554P01CloseoutP02FoundationDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp554P01CloseoutP02FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p01_closeout_p02_foundation_case_set ?? createAiLegalWorkflowsCoreCp554P01CloseoutP02FoundationCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp554P01CloseoutP02FoundationDescriptor") errors.push("CP00-554 descriptor type drift");
  if (descriptor.source_p01_workflow_permission_slice_descriptor !== "AiLegalWorkflowsCoreCp553P01WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-554 source p01 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP554_REQUIREMENTS.required_section_rows).length) errors.push("CP00-554 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP554_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-554 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-554 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-554 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-554 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-554 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-554 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-554 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP554_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-554 ${microId} must not allow cross-tenant access`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-554 ${microId} matter trace requirement drift`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-554 ${microId} must not write state transitions`);
    }
    if (rows.validation_helper && rows.validation_helper.validation_error_detail_included !== false) {
      errors.push(`CP00-554 ${microId} must not expose validation error details`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-554 ${microId} must not load real fixture data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-554 ${microId} must not execute test runtime`);
    }
    if (rows.invalid_reference_test && rows.invalid_reference_test.expected_outcome !== "rejected_customer_safe") {
      errors.push(`CP00-554 ${microId} invalid reference outcome drift`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-554 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-554 ${microId} must not claim Claude final approval`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP554_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-554 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP554_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-554 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-554 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-554 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-554 contract current_pack drift");
  }
  if (
    contractProjection?.p01_closeout_p02_foundation_descriptor?.descriptor &&
    contractProjection.p01_closeout_p02_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-554 contract p01_closeout_p02_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.next_pack_id) errors.push("CP00-554 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-554 next subphase drift");
  }
  return freezeCp554Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp554HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp554P01CloseoutP02FoundationDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp554Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp554P01CloseoutP02FoundationDescriptor(descriptor, contractProjection);
  return freezeCp554Validation({
    evidence_packet: "H18.CP00-554.ai_legal_workflows_core_p01_closeout_p02_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p01_closeout_p02_foundation_valid: slice.valid,
    no_real_data: true,
    source_p01_workflow_permission_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP554_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp554ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp554Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp554P01CloseoutP02FoundationDescriptor(createAiLegalWorkflowsCoreCp554P01CloseoutP02FoundationDescriptor(), {});
  return freezeCp554Validation({
    review_packet: "C18.CP00-554.ai_legal_workflows_core_p01_closeout_p02_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP554_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p01_closeout_p02_foundation_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP554_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-554 cover exactly the 150 planned RP18 units from RP18.P01.M06.S05 through RP18.P02.M02.S22?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-554 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp554CloseoutHandoff() {
  return freezeCp554Validation({
    handoff_id: "CP00-554-to-CP00-555",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.range,
    open_scope: "Continue RP18.P02.M03.S01 onward with the primary implementation rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP554_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp555Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP555_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp555CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp555Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp555Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-555 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-555");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.risk_class) errors.push("CP00-555 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.unit_count) errors.push("CP00-555 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.first_unit_id) errors.push("CP00-555 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.last_unit_id) errors.push("CP00-555 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-555 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-555 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp555CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP555_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-555 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP555_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-555 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP555_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-555 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP555_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-555 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP555_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-555 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-555 ${microId} missing row ${title}`);
    }
  }
  return freezeCp555Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp555P02ImplementationSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp555P02ImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_implementation_slice_case_set ?? createAiLegalWorkflowsCoreCp555P02ImplementationSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp555P02ImplementationSliceDescriptor") errors.push("CP00-555 descriptor type drift");
  if (descriptor.source_p01_closeout_p02_foundation_descriptor !== "AiLegalWorkflowsCoreCp554P01CloseoutP02FoundationDescriptor") {
    errors.push("CP00-555 source p01 closeout p02 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP555_REQUIREMENTS.required_section_rows).length) errors.push("CP00-555 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP555_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-555 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-555 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-555 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-555 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-555 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-555 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-555 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP555_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-555 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP555_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-555 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-555 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-555 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-555 contract current_pack drift");
  }
  if (
    contractProjection?.p02_implementation_slice_descriptor?.descriptor &&
    contractProjection.p02_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-555 contract p02_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.next_pack_id) errors.push("CP00-555 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-555 next subphase drift");
  }
  return freezeCp555Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp555HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp555P02ImplementationSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp555Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp555P02ImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp555Validation({
    evidence_packet: "H18.CP00-555.ai_legal_workflows_core_p02_implementation_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_p01_closeout_p02_foundation_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP555_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp555ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp555Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp555P02ImplementationSliceDescriptor(createAiLegalWorkflowsCoreCp555P02ImplementationSliceDescriptor(), {});
  return freezeCp555Validation({
    review_packet: "C18.CP00-555.ai_legal_workflows_core_p02_implementation_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP555_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_implementation_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP555_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-555 cover exactly the 40 planned RP18 units from RP18.P02.M03.S01 through RP18.P02.M04.S10?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-555 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp555CloseoutHandoff() {
  return freezeCp555Validation({
    handoff_id: "CP00-555-to-CP00-556",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.range,
    open_scope: "Continue RP18.P02.M04.S11 onward with the remaining secondary workflow rows while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP555_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp556Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP556_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp556CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp556Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp556Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-556 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-556");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.risk_class) errors.push("CP00-556 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.unit_count) errors.push("CP00-556 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.first_unit_id) errors.push("CP00-556 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.last_unit_id) errors.push("CP00-556 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-556 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-556 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp556CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP556_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-556 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP556_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-556 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP556_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-556 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP556_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-556 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP556_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-556 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-556 ${microId} missing row ${title}`);
    }
  }
  return freezeCp556Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp556P02WorkflowSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp556P02WorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_workflow_slice_case_set ?? createAiLegalWorkflowsCoreCp556P02WorkflowSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp556P02WorkflowSliceDescriptor") errors.push("CP00-556 descriptor type drift");
  if (descriptor.source_p02_implementation_slice_descriptor !== "AiLegalWorkflowsCoreCp555P02ImplementationSliceDescriptor") {
    errors.push("CP00-556 source p02 implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP556_REQUIREMENTS.required_section_rows).length) errors.push("CP00-556 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP556_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-556 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-556 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-556 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-556 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-556 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-556 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-556 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP556_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-556 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP556_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-556 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-556 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-556 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-556 contract current_pack drift");
  }
  if (
    contractProjection?.p02_workflow_slice_descriptor?.descriptor &&
    contractProjection.p02_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-556 contract p02_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.next_pack_id) errors.push("CP00-556 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-556 next subphase drift");
  }
  return freezeCp556Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp556HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp556P02WorkflowSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp556Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp556P02WorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp556Validation({
    evidence_packet: "H18.CP00-556.ai_legal_workflows_core_p02_workflow_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_implementation_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP556_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp556ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp556Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp556P02WorkflowSliceDescriptor(createAiLegalWorkflowsCoreCp556P02WorkflowSliceDescriptor(), {});
  return freezeCp556Validation({
    review_packet: "C18.CP00-556.ai_legal_workflows_core_p02_workflow_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP556_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_workflow_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP556_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-556 cover exactly the 10 planned RP18 units from RP18.P02.M04.S11 through RP18.P02.M04.S20?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-556 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp556CloseoutHandoff() {
  return freezeCp556Validation({
    handoff_id: "CP00-556-to-CP00-557",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.range,
    open_scope: "Continue RP18.P02.M04.S21 onward with the remaining secondary workflow rows and the permission micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP556_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp557Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP557_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp557CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp557Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp557Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-557 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-557");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.risk_class) errors.push("CP00-557 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.unit_count) errors.push("CP00-557 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.first_unit_id) errors.push("CP00-557 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.last_unit_id) errors.push("CP00-557 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-557 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-557 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp557CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP557_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-557 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP557_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-557 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP557_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-557 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP557_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-557 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP557_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-557 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-557 ${microId} missing row ${title}`);
    }
  }
  return freezeCp557Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp557P02WorkflowPermissionSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp557P02WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_workflow_permission_slice_case_set ?? createAiLegalWorkflowsCoreCp557P02WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp557P02WorkflowPermissionSliceDescriptor") errors.push("CP00-557 descriptor type drift");
  if (descriptor.source_p02_workflow_slice_descriptor !== "AiLegalWorkflowsCoreCp556P02WorkflowSliceDescriptor") {
    errors.push("CP00-557 source p02 workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP557_REQUIREMENTS.required_section_rows).length) errors.push("CP00-557 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP557_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-557 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-557 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-557 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-557 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-557 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-557 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-557 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP557_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-557 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP557_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-557 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-557 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-557 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-557 contract current_pack drift");
  }
  if (
    contractProjection?.p02_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p02_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-557 contract p02_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.next_pack_id) errors.push("CP00-557 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-557 next subphase drift");
  }
  return freezeCp557Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp557HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp557P02WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp557Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp557P02WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp557Validation({
    evidence_packet: "H18.CP00-557.ai_legal_workflows_core_p02_workflow_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_workflow_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP557_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp557ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp557Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp557P02WorkflowPermissionSliceDescriptor(createAiLegalWorkflowsCoreCp557P02WorkflowPermissionSliceDescriptor(), {});
  return freezeCp557Validation({
    review_packet: "C18.CP00-557.ai_legal_workflows_core_p02_workflow_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP557_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP557_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-557 cover exactly the 40 planned RP18 units from RP18.P02.M04.S21 through RP18.P02.M05.S30?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-557 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp557CloseoutHandoff() {
  return freezeCp557Validation({
    handoff_id: "CP00-557-to-CP00-558",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.range,
    open_scope: "Continue RP18.P02.M06.S01 onward with the synthetic fixture rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP557_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp558Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP558_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp558CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp558Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp558Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-558 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-558");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.risk_class) errors.push("CP00-558 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.unit_count) errors.push("CP00-558 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.first_unit_id) errors.push("CP00-558 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.last_unit_id) errors.push("CP00-558 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-558 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-558 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp558CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP558_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-558 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP558_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-558 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP558_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-558 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP558_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-558 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP558_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-558 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-558 ${microId} missing row ${title}`);
    }
  }
  return freezeCp558Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp558P02CloseoutP03FoundationDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp558P02CloseoutP03FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_closeout_p03_foundation_case_set ?? createAiLegalWorkflowsCoreCp558P02CloseoutP03FoundationCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp558P02CloseoutP03FoundationDescriptor") errors.push("CP00-558 descriptor type drift");
  if (descriptor.source_p02_workflow_permission_slice_descriptor !== "AiLegalWorkflowsCoreCp557P02WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-558 source p02 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP558_REQUIREMENTS.required_section_rows).length) errors.push("CP00-558 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP558_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-558 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-558 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-558 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-558 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-558 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-558 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-558 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP558_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-558 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP558_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-558 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-558 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-558 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-558 contract current_pack drift");
  }
  if (
    contractProjection?.p02_closeout_p03_foundation_descriptor?.descriptor &&
    contractProjection.p02_closeout_p03_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-558 contract p02_closeout_p03_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.next_pack_id) errors.push("CP00-558 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-558 next subphase drift");
  }
  return freezeCp558Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp558HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp558P02CloseoutP03FoundationDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp558Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp558P02CloseoutP03FoundationDescriptor(descriptor, contractProjection);
  return freezeCp558Validation({
    evidence_packet: "H18.CP00-558.ai_legal_workflows_core_p02_closeout_p03_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_closeout_p03_foundation_valid: slice.valid,
    no_real_data: true,
    source_p02_workflow_permission_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP558_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp558ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp558Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp558P02CloseoutP03FoundationDescriptor(createAiLegalWorkflowsCoreCp558P02CloseoutP03FoundationDescriptor(), {});
  return freezeCp558Validation({
    review_packet: "C18.CP00-558.ai_legal_workflows_core_p02_closeout_p03_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP558_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_closeout_p03_foundation_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP558_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-558 cover exactly the 150 planned RP18 units from RP18.P02.M06.S01 through RP18.P03.M02.S14?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-558 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp558CloseoutHandoff() {
  return freezeCp558Validation({
    handoff_id: "CP00-558-to-CP00-559",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.range,
    open_scope: "Continue RP18.P03.M02.S15 onward with the remaining type and shape rows and the primary implementation micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP558_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp559Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP559_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp559CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp559Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp559Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-559 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-559");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.risk_class) errors.push("CP00-559 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.unit_count) errors.push("CP00-559 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.first_unit_id) errors.push("CP00-559 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.last_unit_id) errors.push("CP00-559 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-559 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-559 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp559CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP559_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-559 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP559_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-559 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP559_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-559 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP559_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-559 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP559_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-559 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-559 ${microId} missing row ${title}`);
    }
  }
  return freezeCp559Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp559P03ImplementationWorkflowSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp559P03ImplementationWorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_implementation_workflow_slice_case_set ?? createAiLegalWorkflowsCoreCp559P03ImplementationWorkflowSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp559P03ImplementationWorkflowSliceDescriptor") errors.push("CP00-559 descriptor type drift");
  if (descriptor.source_p02_closeout_p03_foundation_descriptor !== "AiLegalWorkflowsCoreCp558P02CloseoutP03FoundationDescriptor") {
    errors.push("CP00-559 source p02 closeout p03 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP559_REQUIREMENTS.required_section_rows).length) errors.push("CP00-559 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP559_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-559 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-559 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-559 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-559 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-559 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-559 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-559 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP559_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-559 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP559_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-559 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-559 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-559 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-559 contract current_pack drift");
  }
  if (
    contractProjection?.p03_implementation_workflow_slice_descriptor?.descriptor &&
    contractProjection.p03_implementation_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-559 contract p03_implementation_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.next_pack_id) errors.push("CP00-559 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-559 next subphase drift");
  }
  return freezeCp559Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp559HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp559P03ImplementationWorkflowSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp559Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp559P03ImplementationWorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp559Validation({
    evidence_packet: "H18.CP00-559.ai_legal_workflows_core_p03_implementation_workflow_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_implementation_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_closeout_p03_foundation_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP559_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp559ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp559Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp559P03ImplementationWorkflowSliceDescriptor(createAiLegalWorkflowsCoreCp559P03ImplementationWorkflowSliceDescriptor(), {});
  return freezeCp559Validation({
    review_packet: "C18.CP00-559.ai_legal_workflows_core_p03_implementation_workflow_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP559_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_implementation_workflow_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP559_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-559 cover exactly the 40 planned RP18 units from RP18.P03.M02.S15 through RP18.P03.M04.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-559 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp559CloseoutHandoff() {
  return freezeCp559Validation({
    handoff_id: "CP00-559-to-CP00-560",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.range,
    open_scope: "Continue RP18.P03.M04.S13 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP559_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp560Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP560_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp560CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp560Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp560Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-560 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-560");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.risk_class) errors.push("CP00-560 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.unit_count) errors.push("CP00-560 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.first_unit_id) errors.push("CP00-560 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.last_unit_id) errors.push("CP00-560 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-560 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-560 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp560CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP560_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-560 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP560_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-560 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP560_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-560 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP560_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-560 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP560_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-560 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-560 ${microId} missing row ${title}`);
    }
  }
  return freezeCp560Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp560P03WorkflowPermissionSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp560P03WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_workflow_permission_slice_case_set ?? createAiLegalWorkflowsCoreCp560P03WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp560P03WorkflowPermissionSliceDescriptor") errors.push("CP00-560 descriptor type drift");
  if (descriptor.source_p03_implementation_workflow_slice_descriptor !== "AiLegalWorkflowsCoreCp559P03ImplementationWorkflowSliceDescriptor") {
    errors.push("CP00-560 source p03 implementation workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP560_REQUIREMENTS.required_section_rows).length) errors.push("CP00-560 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP560_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-560 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-560 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-560 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-560 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-560 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-560 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-560 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP560_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-560 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP560_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-560 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-560 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-560 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-560 contract current_pack drift");
  }
  if (
    contractProjection?.p03_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p03_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-560 contract p03_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.next_pack_id) errors.push("CP00-560 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-560 next subphase drift");
  }
  return freezeCp560Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp560HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp560P03WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp560Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp560P03WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp560Validation({
    evidence_packet: "H18.CP00-560.ai_legal_workflows_core_p03_workflow_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p03_implementation_workflow_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP560_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp560ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp560Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp560P03WorkflowPermissionSliceDescriptor(createAiLegalWorkflowsCoreCp560P03WorkflowPermissionSliceDescriptor(), {});
  return freezeCp560Validation({
    review_packet: "C18.CP00-560.ai_legal_workflows_core_p03_workflow_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP560_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP560_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-560 cover exactly the 40 planned RP18 units from RP18.P03.M04.S13 through RP18.P03.M06.S10?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-560 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp560CloseoutHandoff() {
  return freezeCp560Validation({
    handoff_id: "CP00-560-to-CP00-561",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.range,
    open_scope: "Continue RP18.P03.M06.S11 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP560_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp561Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP561_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp561CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp561Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp561Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-561 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-561");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.risk_class) errors.push("CP00-561 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.unit_count) errors.push("CP00-561 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.first_unit_id) errors.push("CP00-561 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.last_unit_id) errors.push("CP00-561 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-561 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-561 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp561CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP561_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-561 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP561_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-561 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP561_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-561 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP561_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-561 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP561_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-561 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-561 ${microId} missing row ${title}`);
    }
  }
  return freezeCp561Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp561P03CloseoutP04FoundationDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp561P03CloseoutP04FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_closeout_p04_foundation_case_set ?? createAiLegalWorkflowsCoreCp561P03CloseoutP04FoundationCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp561P03CloseoutP04FoundationDescriptor") errors.push("CP00-561 descriptor type drift");
  if (descriptor.source_p03_workflow_permission_slice_descriptor !== "AiLegalWorkflowsCoreCp560P03WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-561 source p03 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP561_REQUIREMENTS.required_section_rows).length) errors.push("CP00-561 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP561_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-561 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-561 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-561 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-561 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-561 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-561 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-561 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP561_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-561 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP561_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-561 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-561 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-561 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-561 contract current_pack drift");
  }
  if (
    contractProjection?.p03_closeout_p04_foundation_descriptor?.descriptor &&
    contractProjection.p03_closeout_p04_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-561 contract p03_closeout_p04_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.next_pack_id) errors.push("CP00-561 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-561 next subphase drift");
  }
  return freezeCp561Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp561HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp561P03CloseoutP04FoundationDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp561Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp561P03CloseoutP04FoundationDescriptor(descriptor, contractProjection);
  return freezeCp561Validation({
    evidence_packet: "H18.CP00-561.ai_legal_workflows_core_p03_closeout_p04_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_closeout_p04_foundation_valid: slice.valid,
    no_real_data: true,
    source_p03_workflow_permission_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP561_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp561ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp561Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp561P03CloseoutP04FoundationDescriptor(createAiLegalWorkflowsCoreCp561P03CloseoutP04FoundationDescriptor(), {});
  return freezeCp561Validation({
    review_packet: "C18.CP00-561.ai_legal_workflows_core_p03_closeout_p04_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP561_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_closeout_p04_foundation_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP561_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-561 cover exactly the 150 planned RP18 units from RP18.P03.M06.S11 through RP18.P04.M03.S18?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-561 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp561CloseoutHandoff() {
  return freezeCp561Validation({
    handoff_id: "CP00-561-to-CP00-562",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.range,
    open_scope: "Continue RP18.P04.M03.S19 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP561_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp562Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP562_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp562CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp562Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp562Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-562 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-562");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.risk_class) errors.push("CP00-562 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.unit_count) errors.push("CP00-562 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.first_unit_id) errors.push("CP00-562 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.last_unit_id) errors.push("CP00-562 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-562 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-562 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp562CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP562_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-562 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP562_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-562 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP562_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-562 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP562_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-562 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP562_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-562 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-562 ${microId} missing row ${title}`);
    }
  }
  return freezeCp562Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp562P04WorkflowPermissionSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp562P04WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_workflow_permission_slice_case_set ?? createAiLegalWorkflowsCoreCp562P04WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp562P04WorkflowPermissionSliceDescriptor") errors.push("CP00-562 descriptor type drift");
  if (descriptor.source_p03_closeout_p04_foundation_descriptor !== "AiLegalWorkflowsCoreCp561P03CloseoutP04FoundationDescriptor") {
    errors.push("CP00-562 source p03 closeout p04 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP562_REQUIREMENTS.required_section_rows).length) errors.push("CP00-562 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP562_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-562 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-562 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-562 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-562 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-562 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-562 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-562 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP562_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-562 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP562_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-562 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-562 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-562 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-562 contract current_pack drift");
  }
  if (
    contractProjection?.p04_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p04_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-562 contract p04_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.next_pack_id) errors.push("CP00-562 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-562 next subphase drift");
  }
  return freezeCp562Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp562HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp562P04WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp562Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp562P04WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp562Validation({
    evidence_packet: "H18.CP00-562.ai_legal_workflows_core_p04_workflow_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p03_closeout_p04_foundation_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP562_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp562ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp562Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp562P04WorkflowPermissionSliceDescriptor(createAiLegalWorkflowsCoreCp562P04WorkflowPermissionSliceDescriptor(), {});
  return freezeCp562Validation({
    review_packet: "C18.CP00-562.ai_legal_workflows_core_p04_workflow_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP562_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP562_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-562 cover exactly the 40 planned RP18 units from RP18.P04.M03.S19 through RP18.P04.M05.S14?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-562 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp562CloseoutHandoff() {
  return freezeCp562Validation({
    handoff_id: "CP00-562-to-CP00-563",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.range,
    open_scope: "Continue RP18.P04.M05.S15 onward with the remaining permission and audit binding rows and the synthetic fixture micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP562_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp563Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP563_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp563CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp563Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp563Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-563 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-563");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.risk_class) errors.push("CP00-563 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.unit_count) errors.push("CP00-563 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.first_unit_id) errors.push("CP00-563 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.last_unit_id) errors.push("CP00-563 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-563 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-563 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp563CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP563_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-563 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP563_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-563 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP563_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-563 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP563_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-563 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP563_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-563 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-563 ${microId} missing row ${title}`);
    }
  }
  return freezeCp563Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp563P04PermissionFixtureSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp563P04PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_permission_fixture_slice_case_set ?? createAiLegalWorkflowsCoreCp563P04PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp563P04PermissionFixtureSliceDescriptor") errors.push("CP00-563 descriptor type drift");
  if (descriptor.source_p04_workflow_permission_slice_descriptor !== "AiLegalWorkflowsCoreCp562P04WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-563 source p04 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP563_REQUIREMENTS.required_section_rows).length) errors.push("CP00-563 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP563_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-563 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-563 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-563 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-563 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-563 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-563 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-563 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP563_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-563 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP563_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-563 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-563 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-563 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-563 contract current_pack drift");
  }
  if (
    contractProjection?.p04_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p04_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-563 contract p04_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.next_pack_id) errors.push("CP00-563 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-563 next subphase drift");
  }
  return freezeCp563Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp563HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp563P04PermissionFixtureSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp563Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp563P04PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp563Validation({
    evidence_packet: "H18.CP00-563.ai_legal_workflows_core_p04_permission_fixture_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p04_workflow_permission_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP563_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp563ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp563Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp563P04PermissionFixtureSliceDescriptor(createAiLegalWorkflowsCoreCp563P04PermissionFixtureSliceDescriptor(), {});
  return freezeCp563Validation({
    review_packet: "C18.CP00-563.ai_legal_workflows_core_p04_permission_fixture_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP563_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP563_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-563 cover exactly the 10 planned RP18 units from RP18.P04.M05.S15 through RP18.P04.M06.S02?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-563 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp563CloseoutHandoff() {
  return freezeCp563Validation({
    handoff_id: "CP00-563-to-CP00-564",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.range,
    open_scope: "Continue RP18.P04.M06.S03 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP563_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp564Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP564_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp564CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp564Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp564Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-564 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-564");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.risk_class) errors.push("CP00-564 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.unit_count) errors.push("CP00-564 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.first_unit_id) errors.push("CP00-564 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.last_unit_id) errors.push("CP00-564 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-564 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-564 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp564CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP564_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-564 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP564_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-564 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP564_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-564 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP564_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-564 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP564_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-564 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-564 ${microId} missing row ${title}`);
    }
  }
  return freezeCp564Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp564P04CloseoutP05FoundationDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp564P04CloseoutP05FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_closeout_p05_foundation_case_set ?? createAiLegalWorkflowsCoreCp564P04CloseoutP05FoundationCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp564P04CloseoutP05FoundationDescriptor") errors.push("CP00-564 descriptor type drift");
  if (descriptor.source_p04_permission_fixture_slice_descriptor !== "AiLegalWorkflowsCoreCp563P04PermissionFixtureSliceDescriptor") {
    errors.push("CP00-564 source p04 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP564_REQUIREMENTS.required_section_rows).length) errors.push("CP00-564 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP564_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-564 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-564 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-564 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-564 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-564 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-564 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-564 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP564_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-564 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP564_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-564 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-564 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-564 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-564 contract current_pack drift");
  }
  if (
    contractProjection?.p04_closeout_p05_foundation_descriptor?.descriptor &&
    contractProjection.p04_closeout_p05_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-564 contract p04_closeout_p05_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.next_pack_id) errors.push("CP00-564 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-564 next subphase drift");
  }
  return freezeCp564Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp564HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp564P04CloseoutP05FoundationDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp564Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp564P04CloseoutP05FoundationDescriptor(descriptor, contractProjection);
  return freezeCp564Validation({
    evidence_packet: "H18.CP00-564.ai_legal_workflows_core_p04_closeout_p05_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_closeout_p05_foundation_valid: slice.valid,
    no_real_data: true,
    source_p04_permission_fixture_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP564_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp564ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp564Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp564P04CloseoutP05FoundationDescriptor(createAiLegalWorkflowsCoreCp564P04CloseoutP05FoundationDescriptor(), {});
  return freezeCp564Validation({
    review_packet: "C18.CP00-564.ai_legal_workflows_core_p04_closeout_p05_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP564_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_closeout_p05_foundation_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP564_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-564 cover exactly the 150 planned RP18 units from RP18.P04.M06.S03 through RP18.P05.M02.S14?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-564 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp564CloseoutHandoff() {
  return freezeCp564Validation({
    handoff_id: "CP00-564-to-CP00-565",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.range,
    open_scope: "Continue RP18.P05.M02.S15 onward with the remaining type and shape rows and the primary implementation micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP564_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp565Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP565_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp565CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp565Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp565Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-565 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-565");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.risk_class) errors.push("CP00-565 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.unit_count) errors.push("CP00-565 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.first_unit_id) errors.push("CP00-565 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.last_unit_id) errors.push("CP00-565 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-565 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-565 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp565CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP565_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-565 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP565_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-565 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP565_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-565 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP565_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-565 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP565_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-565 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-565 ${microId} missing row ${title}`);
    }
  }
  return freezeCp565Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp565P05ImplementationWorkflowSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp565P05ImplementationWorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_implementation_workflow_slice_case_set ?? createAiLegalWorkflowsCoreCp565P05ImplementationWorkflowSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp565P05ImplementationWorkflowSliceDescriptor") errors.push("CP00-565 descriptor type drift");
  if (descriptor.source_p04_closeout_p05_foundation_descriptor !== "AiLegalWorkflowsCoreCp564P04CloseoutP05FoundationDescriptor") {
    errors.push("CP00-565 source p04 closeout p05 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP565_REQUIREMENTS.required_section_rows).length) errors.push("CP00-565 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP565_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-565 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-565 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-565 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-565 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-565 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-565 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-565 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP565_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-565 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP565_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-565 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-565 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-565 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-565 contract current_pack drift");
  }
  if (
    contractProjection?.p05_implementation_workflow_slice_descriptor?.descriptor &&
    contractProjection.p05_implementation_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-565 contract p05_implementation_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.next_pack_id) errors.push("CP00-565 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-565 next subphase drift");
  }
  return freezeCp565Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp565HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp565P05ImplementationWorkflowSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp565Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp565P05ImplementationWorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp565Validation({
    evidence_packet: "H18.CP00-565.ai_legal_workflows_core_p05_implementation_workflow_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_implementation_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_p04_closeout_p05_foundation_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP565_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp565ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp565Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp565P05ImplementationWorkflowSliceDescriptor(createAiLegalWorkflowsCoreCp565P05ImplementationWorkflowSliceDescriptor(), {});
  return freezeCp565Validation({
    review_packet: "C18.CP00-565.ai_legal_workflows_core_p05_implementation_workflow_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP565_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_implementation_workflow_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP565_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-565 cover exactly the 40 planned RP18 units from RP18.P05.M02.S15 through RP18.P05.M04.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-565 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp565CloseoutHandoff() {
  return freezeCp565Validation({
    handoff_id: "CP00-565-to-CP00-566",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.range,
    open_scope: "Continue RP18.P05.M04.S13 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP565_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp566Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP566_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp566CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp566Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp566Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-566 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-566");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.risk_class) errors.push("CP00-566 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.unit_count) errors.push("CP00-566 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.first_unit_id) errors.push("CP00-566 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.last_unit_id) errors.push("CP00-566 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-566 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-566 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp566CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP566_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-566 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP566_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-566 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP566_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-566 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP566_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-566 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP566_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-566 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-566 ${microId} missing row ${title}`);
    }
  }
  return freezeCp566Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp566P05WorkflowPermissionSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp566P05WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_workflow_permission_slice_case_set ?? createAiLegalWorkflowsCoreCp566P05WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp566P05WorkflowPermissionSliceDescriptor") errors.push("CP00-566 descriptor type drift");
  if (descriptor.source_p05_implementation_workflow_slice_descriptor !== "AiLegalWorkflowsCoreCp565P05ImplementationWorkflowSliceDescriptor") {
    errors.push("CP00-566 source p05 implementation workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP566_REQUIREMENTS.required_section_rows).length) errors.push("CP00-566 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP566_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-566 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-566 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-566 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-566 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-566 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-566 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-566 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP566_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-566 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP566_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-566 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-566 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-566 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-566 contract current_pack drift");
  }
  if (
    contractProjection?.p05_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p05_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-566 contract p05_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.next_pack_id) errors.push("CP00-566 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-566 next subphase drift");
  }
  return freezeCp566Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp566HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp566P05WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp566Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp566P05WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp566Validation({
    evidence_packet: "H18.CP00-566.ai_legal_workflows_core_p05_workflow_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_implementation_workflow_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP566_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp566ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp566Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp566P05WorkflowPermissionSliceDescriptor(createAiLegalWorkflowsCoreCp566P05WorkflowPermissionSliceDescriptor(), {});
  return freezeCp566Validation({
    review_packet: "C18.CP00-566.ai_legal_workflows_core_p05_workflow_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP566_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP566_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-566 cover exactly the 40 planned RP18 units from RP18.P05.M04.S13 through RP18.P05.M06.S08?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-566 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp566CloseoutHandoff() {
  return freezeCp566Validation({
    handoff_id: "CP00-566-to-CP00-567",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.range,
    open_scope: "Continue RP18.P05.M06.S09 onward with the remaining synthetic fixture rows while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP566_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp567Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP567_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp567CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp567Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp567Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-567 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-567");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.risk_class) errors.push("CP00-567 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.unit_count) errors.push("CP00-567 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.first_unit_id) errors.push("CP00-567 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.last_unit_id) errors.push("CP00-567 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-567 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-567 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp567CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP567_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-567 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP567_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-567 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP567_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-567 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP567_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-567 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP567_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-567 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-567 ${microId} missing row ${title}`);
    }
  }
  return freezeCp567Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp567P05FixtureSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp567P05FixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_fixture_slice_case_set ?? createAiLegalWorkflowsCoreCp567P05FixtureSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp567P05FixtureSliceDescriptor") errors.push("CP00-567 descriptor type drift");
  if (descriptor.source_p05_workflow_permission_slice_descriptor !== "AiLegalWorkflowsCoreCp566P05WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-567 source p05 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP567_REQUIREMENTS.required_section_rows).length) errors.push("CP00-567 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP567_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-567 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-567 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-567 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-567 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-567 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-567 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-567 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP567_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-567 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP567_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-567 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-567 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-567 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-567 contract current_pack drift");
  }
  if (
    contractProjection?.p05_fixture_slice_descriptor?.descriptor &&
    contractProjection.p05_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-567 contract p05_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.next_pack_id) errors.push("CP00-567 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-567 next subphase drift");
  }
  return freezeCp567Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp567HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp567P05FixtureSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp567Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp567P05FixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp567Validation({
    evidence_packet: "H18.CP00-567.ai_legal_workflows_core_p05_fixture_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_workflow_permission_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP567_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp567ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp567Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp567P05FixtureSliceDescriptor(createAiLegalWorkflowsCoreCp567P05FixtureSliceDescriptor(), {});
  return freezeCp567Validation({
    review_packet: "C18.CP00-567.ai_legal_workflows_core_p05_fixture_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP567_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_fixture_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP567_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-567 cover exactly the 10 planned RP18 units from RP18.P05.M06.S09 through RP18.P05.M06.S18?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-567 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp567CloseoutHandoff() {
  return freezeCp567Validation({
    handoff_id: "CP00-567-to-CP00-568",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.range,
    open_scope: "Continue RP18.P05.M06.S19 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP567_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp568Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP568_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp568CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp568Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp568Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-568 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-568");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.risk_class) errors.push("CP00-568 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.unit_count) errors.push("CP00-568 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.first_unit_id) errors.push("CP00-568 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.last_unit_id) errors.push("CP00-568 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-568 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-568 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp568CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP568_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-568 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP568_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-568 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP568_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-568 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP568_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-568 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP568_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-568 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-568 ${microId} missing row ${title}`);
    }
  }
  return freezeCp568Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp568P05CloseoutP06FoundationDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp568P05CloseoutP06FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_closeout_p06_foundation_case_set ?? createAiLegalWorkflowsCoreCp568P05CloseoutP06FoundationCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp568P05CloseoutP06FoundationDescriptor") errors.push("CP00-568 descriptor type drift");
  if (descriptor.source_p05_fixture_slice_descriptor !== "AiLegalWorkflowsCoreCp567P05FixtureSliceDescriptor") {
    errors.push("CP00-568 source p05 fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP568_REQUIREMENTS.required_section_rows).length) errors.push("CP00-568 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP568_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-568 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-568 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-568 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-568 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-568 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-568 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-568 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP568_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-568 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP568_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-568 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-568 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-568 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-568 contract current_pack drift");
  }
  if (
    contractProjection?.p05_closeout_p06_foundation_descriptor?.descriptor &&
    contractProjection.p05_closeout_p06_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-568 contract p05_closeout_p06_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.next_pack_id) errors.push("CP00-568 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-568 next subphase drift");
  }
  return freezeCp568Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp568HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp568P05CloseoutP06FoundationDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp568Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp568P05CloseoutP06FoundationDescriptor(descriptor, contractProjection);
  return freezeCp568Validation({
    evidence_packet: "H18.CP00-568.ai_legal_workflows_core_p05_closeout_p06_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_closeout_p06_foundation_valid: slice.valid,
    no_real_data: true,
    source_p05_fixture_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP568_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp568ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp568Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp568P05CloseoutP06FoundationDescriptor(createAiLegalWorkflowsCoreCp568P05CloseoutP06FoundationDescriptor(), {});
  return freezeCp568Validation({
    review_packet: "C18.CP00-568.ai_legal_workflows_core_p05_closeout_p06_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP568_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_closeout_p06_foundation_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP568_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-568 cover exactly the 150 planned RP18 units from RP18.P05.M06.S19 through RP18.P06.M02.S20?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-568 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp568CloseoutHandoff() {
  return freezeCp568Validation({
    handoff_id: "CP00-568-to-CP00-569",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.range,
    open_scope: "Continue RP18.P06.M02.S21 onward with the remaining type and shape rows and the primary implementation micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP568_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp569Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP569_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp569CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp569Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp569Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-569 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-569");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.risk_class) errors.push("CP00-569 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.unit_count) errors.push("CP00-569 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.first_unit_id) errors.push("CP00-569 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.last_unit_id) errors.push("CP00-569 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-569 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-569 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp569CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP569_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-569 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP569_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-569 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP569_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-569 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP569_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-569 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP569_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-569 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-569 ${microId} missing row ${title}`);
    }
  }
  return freezeCp569Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp569P06FoundationSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp569P06FoundationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_foundation_slice_case_set ?? createAiLegalWorkflowsCoreCp569P06FoundationSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp569P06FoundationSliceDescriptor") errors.push("CP00-569 descriptor type drift");
  if (descriptor.source_p05_closeout_p06_foundation_descriptor !== "AiLegalWorkflowsCoreCp568P05CloseoutP06FoundationDescriptor") {
    errors.push("CP00-569 source p05 closeout p06 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP569_REQUIREMENTS.required_section_rows).length) errors.push("CP00-569 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP569_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-569 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-569 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-569 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-569 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-569 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-569 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-569 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP569_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-569 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP569_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-569 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-569 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-569 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-569 contract current_pack drift");
  }
  if (
    contractProjection?.p06_foundation_slice_descriptor?.descriptor &&
    contractProjection.p06_foundation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-569 contract p06_foundation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.next_pack_id) errors.push("CP00-569 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-569 next subphase drift");
  }
  return freezeCp569Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp569HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp569P06FoundationSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp569Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp569P06FoundationSliceDescriptor(descriptor, contractProjection);
  return freezeCp569Validation({
    evidence_packet: "H18.CP00-569.ai_legal_workflows_core_p06_foundation_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_foundation_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_closeout_p06_foundation_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP569_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp569ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp569Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp569P06FoundationSliceDescriptor(createAiLegalWorkflowsCoreCp569P06FoundationSliceDescriptor(), {});
  return freezeCp569Validation({
    review_packet: "C18.CP00-569.ai_legal_workflows_core_p06_foundation_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP569_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_foundation_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP569_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-569 cover exactly the 10 planned RP18 units from RP18.P06.M02.S21 through RP18.P06.M03.S08?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-569 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp569CloseoutHandoff() {
  return freezeCp569Validation({
    handoff_id: "CP00-569-to-CP00-570",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.range,
    open_scope: "Continue RP18.P06.M03.S09 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP569_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp570Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP570_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp570CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp570Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp570Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-570 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-570");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.risk_class) errors.push("CP00-570 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.unit_count) errors.push("CP00-570 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.first_unit_id) errors.push("CP00-570 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.last_unit_id) errors.push("CP00-570 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-570 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-570 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp570CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP570_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-570 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP570_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-570 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP570_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-570 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP570_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-570 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP570_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-570 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-570 ${microId} missing row ${title}`);
    }
  }
  return freezeCp570Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp570P06ImplementationSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp570P06ImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_implementation_slice_case_set ?? createAiLegalWorkflowsCoreCp570P06ImplementationSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp570P06ImplementationSliceDescriptor") errors.push("CP00-570 descriptor type drift");
  if (descriptor.source_p06_foundation_slice_descriptor !== "AiLegalWorkflowsCoreCp569P06FoundationSliceDescriptor") {
    errors.push("CP00-570 source p06 foundation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP570_REQUIREMENTS.required_section_rows).length) errors.push("CP00-570 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP570_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-570 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-570 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-570 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-570 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-570 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-570 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-570 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP570_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-570 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP570_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-570 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-570 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-570 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-570 contract current_pack drift");
  }
  if (
    contractProjection?.p06_implementation_slice_descriptor?.descriptor &&
    contractProjection.p06_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-570 contract p06_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.next_pack_id) errors.push("CP00-570 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-570 next subphase drift");
  }
  return freezeCp570Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp570HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp570P06ImplementationSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp570Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp570P06ImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp570Validation({
    evidence_packet: "H18.CP00-570.ai_legal_workflows_core_p06_implementation_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_foundation_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP570_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp570ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp570Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp570P06ImplementationSliceDescriptor(createAiLegalWorkflowsCoreCp570P06ImplementationSliceDescriptor(), {});
  return freezeCp570Validation({
    review_packet: "C18.CP00-570.ai_legal_workflows_core_p06_implementation_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP570_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_implementation_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP570_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-570 cover exactly the 40 planned RP18 units from RP18.P06.M03.S09 through RP18.P06.M04.S18?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-570 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp570CloseoutHandoff() {
  return freezeCp570Validation({
    handoff_id: "CP00-570-to-CP00-571",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.range,
    open_scope: "Continue RP18.P06.M04.S19 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP570_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp571Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP571_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp571CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp571Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp571Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-571 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-571");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.risk_class) errors.push("CP00-571 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.unit_count) errors.push("CP00-571 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.first_unit_id) errors.push("CP00-571 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.last_unit_id) errors.push("CP00-571 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-571 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-571 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp571CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP571_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-571 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP571_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-571 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP571_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-571 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP571_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-571 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP571_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-571 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-571 ${microId} missing row ${title}`);
    }
  }
  return freezeCp571Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp571P06WorkflowPermissionSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp571P06WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_workflow_permission_slice_case_set ?? createAiLegalWorkflowsCoreCp571P06WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp571P06WorkflowPermissionSliceDescriptor") errors.push("CP00-571 descriptor type drift");
  if (descriptor.source_p06_implementation_slice_descriptor !== "AiLegalWorkflowsCoreCp570P06ImplementationSliceDescriptor") {
    errors.push("CP00-571 source p06 implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP571_REQUIREMENTS.required_section_rows).length) errors.push("CP00-571 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP571_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-571 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-571 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-571 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-571 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-571 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-571 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-571 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP571_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-571 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP571_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-571 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-571 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-571 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-571 contract current_pack drift");
  }
  if (
    contractProjection?.p06_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p06_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-571 contract p06_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.next_pack_id) errors.push("CP00-571 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-571 next subphase drift");
  }
  return freezeCp571Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp571HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp571P06WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp571Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp571P06WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp571Validation({
    evidence_packet: "H18.CP00-571.ai_legal_workflows_core_p06_workflow_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_implementation_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP571_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp571ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp571Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp571P06WorkflowPermissionSliceDescriptor(createAiLegalWorkflowsCoreCp571P06WorkflowPermissionSliceDescriptor(), {});
  return freezeCp571Validation({
    review_packet: "C18.CP00-571.ai_legal_workflows_core_p06_workflow_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP571_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP571_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-571 cover exactly the 40 planned RP18 units from RP18.P06.M04.S19 through RP18.P06.M05.S28?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-571 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp571CloseoutHandoff() {
  return freezeCp571Validation({
    handoff_id: "CP00-571-to-CP00-572",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.range,
    open_scope: "Continue RP18.P06.M05.S29 onward with the synthetic fixture rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP571_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp572Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP572_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp572CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp572Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp572Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-572 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-572");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.risk_class) errors.push("CP00-572 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.unit_count) errors.push("CP00-572 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.first_unit_id) errors.push("CP00-572 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.last_unit_id) errors.push("CP00-572 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-572 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-572 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp572CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP572_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-572 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP572_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-572 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP572_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-572 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP572_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-572 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP572_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-572 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-572 ${microId} missing row ${title}`);
    }
  }
  return freezeCp572Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp572P06PermissionFixtureSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp572P06PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_permission_fixture_slice_case_set ?? createAiLegalWorkflowsCoreCp572P06PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp572P06PermissionFixtureSliceDescriptor") errors.push("CP00-572 descriptor type drift");
  if (descriptor.source_p06_workflow_permission_slice_descriptor !== "AiLegalWorkflowsCoreCp571P06WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-572 source p06 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP572_REQUIREMENTS.required_section_rows).length) errors.push("CP00-572 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP572_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-572 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-572 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-572 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-572 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-572 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-572 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-572 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP572_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-572 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP572_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-572 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-572 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-572 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-572 contract current_pack drift");
  }
  if (
    contractProjection?.p06_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p06_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-572 contract p06_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.next_pack_id) errors.push("CP00-572 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-572 next subphase drift");
  }
  return freezeCp572Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp572HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp572P06PermissionFixtureSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp572Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp572P06PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp572Validation({
    evidence_packet: "H18.CP00-572.ai_legal_workflows_core_p06_permission_fixture_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_workflow_permission_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP572_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp572ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp572Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp572P06PermissionFixtureSliceDescriptor(createAiLegalWorkflowsCoreCp572P06PermissionFixtureSliceDescriptor(), {});
  return freezeCp572Validation({
    review_packet: "C18.CP00-572.ai_legal_workflows_core_p06_permission_fixture_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP572_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP572_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-572 cover exactly the 10 planned RP18 units from RP18.P06.M05.S29 through RP18.P06.M06.S08?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-572 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp572CloseoutHandoff() {
  return freezeCp572Validation({
    handoff_id: "CP00-572-to-CP00-573",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.range,
    open_scope: "Continue RP18.P06.M06.S09 onward with the remaining synthetic fixture rows and the closeout micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP572_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp573Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP573_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp573CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp573Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp573Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-573 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-573");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.risk_class) errors.push("CP00-573 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.unit_count) errors.push("CP00-573 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.first_unit_id) errors.push("CP00-573 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.last_unit_id) errors.push("CP00-573 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-573 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-573 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp573CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP573_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-573 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP573_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-573 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP573_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-573 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP573_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-573 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP573_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-573 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-573 ${microId} missing row ${title}`);
    }
  }
  return freezeCp573Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp573P06CloseoutP07FoundationDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp573P06CloseoutP07FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_closeout_p07_foundation_case_set ?? createAiLegalWorkflowsCoreCp573P06CloseoutP07FoundationCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp573P06CloseoutP07FoundationDescriptor") errors.push("CP00-573 descriptor type drift");
  if (descriptor.source_p06_permission_fixture_slice_descriptor !== "AiLegalWorkflowsCoreCp572P06PermissionFixtureSliceDescriptor") {
    errors.push("CP00-573 source p06 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP573_REQUIREMENTS.required_section_rows).length) errors.push("CP00-573 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP573_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-573 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-573 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-573 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-573 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-573 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-573 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-573 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP573_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-573 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP573_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-573 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-573 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-573 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-573 contract current_pack drift");
  }
  if (
    contractProjection?.p06_closeout_p07_foundation_descriptor?.descriptor &&
    contractProjection.p06_closeout_p07_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-573 contract p06_closeout_p07_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.next_pack_id) errors.push("CP00-573 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-573 next subphase drift");
  }
  return freezeCp573Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp573HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp573P06CloseoutP07FoundationDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp573Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp573P06CloseoutP07FoundationDescriptor(descriptor, contractProjection);
  return freezeCp573Validation({
    evidence_packet: "H18.CP00-573.ai_legal_workflows_core_p06_closeout_p07_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_closeout_p07_foundation_valid: slice.valid,
    no_real_data: true,
    source_p06_permission_fixture_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP573_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp573ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp573Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp573P06CloseoutP07FoundationDescriptor(createAiLegalWorkflowsCoreCp573P06CloseoutP07FoundationDescriptor(), {});
  return freezeCp573Validation({
    review_packet: "C18.CP00-573.ai_legal_workflows_core_p06_closeout_p07_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP573_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_closeout_p07_foundation_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP573_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-573 cover exactly the 150 planned RP18 units from RP18.P06.M06.S09 through RP18.P07.M02.S02?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-573 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp573CloseoutHandoff() {
  return freezeCp573Validation({
    handoff_id: "CP00-573-to-CP00-574",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.range,
    open_scope: "Continue RP18.P07.M02.S03 onward with the remaining type and shape rows and the primary implementation micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP573_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp574Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP574_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp574CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp574Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp574Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-574 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-574");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.risk_class) errors.push("CP00-574 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.unit_count) errors.push("CP00-574 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.first_unit_id) errors.push("CP00-574 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.last_unit_id) errors.push("CP00-574 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-574 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-574 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp574CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP574_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-574 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP574_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-574 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP574_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-574 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP574_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-574 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP574_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-574 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-574 ${microId} missing row ${title}`);
    }
  }
  return freezeCp574Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp574P07ContractImplementationSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp574P07ContractImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_contract_implementation_slice_case_set ?? createAiLegalWorkflowsCoreCp574P07ContractImplementationSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp574P07ContractImplementationSliceDescriptor") errors.push("CP00-574 descriptor type drift");
  if (descriptor.source_p06_closeout_p07_foundation_descriptor !== "AiLegalWorkflowsCoreCp573P06CloseoutP07FoundationDescriptor") {
    errors.push("CP00-574 source p06 closeout p07 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP574_REQUIREMENTS.required_section_rows).length) errors.push("CP00-574 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP574_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-574 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-574 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-574 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-574 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-574 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-574 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-574 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP574_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-574 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP574_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-574 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-574 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-574 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-574 contract current_pack drift");
  }
  if (
    contractProjection?.p07_contract_implementation_slice_descriptor?.descriptor &&
    contractProjection.p07_contract_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-574 contract p07_contract_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.next_pack_id) errors.push("CP00-574 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-574 next subphase drift");
  }
  return freezeCp574Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp574HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp574P07ContractImplementationSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp574Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp574P07ContractImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp574Validation({
    evidence_packet: "H18.CP00-574.ai_legal_workflows_core_p07_contract_implementation_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_contract_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_closeout_p07_foundation_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP574_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp574ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp574Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp574P07ContractImplementationSliceDescriptor(createAiLegalWorkflowsCoreCp574P07ContractImplementationSliceDescriptor(), {});
  return freezeCp574Validation({
    review_packet: "C18.CP00-574.ai_legal_workflows_core_p07_contract_implementation_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP574_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_contract_implementation_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP574_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-574 cover exactly the 40 planned RP18 units from RP18.P07.M02.S03 through RP18.P07.M03.S20?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-574 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp574CloseoutHandoff() {
  return freezeCp574Validation({
    handoff_id: "CP00-574-to-CP00-575",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.range,
    open_scope: "Continue RP18.P07.M03.S21 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP574_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp575Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP575_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp575CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp575Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp575Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-575 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-575");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.risk_class) errors.push("CP00-575 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.unit_count) errors.push("CP00-575 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.first_unit_id) errors.push("CP00-575 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.last_unit_id) errors.push("CP00-575 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-575 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-575 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp575CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP575_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-575 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP575_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-575 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP575_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-575 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP575_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-575 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP575_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-575 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-575 ${microId} missing row ${title}`);
    }
  }
  return freezeCp575Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp575P07ImplementationWorkflowSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp575P07ImplementationWorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_implementation_workflow_slice_case_set ?? createAiLegalWorkflowsCoreCp575P07ImplementationWorkflowSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp575P07ImplementationWorkflowSliceDescriptor") errors.push("CP00-575 descriptor type drift");
  if (descriptor.source_p07_contract_implementation_slice_descriptor !== "AiLegalWorkflowsCoreCp574P07ContractImplementationSliceDescriptor") {
    errors.push("CP00-575 source p07 contract implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP575_REQUIREMENTS.required_section_rows).length) errors.push("CP00-575 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP575_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-575 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-575 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-575 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-575 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-575 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-575 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-575 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP575_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-575 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP575_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-575 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-575 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-575 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-575 contract current_pack drift");
  }
  if (
    contractProjection?.p07_implementation_workflow_slice_descriptor?.descriptor &&
    contractProjection.p07_implementation_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-575 contract p07_implementation_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.next_pack_id) errors.push("CP00-575 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-575 next subphase drift");
  }
  return freezeCp575Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp575HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp575P07ImplementationWorkflowSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp575Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp575P07ImplementationWorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp575Validation({
    evidence_packet: "H18.CP00-575.ai_legal_workflows_core_p07_implementation_workflow_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_implementation_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_contract_implementation_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP575_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp575ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp575Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp575P07ImplementationWorkflowSliceDescriptor(createAiLegalWorkflowsCoreCp575P07ImplementationWorkflowSliceDescriptor(), {});
  return freezeCp575Validation({
    review_packet: "C18.CP00-575.ai_legal_workflows_core_p07_implementation_workflow_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP575_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_implementation_workflow_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP575_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-575 cover exactly the 40 planned RP18 units from RP18.P07.M03.S21 through RP18.P07.M04.S30?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-575 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp575CloseoutHandoff() {
  return freezeCp575Validation({
    handoff_id: "CP00-575-to-CP00-576",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.range,
    open_scope: "Continue RP18.P07.M05.S01 onward with the permission and audit binding micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP575_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp576Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP576_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp576CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp576Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp576Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-576 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-576");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.risk_class) errors.push("CP00-576 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.unit_count) errors.push("CP00-576 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.first_unit_id) errors.push("CP00-576 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.last_unit_id) errors.push("CP00-576 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-576 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-576 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp576CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP576_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-576 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP576_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-576 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP576_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-576 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP576_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-576 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP576_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-576 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-576 ${microId} missing row ${title}`);
    }
  }
  return freezeCp576Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp576P07PermissionSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp576P07PermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_permission_slice_case_set ?? createAiLegalWorkflowsCoreCp576P07PermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp576P07PermissionSliceDescriptor") errors.push("CP00-576 descriptor type drift");
  if (descriptor.source_p07_implementation_workflow_slice_descriptor !== "AiLegalWorkflowsCoreCp575P07ImplementationWorkflowSliceDescriptor") {
    errors.push("CP00-576 source p07 implementation workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP576_REQUIREMENTS.required_section_rows).length) errors.push("CP00-576 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP576_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-576 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-576 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-576 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-576 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-576 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-576 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-576 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP576_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-576 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP576_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-576 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-576 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-576 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-576 contract current_pack drift");
  }
  if (
    contractProjection?.p07_permission_slice_descriptor?.descriptor &&
    contractProjection.p07_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-576 contract p07_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.next_pack_id) errors.push("CP00-576 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-576 next subphase drift");
  }
  return freezeCp576Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp576HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp576P07PermissionSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp576Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp576P07PermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp576Validation({
    evidence_packet: "H18.CP00-576.ai_legal_workflows_core_p07_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_implementation_workflow_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP576_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp576ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp576Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp576P07PermissionSliceDescriptor(createAiLegalWorkflowsCoreCp576P07PermissionSliceDescriptor(), {});
  return freezeCp576Validation({
    review_packet: "C18.CP00-576.ai_legal_workflows_core_p07_permission_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP576_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP576_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-576 cover exactly the 10 planned RP18 units from RP18.P07.M05.S01 through RP18.P07.M05.S10?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-576 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp576CloseoutHandoff() {
  return freezeCp576Validation({
    handoff_id: "CP00-576-to-CP00-577",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.range,
    open_scope: "Continue RP18.P07.M05.S11 onward with the remaining permission and audit binding rows while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP576_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp577Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP577_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp577CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp577Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp577Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-577 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-577");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.risk_class) errors.push("CP00-577 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.unit_count) errors.push("CP00-577 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.first_unit_id) errors.push("CP00-577 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.last_unit_id) errors.push("CP00-577 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-577 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-577 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp577CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP577_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-577 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP577_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-577 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP577_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-577 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP577_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-577 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP577_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-577 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-577 ${microId} missing row ${title}`);
    }
  }
  return freezeCp577Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp577P07AuditBindingSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp577P07AuditBindingSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_audit_binding_slice_case_set ?? createAiLegalWorkflowsCoreCp577P07AuditBindingSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp577P07AuditBindingSliceDescriptor") errors.push("CP00-577 descriptor type drift");
  if (descriptor.source_p07_permission_slice_descriptor !== "AiLegalWorkflowsCoreCp576P07PermissionSliceDescriptor") {
    errors.push("CP00-577 source p07 permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP577_REQUIREMENTS.required_section_rows).length) errors.push("CP00-577 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP577_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-577 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-577 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-577 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-577 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-577 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-577 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-577 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP577_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-577 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP577_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-577 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-577 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-577 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-577 contract current_pack drift");
  }
  if (
    contractProjection?.p07_audit_binding_slice_descriptor?.descriptor &&
    contractProjection.p07_audit_binding_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-577 contract p07_audit_binding_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.next_pack_id) errors.push("CP00-577 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-577 next subphase drift");
  }
  return freezeCp577Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp577HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp577P07AuditBindingSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp577Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp577P07AuditBindingSliceDescriptor(descriptor, contractProjection);
  return freezeCp577Validation({
    evidence_packet: "H18.CP00-577.ai_legal_workflows_core_p07_audit_binding_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_audit_binding_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_permission_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP577_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp577ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp577Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp577P07AuditBindingSliceDescriptor(createAiLegalWorkflowsCoreCp577P07AuditBindingSliceDescriptor(), {});
  return freezeCp577Validation({
    review_packet: "C18.CP00-577.ai_legal_workflows_core_p07_audit_binding_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP577_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_audit_binding_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP577_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-577 cover exactly the 10 planned RP18 units from RP18.P07.M05.S11 through RP18.P07.M05.S20?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-577 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp577CloseoutHandoff() {
  return freezeCp577Validation({
    handoff_id: "CP00-577-to-CP00-578",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.range,
    open_scope: "Continue RP18.P07.M05.S21 onward with the remaining permission and audit binding rows while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP577_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp578Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP578_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp578CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp578Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp578Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-578 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-578");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.risk_class) errors.push("CP00-578 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.unit_count) errors.push("CP00-578 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.first_unit_id) errors.push("CP00-578 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.last_unit_id) errors.push("CP00-578 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-578 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-578 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp578CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP578_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-578 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP578_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-578 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP578_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-578 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP578_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-578 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP578_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-578 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-578 ${microId} missing row ${title}`);
    }
  }
  return freezeCp578Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp578P07PermissionAuditSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp578P07PermissionAuditSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_permission_audit_slice_case_set ?? createAiLegalWorkflowsCoreCp578P07PermissionAuditSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp578P07PermissionAuditSliceDescriptor") errors.push("CP00-578 descriptor type drift");
  if (descriptor.source_p07_audit_binding_slice_descriptor !== "AiLegalWorkflowsCoreCp577P07AuditBindingSliceDescriptor") {
    errors.push("CP00-578 source p07 audit binding slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP578_REQUIREMENTS.required_section_rows).length) errors.push("CP00-578 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP578_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-578 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-578 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-578 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-578 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-578 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-578 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-578 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP578_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-578 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP578_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-578 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-578 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-578 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-578 contract current_pack drift");
  }
  if (
    contractProjection?.p07_permission_audit_slice_descriptor?.descriptor &&
    contractProjection.p07_permission_audit_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-578 contract p07_permission_audit_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.next_pack_id) errors.push("CP00-578 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-578 next subphase drift");
  }
  return freezeCp578Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp578HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp578P07PermissionAuditSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp578Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp578P07PermissionAuditSliceDescriptor(descriptor, contractProjection);
  return freezeCp578Validation({
    evidence_packet: "H18.CP00-578.ai_legal_workflows_core_p07_permission_audit_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_permission_audit_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_audit_binding_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP578_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp578ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp578Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp578P07PermissionAuditSliceDescriptor(createAiLegalWorkflowsCoreCp578P07PermissionAuditSliceDescriptor(), {});
  return freezeCp578Validation({
    review_packet: "C18.CP00-578.ai_legal_workflows_core_p07_permission_audit_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP578_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_permission_audit_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP578_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-578 cover exactly the 10 planned RP18 units from RP18.P07.M05.S21 through RP18.P07.M05.S30?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-578 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp578CloseoutHandoff() {
  return freezeCp578Validation({
    handoff_id: "CP00-578-to-CP00-579",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.range,
    open_scope: "Continue RP18.P07.M06.S01 onward with the synthetic fixture rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP578_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp579Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP579_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp579CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp579Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp579Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-579 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-579");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.risk_class) errors.push("CP00-579 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.unit_count) errors.push("CP00-579 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.first_unit_id) errors.push("CP00-579 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.last_unit_id) errors.push("CP00-579 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-579 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-579 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp579CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP579_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-579 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP579_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-579 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP579_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-579 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP579_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-579 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP579_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-579 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-579 ${microId} missing row ${title}`);
    }
  }
  return freezeCp579Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp579P07CloseoutP08FoundationDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp579P07CloseoutP08FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_closeout_p08_foundation_case_set ?? createAiLegalWorkflowsCoreCp579P07CloseoutP08FoundationCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp579P07CloseoutP08FoundationDescriptor") errors.push("CP00-579 descriptor type drift");
  if (descriptor.source_p07_permission_audit_slice_descriptor !== "AiLegalWorkflowsCoreCp578P07PermissionAuditSliceDescriptor") {
    errors.push("CP00-579 source p07 permission audit slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP579_REQUIREMENTS.required_section_rows).length) errors.push("CP00-579 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP579_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-579 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-579 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-579 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-579 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-579 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-579 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-579 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP579_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-579 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP579_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-579 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-579 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-579 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-579 contract current_pack drift");
  }
  if (
    contractProjection?.p07_closeout_p08_foundation_descriptor?.descriptor &&
    contractProjection.p07_closeout_p08_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-579 contract p07_closeout_p08_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.next_pack_id) errors.push("CP00-579 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-579 next subphase drift");
  }
  return freezeCp579Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp579HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp579P07CloseoutP08FoundationDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp579Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp579P07CloseoutP08FoundationDescriptor(descriptor, contractProjection);
  return freezeCp579Validation({
    evidence_packet: "H18.CP00-579.ai_legal_workflows_core_p07_closeout_p08_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_closeout_p08_foundation_valid: slice.valid,
    no_real_data: true,
    source_p07_permission_audit_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP579_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp579ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp579Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp579P07CloseoutP08FoundationDescriptor(createAiLegalWorkflowsCoreCp579P07CloseoutP08FoundationDescriptor(), {});
  return freezeCp579Validation({
    review_packet: "C18.CP00-579.ai_legal_workflows_core_p07_closeout_p08_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP579_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_closeout_p08_foundation_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP579_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-579 cover exactly the 150 planned RP18 units from RP18.P07.M06.S01 through RP18.P08.M02.S04?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-579 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp579CloseoutHandoff() {
  return freezeCp579Validation({
    handoff_id: "CP00-579-to-CP00-580",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.range,
    open_scope: "Continue RP18.P08.M02.S05 onward with the remaining type and shape rows and the primary implementation micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP579_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp580Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP580_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp580CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp580Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp580Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-580 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-580");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.risk_class) errors.push("CP00-580 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.unit_count) errors.push("CP00-580 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.first_unit_id) errors.push("CP00-580 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.last_unit_id) errors.push("CP00-580 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-580 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-580 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp580CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP580_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-580 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP580_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-580 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP580_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-580 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP580_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-580 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP580_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-580 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-580 ${microId} missing row ${title}`);
    }
  }
  return freezeCp580Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp580P08ImplementationReviewSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp580P08ImplementationReviewSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_implementation_review_slice_case_set ?? createAiLegalWorkflowsCoreCp580P08ImplementationReviewSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp580P08ImplementationReviewSliceDescriptor") errors.push("CP00-580 descriptor type drift");
  if (descriptor.source_p07_closeout_p08_foundation_descriptor !== "AiLegalWorkflowsCoreCp579P07CloseoutP08FoundationDescriptor") {
    errors.push("CP00-580 source p07 closeout p08 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP580_REQUIREMENTS.required_section_rows).length) errors.push("CP00-580 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP580_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-580 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-580 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-580 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-580 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-580 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-580 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-580 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP580_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-580 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP580_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-580 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-580 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-580 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-580 contract current_pack drift");
  }
  if (
    contractProjection?.p08_implementation_review_slice_descriptor?.descriptor &&
    contractProjection.p08_implementation_review_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-580 contract p08_implementation_review_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.next_pack_id) errors.push("CP00-580 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-580 next subphase drift");
  }
  return freezeCp580Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp580HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp580P08ImplementationReviewSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp580Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp580P08ImplementationReviewSliceDescriptor(descriptor, contractProjection);
  return freezeCp580Validation({
    evidence_packet: "H18.CP00-580.ai_legal_workflows_core_p08_implementation_review_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_implementation_review_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_closeout_p08_foundation_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP580_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp580ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp580Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp580P08ImplementationReviewSliceDescriptor(createAiLegalWorkflowsCoreCp580P08ImplementationReviewSliceDescriptor(), {});
  return freezeCp580Validation({
    review_packet: "C18.CP00-580.ai_legal_workflows_core_p08_implementation_review_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP580_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_implementation_review_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP580_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-580 cover exactly the 150 planned RP18 units from RP18.P08.M02.S05 through RP18.P08.M09.S02?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-580 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp580CloseoutHandoff() {
  return freezeCp580Validation({
    handoff_id: "CP00-580-to-CP00-581",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.range,
    open_scope: "Continue RP18.P08.M09.S03 onward with the remaining claude review rows and the closeout micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP580_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp581Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP581_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp581CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp581Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp581Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-581 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-581");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.risk_class) errors.push("CP00-581 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.unit_count) errors.push("CP00-581 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.first_unit_id) errors.push("CP00-581 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.last_unit_id) errors.push("CP00-581 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-581 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-581 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp581CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP581_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-581 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP581_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-581 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP581_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-581 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP581_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-581 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP581_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-581 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-581 ${microId} missing row ${title}`);
    }
  }
  return freezeCp581Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp581P08CloseoutP09FoundationDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp581P08CloseoutP09FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_closeout_p09_foundation_case_set ?? createAiLegalWorkflowsCoreCp581P08CloseoutP09FoundationCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp581P08CloseoutP09FoundationDescriptor") errors.push("CP00-581 descriptor type drift");
  if (descriptor.source_p08_implementation_review_slice_descriptor !== "AiLegalWorkflowsCoreCp580P08ImplementationReviewSliceDescriptor") {
    errors.push("CP00-581 source p08 implementation review slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP581_REQUIREMENTS.required_section_rows).length) errors.push("CP00-581 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP581_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-581 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-581 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-581 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-581 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-581 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-581 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-581 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP581_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-581 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP581_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-581 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-581 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-581 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id, AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-581 contract current_pack drift");
  }
  if (
    contractProjection?.p08_closeout_p09_foundation_descriptor?.descriptor &&
    contractProjection.p08_closeout_p09_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-581 contract p08_closeout_p09_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.next_pack_id) errors.push("CP00-581 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-581 next subphase drift");
  }
  return freezeCp581Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp581HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp581P08CloseoutP09FoundationDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp581Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp581P08CloseoutP09FoundationDescriptor(descriptor, contractProjection);
  return freezeCp581Validation({
    evidence_packet: "H18.CP00-581.ai_legal_workflows_core_p08_closeout_p09_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_closeout_p09_foundation_valid: slice.valid,
    no_real_data: true,
    source_p08_implementation_review_slice_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP581_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp581ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp581Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp581P08CloseoutP09FoundationDescriptor(createAiLegalWorkflowsCoreCp581P08CloseoutP09FoundationDescriptor(), {});
  return freezeCp581Validation({
    review_packet: "C18.CP00-581.ai_legal_workflows_core_p08_closeout_p09_foundation_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP581_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_closeout_p09_foundation_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP581_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-581 cover exactly the 150 planned RP18 units from RP18.P08.M09.S03 through RP18.P09.M06.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-581 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp581CloseoutHandoff() {
  return freezeCp581Validation({
    handoff_id: "CP00-581-to-CP00-582",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.range,
    open_scope: "Continue RP18.P09.M06.S07 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP581_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp582Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id,
    no_write_attestation: AI_LEGAL_WORKFLOWS_CORE_CP582_NO_WRITE_ATTESTATION,
  });
}

export function createAiLegalWorkflowsCoreCp582CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp582Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiLegalWorkflowsCoreCp582Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-582 plan pack is required");
  if (planPack?.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-582");
  if (planPack?.risk_class !== AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.risk_class) errors.push("CP00-582 risk class drift");
  if (planPack?.unit_count !== AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.unit_count) errors.push("CP00-582 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.first_unit_id) errors.push("CP00-582 first unit drift");
  if (unitIds.at(-1) !== AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.last_unit_id) errors.push("CP00-582 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-582 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP18")) errors.push("CP00-582 must only include RP18 units");
  const summary = createAiLegalWorkflowsCoreCp582CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP582_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-582 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP582_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-582 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP582_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-582 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP582_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-582 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP582_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-582 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-582 ${microId} missing row ${title}`);
    }
  }
  return freezeCp582Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiLegalWorkflowsCoreCp582P09CloseoutHandoffSliceDescriptor(
  descriptor = createAiLegalWorkflowsCoreCp582P09CloseoutHandoffSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_closeout_handoff_slice_case_set ?? createAiLegalWorkflowsCoreCp582P09CloseoutHandoffSliceCaseSet();
  if (descriptor.descriptor !== "AiLegalWorkflowsCoreCp582P09CloseoutHandoffSliceDescriptor") errors.push("CP00-582 descriptor type drift");
  if (descriptor.source_p08_closeout_p09_foundation_descriptor !== "AiLegalWorkflowsCoreCp581P08CloseoutP09FoundationDescriptor") {
    errors.push("CP00-582 source p08 closeout p09 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_LEGAL_WORKFLOWS_CORE_CP582_REQUIREMENTS.required_section_rows).length) errors.push("CP00-582 section count drift");
  for (const [microId, titles] of Object.entries(AI_LEGAL_WORKFLOWS_CORE_CP582_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-582 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-582 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiLegalWorkflowsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-582 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-582 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-582 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-582 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-582 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_LEGAL_WORKFLOWS_CORE_CP582_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-582 must not promote Claude");
  if (AI_LEGAL_WORKFLOWS_CORE_CP582_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-582 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-582 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-582 local validation trust boundary drift");
  }
  if (contractProjection?.current_pack?.pack_id && contractProjection.current_pack.pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id) {
    errors.push("CP00-582 contract current_pack drift");
  }
  if (
    contractProjection?.p09_closeout_handoff_slice_descriptor?.descriptor &&
    contractProjection.p09_closeout_handoff_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-582 contract p09_closeout_handoff_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.next_pack_id) errors.push("CP00-582 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-582 next subphase drift");
  }
  return freezeCp582Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiLegalWorkflowsCoreCp582HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiLegalWorkflowsCoreCp582P09CloseoutHandoffSliceDescriptor(),
) {
  const coverage = validateAiLegalWorkflowsCoreCp582Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp582P09CloseoutHandoffSliceDescriptor(descriptor, contractProjection);
  return freezeCp582Validation({
    evidence_packet: "H18.CP00-582.ai_legal_workflows_core_p09_closeout_handoff_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_closeout_handoff_slice_valid: slice.valid,
    no_real_data: true,
    source_p08_closeout_p09_foundation_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_LEGAL_WORKFLOWS_CORE_CP582_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiLegalWorkflowsCoreCp582ClaudeReviewPacket(planPack) {
  const coverage = validateAiLegalWorkflowsCoreCp582Coverage(planPack);
  const slice = validateAiLegalWorkflowsCoreCp582P09CloseoutHandoffSliceDescriptor(createAiLegalWorkflowsCoreCp582P09CloseoutHandoffSliceDescriptor(), {});
  return freezeCp582Validation({
    review_packet: "C18.CP00-582.ai_legal_workflows_core_p09_closeout_handoff_slice_descriptor",
    gate: AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_LEGAL_WORKFLOWS_CORE_CP582_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_closeout_handoff_slice_valid: slice.valid,
    invalid_review_blockers: AI_LEGAL_WORKFLOWS_CORE_CP582_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-582 cover exactly the 86 planned RP18 units from RP18.P09.M06.S07 through RP18.P09.M10.S10?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI legal workflows guards applied?",
      "Does CP00-582 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiLegalWorkflowsCoreCp582CloseoutHandoff() {
  return freezeCp582Validation({
    handoff_id: "CP00-582-to-CP00-583",
    from_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.pack_id,
    to_pack_id: AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.next_subphase_id,
    closed_scope: AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.range,
    open_scope: "Continue RP19.P00.M00.S01 onward with the RP19 Client Portal scope inventory micros while preserving descriptor-only AI legal workflows boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_LEGAL_WORKFLOWS_CORE_CP582_PACK_BINDING.production_ready_flag,
  });
}
