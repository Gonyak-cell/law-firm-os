import assert from "node:assert/strict";
import test from "node:test";
import { existsSync, readFileSync } from "node:fs";
import * as finance from "../src/index.js";

function readJson(path) { return JSON.parse(readFileSync(path, "utf8")); }
function planPack(binding) {
  const plan = readJson("docs/closeout-pack-plan/closeout-pack-plan.json");
  const fromPlan = plan.packs.find((item) => item.pack_id === binding.pack_id);
  if (fromPlan) return fromPlan;
  const manifestPath = "docs/closeout-packs/" + binding.pack_id.toLowerCase() + "/manifest.json";
  if (existsSync(manifestPath)) return readJson(manifestPath).plan_binding_snapshot;
  return undefined;
}

test("CP00-694 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP694_PACK_BINDING; assert.equal(binding.pack_id, "CP00-694"); assert.equal(binding.unit_count, 150); const coverage = finance.validateFinanceIntegrationsCp694ScopeContractFoundationCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP694_REQUIREMENTS.deliverable_counts); });
test("CP00-694 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP694_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp694ScopeContractFoundationDescriptor(); const validation = finance.validateFinanceIntegrationsCp694ScopeContractFoundationDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); const hermes = finance.createFinanceIntegrationsCp694ScopeContractFoundationHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp694ScopeContractFoundationClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp694ScopeContractFoundationCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-695 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP695_PACK_BINDING; assert.equal(binding.pack_id, "CP00-695"); assert.equal(binding.unit_count, 40); const coverage = finance.validateFinanceIntegrationsCp695DomainModelWorkflowCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP695_REQUIREMENTS.deliverable_counts); });
test("CP00-695 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP695_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp695DomainModelWorkflowDescriptor(); const validation = finance.validateFinanceIntegrationsCp695DomainModelWorkflowDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); const hermes = finance.createFinanceIntegrationsCp695DomainModelWorkflowHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp695DomainModelWorkflowClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp695DomainModelWorkflowCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-696 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP696_PACK_BINDING; assert.equal(binding.pack_id, "CP00-696"); assert.equal(binding.unit_count, 40); const coverage = finance.validateFinanceIntegrationsCp696WorkflowPermissionFixtureCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP696_REQUIREMENTS.deliverable_counts); });
test("CP00-696 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP696_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp696WorkflowPermissionFixtureDescriptor(); const validation = finance.validateFinanceIntegrationsCp696WorkflowPermissionFixtureDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); const hermes = finance.createFinanceIntegrationsCp696WorkflowPermissionFixtureHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp696WorkflowPermissionFixtureClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp696WorkflowPermissionFixtureCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-697 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP697_PACK_BINDING; assert.equal(binding.pack_id, "CP00-697"); assert.equal(binding.unit_count, 150); const coverage = finance.validateFinanceIntegrationsCp697SyntheticEvidenceScopeContractCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP697_REQUIREMENTS.deliverable_counts); });
test("CP00-697 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP697_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp697SyntheticEvidenceScopeContractDescriptor(); const validation = finance.validateFinanceIntegrationsCp697SyntheticEvidenceScopeContractDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-698"); const hermes = finance.createFinanceIntegrationsCp697SyntheticEvidenceScopeContractHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp697SyntheticEvidenceScopeContractClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp697SyntheticEvidenceScopeContractCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-698 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP698_PACK_BINDING; assert.equal(binding.pack_id, "CP00-698"); assert.equal(binding.unit_count, 40); const coverage = finance.validateFinanceIntegrationsCp698P02ImplementationWorkflowCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP698_REQUIREMENTS.deliverable_counts); });
test("CP00-698 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP698_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp698P02ImplementationWorkflowDescriptor(); const validation = finance.validateFinanceIntegrationsCp698P02ImplementationWorkflowDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-699"); const hermes = finance.createFinanceIntegrationsCp698P02ImplementationWorkflowHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp698P02ImplementationWorkflowClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp698P02ImplementationWorkflowCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-699 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP699_PACK_BINDING; assert.equal(binding.pack_id, "CP00-699"); assert.equal(binding.unit_count, 40); const coverage = finance.validateFinanceIntegrationsCp699P02PermissionFixtureCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP699_REQUIREMENTS.deliverable_counts); });
test("CP00-699 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP699_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp699P02PermissionFixtureDescriptor(); const validation = finance.validateFinanceIntegrationsCp699P02PermissionFixtureDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-700"); const hermes = finance.createFinanceIntegrationsCp699P02PermissionFixtureHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp699P02PermissionFixtureClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp699P02PermissionFixtureCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-700 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP700_PACK_BINDING; assert.equal(binding.pack_id, "CP00-700"); assert.equal(binding.unit_count, 10); const coverage = finance.validateFinanceIntegrationsCp700P02FixtureTestBridgeCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP700_REQUIREMENTS.deliverable_counts); });
test("CP00-700 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP700_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp700P02FixtureTestBridgeDescriptor(); const validation = finance.validateFinanceIntegrationsCp700P02FixtureTestBridgeDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-701"); const hermes = finance.createFinanceIntegrationsCp700P02FixtureTestBridgeHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp700P02FixtureTestBridgeClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp700P02FixtureTestBridgeCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-701 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP701_PACK_BINDING; assert.equal(binding.pack_id, "CP00-701"); assert.equal(binding.unit_count, 10); const coverage = finance.validateFinanceIntegrationsCp701P02TestGoldenCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP701_REQUIREMENTS.deliverable_counts); });
test("CP00-701 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP701_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp701P02TestGoldenDescriptor(); const validation = finance.validateFinanceIntegrationsCp701P02TestGoldenDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-702"); const hermes = finance.createFinanceIntegrationsCp701P02TestGoldenHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp701P02TestGoldenClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp701P02TestGoldenCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-702 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP702_PACK_BINDING; assert.equal(binding.pack_id, "CP00-702"); assert.equal(binding.unit_count, 40); const coverage = finance.validateFinanceIntegrationsCp702P02TestEvidenceReviewBridgeCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP702_REQUIREMENTS.deliverable_counts); });
test("CP00-702 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP702_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp702P02TestEvidenceReviewBridgeDescriptor(); const validation = finance.validateFinanceIntegrationsCp702P02TestEvidenceReviewBridgeDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-703"); const hermes = finance.createFinanceIntegrationsCp702P02TestEvidenceReviewBridgeHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp702P02TestEvidenceReviewBridgeClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp702P02TestEvidenceReviewBridgeCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-703 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP703_PACK_BINDING; assert.equal(binding.pack_id, "CP00-703"); assert.equal(binding.unit_count, 150); const coverage = finance.validateFinanceIntegrationsCp703P02ReviewCloseoutP03FoundationCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP703_REQUIREMENTS.deliverable_counts); });
test("CP00-703 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP703_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp703P02ReviewCloseoutP03FoundationDescriptor(); const validation = finance.validateFinanceIntegrationsCp703P02ReviewCloseoutP03FoundationDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-704"); const hermes = finance.createFinanceIntegrationsCp703P02ReviewCloseoutP03FoundationHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp703P02ReviewCloseoutP03FoundationClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp703P02ReviewCloseoutP03FoundationCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-704 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP704_PACK_BINDING; assert.equal(binding.pack_id, "CP00-704"); assert.equal(binding.unit_count, 150); const coverage = finance.validateFinanceIntegrationsCp704P03FixtureReviewCloseoutP04UiFoundationCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP704_REQUIREMENTS.deliverable_counts); });
test("CP00-704 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP704_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp704P03FixtureReviewCloseoutP04UiFoundationDescriptor(); const validation = finance.validateFinanceIntegrationsCp704P03FixtureReviewCloseoutP04UiFoundationDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-705"); const hermes = finance.createFinanceIntegrationsCp704P03FixtureReviewCloseoutP04UiFoundationHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp704P03FixtureReviewCloseoutP04UiFoundationClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp704P03FixtureReviewCloseoutP04UiFoundationCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-705 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP705_PACK_BINDING; assert.equal(binding.pack_id, "CP00-705"); assert.equal(binding.unit_count, 40); const coverage = finance.validateFinanceIntegrationsCp705P04ImplementationWorkflowPermissionSliceCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP705_REQUIREMENTS.deliverable_counts); });
test("CP00-705 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP705_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp705P04ImplementationWorkflowPermissionSliceDescriptor(); const validation = finance.validateFinanceIntegrationsCp705P04ImplementationWorkflowPermissionSliceDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-706"); const hermes = finance.createFinanceIntegrationsCp705P04ImplementationWorkflowPermissionSliceHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp705P04ImplementationWorkflowPermissionSliceClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp705P04ImplementationWorkflowPermissionSliceCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-706 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP706_PACK_BINDING; assert.equal(binding.pack_id, "CP00-706"); assert.equal(binding.unit_count, 10); const coverage = finance.validateFinanceIntegrationsCp706P04PermissionFixtureBridgeCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP706_REQUIREMENTS.deliverable_counts); });
test("CP00-706 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP706_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp706P04PermissionFixtureBridgeDescriptor(); const validation = finance.validateFinanceIntegrationsCp706P04PermissionFixtureBridgeDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-707"); const hermes = finance.createFinanceIntegrationsCp706P04PermissionFixtureBridgeHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp706P04PermissionFixtureBridgeClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp706P04PermissionFixtureBridgeCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-707 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP707_PACK_BINDING; assert.equal(binding.pack_id, "CP00-707"); assert.equal(binding.unit_count, 150); const coverage = finance.validateFinanceIntegrationsCp707P04FixtureEvidenceP05ContractBridgeCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP707_REQUIREMENTS.deliverable_counts); });
test("CP00-707 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP707_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp707P04FixtureEvidenceP05ContractBridgeDescriptor(); const validation = finance.validateFinanceIntegrationsCp707P04FixtureEvidenceP05ContractBridgeDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-708"); const hermes = finance.createFinanceIntegrationsCp707P04FixtureEvidenceP05ContractBridgeHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp707P04FixtureEvidenceP05ContractBridgeClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp707P04FixtureEvidenceP05ContractBridgeCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-708 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP708_PACK_BINDING; assert.equal(binding.pack_id, "CP00-708"); assert.equal(binding.unit_count, 10); const coverage = finance.validateFinanceIntegrationsCp708P05PrimaryImplementationTailCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP708_REQUIREMENTS.deliverable_counts); });
test("CP00-708 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP708_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp708P05PrimaryImplementationTailDescriptor(); const validation = finance.validateFinanceIntegrationsCp708P05PrimaryImplementationTailDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-709"); const hermes = finance.createFinanceIntegrationsCp708P05PrimaryImplementationTailHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp708P05PrimaryImplementationTailClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp708P05PrimaryImplementationTailCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-709 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP709_PACK_BINDING; assert.equal(binding.pack_id, "CP00-709"); assert.equal(binding.unit_count, 150); const coverage = finance.validateFinanceIntegrationsCp709P05CloseoutP06ScopeBridgeCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP709_REQUIREMENTS.deliverable_counts); });
test("CP00-709 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP709_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp709P05CloseoutP06ScopeBridgeDescriptor(); const validation = finance.validateFinanceIntegrationsCp709P05CloseoutP06ScopeBridgeDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-710"); const hermes = finance.createFinanceIntegrationsCp709P05CloseoutP06ScopeBridgeHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp709P05CloseoutP06ScopeBridgeClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp709P05CloseoutP06ScopeBridgeCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-710 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP710_PACK_BINDING; assert.equal(binding.pack_id, "CP00-710"); assert.equal(binding.unit_count, 150); const coverage = finance.validateFinanceIntegrationsCp710P06ScopeInventoryTestGoldenBridgeCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP710_REQUIREMENTS.deliverable_counts); });
test("CP00-710 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP710_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp710P06ScopeInventoryTestGoldenBridgeDescriptor(); const validation = finance.validateFinanceIntegrationsCp710P06ScopeInventoryTestGoldenBridgeDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-711"); const hermes = finance.createFinanceIntegrationsCp710P06ScopeInventoryTestGoldenBridgeHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp710P06ScopeInventoryTestGoldenBridgeClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp710P06ScopeInventoryTestGoldenBridgeCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-711 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP711_PACK_BINDING; assert.equal(binding.pack_id, "CP00-711"); assert.equal(binding.unit_count, 40); const coverage = finance.validateFinanceIntegrationsCp711P06TestEvidenceClaudeReviewBridgeCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP711_REQUIREMENTS.deliverable_counts); });
test("CP00-711 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP711_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp711P06TestEvidenceClaudeReviewBridgeDescriptor(); const validation = finance.validateFinanceIntegrationsCp711P06TestEvidenceClaudeReviewBridgeDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-712"); const hermes = finance.createFinanceIntegrationsCp711P06TestEvidenceClaudeReviewBridgeHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp711P06TestEvidenceClaudeReviewBridgeClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp711P06TestEvidenceClaudeReviewBridgeCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-712 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP712_PACK_BINDING; assert.equal(binding.pack_id, "CP00-712"); assert.equal(binding.unit_count, 150); const coverage = finance.validateFinanceIntegrationsCp712P06ReviewCloseoutP07PermissionBridgeCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP712_REQUIREMENTS.deliverable_counts); });
test("CP00-712 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP712_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp712P06ReviewCloseoutP07PermissionBridgeDescriptor(); const validation = finance.validateFinanceIntegrationsCp712P06ReviewCloseoutP07PermissionBridgeDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-713"); const hermes = finance.createFinanceIntegrationsCp712P06ReviewCloseoutP07PermissionBridgeHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp712P06ReviewCloseoutP07PermissionBridgeClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp712P06ReviewCloseoutP07PermissionBridgeCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-713 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP713_PACK_BINDING; assert.equal(binding.pack_id, "CP00-713"); assert.equal(binding.unit_count, 10); const coverage = finance.validateFinanceIntegrationsCp713P07PermissionAuditFailureTailCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP713_REQUIREMENTS.deliverable_counts); });
test("CP00-713 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP713_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp713P07PermissionAuditFailureTailDescriptor(); const validation = finance.validateFinanceIntegrationsCp713P07PermissionAuditFailureTailDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-714"); const hermes = finance.createFinanceIntegrationsCp713P07PermissionAuditFailureTailHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp713P07PermissionAuditFailureTailClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp713P07PermissionAuditFailureTailCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-714 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP714_PACK_BINDING; assert.equal(binding.pack_id, "CP00-714"); assert.equal(binding.unit_count, 10); const coverage = finance.validateFinanceIntegrationsCp714P07PermissionAuditEvidenceFixtureBridgeCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP714_REQUIREMENTS.deliverable_counts); });
test("CP00-714 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP714_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp714P07PermissionAuditEvidenceFixtureBridgeDescriptor(); const validation = finance.validateFinanceIntegrationsCp714P07PermissionAuditEvidenceFixtureBridgeDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-715"); const hermes = finance.createFinanceIntegrationsCp714P07PermissionAuditEvidenceFixtureBridgeHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp714P07PermissionAuditEvidenceFixtureBridgeClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp714P07PermissionAuditEvidenceFixtureBridgeCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-715 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP715_PACK_BINDING; assert.equal(binding.pack_id, "CP00-715"); assert.equal(binding.unit_count, 150); const coverage = finance.validateFinanceIntegrationsCp715P07FixtureEvidenceReviewCloseoutP08ScopeBridgeCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP715_REQUIREMENTS.deliverable_counts); });
test("CP00-715 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP715_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp715P07FixtureEvidenceReviewCloseoutP08ScopeBridgeDescriptor(); const validation = finance.validateFinanceIntegrationsCp715P07FixtureEvidenceReviewCloseoutP08ScopeBridgeDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-716"); const hermes = finance.createFinanceIntegrationsCp715P07FixtureEvidenceReviewCloseoutP08ScopeBridgeHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp715P07FixtureEvidenceReviewCloseoutP08ScopeBridgeClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp715P07FixtureEvidenceReviewCloseoutP08ScopeBridgeCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-716 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP716_PACK_BINDING; assert.equal(binding.pack_id, "CP00-716"); assert.equal(binding.unit_count, 40); const coverage = finance.validateFinanceIntegrationsCp716P08TypeImplementationWorkflowSliceCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP716_REQUIREMENTS.deliverable_counts); });
test("CP00-716 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP716_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp716P08TypeImplementationWorkflowSliceDescriptor(); const validation = finance.validateFinanceIntegrationsCp716P08TypeImplementationWorkflowSliceDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-717"); const hermes = finance.createFinanceIntegrationsCp716P08TypeImplementationWorkflowSliceHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp716P08TypeImplementationWorkflowSliceClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp716P08TypeImplementationWorkflowSliceCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-717 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP717_PACK_BINDING; assert.equal(binding.pack_id, "CP00-717"); assert.equal(binding.unit_count, 40); const coverage = finance.validateFinanceIntegrationsCp717P08WorkflowPermissionFixtureBridgeCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP717_REQUIREMENTS.deliverable_counts); });
test("CP00-717 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP717_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp717P08WorkflowPermissionFixtureBridgeDescriptor(); const validation = finance.validateFinanceIntegrationsCp717P08WorkflowPermissionFixtureBridgeDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-718"); const hermes = finance.createFinanceIntegrationsCp717P08WorkflowPermissionFixtureBridgeHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp717P08WorkflowPermissionFixtureBridgeClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp717P08WorkflowPermissionFixtureBridgeCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-718 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP718_PACK_BINDING; assert.equal(binding.pack_id, "CP00-718"); assert.equal(binding.unit_count, 150); const coverage = finance.validateFinanceIntegrationsCp718P08FixtureReviewCloseoutP09ImplementationBridgeCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP718_REQUIREMENTS.deliverable_counts); });
test("CP00-718 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP718_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp718P08FixtureReviewCloseoutP09ImplementationBridgeDescriptor(); const validation = finance.validateFinanceIntegrationsCp718P08FixtureReviewCloseoutP09ImplementationBridgeDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-719"); const hermes = finance.createFinanceIntegrationsCp718P08FixtureReviewCloseoutP09ImplementationBridgeHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp718P08FixtureReviewCloseoutP09ImplementationBridgeClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp718P08FixtureReviewCloseoutP09ImplementationBridgeCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-719 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP719_PACK_BINDING; assert.equal(binding.pack_id, "CP00-719"); assert.equal(binding.unit_count, 40); const coverage = finance.validateFinanceIntegrationsCp719P09WorkflowPermissionAuditBindingCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP719_REQUIREMENTS.deliverable_counts); });
test("CP00-719 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP719_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp719P09WorkflowPermissionAuditBindingDescriptor(); const validation = finance.validateFinanceIntegrationsCp719P09WorkflowPermissionAuditBindingDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-720"); const hermes = finance.createFinanceIntegrationsCp719P09WorkflowPermissionAuditBindingHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp719P09WorkflowPermissionAuditBindingClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp719P09WorkflowPermissionAuditBindingCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-720 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP720_PACK_BINDING; assert.equal(binding.pack_id, "CP00-720"); assert.equal(binding.unit_count, 10); const coverage = finance.validateFinanceIntegrationsCp720P09PermissionAuditSyntheticFixtureBridgeCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP720_REQUIREMENTS.deliverable_counts); });
test("CP00-720 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP720_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp720P09PermissionAuditSyntheticFixtureBridgeDescriptor(); const validation = finance.validateFinanceIntegrationsCp720P09PermissionAuditSyntheticFixtureBridgeDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-721"); const hermes = finance.createFinanceIntegrationsCp720P09PermissionAuditSyntheticFixtureBridgeHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp720P09PermissionAuditSyntheticFixtureBridgeClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp720P09PermissionAuditSyntheticFixtureBridgeCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-721 binding and coverage", () => { const binding = finance.FINANCE_INTEGRATIONS_CP721_PACK_BINDING; assert.equal(binding.pack_id, "CP00-721"); assert.equal(binding.unit_count, 84); const coverage = finance.validateFinanceIntegrationsCp721P09FixtureEvidenceReviewCloseoutCoverage(planPack(binding)); assert.deepEqual(coverage.errors, []); assert.deepEqual(coverage.summary.by_deliverable, finance.FINANCE_INTEGRATIONS_CP721_REQUIREMENTS.deliverable_counts); });
test("CP00-721 descriptor and packets stay runtime-closed", () => { const binding = finance.FINANCE_INTEGRATIONS_CP721_PACK_BINDING; const descriptor = finance.createFinanceIntegrationsCp721P09FixtureEvidenceReviewCloseoutDescriptor(); const validation = finance.validateFinanceIntegrationsCp721P09FixtureEvidenceReviewCloseoutDescriptor(descriptor, { latest_pack: binding }); assert.deepEqual(validation.errors, []); assert.equal(descriptor.authority_boundary.claude_is_final_approval, false); assert.equal(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening, true); assert.equal(descriptor.authority_boundary.runtime_opening_pack_id, "CP00-722"); const hermes = finance.createFinanceIntegrationsCp721P09FixtureEvidenceReviewCloseoutHermesEvidencePacket(); assert.equal(hermes.emits_runtime_receipt, false); const claude = finance.createFinanceIntegrationsCp721P09FixtureEvidenceReviewCloseoutClaudeReviewPacket(); assert.equal(claude.read_only, true); const handoff = finance.createFinanceIntegrationsCp721P09FixtureEvidenceReviewCloseoutCloseoutHandoff(); assert.equal(handoff.to_pack_id, binding.next_pack_id); });
test("CP00-721 contract projection is latest while CP00-694 through CP00-720 stay historical", () => { const contract = readJson("contracts/external-integrations-ii-contract.json"); assert.equal(contract.program_contract.program_id, "RP23"); assert.equal(contract.latest_pack.pack_id, "CP00-721"); assert.equal(contract.latest_projection.descriptor, "FinanceIntegrationsCp721P09FixtureEvidenceReviewCloseoutDescriptor"); const expected = {"cp694":"FinanceIntegrationsCp694ScopeContractFoundationDescriptor","cp695":"FinanceIntegrationsCp695DomainModelWorkflowSliceDescriptor","cp696":"FinanceIntegrationsCp696WorkflowPermissionFixtureDescriptor","cp697":"FinanceIntegrationsCp697SyntheticEvidenceScopeContractDescriptor","cp698":"FinanceIntegrationsCp698P02ImplementationWorkflowSliceDescriptor","cp699":"FinanceIntegrationsCp699P02PermissionFixtureSliceDescriptor","cp700":"FinanceIntegrationsCp700P02FixtureTestBridgeDescriptor","cp701":"FinanceIntegrationsCp701P02TestGoldenCaseDescriptor","cp702":"FinanceIntegrationsCp702P02TestEvidenceReviewBridgeDescriptor","cp703":"FinanceIntegrationsCp703P02ReviewCloseoutP03FoundationDescriptor","cp704":"FinanceIntegrationsCp704P03FixtureReviewCloseoutP04UiFoundationDescriptor","cp705":"FinanceIntegrationsCp705P04ImplementationWorkflowPermissionSliceDescriptor","cp706":"FinanceIntegrationsCp706P04PermissionFixtureBridgeDescriptor","cp707":"FinanceIntegrationsCp707P04FixtureEvidenceP05ContractBridgeDescriptor","cp708":"FinanceIntegrationsCp708P05PrimaryImplementationTailDescriptor","cp709":"FinanceIntegrationsCp709P05CloseoutP06ScopeBridgeDescriptor","cp710":"FinanceIntegrationsCp710P06ScopeInventoryTestGoldenBridgeDescriptor","cp711":"FinanceIntegrationsCp711P06TestEvidenceClaudeReviewBridgeDescriptor","cp712":"FinanceIntegrationsCp712P06ReviewCloseoutP07PermissionBridgeDescriptor","cp713":"FinanceIntegrationsCp713P07PermissionAuditFailureTailDescriptor","cp714":"FinanceIntegrationsCp714P07PermissionAuditEvidenceFixtureBridgeDescriptor","cp715":"FinanceIntegrationsCp715P07FixtureEvidenceReviewCloseoutP08ScopeBridgeDescriptor","cp716":"FinanceIntegrationsCp716P08TypeImplementationWorkflowSliceDescriptor","cp717":"FinanceIntegrationsCp717P08WorkflowPermissionFixtureBridgeDescriptor","cp718":"FinanceIntegrationsCp718P08FixtureReviewCloseoutP09ImplementationBridgeDescriptor","cp719":"FinanceIntegrationsCp719P09WorkflowPermissionAuditBindingDescriptor","cp720":"FinanceIntegrationsCp720P09PermissionAuditSyntheticFixtureBridgeDescriptor"}; for (const [key, value] of Object.entries(expected)) assert.equal(contract.projections[key].descriptor, value); assert.equal(contract.validation.valid, true); });
