import {
  AI_GOVERNANCE_CORE_CP514_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP514_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP514_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP515_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP515_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP515_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP516_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP516_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP516_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP517_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP517_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP517_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP518_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP518_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP518_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP519_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP519_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP519_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP520_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP520_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP520_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP521_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP521_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP521_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP522_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP522_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP522_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP523_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP523_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP523_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP524_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP524_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP524_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP525_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP525_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP525_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP526_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP526_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP526_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP527_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP527_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP527_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP528_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP528_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP528_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP529_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP529_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP529_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP530_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP530_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP530_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP531_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP531_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP531_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP532_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP532_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP532_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP533_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP533_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP533_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP534_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP534_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP534_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP535_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP535_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP535_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP536_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP536_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP536_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP537_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP537_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP537_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP538_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP538_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP538_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP539_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP539_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP539_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP540_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP540_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP540_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP541_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP541_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP541_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP542_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP542_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP542_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP543_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP543_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP543_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP544_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP544_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP544_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP545_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP545_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP545_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP546_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP546_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP546_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP547_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP547_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP547_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP548_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP548_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP548_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP549_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP549_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP549_REQUIREMENTS,
  AI_GOVERNANCE_CORE_CP550_NO_WRITE_ATTESTATION,
  AI_GOVERNANCE_CORE_CP550_PACK_BINDING,
  AI_GOVERNANCE_CORE_CP550_REQUIREMENTS,
} from "./registry.js";
import {
  createAiGovernanceCoreCp514ScopeContractFoundationCaseSet,
  createAiGovernanceCoreCp514ScopeContractFoundationDescriptor,
  createAiGovernanceCoreCp515P01ImplementationSliceCaseSet,
  createAiGovernanceCoreCp515P01ImplementationSliceDescriptor,
  createAiGovernanceCoreCp516P01WorkflowPermissionSliceCaseSet,
  createAiGovernanceCoreCp516P01WorkflowPermissionSliceDescriptor,
  createAiGovernanceCoreCp517P01CloseoutP02FoundationCaseSet,
  createAiGovernanceCoreCp517P01CloseoutP02FoundationDescriptor,
  createAiGovernanceCoreCp518P02ImplementationSliceCaseSet,
  createAiGovernanceCoreCp518P02ImplementationSliceDescriptor,
  createAiGovernanceCoreCp519P02WorkflowSliceCaseSet,
  createAiGovernanceCoreCp519P02WorkflowSliceDescriptor,
  createAiGovernanceCoreCp520P02PermissionSliceCaseSet,
  createAiGovernanceCoreCp520P02PermissionSliceDescriptor,
  createAiGovernanceCoreCp521P02AuditBindingSliceCaseSet,
  createAiGovernanceCoreCp521P02AuditBindingSliceDescriptor,
  createAiGovernanceCoreCp522P02PermissionFixtureSliceCaseSet,
  createAiGovernanceCoreCp522P02PermissionFixtureSliceDescriptor,
  createAiGovernanceCoreCp523P02FixtureSliceCaseSet,
  createAiGovernanceCoreCp523P02FixtureSliceDescriptor,
  createAiGovernanceCoreCp524P02FixtureTestSliceCaseSet,
  createAiGovernanceCoreCp524P02FixtureTestSliceDescriptor,
  createAiGovernanceCoreCp525P02TestHermesSliceCaseSet,
  createAiGovernanceCoreCp525P02TestHermesSliceDescriptor,
  createAiGovernanceCoreCp526P02CloseoutP03FoundationCaseSet,
  createAiGovernanceCoreCp526P02CloseoutP03FoundationDescriptor,
  createAiGovernanceCoreCp527P03CloseoutP04FoundationCaseSet,
  createAiGovernanceCoreCp527P03CloseoutP04FoundationDescriptor,
  createAiGovernanceCoreCp528P04ImplementationSliceCaseSet,
  createAiGovernanceCoreCp528P04ImplementationSliceDescriptor,
  createAiGovernanceCoreCp529P04WorkflowPermissionSliceCaseSet,
  createAiGovernanceCoreCp529P04WorkflowPermissionSliceDescriptor,
  createAiGovernanceCoreCp530P04PermissionFixtureSliceCaseSet,
  createAiGovernanceCoreCp530P04PermissionFixtureSliceDescriptor,
  createAiGovernanceCoreCp531P04CloseoutP05FoundationCaseSet,
  createAiGovernanceCoreCp531P04CloseoutP05FoundationDescriptor,
  createAiGovernanceCoreCp532P05ImplementationWorkflowSliceCaseSet,
  createAiGovernanceCoreCp532P05ImplementationWorkflowSliceDescriptor,
  createAiGovernanceCoreCp533P05WorkflowPermissionSliceCaseSet,
  createAiGovernanceCoreCp533P05WorkflowPermissionSliceDescriptor,
  createAiGovernanceCoreCp534P05FixtureSliceCaseSet,
  createAiGovernanceCoreCp534P05FixtureSliceDescriptor,
  createAiGovernanceCoreCp535P05CloseoutP06FoundationCaseSet,
  createAiGovernanceCoreCp535P05CloseoutP06FoundationDescriptor,
  createAiGovernanceCoreCp536P06FoundationSliceCaseSet,
  createAiGovernanceCoreCp536P06FoundationSliceDescriptor,
  createAiGovernanceCoreCp537P06ImplementationWorkflowSliceCaseSet,
  createAiGovernanceCoreCp537P06ImplementationWorkflowSliceDescriptor,
  createAiGovernanceCoreCp538P06WorkflowPermissionSliceCaseSet,
  createAiGovernanceCoreCp538P06WorkflowPermissionSliceDescriptor,
  createAiGovernanceCoreCp539P06CloseoutP07FoundationCaseSet,
  createAiGovernanceCoreCp539P06CloseoutP07FoundationDescriptor,
  createAiGovernanceCoreCp540P07ImplementationSliceCaseSet,
  createAiGovernanceCoreCp540P07ImplementationSliceDescriptor,
  createAiGovernanceCoreCp541P07WorkflowPermissionSliceCaseSet,
  createAiGovernanceCoreCp541P07WorkflowPermissionSliceDescriptor,
  createAiGovernanceCoreCp542P07PermissionFixtureSliceCaseSet,
  createAiGovernanceCoreCp542P07PermissionFixtureSliceDescriptor,
  createAiGovernanceCoreCp543P07CloseoutP08FoundationCaseSet,
  createAiGovernanceCoreCp543P07CloseoutP08FoundationDescriptor,
  createAiGovernanceCoreCp544P08ImplementationWorkflowSliceCaseSet,
  createAiGovernanceCoreCp544P08ImplementationWorkflowSliceDescriptor,
  createAiGovernanceCoreCp545P08WorkflowPermissionSliceCaseSet,
  createAiGovernanceCoreCp545P08WorkflowPermissionSliceDescriptor,
  createAiGovernanceCoreCp546P08CloseoutP09FoundationCaseSet,
  createAiGovernanceCoreCp546P08CloseoutP09FoundationDescriptor,
  createAiGovernanceCoreCp547P09WorkflowPermissionSliceCaseSet,
  createAiGovernanceCoreCp547P09WorkflowPermissionSliceDescriptor,
  createAiGovernanceCoreCp548P09PermissionSliceCaseSet,
  createAiGovernanceCoreCp548P09PermissionSliceDescriptor,
  createAiGovernanceCoreCp549P09PermissionFixtureSliceCaseSet,
  createAiGovernanceCoreCp549P09PermissionFixtureSliceDescriptor,
  createAiGovernanceCoreCp550P09CloseoutHandoffSliceCaseSet,
  createAiGovernanceCoreCp550P09CloseoutHandoffSliceDescriptor,
  aiGovernanceCoreRowKey,
} from "./service.js";

function countBy(units, field) {
  const counts = {};
  for (const unit of units) {
    const key = unit?.[field];
    if (key === undefined) continue;
    counts[key] = (counts[key] ?? 0) + 1;
  }
  return Object.freeze(counts);
}

function freezeCp514Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP514_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP514_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp514CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp514Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp514Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-514 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP514_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-514");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP514_PACK_BINDING.risk_class) errors.push("CP00-514 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP514_PACK_BINDING.unit_count) errors.push("CP00-514 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP514_PACK_BINDING.first_unit_id) errors.push("CP00-514 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP514_PACK_BINDING.last_unit_id) errors.push("CP00-514 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-514 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-514 must only include RP17 units");
  const summary = createAiGovernanceCoreCp514CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP514_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-514 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP514_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-514 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP514_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-514 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP514_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-514 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP514_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-514 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-514 ${microId} missing row ${title}`);
    }
  }
  return freezeCp514Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp514ScopeContractFoundationDescriptor(
  descriptor = createAiGovernanceCoreCp514ScopeContractFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.scope_contract_foundation_case_set ?? createAiGovernanceCoreCp514ScopeContractFoundationCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp514ScopeContractFoundationDescriptor") errors.push("CP00-514 descriptor type drift");
  if (descriptor.program_contract?.program_id !== "RP17") errors.push("CP00-514 program id drift");
  if (descriptor.program_contract?.upstream_program_id !== "RP16") errors.push("CP00-514 upstream program drift");
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP514_REQUIREMENTS.required_section_rows).length) errors.push("CP00-514 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP514_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-514 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-514 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-514 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-514 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-514 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-514 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-514 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(AI_GOVERNANCE_CORE_CP514_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.non_goal_boundary && rows.non_goal_boundary.ai_governance_runtime_opened !== false) {
      errors.push(`CP00-514 ${microId} must not open AI governance runtime`);
    }
    if (rows.permission_baseline_note && rows.permission_baseline_note.permission_decision_detail_included !== false) {
      errors.push(`CP00-514 ${microId} permission baseline must not expose decisions`);
    }
    if (rows.permission_baseline_note && rows.permission_baseline_note.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-514 ${microId} permission baseline must deny cross-tenant access`);
    }
    if (rows.audit_baseline_note && rows.audit_baseline_note.audit_event_body_included !== false) {
      errors.push(`CP00-514 ${microId} audit baseline must not expose event bodies`);
    }
    if (rows.synthetic_data_policy && rows.synthetic_data_policy.real_client_data_loaded !== false) {
      errors.push(`CP00-514 ${microId} synthetic data policy drift`);
    }
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-514 ${microId} tenant scope must deny cross-tenant access`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-514 ${microId} matter trace must be required`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-514 ${microId} state transition map must not write state`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-514 ${microId} fixture model must not load real data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-514 ${microId} must not execute test runtime`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-514 ${microId} must not emit Hermes receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-514 ${microId} must not claim Claude final approval`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP514_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-514 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP514_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-514 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-514 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-514 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP514_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP515_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP516_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP517_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP518_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP519_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP520_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP521_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP522_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP523_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-514 contract current_pack drift");
  }
  if (
    contractProjection?.scope_contract_foundation_descriptor?.descriptor &&
    contractProjection.scope_contract_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-514 contract scope_contract_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP514_PACK_BINDING.next_pack_id) errors.push("CP00-514 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP514_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-514 next subphase drift");
  }
  return freezeCp514Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp514HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp514ScopeContractFoundationDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp514Coverage(planPack);
  const foundation = validateAiGovernanceCoreCp514ScopeContractFoundationDescriptor(descriptor, contractProjection);
  return freezeCp514Validation({
    evidence_packet: "H17.CP00-514.ai_governance_core_scope_contract_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP514_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    scope_contract_foundation_valid: foundation.valid,
    no_real_data: true,
    source_governance_core_pack_id: AI_GOVERNANCE_CORE_CP514_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP514_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP514_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP514_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createAiGovernanceCoreCp514ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp514Coverage(planPack);
  const foundation = validateAiGovernanceCoreCp514ScopeContractFoundationDescriptor(createAiGovernanceCoreCp514ScopeContractFoundationDescriptor(), {});
  return freezeCp514Validation({
    review_packet: "C17.CP00-514.ai_governance_core_scope_contract_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP514_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP514_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    scope_contract_foundation_valid: foundation.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP514_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-514 cover exactly the 150 planned RP17 units from RP17.P00.M00.S01 through RP17.P01.M02.S08?",
      "Do the twenty-one descriptor sections (P00 M00-M10 scope foundation cycle and P01 M00-M08 model foundation cycle with package directory layout, primary entity identifier, tenant scope field, matter trace reference, lifecycle status enum, ownership metadata, reference relationship map, field registries, state transition map, validation helper, fixture model, serialization shape, public export, model tests, Hermes model summary, Claude model review prompt, and closeout handoff) cover every planned row title as descriptor-only rows?",
      "Does CP00-514 avoid ai-governance/model-policy/retrieval-scope/citation runtime, runtime permission evaluation, audit event writes, state writes, object storage, command runtime execution, Hermes runtime receipts, real data, permission decision leaks, audit event body leaks, fixture payload leaks, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp514CloseoutHandoff() {
  return freezeCp514Validation({
    handoff_id: "CP00-514-to-CP00-515",
    from_pack_id: AI_GOVERNANCE_CORE_CP514_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP514_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP514_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP514_PACK_BINDING.range,
    open_scope: "Continue RP17.P01.M02.S09 onward with the remaining model foundation rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP514_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp515Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP515_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP515_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp515CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp515Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp515Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-515 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP515_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-515");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP515_PACK_BINDING.risk_class) errors.push("CP00-515 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP515_PACK_BINDING.unit_count) errors.push("CP00-515 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP515_PACK_BINDING.first_unit_id) errors.push("CP00-515 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP515_PACK_BINDING.last_unit_id) errors.push("CP00-515 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-515 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-515 must only include RP17 units");
  const summary = createAiGovernanceCoreCp515CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP515_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-515 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP515_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-515 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP515_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-515 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP515_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-515 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP515_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-515 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-515 ${microId} missing row ${title}`);
    }
  }
  return freezeCp515Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp515P01ImplementationSliceDescriptor(
  descriptor = createAiGovernanceCoreCp515P01ImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p01_implementation_slice_case_set ?? createAiGovernanceCoreCp515P01ImplementationSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp515P01ImplementationSliceDescriptor") errors.push("CP00-515 descriptor type drift");
  if (descriptor.source_scope_contract_foundation_descriptor !== "AiGovernanceCoreCp514ScopeContractFoundationDescriptor") {
    errors.push("CP00-515 source scope contract foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP515_REQUIREMENTS.required_section_rows).length) errors.push("CP00-515 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP515_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-515 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-515 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-515 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-515 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-515 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-515 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-515 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(AI_GOVERNANCE_CORE_CP515_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-515 ${microId} must not allow cross-tenant access`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-515 ${microId} matter trace requirement drift`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-515 ${microId} must not write state transitions`);
    }
    if (rows.validation_helper && rows.validation_helper.validation_error_detail_included !== false) {
      errors.push(`CP00-515 ${microId} must not expose validation error details`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-515 ${microId} must not load real fixture data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-515 ${microId} must not execute test runtime`);
    }
    if (rows.invalid_reference_test && rows.invalid_reference_test.expected_outcome !== "rejected_customer_safe") {
      errors.push(`CP00-515 ${microId} invalid reference outcome drift`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-515 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-515 ${microId} must not claim Claude final approval`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP515_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-515 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP515_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-515 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-515 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-515 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP515_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP516_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP517_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP518_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP519_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP520_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP521_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP522_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP523_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-515 contract current_pack drift");
  }
  if (
    contractProjection?.p01_implementation_slice_descriptor?.descriptor &&
    contractProjection.p01_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-515 contract p01_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP515_PACK_BINDING.next_pack_id) errors.push("CP00-515 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP515_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-515 next subphase drift");
  }
  return freezeCp515Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp515HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp515P01ImplementationSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp515Coverage(planPack);
  const slice = validateAiGovernanceCoreCp515P01ImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp515Validation({
    evidence_packet: "H17.CP00-515.ai_governance_core_p01_implementation_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP515_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p01_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_scope_contract_foundation_pack_id: AI_GOVERNANCE_CORE_CP515_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP515_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP515_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP515_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp515ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp515Coverage(planPack);
  const slice = validateAiGovernanceCoreCp515P01ImplementationSliceDescriptor(createAiGovernanceCoreCp515P01ImplementationSliceDescriptor(), {});
  return freezeCp515Validation({
    review_packet: "C17.CP00-515.ai_governance_core_p01_implementation_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP515_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP515_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p01_implementation_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP515_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-515 cover exactly the 40 planned RP17 units from RP17.P01.M02.S09 through RP17.P01.M04.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-515 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp515CloseoutHandoff() {
  return freezeCp515Validation({
    handoff_id: "CP00-515-to-CP00-516",
    from_pack_id: AI_GOVERNANCE_CORE_CP515_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP515_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP515_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP515_PACK_BINDING.range,
    open_scope: "Continue RP17.P01.M04.S07 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP515_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp516Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP516_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP516_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp516CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp516Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp516Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-516 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP516_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-516");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP516_PACK_BINDING.risk_class) errors.push("CP00-516 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP516_PACK_BINDING.unit_count) errors.push("CP00-516 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP516_PACK_BINDING.first_unit_id) errors.push("CP00-516 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP516_PACK_BINDING.last_unit_id) errors.push("CP00-516 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-516 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-516 must only include RP17 units");
  const summary = createAiGovernanceCoreCp516CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP516_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-516 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP516_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-516 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP516_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-516 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP516_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-516 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP516_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-516 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-516 ${microId} missing row ${title}`);
    }
  }
  return freezeCp516Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp516P01WorkflowPermissionSliceDescriptor(
  descriptor = createAiGovernanceCoreCp516P01WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p01_workflow_permission_slice_case_set ?? createAiGovernanceCoreCp516P01WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp516P01WorkflowPermissionSliceDescriptor") errors.push("CP00-516 descriptor type drift");
  if (descriptor.source_p01_implementation_slice_descriptor !== "AiGovernanceCoreCp515P01ImplementationSliceDescriptor") {
    errors.push("CP00-516 source p01 implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP516_REQUIREMENTS.required_section_rows).length) errors.push("CP00-516 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP516_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-516 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-516 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-516 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-516 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-516 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-516 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-516 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(AI_GOVERNANCE_CORE_CP516_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-516 ${microId} must not allow cross-tenant access`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-516 ${microId} matter trace requirement drift`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-516 ${microId} must not write state transitions`);
    }
    if (rows.validation_helper && rows.validation_helper.validation_error_detail_included !== false) {
      errors.push(`CP00-516 ${microId} must not expose validation error details`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-516 ${microId} must not load real fixture data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-516 ${microId} must not execute test runtime`);
    }
    if (rows.invalid_reference_test && rows.invalid_reference_test.expected_outcome !== "rejected_customer_safe") {
      errors.push(`CP00-516 ${microId} invalid reference outcome drift`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-516 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-516 ${microId} must not claim Claude final approval`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP516_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-516 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP516_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-516 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-516 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-516 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP516_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP517_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP518_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP519_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP520_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP521_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP522_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP523_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-516 contract current_pack drift");
  }
  if (
    contractProjection?.p01_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p01_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-516 contract p01_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP516_PACK_BINDING.next_pack_id) errors.push("CP00-516 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP516_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-516 next subphase drift");
  }
  return freezeCp516Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp516HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp516P01WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp516Coverage(planPack);
  const slice = validateAiGovernanceCoreCp516P01WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp516Validation({
    evidence_packet: "H17.CP00-516.ai_governance_core_p01_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP516_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p01_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p01_implementation_slice_pack_id: AI_GOVERNANCE_CORE_CP516_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP516_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP516_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP516_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp516ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp516Coverage(planPack);
  const slice = validateAiGovernanceCoreCp516P01WorkflowPermissionSliceDescriptor(createAiGovernanceCoreCp516P01WorkflowPermissionSliceDescriptor(), {});
  return freezeCp516Validation({
    review_packet: "C17.CP00-516.ai_governance_core_p01_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP516_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP516_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p01_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP516_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-516 cover exactly the 40 planned RP17 units from RP17.P01.M04.S07 through RP17.P01.M06.S04?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-516 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp516CloseoutHandoff() {
  return freezeCp516Validation({
    handoff_id: "CP00-516-to-CP00-517",
    from_pack_id: AI_GOVERNANCE_CORE_CP516_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP516_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP516_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP516_PACK_BINDING.range,
    open_scope: "Continue RP17.P01.M06.S05 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP516_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp517Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP517_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP517_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp517CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp517Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp517Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-517 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP517_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-517");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP517_PACK_BINDING.risk_class) errors.push("CP00-517 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP517_PACK_BINDING.unit_count) errors.push("CP00-517 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP517_PACK_BINDING.first_unit_id) errors.push("CP00-517 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP517_PACK_BINDING.last_unit_id) errors.push("CP00-517 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-517 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-517 must only include RP17 units");
  const summary = createAiGovernanceCoreCp517CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP517_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-517 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP517_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-517 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP517_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-517 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP517_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-517 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP517_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-517 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-517 ${microId} missing row ${title}`);
    }
  }
  return freezeCp517Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp517P01CloseoutP02FoundationDescriptor(
  descriptor = createAiGovernanceCoreCp517P01CloseoutP02FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p01_closeout_p02_foundation_case_set ?? createAiGovernanceCoreCp517P01CloseoutP02FoundationCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp517P01CloseoutP02FoundationDescriptor") errors.push("CP00-517 descriptor type drift");
  if (descriptor.source_p01_workflow_permission_slice_descriptor !== "AiGovernanceCoreCp516P01WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-517 source p01 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP517_REQUIREMENTS.required_section_rows).length) errors.push("CP00-517 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP517_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-517 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-517 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-517 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-517 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-517 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-517 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-517 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(AI_GOVERNANCE_CORE_CP517_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-517 ${microId} must not allow cross-tenant access`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-517 ${microId} matter trace requirement drift`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-517 ${microId} must not write state transitions`);
    }
    if (rows.validation_helper && rows.validation_helper.validation_error_detail_included !== false) {
      errors.push(`CP00-517 ${microId} must not expose validation error details`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-517 ${microId} must not load real fixture data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-517 ${microId} must not execute test runtime`);
    }
    if (rows.invalid_reference_test && rows.invalid_reference_test.expected_outcome !== "rejected_customer_safe") {
      errors.push(`CP00-517 ${microId} invalid reference outcome drift`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-517 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-517 ${microId} must not claim Claude final approval`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP517_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-517 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP517_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-517 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-517 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-517 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP517_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP518_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP519_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP520_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP521_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP522_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP523_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-517 contract current_pack drift");
  }
  if (
    contractProjection?.p01_closeout_p02_foundation_descriptor?.descriptor &&
    contractProjection.p01_closeout_p02_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-517 contract p01_closeout_p02_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP517_PACK_BINDING.next_pack_id) errors.push("CP00-517 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP517_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-517 next subphase drift");
  }
  return freezeCp517Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp517HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp517P01CloseoutP02FoundationDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp517Coverage(planPack);
  const slice = validateAiGovernanceCoreCp517P01CloseoutP02FoundationDescriptor(descriptor, contractProjection);
  return freezeCp517Validation({
    evidence_packet: "H17.CP00-517.ai_governance_core_p01_closeout_p02_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP517_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p01_closeout_p02_foundation_valid: slice.valid,
    no_real_data: true,
    source_p01_workflow_permission_slice_pack_id: AI_GOVERNANCE_CORE_CP517_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP517_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP517_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP517_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp517ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp517Coverage(planPack);
  const slice = validateAiGovernanceCoreCp517P01CloseoutP02FoundationDescriptor(createAiGovernanceCoreCp517P01CloseoutP02FoundationDescriptor(), {});
  return freezeCp517Validation({
    review_packet: "C17.CP00-517.ai_governance_core_p01_closeout_p02_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP517_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP517_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p01_closeout_p02_foundation_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP517_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-517 cover exactly the 150 planned RP17 units from RP17.P01.M06.S05 through RP17.P02.M02.S22?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-517 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp517CloseoutHandoff() {
  return freezeCp517Validation({
    handoff_id: "CP00-517-to-CP00-518",
    from_pack_id: AI_GOVERNANCE_CORE_CP517_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP517_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP517_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP517_PACK_BINDING.range,
    open_scope: "Continue RP17.P02.M03.S01 onward with the primary implementation rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP517_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp518Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP518_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP518_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp518CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp518Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp518Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-518 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP518_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-518");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP518_PACK_BINDING.risk_class) errors.push("CP00-518 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP518_PACK_BINDING.unit_count) errors.push("CP00-518 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP518_PACK_BINDING.first_unit_id) errors.push("CP00-518 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP518_PACK_BINDING.last_unit_id) errors.push("CP00-518 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-518 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-518 must only include RP17 units");
  const summary = createAiGovernanceCoreCp518CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP518_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-518 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP518_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-518 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP518_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-518 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP518_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-518 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP518_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-518 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-518 ${microId} missing row ${title}`);
    }
  }
  return freezeCp518Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp518P02ImplementationSliceDescriptor(
  descriptor = createAiGovernanceCoreCp518P02ImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_implementation_slice_case_set ?? createAiGovernanceCoreCp518P02ImplementationSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp518P02ImplementationSliceDescriptor") errors.push("CP00-518 descriptor type drift");
  if (descriptor.source_p01_closeout_p02_foundation_descriptor !== "AiGovernanceCoreCp517P01CloseoutP02FoundationDescriptor") {
    errors.push("CP00-518 source p01 closeout p02 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP518_REQUIREMENTS.required_section_rows).length) errors.push("CP00-518 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP518_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-518 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-518 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-518 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-518 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-518 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-518 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-518 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP518_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-518 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP518_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-518 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-518 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-518 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP518_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP519_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP520_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP521_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP522_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP523_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-518 contract current_pack drift");
  }
  if (
    contractProjection?.p02_implementation_slice_descriptor?.descriptor &&
    contractProjection.p02_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-518 contract p02_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP518_PACK_BINDING.next_pack_id) errors.push("CP00-518 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP518_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-518 next subphase drift");
  }
  return freezeCp518Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp518HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp518P02ImplementationSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp518Coverage(planPack);
  const slice = validateAiGovernanceCoreCp518P02ImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp518Validation({
    evidence_packet: "H17.CP00-518.ai_governance_core_p02_implementation_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP518_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_p01_closeout_p02_foundation_pack_id: AI_GOVERNANCE_CORE_CP518_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP518_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP518_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP518_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp518ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp518Coverage(planPack);
  const slice = validateAiGovernanceCoreCp518P02ImplementationSliceDescriptor(createAiGovernanceCoreCp518P02ImplementationSliceDescriptor(), {});
  return freezeCp518Validation({
    review_packet: "C17.CP00-518.ai_governance_core_p02_implementation_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP518_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP518_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_implementation_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP518_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-518 cover exactly the 40 planned RP17 units from RP17.P02.M03.S01 through RP17.P02.M04.S15?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-518 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp518CloseoutHandoff() {
  return freezeCp518Validation({
    handoff_id: "CP00-518-to-CP00-519",
    from_pack_id: AI_GOVERNANCE_CORE_CP518_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP518_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP518_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP518_PACK_BINDING.range,
    open_scope: "Continue RP17.P02.M04.S16 onward with the remaining secondary workflow rows while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP518_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp519Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP519_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP519_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp519CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp519Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp519Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-519 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP519_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-519");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP519_PACK_BINDING.risk_class) errors.push("CP00-519 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP519_PACK_BINDING.unit_count) errors.push("CP00-519 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP519_PACK_BINDING.first_unit_id) errors.push("CP00-519 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP519_PACK_BINDING.last_unit_id) errors.push("CP00-519 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-519 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-519 must only include RP17 units");
  const summary = createAiGovernanceCoreCp519CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP519_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-519 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP519_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-519 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP519_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-519 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP519_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-519 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP519_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-519 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-519 ${microId} missing row ${title}`);
    }
  }
  return freezeCp519Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp519P02WorkflowSliceDescriptor(
  descriptor = createAiGovernanceCoreCp519P02WorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_workflow_slice_case_set ?? createAiGovernanceCoreCp519P02WorkflowSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp519P02WorkflowSliceDescriptor") errors.push("CP00-519 descriptor type drift");
  if (descriptor.source_p02_implementation_slice_descriptor !== "AiGovernanceCoreCp518P02ImplementationSliceDescriptor") {
    errors.push("CP00-519 source p02 implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP519_REQUIREMENTS.required_section_rows).length) errors.push("CP00-519 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP519_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-519 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-519 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-519 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-519 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-519 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-519 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-519 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP519_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-519 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP519_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-519 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-519 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-519 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP519_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP520_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP521_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP522_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP523_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-519 contract current_pack drift");
  }
  if (
    contractProjection?.p02_workflow_slice_descriptor?.descriptor &&
    contractProjection.p02_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-519 contract p02_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP519_PACK_BINDING.next_pack_id) errors.push("CP00-519 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP519_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-519 next subphase drift");
  }
  return freezeCp519Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp519HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp519P02WorkflowSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp519Coverage(planPack);
  const slice = validateAiGovernanceCoreCp519P02WorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp519Validation({
    evidence_packet: "H17.CP00-519.ai_governance_core_p02_workflow_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP519_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_implementation_slice_pack_id: AI_GOVERNANCE_CORE_CP519_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP519_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP519_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP519_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp519ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp519Coverage(planPack);
  const slice = validateAiGovernanceCoreCp519P02WorkflowSliceDescriptor(createAiGovernanceCoreCp519P02WorkflowSliceDescriptor(), {});
  return freezeCp519Validation({
    review_packet: "C17.CP00-519.ai_governance_core_p02_workflow_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP519_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP519_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_workflow_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP519_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-519 cover exactly the 10 planned RP17 units from RP17.P02.M04.S16 through RP17.P02.M04.S25?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-519 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp519CloseoutHandoff() {
  return freezeCp519Validation({
    handoff_id: "CP00-519-to-CP00-520",
    from_pack_id: AI_GOVERNANCE_CORE_CP519_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP519_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP519_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP519_PACK_BINDING.range,
    open_scope: "Continue RP17.P02.M05.S01 onward with the permission and audit binding micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP519_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp520Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP520_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP520_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp520CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp520Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp520Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-520 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP520_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-520");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP520_PACK_BINDING.risk_class) errors.push("CP00-520 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP520_PACK_BINDING.unit_count) errors.push("CP00-520 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP520_PACK_BINDING.first_unit_id) errors.push("CP00-520 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP520_PACK_BINDING.last_unit_id) errors.push("CP00-520 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-520 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-520 must only include RP17 units");
  const summary = createAiGovernanceCoreCp520CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP520_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-520 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP520_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-520 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP520_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-520 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP520_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-520 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP520_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-520 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-520 ${microId} missing row ${title}`);
    }
  }
  return freezeCp520Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp520P02PermissionSliceDescriptor(
  descriptor = createAiGovernanceCoreCp520P02PermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_permission_slice_case_set ?? createAiGovernanceCoreCp520P02PermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp520P02PermissionSliceDescriptor") errors.push("CP00-520 descriptor type drift");
  if (descriptor.source_p02_workflow_slice_descriptor !== "AiGovernanceCoreCp519P02WorkflowSliceDescriptor") {
    errors.push("CP00-520 source p02 workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP520_REQUIREMENTS.required_section_rows).length) errors.push("CP00-520 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP520_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-520 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-520 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-520 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-520 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-520 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-520 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-520 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP520_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-520 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP520_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-520 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-520 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-520 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP520_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP521_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP522_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP523_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-520 contract current_pack drift");
  }
  if (
    contractProjection?.p02_permission_slice_descriptor?.descriptor &&
    contractProjection.p02_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-520 contract p02_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP520_PACK_BINDING.next_pack_id) errors.push("CP00-520 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP520_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-520 next subphase drift");
  }
  return freezeCp520Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp520HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp520P02PermissionSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp520Coverage(planPack);
  const slice = validateAiGovernanceCoreCp520P02PermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp520Validation({
    evidence_packet: "H17.CP00-520.ai_governance_core_p02_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP520_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_workflow_slice_pack_id: AI_GOVERNANCE_CORE_CP520_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP520_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP520_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP520_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp520ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp520Coverage(planPack);
  const slice = validateAiGovernanceCoreCp520P02PermissionSliceDescriptor(createAiGovernanceCoreCp520P02PermissionSliceDescriptor(), {});
  return freezeCp520Validation({
    review_packet: "C17.CP00-520.ai_governance_core_p02_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP520_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP520_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP520_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-520 cover exactly the 10 planned RP17 units from RP17.P02.M05.S01 through RP17.P02.M05.S10?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-520 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp520CloseoutHandoff() {
  return freezeCp520Validation({
    handoff_id: "CP00-520-to-CP00-521",
    from_pack_id: AI_GOVERNANCE_CORE_CP520_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP520_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP520_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP520_PACK_BINDING.range,
    open_scope: "Continue RP17.P02.M05.S11 onward with the remaining permission and audit binding rows while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP520_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp521Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP521_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP521_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp521CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp521Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp521Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-521 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP521_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-521");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP521_PACK_BINDING.risk_class) errors.push("CP00-521 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP521_PACK_BINDING.unit_count) errors.push("CP00-521 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP521_PACK_BINDING.first_unit_id) errors.push("CP00-521 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP521_PACK_BINDING.last_unit_id) errors.push("CP00-521 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-521 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-521 must only include RP17 units");
  const summary = createAiGovernanceCoreCp521CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP521_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-521 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP521_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-521 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP521_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-521 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP521_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-521 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP521_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-521 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-521 ${microId} missing row ${title}`);
    }
  }
  return freezeCp521Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp521P02AuditBindingSliceDescriptor(
  descriptor = createAiGovernanceCoreCp521P02AuditBindingSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_audit_binding_slice_case_set ?? createAiGovernanceCoreCp521P02AuditBindingSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp521P02AuditBindingSliceDescriptor") errors.push("CP00-521 descriptor type drift");
  if (descriptor.source_p02_permission_slice_descriptor !== "AiGovernanceCoreCp520P02PermissionSliceDescriptor") {
    errors.push("CP00-521 source p02 permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP521_REQUIREMENTS.required_section_rows).length) errors.push("CP00-521 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP521_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-521 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-521 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-521 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-521 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-521 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-521 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-521 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP521_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-521 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP521_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-521 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-521 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-521 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP521_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP522_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP523_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-521 contract current_pack drift");
  }
  if (
    contractProjection?.p02_audit_binding_slice_descriptor?.descriptor &&
    contractProjection.p02_audit_binding_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-521 contract p02_audit_binding_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP521_PACK_BINDING.next_pack_id) errors.push("CP00-521 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP521_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-521 next subphase drift");
  }
  return freezeCp521Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp521HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp521P02AuditBindingSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp521Coverage(planPack);
  const slice = validateAiGovernanceCoreCp521P02AuditBindingSliceDescriptor(descriptor, contractProjection);
  return freezeCp521Validation({
    evidence_packet: "H17.CP00-521.ai_governance_core_p02_audit_binding_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP521_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_audit_binding_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_permission_slice_pack_id: AI_GOVERNANCE_CORE_CP521_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP521_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP521_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP521_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp521ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp521Coverage(planPack);
  const slice = validateAiGovernanceCoreCp521P02AuditBindingSliceDescriptor(createAiGovernanceCoreCp521P02AuditBindingSliceDescriptor(), {});
  return freezeCp521Validation({
    review_packet: "C17.CP00-521.ai_governance_core_p02_audit_binding_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP521_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP521_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_audit_binding_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP521_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-521 cover exactly the 10 planned RP17 units from RP17.P02.M05.S11 through RP17.P02.M05.S20?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-521 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp521CloseoutHandoff() {
  return freezeCp521Validation({
    handoff_id: "CP00-521-to-CP00-522",
    from_pack_id: AI_GOVERNANCE_CORE_CP521_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP521_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP521_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP521_PACK_BINDING.range,
    open_scope: "Continue RP17.P02.M05.S21 onward with the remaining permission and audit binding rows and the synthetic fixture micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP521_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp522Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP522_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP522_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp522CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp522Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp522Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-522 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP522_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-522");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP522_PACK_BINDING.risk_class) errors.push("CP00-522 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP522_PACK_BINDING.unit_count) errors.push("CP00-522 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP522_PACK_BINDING.first_unit_id) errors.push("CP00-522 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP522_PACK_BINDING.last_unit_id) errors.push("CP00-522 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-522 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-522 must only include RP17 units");
  const summary = createAiGovernanceCoreCp522CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP522_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-522 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP522_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-522 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP522_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-522 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP522_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-522 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP522_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-522 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-522 ${microId} missing row ${title}`);
    }
  }
  return freezeCp522Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp522P02PermissionFixtureSliceDescriptor(
  descriptor = createAiGovernanceCoreCp522P02PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_permission_fixture_slice_case_set ?? createAiGovernanceCoreCp522P02PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp522P02PermissionFixtureSliceDescriptor") errors.push("CP00-522 descriptor type drift");
  if (descriptor.source_p02_audit_binding_slice_descriptor !== "AiGovernanceCoreCp521P02AuditBindingSliceDescriptor") {
    errors.push("CP00-522 source p02 audit binding slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP522_REQUIREMENTS.required_section_rows).length) errors.push("CP00-522 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP522_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-522 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-522 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-522 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-522 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-522 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-522 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-522 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP522_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-522 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP522_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-522 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-522 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-522 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP522_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP523_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-522 contract current_pack drift");
  }
  if (
    contractProjection?.p02_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p02_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-522 contract p02_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP522_PACK_BINDING.next_pack_id) errors.push("CP00-522 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP522_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-522 next subphase drift");
  }
  return freezeCp522Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp522HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp522P02PermissionFixtureSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp522Coverage(planPack);
  const slice = validateAiGovernanceCoreCp522P02PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp522Validation({
    evidence_packet: "H17.CP00-522.ai_governance_core_p02_permission_fixture_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP522_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_audit_binding_slice_pack_id: AI_GOVERNANCE_CORE_CP522_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP522_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP522_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP522_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp522ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp522Coverage(planPack);
  const slice = validateAiGovernanceCoreCp522P02PermissionFixtureSliceDescriptor(createAiGovernanceCoreCp522P02PermissionFixtureSliceDescriptor(), {});
  return freezeCp522Validation({
    review_packet: "C17.CP00-522.ai_governance_core_p02_permission_fixture_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP522_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP522_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP522_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-522 cover exactly the 10 planned RP17 units from RP17.P02.M05.S21 through RP17.P02.M06.S05?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-522 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp522CloseoutHandoff() {
  return freezeCp522Validation({
    handoff_id: "CP00-522-to-CP00-523",
    from_pack_id: AI_GOVERNANCE_CORE_CP522_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP522_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP522_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP522_PACK_BINDING.range,
    open_scope: "Continue RP17.P02.M06.S06 onward with the remaining synthetic fixture rows while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP522_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp523Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP523_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP523_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp523CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp523Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp523Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-523 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP523_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-523");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP523_PACK_BINDING.risk_class) errors.push("CP00-523 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP523_PACK_BINDING.unit_count) errors.push("CP00-523 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP523_PACK_BINDING.first_unit_id) errors.push("CP00-523 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP523_PACK_BINDING.last_unit_id) errors.push("CP00-523 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-523 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-523 must only include RP17 units");
  const summary = createAiGovernanceCoreCp523CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP523_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-523 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP523_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-523 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP523_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-523 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP523_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-523 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP523_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-523 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-523 ${microId} missing row ${title}`);
    }
  }
  return freezeCp523Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp523P02FixtureSliceDescriptor(
  descriptor = createAiGovernanceCoreCp523P02FixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_fixture_slice_case_set ?? createAiGovernanceCoreCp523P02FixtureSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp523P02FixtureSliceDescriptor") errors.push("CP00-523 descriptor type drift");
  if (descriptor.source_p02_permission_fixture_slice_descriptor !== "AiGovernanceCoreCp522P02PermissionFixtureSliceDescriptor") {
    errors.push("CP00-523 source p02 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP523_REQUIREMENTS.required_section_rows).length) errors.push("CP00-523 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP523_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-523 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-523 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-523 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-523 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-523 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-523 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-523 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP523_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-523 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP523_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-523 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-523 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-523 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP523_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-523 contract current_pack drift");
  }
  if (
    contractProjection?.p02_fixture_slice_descriptor?.descriptor &&
    contractProjection.p02_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-523 contract p02_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP523_PACK_BINDING.next_pack_id) errors.push("CP00-523 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP523_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-523 next subphase drift");
  }
  return freezeCp523Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp523HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp523P02FixtureSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp523Coverage(planPack);
  const slice = validateAiGovernanceCoreCp523P02FixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp523Validation({
    evidence_packet: "H17.CP00-523.ai_governance_core_p02_fixture_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP523_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_permission_fixture_slice_pack_id: AI_GOVERNANCE_CORE_CP523_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP523_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP523_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP523_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp523ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp523Coverage(planPack);
  const slice = validateAiGovernanceCoreCp523P02FixtureSliceDescriptor(createAiGovernanceCoreCp523P02FixtureSliceDescriptor(), {});
  return freezeCp523Validation({
    review_packet: "C17.CP00-523.ai_governance_core_p02_fixture_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP523_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP523_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_fixture_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP523_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-523 cover exactly the 10 planned RP17 units from RP17.P02.M06.S06 through RP17.P02.M06.S15?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-523 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp523CloseoutHandoff() {
  return freezeCp523Validation({
    handoff_id: "CP00-523-to-CP00-524",
    from_pack_id: AI_GOVERNANCE_CORE_CP523_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP523_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP523_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP523_PACK_BINDING.range,
    open_scope: "Continue RP17.P02.M06.S16 onward with the remaining synthetic fixture rows and the test micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP523_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp524Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP524_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp524CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp524Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp524Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-524 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-524");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP524_PACK_BINDING.risk_class) errors.push("CP00-524 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP524_PACK_BINDING.unit_count) errors.push("CP00-524 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP524_PACK_BINDING.first_unit_id) errors.push("CP00-524 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP524_PACK_BINDING.last_unit_id) errors.push("CP00-524 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-524 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-524 must only include RP17 units");
  const summary = createAiGovernanceCoreCp524CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP524_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-524 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP524_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-524 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP524_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-524 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP524_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-524 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP524_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-524 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-524 ${microId} missing row ${title}`);
    }
  }
  return freezeCp524Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp524P02FixtureTestSliceDescriptor(
  descriptor = createAiGovernanceCoreCp524P02FixtureTestSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_fixture_test_slice_case_set ?? createAiGovernanceCoreCp524P02FixtureTestSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp524P02FixtureTestSliceDescriptor") errors.push("CP00-524 descriptor type drift");
  if (descriptor.source_p02_fixture_slice_descriptor !== "AiGovernanceCoreCp523P02FixtureSliceDescriptor") {
    errors.push("CP00-524 source p02 fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP524_REQUIREMENTS.required_section_rows).length) errors.push("CP00-524 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP524_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-524 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-524 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-524 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-524 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-524 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-524 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-524 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP524_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-524 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP524_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-524 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-524 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-524 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-524 contract current_pack drift");
  }
  if (
    contractProjection?.p02_fixture_test_slice_descriptor?.descriptor &&
    contractProjection.p02_fixture_test_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-524 contract p02_fixture_test_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP524_PACK_BINDING.next_pack_id) errors.push("CP00-524 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP524_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-524 next subphase drift");
  }
  return freezeCp524Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp524HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp524P02FixtureTestSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp524Coverage(planPack);
  const slice = validateAiGovernanceCoreCp524P02FixtureTestSliceDescriptor(descriptor, contractProjection);
  return freezeCp524Validation({
    evidence_packet: "H17.CP00-524.ai_governance_core_p02_fixture_test_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP524_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_fixture_test_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_fixture_slice_pack_id: AI_GOVERNANCE_CORE_CP524_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP524_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP524_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP524_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp524ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp524Coverage(planPack);
  const slice = validateAiGovernanceCoreCp524P02FixtureTestSliceDescriptor(createAiGovernanceCoreCp524P02FixtureTestSliceDescriptor(), {});
  return freezeCp524Validation({
    review_packet: "C17.CP00-524.ai_governance_core_p02_fixture_test_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP524_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP524_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_fixture_test_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP524_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-524 cover exactly the 10 planned RP17 units from RP17.P02.M06.S16 through RP17.P02.M07.S03?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-524 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp524CloseoutHandoff() {
  return freezeCp524Validation({
    handoff_id: "CP00-524-to-CP00-525",
    from_pack_id: AI_GOVERNANCE_CORE_CP524_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP524_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP524_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP524_PACK_BINDING.range,
    open_scope: "Continue RP17.P02.M07.S04 onward with the remaining test and golden case rows while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP524_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp525Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP525_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp525CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp525Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp525Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-525 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-525");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP525_PACK_BINDING.risk_class) errors.push("CP00-525 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP525_PACK_BINDING.unit_count) errors.push("CP00-525 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP525_PACK_BINDING.first_unit_id) errors.push("CP00-525 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP525_PACK_BINDING.last_unit_id) errors.push("CP00-525 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-525 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-525 must only include RP17 units");
  const summary = createAiGovernanceCoreCp525CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP525_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-525 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP525_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-525 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP525_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-525 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP525_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-525 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP525_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-525 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-525 ${microId} missing row ${title}`);
    }
  }
  return freezeCp525Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp525P02TestHermesSliceDescriptor(
  descriptor = createAiGovernanceCoreCp525P02TestHermesSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_test_hermes_slice_case_set ?? createAiGovernanceCoreCp525P02TestHermesSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp525P02TestHermesSliceDescriptor") errors.push("CP00-525 descriptor type drift");
  if (descriptor.source_p02_fixture_test_slice_descriptor !== "AiGovernanceCoreCp524P02FixtureTestSliceDescriptor") {
    errors.push("CP00-525 source p02 fixture test slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP525_REQUIREMENTS.required_section_rows).length) errors.push("CP00-525 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP525_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-525 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-525 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-525 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-525 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-525 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-525 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-525 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP525_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-525 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP525_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-525 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-525 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-525 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-525 contract current_pack drift");
  }
  if (
    contractProjection?.p02_test_hermes_slice_descriptor?.descriptor &&
    contractProjection.p02_test_hermes_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-525 contract p02_test_hermes_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP525_PACK_BINDING.next_pack_id) errors.push("CP00-525 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP525_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-525 next subphase drift");
  }
  return freezeCp525Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp525HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp525P02TestHermesSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp525Coverage(planPack);
  const slice = validateAiGovernanceCoreCp525P02TestHermesSliceDescriptor(descriptor, contractProjection);
  return freezeCp525Validation({
    evidence_packet: "H17.CP00-525.ai_governance_core_p02_test_hermes_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP525_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_test_hermes_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_fixture_test_slice_pack_id: AI_GOVERNANCE_CORE_CP525_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP525_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP525_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP525_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp525ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp525Coverage(planPack);
  const slice = validateAiGovernanceCoreCp525P02TestHermesSliceDescriptor(createAiGovernanceCoreCp525P02TestHermesSliceDescriptor(), {});
  return freezeCp525Validation({
    review_packet: "C17.CP00-525.ai_governance_core_p02_test_hermes_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP525_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP525_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_test_hermes_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP525_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-525 cover exactly the 40 planned RP17 units from RP17.P02.M07.S04 through RP17.P02.M08.S18?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-525 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp525CloseoutHandoff() {
  return freezeCp525Validation({
    handoff_id: "CP00-525-to-CP00-526",
    from_pack_id: AI_GOVERNANCE_CORE_CP525_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP525_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP525_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP525_PACK_BINDING.range,
    open_scope: "Continue RP17.P02.M08.S19 onward with the remaining hermes evidence rows and the review micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP525_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp526Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP526_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp526CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp526Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp526Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-526 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-526");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP526_PACK_BINDING.risk_class) errors.push("CP00-526 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP526_PACK_BINDING.unit_count) errors.push("CP00-526 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP526_PACK_BINDING.first_unit_id) errors.push("CP00-526 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP526_PACK_BINDING.last_unit_id) errors.push("CP00-526 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-526 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-526 must only include RP17 units");
  const summary = createAiGovernanceCoreCp526CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP526_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-526 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP526_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-526 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP526_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-526 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP526_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-526 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP526_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-526 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-526 ${microId} missing row ${title}`);
    }
  }
  return freezeCp526Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp526P02CloseoutP03FoundationDescriptor(
  descriptor = createAiGovernanceCoreCp526P02CloseoutP03FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_closeout_p03_foundation_case_set ?? createAiGovernanceCoreCp526P02CloseoutP03FoundationCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp526P02CloseoutP03FoundationDescriptor") errors.push("CP00-526 descriptor type drift");
  if (descriptor.source_p02_test_hermes_slice_descriptor !== "AiGovernanceCoreCp525P02TestHermesSliceDescriptor") {
    errors.push("CP00-526 source p02 test hermes slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP526_REQUIREMENTS.required_section_rows).length) errors.push("CP00-526 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP526_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-526 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-526 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-526 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-526 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-526 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-526 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-526 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP526_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-526 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP526_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-526 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-526 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-526 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-526 contract current_pack drift");
  }
  if (
    contractProjection?.p02_closeout_p03_foundation_descriptor?.descriptor &&
    contractProjection.p02_closeout_p03_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-526 contract p02_closeout_p03_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP526_PACK_BINDING.next_pack_id) errors.push("CP00-526 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP526_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-526 next subphase drift");
  }
  return freezeCp526Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp526HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp526P02CloseoutP03FoundationDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp526Coverage(planPack);
  const slice = validateAiGovernanceCoreCp526P02CloseoutP03FoundationDescriptor(descriptor, contractProjection);
  return freezeCp526Validation({
    evidence_packet: "H17.CP00-526.ai_governance_core_p02_closeout_p03_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP526_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_closeout_p03_foundation_valid: slice.valid,
    no_real_data: true,
    source_p02_test_hermes_slice_pack_id: AI_GOVERNANCE_CORE_CP526_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP526_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP526_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP526_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp526ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp526Coverage(planPack);
  const slice = validateAiGovernanceCoreCp526P02CloseoutP03FoundationDescriptor(createAiGovernanceCoreCp526P02CloseoutP03FoundationDescriptor(), {});
  return freezeCp526Validation({
    review_packet: "C17.CP00-526.ai_governance_core_p02_closeout_p03_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP526_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP526_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_closeout_p03_foundation_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP526_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-526 cover exactly the 150 planned RP17 units from RP17.P02.M08.S19 through RP17.P03.M05.S22?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-526 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp526CloseoutHandoff() {
  return freezeCp526Validation({
    handoff_id: "CP00-526-to-CP00-527",
    from_pack_id: AI_GOVERNANCE_CORE_CP526_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP526_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP526_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP526_PACK_BINDING.range,
    open_scope: "Continue RP17.P03.M06.S01 onward with the synthetic fixture rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP526_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp527Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP527_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp527CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp527Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp527Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-527 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-527");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP527_PACK_BINDING.risk_class) errors.push("CP00-527 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP527_PACK_BINDING.unit_count) errors.push("CP00-527 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP527_PACK_BINDING.first_unit_id) errors.push("CP00-527 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP527_PACK_BINDING.last_unit_id) errors.push("CP00-527 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-527 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-527 must only include RP17 units");
  const summary = createAiGovernanceCoreCp527CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP527_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-527 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP527_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-527 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP527_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-527 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP527_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-527 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP527_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-527 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-527 ${microId} missing row ${title}`);
    }
  }
  return freezeCp527Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp527P03CloseoutP04FoundationDescriptor(
  descriptor = createAiGovernanceCoreCp527P03CloseoutP04FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_closeout_p04_foundation_case_set ?? createAiGovernanceCoreCp527P03CloseoutP04FoundationCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp527P03CloseoutP04FoundationDescriptor") errors.push("CP00-527 descriptor type drift");
  if (descriptor.source_p02_closeout_p03_foundation_descriptor !== "AiGovernanceCoreCp526P02CloseoutP03FoundationDescriptor") {
    errors.push("CP00-527 source p02 closeout p03 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP527_REQUIREMENTS.required_section_rows).length) errors.push("CP00-527 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP527_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-527 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-527 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-527 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-527 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-527 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-527 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-527 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP527_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-527 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP527_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-527 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-527 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-527 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-527 contract current_pack drift");
  }
  if (
    contractProjection?.p03_closeout_p04_foundation_descriptor?.descriptor &&
    contractProjection.p03_closeout_p04_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-527 contract p03_closeout_p04_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP527_PACK_BINDING.next_pack_id) errors.push("CP00-527 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP527_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-527 next subphase drift");
  }
  return freezeCp527Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp527HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp527P03CloseoutP04FoundationDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp527Coverage(planPack);
  const slice = validateAiGovernanceCoreCp527P03CloseoutP04FoundationDescriptor(descriptor, contractProjection);
  return freezeCp527Validation({
    evidence_packet: "H17.CP00-527.ai_governance_core_p03_closeout_p04_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP527_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_closeout_p04_foundation_valid: slice.valid,
    no_real_data: true,
    source_p02_closeout_p03_foundation_pack_id: AI_GOVERNANCE_CORE_CP527_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP527_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP527_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP527_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp527ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp527Coverage(planPack);
  const slice = validateAiGovernanceCoreCp527P03CloseoutP04FoundationDescriptor(createAiGovernanceCoreCp527P03CloseoutP04FoundationDescriptor(), {});
  return freezeCp527Validation({
    review_packet: "C17.CP00-527.ai_governance_core_p03_closeout_p04_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP527_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP527_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_closeout_p04_foundation_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP527_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-527 cover exactly the 150 planned RP17 units from RP17.P03.M06.S01 through RP17.P04.M03.S08?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-527 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp527CloseoutHandoff() {
  return freezeCp527Validation({
    handoff_id: "CP00-527-to-CP00-528",
    from_pack_id: AI_GOVERNANCE_CORE_CP527_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP527_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP527_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP527_PACK_BINDING.range,
    open_scope: "Continue RP17.P04.M03.S09 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP527_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp528Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP528_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp528CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp528Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp528Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-528 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-528");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP528_PACK_BINDING.risk_class) errors.push("CP00-528 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP528_PACK_BINDING.unit_count) errors.push("CP00-528 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP528_PACK_BINDING.first_unit_id) errors.push("CP00-528 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP528_PACK_BINDING.last_unit_id) errors.push("CP00-528 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-528 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-528 must only include RP17 units");
  const summary = createAiGovernanceCoreCp528CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP528_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-528 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP528_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-528 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP528_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-528 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP528_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-528 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP528_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-528 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-528 ${microId} missing row ${title}`);
    }
  }
  return freezeCp528Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp528P04ImplementationSliceDescriptor(
  descriptor = createAiGovernanceCoreCp528P04ImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_implementation_slice_case_set ?? createAiGovernanceCoreCp528P04ImplementationSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp528P04ImplementationSliceDescriptor") errors.push("CP00-528 descriptor type drift");
  if (descriptor.source_p03_closeout_p04_foundation_descriptor !== "AiGovernanceCoreCp527P03CloseoutP04FoundationDescriptor") {
    errors.push("CP00-528 source p03 closeout p04 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP528_REQUIREMENTS.required_section_rows).length) errors.push("CP00-528 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP528_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-528 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-528 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-528 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-528 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-528 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-528 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-528 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP528_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-528 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP528_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-528 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-528 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-528 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-528 contract current_pack drift");
  }
  if (
    contractProjection?.p04_implementation_slice_descriptor?.descriptor &&
    contractProjection.p04_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-528 contract p04_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP528_PACK_BINDING.next_pack_id) errors.push("CP00-528 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP528_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-528 next subphase drift");
  }
  return freezeCp528Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp528HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp528P04ImplementationSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp528Coverage(planPack);
  const slice = validateAiGovernanceCoreCp528P04ImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp528Validation({
    evidence_packet: "H17.CP00-528.ai_governance_core_p04_implementation_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP528_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_p03_closeout_p04_foundation_pack_id: AI_GOVERNANCE_CORE_CP528_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP528_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP528_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP528_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp528ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp528Coverage(planPack);
  const slice = validateAiGovernanceCoreCp528P04ImplementationSliceDescriptor(createAiGovernanceCoreCp528P04ImplementationSliceDescriptor(), {});
  return freezeCp528Validation({
    review_packet: "C17.CP00-528.ai_governance_core_p04_implementation_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP528_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP528_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_implementation_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP528_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-528 cover exactly the 10 planned RP17 units from RP17.P04.M03.S09 through RP17.P04.M03.S18?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-528 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp528CloseoutHandoff() {
  return freezeCp528Validation({
    handoff_id: "CP00-528-to-CP00-529",
    from_pack_id: AI_GOVERNANCE_CORE_CP528_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP528_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP528_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP528_PACK_BINDING.range,
    open_scope: "Continue RP17.P04.M03.S19 onward with the remaining primary implementation rows while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP528_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp529Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP529_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp529CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp529Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp529Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-529 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-529");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP529_PACK_BINDING.risk_class) errors.push("CP00-529 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP529_PACK_BINDING.unit_count) errors.push("CP00-529 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP529_PACK_BINDING.first_unit_id) errors.push("CP00-529 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP529_PACK_BINDING.last_unit_id) errors.push("CP00-529 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-529 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-529 must only include RP17 units");
  const summary = createAiGovernanceCoreCp529CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP529_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-529 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP529_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-529 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP529_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-529 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP529_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-529 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP529_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-529 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-529 ${microId} missing row ${title}`);
    }
  }
  return freezeCp529Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp529P04WorkflowPermissionSliceDescriptor(
  descriptor = createAiGovernanceCoreCp529P04WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_workflow_permission_slice_case_set ?? createAiGovernanceCoreCp529P04WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp529P04WorkflowPermissionSliceDescriptor") errors.push("CP00-529 descriptor type drift");
  if (descriptor.source_p04_implementation_slice_descriptor !== "AiGovernanceCoreCp528P04ImplementationSliceDescriptor") {
    errors.push("CP00-529 source p04 implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP529_REQUIREMENTS.required_section_rows).length) errors.push("CP00-529 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP529_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-529 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-529 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-529 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-529 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-529 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-529 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-529 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP529_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-529 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP529_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-529 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-529 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-529 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-529 contract current_pack drift");
  }
  if (
    contractProjection?.p04_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p04_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-529 contract p04_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP529_PACK_BINDING.next_pack_id) errors.push("CP00-529 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP529_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-529 next subphase drift");
  }
  return freezeCp529Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp529HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp529P04WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp529Coverage(planPack);
  const slice = validateAiGovernanceCoreCp529P04WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp529Validation({
    evidence_packet: "H17.CP00-529.ai_governance_core_p04_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP529_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p04_implementation_slice_pack_id: AI_GOVERNANCE_CORE_CP529_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP529_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP529_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP529_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp529ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp529Coverage(planPack);
  const slice = validateAiGovernanceCoreCp529P04WorkflowPermissionSliceDescriptor(createAiGovernanceCoreCp529P04WorkflowPermissionSliceDescriptor(), {});
  return freezeCp529Validation({
    review_packet: "C17.CP00-529.ai_governance_core_p04_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP529_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP529_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP529_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-529 cover exactly the 40 planned RP17 units from RP17.P04.M03.S19 through RP17.P04.M05.S14?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-529 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp529CloseoutHandoff() {
  return freezeCp529Validation({
    handoff_id: "CP00-529-to-CP00-530",
    from_pack_id: AI_GOVERNANCE_CORE_CP529_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP529_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP529_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP529_PACK_BINDING.range,
    open_scope: "Continue RP17.P04.M05.S15 onward with the remaining permission and audit binding rows and the synthetic fixture micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP529_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp530Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP530_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp530CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp530Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp530Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-530 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-530");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP530_PACK_BINDING.risk_class) errors.push("CP00-530 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP530_PACK_BINDING.unit_count) errors.push("CP00-530 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP530_PACK_BINDING.first_unit_id) errors.push("CP00-530 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP530_PACK_BINDING.last_unit_id) errors.push("CP00-530 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-530 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-530 must only include RP17 units");
  const summary = createAiGovernanceCoreCp530CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP530_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-530 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP530_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-530 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP530_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-530 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP530_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-530 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP530_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-530 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-530 ${microId} missing row ${title}`);
    }
  }
  return freezeCp530Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp530P04PermissionFixtureSliceDescriptor(
  descriptor = createAiGovernanceCoreCp530P04PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_permission_fixture_slice_case_set ?? createAiGovernanceCoreCp530P04PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp530P04PermissionFixtureSliceDescriptor") errors.push("CP00-530 descriptor type drift");
  if (descriptor.source_p04_workflow_permission_slice_descriptor !== "AiGovernanceCoreCp529P04WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-530 source p04 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP530_REQUIREMENTS.required_section_rows).length) errors.push("CP00-530 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP530_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-530 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-530 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-530 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-530 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-530 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-530 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-530 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP530_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-530 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP530_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-530 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-530 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-530 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-530 contract current_pack drift");
  }
  if (
    contractProjection?.p04_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p04_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-530 contract p04_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP530_PACK_BINDING.next_pack_id) errors.push("CP00-530 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP530_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-530 next subphase drift");
  }
  return freezeCp530Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp530HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp530P04PermissionFixtureSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp530Coverage(planPack);
  const slice = validateAiGovernanceCoreCp530P04PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp530Validation({
    evidence_packet: "H17.CP00-530.ai_governance_core_p04_permission_fixture_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP530_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p04_workflow_permission_slice_pack_id: AI_GOVERNANCE_CORE_CP530_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP530_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP530_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP530_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp530ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp530Coverage(planPack);
  const slice = validateAiGovernanceCoreCp530P04PermissionFixtureSliceDescriptor(createAiGovernanceCoreCp530P04PermissionFixtureSliceDescriptor(), {});
  return freezeCp530Validation({
    review_packet: "C17.CP00-530.ai_governance_core_p04_permission_fixture_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP530_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP530_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP530_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-530 cover exactly the 10 planned RP17 units from RP17.P04.M05.S15 through RP17.P04.M06.S02?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-530 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp530CloseoutHandoff() {
  return freezeCp530Validation({
    handoff_id: "CP00-530-to-CP00-531",
    from_pack_id: AI_GOVERNANCE_CORE_CP530_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP530_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP530_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP530_PACK_BINDING.range,
    open_scope: "Continue RP17.P04.M06.S03 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP530_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp531Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP531_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp531CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp531Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp531Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-531 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-531");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP531_PACK_BINDING.risk_class) errors.push("CP00-531 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP531_PACK_BINDING.unit_count) errors.push("CP00-531 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP531_PACK_BINDING.first_unit_id) errors.push("CP00-531 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP531_PACK_BINDING.last_unit_id) errors.push("CP00-531 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-531 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-531 must only include RP17 units");
  const summary = createAiGovernanceCoreCp531CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP531_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-531 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP531_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-531 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP531_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-531 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP531_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-531 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP531_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-531 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-531 ${microId} missing row ${title}`);
    }
  }
  return freezeCp531Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp531P04CloseoutP05FoundationDescriptor(
  descriptor = createAiGovernanceCoreCp531P04CloseoutP05FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_closeout_p05_foundation_case_set ?? createAiGovernanceCoreCp531P04CloseoutP05FoundationCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp531P04CloseoutP05FoundationDescriptor") errors.push("CP00-531 descriptor type drift");
  if (descriptor.source_p04_permission_fixture_slice_descriptor !== "AiGovernanceCoreCp530P04PermissionFixtureSliceDescriptor") {
    errors.push("CP00-531 source p04 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP531_REQUIREMENTS.required_section_rows).length) errors.push("CP00-531 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP531_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-531 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-531 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-531 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-531 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-531 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-531 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-531 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP531_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-531 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP531_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-531 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-531 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-531 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-531 contract current_pack drift");
  }
  if (
    contractProjection?.p04_closeout_p05_foundation_descriptor?.descriptor &&
    contractProjection.p04_closeout_p05_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-531 contract p04_closeout_p05_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP531_PACK_BINDING.next_pack_id) errors.push("CP00-531 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP531_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-531 next subphase drift");
  }
  return freezeCp531Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp531HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp531P04CloseoutP05FoundationDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp531Coverage(planPack);
  const slice = validateAiGovernanceCoreCp531P04CloseoutP05FoundationDescriptor(descriptor, contractProjection);
  return freezeCp531Validation({
    evidence_packet: "H17.CP00-531.ai_governance_core_p04_closeout_p05_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP531_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_closeout_p05_foundation_valid: slice.valid,
    no_real_data: true,
    source_p04_permission_fixture_slice_pack_id: AI_GOVERNANCE_CORE_CP531_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP531_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP531_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP531_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp531ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp531Coverage(planPack);
  const slice = validateAiGovernanceCoreCp531P04CloseoutP05FoundationDescriptor(createAiGovernanceCoreCp531P04CloseoutP05FoundationDescriptor(), {});
  return freezeCp531Validation({
    review_packet: "C17.CP00-531.ai_governance_core_p04_closeout_p05_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP531_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP531_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_closeout_p05_foundation_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP531_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-531 cover exactly the 150 planned RP17 units from RP17.P04.M06.S03 through RP17.P05.M02.S14?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-531 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp531CloseoutHandoff() {
  return freezeCp531Validation({
    handoff_id: "CP00-531-to-CP00-532",
    from_pack_id: AI_GOVERNANCE_CORE_CP531_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP531_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP531_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP531_PACK_BINDING.range,
    open_scope: "Continue RP17.P05.M02.S15 onward with the remaining type and shape rows and the primary implementation micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP531_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp532Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP532_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp532CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp532Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp532Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-532 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-532");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP532_PACK_BINDING.risk_class) errors.push("CP00-532 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP532_PACK_BINDING.unit_count) errors.push("CP00-532 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP532_PACK_BINDING.first_unit_id) errors.push("CP00-532 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP532_PACK_BINDING.last_unit_id) errors.push("CP00-532 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-532 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-532 must only include RP17 units");
  const summary = createAiGovernanceCoreCp532CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP532_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-532 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP532_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-532 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP532_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-532 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP532_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-532 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP532_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-532 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-532 ${microId} missing row ${title}`);
    }
  }
  return freezeCp532Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp532P05ImplementationWorkflowSliceDescriptor(
  descriptor = createAiGovernanceCoreCp532P05ImplementationWorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_implementation_workflow_slice_case_set ?? createAiGovernanceCoreCp532P05ImplementationWorkflowSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp532P05ImplementationWorkflowSliceDescriptor") errors.push("CP00-532 descriptor type drift");
  if (descriptor.source_p04_closeout_p05_foundation_descriptor !== "AiGovernanceCoreCp531P04CloseoutP05FoundationDescriptor") {
    errors.push("CP00-532 source p04 closeout p05 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP532_REQUIREMENTS.required_section_rows).length) errors.push("CP00-532 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP532_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-532 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-532 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-532 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-532 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-532 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-532 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-532 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP532_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-532 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP532_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-532 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-532 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-532 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-532 contract current_pack drift");
  }
  if (
    contractProjection?.p05_implementation_workflow_slice_descriptor?.descriptor &&
    contractProjection.p05_implementation_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-532 contract p05_implementation_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP532_PACK_BINDING.next_pack_id) errors.push("CP00-532 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP532_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-532 next subphase drift");
  }
  return freezeCp532Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp532HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp532P05ImplementationWorkflowSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp532Coverage(planPack);
  const slice = validateAiGovernanceCoreCp532P05ImplementationWorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp532Validation({
    evidence_packet: "H17.CP00-532.ai_governance_core_p05_implementation_workflow_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP532_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_implementation_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_p04_closeout_p05_foundation_pack_id: AI_GOVERNANCE_CORE_CP532_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP532_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP532_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP532_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp532ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp532Coverage(planPack);
  const slice = validateAiGovernanceCoreCp532P05ImplementationWorkflowSliceDescriptor(createAiGovernanceCoreCp532P05ImplementationWorkflowSliceDescriptor(), {});
  return freezeCp532Validation({
    review_packet: "C17.CP00-532.ai_governance_core_p05_implementation_workflow_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP532_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP532_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_implementation_workflow_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP532_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-532 cover exactly the 40 planned RP17 units from RP17.P05.M02.S15 through RP17.P05.M04.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-532 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp532CloseoutHandoff() {
  return freezeCp532Validation({
    handoff_id: "CP00-532-to-CP00-533",
    from_pack_id: AI_GOVERNANCE_CORE_CP532_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP532_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP532_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP532_PACK_BINDING.range,
    open_scope: "Continue RP17.P05.M04.S13 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP532_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp533Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP533_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp533CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp533Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp533Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-533 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-533");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP533_PACK_BINDING.risk_class) errors.push("CP00-533 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP533_PACK_BINDING.unit_count) errors.push("CP00-533 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP533_PACK_BINDING.first_unit_id) errors.push("CP00-533 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP533_PACK_BINDING.last_unit_id) errors.push("CP00-533 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-533 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-533 must only include RP17 units");
  const summary = createAiGovernanceCoreCp533CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP533_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-533 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP533_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-533 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP533_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-533 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP533_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-533 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP533_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-533 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-533 ${microId} missing row ${title}`);
    }
  }
  return freezeCp533Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp533P05WorkflowPermissionSliceDescriptor(
  descriptor = createAiGovernanceCoreCp533P05WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_workflow_permission_slice_case_set ?? createAiGovernanceCoreCp533P05WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp533P05WorkflowPermissionSliceDescriptor") errors.push("CP00-533 descriptor type drift");
  if (descriptor.source_p05_implementation_workflow_slice_descriptor !== "AiGovernanceCoreCp532P05ImplementationWorkflowSliceDescriptor") {
    errors.push("CP00-533 source p05 implementation workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP533_REQUIREMENTS.required_section_rows).length) errors.push("CP00-533 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP533_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-533 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-533 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-533 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-533 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-533 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-533 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-533 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP533_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-533 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP533_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-533 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-533 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-533 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-533 contract current_pack drift");
  }
  if (
    contractProjection?.p05_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p05_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-533 contract p05_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP533_PACK_BINDING.next_pack_id) errors.push("CP00-533 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP533_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-533 next subphase drift");
  }
  return freezeCp533Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp533HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp533P05WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp533Coverage(planPack);
  const slice = validateAiGovernanceCoreCp533P05WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp533Validation({
    evidence_packet: "H17.CP00-533.ai_governance_core_p05_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP533_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_implementation_workflow_slice_pack_id: AI_GOVERNANCE_CORE_CP533_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP533_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP533_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP533_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp533ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp533Coverage(planPack);
  const slice = validateAiGovernanceCoreCp533P05WorkflowPermissionSliceDescriptor(createAiGovernanceCoreCp533P05WorkflowPermissionSliceDescriptor(), {});
  return freezeCp533Validation({
    review_packet: "C17.CP00-533.ai_governance_core_p05_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP533_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP533_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP533_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-533 cover exactly the 40 planned RP17 units from RP17.P05.M04.S13 through RP17.P05.M06.S08?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-533 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp533CloseoutHandoff() {
  return freezeCp533Validation({
    handoff_id: "CP00-533-to-CP00-534",
    from_pack_id: AI_GOVERNANCE_CORE_CP533_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP533_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP533_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP533_PACK_BINDING.range,
    open_scope: "Continue RP17.P05.M06.S09 onward with the remaining synthetic fixture rows while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP533_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp534Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP534_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp534CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp534Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp534Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-534 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-534");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP534_PACK_BINDING.risk_class) errors.push("CP00-534 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP534_PACK_BINDING.unit_count) errors.push("CP00-534 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP534_PACK_BINDING.first_unit_id) errors.push("CP00-534 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP534_PACK_BINDING.last_unit_id) errors.push("CP00-534 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-534 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-534 must only include RP17 units");
  const summary = createAiGovernanceCoreCp534CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP534_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-534 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP534_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-534 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP534_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-534 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP534_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-534 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP534_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-534 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-534 ${microId} missing row ${title}`);
    }
  }
  return freezeCp534Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp534P05FixtureSliceDescriptor(
  descriptor = createAiGovernanceCoreCp534P05FixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_fixture_slice_case_set ?? createAiGovernanceCoreCp534P05FixtureSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp534P05FixtureSliceDescriptor") errors.push("CP00-534 descriptor type drift");
  if (descriptor.source_p05_workflow_permission_slice_descriptor !== "AiGovernanceCoreCp533P05WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-534 source p05 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP534_REQUIREMENTS.required_section_rows).length) errors.push("CP00-534 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP534_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-534 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-534 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-534 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-534 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-534 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-534 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-534 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP534_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-534 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP534_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-534 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-534 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-534 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-534 contract current_pack drift");
  }
  if (
    contractProjection?.p05_fixture_slice_descriptor?.descriptor &&
    contractProjection.p05_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-534 contract p05_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP534_PACK_BINDING.next_pack_id) errors.push("CP00-534 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP534_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-534 next subphase drift");
  }
  return freezeCp534Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp534HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp534P05FixtureSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp534Coverage(planPack);
  const slice = validateAiGovernanceCoreCp534P05FixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp534Validation({
    evidence_packet: "H17.CP00-534.ai_governance_core_p05_fixture_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP534_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_workflow_permission_slice_pack_id: AI_GOVERNANCE_CORE_CP534_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP534_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP534_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP534_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp534ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp534Coverage(planPack);
  const slice = validateAiGovernanceCoreCp534P05FixtureSliceDescriptor(createAiGovernanceCoreCp534P05FixtureSliceDescriptor(), {});
  return freezeCp534Validation({
    review_packet: "C17.CP00-534.ai_governance_core_p05_fixture_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP534_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP534_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_fixture_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP534_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-534 cover exactly the 10 planned RP17 units from RP17.P05.M06.S09 through RP17.P05.M06.S18?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-534 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp534CloseoutHandoff() {
  return freezeCp534Validation({
    handoff_id: "CP00-534-to-CP00-535",
    from_pack_id: AI_GOVERNANCE_CORE_CP534_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP534_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP534_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP534_PACK_BINDING.range,
    open_scope: "Continue RP17.P05.M06.S19 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP534_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp535Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP535_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp535CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp535Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp535Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-535 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-535");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP535_PACK_BINDING.risk_class) errors.push("CP00-535 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP535_PACK_BINDING.unit_count) errors.push("CP00-535 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP535_PACK_BINDING.first_unit_id) errors.push("CP00-535 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP535_PACK_BINDING.last_unit_id) errors.push("CP00-535 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-535 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-535 must only include RP17 units");
  const summary = createAiGovernanceCoreCp535CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP535_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-535 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP535_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-535 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP535_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-535 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP535_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-535 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP535_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-535 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-535 ${microId} missing row ${title}`);
    }
  }
  return freezeCp535Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp535P05CloseoutP06FoundationDescriptor(
  descriptor = createAiGovernanceCoreCp535P05CloseoutP06FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_closeout_p06_foundation_case_set ?? createAiGovernanceCoreCp535P05CloseoutP06FoundationCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp535P05CloseoutP06FoundationDescriptor") errors.push("CP00-535 descriptor type drift");
  if (descriptor.source_p05_fixture_slice_descriptor !== "AiGovernanceCoreCp534P05FixtureSliceDescriptor") {
    errors.push("CP00-535 source p05 fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP535_REQUIREMENTS.required_section_rows).length) errors.push("CP00-535 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP535_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-535 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-535 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-535 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-535 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-535 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-535 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-535 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP535_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-535 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP535_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-535 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-535 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-535 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-535 contract current_pack drift");
  }
  if (
    contractProjection?.p05_closeout_p06_foundation_descriptor?.descriptor &&
    contractProjection.p05_closeout_p06_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-535 contract p05_closeout_p06_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP535_PACK_BINDING.next_pack_id) errors.push("CP00-535 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP535_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-535 next subphase drift");
  }
  return freezeCp535Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp535HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp535P05CloseoutP06FoundationDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp535Coverage(planPack);
  const slice = validateAiGovernanceCoreCp535P05CloseoutP06FoundationDescriptor(descriptor, contractProjection);
  return freezeCp535Validation({
    evidence_packet: "H17.CP00-535.ai_governance_core_p05_closeout_p06_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP535_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_closeout_p06_foundation_valid: slice.valid,
    no_real_data: true,
    source_p05_fixture_slice_pack_id: AI_GOVERNANCE_CORE_CP535_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP535_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP535_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP535_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp535ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp535Coverage(planPack);
  const slice = validateAiGovernanceCoreCp535P05CloseoutP06FoundationDescriptor(createAiGovernanceCoreCp535P05CloseoutP06FoundationDescriptor(), {});
  return freezeCp535Validation({
    review_packet: "C17.CP00-535.ai_governance_core_p05_closeout_p06_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP535_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP535_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_closeout_p06_foundation_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP535_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-535 cover exactly the 150 planned RP17 units from RP17.P05.M06.S19 through RP17.P06.M02.S20?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-535 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp535CloseoutHandoff() {
  return freezeCp535Validation({
    handoff_id: "CP00-535-to-CP00-536",
    from_pack_id: AI_GOVERNANCE_CORE_CP535_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP535_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP535_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP535_PACK_BINDING.range,
    open_scope: "Continue RP17.P06.M02.S21 onward with the remaining type and shape rows and the primary implementation micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP535_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp536Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP536_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp536CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp536Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp536Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-536 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-536");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP536_PACK_BINDING.risk_class) errors.push("CP00-536 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP536_PACK_BINDING.unit_count) errors.push("CP00-536 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP536_PACK_BINDING.first_unit_id) errors.push("CP00-536 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP536_PACK_BINDING.last_unit_id) errors.push("CP00-536 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-536 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-536 must only include RP17 units");
  const summary = createAiGovernanceCoreCp536CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP536_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-536 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP536_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-536 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP536_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-536 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP536_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-536 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP536_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-536 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-536 ${microId} missing row ${title}`);
    }
  }
  return freezeCp536Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp536P06FoundationSliceDescriptor(
  descriptor = createAiGovernanceCoreCp536P06FoundationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_foundation_slice_case_set ?? createAiGovernanceCoreCp536P06FoundationSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp536P06FoundationSliceDescriptor") errors.push("CP00-536 descriptor type drift");
  if (descriptor.source_p05_closeout_p06_foundation_descriptor !== "AiGovernanceCoreCp535P05CloseoutP06FoundationDescriptor") {
    errors.push("CP00-536 source p05 closeout p06 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP536_REQUIREMENTS.required_section_rows).length) errors.push("CP00-536 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP536_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-536 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-536 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-536 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-536 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-536 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-536 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-536 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP536_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-536 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP536_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-536 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-536 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-536 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-536 contract current_pack drift");
  }
  if (
    contractProjection?.p06_foundation_slice_descriptor?.descriptor &&
    contractProjection.p06_foundation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-536 contract p06_foundation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP536_PACK_BINDING.next_pack_id) errors.push("CP00-536 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP536_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-536 next subphase drift");
  }
  return freezeCp536Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp536HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp536P06FoundationSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp536Coverage(planPack);
  const slice = validateAiGovernanceCoreCp536P06FoundationSliceDescriptor(descriptor, contractProjection);
  return freezeCp536Validation({
    evidence_packet: "H17.CP00-536.ai_governance_core_p06_foundation_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP536_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_foundation_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_closeout_p06_foundation_pack_id: AI_GOVERNANCE_CORE_CP536_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP536_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP536_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP536_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp536ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp536Coverage(planPack);
  const slice = validateAiGovernanceCoreCp536P06FoundationSliceDescriptor(createAiGovernanceCoreCp536P06FoundationSliceDescriptor(), {});
  return freezeCp536Validation({
    review_packet: "C17.CP00-536.ai_governance_core_p06_foundation_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP536_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP536_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_foundation_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP536_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-536 cover exactly the 10 planned RP17 units from RP17.P06.M02.S21 through RP17.P06.M03.S08?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-536 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp536CloseoutHandoff() {
  return freezeCp536Validation({
    handoff_id: "CP00-536-to-CP00-537",
    from_pack_id: AI_GOVERNANCE_CORE_CP536_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP536_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP536_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP536_PACK_BINDING.range,
    open_scope: "Continue RP17.P06.M03.S09 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP536_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp537Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP537_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp537CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp537Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp537Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-537 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-537");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP537_PACK_BINDING.risk_class) errors.push("CP00-537 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP537_PACK_BINDING.unit_count) errors.push("CP00-537 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP537_PACK_BINDING.first_unit_id) errors.push("CP00-537 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP537_PACK_BINDING.last_unit_id) errors.push("CP00-537 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-537 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-537 must only include RP17 units");
  const summary = createAiGovernanceCoreCp537CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP537_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-537 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP537_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-537 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP537_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-537 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP537_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-537 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP537_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-537 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-537 ${microId} missing row ${title}`);
    }
  }
  return freezeCp537Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp537P06ImplementationWorkflowSliceDescriptor(
  descriptor = createAiGovernanceCoreCp537P06ImplementationWorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_implementation_workflow_slice_case_set ?? createAiGovernanceCoreCp537P06ImplementationWorkflowSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp537P06ImplementationWorkflowSliceDescriptor") errors.push("CP00-537 descriptor type drift");
  if (descriptor.source_p06_foundation_slice_descriptor !== "AiGovernanceCoreCp536P06FoundationSliceDescriptor") {
    errors.push("CP00-537 source p06 foundation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP537_REQUIREMENTS.required_section_rows).length) errors.push("CP00-537 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP537_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-537 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-537 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-537 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-537 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-537 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-537 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-537 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP537_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-537 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP537_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-537 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-537 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-537 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-537 contract current_pack drift");
  }
  if (
    contractProjection?.p06_implementation_workflow_slice_descriptor?.descriptor &&
    contractProjection.p06_implementation_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-537 contract p06_implementation_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP537_PACK_BINDING.next_pack_id) errors.push("CP00-537 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP537_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-537 next subphase drift");
  }
  return freezeCp537Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp537HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp537P06ImplementationWorkflowSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp537Coverage(planPack);
  const slice = validateAiGovernanceCoreCp537P06ImplementationWorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp537Validation({
    evidence_packet: "H17.CP00-537.ai_governance_core_p06_implementation_workflow_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP537_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_implementation_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_foundation_slice_pack_id: AI_GOVERNANCE_CORE_CP537_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP537_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP537_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP537_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp537ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp537Coverage(planPack);
  const slice = validateAiGovernanceCoreCp537P06ImplementationWorkflowSliceDescriptor(createAiGovernanceCoreCp537P06ImplementationWorkflowSliceDescriptor(), {});
  return freezeCp537Validation({
    review_packet: "C17.CP00-537.ai_governance_core_p06_implementation_workflow_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP537_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP537_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_implementation_workflow_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP537_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-537 cover exactly the 40 planned RP17 units from RP17.P06.M03.S09 through RP17.P06.M04.S23?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-537 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp537CloseoutHandoff() {
  return freezeCp537Validation({
    handoff_id: "CP00-537-to-CP00-538",
    from_pack_id: AI_GOVERNANCE_CORE_CP537_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP537_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP537_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP537_PACK_BINDING.range,
    open_scope: "Continue RP17.P06.M04.S24 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP537_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp538Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP538_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp538CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp538Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp538Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-538 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-538");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP538_PACK_BINDING.risk_class) errors.push("CP00-538 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP538_PACK_BINDING.unit_count) errors.push("CP00-538 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP538_PACK_BINDING.first_unit_id) errors.push("CP00-538 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP538_PACK_BINDING.last_unit_id) errors.push("CP00-538 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-538 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-538 must only include RP17 units");
  const summary = createAiGovernanceCoreCp538CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP538_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-538 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP538_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-538 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP538_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-538 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP538_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-538 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP538_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-538 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-538 ${microId} missing row ${title}`);
    }
  }
  return freezeCp538Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp538P06WorkflowPermissionSliceDescriptor(
  descriptor = createAiGovernanceCoreCp538P06WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_workflow_permission_slice_case_set ?? createAiGovernanceCoreCp538P06WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp538P06WorkflowPermissionSliceDescriptor") errors.push("CP00-538 descriptor type drift");
  if (descriptor.source_p06_implementation_workflow_slice_descriptor !== "AiGovernanceCoreCp537P06ImplementationWorkflowSliceDescriptor") {
    errors.push("CP00-538 source p06 implementation workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP538_REQUIREMENTS.required_section_rows).length) errors.push("CP00-538 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP538_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-538 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-538 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-538 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-538 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-538 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-538 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-538 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP538_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-538 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP538_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-538 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-538 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-538 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-538 contract current_pack drift");
  }
  if (
    contractProjection?.p06_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p06_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-538 contract p06_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP538_PACK_BINDING.next_pack_id) errors.push("CP00-538 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP538_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-538 next subphase drift");
  }
  return freezeCp538Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp538HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp538P06WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp538Coverage(planPack);
  const slice = validateAiGovernanceCoreCp538P06WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp538Validation({
    evidence_packet: "H17.CP00-538.ai_governance_core_p06_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP538_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_implementation_workflow_slice_pack_id: AI_GOVERNANCE_CORE_CP538_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP538_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP538_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP538_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp538ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp538Coverage(planPack);
  const slice = validateAiGovernanceCoreCp538P06WorkflowPermissionSliceDescriptor(createAiGovernanceCoreCp538P06WorkflowPermissionSliceDescriptor(), {});
  return freezeCp538Validation({
    review_packet: "C17.CP00-538.ai_governance_core_p06_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP538_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP538_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP538_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-538 cover exactly the 40 planned RP17 units from RP17.P06.M04.S24 through RP17.P06.M06.S13?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-538 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp538CloseoutHandoff() {
  return freezeCp538Validation({
    handoff_id: "CP00-538-to-CP00-539",
    from_pack_id: AI_GOVERNANCE_CORE_CP538_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP538_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP538_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP538_PACK_BINDING.range,
    open_scope: "Continue RP17.P06.M06.S14 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP538_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp539Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP539_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp539CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp539Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp539Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-539 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-539");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP539_PACK_BINDING.risk_class) errors.push("CP00-539 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP539_PACK_BINDING.unit_count) errors.push("CP00-539 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP539_PACK_BINDING.first_unit_id) errors.push("CP00-539 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP539_PACK_BINDING.last_unit_id) errors.push("CP00-539 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-539 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-539 must only include RP17 units");
  const summary = createAiGovernanceCoreCp539CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP539_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-539 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP539_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-539 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP539_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-539 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP539_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-539 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP539_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-539 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-539 ${microId} missing row ${title}`);
    }
  }
  return freezeCp539Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp539P06CloseoutP07FoundationDescriptor(
  descriptor = createAiGovernanceCoreCp539P06CloseoutP07FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_closeout_p07_foundation_case_set ?? createAiGovernanceCoreCp539P06CloseoutP07FoundationCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp539P06CloseoutP07FoundationDescriptor") errors.push("CP00-539 descriptor type drift");
  if (descriptor.source_p06_workflow_permission_slice_descriptor !== "AiGovernanceCoreCp538P06WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-539 source p06 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP539_REQUIREMENTS.required_section_rows).length) errors.push("CP00-539 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP539_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-539 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-539 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-539 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-539 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-539 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-539 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-539 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP539_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-539 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP539_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-539 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-539 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-539 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-539 contract current_pack drift");
  }
  if (
    contractProjection?.p06_closeout_p07_foundation_descriptor?.descriptor &&
    contractProjection.p06_closeout_p07_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-539 contract p06_closeout_p07_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP539_PACK_BINDING.next_pack_id) errors.push("CP00-539 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP539_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-539 next subphase drift");
  }
  return freezeCp539Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp539HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp539P06CloseoutP07FoundationDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp539Coverage(planPack);
  const slice = validateAiGovernanceCoreCp539P06CloseoutP07FoundationDescriptor(descriptor, contractProjection);
  return freezeCp539Validation({
    evidence_packet: "H17.CP00-539.ai_governance_core_p06_closeout_p07_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP539_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_closeout_p07_foundation_valid: slice.valid,
    no_real_data: true,
    source_p06_workflow_permission_slice_pack_id: AI_GOVERNANCE_CORE_CP539_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP539_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP539_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP539_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp539ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp539Coverage(planPack);
  const slice = validateAiGovernanceCoreCp539P06CloseoutP07FoundationDescriptor(createAiGovernanceCoreCp539P06CloseoutP07FoundationDescriptor(), {});
  return freezeCp539Validation({
    review_packet: "C17.CP00-539.ai_governance_core_p06_closeout_p07_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP539_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP539_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_closeout_p07_foundation_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP539_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-539 cover exactly the 150 planned RP17 units from RP17.P06.M06.S14 through RP17.P07.M02.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-539 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp539CloseoutHandoff() {
  return freezeCp539Validation({
    handoff_id: "CP00-539-to-CP00-540",
    from_pack_id: AI_GOVERNANCE_CORE_CP539_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP539_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP539_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP539_PACK_BINDING.range,
    open_scope: "Continue RP17.P07.M02.S13 onward with the remaining type and shape rows and the primary implementation micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP539_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp540Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP540_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp540CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp540Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp540Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-540 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-540");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP540_PACK_BINDING.risk_class) errors.push("CP00-540 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP540_PACK_BINDING.unit_count) errors.push("CP00-540 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP540_PACK_BINDING.first_unit_id) errors.push("CP00-540 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP540_PACK_BINDING.last_unit_id) errors.push("CP00-540 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-540 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-540 must only include RP17 units");
  const summary = createAiGovernanceCoreCp540CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP540_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-540 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP540_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-540 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP540_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-540 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP540_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-540 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP540_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-540 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-540 ${microId} missing row ${title}`);
    }
  }
  return freezeCp540Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp540P07ImplementationSliceDescriptor(
  descriptor = createAiGovernanceCoreCp540P07ImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_implementation_slice_case_set ?? createAiGovernanceCoreCp540P07ImplementationSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp540P07ImplementationSliceDescriptor") errors.push("CP00-540 descriptor type drift");
  if (descriptor.source_p06_closeout_p07_foundation_descriptor !== "AiGovernanceCoreCp539P06CloseoutP07FoundationDescriptor") {
    errors.push("CP00-540 source p06 closeout p07 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP540_REQUIREMENTS.required_section_rows).length) errors.push("CP00-540 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP540_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-540 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-540 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-540 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-540 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-540 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-540 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-540 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP540_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-540 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP540_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-540 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-540 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-540 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-540 contract current_pack drift");
  }
  if (
    contractProjection?.p07_implementation_slice_descriptor?.descriptor &&
    contractProjection.p07_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-540 contract p07_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP540_PACK_BINDING.next_pack_id) errors.push("CP00-540 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP540_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-540 next subphase drift");
  }
  return freezeCp540Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp540HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp540P07ImplementationSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp540Coverage(planPack);
  const slice = validateAiGovernanceCoreCp540P07ImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp540Validation({
    evidence_packet: "H17.CP00-540.ai_governance_core_p07_implementation_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP540_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_closeout_p07_foundation_pack_id: AI_GOVERNANCE_CORE_CP540_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP540_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP540_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP540_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp540ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp540Coverage(planPack);
  const slice = validateAiGovernanceCoreCp540P07ImplementationSliceDescriptor(createAiGovernanceCoreCp540P07ImplementationSliceDescriptor(), {});
  return freezeCp540Validation({
    review_packet: "C17.CP00-540.ai_governance_core_p07_implementation_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP540_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP540_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_implementation_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP540_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-540 cover exactly the 40 planned RP17 units from RP17.P07.M02.S13 through RP17.P07.M04.S05?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-540 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp540CloseoutHandoff() {
  return freezeCp540Validation({
    handoff_id: "CP00-540-to-CP00-541",
    from_pack_id: AI_GOVERNANCE_CORE_CP540_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP540_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP540_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP540_PACK_BINDING.range,
    open_scope: "Continue RP17.P07.M04.S06 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP540_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp541Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP541_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp541CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp541Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp541Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-541 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-541");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP541_PACK_BINDING.risk_class) errors.push("CP00-541 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP541_PACK_BINDING.unit_count) errors.push("CP00-541 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP541_PACK_BINDING.first_unit_id) errors.push("CP00-541 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP541_PACK_BINDING.last_unit_id) errors.push("CP00-541 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-541 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-541 must only include RP17 units");
  const summary = createAiGovernanceCoreCp541CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP541_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-541 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP541_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-541 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP541_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-541 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP541_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-541 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP541_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-541 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-541 ${microId} missing row ${title}`);
    }
  }
  return freezeCp541Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp541P07WorkflowPermissionSliceDescriptor(
  descriptor = createAiGovernanceCoreCp541P07WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_workflow_permission_slice_case_set ?? createAiGovernanceCoreCp541P07WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp541P07WorkflowPermissionSliceDescriptor") errors.push("CP00-541 descriptor type drift");
  if (descriptor.source_p07_implementation_slice_descriptor !== "AiGovernanceCoreCp540P07ImplementationSliceDescriptor") {
    errors.push("CP00-541 source p07 implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP541_REQUIREMENTS.required_section_rows).length) errors.push("CP00-541 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP541_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-541 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-541 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-541 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-541 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-541 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-541 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-541 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP541_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-541 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP541_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-541 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-541 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-541 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-541 contract current_pack drift");
  }
  if (
    contractProjection?.p07_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p07_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-541 contract p07_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP541_PACK_BINDING.next_pack_id) errors.push("CP00-541 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP541_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-541 next subphase drift");
  }
  return freezeCp541Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp541HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp541P07WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp541Coverage(planPack);
  const slice = validateAiGovernanceCoreCp541P07WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp541Validation({
    evidence_packet: "H17.CP00-541.ai_governance_core_p07_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP541_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_implementation_slice_pack_id: AI_GOVERNANCE_CORE_CP541_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP541_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP541_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP541_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp541ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp541Coverage(planPack);
  const slice = validateAiGovernanceCoreCp541P07WorkflowPermissionSliceDescriptor(createAiGovernanceCoreCp541P07WorkflowPermissionSliceDescriptor(), {});
  return freezeCp541Validation({
    review_packet: "C17.CP00-541.ai_governance_core_p07_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP541_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP541_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP541_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-541 cover exactly the 40 planned RP17 units from RP17.P07.M04.S06 through RP17.P07.M05.S20?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-541 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp541CloseoutHandoff() {
  return freezeCp541Validation({
    handoff_id: "CP00-541-to-CP00-542",
    from_pack_id: AI_GOVERNANCE_CORE_CP541_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP541_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP541_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP541_PACK_BINDING.range,
    open_scope: "Continue RP17.P07.M05.S21 onward with the remaining permission and audit binding rows and the synthetic fixture micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP541_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp542Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP542_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp542CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp542Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp542Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-542 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-542");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP542_PACK_BINDING.risk_class) errors.push("CP00-542 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP542_PACK_BINDING.unit_count) errors.push("CP00-542 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP542_PACK_BINDING.first_unit_id) errors.push("CP00-542 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP542_PACK_BINDING.last_unit_id) errors.push("CP00-542 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-542 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-542 must only include RP17 units");
  const summary = createAiGovernanceCoreCp542CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP542_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-542 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP542_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-542 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP542_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-542 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP542_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-542 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP542_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-542 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-542 ${microId} missing row ${title}`);
    }
  }
  return freezeCp542Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp542P07PermissionFixtureSliceDescriptor(
  descriptor = createAiGovernanceCoreCp542P07PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_permission_fixture_slice_case_set ?? createAiGovernanceCoreCp542P07PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp542P07PermissionFixtureSliceDescriptor") errors.push("CP00-542 descriptor type drift");
  if (descriptor.source_p07_workflow_permission_slice_descriptor !== "AiGovernanceCoreCp541P07WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-542 source p07 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP542_REQUIREMENTS.required_section_rows).length) errors.push("CP00-542 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP542_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-542 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-542 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-542 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-542 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-542 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-542 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-542 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP542_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-542 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP542_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-542 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-542 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-542 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-542 contract current_pack drift");
  }
  if (
    contractProjection?.p07_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p07_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-542 contract p07_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP542_PACK_BINDING.next_pack_id) errors.push("CP00-542 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP542_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-542 next subphase drift");
  }
  return freezeCp542Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp542HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp542P07PermissionFixtureSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp542Coverage(planPack);
  const slice = validateAiGovernanceCoreCp542P07PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp542Validation({
    evidence_packet: "H17.CP00-542.ai_governance_core_p07_permission_fixture_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP542_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_workflow_permission_slice_pack_id: AI_GOVERNANCE_CORE_CP542_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP542_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP542_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP542_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp542ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp542Coverage(planPack);
  const slice = validateAiGovernanceCoreCp542P07PermissionFixtureSliceDescriptor(createAiGovernanceCoreCp542P07PermissionFixtureSliceDescriptor(), {});
  return freezeCp542Validation({
    review_packet: "C17.CP00-542.ai_governance_core_p07_permission_fixture_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP542_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP542_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP542_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-542 cover exactly the 10 planned RP17 units from RP17.P07.M05.S21 through RP17.P07.M06.S05?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-542 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp542CloseoutHandoff() {
  return freezeCp542Validation({
    handoff_id: "CP00-542-to-CP00-543",
    from_pack_id: AI_GOVERNANCE_CORE_CP542_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP542_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP542_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP542_PACK_BINDING.range,
    open_scope: "Continue RP17.P07.M06.S06 onward with the remaining synthetic fixture rows while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP542_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp543Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP543_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp543CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp543Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp543Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-543 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-543");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP543_PACK_BINDING.risk_class) errors.push("CP00-543 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP543_PACK_BINDING.unit_count) errors.push("CP00-543 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP543_PACK_BINDING.first_unit_id) errors.push("CP00-543 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP543_PACK_BINDING.last_unit_id) errors.push("CP00-543 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-543 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-543 must only include RP17 units");
  const summary = createAiGovernanceCoreCp543CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP543_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-543 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP543_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-543 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP543_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-543 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP543_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-543 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP543_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-543 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-543 ${microId} missing row ${title}`);
    }
  }
  return freezeCp543Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp543P07CloseoutP08FoundationDescriptor(
  descriptor = createAiGovernanceCoreCp543P07CloseoutP08FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_closeout_p08_foundation_case_set ?? createAiGovernanceCoreCp543P07CloseoutP08FoundationCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp543P07CloseoutP08FoundationDescriptor") errors.push("CP00-543 descriptor type drift");
  if (descriptor.source_p07_permission_fixture_slice_descriptor !== "AiGovernanceCoreCp542P07PermissionFixtureSliceDescriptor") {
    errors.push("CP00-543 source p07 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP543_REQUIREMENTS.required_section_rows).length) errors.push("CP00-543 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP543_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-543 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-543 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-543 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-543 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-543 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-543 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-543 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP543_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-543 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP543_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-543 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-543 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-543 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-543 contract current_pack drift");
  }
  if (
    contractProjection?.p07_closeout_p08_foundation_descriptor?.descriptor &&
    contractProjection.p07_closeout_p08_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-543 contract p07_closeout_p08_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP543_PACK_BINDING.next_pack_id) errors.push("CP00-543 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP543_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-543 next subphase drift");
  }
  return freezeCp543Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp543HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp543P07CloseoutP08FoundationDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp543Coverage(planPack);
  const slice = validateAiGovernanceCoreCp543P07CloseoutP08FoundationDescriptor(descriptor, contractProjection);
  return freezeCp543Validation({
    evidence_packet: "H17.CP00-543.ai_governance_core_p07_closeout_p08_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP543_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_closeout_p08_foundation_valid: slice.valid,
    no_real_data: true,
    source_p07_permission_fixture_slice_pack_id: AI_GOVERNANCE_CORE_CP543_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP543_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP543_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP543_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp543ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp543Coverage(planPack);
  const slice = validateAiGovernanceCoreCp543P07CloseoutP08FoundationDescriptor(createAiGovernanceCoreCp543P07CloseoutP08FoundationDescriptor(), {});
  return freezeCp543Validation({
    review_packet: "C17.CP00-543.ai_governance_core_p07_closeout_p08_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP543_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP543_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_closeout_p08_foundation_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP543_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-543 cover exactly the 150 planned RP17 units from RP17.P07.M06.S06 through RP17.P08.M02.S14?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-543 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp543CloseoutHandoff() {
  return freezeCp543Validation({
    handoff_id: "CP00-543-to-CP00-544",
    from_pack_id: AI_GOVERNANCE_CORE_CP543_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP543_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP543_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP543_PACK_BINDING.range,
    open_scope: "Continue RP17.P08.M02.S15 onward with the remaining type and shape rows and the primary implementation micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP543_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp544Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP544_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp544CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp544Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp544Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-544 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-544");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP544_PACK_BINDING.risk_class) errors.push("CP00-544 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP544_PACK_BINDING.unit_count) errors.push("CP00-544 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP544_PACK_BINDING.first_unit_id) errors.push("CP00-544 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP544_PACK_BINDING.last_unit_id) errors.push("CP00-544 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-544 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-544 must only include RP17 units");
  const summary = createAiGovernanceCoreCp544CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP544_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-544 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP544_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-544 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP544_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-544 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP544_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-544 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP544_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-544 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-544 ${microId} missing row ${title}`);
    }
  }
  return freezeCp544Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp544P08ImplementationWorkflowSliceDescriptor(
  descriptor = createAiGovernanceCoreCp544P08ImplementationWorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_implementation_workflow_slice_case_set ?? createAiGovernanceCoreCp544P08ImplementationWorkflowSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp544P08ImplementationWorkflowSliceDescriptor") errors.push("CP00-544 descriptor type drift");
  if (descriptor.source_p07_closeout_p08_foundation_descriptor !== "AiGovernanceCoreCp543P07CloseoutP08FoundationDescriptor") {
    errors.push("CP00-544 source p07 closeout p08 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP544_REQUIREMENTS.required_section_rows).length) errors.push("CP00-544 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP544_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-544 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-544 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-544 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-544 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-544 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-544 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-544 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP544_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-544 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP544_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-544 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-544 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-544 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-544 contract current_pack drift");
  }
  if (
    contractProjection?.p08_implementation_workflow_slice_descriptor?.descriptor &&
    contractProjection.p08_implementation_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-544 contract p08_implementation_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP544_PACK_BINDING.next_pack_id) errors.push("CP00-544 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP544_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-544 next subphase drift");
  }
  return freezeCp544Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp544HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp544P08ImplementationWorkflowSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp544Coverage(planPack);
  const slice = validateAiGovernanceCoreCp544P08ImplementationWorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp544Validation({
    evidence_packet: "H17.CP00-544.ai_governance_core_p08_implementation_workflow_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP544_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_implementation_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_closeout_p08_foundation_pack_id: AI_GOVERNANCE_CORE_CP544_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP544_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP544_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP544_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp544ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp544Coverage(planPack);
  const slice = validateAiGovernanceCoreCp544P08ImplementationWorkflowSliceDescriptor(createAiGovernanceCoreCp544P08ImplementationWorkflowSliceDescriptor(), {});
  return freezeCp544Validation({
    review_packet: "C17.CP00-544.ai_governance_core_p08_implementation_workflow_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP544_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP544_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_implementation_workflow_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP544_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-544 cover exactly the 40 planned RP17 units from RP17.P08.M02.S15 through RP17.P08.M04.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-544 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp544CloseoutHandoff() {
  return freezeCp544Validation({
    handoff_id: "CP00-544-to-CP00-545",
    from_pack_id: AI_GOVERNANCE_CORE_CP544_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP544_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP544_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP544_PACK_BINDING.range,
    open_scope: "Continue RP17.P08.M04.S13 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP544_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp545Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP545_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp545CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp545Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp545Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-545 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-545");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP545_PACK_BINDING.risk_class) errors.push("CP00-545 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP545_PACK_BINDING.unit_count) errors.push("CP00-545 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP545_PACK_BINDING.first_unit_id) errors.push("CP00-545 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP545_PACK_BINDING.last_unit_id) errors.push("CP00-545 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-545 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-545 must only include RP17 units");
  const summary = createAiGovernanceCoreCp545CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP545_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-545 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP545_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-545 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP545_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-545 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP545_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-545 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP545_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-545 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-545 ${microId} missing row ${title}`);
    }
  }
  return freezeCp545Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp545P08WorkflowPermissionSliceDescriptor(
  descriptor = createAiGovernanceCoreCp545P08WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_workflow_permission_slice_case_set ?? createAiGovernanceCoreCp545P08WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp545P08WorkflowPermissionSliceDescriptor") errors.push("CP00-545 descriptor type drift");
  if (descriptor.source_p08_implementation_workflow_slice_descriptor !== "AiGovernanceCoreCp544P08ImplementationWorkflowSliceDescriptor") {
    errors.push("CP00-545 source p08 implementation workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP545_REQUIREMENTS.required_section_rows).length) errors.push("CP00-545 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP545_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-545 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-545 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-545 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-545 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-545 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-545 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-545 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP545_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-545 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP545_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-545 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-545 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-545 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-545 contract current_pack drift");
  }
  if (
    contractProjection?.p08_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p08_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-545 contract p08_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP545_PACK_BINDING.next_pack_id) errors.push("CP00-545 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP545_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-545 next subphase drift");
  }
  return freezeCp545Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp545HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp545P08WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp545Coverage(planPack);
  const slice = validateAiGovernanceCoreCp545P08WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp545Validation({
    evidence_packet: "H17.CP00-545.ai_governance_core_p08_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP545_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p08_implementation_workflow_slice_pack_id: AI_GOVERNANCE_CORE_CP545_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP545_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP545_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP545_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp545ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp545Coverage(planPack);
  const slice = validateAiGovernanceCoreCp545P08WorkflowPermissionSliceDescriptor(createAiGovernanceCoreCp545P08WorkflowPermissionSliceDescriptor(), {});
  return freezeCp545Validation({
    review_packet: "C17.CP00-545.ai_governance_core_p08_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP545_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP545_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP545_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-545 cover exactly the 40 planned RP17 units from RP17.P08.M04.S13 through RP17.P08.M06.S08?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-545 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp545CloseoutHandoff() {
  return freezeCp545Validation({
    handoff_id: "CP00-545-to-CP00-546",
    from_pack_id: AI_GOVERNANCE_CORE_CP545_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP545_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP545_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP545_PACK_BINDING.range,
    open_scope: "Continue RP17.P08.M06.S09 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP545_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp546Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP546_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp546CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp546Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp546Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-546 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-546");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP546_PACK_BINDING.risk_class) errors.push("CP00-546 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP546_PACK_BINDING.unit_count) errors.push("CP00-546 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP546_PACK_BINDING.first_unit_id) errors.push("CP00-546 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP546_PACK_BINDING.last_unit_id) errors.push("CP00-546 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-546 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-546 must only include RP17 units");
  const summary = createAiGovernanceCoreCp546CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP546_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-546 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP546_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-546 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP546_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-546 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP546_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-546 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP546_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-546 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-546 ${microId} missing row ${title}`);
    }
  }
  return freezeCp546Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp546P08CloseoutP09FoundationDescriptor(
  descriptor = createAiGovernanceCoreCp546P08CloseoutP09FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_closeout_p09_foundation_case_set ?? createAiGovernanceCoreCp546P08CloseoutP09FoundationCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp546P08CloseoutP09FoundationDescriptor") errors.push("CP00-546 descriptor type drift");
  if (descriptor.source_p08_workflow_permission_slice_descriptor !== "AiGovernanceCoreCp545P08WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-546 source p08 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP546_REQUIREMENTS.required_section_rows).length) errors.push("CP00-546 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP546_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-546 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-546 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-546 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-546 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-546 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-546 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-546 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP546_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-546 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP546_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-546 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-546 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-546 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-546 contract current_pack drift");
  }
  if (
    contractProjection?.p08_closeout_p09_foundation_descriptor?.descriptor &&
    contractProjection.p08_closeout_p09_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-546 contract p08_closeout_p09_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP546_PACK_BINDING.next_pack_id) errors.push("CP00-546 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP546_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-546 next subphase drift");
  }
  return freezeCp546Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp546HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp546P08CloseoutP09FoundationDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp546Coverage(planPack);
  const slice = validateAiGovernanceCoreCp546P08CloseoutP09FoundationDescriptor(descriptor, contractProjection);
  return freezeCp546Validation({
    evidence_packet: "H17.CP00-546.ai_governance_core_p08_closeout_p09_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP546_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_closeout_p09_foundation_valid: slice.valid,
    no_real_data: true,
    source_p08_workflow_permission_slice_pack_id: AI_GOVERNANCE_CORE_CP546_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP546_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP546_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP546_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp546ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp546Coverage(planPack);
  const slice = validateAiGovernanceCoreCp546P08CloseoutP09FoundationDescriptor(createAiGovernanceCoreCp546P08CloseoutP09FoundationDescriptor(), {});
  return freezeCp546Validation({
    review_packet: "C17.CP00-546.ai_governance_core_p08_closeout_p09_foundation_descriptor",
    gate: AI_GOVERNANCE_CORE_CP546_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP546_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_closeout_p09_foundation_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP546_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-546 cover exactly the 150 planned RP17 units from RP17.P08.M06.S09 through RP17.P09.M03.S10?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-546 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp546CloseoutHandoff() {
  return freezeCp546Validation({
    handoff_id: "CP00-546-to-CP00-547",
    from_pack_id: AI_GOVERNANCE_CORE_CP546_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP546_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP546_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP546_PACK_BINDING.range,
    open_scope: "Continue RP17.P09.M03.S11 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP546_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp547Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP547_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp547CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp547Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp547Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-547 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-547");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP547_PACK_BINDING.risk_class) errors.push("CP00-547 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP547_PACK_BINDING.unit_count) errors.push("CP00-547 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP547_PACK_BINDING.first_unit_id) errors.push("CP00-547 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP547_PACK_BINDING.last_unit_id) errors.push("CP00-547 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-547 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-547 must only include RP17 units");
  const summary = createAiGovernanceCoreCp547CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP547_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-547 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP547_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-547 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP547_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-547 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP547_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-547 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP547_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-547 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-547 ${microId} missing row ${title}`);
    }
  }
  return freezeCp547Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp547P09WorkflowPermissionSliceDescriptor(
  descriptor = createAiGovernanceCoreCp547P09WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_workflow_permission_slice_case_set ?? createAiGovernanceCoreCp547P09WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp547P09WorkflowPermissionSliceDescriptor") errors.push("CP00-547 descriptor type drift");
  if (descriptor.source_p08_closeout_p09_foundation_descriptor !== "AiGovernanceCoreCp546P08CloseoutP09FoundationDescriptor") {
    errors.push("CP00-547 source p08 closeout p09 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP547_REQUIREMENTS.required_section_rows).length) errors.push("CP00-547 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP547_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-547 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-547 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-547 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-547 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-547 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-547 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-547 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP547_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-547 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP547_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-547 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-547 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-547 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-547 contract current_pack drift");
  }
  if (
    contractProjection?.p09_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p09_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-547 contract p09_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP547_PACK_BINDING.next_pack_id) errors.push("CP00-547 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP547_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-547 next subphase drift");
  }
  return freezeCp547Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp547HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp547P09WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp547Coverage(planPack);
  const slice = validateAiGovernanceCoreCp547P09WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp547Validation({
    evidence_packet: "H17.CP00-547.ai_governance_core_p09_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP547_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p08_closeout_p09_foundation_pack_id: AI_GOVERNANCE_CORE_CP547_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP547_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP547_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP547_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp547ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp547Coverage(planPack);
  const slice = validateAiGovernanceCoreCp547P09WorkflowPermissionSliceDescriptor(createAiGovernanceCoreCp547P09WorkflowPermissionSliceDescriptor(), {});
  return freezeCp547Validation({
    review_packet: "C17.CP00-547.ai_governance_core_p09_workflow_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP547_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP547_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP547_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-547 cover exactly the 40 planned RP17 units from RP17.P09.M03.S11 through RP17.P09.M05.S08?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-547 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp547CloseoutHandoff() {
  return freezeCp547Validation({
    handoff_id: "CP00-547-to-CP00-548",
    from_pack_id: AI_GOVERNANCE_CORE_CP547_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP547_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP547_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP547_PACK_BINDING.range,
    open_scope: "Continue RP17.P09.M05.S09 onward with the remaining permission and audit binding rows while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP547_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp548Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP548_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp548CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp548Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp548Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-548 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-548");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP548_PACK_BINDING.risk_class) errors.push("CP00-548 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP548_PACK_BINDING.unit_count) errors.push("CP00-548 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP548_PACK_BINDING.first_unit_id) errors.push("CP00-548 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP548_PACK_BINDING.last_unit_id) errors.push("CP00-548 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-548 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-548 must only include RP17 units");
  const summary = createAiGovernanceCoreCp548CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP548_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-548 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP548_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-548 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP548_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-548 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP548_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-548 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP548_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-548 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-548 ${microId} missing row ${title}`);
    }
  }
  return freezeCp548Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp548P09PermissionSliceDescriptor(
  descriptor = createAiGovernanceCoreCp548P09PermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_permission_slice_case_set ?? createAiGovernanceCoreCp548P09PermissionSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp548P09PermissionSliceDescriptor") errors.push("CP00-548 descriptor type drift");
  if (descriptor.source_p09_workflow_permission_slice_descriptor !== "AiGovernanceCoreCp547P09WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-548 source p09 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP548_REQUIREMENTS.required_section_rows).length) errors.push("CP00-548 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP548_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-548 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-548 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-548 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-548 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-548 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-548 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-548 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP548_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-548 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP548_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-548 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-548 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-548 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-548 contract current_pack drift");
  }
  if (
    contractProjection?.p09_permission_slice_descriptor?.descriptor &&
    contractProjection.p09_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-548 contract p09_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP548_PACK_BINDING.next_pack_id) errors.push("CP00-548 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP548_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-548 next subphase drift");
  }
  return freezeCp548Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp548HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp548P09PermissionSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp548Coverage(planPack);
  const slice = validateAiGovernanceCoreCp548P09PermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp548Validation({
    evidence_packet: "H17.CP00-548.ai_governance_core_p09_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP548_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_workflow_permission_slice_pack_id: AI_GOVERNANCE_CORE_CP548_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP548_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP548_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP548_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp548ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp548Coverage(planPack);
  const slice = validateAiGovernanceCoreCp548P09PermissionSliceDescriptor(createAiGovernanceCoreCp548P09PermissionSliceDescriptor(), {});
  return freezeCp548Validation({
    review_packet: "C17.CP00-548.ai_governance_core_p09_permission_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP548_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP548_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_permission_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP548_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-548 cover exactly the 10 planned RP17 units from RP17.P09.M05.S09 through RP17.P09.M05.S18?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-548 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp548CloseoutHandoff() {
  return freezeCp548Validation({
    handoff_id: "CP00-548-to-CP00-549",
    from_pack_id: AI_GOVERNANCE_CORE_CP548_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP548_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP548_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP548_PACK_BINDING.range,
    open_scope: "Continue RP17.P09.M05.S19 onward with the remaining permission and audit binding rows and the synthetic fixture micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP548_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp549Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP549_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp549CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp549Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp549Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-549 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-549");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP549_PACK_BINDING.risk_class) errors.push("CP00-549 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP549_PACK_BINDING.unit_count) errors.push("CP00-549 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP549_PACK_BINDING.first_unit_id) errors.push("CP00-549 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP549_PACK_BINDING.last_unit_id) errors.push("CP00-549 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-549 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-549 must only include RP17 units");
  const summary = createAiGovernanceCoreCp549CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP549_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-549 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP549_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-549 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP549_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-549 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP549_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-549 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP549_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-549 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-549 ${microId} missing row ${title}`);
    }
  }
  return freezeCp549Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp549P09PermissionFixtureSliceDescriptor(
  descriptor = createAiGovernanceCoreCp549P09PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_permission_fixture_slice_case_set ?? createAiGovernanceCoreCp549P09PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp549P09PermissionFixtureSliceDescriptor") errors.push("CP00-549 descriptor type drift");
  if (descriptor.source_p09_permission_slice_descriptor !== "AiGovernanceCoreCp548P09PermissionSliceDescriptor") {
    errors.push("CP00-549 source p09 permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP549_REQUIREMENTS.required_section_rows).length) errors.push("CP00-549 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP549_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-549 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-549 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-549 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-549 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-549 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-549 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-549 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP549_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-549 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP549_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-549 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-549 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-549 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id, AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-549 contract current_pack drift");
  }
  if (
    contractProjection?.p09_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p09_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-549 contract p09_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP549_PACK_BINDING.next_pack_id) errors.push("CP00-549 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP549_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-549 next subphase drift");
  }
  return freezeCp549Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp549HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp549P09PermissionFixtureSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp549Coverage(planPack);
  const slice = validateAiGovernanceCoreCp549P09PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp549Validation({
    evidence_packet: "H17.CP00-549.ai_governance_core_p09_permission_fixture_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP549_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_permission_slice_pack_id: AI_GOVERNANCE_CORE_CP549_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP549_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP549_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP549_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp549ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp549Coverage(planPack);
  const slice = validateAiGovernanceCoreCp549P09PermissionFixtureSliceDescriptor(createAiGovernanceCoreCp549P09PermissionFixtureSliceDescriptor(), {});
  return freezeCp549Validation({
    review_packet: "C17.CP00-549.ai_governance_core_p09_permission_fixture_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP549_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP549_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP549_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-549 cover exactly the 10 planned RP17 units from RP17.P09.M05.S19 through RP17.P09.M06.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-549 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp549CloseoutHandoff() {
  return freezeCp549Validation({
    handoff_id: "CP00-549-to-CP00-550",
    from_pack_id: AI_GOVERNANCE_CORE_CP549_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP549_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP549_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP549_PACK_BINDING.range,
    open_scope: "Continue RP17.P09.M06.S07 onward with the remaining synthetic fixture rows and the closeout micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP549_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp550Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id,
    no_write_attestation: AI_GOVERNANCE_CORE_CP550_NO_WRITE_ATTESTATION,
  });
}

export function createAiGovernanceCoreCp550CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp550Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateAiGovernanceCoreCp550Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-550 plan pack is required");
  if (planPack?.pack_id !== AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-550");
  if (planPack?.risk_class !== AI_GOVERNANCE_CORE_CP550_PACK_BINDING.risk_class) errors.push("CP00-550 risk class drift");
  if (planPack?.unit_count !== AI_GOVERNANCE_CORE_CP550_PACK_BINDING.unit_count) errors.push("CP00-550 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== AI_GOVERNANCE_CORE_CP550_PACK_BINDING.first_unit_id) errors.push("CP00-550 first unit drift");
  if (unitIds.at(-1) !== AI_GOVERNANCE_CORE_CP550_PACK_BINDING.last_unit_id) errors.push("CP00-550 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-550 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP17")) errors.push("CP00-550 must only include RP17 units");
  const summary = createAiGovernanceCoreCp550CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(AI_GOVERNANCE_CORE_CP550_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-550 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(AI_GOVERNANCE_CORE_CP550_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-550 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(AI_GOVERNANCE_CORE_CP550_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-550 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(AI_GOVERNANCE_CORE_CP550_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-550 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP550_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-550 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-550 ${microId} missing row ${title}`);
    }
  }
  return freezeCp550Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateAiGovernanceCoreCp550P09CloseoutHandoffSliceDescriptor(
  descriptor = createAiGovernanceCoreCp550P09CloseoutHandoffSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_closeout_handoff_slice_case_set ?? createAiGovernanceCoreCp550P09CloseoutHandoffSliceCaseSet();
  if (descriptor.descriptor !== "AiGovernanceCoreCp550P09CloseoutHandoffSliceDescriptor") errors.push("CP00-550 descriptor type drift");
  if (descriptor.source_p09_permission_fixture_slice_descriptor !== "AiGovernanceCoreCp549P09PermissionFixtureSliceDescriptor") {
    errors.push("CP00-550 source p09 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(AI_GOVERNANCE_CORE_CP550_REQUIREMENTS.required_section_rows).length) errors.push("CP00-550 section count drift");
  for (const [microId, titles] of Object.entries(AI_GOVERNANCE_CORE_CP550_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-550 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-550 ${microId} row count drift`);
    for (const title of titles) {
      const key = aiGovernanceCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-550 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-550 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-550 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-550 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-550 ${microId} ${key} must not include raw payload`);
    }
  }
  if (AI_GOVERNANCE_CORE_CP550_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-550 must not promote Claude");
  if (AI_GOVERNANCE_CORE_CP550_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-550 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-550 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-550 local validation trust boundary drift");
  }
  if (contractProjection?.current_pack?.pack_id && contractProjection.current_pack.pack_id !== AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id) {
    errors.push("CP00-550 contract current_pack drift");
  }
  if (
    contractProjection?.p09_closeout_handoff_slice_descriptor?.descriptor &&
    contractProjection.p09_closeout_handoff_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-550 contract p09_closeout_handoff_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== AI_GOVERNANCE_CORE_CP550_PACK_BINDING.next_pack_id) errors.push("CP00-550 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== AI_GOVERNANCE_CORE_CP550_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-550 next subphase drift");
  }
  return freezeCp550Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createAiGovernanceCoreCp550HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createAiGovernanceCoreCp550P09CloseoutHandoffSliceDescriptor(),
) {
  const coverage = validateAiGovernanceCoreCp550Coverage(planPack);
  const slice = validateAiGovernanceCoreCp550P09CloseoutHandoffSliceDescriptor(descriptor, contractProjection);
  return freezeCp550Validation({
    evidence_packet: "H17.CP00-550.ai_governance_core_p09_closeout_handoff_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP550_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_closeout_handoff_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_permission_fixture_slice_pack_id: AI_GOVERNANCE_CORE_CP550_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: AI_GOVERNANCE_CORE_CP550_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: AI_GOVERNANCE_CORE_CP550_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP550_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createAiGovernanceCoreCp550ClaudeReviewPacket(planPack) {
  const coverage = validateAiGovernanceCoreCp550Coverage(planPack);
  const slice = validateAiGovernanceCoreCp550P09CloseoutHandoffSliceDescriptor(createAiGovernanceCoreCp550P09CloseoutHandoffSliceDescriptor(), {});
  return freezeCp550Validation({
    review_packet: "C17.CP00-550.ai_governance_core_p09_closeout_handoff_slice_descriptor",
    gate: AI_GOVERNANCE_CORE_CP550_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: AI_GOVERNANCE_CORE_CP550_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_closeout_handoff_slice_valid: slice.valid,
    invalid_review_blockers: AI_GOVERNANCE_CORE_CP550_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-550 cover exactly the 86 planned RP17 units from RP17.P09.M06.S07 through RP17.P09.M10.S10?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared AI governance guards applied?",
      "Does CP00-550 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createAiGovernanceCoreCp550CloseoutHandoff() {
  return freezeCp550Validation({
    handoff_id: "CP00-550-to-CP00-551",
    from_pack_id: AI_GOVERNANCE_CORE_CP550_PACK_BINDING.pack_id,
    to_pack_id: AI_GOVERNANCE_CORE_CP550_PACK_BINDING.next_pack_id,
    next_subphase_id: AI_GOVERNANCE_CORE_CP550_PACK_BINDING.next_subphase_id,
    closed_scope: AI_GOVERNANCE_CORE_CP550_PACK_BINDING.range,
    open_scope: "Continue RP18.P00.M00.S01 onward with the RP18 AI Legal Workflows scope inventory micros while preserving descriptor-only AI governance boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: AI_GOVERNANCE_CORE_CP550_PACK_BINDING.production_ready_flag,
  });
}
