# @law-firm-os/ai-governance

RP17 "AI Governance" descriptor-only program package (Model policy, retrieval scope, audit, citation). Gates H17/C17, upstream RP16. Every pack in this package is descriptor-only: no AI governance, model policy, retrieval scope, or citation runtime, no runtime permission or audit evaluation, no state writes, no object storage, no Hermes runtime receipts, no real client data, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-514

CP00-514 bootstraps the RP17 program: the program contract (contracts/ai-governance-core-contract.json), this package, and the rp17:ai-governance-core:validate gate, covering the full RP17.P00 scope foundation cycle (M00-M10: scope inventory, acceptance gate definition, non-goal boundary, target file map, contract schema outline, ownership note, matter-first trace note, permission baseline note, audit baseline note, synthetic data policy, risk register row, blocked-claim rule, Hermes preflight fields, Claude review prompts, human approval note, closeout handoff, dependency list, downstream RP routing, command matrix, receipt shape) and the RP17.P01 model foundation head (M00-M02: package directory layout, primary entity identifier, tenant scope field, matter trace reference, lifecycle status enum, ownership metadata, reference relationship map, required/optional field registries) as fourteen descriptor-only sections over 150 planned rows.

## CP00-515

CP00-515 implements descriptor-only coverage of the RP17.P01 implementation slice: the M02 Type And Shape Definition tail (the twelve remaining rows), the full M03 Primary Implementation Slice cycle (twenty-two rows), and the M04 Secondary Workflow Slice head (the first six rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP17.P01.M04.S07, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-516

CP00-516 implements descriptor-only coverage of the RP17.P01 workflow permission slice: the M04 Secondary Workflow Slice tail (the fourteen remaining rows), the full M05 Permission And Audit Binding cycle (twenty-two rows), and the M06 Synthetic Fixture Set head (the first four rows) as three descriptor-only sections over 40 planned rows. The pack also remediates the CP00-515 P3 finding by renaming the four CP515 descriptor runtime-dispatch labels to the RP17 AI Governance scope names (ai_governance, model_policy, citation, retrieval_scope), with the contract refreshed from the factories. The pack hands off to RP17.P01.M06.S05, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-517

CP00-517 implements descriptor-only coverage of the RP17 p01 closeout p02 foundation: the P01 Synthetic Fixture Set tail (the sixteen remaining rows), the full P01 Test And Golden Case Set cycle (twenty-two rows), the full P01 Hermes Evidence Packet cycle (twenty rows), the full P01 Claude Review Packet cycle (twenty rows), the full P01 Closeout And Next Handoff cycle (ten rows), the full P02 Scope Inventory cycle (twenty rows), the full P02 Contract Draft cycle (twenty rows), and the full P02 Type And Shape Definition cycle (twenty-two rows) as eight descriptor-only sections over 150 planned rows. The pack also remediates the CP00-516 P3 finding by adding the matter trace reference guard to the CP00-515 and CP00-516 descriptor validators. The pack hands off to RP17.P02.M03.S01, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-518

CP00-518 implements descriptor-only coverage of the RP17.P02 implementation slice: the full M03 Primary Implementation Slice cycle (twenty-five rows) and the M04 Secondary Workflow Slice head (the first fifteen rows) as two descriptor-only sections over 40 planned rows. The pack hands off to RP17.P02.M04.S16, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-519

CP00-519 implements descriptor-only coverage of the RP17.P02 workflow slice: the M04 Secondary Workflow Slice tail (the ten remaining rows) as one descriptor-only section over 10 planned rows. The pack hands off to RP17.P02.M05.S01, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-520

CP00-520 implements descriptor-only coverage of the RP17.P02 permission slice: the first ten rows of the M05 Permission And Audit Binding cycle as one descriptor-only section over 10 planned rows. The pack hands off to RP17.P02.M05.S11, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-521

CP00-521 implements descriptor-only coverage of the RP17.P02 audit binding slice: ten further rows of the M05 Permission And Audit Binding cycle as one descriptor-only section over 10 planned rows. The pack hands off to RP17.P02.M05.S21, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-522

CP00-522 implements descriptor-only coverage of the RP17.P02 permission fixture slice: the M05 Permission And Audit Binding tail (the five remaining rows) and the M06 Synthetic Fixture Set head (the first five rows) as two descriptor-only sections over 10 planned rows. The pack hands off to RP17.P02.M06.S06, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-523

CP00-523 implements descriptor-only coverage of the RP17.P02 fixture slice: ten further rows of the M06 Synthetic Fixture Set cycle as one descriptor-only section over 10 planned rows. The pack hands off to RP17.P02.M06.S16, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-524

CP00-524 implements descriptor-only coverage of the RP17.P02 fixture test slice: the M06 Synthetic Fixture Set tail (the seven remaining rows) and the M07 Test And Golden Case Set head (the first three rows) as two descriptor-only sections over 10 planned rows. The pack hands off to RP17.P02.M07.S04, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-525

CP00-525 implements descriptor-only coverage of the RP17.P02 test hermes slice: the M07 Test And Golden Case Set tail (the twenty-two remaining rows) and the M08 Hermes Evidence Packet head (the first eighteen rows) as two descriptor-only sections over 40 planned rows. The pack hands off to RP17.P02.M08.S19, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-526

CP00-526 implements descriptor-only coverage of the RP17 p02 closeout p03 foundation: the P02 Hermes Evidence Packet tail (the four remaining rows), the full P02 Claude Review Packet cycle (twenty-two rows), the full P02 Closeout And Next Handoff cycle (twenty rows), the full P03 Scope Inventory cycle (ten rows), the full P03 Contract Draft cycle (ten rows), the full P03 Type And Shape Definition cycle (twenty rows), the full P03 Primary Implementation Slice cycle (twenty-two rows), the full P03 Secondary Workflow Slice cycle (twenty rows), and the full P03 Permission And Audit Binding cycle (twenty-two rows) as nine descriptor-only sections over 150 planned rows. The pack hands off to RP17.P03.M06.S01, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-527

CP00-527 implements descriptor-only coverage of the RP17 p03 closeout p04 foundation: the full P03 Synthetic Fixture Set cycle (twenty rows), the full P03 Test And Golden Case Set cycle (twenty-two rows), the full P03 Hermes Evidence Packet cycle (twenty rows), the full P03 Claude Review Packet cycle (twenty rows), the full P03 Closeout And Next Handoff cycle (ten rows), the full P04 Scope Inventory cycle (ten rows), the full P04 Contract Draft cycle (twenty rows), the full P04 Type And Shape Definition cycle (twenty rows), and the P04 Primary Implementation Slice head (the first eight rows) as nine descriptor-only sections over 150 planned rows. The pack hands off to RP17.P04.M03.S09, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-528

CP00-528 implements descriptor-only coverage of the RP17.P04 implementation slice: ten further rows of the M03 Primary Implementation Slice cycle as one descriptor-only section over 10 planned rows. The pack hands off to RP17.P04.M03.S19, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-529

CP00-529 implements descriptor-only coverage of the RP17.P04 workflow permission slice: the M03 Primary Implementation Slice tail (the four remaining rows), the full M04 Secondary Workflow Slice cycle (twenty-two rows), and the M05 Permission And Audit Binding head (the first fourteen rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP17.P04.M05.S15, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-530

CP00-530 implements descriptor-only coverage of the RP17.P04 permission fixture slice: the M05 Permission And Audit Binding tail (the eight remaining rows) and the M06 Synthetic Fixture Set head (the first two rows) as two descriptor-only sections over 10 planned rows. The pack hands off to RP17.P04.M06.S03, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-531

CP00-531 implements descriptor-only coverage of the RP17 p04 closeout p05 foundation: the P04 Synthetic Fixture Set tail (the twenty remaining rows), the full P04 Test And Golden Case Set cycle (twenty-two rows), the full P04 Hermes Evidence Packet cycle (twenty-two rows), the full P04 Claude Review Packet cycle (twenty-two rows), the full P04 Closeout And Next Handoff cycle (twenty rows), the full P05 Scope Inventory cycle (ten rows), the full P05 Contract Draft cycle (twenty rows), and the P05 Type And Shape Definition head (the first fourteen rows) as eight descriptor-only sections over 150 planned rows. The pack hands off to RP17.P05.M02.S15, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-532

CP00-532 implements descriptor-only coverage of the RP17.P05 implementation workflow slice: the M02 Type And Shape Definition tail (the six remaining rows), the full M03 Primary Implementation Slice cycle (twenty-two rows), and the M04 Secondary Workflow Slice head (the first twelve rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP17.P05.M04.S13, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-533

CP00-533 implements descriptor-only coverage of the RP17.P05 workflow permission slice: the M04 Secondary Workflow Slice tail (the ten remaining rows), the full M05 Permission And Audit Binding cycle (twenty-two rows), and the M06 Synthetic Fixture Set head (the first eight rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP17.P05.M06.S09, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-534

CP00-534 implements descriptor-only coverage of the RP17.P05 fixture slice: ten further rows of the M06 Synthetic Fixture Set cycle as one descriptor-only section over 10 planned rows. The pack hands off to RP17.P05.M06.S19, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-535

CP00-535 implements descriptor-only coverage of the RP17 p05 closeout p06 foundation: the P05 Synthetic Fixture Set tail (the four remaining rows), the full P05 Test And Golden Case Set cycle (twenty-two rows), the full P05 Hermes Evidence Packet cycle (twenty-two rows), the full P05 Claude Review Packet cycle (twenty-two rows), the full P05 Closeout And Next Handoff cycle (twenty rows), the full P06 Scope Inventory cycle (twenty rows), the full P06 Contract Draft cycle (twenty rows), and the P06 Type And Shape Definition head (the first twenty rows) as eight descriptor-only sections over 150 planned rows. The pack hands off to RP17.P06.M02.S21, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-536

CP00-536 implements descriptor-only coverage of the RP17.P06 foundation slice: the M02 Type And Shape Definition tail (the two remaining rows) and the M03 Primary Implementation Slice head (the first eight rows) as two descriptor-only sections over 10 planned rows. The pack hands off to RP17.P06.M03.S09, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-537

CP00-537 implements descriptor-only coverage of the RP17.P06 implementation workflow slice: the M03 Primary Implementation Slice tail (the seventeen remaining rows) and the M04 Secondary Workflow Slice head (the first twenty-three rows) as two descriptor-only sections over 40 planned rows. The pack hands off to RP17.P06.M04.S24, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-538

CP00-538 implements descriptor-only coverage of the RP17.P06 workflow permission slice: the M04 Secondary Workflow Slice tail (the two remaining rows), the full M05 Permission And Audit Binding cycle (twenty-five rows), and the M06 Synthetic Fixture Set head (the first thirteen rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP17.P06.M06.S14, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-539

CP00-539 implements descriptor-only coverage of the RP17 p06 closeout p07 foundation: the P06 Synthetic Fixture Set tail (the nine remaining rows), the full P06 Test And Golden Case Set cycle (twenty-five rows), the full P06 Hermes Evidence Packet cycle (twenty-two rows), the full P06 Claude Review Packet cycle (twenty-two rows), the full P06 Closeout And Next Handoff cycle (twenty rows), the full P07 Scope Inventory cycle (twenty rows), the full P07 Contract Draft cycle (twenty rows), and the P07 Type And Shape Definition head (the first twelve rows) as eight descriptor-only sections over 150 planned rows. The pack hands off to RP17.P07.M02.S13, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-540

CP00-540 implements descriptor-only coverage of the RP17.P07 implementation slice: the M02 Type And Shape Definition tail (the ten remaining rows), the full M03 Primary Implementation Slice cycle (twenty-five rows), and the M04 Secondary Workflow Slice head (the first five rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP17.P07.M04.S06, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-541

CP00-541 implements descriptor-only coverage of the RP17.P07 workflow permission slice: the M04 Secondary Workflow Slice tail (the twenty remaining rows) and the M05 Permission And Audit Binding head (the first twenty rows) as two descriptor-only sections over 40 planned rows. The pack hands off to RP17.P07.M05.S21, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-542

CP00-542 implements descriptor-only coverage of the RP17.P07 permission fixture slice: the M05 Permission And Audit Binding tail (the five remaining rows) and the M06 Synthetic Fixture Set head (the first five rows) as two descriptor-only sections over 10 planned rows. The pack hands off to RP17.P07.M06.S06, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-543

CP00-543 implements descriptor-only coverage of the RP17 p07 closeout p08 foundation: the P07 Synthetic Fixture Set tail (the seventeen remaining rows), the full P07 Test And Golden Case Set cycle (twenty-five rows), the full P07 Hermes Evidence Packet cycle (twenty-two rows), the full P07 Claude Review Packet cycle (twenty-two rows), the full P07 Closeout And Next Handoff cycle (twenty rows), the full P08 Scope Inventory cycle (ten rows), the full P08 Contract Draft cycle (twenty rows), and the P08 Type And Shape Definition head (the first fourteen rows) as eight descriptor-only sections over 150 planned rows. The pack hands off to RP17.P08.M02.S15, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-544

CP00-544 implements descriptor-only coverage of the RP17.P08 implementation workflow slice: the M02 Type And Shape Definition tail (the six remaining rows), the full M03 Primary Implementation Slice cycle (twenty-two rows), and the M04 Secondary Workflow Slice head (the first twelve rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP17.P08.M04.S13, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-545

CP00-545 implements descriptor-only coverage of the RP17.P08 workflow permission slice: the M04 Secondary Workflow Slice tail (the ten remaining rows), the full M05 Permission And Audit Binding cycle (twenty-two rows), and the M06 Synthetic Fixture Set head (the first eight rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP17.P08.M06.S09, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-546

CP00-546 implements descriptor-only coverage of the RP17 p08 closeout p09 foundation: the P08 Synthetic Fixture Set tail (the fourteen remaining rows), the full P08 Test And Golden Case Set cycle (twenty-two rows), the full P08 Hermes Evidence Packet cycle (twenty-two rows), the full P08 Claude Review Packet cycle (twenty-two rows), the full P08 Closeout And Next Handoff cycle (twenty rows), the full P09 Scope Inventory cycle (ten rows), the full P09 Contract Draft cycle (ten rows), the full P09 Type And Shape Definition cycle (twenty rows), and the P09 Primary Implementation Slice head (the first ten rows) as nine descriptor-only sections over 150 planned rows. The pack hands off to RP17.P09.M03.S11, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-547

CP00-547 implements descriptor-only coverage of the RP17.P09 workflow permission slice: the M03 Primary Implementation Slice tail (the twelve remaining rows), the full M04 Secondary Workflow Slice cycle (twenty rows), and the M05 Permission And Audit Binding head (the first eight rows) as three descriptor-only sections over 40 planned rows. The pack hands off to RP17.P09.M05.S09, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-548

CP00-548 implements descriptor-only coverage of the RP17.P09 permission slice: ten further rows of the M05 Permission And Audit Binding cycle as one descriptor-only section over 10 planned rows. The pack hands off to RP17.P09.M05.S19, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-549

CP00-549 implements descriptor-only coverage of the RP17.P09 permission fixture slice: the M05 Permission And Audit Binding tail (the four remaining rows) and the M06 Synthetic Fixture Set head (the first six rows) as two descriptor-only sections over 10 planned rows. The pack hands off to RP17.P09.M06.S07, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.

## CP00-550

CP00-550 implements descriptor-only coverage of the RP17.P09 closeout handoff slice: the M06 Synthetic Fixture Set tail (the fourteen remaining rows), the full M07 Test And Golden Case Set cycle (twenty-two rows), the full M08 Hermes Evidence Packet cycle (twenty rows), the full M09 Claude Review Packet cycle (twenty rows), and the full M10 Closeout And Next Handoff cycle (ten rows) as five descriptor-only sections over 86 planned rows. This closes RP17 AI Governance and the pack hands off to RP18.P00.M00.S01, with no AI governance, model policy, retrieval scope, or citation runtime, no state writes, no Claude final approval, and no enterprise trust claim from local validation.
