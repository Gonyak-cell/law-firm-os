import {
  DMS_CORE_CP198_NO_WRITE_ATTESTATION,
  DMS_CORE_CP198_PACK_BINDING,
  DMS_CORE_CP198_REQUIREMENTS,
  DMS_CORE_CP199_NO_WRITE_ATTESTATION,
  DMS_CORE_CP199_PACK_BINDING,
  DMS_CORE_CP199_REQUIREMENTS,
  DMS_CORE_CP200_NO_WRITE_ATTESTATION,
  DMS_CORE_CP200_PACK_BINDING,
  DMS_CORE_CP200_REQUIREMENTS,
  DMS_CORE_CP201_NO_WRITE_ATTESTATION,
  DMS_CORE_CP201_PACK_BINDING,
  DMS_CORE_CP201_REQUIREMENTS,
  DMS_CORE_CP202_NO_WRITE_ATTESTATION,
  DMS_CORE_CP202_PACK_BINDING,
  DMS_CORE_CP202_REQUIREMENTS,
  DMS_CORE_CP203_NO_WRITE_ATTESTATION,
  DMS_CORE_CP203_PACK_BINDING,
  DMS_CORE_CP203_REQUIREMENTS,
  DMS_CORE_CP204_NO_WRITE_ATTESTATION,
  DMS_CORE_CP204_PACK_BINDING,
  DMS_CORE_CP204_REQUIREMENTS,
  DMS_CORE_CP205_NO_WRITE_ATTESTATION,
  DMS_CORE_CP205_PACK_BINDING,
  DMS_CORE_CP205_REQUIREMENTS,
  DMS_CORE_CP206_NO_WRITE_ATTESTATION,
  DMS_CORE_CP206_PACK_BINDING,
  DMS_CORE_CP206_REQUIREMENTS,
  DMS_CORE_CP207_NO_WRITE_ATTESTATION,
  DMS_CORE_CP207_PACK_BINDING,
  DMS_CORE_CP207_REQUIREMENTS,
  DMS_CORE_CP208_NO_WRITE_ATTESTATION,
  DMS_CORE_CP208_PACK_BINDING,
  DMS_CORE_CP208_REQUIREMENTS,
  DMS_CORE_CP209_NO_WRITE_ATTESTATION,
  DMS_CORE_CP209_PACK_BINDING,
  DMS_CORE_CP209_REQUIREMENTS,
  DMS_CORE_CP210_NO_WRITE_ATTESTATION,
  DMS_CORE_CP210_PACK_BINDING,
  DMS_CORE_CP210_REQUIREMENTS,
  DMS_CORE_CP211_NO_WRITE_ATTESTATION,
  DMS_CORE_CP211_PACK_BINDING,
  DMS_CORE_CP211_REQUIREMENTS,
  DMS_CORE_CP212_NO_WRITE_ATTESTATION,
  DMS_CORE_CP212_PACK_BINDING,
  DMS_CORE_CP212_REQUIREMENTS,
  DMS_CORE_CP213_NO_WRITE_ATTESTATION,
  DMS_CORE_CP213_PACK_BINDING,
  DMS_CORE_CP213_REQUIREMENTS,
  DMS_CORE_CP214_NO_WRITE_ATTESTATION,
  DMS_CORE_CP214_PACK_BINDING,
  DMS_CORE_CP214_REQUIREMENTS,
  DMS_CORE_CP215_NO_WRITE_ATTESTATION,
  DMS_CORE_CP215_PACK_BINDING,
  DMS_CORE_CP215_REQUIREMENTS,
  DMS_CORE_CP216_NO_WRITE_ATTESTATION,
  DMS_CORE_CP216_PACK_BINDING,
  DMS_CORE_CP216_REQUIREMENTS,
  DMS_CORE_CP217_NO_WRITE_ATTESTATION,
  DMS_CORE_CP217_PACK_BINDING,
  DMS_CORE_CP217_REQUIREMENTS,
  DMS_CORE_CP218_NO_WRITE_ATTESTATION,
  DMS_CORE_CP218_PACK_BINDING,
  DMS_CORE_CP218_REQUIREMENTS,
  DMS_CORE_CP219_NO_WRITE_ATTESTATION,
  DMS_CORE_CP219_PACK_BINDING,
  DMS_CORE_CP219_REQUIREMENTS,
  DMS_CORE_CP220_NO_WRITE_ATTESTATION,
  DMS_CORE_CP220_PACK_BINDING,
  DMS_CORE_CP220_REQUIREMENTS,
  DMS_CORE_CP221_NO_WRITE_ATTESTATION,
  DMS_CORE_CP221_PACK_BINDING,
  DMS_CORE_CP221_REQUIREMENTS,
  DMS_CORE_CP222_NO_WRITE_ATTESTATION,
  DMS_CORE_CP222_PACK_BINDING,
  DMS_CORE_CP222_REQUIREMENTS,
  DMS_CORE_CP223_NO_WRITE_ATTESTATION,
  DMS_CORE_CP223_PACK_BINDING,
  DMS_CORE_CP223_REQUIREMENTS,
  DMS_CORE_CP224_NO_WRITE_ATTESTATION,
  DMS_CORE_CP224_PACK_BINDING,
  DMS_CORE_CP224_REQUIREMENTS,
  DMS_CORE_CP225_NO_WRITE_ATTESTATION,
  DMS_CORE_CP225_PACK_BINDING,
  DMS_CORE_CP225_REQUIREMENTS,
  DMS_CORE_CP226_NO_WRITE_ATTESTATION,
  DMS_CORE_CP226_PACK_BINDING,
  DMS_CORE_CP226_REQUIREMENTS,
  DMS_CORE_CP227_NO_WRITE_ATTESTATION,
  DMS_CORE_CP227_PACK_BINDING,
  DMS_CORE_CP227_REQUIREMENTS,
  DMS_CORE_CP228_NO_WRITE_ATTESTATION,
  DMS_CORE_CP228_PACK_BINDING,
  DMS_CORE_CP228_REQUIREMENTS,
  DMS_CORE_CP229_NO_WRITE_ATTESTATION,
  DMS_CORE_CP229_PACK_BINDING,
  DMS_CORE_CP229_REQUIREMENTS,
  DMS_CORE_CP230_NO_WRITE_ATTESTATION,
  DMS_CORE_CP230_PACK_BINDING,
  DMS_CORE_CP230_REQUIREMENTS,
  DMS_CORE_CP231_NO_WRITE_ATTESTATION,
  DMS_CORE_CP231_PACK_BINDING,
  DMS_CORE_CP231_REQUIREMENTS,
  DMS_CORE_CP232_NO_WRITE_ATTESTATION,
  DMS_CORE_CP232_PACK_BINDING,
  DMS_CORE_CP232_REQUIREMENTS,
  DMS_CORE_CP233_NO_WRITE_ATTESTATION,
  DMS_CORE_CP233_PACK_BINDING,
  DMS_CORE_CP233_REQUIREMENTS,
  DMS_CORE_CP234_NO_WRITE_ATTESTATION,
  DMS_CORE_CP234_PACK_BINDING,
  DMS_CORE_CP234_REQUIREMENTS,
  DMS_CORE_MODEL_DEFINITIONS,
  DMS_CORE_OPTIONAL_FIELD_REGISTRY,
  DMS_CORE_PROGRAM_CONTRACT,
  DMS_CORE_REFERENCE_RELATIONSHIP_MAP,
  DMS_CORE_REQUIRED_FIELD_REGISTRY,
  DMS_CORE_SERIALIZATION_SHAPE,
  DMS_CORE_STATE_TRANSITION_MAP,
  DMS_LIFECYCLE_STATUSES,
  DMS_REVIEW_STATUSES,
  DMS_SOURCE_POLICIES,
  DMS_VERSION_STATUSES,
  DMS_WORKSPACE_STATUSES,
} from "./registry.js";
import {
  createDmsCoreCp199FixtureModel,
  createDmsCoreCp199TypeShapeDescriptor,
  createDmsCoreCp200FixtureModel,
  createDmsCoreCp200PermissionAuditDescriptor,
  createDmsCoreCp201GoldenCaseSet,
  createDmsCoreCp201ServiceContractDescriptor,
  createDmsCoreCp201ServiceDescriptor,
  createDmsCoreCp202RouteMatrix,
  createDmsCoreCp203SensitiveTailCaseSet,
  createDmsCoreCp203SensitiveTailDescriptor,
  createDmsCoreCp204PermissionAuditGateSet,
  createDmsCoreCp204PermissionAuditWorkflowBindingDescriptor,
  createDmsCoreCp205PermissionAuditTailCaseSet,
  createDmsCoreCp205PermissionAuditTailDescriptor,
  createDmsCoreCp206SyntheticFixtureServiceCaseSet,
  createDmsCoreCp206SyntheticFixtureServiceDescriptor,
  createDmsCoreCp207SyntheticFixtureWorkflowCaseSet,
  createDmsCoreCp207SyntheticFixtureWorkflowDescriptor,
  createDmsCoreCp208SyntheticFixtureTailEntrypointCaseSet,
  createDmsCoreCp208SyntheticFixtureTailEntrypointDescriptor,
  createDmsCoreCp209GoldenCaseHermesCycleCaseSet,
  createDmsCoreCp209GoldenCaseHermesCycleDescriptor,
  createDmsCoreCp210P02CloseoutP03FoundationCaseSet,
  createDmsCoreCp210P02CloseoutP03FoundationDescriptor,
  createDmsCoreCp211P03CloseoutP04UiFoundationCaseSet,
  createDmsCoreCp211P03CloseoutP04UiFoundationDescriptor,
  createDmsCoreCp212UiPrimarySliceTailCaseSet,
  createDmsCoreCp212UiPrimarySliceTailDescriptor,
  createDmsCoreCp213UiSecondarySliceBindingCaseSet,
  createDmsCoreCp213UiSecondarySliceBindingDescriptor,
  createDmsCoreCp214UiBindingTailCaseSet,
  createDmsCoreCp214UiBindingTailDescriptor,
  createDmsCoreCp215P04CloseoutP05FixtureFoundationCaseSet,
  createDmsCoreCp215P04CloseoutP05FixtureFoundationDescriptor,
  createDmsCoreCp216FixtureCaseSliceCaseSet,
  createDmsCoreCp216FixtureCaseSliceDescriptor,
  createDmsCoreCp217FixtureBindingSliceCaseSet,
  createDmsCoreCp217FixtureBindingSliceDescriptor,
  createDmsCoreCp218FixtureSetTailCaseSet,
  createDmsCoreCp218FixtureSetTailDescriptor,
  createDmsCoreCp219P05CloseoutP06PermissionMatrixCaseSet,
  createDmsCoreCp219P05CloseoutP06PermissionMatrixDescriptor,
  createDmsCoreCp220PermissionMatrixSliceCaseSet,
  createDmsCoreCp220PermissionMatrixSliceDescriptor,
  createDmsCoreCp221PermissionWorkflowSliceCaseSet,
  createDmsCoreCp221PermissionWorkflowSliceDescriptor,
  createDmsCoreCp222PermissionAuditBindingSliceCaseSet,
  createDmsCoreCp222PermissionAuditBindingSliceDescriptor,
  createDmsCoreCp223P06CloseoutP07FailureFoundationCaseSet,
  createDmsCoreCp223P06CloseoutP07FailureFoundationDescriptor,
  createDmsCoreCp224FailureRecoverySliceCaseSet,
  createDmsCoreCp224FailureRecoverySliceDescriptor,
  createDmsCoreCp225FailureBindingSliceCaseSet,
  createDmsCoreCp225FailureBindingSliceDescriptor,
  createDmsCoreCp226FailureAuditTailCaseSet,
  createDmsCoreCp226FailureAuditTailDescriptor,
  createDmsCoreCp227P07CloseoutP08HermesReceiptCaseSet,
  createDmsCoreCp227P07CloseoutP08HermesReceiptDescriptor,
  createDmsCoreCp228HermesReceiptSliceCaseSet,
  createDmsCoreCp228HermesReceiptSliceDescriptor,
  createDmsCoreCp229HermesBindingSliceCaseSet,
  createDmsCoreCp229HermesBindingSliceDescriptor,
  createDmsCoreCp230P08CloseoutP09ReviewGateCaseSet,
  createDmsCoreCp230P08CloseoutP09ReviewGateDescriptor,
  createDmsCoreCp231ReviewGateSliceCaseSet,
  createDmsCoreCp231ReviewGateSliceDescriptor,
  createDmsCoreCp232ReviewAuditBindingCaseSet,
  createDmsCoreCp232ReviewAuditBindingDescriptor,
  createDmsCoreCp233ReviewFixtureTailCaseSet,
  createDmsCoreCp233ReviewFixtureTailDescriptor,
  createDmsCoreCp234P09ReviewCloseoutCaseSet,
  createDmsCoreCp234P09ReviewCloseoutDescriptor,
  dmsCoreCp210RowKey,
  createDmsCoreCp202WorkflowCaseSet,
  createDmsCoreCp202WorkflowDescriptor,
  createDmsCorePermissionAuditBindingRegistry,
  createDmsCoreFoundationDescriptor,
  createDmsCoreModelShapeRegistry,
  serializeDmsCoreDescriptor,
} from "./service.js";
import { getDmsCoreModelDefinition, missingDmsCoreRequiredFields } from "./model.js";

function freezeValidation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP198_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP198_NO_WRITE_ATTESTATION,
  });
}

function freezeCp199Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP199_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP199_NO_WRITE_ATTESTATION,
  });
}

function freezeCp200Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP200_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP200_NO_WRITE_ATTESTATION,
  });
}

function freezeCp201Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP201_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP201_NO_WRITE_ATTESTATION,
  });
}

function freezeCp202Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP202_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP202_NO_WRITE_ATTESTATION,
  });
}

function freezeCp203Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP203_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP203_NO_WRITE_ATTESTATION,
  });
}

function freezeCp204Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP204_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP204_NO_WRITE_ATTESTATION,
  });
}

function freezeCp205Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP205_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP205_NO_WRITE_ATTESTATION,
  });
}

function freezeCp206Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP206_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP206_NO_WRITE_ATTESTATION,
  });
}

function freezeCp207Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP207_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP207_NO_WRITE_ATTESTATION,
  });
}

function freezeCp208Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP208_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP208_NO_WRITE_ATTESTATION,
  });
}

function freezeCp209Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP209_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP209_NO_WRITE_ATTESTATION,
  });
}

function freezeCp210Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP210_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP210_NO_WRITE_ATTESTATION,
  });
}

function freezeCp211Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP211_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP211_NO_WRITE_ATTESTATION,
  });
}

function freezeCp212Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP212_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP212_NO_WRITE_ATTESTATION,
  });
}

function freezeCp213Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP213_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP213_NO_WRITE_ATTESTATION,
  });
}

function freezeCp214Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP214_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP214_NO_WRITE_ATTESTATION,
  });
}

function freezeCp215Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP215_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP215_NO_WRITE_ATTESTATION,
  });
}

function freezeCp216Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP216_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP216_NO_WRITE_ATTESTATION,
  });
}

function freezeCp217Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP217_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP217_NO_WRITE_ATTESTATION,
  });
}

function freezeCp218Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP218_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP218_NO_WRITE_ATTESTATION,
  });
}

function freezeCp219Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP219_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP219_NO_WRITE_ATTESTATION,
  });
}

function freezeCp220Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP220_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP220_NO_WRITE_ATTESTATION,
  });
}

function freezeCp221Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP221_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP221_NO_WRITE_ATTESTATION,
  });
}

function freezeCp222Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP222_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP222_NO_WRITE_ATTESTATION,
  });
}

function freezeCp223Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP223_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP223_NO_WRITE_ATTESTATION,
  });
}

function freezeCp224Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP224_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP224_NO_WRITE_ATTESTATION,
  });
}

function freezeCp225Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP225_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP225_NO_WRITE_ATTESTATION,
  });
}

function freezeCp226Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP226_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP226_NO_WRITE_ATTESTATION,
  });
}

function freezeCp227Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP227_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP227_NO_WRITE_ATTESTATION,
  });
}

function freezeCp228Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP228_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP228_NO_WRITE_ATTESTATION,
  });
}

function freezeCp229Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP229_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP229_NO_WRITE_ATTESTATION,
  });
}

function freezeCp230Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP230_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP230_NO_WRITE_ATTESTATION,
  });
}

function freezeCp231Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP231_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP231_NO_WRITE_ATTESTATION,
  });
}

function freezeCp232Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP232_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP232_NO_WRITE_ATTESTATION,
  });
}

function freezeCp233Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP233_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP233_NO_WRITE_ATTESTATION,
  });
}

function freezeCp234Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: DMS_CORE_CP234_PACK_BINDING.pack_id,
    no_write_attestation: DMS_CORE_CP234_NO_WRITE_ATTESTATION,
  });
}

function countBy(units, key) {
  return Object.freeze(
    units.reduce((acc, unit) => {
      acc[unit[key]] = (acc[unit[key]] ?? 0) + 1;
      return acc;
    }, {}),
  );
}

export function validateDmsCoreRegistry() {
  const errors = [];
  const definitions = Object.values(DMS_CORE_MODEL_DEFINITIONS);
  if (definitions.length !== 10) errors.push("DMS Core registry must include 10 model definitions");
  for (const definition of definitions) {
    if (definition.owner_module !== "legal-workspace-dms") errors.push(`${definition.model_type} owner_module drift`);
    if (!definition.required_fields.includes("tenant_id")) errors.push(`${definition.model_type} missing tenant_id`);
    if (!definition.required_fields.includes("matter_id")) errors.push(`${definition.model_type} missing matter_id`);
    if (!definition.required_fields.includes("permission_envelope_id")) errors.push(`${definition.model_type} missing permission envelope`);
    if (!definition.required_fields.includes("audit_trace_id")) errors.push(`${definition.model_type} missing audit trace`);
  }
  if (!DMS_CORE_PROGRAM_CONTRACT.invariants.includes("dms_independent_layer")) errors.push("DMS invariant missing");
  if (!DMS_CORE_PROGRAM_CONTRACT.invariants.includes("version_safe_dms")) errors.push("version-safe DMS invariant missing");
  return freezeValidation({ valid: errors.length === 0, errors: Object.freeze(errors), model_count: definitions.length });
}

export function validateDmsCoreRecord(modelType, record) {
  const errors = [];
  const definition = getDmsCoreModelDefinition(modelType);
  if (!definition) errors.push(`unknown DMS model type ${modelType}`);
  for (const field of missingDmsCoreRequiredFields(modelType, record)) errors.push(`missing required field ${field}`);
  if (record?.model_type !== modelType) errors.push(`${modelType} model_type drift`);
  if (record?.owner_module !== "legal-workspace-dms") errors.push(`${modelType} owner_module drift`);
  if (record?.writes_product_state !== false) errors.push(`${modelType} must not write product state`);
  if (record?.evaluates_runtime_permission !== false) errors.push(`${modelType} must not evaluate runtime permission`);
  if (record?.writes_audit_event !== false) errors.push(`${modelType} must not write audit events`);
  if (record?.reads_object_storage !== false) errors.push(`${modelType} must not read object storage`);
  if (record?.writes_object_storage !== false) errors.push(`${modelType} must not write object storage`);
  if (record?.exposes_document_bytes !== false) errors.push(`${modelType} must not expose document bytes`);
  if (record?.exposes_extracted_text !== false) errors.push(`${modelType} must not expose extracted text`);
  const allowed = definition?.lifecycle_statuses ?? [];
  const status = record?.status ?? record?.review_status;
  if (status && !allowed.includes(status)) errors.push(`${modelType} status must be in registry lifecycle statuses`);
  if (record?.source_policy && !DMS_SOURCE_POLICIES.includes(record.source_policy)) {
    errors.push(`${modelType} source_policy invalid`);
  }
  return freezeValidation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp198CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeValidation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp198Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-198 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP198_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-198");
  if (planPack?.risk_class !== DMS_CORE_CP198_PACK_BINDING.risk_class) errors.push("CP00-198 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP198_PACK_BINDING.unit_count) errors.push("CP00-198 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP198_PACK_BINDING.first_unit_id) errors.push("CP00-198 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP198_PACK_BINDING.last_unit_id) errors.push("CP00-198 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-198 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-198 must only include RP06 units");
  const summary = createDmsCoreCp198CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP198_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-198 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP198_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-198 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP198_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-198 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP198_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-198 ${microTitle} distribution drift`);
  }
  for (const title of DMS_CORE_CP198_REQUIREMENTS.required_unit_titles) {
    if (!summary.unit_titles.includes(title)) errors.push(`CP00-198 missing unit title ${title}`);
  }
  return freezeValidation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp198Foundation(contractProjection = {}, descriptor = null) {
  const errors = [];
  const registry = validateDmsCoreRegistry();
  if (!registry.valid) errors.push(...registry.errors);
  if (DMS_CORE_CP198_NO_WRITE_ATTESTATION.writes_product_state !== false) errors.push("CP00-198 must not write product state");
  if (DMS_CORE_CP198_NO_WRITE_ATTESTATION.evaluates_runtime_permission !== false) errors.push("CP00-198 must not evaluate runtime permissions");
  if (DMS_CORE_CP198_NO_WRITE_ATTESTATION.writes_audit_event !== false) errors.push("CP00-198 must not write audit events");
  if (DMS_CORE_CP198_NO_WRITE_ATTESTATION.executes_object_storage_read !== false) errors.push("CP00-198 must not read object storage");
  if (DMS_CORE_CP198_NO_WRITE_ATTESTATION.executes_object_storage_write !== false) errors.push("CP00-198 must not write object storage");
  if (DMS_CORE_CP198_NO_WRITE_ATTESTATION.executes_ocr !== false) errors.push("CP00-198 must not execute OCR");
  if (DMS_CORE_CP198_NO_WRITE_ATTESTATION.executes_search_indexing !== false) errors.push("CP00-198 must not execute search indexing");
  if (DMS_CORE_CP198_NO_WRITE_ATTESTATION.exposes_document_bytes !== false) errors.push("CP00-198 must not expose document bytes");
  if (DMS_CORE_CP198_NO_WRITE_ATTESTATION.exposes_extracted_text !== false) errors.push("CP00-198 must not expose extracted text");
  if (DMS_CORE_CP198_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-198 must not promote Claude");
  if (DMS_CORE_CP198_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) errors.push("CP00-198 must not claim enterprise trust");
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP198_PACK_BINDING.pack_id,
      DMS_CORE_CP199_PACK_BINDING.pack_id,
      DMS_CORE_CP200_PACK_BINDING.pack_id,
      DMS_CORE_CP201_PACK_BINDING.pack_id,
      DMS_CORE_CP202_PACK_BINDING.pack_id,
      DMS_CORE_CP203_PACK_BINDING.pack_id,
      DMS_CORE_CP204_PACK_BINDING.pack_id,
      DMS_CORE_CP205_PACK_BINDING.pack_id,
      DMS_CORE_CP206_PACK_BINDING.pack_id,
      DMS_CORE_CP207_PACK_BINDING.pack_id,
      DMS_CORE_CP208_PACK_BINDING.pack_id,
      DMS_CORE_CP209_PACK_BINDING.pack_id,
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-198 contract current_pack drift");
  }
  if (contractProjection?.program?.program_id && contractProjection.program.program_id !== "RP06") errors.push("CP00-198 program drift");
  if (
    contractProjection?.program?.current_pack_id &&
    ![
      DMS_CORE_CP198_PACK_BINDING.pack_id,
      DMS_CORE_CP199_PACK_BINDING.pack_id,
      DMS_CORE_CP200_PACK_BINDING.pack_id,
      DMS_CORE_CP201_PACK_BINDING.pack_id,
      DMS_CORE_CP202_PACK_BINDING.pack_id,
      DMS_CORE_CP203_PACK_BINDING.pack_id,
      DMS_CORE_CP204_PACK_BINDING.pack_id,
      DMS_CORE_CP205_PACK_BINDING.pack_id,
      DMS_CORE_CP206_PACK_BINDING.pack_id,
      DMS_CORE_CP207_PACK_BINDING.pack_id,
      DMS_CORE_CP208_PACK_BINDING.pack_id,
      DMS_CORE_CP209_PACK_BINDING.pack_id,
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.program.current_pack_id,
    )
  ) {
    errors.push("CP00-198 program current_pack_id drift");
  }
  if (descriptor) {
    if (descriptor.descriptor !== "DmsCoreFoundationDescriptor") errors.push("CP00-198 descriptor type drift");
    if (descriptor.source_matter_core_pack_id !== DMS_CORE_CP198_PACK_BINDING.upstream_pack_id) {
      errors.push("CP00-198 descriptor must source CP00-197");
    }
    if (descriptor.fixture?.model_count !== Object.keys(DMS_CORE_MODEL_DEFINITIONS).length) errors.push("CP00-198 fixture model count drift");
    if (descriptor.permission_baseline?.permission_decision_detail_exposed !== false) errors.push("CP00-198 leaks permission decision detail");
    if (descriptor.audit_baseline?.audit_event_body_exposed !== false) errors.push("CP00-198 leaks audit event body");
    if (descriptor.audit_baseline?.audit_event_written !== false) errors.push("CP00-198 writes audit event");
    if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP198_PACK_BINDING.next_pack_id) errors.push("CP00-198 handoff target drift");
    if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP198_PACK_BINDING.next_subphase_id) {
      errors.push("CP00-198 next subphase drift");
    }
  }
  return freezeValidation({
    valid: errors.length === 0,
    errors: Object.freeze(errors),
    model_count: Object.keys(DMS_CORE_MODEL_DEFINITIONS).length,
    lifecycle_statuses: Object.freeze({
      workspace: DMS_WORKSPACE_STATUSES,
      document: DMS_LIFECYCLE_STATUSES,
      version: DMS_VERSION_STATUSES,
      review: DMS_REVIEW_STATUSES,
    }),
  });
}

export function createDmsCoreCp198HermesEvidencePacket(planPack, contractProjection = {}, descriptor = null) {
  const coverage = validateDmsCoreCp198Coverage(planPack);
  const foundation = validateDmsCoreCp198Foundation(contractProjection, descriptor);
  return freezeValidation({
    evidence_packet: "H06.CP00-198.dms_core_foundation_model_registry",
    gate: DMS_CORE_CP198_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    foundation_valid: foundation.valid,
    no_real_data: true,
    source_matter_core_pack_id: DMS_CORE_CP198_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP198_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP198_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP198_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createDmsCoreCp198ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp198Coverage(planPack);
  const foundation = validateDmsCoreCp198Foundation({}, createDmsCoreFoundationDescriptor());
  return freezeValidation({
    review_packet: "C06.CP00-198.dms_core_foundation_model_registry",
    gate: DMS_CORE_CP198_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP198_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    foundation_valid: foundation.valid,
    invalid_review_blockers: DMS_CORE_CP198_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-198 cover exactly the 150 planned RP06 units from RP06.P00.M00.S01 through RP06.P01.M02.S08?",
      "Does DMS Core remain matter-first, version-safe, and independent while referencing upstream Matter Core rather than duplicating Matter identity?",
      "Are object storage, document bytes, extracted text, OCR, search indexing, email runtime, Citation Ledger runtime, and Loop runtime all out of scope?",
      "Do permission and audit baselines fail closed without leaking decision details, audit event bodies, or unauthorized counts?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp198CloseoutHandoff() {
  return freezeValidation({
    handoff_id: "CP00-198-to-CP00-199",
    from_pack_id: DMS_CORE_CP198_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP198_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP198_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP198_PACK_BINDING.range,
    open_scope: "Continue DMS required/optional fields and state-transition mapping from RP06.P01.M02.S09 onward without activating object storage, OCR, search, email, Citation Ledger, or Loop runtime.",
    production_ready_flag: DMS_CORE_CP198_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp199CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp199Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp199Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-199 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP199_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-199");
  if (planPack?.risk_class !== DMS_CORE_CP199_PACK_BINDING.risk_class) errors.push("CP00-199 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP199_PACK_BINDING.unit_count) errors.push("CP00-199 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP199_PACK_BINDING.first_unit_id) errors.push("CP00-199 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP199_PACK_BINDING.last_unit_id) errors.push("CP00-199 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-199 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-199 must only include RP06 units");
  const summary = createDmsCoreCp199CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP199_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-199 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP199_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-199 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP199_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-199 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP199_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-199 ${microTitle} distribution drift`);
  }
  for (const title of DMS_CORE_CP199_REQUIREMENTS.required_unit_titles) {
    if (!summary.unit_titles.includes(title)) errors.push(`CP00-199 missing unit title ${title}`);
  }
  return freezeCp199Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp199ModelShapeRegistry(registry = createDmsCoreModelShapeRegistry()) {
  const errors = [];
  const modelShapes = registry.model_shapes ?? {};
  for (const [modelType, definition] of Object.entries(DMS_CORE_MODEL_DEFINITIONS)) {
    const shape = modelShapes[modelType];
    if (!shape) {
      errors.push(`${modelType} missing CP00-199 model shape`);
      continue;
    }
    if (shape.owner_module && shape.owner_module !== "legal-workspace-dms") errors.push(`${modelType} owner_module drift`);
    if (!definition.required_fields.includes(shape.primary_entity_identifier)) {
      errors.push(`${modelType} primary_entity_identifier must be required`);
    }
    if (shape.tenant_scope_field !== "tenant_id") errors.push(`${modelType} tenant_scope_field drift`);
    if (shape.matter_trace_reference !== "matter_id") errors.push(`${modelType} matter_trace_reference drift`);
    for (const requiredField of definition.required_fields) {
      if (!shape.required_fields.includes(requiredField)) errors.push(`${modelType} missing required field ${requiredField}`);
    }
    for (const optionalField of DMS_CORE_OPTIONAL_FIELD_REGISTRY[modelType] ?? []) {
      if (!shape.optional_fields.includes(optionalField)) errors.push(`${modelType} missing optional field ${optionalField}`);
    }
    for (const relationship of DMS_CORE_REFERENCE_RELATIONSHIP_MAP[modelType] ?? []) {
      if (!shape.reference_relationships.includes(relationship)) errors.push(`${modelType} missing relationship ${relationship}`);
    }
    for (const status of definition.lifecycle_statuses) {
      if (!shape.lifecycle_status_enum.includes(status)) errors.push(`${modelType} missing lifecycle status ${status}`);
      if (!Object.hasOwn(DMS_CORE_STATE_TRANSITION_MAP, status)) errors.push(`${modelType} state transition map missing status ${status}`);
    }
    if (shape.ownership_metadata?.owner_module !== definition.owner_module) errors.push(`${modelType} ownership metadata drift`);
  }
  return freezeCp199Validation({ valid: errors.length === 0, errors: Object.freeze(errors), model_count: Object.keys(modelShapes).length });
}

export function validateDmsCoreCp199SerializedDescriptor(record) {
  const errors = [];
  const envelope = serializeDmsCoreDescriptor(record);
  if (envelope.envelope !== DMS_CORE_SERIALIZATION_SHAPE.envelope) errors.push("CP00-199 serialization envelope drift");
  if (!envelope.descriptor_ref) errors.push("CP00-199 serialized descriptor missing descriptor_ref");
  if (envelope.tenant_id !== record?.tenant_id) errors.push("CP00-199 serialized descriptor tenant drift");
  if (envelope.matter_id !== record?.matter_id) errors.push("CP00-199 serialized descriptor matter drift");
  if (envelope.raw_payload_included !== false) errors.push("CP00-199 serialized descriptor must not include raw payload");
  if (envelope.document_bytes_included !== false) errors.push("CP00-199 serialized descriptor must not include document bytes");
  if (envelope.extracted_text_included !== false) errors.push("CP00-199 serialized descriptor must not include extracted text");
  if (envelope.permission_decision_detail_included !== false) errors.push("CP00-199 serialized descriptor must not include permission detail");
  if (envelope.audit_event_body_included !== false) errors.push("CP00-199 serialized descriptor must not include audit body");
  if (envelope.writes_product_state !== false) errors.push("CP00-199 serialized descriptor must not write product state");
  if (envelope.evaluates_runtime_permission !== false) errors.push("CP00-199 serialized descriptor must not evaluate runtime permission");
  if (envelope.writes_audit_event !== false) errors.push("CP00-199 serialized descriptor must not write audit events");
  return freezeCp199Validation({ valid: errors.length === 0, errors: Object.freeze(errors), envelope });
}

export function validateDmsCoreCp199TypeShape(contractProjection = {}, descriptor = createDmsCoreCp199TypeShapeDescriptor()) {
  const errors = [];
  const registry = validateDmsCoreCp199ModelShapeRegistry(descriptor.model_shape_registry);
  if (!registry.valid) errors.push(...registry.errors);
  const fixture = descriptor.fixture ?? createDmsCoreCp199FixtureModel();
  if (fixture.records?.length !== Object.keys(DMS_CORE_MODEL_DEFINITIONS).length) errors.push("CP00-199 fixture record count drift");
  if (fixture.serialized_records?.length !== fixture.records?.length) errors.push("CP00-199 serialized record count drift");
  for (const record of fixture.records ?? []) {
    const serialized = validateDmsCoreCp199SerializedDescriptor(record);
    if (!serialized.valid) errors.push(...serialized.errors);
  }
  if (DMS_CORE_CP199_NO_WRITE_ATTESTATION.writes_product_state !== false) errors.push("CP00-199 must not write product state");
  if (DMS_CORE_CP199_NO_WRITE_ATTESTATION.evaluates_runtime_permission !== false) errors.push("CP00-199 must not evaluate runtime permission");
  if (DMS_CORE_CP199_NO_WRITE_ATTESTATION.writes_audit_event !== false) errors.push("CP00-199 must not write audit events");
  if (DMS_CORE_CP199_NO_WRITE_ATTESTATION.executes_object_storage_read !== false) errors.push("CP00-199 must not read object storage");
  if (DMS_CORE_CP199_NO_WRITE_ATTESTATION.executes_ocr !== false) errors.push("CP00-199 must not execute OCR");
  if (DMS_CORE_CP199_NO_WRITE_ATTESTATION.executes_search_indexing !== false) errors.push("CP00-199 must not execute search indexing");
  if (DMS_CORE_CP199_NO_WRITE_ATTESTATION.implements_loop_engine !== false) errors.push("CP00-199 must not implement Loop engine");
  if (DMS_CORE_CP199_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-199 must not promote Claude");
  if (DMS_CORE_CP199_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-199 must not claim enterprise trust");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP199_PACK_BINDING.pack_id,
      DMS_CORE_CP200_PACK_BINDING.pack_id,
      DMS_CORE_CP201_PACK_BINDING.pack_id,
      DMS_CORE_CP202_PACK_BINDING.pack_id,
      DMS_CORE_CP203_PACK_BINDING.pack_id,
      DMS_CORE_CP204_PACK_BINDING.pack_id,
      DMS_CORE_CP205_PACK_BINDING.pack_id,
      DMS_CORE_CP206_PACK_BINDING.pack_id,
      DMS_CORE_CP207_PACK_BINDING.pack_id,
      DMS_CORE_CP208_PACK_BINDING.pack_id,
      DMS_CORE_CP209_PACK_BINDING.pack_id,
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-199 contract current_pack drift");
  }
  if (contractProjection?.type_shape_descriptor?.descriptor && contractProjection.type_shape_descriptor.descriptor !== descriptor.descriptor) {
    errors.push("CP00-199 contract type_shape_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP199_PACK_BINDING.next_pack_id) errors.push("CP00-199 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP199_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-199 next subphase drift");
  }
  return freezeCp199Validation({
    valid: errors.length === 0,
    errors: Object.freeze(errors),
    model_shape_registry_valid: registry.valid,
    serialized_record_count: fixture.serialized_records?.length ?? 0,
    state_transition_status_count: Object.keys(DMS_CORE_STATE_TRANSITION_MAP).length,
  });
}

export function createDmsCoreCp199HermesEvidencePacket(planPack, contractProjection = {}, descriptor = createDmsCoreCp199TypeShapeDescriptor()) {
  const coverage = validateDmsCoreCp199Coverage(planPack);
  const typeShape = validateDmsCoreCp199TypeShape(contractProjection, descriptor);
  return freezeCp199Validation({
    evidence_packet: "H06.CP00-199.dms_core_type_shape_service_descriptor",
    gate: DMS_CORE_CP199_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    type_shape_valid: typeShape.valid,
    no_real_data: true,
    source_foundation_pack_id: DMS_CORE_CP199_PACK_BINDING.upstream_pack_id,
    required_serialization_guards: DMS_CORE_CP199_REQUIREMENTS.required_serialization_guards,
    next_pack_id: DMS_CORE_CP199_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP199_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && typeShape.valid,
  });
}

export function createDmsCoreCp199ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp199Coverage(planPack);
  const typeShape = validateDmsCoreCp199TypeShape({}, createDmsCoreCp199TypeShapeDescriptor());
  return freezeCp199Validation({
    review_packet: "C06.CP00-199.dms_core_type_shape_service_descriptor",
    gate: DMS_CORE_CP199_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP199_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    type_shape_valid: typeShape.valid,
    invalid_review_blockers: DMS_CORE_CP199_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-199 cover exactly the 40 planned RP06 units from RP06.P01.M02.S09 through RP06.P01.M04.S06?",
      "Does DMS Core define required/optional fields, reference relationships, ownership metadata, and lifecycle transitions without executing persistence?",
      "Does serialization exclude raw payloads, document bytes, extracted text, permission decision detail, and audit event bodies?",
      "Does CP00-199 hand off to CP00-200 / RP06.P01.M04.S07 without moving object storage, OCR, search, email, Citation Ledger, or Loop runtime early?",
    ]),
  });
}

export function createDmsCoreCp199CloseoutHandoff() {
  return freezeCp199Validation({
    handoff_id: "CP00-199-to-CP00-200",
    from_pack_id: DMS_CORE_CP199_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP199_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP199_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP199_PACK_BINDING.range,
    open_scope: "Continue RP06.P01.M04.S07 onward by extending DMS secondary workflow descriptors without activating persistence, object storage, OCR, search, email, Citation Ledger, or Loop runtime.",
    production_ready_flag: DMS_CORE_CP199_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp200CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp200Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp200Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-200 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP200_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-200");
  if (planPack?.risk_class !== DMS_CORE_CP200_PACK_BINDING.risk_class) errors.push("CP00-200 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP200_PACK_BINDING.unit_count) errors.push("CP00-200 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP200_PACK_BINDING.first_unit_id) errors.push("CP00-200 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP200_PACK_BINDING.last_unit_id) errors.push("CP00-200 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-200 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-200 must only include RP06 units");
  const summary = createDmsCoreCp200CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP200_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-200 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP200_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-200 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP200_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-200 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP200_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-200 ${microTitle} distribution drift`);
  }
  for (const title of DMS_CORE_CP200_REQUIREMENTS.required_unit_titles) {
    if (!summary.unit_titles.includes(title)) errors.push(`CP00-200 missing unit title ${title}`);
  }
  return freezeCp200Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp200PermissionAuditBinding(
  descriptor = createDmsCoreCp200PermissionAuditDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const bindingRegistry = descriptor.permission_audit_binding_registry ?? createDmsCorePermissionAuditBindingRegistry();
  if (bindingRegistry.binding_count !== Object.keys(DMS_CORE_MODEL_DEFINITIONS).length) errors.push("CP00-200 binding count drift");
  for (const [modelType, definition] of Object.entries(DMS_CORE_MODEL_DEFINITIONS)) {
    const binding = bindingRegistry.bindings?.[modelType];
    if (!binding) {
      errors.push(`${modelType} missing CP00-200 binding`);
      continue;
    }
    for (const field of DMS_CORE_CP200_REQUIREMENTS.required_binding_fields) {
      if (!binding.required_fields.includes(field)) errors.push(`${modelType} missing required binding field ${field}`);
      if (!DMS_CORE_REQUIRED_FIELD_REGISTRY[modelType].includes(field)) errors.push(`${modelType} registry missing ${field}`);
    }
    if (binding.primary_entity_identifier !== createDmsCoreModelShapeRegistry().model_shapes[modelType].primary_entity_identifier) {
      errors.push(`${modelType} primary identifier drift`);
    }
    if (binding.tenant_scope_field !== "tenant_id") errors.push(`${modelType} tenant scope drift`);
    if (binding.matter_trace_reference !== "matter_id") errors.push(`${modelType} matter trace drift`);
    if (binding.ownership_metadata?.owner_module !== definition.owner_module) errors.push(`${modelType} ownership metadata drift`);
    if (binding.permission_binding?.envelope_field !== "permission_envelope_id") errors.push(`${modelType} permission field drift`);
    if (binding.permission_binding?.envelope_required !== true) errors.push(`${modelType} permission envelope must be required`);
    if (binding.permission_binding?.deny_over_allow !== true) errors.push(`${modelType} deny-over-allow guard missing`);
    if (binding.permission_binding?.runtime_permission_evaluated !== false) errors.push(`${modelType} must not evaluate permission runtime`);
    if (binding.permission_binding?.permission_decision_detail_exposed !== false) errors.push(`${modelType} must not expose permission detail`);
    if (binding.audit_binding?.trace_field !== "audit_trace_id") errors.push(`${modelType} audit trace field drift`);
    if (binding.audit_binding?.trace_required !== true) errors.push(`${modelType} audit trace must be required`);
    if (binding.audit_binding?.audit_runtime_executed !== false) errors.push(`${modelType} must not execute audit runtime`);
    if (binding.audit_binding?.audit_event_written !== false) errors.push(`${modelType} must not write audit events`);
    if (binding.audit_binding?.audit_event_body_exposed !== false) errors.push(`${modelType} must not expose audit event bodies`);
    if (binding.persistence_boundary?.creates_database_rows !== false) errors.push(`${modelType} must not create database rows`);
  }
  if (DMS_CORE_CP200_NO_WRITE_ATTESTATION.dispatches_permission_runtime !== false) errors.push("CP00-200 must not dispatch permission runtime");
  if (DMS_CORE_CP200_NO_WRITE_ATTESTATION.dispatches_audit_runtime !== false) errors.push("CP00-200 must not dispatch audit runtime");
  if (DMS_CORE_CP200_NO_WRITE_ATTESTATION.exposes_permission_envelope_payload !== false) {
    errors.push("CP00-200 must not expose permission envelope payload");
  }
  if (DMS_CORE_CP200_NO_WRITE_ATTESTATION.exposes_audit_trace_payload !== false) errors.push("CP00-200 must not expose audit trace payload");
  if (DMS_CORE_CP200_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-200 must not promote Claude");
  if (DMS_CORE_CP200_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-200 must not claim enterprise trust");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP200_PACK_BINDING.pack_id,
      DMS_CORE_CP201_PACK_BINDING.pack_id,
      DMS_CORE_CP202_PACK_BINDING.pack_id,
      DMS_CORE_CP203_PACK_BINDING.pack_id,
      DMS_CORE_CP204_PACK_BINDING.pack_id,
      DMS_CORE_CP205_PACK_BINDING.pack_id,
      DMS_CORE_CP206_PACK_BINDING.pack_id,
      DMS_CORE_CP207_PACK_BINDING.pack_id,
      DMS_CORE_CP208_PACK_BINDING.pack_id,
      DMS_CORE_CP209_PACK_BINDING.pack_id,
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-200 contract current_pack drift");
  }
  if (contractProjection?.permission_audit_descriptor?.descriptor && contractProjection.permission_audit_descriptor.descriptor !== descriptor.descriptor) {
    errors.push("CP00-200 contract permission_audit_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP200_PACK_BINDING.next_pack_id) errors.push("CP00-200 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP200_PACK_BINDING.next_subphase_id) errors.push("CP00-200 next subphase drift");
  return freezeCp200Validation({
    valid: errors.length === 0,
    errors: Object.freeze(errors),
    binding_count: bindingRegistry.binding_count,
  });
}

export function validateDmsCoreCp200FixtureIntegrity(fixture = createDmsCoreCp200FixtureModel()) {
  const errors = [];
  if (fixture.record_count !== Object.keys(DMS_CORE_MODEL_DEFINITIONS).length) errors.push("CP00-200 fixture record count drift");
  for (const record of fixture.records ?? []) {
    if (record.permission_envelope_id_present !== true) errors.push(`${record.model_type} fixture missing permission envelope`);
    if (record.audit_trace_id_present !== true) errors.push(`${record.model_type} fixture missing audit trace`);
    if (record.serialized_descriptor?.permission_decision_detail_included !== false) {
      errors.push(`${record.model_type} fixture leaks permission decision detail`);
    }
    if (record.serialized_descriptor?.audit_event_body_included !== false) errors.push(`${record.model_type} fixture leaks audit event body`);
    if (record.serialized_descriptor?.document_bytes_included !== false) errors.push(`${record.model_type} fixture leaks document bytes`);
    if (record.serialized_descriptor?.extracted_text_included !== false) errors.push(`${record.model_type} fixture leaks extracted text`);
    if (record.permission_binding?.runtime_permission_evaluated !== false) errors.push(`${record.model_type} fixture evaluates permission runtime`);
    if (record.audit_binding?.audit_event_written !== false) errors.push(`${record.model_type} fixture writes audit event`);
  }
  const invalidCaseIds = new Set((fixture.invalid_reference_cases ?? []).map((item) => item.case_id));
  if (!invalidCaseIds.has("missing_permission_envelope_id")) errors.push("CP00-200 missing invalid permission reference case");
  if (!invalidCaseIds.has("missing_audit_trace_id")) errors.push("CP00-200 missing invalid audit reference case");
  if (!invalidCaseIds.has("cross_tenant_reference_blocked_by_descriptor")) errors.push("CP00-200 missing cross-tenant invalid reference case");
  return freezeCp200Validation({ valid: errors.length === 0, errors: Object.freeze(errors), record_count: fixture.record_count });
}

export function createDmsCoreCp200HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp200PermissionAuditDescriptor(),
) {
  const coverage = validateDmsCoreCp200Coverage(planPack);
  const binding = validateDmsCoreCp200PermissionAuditBinding(descriptor, contractProjection);
  const fixture = validateDmsCoreCp200FixtureIntegrity(descriptor.fixture);
  return freezeCp200Validation({
    evidence_packet: "H06.CP00-200.dms_core_permission_audit_fixture_binding",
    gate: DMS_CORE_CP200_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    binding_valid: binding.valid,
    fixture_valid: fixture.valid,
    no_real_data: true,
    source_type_shape_pack_id: DMS_CORE_CP200_PACK_BINDING.upstream_pack_id,
    required_permission_guards: DMS_CORE_CP200_REQUIREMENTS.required_permission_guards,
    required_audit_guards: DMS_CORE_CP200_REQUIREMENTS.required_audit_guards,
    next_pack_id: DMS_CORE_CP200_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP200_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && binding.valid && fixture.valid,
  });
}

export function createDmsCoreCp200ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp200Coverage(planPack);
  const binding = validateDmsCoreCp200PermissionAuditBinding(createDmsCoreCp200PermissionAuditDescriptor(), {});
  const fixture = validateDmsCoreCp200FixtureIntegrity();
  return freezeCp200Validation({
    review_packet: "C06.CP00-200.dms_core_permission_audit_fixture_binding",
    gate: DMS_CORE_CP200_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP200_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    binding_valid: binding.valid,
    fixture_valid: fixture.valid,
    invalid_review_blockers: DMS_CORE_CP200_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-200 cover exactly the 40 planned RP06 units from RP06.P01.M04.S07 through RP06.P01.M06.S04?",
      "Does the DMS permission/audit binding registry require tenant_id, matter_id, permission_envelope_id, and audit_trace_id for every DMS model?",
      "Do the synthetic fixtures include invalid reference and ownership drift cases without using real client, matter, or document data?",
      "Does CP00-200 keep permission runtime, audit runtime, persistence, object storage, OCR, search, email, Citation Ledger, and Loop execution out of scope?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp200CloseoutHandoff() {
  return freezeCp200Validation({
    handoff_id: "CP00-200-to-CP00-201",
    from_pack_id: DMS_CORE_CP200_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP200_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP200_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP200_PACK_BINDING.range,
    open_scope: "Continue RP06.P01.M06.S05 onward with broader synthetic fixture/test/evidence packs while preserving descriptor-only DMS boundaries.",
    production_ready_flag: DMS_CORE_CP200_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp201CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp201Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp201Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-201 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP201_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-201");
  if (planPack?.risk_class !== DMS_CORE_CP201_PACK_BINDING.risk_class) errors.push("CP00-201 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP201_PACK_BINDING.unit_count) errors.push("CP00-201 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP201_PACK_BINDING.first_unit_id) errors.push("CP00-201 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP201_PACK_BINDING.last_unit_id) errors.push("CP00-201 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-201 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-201 must only include RP06 units");
  const summary = createDmsCoreCp201CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP201_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-201 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP201_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-201 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP201_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-201 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP201_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-201 ${microTitle} distribution drift`);
  }
  for (const title of DMS_CORE_CP201_REQUIREMENTS.required_unit_titles) {
    if (!summary.unit_titles.includes(title)) errors.push(`CP00-201 missing unit title ${title}`);
  }
  return freezeCp201Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp201ServiceContract(
  descriptor = createDmsCoreCp201ServiceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const serviceContract = descriptor.service_contract ?? createDmsCoreCp201ServiceContractDescriptor();
  const entrypoint = serviceContract.entrypoint_contract ?? {};
  for (const field of DMS_CORE_CP201_REQUIREMENTS.required_service_guards) {
    const normalizedField = field
      .replace("idempotency_descriptor_only", "idempotency_key_handling_descriptor")
      .replace("lock_descriptor_only", "lock_acquisition_rule_descriptor")
      .replace("rollback_descriptor_only", "rollback_behavior_descriptor")
      .replace("retry_descriptor_only", "retry_behavior_descriptor");
    if (normalizedField === "persistence_boundary_closed") {
      if (entrypoint.persistence_boundary_closed !== true) errors.push("CP00-201 persistence boundary must be closed");
    } else if (entrypoint[normalizedField] !== true) {
      errors.push(`CP00-201 missing service guard ${field}`);
    }
  }
  if (entrypoint.runtime_execution !== false) errors.push("CP00-201 service contract must not execute runtime");
  const typeShape = serviceContract.type_shape_definition ?? {};
  if (typeShape.primary_entity_identifier !== "document_id") errors.push("CP00-201 primary entity identifier drift");
  if (typeShape.tenant_scope_field !== "tenant_id") errors.push("CP00-201 tenant scope drift");
  if (typeShape.matter_trace_reference !== "matter_id") errors.push("CP00-201 matter trace drift");
  if (typeShape.ownership_metadata?.owner_module !== DMS_CORE_PROGRAM_CONTRACT.owner_module) errors.push("CP00-201 owner metadata drift");
  if (!typeShape.required_field_registry?.DmsDocument?.includes("permission_envelope_id")) {
    errors.push("CP00-201 required field registry missing DmsDocument permission envelope");
  }
  if (!typeShape.reference_relationship_map?.DmsDocument?.includes("current_version_id")) {
    errors.push("CP00-201 reference relationship map missing current_version_id");
  }
  const normalized = serviceContract.normalized_example_request;
  if (normalized?.tenant_boundary_precheck !== true) errors.push("CP00-201 normalized request tenant precheck drift");
  if (normalized?.matter_trace_precheck !== true) errors.push("CP00-201 normalized request matter precheck drift");
  if (normalized?.permission_precheck !== true) errors.push("CP00-201 normalized request permission precheck drift");
  if (normalized?.audit_hint_precheck !== true) errors.push("CP00-201 normalized request audit precheck drift");
  if (DMS_CORE_CP201_NO_WRITE_ATTESTATION.dispatches_dms_runtime_service !== false) errors.push("CP00-201 must not dispatch DMS runtime service");
  if (DMS_CORE_CP201_NO_WRITE_ATTESTATION.persists_idempotency_key !== false) errors.push("CP00-201 must not persist idempotency keys");
  if (DMS_CORE_CP201_NO_WRITE_ATTESTATION.acquires_runtime_lock !== false) errors.push("CP00-201 must not acquire runtime locks");
  if (DMS_CORE_CP201_NO_WRITE_ATTESTATION.performs_rollback_runtime !== false) errors.push("CP00-201 must not perform rollback runtime");
  if (DMS_CORE_CP201_NO_WRITE_ATTESTATION.performs_retry_runtime !== false) errors.push("CP00-201 must not perform retry runtime");
  if (DMS_CORE_CP201_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-201 must not promote Claude");
  if (DMS_CORE_CP201_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-201 must not claim enterprise trust");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP201_PACK_BINDING.pack_id,
      DMS_CORE_CP202_PACK_BINDING.pack_id,
      DMS_CORE_CP203_PACK_BINDING.pack_id,
      DMS_CORE_CP204_PACK_BINDING.pack_id,
      DMS_CORE_CP205_PACK_BINDING.pack_id,
      DMS_CORE_CP206_PACK_BINDING.pack_id,
      DMS_CORE_CP207_PACK_BINDING.pack_id,
      DMS_CORE_CP208_PACK_BINDING.pack_id,
      DMS_CORE_CP209_PACK_BINDING.pack_id,
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-201 contract current_pack drift");
  }
  if (contractProjection?.service_contract_descriptor?.descriptor && contractProjection.service_contract_descriptor.descriptor !== descriptor.descriptor) {
    errors.push("CP00-201 contract service_contract_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP201_PACK_BINDING.next_pack_id) errors.push("CP00-201 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP201_PACK_BINDING.next_subphase_id) errors.push("CP00-201 next subphase drift");
  return freezeCp201Validation({ valid: errors.length === 0, errors: Object.freeze(errors), operation_count: DMS_CORE_CP201_REQUIREMENTS.service_operations.length });
}

export function validateDmsCoreCp201GoldenCases(goldenCases = createDmsCoreCp201GoldenCaseSet()) {
  const errors = [];
  const records = goldenCases.records ?? [];
  if (records.length !== DMS_CORE_CP201_REQUIREMENTS.service_outcomes.length) errors.push("CP00-201 golden case count drift");
  const outcomes = new Set(records.map((record) => record.outcome));
  for (const outcome of DMS_CORE_CP201_REQUIREMENTS.service_outcomes) {
    if (!outcomes.has(outcome)) errors.push(`CP00-201 missing golden outcome ${outcome}`);
  }
  const happy = records.find((record) => record.outcome === "happy_path_descriptor");
  const denied = records.find((record) => record.outcome === "denied_path_descriptor");
  const blocked = records.find((record) => record.outcome === "blocked_claim_descriptor");
  if (happy?.prechecks_pass !== true) errors.push("CP00-201 happy path prechecks must pass");
  if (denied?.request?.tenant_boundary_precheck !== false) errors.push("CP00-201 denied path must model tenant boundary failure");
  if (blocked?.allowed_operation !== false) errors.push("CP00-201 blocked claim must model unsupported operation");
  for (const record of records) {
    if (record.writes_product_state !== false) errors.push(`${record.outcome} must not write product state`);
    if (record.creates_database_rows !== false) errors.push(`${record.outcome} must not create database rows`);
    if (record.evaluates_runtime_permission !== false) errors.push(`${record.outcome} must not evaluate runtime permission`);
    if (record.writes_audit_event !== false) errors.push(`${record.outcome} must not write audit events`);
    if (record.persists_idempotency_key !== false) errors.push(`${record.outcome} must not persist idempotency keys`);
    if (record.acquires_runtime_lock !== false) errors.push(`${record.outcome} must not acquire runtime locks`);
    if (record.persistence_boundary?.descriptor_only !== true) errors.push(`${record.outcome} persistence boundary must be descriptor-only`);
  }
  if (goldenCases.integration_smoke_case?.executes_runtime !== false) errors.push("CP00-201 integration smoke must not execute runtime");
  return freezeCp201Validation({ valid: errors.length === 0, errors: Object.freeze(errors), case_count: records.length });
}

export function createDmsCoreCp201HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp201ServiceDescriptor(),
) {
  const coverage = validateDmsCoreCp201Coverage(planPack);
  const service = validateDmsCoreCp201ServiceContract(descriptor, contractProjection);
  const goldenCases = validateDmsCoreCp201GoldenCases(descriptor.golden_cases);
  return freezeCp201Validation({
    evidence_packet: "H06.CP00-201.dms_core_service_contract_golden_case_descriptor",
    gate: DMS_CORE_CP201_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    service_contract_valid: service.valid,
    golden_cases_valid: goldenCases.valid,
    no_real_data: true,
    source_permission_audit_pack_id: DMS_CORE_CP201_PACK_BINDING.upstream_pack_id,
    required_service_guards: DMS_CORE_CP201_REQUIREMENTS.required_service_guards,
    next_pack_id: DMS_CORE_CP201_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP201_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && service.valid && goldenCases.valid,
  });
}

export function createDmsCoreCp201ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp201Coverage(planPack);
  const service = validateDmsCoreCp201ServiceContract(createDmsCoreCp201ServiceDescriptor(), {});
  const goldenCases = validateDmsCoreCp201GoldenCases();
  return freezeCp201Validation({
    review_packet: "C06.CP00-201.dms_core_service_contract_golden_case_descriptor",
    gate: DMS_CORE_CP201_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP201_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    service_contract_valid: service.valid,
    golden_cases_valid: goldenCases.valid,
    invalid_review_blockers: DMS_CORE_CP201_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-201 cover exactly the 150 planned RP06 units from RP06.P01.M06.S05 through RP06.P02.M02.S22?",
      "Does the DMS service contract model request normalization, tenant/matter/permission/audit prechecks, idempotency, lock, persistence, rollback, and retry as descriptors only?",
      "Do the golden cases include happy, denied, review-required, approval-required, blocked, rollback, retry, and integration smoke paths without runtime mutation?",
      "Does CP00-201 keep object storage, OCR, search, email, Citation Ledger, and Loop execution out of scope?",
    ]),
  });
}

export function createDmsCoreCp201CloseoutHandoff() {
  return freezeCp201Validation({
    handoff_id: "CP00-201-to-CP00-202",
    from_pack_id: DMS_CORE_CP201_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP201_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP201_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP201_PACK_BINDING.range,
    open_scope: "Continue RP06.P02.M03.S01 onward with service/domain implementation continuation while preserving no-write/no-runtime boundaries until the responsible downstream packs open them.",
    production_ready_flag: DMS_CORE_CP201_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp202CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp202Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp202Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-202 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP202_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-202");
  if (planPack?.risk_class !== DMS_CORE_CP202_PACK_BINDING.risk_class) errors.push("CP00-202 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP202_PACK_BINDING.unit_count) errors.push("CP00-202 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP202_PACK_BINDING.first_unit_id) errors.push("CP00-202 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP202_PACK_BINDING.last_unit_id) errors.push("CP00-202 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-202 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-202 must only include RP06 units");
  const summary = createDmsCoreCp202CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP202_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-202 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP202_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-202 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP202_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-202 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP202_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-202 ${microTitle} distribution drift`);
  }
  for (const title of DMS_CORE_CP202_REQUIREMENTS.required_unit_titles) {
    if (!summary.unit_titles.includes(title)) errors.push(`CP00-202 missing unit title ${title}`);
  }
  return freezeCp202Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp202RouteMatrix(routeMatrix = createDmsCoreCp202RouteMatrix()) {
  const errors = [];
  const routes = routeMatrix.route_rows ?? [];
  const routeOutcomes = new Set(routes.map((route) => route.outcome));
  for (const workflowPath of DMS_CORE_CP202_REQUIREMENTS.workflow_paths) {
    const expectedOutcome = workflowPath
      .replace("primary_happy_path", "happy_path_descriptor")
      .replace("secondary_workflow_path", "review_required_descriptor")
      .replace("review_required_route", "review_required_descriptor")
      .replace("approval_required_route", "approval_required_descriptor")
      .replace("blocked_claim_output", "blocked_claim_descriptor")
      .replace("denied_path", "denied_path_descriptor");
    if (!routeOutcomes.has(expectedOutcome)) errors.push(`CP00-202 missing route outcome ${expectedOutcome}`);
  }
  if (routeMatrix.customer_safe_errors_only !== true) errors.push("CP00-202 route matrix must use customer-safe errors");
  if (routeMatrix.path_count !== DMS_CORE_CP202_REQUIREMENTS.workflow_paths.length) errors.push("CP00-202 route path count drift");
  for (const route of routes) {
    if (route.writes_product_state !== false) errors.push(`${route.outcome} must not write product state`);
    if (route.creates_database_rows !== false) errors.push(`${route.outcome} must not create database rows`);
    if (route.evaluates_runtime_permission !== false) errors.push(`${route.outcome} must not evaluate runtime permission`);
    if (route.writes_audit_event !== false) errors.push(`${route.outcome} must not write audit events`);
    if (route.persists_idempotency_key !== false) errors.push(`${route.outcome} must not persist idempotency keys`);
    if (route.acquires_runtime_lock !== false) errors.push(`${route.outcome} must not acquire runtime locks`);
    if (route.persistence_boundary?.descriptor_only !== true) errors.push(`${route.outcome} persistence boundary must be descriptor-only`);
  }
  return freezeCp202Validation({ valid: errors.length === 0, errors: Object.freeze(errors), route_count: routes.length });
}

export function validateDmsCoreCp202WorkflowDescriptor(
  descriptor = createDmsCoreCp202WorkflowDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const routeMatrix = validateDmsCoreCp202RouteMatrix(descriptor.route_matrix);
  if (!routeMatrix.valid) errors.push(...routeMatrix.errors);
  if (descriptor.descriptor !== "DmsCoreCp202PrimarySecondaryWorkflowDescriptor") errors.push("CP00-202 descriptor type drift");
  if (descriptor.source_service_contract?.descriptor !== "DmsCoreCp201ServiceContractDescriptor") errors.push("CP00-202 source service contract drift");
  if (descriptor.state_transition_enforcement?.descriptor_only !== true) errors.push("CP00-202 state transition must be descriptor-only");
  if (descriptor.state_transition_enforcement?.runtime_state_write !== false) errors.push("CP00-202 must not write runtime state");
  if (descriptor.persistence_boundary?.descriptor_only !== true) errors.push("CP00-202 persistence boundary must be descriptor-only");
  if (descriptor.persistence_boundary?.persists_workflow_attempt !== false) errors.push("CP00-202 must not persist workflow attempts");
  if (descriptor.workflow_case_set?.integration_smoke_case?.executes_runtime !== false) errors.push("CP00-202 smoke case must not execute runtime");
  for (const field of DMS_CORE_CP202_REQUIREMENTS.required_routing_guards) {
    if (!descriptor.route_matrix?.required_routing_guards?.includes(field)) errors.push(`CP00-202 missing routing guard ${field}`);
  }
  if (DMS_CORE_CP202_NO_WRITE_ATTESTATION.dispatches_primary_workflow_runtime !== false) errors.push("CP00-202 must not dispatch primary runtime");
  if (DMS_CORE_CP202_NO_WRITE_ATTESTATION.dispatches_secondary_workflow_runtime !== false) errors.push("CP00-202 must not dispatch secondary runtime");
  if (DMS_CORE_CP202_NO_WRITE_ATTESTATION.dispatches_review_route_runtime !== false) errors.push("CP00-202 must not dispatch review runtime");
  if (DMS_CORE_CP202_NO_WRITE_ATTESTATION.dispatches_approval_route_runtime !== false) errors.push("CP00-202 must not dispatch approval runtime");
  if (DMS_CORE_CP202_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-202 must not promote Claude");
  if (DMS_CORE_CP202_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-202 must not claim enterprise trust");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP202_PACK_BINDING.pack_id,
      DMS_CORE_CP203_PACK_BINDING.pack_id,
      DMS_CORE_CP204_PACK_BINDING.pack_id,
      DMS_CORE_CP205_PACK_BINDING.pack_id,
      DMS_CORE_CP206_PACK_BINDING.pack_id,
      DMS_CORE_CP207_PACK_BINDING.pack_id,
      DMS_CORE_CP208_PACK_BINDING.pack_id,
      DMS_CORE_CP209_PACK_BINDING.pack_id,
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-202 contract current_pack drift");
  }
  if (
    contractProjection?.workflow_descriptor?.descriptor &&
    contractProjection.workflow_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-202 contract workflow_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP202_PACK_BINDING.next_pack_id) errors.push("CP00-202 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP202_PACK_BINDING.next_subphase_id) errors.push("CP00-202 next subphase drift");
  return freezeCp202Validation({ valid: errors.length === 0, errors: Object.freeze(errors), route_matrix_valid: routeMatrix.valid });
}

export function createDmsCoreCp202HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp202WorkflowDescriptor(),
) {
  const coverage = validateDmsCoreCp202Coverage(planPack);
  const workflow = validateDmsCoreCp202WorkflowDescriptor(descriptor, contractProjection);
  const routeMatrix = validateDmsCoreCp202RouteMatrix(descriptor.route_matrix);
  return freezeCp202Validation({
    evidence_packet: "H06.CP00-202.dms_core_primary_secondary_workflow_descriptor",
    gate: DMS_CORE_CP202_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    workflow_descriptor_valid: workflow.valid,
    route_matrix_valid: routeMatrix.valid,
    no_real_data: true,
    source_service_contract_pack_id: DMS_CORE_CP202_PACK_BINDING.upstream_pack_id,
    required_routing_guards: DMS_CORE_CP202_REQUIREMENTS.required_routing_guards,
    next_pack_id: DMS_CORE_CP202_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP202_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && workflow.valid && routeMatrix.valid,
  });
}

export function createDmsCoreCp202ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp202Coverage(planPack);
  const workflow = validateDmsCoreCp202WorkflowDescriptor(createDmsCoreCp202WorkflowDescriptor(), {});
  const routeMatrix = validateDmsCoreCp202RouteMatrix();
  return freezeCp202Validation({
    review_packet: "C06.CP00-202.dms_core_primary_secondary_workflow_descriptor",
    gate: DMS_CORE_CP202_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP202_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    workflow_descriptor_valid: workflow.valid,
    route_matrix_valid: routeMatrix.valid,
    invalid_review_blockers: DMS_CORE_CP202_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-202 cover exactly the 40 planned RP06 units from RP06.P02.M03.S01 through RP06.P02.M04.S15?",
      "Does the DMS workflow descriptor bind primary and secondary paths to tenant, matter, permission, audit, idempotency, lock, persistence, review, approval, rollback, and retry descriptors?",
      "Does CP00-202 keep workflow dispatch, persistence, object storage, OCR, search, email, Citation Ledger, and Loop execution out of scope?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp202CloseoutHandoff() {
  return freezeCp202Validation({
    handoff_id: "CP00-202-to-CP00-203",
    from_pack_id: DMS_CORE_CP202_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP202_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP202_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP202_PACK_BINDING.range,
    open_scope: "Continue RP06.P02.M04.S16 onward with sensitive workflow outputs and generated test/evidence tails while preserving descriptor-only DMS runtime boundaries.",
    production_ready_flag: DMS_CORE_CP202_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp203CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp203Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp203Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-203 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP203_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-203");
  if (planPack?.risk_class !== DMS_CORE_CP203_PACK_BINDING.risk_class) errors.push("CP00-203 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP203_PACK_BINDING.unit_count) errors.push("CP00-203 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP203_PACK_BINDING.first_unit_id) errors.push("CP00-203 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP203_PACK_BINDING.last_unit_id) errors.push("CP00-203 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-203 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-203 must only include RP06 units");
  const summary = createDmsCoreCp203CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP203_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-203 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP203_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-203 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP203_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-203 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP203_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-203 ${microTitle} distribution drift`);
  }
  for (const title of DMS_CORE_CP203_REQUIREMENTS.required_unit_titles) {
    if (!summary.unit_titles.includes(title)) errors.push(`CP00-203 missing unit title ${title}`);
  }
  return freezeCp203Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp203SensitiveTailDescriptor(
  descriptor = createDmsCoreCp203SensitiveTailDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.sensitive_tail_case_set ?? createDmsCoreCp203SensitiveTailCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp203SensitiveWorkflowTailDescriptor") errors.push("CP00-203 descriptor type drift");
  if (descriptor.source_workflow_descriptor !== "DmsCoreCp202PrimarySecondaryWorkflowDescriptor") {
    errors.push("CP00-203 source workflow descriptor drift");
  }
  for (const path of DMS_CORE_CP203_REQUIREMENTS.sensitive_tail_paths) {
    if (!caseSet.sensitive_tail_paths?.includes(path)) errors.push(`CP00-203 missing sensitive tail path ${path}`);
  }
  const blocked = caseSet.blocked_claim_output ?? {};
  if (blocked.customer_safe_error_code !== "DMS_SERVICE_UNSUPPORTED_OPERATION") errors.push("CP00-203 blocked claim safe code drift");
  if (blocked.details_redacted !== true) errors.push("CP00-203 blocked claim details must be redacted");
  if (blocked.permission_decision_detail_included !== false) errors.push("CP00-203 must not include permission decision detail");
  if (blocked.audit_event_body_included !== false) errors.push("CP00-203 must not include audit event body");
  if (blocked.unauthorized_count_included !== false) errors.push("CP00-203 must not include unauthorized count");
  if (blocked.raw_payload_included !== false) errors.push("CP00-203 must not include raw payload");
  if (blocked.dispatches_runtime !== false) errors.push("CP00-203 blocked claim must not dispatch runtime");
  const rollback = caseSet.rollback_behavior ?? {};
  if (rollback.rollback_runtime_executed !== false) errors.push("CP00-203 rollback runtime must not execute");
  if (rollback.state_restored_by_runtime !== false) errors.push("CP00-203 rollback must not restore runtime state");
  if (rollback.internal_state_included !== false) errors.push("CP00-203 rollback must not include internal state");
  const retry = caseSet.retry_behavior ?? {};
  if (retry.retry_runtime_executed !== false) errors.push("CP00-203 retry runtime must not execute");
  if (retry.idempotency_key_persisted !== false) errors.push("CP00-203 retry must not persist idempotency key");
  if (retry.internal_state_included !== false) errors.push("CP00-203 retry must not include internal state");
  if (caseSet.integration_smoke_case?.executes_runtime !== false) errors.push("CP00-203 smoke case must not execute runtime");
  if ((caseSet.unit_test_cases ?? []).length !== 3) errors.push("CP00-203 must include happy denied and review unit test cases");
  for (const testCase of caseSet.unit_test_cases ?? []) {
    if (testCase.executes_runtime !== false) errors.push(`${testCase.title} must not execute runtime`);
  }
  if (DMS_CORE_CP203_NO_WRITE_ATTESTATION.dispatches_blocked_claim_runtime !== false) errors.push("CP00-203 must not dispatch blocked runtime");
  if (DMS_CORE_CP203_NO_WRITE_ATTESTATION.dispatches_rollback_runtime !== false) errors.push("CP00-203 must not dispatch rollback runtime");
  if (DMS_CORE_CP203_NO_WRITE_ATTESTATION.dispatches_retry_runtime !== false) errors.push("CP00-203 must not dispatch retry runtime");
  if (DMS_CORE_CP203_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-203 must not promote Claude");
  if (DMS_CORE_CP203_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-203 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-203 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-203 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP203_PACK_BINDING.pack_id,
      DMS_CORE_CP204_PACK_BINDING.pack_id,
      DMS_CORE_CP205_PACK_BINDING.pack_id,
      DMS_CORE_CP206_PACK_BINDING.pack_id,
      DMS_CORE_CP207_PACK_BINDING.pack_id,
      DMS_CORE_CP208_PACK_BINDING.pack_id,
      DMS_CORE_CP209_PACK_BINDING.pack_id,
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-203 contract current_pack drift");
  }
  if (
    contractProjection?.sensitive_tail_descriptor?.descriptor &&
    contractProjection.sensitive_tail_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-203 contract sensitive_tail_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP203_PACK_BINDING.next_pack_id) errors.push("CP00-203 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP203_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-203 next subphase drift");
  }
  return freezeCp203Validation({ valid: errors.length === 0, errors: Object.freeze(errors), case_count: caseSet.unit_test_cases?.length ?? 0 });
}

export function createDmsCoreCp203HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp203SensitiveTailDescriptor(),
) {
  const coverage = validateDmsCoreCp203Coverage(planPack);
  const sensitiveTail = validateDmsCoreCp203SensitiveTailDescriptor(descriptor, contractProjection);
  return freezeCp203Validation({
    evidence_packet: "H06.CP00-203.dms_core_sensitive_workflow_tail_descriptor",
    gate: DMS_CORE_CP203_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    sensitive_tail_valid: sensitiveTail.valid,
    no_real_data: true,
    source_workflow_pack_id: DMS_CORE_CP203_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP203_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP203_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP203_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && sensitiveTail.valid,
  });
}

export function createDmsCoreCp203ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp203Coverage(planPack);
  const sensitiveTail = validateDmsCoreCp203SensitiveTailDescriptor(createDmsCoreCp203SensitiveTailDescriptor(), {});
  return freezeCp203Validation({
    review_packet: "C06.CP00-203.dms_core_sensitive_workflow_tail_descriptor",
    gate: DMS_CORE_CP203_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP203_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    sensitive_tail_valid: sensitiveTail.valid,
    invalid_review_blockers: DMS_CORE_CP203_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-203 cover exactly the 10 planned RP06 units from RP06.P02.M04.S16 through RP06.P02.M04.S25?",
      "Does the sensitive workflow tail redact blocked-claim details, permission decisions, audit bodies, unauthorized counts, raw payloads, rollback internals, and retry internals?",
      "Do rollback, retry, unit, smoke, Hermes, and Claude evidence remain descriptor-only without runtime dispatch or persistence?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp203CloseoutHandoff() {
  return freezeCp203Validation({
    handoff_id: "CP00-203-to-CP00-204",
    from_pack_id: DMS_CORE_CP203_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP203_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP203_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP203_PACK_BINDING.range,
    open_scope: "Continue RP06.P02.M05.S01 onward with the next Risk A DMS secondary workflow slice while keeping DMS runtime dispatch, object storage, OCR, search, email, Citation Ledger, and Loop execution out of scope.",
    production_ready_flag: DMS_CORE_CP203_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp204CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp204Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp204Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-204 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP204_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-204");
  if (planPack?.risk_class !== DMS_CORE_CP204_PACK_BINDING.risk_class) errors.push("CP00-204 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP204_PACK_BINDING.unit_count) errors.push("CP00-204 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP204_PACK_BINDING.first_unit_id) errors.push("CP00-204 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP204_PACK_BINDING.last_unit_id) errors.push("CP00-204 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-204 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-204 must only include RP06 units");
  const summary = createDmsCoreCp204CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP204_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-204 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP204_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-204 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP204_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-204 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP204_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-204 ${microTitle} distribution drift`);
  }
  for (const title of DMS_CORE_CP204_REQUIREMENTS.required_unit_titles) {
    if (!summary.unit_titles.includes(title)) errors.push(`CP00-204 missing unit title ${title}`);
  }
  return freezeCp204Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp204PermissionAuditWorkflowBinding(
  descriptor = createDmsCoreCp204PermissionAuditWorkflowBindingDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const gateSet = descriptor.permission_audit_gate_set ?? createDmsCoreCp204PermissionAuditGateSet();
  if (descriptor.descriptor !== "DmsCoreCp204PermissionAuditWorkflowBindingDescriptor") {
    errors.push("CP00-204 descriptor type drift");
  }
  if (descriptor.source_sensitive_tail_descriptor !== "DmsCoreCp203SensitiveWorkflowTailDescriptor") {
    errors.push("CP00-204 source sensitive tail descriptor drift");
  }
  if (gateSet.source_sensitive_tail_descriptor !== "DmsCoreCp203SensitiveWorkflowTailDescriptor") {
    errors.push("CP00-204 gate set source sensitive tail drift");
  }
  for (const guard of DMS_CORE_CP204_REQUIREMENTS.required_permission_audit_guards) {
    if (!gateSet.required_permission_audit_guards?.includes(guard)) errors.push(`CP00-204 missing permission audit guard ${guard}`);
  }
  const serviceEntrypoint = gateSet.service_entrypoint_contract ?? {};
  if (serviceEntrypoint.descriptor_only !== true) errors.push("CP00-204 service entrypoint must be descriptor-only");
  if (serviceEntrypoint.runtime_execution !== false) errors.push("CP00-204 service entrypoint must not execute runtime");
  if (serviceEntrypoint.permission_runtime_evaluated !== false) errors.push("CP00-204 service entrypoint must not evaluate permissions");
  if (serviceEntrypoint.audit_runtime_appended !== false) errors.push("CP00-204 service entrypoint must not append audit runtime");
  const requestNormalization = gateSet.request_normalization ?? {};
  if (requestNormalization.raw_payload_included !== false) errors.push("CP00-204 request normalization must exclude raw payload");
  if (requestNormalization.normalized_payload_excludes_raw_content !== true) {
    errors.push("CP00-204 request normalization must mark raw content excluded");
  }
  const tenant = gateSet.tenant_boundary_precheck ?? {};
  if (tenant.descriptor_only !== true) errors.push("CP00-204 tenant boundary precheck must be descriptor-only");
  if (tenant.passed !== true) errors.push("CP00-204 default tenant boundary precheck must pass");
  if (tenant.policy_rule_detail_included !== false) errors.push("CP00-204 tenant precheck must not expose policy details");
  if (tenant.unauthorized_count_included !== false) errors.push("CP00-204 tenant precheck must not expose unauthorized counts");
  const matter = gateSet.matter_trace_precheck ?? {};
  if (matter.descriptor_only !== true) errors.push("CP00-204 matter trace precheck must be descriptor-only");
  if (matter.passed !== true) errors.push("CP00-204 default matter trace precheck must pass");
  if (matter.matter_payload_included !== false) errors.push("CP00-204 matter precheck must not include matter payload");
  const permission = gateSet.permission_precheck ?? {};
  if (permission.descriptor_only !== true) errors.push("CP00-204 permission precheck must be descriptor-only");
  if (permission.passed !== true) errors.push("CP00-204 default permission precheck must pass");
  if (permission.permission_policy_rule_detail_included !== false) errors.push("CP00-204 permission precheck must not expose policy rule detail");
  if (permission.permission_decision_detail_included !== false) errors.push("CP00-204 permission precheck must not expose decision detail");
  if (permission.permission_envelope_payload_included !== false) errors.push("CP00-204 permission precheck must not expose envelope payload");
  if (permission.runtime_permission_evaluated !== false) errors.push("CP00-204 permission precheck must not evaluate runtime permission");
  if (permission.writes_permission_decision !== false) errors.push("CP00-204 permission precheck must not write decisions");
  const audit = gateSet.audit_hint_precheck ?? {};
  if (audit.descriptor_only !== true) errors.push("CP00-204 audit hint precheck must be descriptor-only");
  if (audit.passed !== true) errors.push("CP00-204 default audit hint precheck must pass");
  if (audit.audit_hint_detail_included !== false) errors.push("CP00-204 audit precheck must not expose hint detail");
  if (audit.audit_event_body_included !== false) errors.push("CP00-204 audit precheck must not expose event body");
  if (audit.audit_trace_payload_included !== false) errors.push("CP00-204 audit precheck must not expose trace payload");
  if (audit.audit_runtime_appended !== false) errors.push("CP00-204 audit precheck must not append runtime audit");
  const primary = gateSet.primary_happy_path ?? {};
  if (primary.prechecks_pass !== true) errors.push("CP00-204 primary happy path prechecks must pass");
  if (primary.permission_gate_passed !== true) errors.push("CP00-204 primary happy path permission gate must pass");
  if (primary.audit_gate_passed !== true) errors.push("CP00-204 primary happy path audit gate must pass");
  if (primary.dispatches_runtime !== false) errors.push("CP00-204 primary happy path must not dispatch runtime");
  const secondary = gateSet.secondary_workflow_path ?? {};
  if ((secondary.source_outcome_ids ?? []).length < 1) errors.push("CP00-204 secondary workflow path must reference source outcomes");
  if (secondary.review_required_descriptor !== true) errors.push("CP00-204 secondary workflow must retain review descriptor");
  if (secondary.approval_required_descriptor !== true) errors.push("CP00-204 secondary workflow must retain approval descriptor");
  if (secondary.permission_gate_descriptor_only !== true) errors.push("CP00-204 secondary permission gate must be descriptor-only");
  if (secondary.audit_gate_descriptor_only !== true) errors.push("CP00-204 secondary audit gate must be descriptor-only");
  if (secondary.dispatches_runtime !== false) errors.push("CP00-204 secondary workflow must not dispatch runtime");
  const stateTransition = gateSet.state_transition_enforcement ?? {};
  if (stateTransition.descriptor_only !== true) errors.push("CP00-204 state transition enforcement must be descriptor-only");
  if (stateTransition.runtime_state_write !== false) errors.push("CP00-204 state transition must not write runtime state");
  const idempotency = gateSet.idempotency_key_handling ?? {};
  if (idempotency.descriptor_only !== true) errors.push("CP00-204 idempotency handling must be descriptor-only");
  if (idempotency.persists_idempotency_key !== false) errors.push("CP00-204 idempotency handling must not persist keys");
  if (idempotency.exposes_key_material !== false) errors.push("CP00-204 idempotency handling must not expose key material");
  if (DMS_CORE_CP204_NO_WRITE_ATTESTATION.dispatches_permission_audit_gate_runtime !== false) {
    errors.push("CP00-204 must not dispatch permission/audit gate runtime");
  }
  if (DMS_CORE_CP204_NO_WRITE_ATTESTATION.evaluates_authorization_policy_runtime !== false) {
    errors.push("CP00-204 must not evaluate authorization policy runtime");
  }
  if (DMS_CORE_CP204_NO_WRITE_ATTESTATION.writes_permission_decision !== false) {
    errors.push("CP00-204 must not write permission decisions");
  }
  if (DMS_CORE_CP204_NO_WRITE_ATTESTATION.appends_audit_trace !== false) errors.push("CP00-204 must not append audit trace");
  if (DMS_CORE_CP204_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-204 must not promote Claude");
  if (DMS_CORE_CP204_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-204 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-204 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-204 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP204_PACK_BINDING.pack_id,
      DMS_CORE_CP205_PACK_BINDING.pack_id,
      DMS_CORE_CP206_PACK_BINDING.pack_id,
      DMS_CORE_CP207_PACK_BINDING.pack_id,
      DMS_CORE_CP208_PACK_BINDING.pack_id,
      DMS_CORE_CP209_PACK_BINDING.pack_id,
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-204 contract current_pack drift");
  }
  if (
    contractProjection?.permission_audit_workflow_binding_descriptor?.descriptor &&
    contractProjection.permission_audit_workflow_binding_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-204 contract permission_audit_workflow_binding_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP204_PACK_BINDING.next_pack_id) errors.push("CP00-204 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP204_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-204 next subphase drift");
  }
  return freezeCp204Validation({
    valid: errors.length === 0,
    errors: Object.freeze(errors),
    guard_count: DMS_CORE_CP204_REQUIREMENTS.required_permission_audit_guards.length,
  });
}

export function createDmsCoreCp204HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp204PermissionAuditWorkflowBindingDescriptor(),
) {
  const coverage = validateDmsCoreCp204Coverage(planPack);
  const permissionAuditWorkflow = validateDmsCoreCp204PermissionAuditWorkflowBinding(descriptor, contractProjection);
  return freezeCp204Validation({
    evidence_packet: "H06.CP00-204.dms_core_permission_audit_workflow_binding",
    gate: DMS_CORE_CP204_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    permission_audit_workflow_binding_valid: permissionAuditWorkflow.valid,
    no_real_data: true,
    source_sensitive_tail_pack_id: DMS_CORE_CP204_PACK_BINDING.upstream_pack_id,
    required_permission_audit_guards: DMS_CORE_CP204_REQUIREMENTS.required_permission_audit_guards,
    required_no_leak_guards: DMS_CORE_CP204_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP204_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP204_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && permissionAuditWorkflow.valid,
  });
}

export function createDmsCoreCp204ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp204Coverage(planPack);
  const permissionAuditWorkflow = validateDmsCoreCp204PermissionAuditWorkflowBinding(
    createDmsCoreCp204PermissionAuditWorkflowBindingDescriptor(),
    {},
  );
  return freezeCp204Validation({
    review_packet: "C06.CP00-204.dms_core_permission_audit_workflow_binding",
    gate: DMS_CORE_CP204_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP204_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    permission_audit_workflow_binding_valid: permissionAuditWorkflow.valid,
    invalid_review_blockers: DMS_CORE_CP204_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-204 cover exactly the 10 planned RP06 units from RP06.P02.M05.S01 through RP06.P02.M05.S10?",
      "Does the permission/audit workflow binding model service entrypoint, request normalization, tenant/matter/permission/audit prechecks, primary and secondary workflow paths, state transition enforcement, and idempotency as descriptors only?",
      "Does CP00-204 avoid runtime permission evaluation, permission decision writes, audit appends, raw payload exposure, policy rule detail exposure, audit hint detail exposure, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp204CloseoutHandoff() {
  return freezeCp204Validation({
    handoff_id: "CP00-204-to-CP00-205",
    from_pack_id: DMS_CORE_CP204_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP204_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP204_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP204_PACK_BINDING.range,
    open_scope: "Continue RP06.P02.M05.S11 onward with lock, persistence boundary, validation mapping, review/approval routing, blocked path, rollback, retry, and Hermes/Claude tail evidence while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP204_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp205CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp205Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp205Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-205 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP205_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-205");
  if (planPack?.risk_class !== DMS_CORE_CP205_PACK_BINDING.risk_class) errors.push("CP00-205 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP205_PACK_BINDING.unit_count) errors.push("CP00-205 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP205_PACK_BINDING.first_unit_id) errors.push("CP00-205 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP205_PACK_BINDING.last_unit_id) errors.push("CP00-205 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-205 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-205 must only include RP06 units");
  const summary = createDmsCoreCp205CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP205_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-205 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP205_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-205 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP205_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-205 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP205_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-205 ${microTitle} distribution drift`);
  }
  for (const title of DMS_CORE_CP205_REQUIREMENTS.required_unit_titles) {
    if (!summary.unit_titles.includes(title)) errors.push(`CP00-205 missing unit title ${title}`);
  }
  return freezeCp205Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp205PermissionAuditTailDescriptor(
  descriptor = createDmsCoreCp205PermissionAuditTailDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.permission_audit_tail_case_set ?? createDmsCoreCp205PermissionAuditTailCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp205PermissionAuditTailDescriptor") errors.push("CP00-205 descriptor type drift");
  if (descriptor.source_permission_audit_workflow_descriptor !== "DmsCoreCp204PermissionAuditWorkflowBindingDescriptor") {
    errors.push("CP00-205 source permission/audit workflow descriptor drift");
  }
  for (const guard of DMS_CORE_CP205_REQUIREMENTS.required_permission_audit_tail_guards) {
    if (!caseSet.required_permission_audit_tail_guards?.includes(guard)) errors.push(`CP00-205 missing tail guard ${guard}`);
  }
  const lock = caseSet.lock_acquisition_rule ?? {};
  if (lock.descriptor_only !== true) errors.push("CP00-205 lock rule must be descriptor-only");
  if (lock.runtime_lock_acquired !== false) errors.push("CP00-205 must not acquire runtime locks");
  if (lock.lock_token_included !== false) errors.push("CP00-205 must not include lock tokens");
  if (lock.lock_wait_queue_executed !== false) errors.push("CP00-205 must not execute lock wait queues");
  const persistence = caseSet.persistence_boundary ?? {};
  if (persistence.descriptor_only !== true) errors.push("CP00-205 persistence boundary must be descriptor-only");
  if (persistence.persists_workflow_attempt !== false) errors.push("CP00-205 must not persist workflow attempts");
  if (persistence.persists_idempotency_key !== false) errors.push("CP00-205 must not persist idempotency keys");
  if (persistence.creates_database_rows !== false) errors.push("CP00-205 must not create database rows");
  if (persistence.updates_database_rows !== false) errors.push("CP00-205 must not update database rows");
  if (persistence.persistence_payload_included !== false) errors.push("CP00-205 must not include persistence payloads");
  if (persistence.raw_payload_included !== false) errors.push("CP00-205 must not include raw payloads");
  const mapping = caseSet.validation_error_mapping ?? {};
  if (mapping.descriptor_only !== true) errors.push("CP00-205 validation mapping must be descriptor-only");
  if (mapping.customer_safe_errors_only !== true) errors.push("CP00-205 validation mapping must be customer-safe");
  if (mapping.validation_error_detail_included !== false) errors.push("CP00-205 must not include validation error detail");
  if (mapping.permission_decision_detail_included !== false) errors.push("CP00-205 must not include permission decision detail in validation mapping");
  if (mapping.audit_event_body_included !== false) errors.push("CP00-205 must not include audit body in validation mapping");
  if (mapping.raw_payload_included !== false) errors.push("CP00-205 validation mapping must exclude raw payload");
  if ((mapping.mappings ?? []).length < 5) errors.push("CP00-205 validation mapping must cover tenant/matter/permission/audit/unsupported operation");
  const review = caseSet.review_required_routing ?? {};
  if (review.descriptor_only !== true) errors.push("CP00-205 review routing must be descriptor-only");
  if (review.review_required !== true) errors.push("CP00-205 review routing must retain review-required state");
  if (review.read_only_claude_review_packet_only !== true) errors.push("CP00-205 review routing must be read-only review packet only");
  if (review.dispatches_review_route_runtime !== false) errors.push("CP00-205 must not dispatch review route runtime");
  const approval = caseSet.approval_required_routing ?? {};
  if (approval.descriptor_only !== true) errors.push("CP00-205 approval routing must be descriptor-only");
  if (approval.approval_required !== true) errors.push("CP00-205 approval routing must retain approval-required state");
  if (approval.human_approval_route_required_before_runtime !== true) errors.push("CP00-205 human approval route boundary missing");
  if (approval.dispatches_approval_route_runtime !== false) errors.push("CP00-205 must not dispatch approval route runtime");
  const blocked = caseSet.blocked_claim_output ?? {};
  if (blocked.customer_safe_error_code !== "DMS_SERVICE_UNSUPPORTED_OPERATION") errors.push("CP00-205 blocked claim safe code drift");
  if (blocked.details_redacted !== true) errors.push("CP00-205 blocked claim details must be redacted");
  if (blocked.validation_error_detail_included !== false) errors.push("CP00-205 blocked claim must not include validation details");
  if (blocked.permission_decision_detail_included !== false) errors.push("CP00-205 blocked claim must not include permission decisions");
  if (blocked.audit_event_body_included !== false) errors.push("CP00-205 blocked claim must not include audit bodies");
  if (blocked.unauthorized_count_included !== false) errors.push("CP00-205 blocked claim must not include unauthorized counts");
  if (blocked.raw_payload_included !== false) errors.push("CP00-205 blocked claim must not include raw payloads");
  if (blocked.dispatches_runtime !== false) errors.push("CP00-205 blocked claim must not dispatch runtime");
  const rollback = caseSet.rollback_behavior ?? {};
  if (rollback.rollback_runtime_executed !== false) errors.push("CP00-205 rollback runtime must not execute");
  if (rollback.state_restored_by_runtime !== false) errors.push("CP00-205 rollback must not restore runtime state");
  if (rollback.lock_release_runtime_executed !== false) errors.push("CP00-205 rollback must not release runtime locks");
  if (rollback.persistence_rollback_payload_included !== false) errors.push("CP00-205 rollback must not include persistence payload");
  if (rollback.internal_state_included !== false) errors.push("CP00-205 rollback must not include internal state");
  const retry = caseSet.retry_behavior ?? {};
  if (retry.retry_runtime_executed !== false) errors.push("CP00-205 retry runtime must not execute");
  if (retry.retry_backoff_runtime_executed !== false) errors.push("CP00-205 retry backoff runtime must not execute");
  if (retry.idempotency_key_persisted !== false) errors.push("CP00-205 retry must not persist idempotency keys");
  if (retry.lock_reacquire_runtime_executed !== false) errors.push("CP00-205 retry must not reacquire runtime locks");
  if (retry.internal_state_included !== false) errors.push("CP00-205 retry must not include internal state");
  const happy = caseSet.happy_path_unit_descriptor ?? {};
  if (happy.expected_prechecks_pass !== true) errors.push("CP00-205 happy unit descriptor must expect passing prechecks");
  if (happy.expected_lock_descriptor_only !== true) errors.push("CP00-205 happy unit descriptor must keep lock descriptor-only");
  if (happy.expected_persistence_descriptor_only !== true) errors.push("CP00-205 happy unit descriptor must keep persistence descriptor-only");
  if (happy.executes_runtime !== false) errors.push("CP00-205 happy unit descriptor must not execute runtime");
  const denied = caseSet.denied_path_unit_descriptor ?? {};
  if (denied.expected_tenant_boundary_precheck !== false) errors.push("CP00-205 denied unit descriptor must model tenant denial");
  if (denied.expected_customer_safe_error_code !== "DMS_SERVICE_TENANT_BOUNDARY") errors.push("CP00-205 denied unit safe error drift");
  if (denied.expected_permission_detail_excluded !== true) errors.push("CP00-205 denied unit must exclude permission details");
  if (denied.expected_audit_body_excluded !== true) errors.push("CP00-205 denied unit must exclude audit bodies");
  if (denied.executes_runtime !== false) errors.push("CP00-205 denied unit descriptor must not execute runtime");
  if (DMS_CORE_CP205_NO_WRITE_ATTESTATION.acquires_runtime_lock !== false) errors.push("CP00-205 must not acquire runtime locks");
  if (DMS_CORE_CP205_NO_WRITE_ATTESTATION.persists_workflow_attempt !== false) errors.push("CP00-205 must not persist workflow attempts");
  if (DMS_CORE_CP205_NO_WRITE_ATTESTATION.dispatches_review_route_runtime !== false) errors.push("CP00-205 must not dispatch review runtime");
  if (DMS_CORE_CP205_NO_WRITE_ATTESTATION.dispatches_approval_route_runtime !== false) errors.push("CP00-205 must not dispatch approval runtime");
  if (DMS_CORE_CP205_NO_WRITE_ATTESTATION.dispatches_blocked_claim_runtime !== false) errors.push("CP00-205 must not dispatch blocked runtime");
  if (DMS_CORE_CP205_NO_WRITE_ATTESTATION.performs_rollback_runtime !== false) errors.push("CP00-205 must not perform rollback runtime");
  if (DMS_CORE_CP205_NO_WRITE_ATTESTATION.performs_retry_runtime !== false) errors.push("CP00-205 must not perform retry runtime");
  if (DMS_CORE_CP205_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-205 must not promote Claude");
  if (DMS_CORE_CP205_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-205 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-205 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-205 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP205_PACK_BINDING.pack_id,
      DMS_CORE_CP206_PACK_BINDING.pack_id,
      DMS_CORE_CP207_PACK_BINDING.pack_id,
      DMS_CORE_CP208_PACK_BINDING.pack_id,
      DMS_CORE_CP209_PACK_BINDING.pack_id,
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-205 contract current_pack drift");
  }
  if (
    contractProjection?.permission_audit_tail_descriptor?.descriptor &&
    contractProjection.permission_audit_tail_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-205 contract permission_audit_tail_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP205_PACK_BINDING.next_pack_id) errors.push("CP00-205 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP205_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-205 next subphase drift");
  }
  return freezeCp205Validation({
    valid: errors.length === 0,
    errors: Object.freeze(errors),
    guard_count: DMS_CORE_CP205_REQUIREMENTS.required_permission_audit_tail_guards.length,
  });
}

export function createDmsCoreCp205HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp205PermissionAuditTailDescriptor(),
) {
  const coverage = validateDmsCoreCp205Coverage(planPack);
  const tail = validateDmsCoreCp205PermissionAuditTailDescriptor(descriptor, contractProjection);
  return freezeCp205Validation({
    evidence_packet: "H06.CP00-205.dms_core_permission_audit_tail_descriptor",
    gate: DMS_CORE_CP205_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    permission_audit_tail_valid: tail.valid,
    no_real_data: true,
    source_permission_audit_workflow_pack_id: DMS_CORE_CP205_PACK_BINDING.upstream_pack_id,
    required_permission_audit_tail_guards: DMS_CORE_CP205_REQUIREMENTS.required_permission_audit_tail_guards,
    required_no_leak_guards: DMS_CORE_CP205_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP205_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP205_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && tail.valid,
  });
}

export function createDmsCoreCp205ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp205Coverage(planPack);
  const tail = validateDmsCoreCp205PermissionAuditTailDescriptor(createDmsCoreCp205PermissionAuditTailDescriptor(), {});
  return freezeCp205Validation({
    review_packet: "C06.CP00-205.dms_core_permission_audit_tail_descriptor",
    gate: DMS_CORE_CP205_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP205_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    permission_audit_tail_valid: tail.valid,
    invalid_review_blockers: DMS_CORE_CP205_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-205 cover exactly the 10 planned RP06 units from RP06.P02.M05.S11 through RP06.P02.M05.S20?",
      "Does the permission/audit tail model lock acquisition, persistence boundary, validation error mapping, review/approval routing, blocked claim output, rollback, retry, happy unit, and denied unit descriptors without runtime mutation?",
      "Does CP00-205 avoid runtime lock acquisition, persistence, review/approval dispatch, blocked dispatch, rollback/retry runtime, raw payloads, validation details, lock tokens, permission decisions, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp205CloseoutHandoff() {
  return freezeCp205Validation({
    handoff_id: "CP00-205-to-CP00-206",
    from_pack_id: DMS_CORE_CP205_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP205_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP205_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP205_PACK_BINDING.range,
    open_scope: "Continue RP06.P02.M05.S21 onward with remaining permission/audit test, smoke, fixture, Hermes evidence, Claude review, and handoff rows while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP205_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp206CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp206Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp206Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-206 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP206_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-206");
  if (planPack?.risk_class !== DMS_CORE_CP206_PACK_BINDING.risk_class) errors.push("CP00-206 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP206_PACK_BINDING.unit_count) errors.push("CP00-206 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP206_PACK_BINDING.first_unit_id) errors.push("CP00-206 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP206_PACK_BINDING.last_unit_id) errors.push("CP00-206 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-206 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-206 must only include RP06 units");
  const summary = createDmsCoreCp206CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP206_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-206 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP206_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-206 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP206_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-206 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP206_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-206 ${microTitle} distribution drift`);
  }
  for (const title of DMS_CORE_CP206_REQUIREMENTS.required_unit_titles) {
    if (!summary.unit_titles.includes(title)) errors.push(`CP00-206 missing unit title ${title}`);
  }
  return freezeCp206Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp206SyntheticFixtureServiceDescriptor(
  descriptor = createDmsCoreCp206SyntheticFixtureServiceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.synthetic_fixture_service_case_set ?? createDmsCoreCp206SyntheticFixtureServiceCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp206SyntheticFixtureServiceDescriptor") errors.push("CP00-206 descriptor type drift");
  if (descriptor.source_permission_audit_tail_descriptor !== "DmsCoreCp205PermissionAuditTailDescriptor") {
    errors.push("CP00-206 source permission/audit tail descriptor drift");
  }
  for (const guard of DMS_CORE_CP206_REQUIREMENTS.required_synthetic_fixture_service_guards) {
    if (!caseSet.required_synthetic_fixture_service_guards?.includes(guard)) errors.push(`CP00-206 missing service guard ${guard}`);
  }
  const review = caseSet.review_path_unit_descriptor ?? {};
  if (review.descriptor_only !== true) errors.push("CP00-206 review path unit must be descriptor-only");
  if (review.review_required !== true) errors.push("CP00-206 review path must preserve review-required state");
  if (review.read_only_claude_review_packet_only !== true) errors.push("CP00-206 review path must use read-only review packet");
  if (review.dispatches_review_route_runtime !== false) errors.push("CP00-206 must not dispatch review route runtime");
  if (review.executes_review_path_runtime !== false) errors.push("CP00-206 must not execute review path runtime");
  if (review.review_payload_included !== false) errors.push("CP00-206 must not include review payload");
  if (review.permission_decision_detail_included !== false) errors.push("CP00-206 must not expose permission detail in review path");
  if (review.audit_event_body_included !== false) errors.push("CP00-206 must not expose audit body in review path");
  const smoke = caseSet.integration_smoke_case_descriptor ?? {};
  if (smoke.descriptor_only !== true) errors.push("CP00-206 smoke case must be descriptor-only");
  if (smoke.invokes_service_entrypoint_runtime !== false) errors.push("CP00-206 must not invoke service entrypoint runtime");
  if (smoke.dispatches_integration_smoke_runtime !== false) errors.push("CP00-206 must not dispatch smoke runtime");
  if (smoke.writes_product_state !== false) errors.push("CP00-206 smoke must not write product state");
  if (smoke.creates_database_rows !== false) errors.push("CP00-206 smoke must not create database rows");
  if (smoke.reads_object_storage !== false || smoke.writes_object_storage !== false) errors.push("CP00-206 smoke must not touch object storage");
  const fixture = caseSet.golden_fixture_binding ?? {};
  if (fixture.descriptor_only !== true) errors.push("CP00-206 golden fixture binding must be descriptor-only");
  if (fixture.synthetic_only !== true || fixture.no_real_data !== true) errors.push("CP00-206 fixture binding must be synthetic-only");
  if (fixture.real_client_data_loaded !== false) errors.push("CP00-206 fixture must not load real client data");
  if (fixture.real_matter_data_loaded !== false) errors.push("CP00-206 fixture must not load real matter data");
  if (fixture.real_document_data_loaded !== false) errors.push("CP00-206 fixture must not load real document data");
  if (fixture.document_bytes_loaded !== false) errors.push("CP00-206 fixture must not load document bytes");
  if (fixture.extracted_text_loaded !== false) errors.push("CP00-206 fixture must not load extracted text");
  if (fixture.fixture_payload_included !== false) errors.push("CP00-206 fixture must not include payload");
  const hermes = caseSet.hermes_service_evidence_descriptor ?? {};
  if (hermes.descriptor_only !== true) errors.push("CP00-206 Hermes evidence must be descriptor-only");
  if (hermes.dispatches_runtime !== false) errors.push("CP00-206 Hermes evidence must not dispatch runtime");
  if (hermes.claims_enterprise_trust !== false) errors.push("CP00-206 Hermes evidence must not claim enterprise trust");
  if (hermes.hermes_runtime_embedded_in_product !== false) errors.push("CP00-206 must not embed Hermes runtime in product");
  const claude = caseSet.claude_service_review_prompt ?? {};
  if (claude.read_only !== true) errors.push("CP00-206 Claude prompt must be read-only");
  if (claude.claude_is_final_approval !== false) errors.push("CP00-206 Claude prompt must not claim final approval");
  if (claude.prompt_embeds_full_source_or_diff !== false) errors.push("CP00-206 prompt must not embed full source or full diff");
  if (claude.source_mutation_allowed !== false) errors.push("CP00-206 prompt must not allow source mutation");
  if (claude.enterprise_trust_claim_allowed !== false) errors.push("CP00-206 prompt must not allow enterprise trust claims");
  const entrypoint = caseSet.synthetic_fixture_service_entrypoint_contract ?? {};
  if (entrypoint.descriptor_only !== true) errors.push("CP00-206 service entrypoint must be descriptor-only");
  if (entrypoint.runtime_execution !== false) errors.push("CP00-206 service entrypoint must not execute runtime");
  if (entrypoint.fixture_service_runtime_dispatched !== false) errors.push("CP00-206 must not dispatch fixture service runtime");
  if (entrypoint.permission_runtime_evaluated !== false) errors.push("CP00-206 entrypoint must not evaluate runtime permission");
  if (entrypoint.audit_runtime_appended !== false) errors.push("CP00-206 entrypoint must not append runtime audit");
  if (entrypoint.object_storage_runtime_executed !== false) errors.push("CP00-206 entrypoint must not execute object storage runtime");
  const normalization = caseSet.synthetic_fixture_request_normalization ?? {};
  if (normalization.raw_payload_included !== false) errors.push("CP00-206 normalization must exclude raw payload");
  if (normalization.normalized_payload_excludes_raw_content !== true) errors.push("CP00-206 normalization must exclude raw content");
  if (normalization.fixture_payload_included !== false) errors.push("CP00-206 normalization must not include fixture payload");
  if (normalization.document_bytes_included !== false) errors.push("CP00-206 normalization must not include document bytes");
  if (normalization.extracted_text_included !== false) errors.push("CP00-206 normalization must not include extracted text");
  const tenant = caseSet.synthetic_fixture_tenant_boundary_precheck ?? {};
  if (tenant.descriptor_only !== true) errors.push("CP00-206 tenant precheck must be descriptor-only");
  if (tenant.passed !== true) errors.push("CP00-206 tenant precheck must pass for synthetic fixture");
  if (tenant.runtime_policy_lookup !== false) errors.push("CP00-206 tenant precheck must not perform runtime policy lookup");
  if (tenant.tenant_policy_detail_included !== false) errors.push("CP00-206 tenant precheck must not expose policy detail");
  const matter = caseSet.synthetic_fixture_matter_trace_precheck ?? {};
  if (matter.descriptor_only !== true) errors.push("CP00-206 matter precheck must be descriptor-only");
  if (matter.passed !== true) errors.push("CP00-206 matter precheck must pass for synthetic fixture");
  if (matter.runtime_matter_lookup !== false) errors.push("CP00-206 matter precheck must not perform runtime matter lookup");
  if (matter.matter_payload_included !== false) errors.push("CP00-206 matter precheck must not include matter payload");
  const permission = caseSet.synthetic_fixture_permission_precheck ?? {};
  if (permission.descriptor_only !== true) errors.push("CP00-206 permission precheck must be descriptor-only");
  if (permission.passed !== true) errors.push("CP00-206 permission precheck must pass for synthetic fixture");
  if (permission.runtime_permission_evaluated !== false) errors.push("CP00-206 permission precheck must not evaluate runtime permission");
  if (permission.writes_permission_decision !== false) errors.push("CP00-206 permission precheck must not write permission decisions");
  if (permission.permission_decision_detail_included !== false) errors.push("CP00-206 permission precheck must not expose decision detail");
  if (permission.permission_envelope_payload_included !== false) errors.push("CP00-206 permission precheck must not expose envelope payload");
  if (DMS_CORE_CP206_NO_WRITE_ATTESTATION.dispatches_integration_smoke_runtime !== false) errors.push("CP00-206 must not dispatch smoke runtime");
  if (DMS_CORE_CP206_NO_WRITE_ATTESTATION.dispatches_fixture_service_runtime !== false) errors.push("CP00-206 must not dispatch fixture service runtime");
  if (DMS_CORE_CP206_NO_WRITE_ATTESTATION.executes_review_path_runtime !== false) errors.push("CP00-206 must not execute review path runtime");
  if (DMS_CORE_CP206_NO_WRITE_ATTESTATION.loads_real_fixture_data !== false) errors.push("CP00-206 must not load real fixture data");
  if (DMS_CORE_CP206_NO_WRITE_ATTESTATION.exposes_review_payload !== false) errors.push("CP00-206 must not expose review payload");
  if (DMS_CORE_CP206_NO_WRITE_ATTESTATION.exposes_fixture_payload !== false) errors.push("CP00-206 must not expose fixture payload");
  if (DMS_CORE_CP206_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-206 must not promote Claude");
  if (DMS_CORE_CP206_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-206 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-206 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-206 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP206_PACK_BINDING.pack_id,
      DMS_CORE_CP207_PACK_BINDING.pack_id,
      DMS_CORE_CP208_PACK_BINDING.pack_id,
      DMS_CORE_CP209_PACK_BINDING.pack_id,
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-206 contract current_pack drift");
  }
  if (
    contractProjection?.synthetic_fixture_service_descriptor?.descriptor &&
    contractProjection.synthetic_fixture_service_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-206 contract synthetic_fixture_service_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP206_PACK_BINDING.next_pack_id) errors.push("CP00-206 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP206_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-206 next subphase drift");
  }
  return freezeCp206Validation({
    valid: errors.length === 0,
    errors: Object.freeze(errors),
    guard_count: DMS_CORE_CP206_REQUIREMENTS.required_synthetic_fixture_service_guards.length,
  });
}

export function createDmsCoreCp206HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp206SyntheticFixtureServiceDescriptor(),
) {
  const coverage = validateDmsCoreCp206Coverage(planPack);
  const service = validateDmsCoreCp206SyntheticFixtureServiceDescriptor(descriptor, contractProjection);
  return freezeCp206Validation({
    evidence_packet: "H06.CP00-206.dms_core_synthetic_fixture_service_descriptor",
    gate: DMS_CORE_CP206_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    synthetic_fixture_service_valid: service.valid,
    no_real_data: true,
    source_permission_audit_tail_pack_id: DMS_CORE_CP206_PACK_BINDING.upstream_pack_id,
    required_synthetic_fixture_service_guards: DMS_CORE_CP206_REQUIREMENTS.required_synthetic_fixture_service_guards,
    required_no_leak_guards: DMS_CORE_CP206_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP206_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP206_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && service.valid,
  });
}

export function createDmsCoreCp206ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp206Coverage(planPack);
  const service = validateDmsCoreCp206SyntheticFixtureServiceDescriptor(createDmsCoreCp206SyntheticFixtureServiceDescriptor(), {});
  return freezeCp206Validation({
    review_packet: "C06.CP00-206.dms_core_synthetic_fixture_service_descriptor",
    gate: DMS_CORE_CP206_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP206_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    synthetic_fixture_service_valid: service.valid,
    invalid_review_blockers: DMS_CORE_CP206_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-206 cover exactly the 10 planned RP06 units from RP06.P02.M05.S21 through RP06.P02.M06.S05?",
      "Does the synthetic fixture service descriptor cover review path unit, integration smoke, golden fixture binding, Hermes evidence, Claude prompt, service entrypoint, request normalization, tenant boundary, matter trace, and permission precheck descriptors?",
      "Does CP00-206 avoid runtime service dispatch, integration smoke runtime, review route runtime, fixture payload exposure, real fixture data, raw payloads, permission details, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp206CloseoutHandoff() {
  return freezeCp206Validation({
    handoff_id: "CP00-206-to-CP00-207",
    from_pack_id: DMS_CORE_CP206_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP206_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP206_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP206_PACK_BINDING.range,
    open_scope: "Continue RP06.P02.M06.S06 onward with audit hint precheck, fixture happy/denied paths, state transition, idempotency, lock, persistence, validation mapping, review/approval routing, and failure descriptors while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP206_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp207CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp207Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp207Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-207 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP207_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-207");
  if (planPack?.risk_class !== DMS_CORE_CP207_PACK_BINDING.risk_class) errors.push("CP00-207 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP207_PACK_BINDING.unit_count) errors.push("CP00-207 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP207_PACK_BINDING.first_unit_id) errors.push("CP00-207 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP207_PACK_BINDING.last_unit_id) errors.push("CP00-207 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-207 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-207 must only include RP06 units");
  const summary = createDmsCoreCp207CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP207_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-207 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP207_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-207 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP207_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-207 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP207_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-207 ${microTitle} distribution drift`);
  }
  for (const title of DMS_CORE_CP207_REQUIREMENTS.required_unit_titles) {
    if (!summary.unit_titles.includes(title)) errors.push(`CP00-207 missing unit title ${title}`);
  }
  return freezeCp207Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp207SyntheticFixtureWorkflowDescriptor(
  descriptor = createDmsCoreCp207SyntheticFixtureWorkflowDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.synthetic_fixture_workflow_case_set ?? createDmsCoreCp207SyntheticFixtureWorkflowCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp207SyntheticFixtureWorkflowDescriptor") errors.push("CP00-207 descriptor type drift");
  if (descriptor.source_synthetic_fixture_service_descriptor !== "DmsCoreCp206SyntheticFixtureServiceDescriptor") {
    errors.push("CP00-207 source synthetic fixture service descriptor drift");
  }
  for (const guard of DMS_CORE_CP207_REQUIREMENTS.required_synthetic_fixture_workflow_guards) {
    if (!caseSet.required_synthetic_fixture_workflow_guards?.includes(guard)) errors.push(`CP00-207 missing workflow guard ${guard}`);
  }
  const audit = caseSet.audit_hint_precheck ?? {};
  if (audit.descriptor_only !== true) errors.push("CP00-207 audit hint precheck must be descriptor-only");
  if (audit.passed !== true) errors.push("CP00-207 audit hint precheck must pass for synthetic fixture");
  if (audit.runtime_audit_lookup !== false) errors.push("CP00-207 audit hint must not perform runtime lookup");
  if (audit.audit_hint_detail_included !== false) errors.push("CP00-207 audit hint must not expose detail");
  if (audit.audit_event_body_included !== false) errors.push("CP00-207 audit hint must not expose event body");
  if (audit.audit_trace_payload_included !== false) errors.push("CP00-207 audit hint must not expose trace payload");
  if (audit.audit_runtime_appended !== false) errors.push("CP00-207 audit hint must not append runtime audit");
  const primary = caseSet.primary_happy_path ?? {};
  if (primary.descriptor_only !== true) errors.push("CP00-207 primary happy path must be descriptor-only");
  if (primary.prechecks_pass !== true) errors.push("CP00-207 primary happy path prechecks must pass");
  if (primary.permission_gate_passed !== true || primary.audit_gate_passed !== true) errors.push("CP00-207 primary happy path gates must pass");
  if (primary.dispatches_primary_fixture_runtime !== false) errors.push("CP00-207 must not dispatch primary fixture runtime");
  if (primary.writes_product_state !== false) errors.push("CP00-207 primary happy path must not write product state");
  const secondary = caseSet.secondary_workflow_path ?? {};
  if (secondary.descriptor_only !== true) errors.push("CP00-207 secondary workflow path must be descriptor-only");
  if (secondary.review_required_descriptor !== true) errors.push("CP00-207 secondary workflow must retain review descriptor");
  if (secondary.approval_required_descriptor !== true) errors.push("CP00-207 secondary workflow must retain approval descriptor");
  if (secondary.dispatches_secondary_fixture_runtime !== false) errors.push("CP00-207 must not dispatch secondary fixture runtime");
  if (secondary.review_payload_included !== false || secondary.approval_payload_included !== false) {
    errors.push("CP00-207 secondary workflow must not include review or approval payloads");
  }
  const state = caseSet.state_transition_enforcement ?? {};
  if (state.descriptor_only !== true) errors.push("CP00-207 state transition enforcement must be descriptor-only");
  if (state.runtime_state_write !== false) errors.push("CP00-207 must not write runtime state");
  if (state.writes_state_transition !== false) errors.push("CP00-207 must not write state transitions");
  if (state.state_transition_payload_included !== false) errors.push("CP00-207 must not include state transition payload");
  const idempotency = caseSet.idempotency_key_handling ?? {};
  if (idempotency.descriptor_only !== true) errors.push("CP00-207 idempotency handling must be descriptor-only");
  if (idempotency.persists_idempotency_key !== false) errors.push("CP00-207 must not persist idempotency keys");
  if (idempotency.idempotency_key_material_included !== false) errors.push("CP00-207 must not include idempotency key material");
  if (idempotency.duplicate_runtime_detection_executed !== false) errors.push("CP00-207 must not execute duplicate runtime detection");
  const lock = caseSet.lock_acquisition_rule ?? {};
  if (lock.descriptor_only !== true) errors.push("CP00-207 lock rule must be descriptor-only");
  if (lock.runtime_lock_acquired !== false) errors.push("CP00-207 must not acquire runtime locks");
  if (lock.lock_token_included !== false) errors.push("CP00-207 must not include lock tokens");
  if (lock.lock_wait_queue_executed !== false) errors.push("CP00-207 must not execute lock queues");
  const persistence = caseSet.persistence_boundary ?? {};
  if (persistence.descriptor_only !== true) errors.push("CP00-207 persistence boundary must be descriptor-only");
  if (persistence.persists_workflow_attempt !== false) errors.push("CP00-207 must not persist workflow attempts");
  if (persistence.persists_idempotency_key !== false) errors.push("CP00-207 persistence must not persist idempotency keys");
  if (persistence.creates_database_rows !== false || persistence.updates_database_rows !== false) errors.push("CP00-207 persistence must not write database rows");
  if (persistence.persistence_payload_included !== false) errors.push("CP00-207 must not include persistence payload");
  if (persistence.raw_payload_included !== false) errors.push("CP00-207 persistence must not include raw payload");
  const mapping = caseSet.validation_error_mapping ?? {};
  if (mapping.descriptor_only !== true) errors.push("CP00-207 validation mapping must be descriptor-only");
  if (mapping.customer_safe_errors_only !== true) errors.push("CP00-207 validation mapping must be customer-safe");
  if (mapping.validation_error_detail_included !== false) errors.push("CP00-207 must not include validation details");
  if (mapping.permission_decision_detail_included !== false) errors.push("CP00-207 must not include permission decisions in mapping");
  if (mapping.audit_event_body_included !== false) errors.push("CP00-207 must not include audit bodies in mapping");
  if (mapping.raw_payload_included !== false) errors.push("CP00-207 validation mapping must exclude raw payload");
  if ((mapping.mappings ?? []).length < 5) errors.push("CP00-207 validation mapping must cover audit/state/idempotency/lock/persistence");
  const review = caseSet.review_required_routing ?? {};
  if (review.descriptor_only !== true) errors.push("CP00-207 review routing must be descriptor-only");
  if (review.review_required !== true) errors.push("CP00-207 review routing must preserve review-required state");
  if (review.read_only_claude_review_packet_only !== true) errors.push("CP00-207 review routing must use read-only Claude packet");
  if (review.dispatches_review_route_runtime !== false) errors.push("CP00-207 must not dispatch review route runtime");
  const approval = caseSet.approval_required_routing ?? {};
  if (approval.descriptor_only !== true) errors.push("CP00-207 approval routing must be descriptor-only");
  if (approval.approval_required !== true) errors.push("CP00-207 approval routing must preserve approval-required state");
  if (approval.human_approval_route_required_before_runtime !== true) errors.push("CP00-207 human approval boundary missing");
  if (approval.dispatches_approval_route_runtime !== false) errors.push("CP00-207 must not dispatch approval route runtime");
  if (approval.approval_payload_included !== false) errors.push("CP00-207 approval routing must not include approval payload");
  if (DMS_CORE_CP207_NO_WRITE_ATTESTATION.dispatches_primary_fixture_runtime !== false) errors.push("CP00-207 must not dispatch primary fixture runtime");
  if (DMS_CORE_CP207_NO_WRITE_ATTESTATION.dispatches_secondary_fixture_runtime !== false) errors.push("CP00-207 must not dispatch secondary fixture runtime");
  if (DMS_CORE_CP207_NO_WRITE_ATTESTATION.writes_state_transition !== false) errors.push("CP00-207 must not write state transitions");
  if (DMS_CORE_CP207_NO_WRITE_ATTESTATION.persists_idempotency_key !== false) errors.push("CP00-207 must not persist idempotency keys");
  if (DMS_CORE_CP207_NO_WRITE_ATTESTATION.acquires_runtime_lock !== false) errors.push("CP00-207 must not acquire locks");
  if (DMS_CORE_CP207_NO_WRITE_ATTESTATION.persists_workflow_attempt !== false) errors.push("CP00-207 must not persist workflow attempts");
  if (DMS_CORE_CP207_NO_WRITE_ATTESTATION.exposes_idempotency_key_material !== false) errors.push("CP00-207 must not expose idempotency material");
  if (DMS_CORE_CP207_NO_WRITE_ATTESTATION.exposes_state_transition_payload !== false) errors.push("CP00-207 must not expose state transition payload");
  if (DMS_CORE_CP207_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-207 must not promote Claude");
  if (DMS_CORE_CP207_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-207 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-207 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-207 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP207_PACK_BINDING.pack_id,
      DMS_CORE_CP208_PACK_BINDING.pack_id,
      DMS_CORE_CP209_PACK_BINDING.pack_id,
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-207 contract current_pack drift");
  }
  if (
    contractProjection?.synthetic_fixture_workflow_descriptor?.descriptor &&
    contractProjection.synthetic_fixture_workflow_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-207 contract synthetic_fixture_workflow_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP207_PACK_BINDING.next_pack_id) errors.push("CP00-207 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP207_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-207 next subphase drift");
  }
  return freezeCp207Validation({
    valid: errors.length === 0,
    errors: Object.freeze(errors),
    guard_count: DMS_CORE_CP207_REQUIREMENTS.required_synthetic_fixture_workflow_guards.length,
  });
}

export function createDmsCoreCp207HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp207SyntheticFixtureWorkflowDescriptor(),
) {
  const coverage = validateDmsCoreCp207Coverage(planPack);
  const workflow = validateDmsCoreCp207SyntheticFixtureWorkflowDescriptor(descriptor, contractProjection);
  return freezeCp207Validation({
    evidence_packet: "H06.CP00-207.dms_core_synthetic_fixture_workflow_descriptor",
    gate: DMS_CORE_CP207_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    synthetic_fixture_workflow_valid: workflow.valid,
    no_real_data: true,
    source_synthetic_fixture_service_pack_id: DMS_CORE_CP207_PACK_BINDING.upstream_pack_id,
    required_synthetic_fixture_workflow_guards: DMS_CORE_CP207_REQUIREMENTS.required_synthetic_fixture_workflow_guards,
    required_no_leak_guards: DMS_CORE_CP207_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP207_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP207_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && workflow.valid,
  });
}

export function createDmsCoreCp207ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp207Coverage(planPack);
  const workflow = validateDmsCoreCp207SyntheticFixtureWorkflowDescriptor(createDmsCoreCp207SyntheticFixtureWorkflowDescriptor(), {});
  return freezeCp207Validation({
    review_packet: "C06.CP00-207.dms_core_synthetic_fixture_workflow_descriptor",
    gate: DMS_CORE_CP207_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP207_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    synthetic_fixture_workflow_valid: workflow.valid,
    invalid_review_blockers: DMS_CORE_CP207_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-207 cover exactly the 10 planned RP06 units from RP06.P02.M06.S06 through RP06.P02.M06.S15?",
      "Does the synthetic fixture workflow descriptor cover audit hint, primary happy path, secondary workflow path, state transition, idempotency, lock, persistence, validation mapping, review routing, and approval routing descriptors?",
      "Does CP00-207 avoid runtime dispatch, review/approval route execution, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, permission details, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp207CloseoutHandoff() {
  return freezeCp207Validation({
    handoff_id: "CP00-207-to-CP00-208",
    from_pack_id: DMS_CORE_CP207_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP207_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP207_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP207_PACK_BINDING.range,
    open_scope: "Continue RP06.P02.M06.S16 onward with blocked-claim, rollback, retry, happy/denied/review unit descriptors, integration smoke, golden fixture binding, Hermes evidence, Claude review prompt, and closeout handoff while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP207_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp208CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp208Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp208Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-208 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP208_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-208");
  if (planPack?.risk_class !== DMS_CORE_CP208_PACK_BINDING.risk_class) errors.push("CP00-208 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP208_PACK_BINDING.unit_count) errors.push("CP00-208 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP208_PACK_BINDING.first_unit_id) errors.push("CP00-208 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP208_PACK_BINDING.last_unit_id) errors.push("CP00-208 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-208 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-208 must only include RP06 units");
  const summary = createDmsCoreCp208CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP208_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-208 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP208_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-208 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP208_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-208 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP208_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-208 ${microTitle} distribution drift`);
  }
  for (const title of DMS_CORE_CP208_REQUIREMENTS.required_unit_titles) {
    if (!summary.unit_titles.includes(title)) errors.push(`CP00-208 missing unit title ${title}`);
  }
  return freezeCp208Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp208SyntheticFixtureTailEntrypointDescriptor(
  descriptor = createDmsCoreCp208SyntheticFixtureTailEntrypointDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.synthetic_fixture_tail_entrypoint_case_set ?? createDmsCoreCp208SyntheticFixtureTailEntrypointCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp208SyntheticFixtureTailEntrypointDescriptor") errors.push("CP00-208 descriptor type drift");
  if (descriptor.source_synthetic_fixture_workflow_descriptor !== "DmsCoreCp207SyntheticFixtureWorkflowDescriptor") {
    errors.push("CP00-208 source synthetic fixture workflow descriptor drift");
  }
  for (const guard of DMS_CORE_CP208_REQUIREMENTS.required_synthetic_fixture_tail_entrypoint_guards) {
    if (!caseSet.required_synthetic_fixture_tail_entrypoint_guards?.includes(guard)) {
      errors.push(`CP00-208 missing tail entrypoint guard ${guard}`);
    }
  }
  const blocked = caseSet.blocked_claim_output ?? {};
  if (blocked.descriptor_only !== true) errors.push("CP00-208 blocked-claim output must be descriptor-only");
  if (blocked.blocked_status !== "blocked") errors.push("CP00-208 blocked-claim output must target blocked status");
  if (blocked.customer_safe_errors_only !== true) errors.push("CP00-208 blocked-claim output must be customer-safe");
  if (blocked.dispatches_blocked_claim_runtime !== false) errors.push("CP00-208 must not dispatch blocked-claim runtime");
  if (blocked.blocked_claim_detail_included !== false) errors.push("CP00-208 must not expose blocked-claim detail");
  if (blocked.permission_decision_detail_included !== false) errors.push("CP00-208 blocked-claim must not expose permission decisions");
  if (blocked.audit_event_body_included !== false) errors.push("CP00-208 blocked-claim must not expose audit bodies");
  if (blocked.raw_payload_included !== false) errors.push("CP00-208 blocked-claim must not include raw payload");
  const rollback = caseSet.rollback_behavior ?? {};
  if (rollback.descriptor_only !== true) errors.push("CP00-208 rollback behavior must be descriptor-only");
  if (rollback.compensates_descriptor_steps_only !== true) errors.push("CP00-208 rollback must compensate descriptor steps only");
  if (rollback.performs_rollback_runtime !== false) errors.push("CP00-208 must not perform rollback runtime");
  if (rollback.rollback_internal_state_included !== false) errors.push("CP00-208 must not expose rollback internal state");
  if (rollback.writes_state_transition !== false) errors.push("CP00-208 rollback must not write state transitions");
  if (rollback.persists_workflow_attempt !== false) errors.push("CP00-208 rollback must not persist workflow attempts");
  const retry = caseSet.retry_behavior ?? {};
  if (retry.descriptor_only !== true) errors.push("CP00-208 retry behavior must be descriptor-only");
  if (retry.bounded_retry_descriptor !== true) errors.push("CP00-208 retry must be bounded");
  if (retry.performs_retry_runtime !== false) errors.push("CP00-208 must not perform retry runtime");
  if (retry.retry_internal_state_included !== false) errors.push("CP00-208 must not expose retry internal state");
  if (retry.duplicate_runtime_detection_executed !== false) errors.push("CP00-208 retry must not execute duplicate runtime detection");
  const happy = caseSet.unit_test_happy_path ?? {};
  if (happy.descriptor_only !== true) errors.push("CP00-208 happy path unit test must be descriptor-only");
  if (happy.expected_outcome !== "allowed") errors.push("CP00-208 happy path expected outcome drift");
  if (happy.executes_unit_test_runtime_paths !== false) errors.push("CP00-208 must not execute happy path runtime");
  if (happy.writes_product_state !== false) errors.push("CP00-208 happy path must not write product state");
  if (happy.fixture_payload_included !== false) errors.push("CP00-208 happy path must not include fixture payload");
  const denied = caseSet.unit_test_denied_path ?? {};
  if (denied.descriptor_only !== true) errors.push("CP00-208 denied path unit test must be descriptor-only");
  if (denied.expected_outcome !== "denied_customer_safe") errors.push("CP00-208 denied path expected outcome drift");
  if (denied.executes_unit_test_runtime_paths !== false) errors.push("CP00-208 must not execute denied path runtime");
  if (denied.permission_decision_detail_included !== false) errors.push("CP00-208 denied path must not expose permission decisions");
  if (denied.validation_error_detail_included !== false) errors.push("CP00-208 denied path must not expose validation details");
  const review = caseSet.unit_test_review_path ?? {};
  if (review.descriptor_only !== true) errors.push("CP00-208 review path unit test must be descriptor-only");
  if (review.expected_outcome !== "review_required") errors.push("CP00-208 review path expected outcome drift");
  if (review.executes_unit_test_runtime_paths !== false) errors.push("CP00-208 must not execute review path runtime");
  if (review.dispatches_review_route_runtime !== false) errors.push("CP00-208 must not dispatch review route runtime");
  if (review.review_payload_included !== false) errors.push("CP00-208 review path must not include review payload");
  const smoke = caseSet.integration_smoke_case ?? {};
  if (smoke.descriptor_only !== true) errors.push("CP00-208 integration smoke case must be descriptor-only");
  if (smoke.smoke_scope !== "descriptor_chain_cp198_to_cp208") errors.push("CP00-208 integration smoke scope drift");
  if (smoke.dispatches_integration_smoke_runtime !== false) errors.push("CP00-208 must not dispatch integration smoke runtime");
  if (smoke.loads_real_fixture_data !== false) errors.push("CP00-208 smoke must not load real fixture data");
  if (smoke.fixture_payload_included !== false) errors.push("CP00-208 smoke must not include fixture payload");
  if (smoke.raw_payload_included !== false) errors.push("CP00-208 smoke must not include raw payload");
  const entrypoint = caseSet.service_entrypoint_contract ?? {};
  if (entrypoint.descriptor_only !== true) errors.push("CP00-208 service entrypoint contract must be descriptor-only");
  if (entrypoint.service_entrypoint !== "createDmsCoreCp208SyntheticFixtureTailEntrypointDescriptor") {
    errors.push("CP00-208 service entrypoint drift");
  }
  if (entrypoint.runtime_execution !== false) errors.push("CP00-208 entrypoint must not execute runtime");
  if (entrypoint.fixture_service_runtime_dispatched !== false) errors.push("CP00-208 entrypoint must not dispatch fixture service runtime");
  if (entrypoint.permission_runtime_evaluated !== false) errors.push("CP00-208 entrypoint must not evaluate permission runtime");
  if (entrypoint.audit_runtime_appended !== false) errors.push("CP00-208 entrypoint must not append audit runtime");
  if (entrypoint.object_storage_runtime_executed !== false) errors.push("CP00-208 entrypoint must not execute object storage runtime");
  const normalization = caseSet.request_normalization ?? {};
  if (normalization.descriptor_only !== true) errors.push("CP00-208 request normalization must be descriptor-only");
  if (normalization.rejects_unknown_fields !== true) errors.push("CP00-208 normalization must reject unknown fields");
  if (normalization.normalizes_before_prechecks !== true) errors.push("CP00-208 normalization must run before prechecks");
  if (normalization.runtime_execution !== false) errors.push("CP00-208 normalization must not execute runtime");
  if (normalization.raw_payload_included !== false) errors.push("CP00-208 normalization must not include raw payload");
  if (normalization.validation_error_detail_included !== false) errors.push("CP00-208 normalization must not expose validation details");
  if ((normalization.normalized_fields ?? []).length < 4) errors.push("CP00-208 normalization must cover tenant/matter/fixture/request fields");
  const tenant = caseSet.tenant_boundary_precheck ?? {};
  if (tenant.descriptor_only !== true) errors.push("CP00-208 tenant boundary precheck must be descriptor-only");
  if (tenant.passed !== true) errors.push("CP00-208 tenant boundary precheck must pass for synthetic fixture");
  if (tenant.cross_tenant_access_allowed !== false) errors.push("CP00-208 must not allow cross-tenant access");
  if (tenant.runtime_tenant_lookup !== false) errors.push("CP00-208 must not perform runtime tenant lookup");
  if (tenant.tenant_policy_detail_included !== false) errors.push("CP00-208 must not expose tenant policy detail");
  if (tenant.permission_decision_detail_included !== false) errors.push("CP00-208 tenant precheck must not expose permission decisions");
  if (DMS_CORE_CP208_NO_WRITE_ATTESTATION.dispatches_blocked_claim_runtime !== false) errors.push("CP00-208 must not dispatch blocked-claim runtime");
  if (DMS_CORE_CP208_NO_WRITE_ATTESTATION.dispatches_integration_smoke_runtime !== false) errors.push("CP00-208 must not dispatch integration smoke runtime");
  if (DMS_CORE_CP208_NO_WRITE_ATTESTATION.performs_rollback_runtime !== false) errors.push("CP00-208 must not perform rollback runtime");
  if (DMS_CORE_CP208_NO_WRITE_ATTESTATION.performs_retry_runtime !== false) errors.push("CP00-208 must not perform retry runtime");
  if (DMS_CORE_CP208_NO_WRITE_ATTESTATION.executes_unit_test_runtime_paths !== false) errors.push("CP00-208 must not execute unit test runtime paths");
  if (DMS_CORE_CP208_NO_WRITE_ATTESTATION.exposes_blocked_claim_detail !== false) errors.push("CP00-208 must not expose blocked-claim detail");
  if (DMS_CORE_CP208_NO_WRITE_ATTESTATION.exposes_rollback_internal_state !== false) errors.push("CP00-208 must not expose rollback internal state");
  if (DMS_CORE_CP208_NO_WRITE_ATTESTATION.exposes_retry_internal_state !== false) errors.push("CP00-208 must not expose retry internal state");
  if (DMS_CORE_CP208_NO_WRITE_ATTESTATION.exposes_tenant_policy_detail !== false) errors.push("CP00-208 must not expose tenant policy detail");
  if (DMS_CORE_CP208_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-208 must not promote Claude");
  if (DMS_CORE_CP208_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-208 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-208 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-208 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP208_PACK_BINDING.pack_id,
      DMS_CORE_CP209_PACK_BINDING.pack_id,
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-208 contract current_pack drift");
  }
  if (
    contractProjection?.synthetic_fixture_tail_entrypoint_descriptor?.descriptor &&
    contractProjection.synthetic_fixture_tail_entrypoint_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-208 contract synthetic_fixture_tail_entrypoint_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP208_PACK_BINDING.next_pack_id) errors.push("CP00-208 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP208_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-208 next subphase drift");
  }
  return freezeCp208Validation({
    valid: errors.length === 0,
    errors: Object.freeze(errors),
    guard_count: DMS_CORE_CP208_REQUIREMENTS.required_synthetic_fixture_tail_entrypoint_guards.length,
  });
}

export function createDmsCoreCp208HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp208SyntheticFixtureTailEntrypointDescriptor(),
) {
  const coverage = validateDmsCoreCp208Coverage(planPack);
  const tailEntrypoint = validateDmsCoreCp208SyntheticFixtureTailEntrypointDescriptor(descriptor, contractProjection);
  return freezeCp208Validation({
    evidence_packet: "H06.CP00-208.dms_core_synthetic_fixture_tail_entrypoint_descriptor",
    gate: DMS_CORE_CP208_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    synthetic_fixture_tail_entrypoint_valid: tailEntrypoint.valid,
    no_real_data: true,
    source_synthetic_fixture_workflow_pack_id: DMS_CORE_CP208_PACK_BINDING.upstream_pack_id,
    required_synthetic_fixture_tail_entrypoint_guards:
      DMS_CORE_CP208_REQUIREMENTS.required_synthetic_fixture_tail_entrypoint_guards,
    required_no_leak_guards: DMS_CORE_CP208_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP208_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP208_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && tailEntrypoint.valid,
  });
}

export function createDmsCoreCp208ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp208Coverage(planPack);
  const tailEntrypoint = validateDmsCoreCp208SyntheticFixtureTailEntrypointDescriptor(
    createDmsCoreCp208SyntheticFixtureTailEntrypointDescriptor(),
    {},
  );
  return freezeCp208Validation({
    review_packet: "C06.CP00-208.dms_core_synthetic_fixture_tail_entrypoint_descriptor",
    gate: DMS_CORE_CP208_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP208_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    synthetic_fixture_tail_entrypoint_valid: tailEntrypoint.valid,
    invalid_review_blockers: DMS_CORE_CP208_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-208 cover exactly the 10 planned RP06 units from RP06.P02.M06.S16 through RP06.P02.M07.S03?",
      "Does the synthetic fixture tail entrypoint descriptor cover blocked-claim output, rollback behavior, retry behavior, happy/denied/review unit test descriptors, integration smoke case, service entrypoint contract, request normalization, and tenant boundary precheck?",
      "Does CP00-208 avoid runtime dispatch, rollback/retry execution, unit-test runtime paths, review/approval route execution, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, permission details, tenant policy details, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp208CloseoutHandoff() {
  return freezeCp208Validation({
    handoff_id: "CP00-208-to-CP00-209",
    from_pack_id: DMS_CORE_CP208_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP208_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP208_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP208_PACK_BINDING.range,
    open_scope: "Continue RP06.P02.M07.S04 onward with golden case set expansion, service contract descriptors, Hermes evidence, Claude review prompt, and closeout handoff while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP208_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp209CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp209Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp209Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-209 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP209_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-209");
  if (planPack?.risk_class !== DMS_CORE_CP209_PACK_BINDING.risk_class) errors.push("CP00-209 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP209_PACK_BINDING.unit_count) errors.push("CP00-209 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP209_PACK_BINDING.first_unit_id) errors.push("CP00-209 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP209_PACK_BINDING.last_unit_id) errors.push("CP00-209 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-209 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-209 must only include RP06 units");
  const summary = createDmsCoreCp209CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP209_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-209 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP209_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-209 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP209_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-209 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP209_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-209 ${microTitle} distribution drift`);
  }
  for (const title of DMS_CORE_CP209_REQUIREMENTS.required_unit_titles) {
    if (!summary.unit_titles.includes(title)) errors.push(`CP00-209 missing unit title ${title}`);
  }
  return freezeCp209Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

function validateDmsCoreCp209CycleCase(errors, cycleName, key, cycleCase) {
  if (!cycleCase) {
    errors.push(`CP00-209 ${cycleName} missing case ${key}`);
    return;
  }
  if (cycleCase.descriptor_only !== true) errors.push(`CP00-209 ${cycleName} ${key} must be descriptor-only`);
  if (cycleCase.runtime_execution !== false) errors.push(`CP00-209 ${cycleName} ${key} must not execute runtime`);
  if (cycleCase.customer_safe_errors_only !== true) errors.push(`CP00-209 ${cycleName} ${key} must be customer-safe`);
  if (cycleCase.raw_payload_included !== false) errors.push(`CP00-209 ${cycleName} ${key} must not include raw payload`);
}

export function validateDmsCoreCp209GoldenCaseHermesCycleDescriptor(
  descriptor = createDmsCoreCp209GoldenCaseHermesCycleDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.golden_case_hermes_cycle_case_set ?? createDmsCoreCp209GoldenCaseHermesCycleCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp209GoldenCaseHermesCycleDescriptor") errors.push("CP00-209 descriptor type drift");
  if (descriptor.source_synthetic_fixture_tail_entrypoint_descriptor !== "DmsCoreCp208SyntheticFixtureTailEntrypointDescriptor") {
    errors.push("CP00-209 source synthetic fixture tail entrypoint descriptor drift");
  }
  const golden = caseSet.golden_case_cycle ?? {};
  const hermes = caseSet.hermes_evidence_cycle ?? {};
  if (golden.micro_phase_id !== "RP06.P02.M07") errors.push("CP00-209 golden case cycle micro phase drift");
  if (hermes.micro_phase_id !== "RP06.P02.M08") errors.push("CP00-209 hermes evidence cycle micro phase drift");
  for (const guard of DMS_CORE_CP209_REQUIREMENTS.required_golden_case_cycle_guards) {
    validateDmsCoreCp209CycleCase(errors, "golden_case_cycle", guard, golden[guard]);
  }
  for (const guard of DMS_CORE_CP209_REQUIREMENTS.required_hermes_evidence_cycle_guards) {
    validateDmsCoreCp209CycleCase(errors, "hermes_evidence_cycle", guard, hermes[guard]);
  }
  for (const cycle of [golden, hermes]) {
    const name = cycle === golden ? "golden_case_cycle" : "hermes_evidence_cycle";
    if (cycle.matter_trace_precheck?.runtime_matter_lookup !== false) errors.push(`CP00-209 ${name} must not perform runtime matter lookup`);
    if (cycle.matter_trace_precheck?.matter_trace_detail_included !== false) errors.push(`CP00-209 ${name} must not expose matter trace detail`);
    if (cycle.permission_precheck?.permission_runtime_evaluated !== false) errors.push(`CP00-209 ${name} must not evaluate permission runtime`);
    if (cycle.permission_precheck?.permission_decision_detail_included !== false) errors.push(`CP00-209 ${name} must not expose permission decisions`);
    if (cycle.audit_hint_precheck?.runtime_audit_lookup !== false) errors.push(`CP00-209 ${name} must not perform runtime audit lookup`);
    if (cycle.audit_hint_precheck?.audit_event_body_included !== false) errors.push(`CP00-209 ${name} must not expose audit bodies`);
    if (cycle.primary_happy_path?.prechecks_pass !== true) errors.push(`CP00-209 ${name} primary happy path prechecks must pass`);
    if (cycle.primary_happy_path?.writes_product_state !== false) errors.push(`CP00-209 ${name} must not write product state`);
    if (cycle.secondary_workflow_path?.review_required_descriptor !== true) errors.push(`CP00-209 ${name} must retain review descriptor`);
    if (cycle.secondary_workflow_path?.approval_required_descriptor !== true) errors.push(`CP00-209 ${name} must retain approval descriptor`);
    if (cycle.state_transition_enforcement?.writes_state_transition !== false) errors.push(`CP00-209 ${name} must not write state transitions`);
    if (cycle.idempotency_key_handling?.persists_idempotency_key !== false) errors.push(`CP00-209 ${name} must not persist idempotency keys`);
    if (cycle.idempotency_key_handling?.idempotency_key_material_included !== false) errors.push(`CP00-209 ${name} must not include idempotency key material`);
    if (cycle.lock_acquisition_rule?.runtime_lock_acquired !== false) errors.push(`CP00-209 ${name} must not acquire runtime locks`);
    if (cycle.lock_acquisition_rule?.lock_token_included !== false) errors.push(`CP00-209 ${name} must not include lock tokens`);
    if (cycle.persistence_boundary?.persists_workflow_attempt !== false) errors.push(`CP00-209 ${name} must not persist workflow attempts`);
    if (cycle.persistence_boundary?.persistence_payload_included !== false) errors.push(`CP00-209 ${name} must not include persistence payload`);
    if (cycle.validation_error_mapping?.validation_error_detail_included !== false) errors.push(`CP00-209 ${name} must not expose validation details`);
    if ((cycle.validation_error_mapping?.mappings ?? []).length < 7) errors.push(`CP00-209 ${name} validation mapping must cover precheck/state/idempotency/lock/persistence sources`);
    if (cycle.review_required_routing?.read_only_claude_review_packet_only !== true) errors.push(`CP00-209 ${name} review routing must use read-only Claude packet`);
    if (cycle.review_required_routing?.dispatches_review_route_runtime !== false) errors.push(`CP00-209 ${name} must not dispatch review route runtime`);
    if (cycle.approval_required_routing?.human_approval_route_required_before_runtime !== true) errors.push(`CP00-209 ${name} human approval boundary missing`);
    if (cycle.approval_required_routing?.dispatches_approval_route_runtime !== false) errors.push(`CP00-209 ${name} must not dispatch approval route runtime`);
    if (cycle.blocked_claim_output?.blocked_status !== "blocked") errors.push(`CP00-209 ${name} blocked-claim output must target blocked status`);
    if (cycle.blocked_claim_output?.blocked_claim_detail_included !== false) errors.push(`CP00-209 ${name} must not expose blocked-claim detail`);
    if (cycle.rollback_behavior?.performs_rollback_runtime !== false) errors.push(`CP00-209 ${name} must not perform rollback runtime`);
    if (cycle.rollback_behavior?.rollback_internal_state_included !== false) errors.push(`CP00-209 ${name} must not expose rollback internal state`);
    if (cycle.retry_behavior?.performs_retry_runtime !== false) errors.push(`CP00-209 ${name} must not perform retry runtime`);
    if (cycle.retry_behavior?.retry_internal_state_included !== false) errors.push(`CP00-209 ${name} must not expose retry internal state`);
  }
  if (golden.unit_test_happy_path?.expected_outcome !== "allowed") errors.push("CP00-209 happy path expected outcome drift");
  if (golden.unit_test_denied_path?.expected_outcome !== "denied_customer_safe") errors.push("CP00-209 denied path expected outcome drift");
  if (golden.unit_test_review_path?.expected_outcome !== "review_required") errors.push("CP00-209 review path expected outcome drift");
  if (golden.integration_smoke_case?.smoke_scope !== "descriptor_chain_cp198_to_cp209") errors.push("CP00-209 integration smoke scope drift");
  if (golden.golden_fixture_binding?.fixture_id !== "dms-core-cp198-synthetic-fixture") errors.push("CP00-209 golden fixture binding drift");
  if (golden.golden_fixture_binding?.golden_case_payload_included !== false) errors.push("CP00-209 must not include golden case payload");
  if (golden.hermes_service_evidence?.emits_hermes_runtime_receipt !== false) errors.push("CP00-209 must not emit Hermes runtime receipts");
  if (golden.hermes_service_evidence?.hermes_packet_body_included !== false) errors.push("CP00-209 must not include Hermes packet body");
  if (golden.claude_service_review_prompt?.read_only !== true) errors.push("CP00-209 Claude service review prompt must be read-only");
  if (golden.claude_service_review_prompt?.claude_final_approval_claimed !== false) errors.push("CP00-209 must not claim Claude final approval");
  if (hermes.service_entrypoint_contract?.service_entrypoint !== "createDmsCoreCp209GoldenCaseHermesCycleDescriptor") {
    errors.push("CP00-209 service entrypoint drift");
  }
  if (hermes.request_normalization?.rejects_unknown_fields !== true) errors.push("CP00-209 normalization must reject unknown fields");
  if ((hermes.request_normalization?.normalized_fields ?? []).length < 5) errors.push("CP00-209 normalization must cover tenant/matter/fixture/request/evidence fields");
  if (hermes.tenant_boundary_precheck?.cross_tenant_access_allowed !== false) errors.push("CP00-209 must not allow cross-tenant access");
  if (hermes.tenant_boundary_precheck?.tenant_policy_detail_included !== false) errors.push("CP00-209 must not expose tenant policy detail");
  if (DMS_CORE_CP209_NO_WRITE_ATTESTATION.dispatches_golden_case_runtime !== false) errors.push("CP00-209 must not dispatch golden case runtime");
  if (DMS_CORE_CP209_NO_WRITE_ATTESTATION.dispatches_hermes_packet_runtime !== false) errors.push("CP00-209 must not dispatch Hermes packet runtime");
  if (DMS_CORE_CP209_NO_WRITE_ATTESTATION.emits_hermes_runtime_receipt !== false) errors.push("CP00-209 must not emit Hermes runtime receipts");
  if (DMS_CORE_CP209_NO_WRITE_ATTESTATION.exposes_golden_case_payload !== false) errors.push("CP00-209 must not expose golden case payload");
  if (DMS_CORE_CP209_NO_WRITE_ATTESTATION.exposes_hermes_packet_body !== false) errors.push("CP00-209 must not expose Hermes packet body");
  if (DMS_CORE_CP209_NO_WRITE_ATTESTATION.exposes_matter_trace_detail !== false) errors.push("CP00-209 must not expose matter trace detail");
  if (DMS_CORE_CP209_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-209 must not promote Claude");
  if (DMS_CORE_CP209_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-209 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-209 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-209 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP209_PACK_BINDING.pack_id,
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-209 contract current_pack drift");
  }
  if (
    contractProjection?.golden_case_hermes_cycle_descriptor?.descriptor &&
    contractProjection.golden_case_hermes_cycle_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-209 contract golden_case_hermes_cycle_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP209_PACK_BINDING.next_pack_id) errors.push("CP00-209 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP209_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-209 next subphase drift");
  }
  return freezeCp209Validation({
    valid: errors.length === 0,
    errors: Object.freeze(errors),
    golden_case_guard_count: DMS_CORE_CP209_REQUIREMENTS.required_golden_case_cycle_guards.length,
    hermes_evidence_guard_count: DMS_CORE_CP209_REQUIREMENTS.required_hermes_evidence_cycle_guards.length,
  });
}

export function createDmsCoreCp209HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp209GoldenCaseHermesCycleDescriptor(),
) {
  const coverage = validateDmsCoreCp209Coverage(planPack);
  const cycle = validateDmsCoreCp209GoldenCaseHermesCycleDescriptor(descriptor, contractProjection);
  return freezeCp209Validation({
    evidence_packet: "H06.CP00-209.dms_core_golden_case_hermes_cycle_descriptor",
    gate: DMS_CORE_CP209_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    golden_case_hermes_cycle_valid: cycle.valid,
    no_real_data: true,
    source_synthetic_fixture_tail_entrypoint_pack_id: DMS_CORE_CP209_PACK_BINDING.upstream_pack_id,
    required_golden_case_cycle_guards: DMS_CORE_CP209_REQUIREMENTS.required_golden_case_cycle_guards,
    required_hermes_evidence_cycle_guards: DMS_CORE_CP209_REQUIREMENTS.required_hermes_evidence_cycle_guards,
    required_no_leak_guards: DMS_CORE_CP209_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP209_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP209_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && cycle.valid,
  });
}

export function createDmsCoreCp209ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp209Coverage(planPack);
  const cycle = validateDmsCoreCp209GoldenCaseHermesCycleDescriptor(createDmsCoreCp209GoldenCaseHermesCycleDescriptor(), {});
  return freezeCp209Validation({
    review_packet: "C06.CP00-209.dms_core_golden_case_hermes_cycle_descriptor",
    gate: DMS_CORE_CP209_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP209_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    golden_case_hermes_cycle_valid: cycle.valid,
    invalid_review_blockers: DMS_CORE_CP209_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-209 cover exactly the 40 planned RP06 units from RP06.P02.M07.S04 through RP06.P02.M08.S18?",
      "Do the golden case cycle (22 Test And Golden Case Set rows) and Hermes evidence cycle (18 Hermes Evidence Packet rows) cover every planned unit title as descriptor-only cases?",
      "Does CP00-209 avoid runtime dispatch, golden case or Hermes packet runtime, rollback/retry execution, unit-test runtime paths, review/approval route execution, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, permission details, tenant policy details, matter trace details, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp209CloseoutHandoff() {
  return freezeCp209Validation({
    handoff_id: "CP00-209-to-CP00-210",
    from_pack_id: DMS_CORE_CP209_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP209_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP209_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP209_PACK_BINDING.range,
    open_scope: "Continue RP06.P02.M08.S19 onward with the remaining Hermes evidence packet rows, Claude review packet rows, and closeout rows while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP209_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp210CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp210Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp210Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-210 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP210_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-210");
  if (planPack?.risk_class !== DMS_CORE_CP210_PACK_BINDING.risk_class) errors.push("CP00-210 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP210_PACK_BINDING.unit_count) errors.push("CP00-210 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP210_PACK_BINDING.first_unit_id) errors.push("CP00-210 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP210_PACK_BINDING.last_unit_id) errors.push("CP00-210 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-210 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-210 must only include RP06 units");
  const summary = createDmsCoreCp210CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP210_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-210 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP210_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-210 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP210_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-210 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP210_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-210 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP210_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-210 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-210 ${microId} missing row ${title}`);
    }
  }
  return freezeCp210Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp210P02CloseoutP03FoundationDescriptor(
  descriptor = createDmsCoreCp210P02CloseoutP03FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_closeout_p03_foundation_case_set ?? createDmsCoreCp210P02CloseoutP03FoundationCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp210P02CloseoutP03FoundationDescriptor") errors.push("CP00-210 descriptor type drift");
  if (descriptor.source_golden_case_hermes_cycle_descriptor !== "DmsCoreCp209GoldenCaseHermesCycleDescriptor") {
    errors.push("CP00-210 source golden case hermes cycle descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  const requiredSections = DMS_CORE_CP210_REQUIREMENTS.required_section_rows;
  if (caseSet.section_count !== Object.keys(requiredSections).length) errors.push("CP00-210 section count drift");
  for (const [microId, titles] of Object.entries(requiredSections)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-210 missing section ${microId}`);
      continue;
    }
    if (section.micro_title !== DMS_CORE_CP210_REQUIREMENTS.required_section_micro_titles[microId]) {
      errors.push(`CP00-210 ${microId} micro title drift`);
    }
    if (section.row_count !== titles.length) errors.push(`CP00-210 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-210 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-210 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-210 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-210 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-210 ${microId} ${key} must not include raw payload`);
      if (row.section_micro_phase_id !== microId) errors.push(`CP00-210 ${microId} ${key} section binding drift`);
    }
  }
  const m09 = sections["RP06.P02.M09"]?.rows ?? {};
  if (m09.claude_interface_prompt?.read_only !== true && m09.review_required_routing?.read_only_claude_review_packet_only !== true) {
    errors.push("CP00-210 M09 Claude review packet cycle must stay read-only");
  }
  for (const apiSection of ["RP06.P03.M00", "RP06.P03.M01", "RP06.P03.M02", "RP06.P03.M03", "RP06.P03.M04", "RP06.P03.M05"]) {
    const rows = sections[apiSection]?.rows ?? {};
    if (rows.response_contract?.api_response_payload_included !== false) errors.push(`CP00-210 ${apiSection} must not include API response payload`);
    if (rows.error_code_taxonomy?.error_taxonomy_internal_detail_included !== false) errors.push(`CP00-210 ${apiSection} must not expose error taxonomy internals`);
    if (rows.pagination_or_filtering_contract?.pagination_cursor_material_included !== false) errors.push(`CP00-210 ${apiSection} must not include pagination cursor material`);
    if (rows.unauthorized_data_omission?.unauthorized_data_omitted !== true) errors.push(`CP00-210 ${apiSection} must omit unauthorized data`);
    if (rows.permission_annotation?.permission_decision_detail_included !== false) errors.push(`CP00-210 ${apiSection} must not expose permission decisions`);
    if (rows.audit_annotation?.audit_event_body_included !== false) errors.push(`CP00-210 ${apiSection} must not expose audit bodies`);
    if (rows.serialization_guard?.serialization_guard_applied !== true) errors.push(`CP00-210 ${apiSection} serialization guard missing`);
    if (rows.api_fixture?.real_client_data_loaded !== false) errors.push(`CP00-210 ${apiSection} must not load real client data`);
  }
  for (const extSection of ["RP06.P03.M02", "RP06.P03.M03", "RP06.P03.M04", "RP06.P03.M05"]) {
    const rows = sections[extSection]?.rows ?? {};
    if (rows.hermes_api_evidence?.emits_hermes_runtime_receipt !== false) errors.push(`CP00-210 ${extSection} must not emit Hermes runtime receipts`);
    if (rows.claude_interface_prompt?.claude_final_approval_claimed !== false) errors.push(`CP00-210 ${extSection} must not claim Claude final approval`);
    if (rows.downstream_consumer_note?.downstream_runtime_opened !== false) errors.push(`CP00-210 ${extSection} must not open downstream runtime`);
    if (rows.command_rerun?.executes_command_runtime !== false) errors.push(`CP00-210 ${extSection} must not execute command runtime`);
  }
  for (const driftSection of ["RP06.P03.M03", "RP06.P03.M05"]) {
    const rows = sections[driftSection]?.rows ?? {};
    if (rows.schema_drift_check?.schema_drift_detected !== false) errors.push(`CP00-210 ${driftSection} schema drift detected`);
    if (rows.backward_compatibility_check?.backward_compatible_descriptor !== true) errors.push(`CP00-210 ${driftSection} backward compatibility drift`);
  }
  if (DMS_CORE_CP210_NO_WRITE_ATTESTATION.executes_api_handler !== false) errors.push("CP00-210 must not execute API handlers");
  if (DMS_CORE_CP210_NO_WRITE_ATTESTATION.serves_api_response_runtime !== false) errors.push("CP00-210 must not serve API responses");
  if (DMS_CORE_CP210_NO_WRITE_ATTESTATION.exposes_api_response_payload !== false) errors.push("CP00-210 must not expose API response payloads");
  if (DMS_CORE_CP210_NO_WRITE_ATTESTATION.exposes_error_taxonomy_internal_detail !== false) errors.push("CP00-210 must not expose error taxonomy internals");
  if (DMS_CORE_CP210_NO_WRITE_ATTESTATION.exposes_pagination_cursor_material !== false) errors.push("CP00-210 must not expose pagination cursor material");
  if (DMS_CORE_CP210_NO_WRITE_ATTESTATION.omits_unauthorized_data !== true) errors.push("CP00-210 must omit unauthorized data");
  if (DMS_CORE_CP210_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-210 must not promote Claude");
  if (DMS_CORE_CP210_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-210 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-210 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-210 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP210_PACK_BINDING.pack_id,
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-210 contract current_pack drift");
  }
  if (
    contractProjection?.p02_closeout_p03_foundation_descriptor?.descriptor &&
    contractProjection.p02_closeout_p03_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-210 contract p02_closeout_p03_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP210_PACK_BINDING.next_pack_id) errors.push("CP00-210 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP210_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-210 next subphase drift");
  }
  return freezeCp210Validation({
    valid: errors.length === 0,
    errors: Object.freeze(errors),
    section_count: Object.keys(DMS_CORE_CP210_REQUIREMENTS.required_section_rows).length,
  });
}

export function createDmsCoreCp210HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp210P02CloseoutP03FoundationDescriptor(),
) {
  const coverage = validateDmsCoreCp210Coverage(planPack);
  const foundation = validateDmsCoreCp210P02CloseoutP03FoundationDescriptor(descriptor, contractProjection);
  return freezeCp210Validation({
    evidence_packet: "H06.CP00-210.dms_core_p02_closeout_p03_foundation_descriptor",
    gate: DMS_CORE_CP210_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_closeout_p03_foundation_valid: foundation.valid,
    no_real_data: true,
    source_golden_case_hermes_cycle_pack_id: DMS_CORE_CP210_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP210_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP210_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP210_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createDmsCoreCp210ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp210Coverage(planPack);
  const foundation = validateDmsCoreCp210P02CloseoutP03FoundationDescriptor(createDmsCoreCp210P02CloseoutP03FoundationDescriptor(), {});
  return freezeCp210Validation({
    review_packet: "C06.CP00-210.dms_core_p02_closeout_p03_foundation_descriptor",
    gate: DMS_CORE_CP210_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP210_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_closeout_p03_foundation_valid: foundation.valid,
    invalid_review_blockers: DMS_CORE_CP210_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-210 cover exactly the 150 planned RP06 units from RP06.P02.M08.S19 through RP06.P03.M05.S22?",
      "Do the nine descriptor sections (M08 test tail, M09 Claude review packet cycle, M10 closeout handoff cycle, and the six P03 API-surface micros) cover every planned row title as descriptor-only rows?",
      "Does CP00-210 avoid API handler execution, API response serving, golden case or Hermes packet runtime, Hermes runtime receipts, rollback/retry execution, unit-test runtime paths, review/approval route execution, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, API response payloads, error taxonomy internals, pagination cursor material, unauthorized data, permission details, tenant policy details, matter trace details, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp210CloseoutHandoff() {
  return freezeCp210Validation({
    handoff_id: "CP00-210-to-CP00-211",
    from_pack_id: DMS_CORE_CP210_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP210_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP210_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP210_PACK_BINDING.range,
    open_scope: "Continue RP06.P03.M06.S01 onward with the P03 synthetic fixture, golden case, Hermes, Claude, closeout rows and the P04 opening rows while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP210_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp211CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp211Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp211Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-211 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP211_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-211");
  if (planPack?.risk_class !== DMS_CORE_CP211_PACK_BINDING.risk_class) errors.push("CP00-211 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP211_PACK_BINDING.unit_count) errors.push("CP00-211 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP211_PACK_BINDING.first_unit_id) errors.push("CP00-211 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP211_PACK_BINDING.last_unit_id) errors.push("CP00-211 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-211 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-211 must only include RP06 units");
  const summary = createDmsCoreCp211CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP211_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-211 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP211_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-211 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP211_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-211 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP211_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-211 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP211_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-211 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-211 ${microId} missing row ${title}`);
    }
  }
  return freezeCp211Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp211P03CloseoutP04UiFoundationDescriptor(
  descriptor = createDmsCoreCp211P03CloseoutP04UiFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_closeout_p04_ui_foundation_case_set ?? createDmsCoreCp211P03CloseoutP04UiFoundationCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp211P03CloseoutP04UiFoundationDescriptor") errors.push("CP00-211 descriptor type drift");
  if (descriptor.source_p02_closeout_p03_foundation_descriptor !== "DmsCoreCp210P02CloseoutP03FoundationDescriptor") {
    errors.push("CP00-211 source p02 closeout p03 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  const requiredSections = DMS_CORE_CP211_REQUIREMENTS.required_section_rows;
  if (caseSet.section_count !== Object.keys(requiredSections).length) errors.push("CP00-211 section count drift");
  for (const [microId, titles] of Object.entries(requiredSections)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-211 missing section ${microId}`);
      continue;
    }
    if (section.micro_title !== DMS_CORE_CP211_REQUIREMENTS.required_section_micro_titles[microId]) {
      errors.push(`CP00-211 ${microId} micro title drift`);
    }
    if (section.row_count !== titles.length) errors.push(`CP00-211 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-211 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-211 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-211 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-211 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-211 ${microId} ${key} must not include raw payload`);
      if (row.section_micro_phase_id !== microId) errors.push(`CP00-211 ${microId} ${key} section binding drift`);
    }
  }
  for (const apiSection of ["RP06.P03.M06", "RP06.P03.M07", "RP06.P03.M08", "RP06.P03.M09", "RP06.P03.M10"]) {
    const rows = sections[apiSection]?.rows ?? {};
    if (rows.response_contract?.api_response_payload_included !== false) errors.push(`CP00-211 ${apiSection} must not include API response payload`);
    if (rows.unauthorized_data_omission?.unauthorized_data_omitted !== true) errors.push(`CP00-211 ${apiSection} must omit unauthorized data`);
    if (rows.permission_annotation?.permission_decision_detail_included !== false) errors.push(`CP00-211 ${apiSection} must not expose permission decisions`);
    if (rows.audit_annotation?.audit_event_body_included !== false) errors.push(`CP00-211 ${apiSection} must not expose audit bodies`);
    if (rows.api_fixture?.real_client_data_loaded !== false) errors.push(`CP00-211 ${apiSection} must not load real client data`);
  }
  for (const uiSection of ["RP06.P04.M00", "RP06.P04.M01", "RP06.P04.M02", "RP06.P04.M03"]) {
    const rows = sections[uiSection]?.rows ?? {};
    if (rows.loading_state?.executes_ui_runtime !== false) errors.push(`CP00-211 ${uiSection} must not execute UI runtime`);
    if (rows.denied_state?.permission_decision_detail_included !== false) errors.push(`CP00-211 ${uiSection} denied state must not expose permission decisions`);
    if (rows.review_required_state?.review_payload_included !== false) errors.push(`CP00-211 ${uiSection} must not include review payload`);
    if (rows.primary_interaction?.writes_product_state !== false) errors.push(`CP00-211 ${uiSection} must not write product state`);
    if (rows.secondary_interaction?.writes_product_state !== false) errors.push(`CP00-211 ${uiSection} secondary interaction must not write product state`);
  }
  for (const badgeSection of ["RP06.P04.M00", "RP06.P04.M01", "RP06.P04.M02"]) {
    const rows = sections[badgeSection]?.rows ?? {};
    if (rows.permission_badge?.permission_decision_detail_included !== false) errors.push(`CP00-211 ${badgeSection} permission badge must not expose decisions`);
    if (rows.audit_hint_display?.audit_hint_detail_included !== false) errors.push(`CP00-211 ${badgeSection} audit hint display must not expose detail`);
  }
  for (const extSection of ["RP06.P04.M01", "RP06.P04.M02"]) {
    const rows = sections[extSection]?.rows ?? {};
    if (rows.error_message_copy?.validation_error_detail_included !== false) errors.push(`CP00-211 ${extSection} error copy must not expose validation detail`);
    if (rows.synthetic_fixture_binding?.real_client_data_loaded !== false) errors.push(`CP00-211 ${extSection} must not load real client data`);
    if (rows.build_smoke?.dispatches_build_runtime !== false) errors.push(`CP00-211 ${extSection} must not dispatch build runtime`);
    if (rows.hermes_ui_evidence?.emits_hermes_runtime_receipt !== false) errors.push(`CP00-211 ${extSection} must not emit Hermes runtime receipts`);
    if (rows.claude_ui_leak_prompt?.claude_final_approval_claimed !== false) errors.push(`CP00-211 ${extSection} must not claim Claude final approval`);
  }
  if (DMS_CORE_CP211_NO_WRITE_ATTESTATION.executes_ui_runtime !== false) errors.push("CP00-211 must not execute UI runtime");
  if (DMS_CORE_CP211_NO_WRITE_ATTESTATION.dispatches_build_runtime !== false) errors.push("CP00-211 must not dispatch build runtime");
  if (DMS_CORE_CP211_NO_WRITE_ATTESTATION.exposes_ui_state_payload !== false) errors.push("CP00-211 must not expose UI state payload");
  if (DMS_CORE_CP211_NO_WRITE_ATTESTATION.executes_api_handler !== false) errors.push("CP00-211 must not execute API handlers");
  if (DMS_CORE_CP211_NO_WRITE_ATTESTATION.omits_unauthorized_data !== true) errors.push("CP00-211 must omit unauthorized data");
  if (DMS_CORE_CP211_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-211 must not promote Claude");
  if (DMS_CORE_CP211_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-211 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-211 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-211 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP211_PACK_BINDING.pack_id,
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-211 contract current_pack drift");
  }
  if (
    contractProjection?.p03_closeout_p04_ui_foundation_descriptor?.descriptor &&
    contractProjection.p03_closeout_p04_ui_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-211 contract p03_closeout_p04_ui_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP211_PACK_BINDING.next_pack_id) errors.push("CP00-211 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP211_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-211 next subphase drift");
  }
  return freezeCp211Validation({
    valid: errors.length === 0,
    errors: Object.freeze(errors),
    section_count: Object.keys(DMS_CORE_CP211_REQUIREMENTS.required_section_rows).length,
  });
}

export function createDmsCoreCp211HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp211P03CloseoutP04UiFoundationDescriptor(),
) {
  const coverage = validateDmsCoreCp211Coverage(planPack);
  const foundation = validateDmsCoreCp211P03CloseoutP04UiFoundationDescriptor(descriptor, contractProjection);
  return freezeCp211Validation({
    evidence_packet: "H06.CP00-211.dms_core_p03_closeout_p04_ui_foundation_descriptor",
    gate: DMS_CORE_CP211_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_closeout_p04_ui_foundation_valid: foundation.valid,
    no_real_data: true,
    source_p02_closeout_p03_foundation_pack_id: DMS_CORE_CP211_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP211_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP211_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP211_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createDmsCoreCp211ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp211Coverage(planPack);
  const foundation = validateDmsCoreCp211P03CloseoutP04UiFoundationDescriptor(createDmsCoreCp211P03CloseoutP04UiFoundationDescriptor(), {});
  return freezeCp211Validation({
    review_packet: "C06.CP00-211.dms_core_p03_closeout_p04_ui_foundation_descriptor",
    gate: DMS_CORE_CP211_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP211_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_closeout_p04_ui_foundation_valid: foundation.valid,
    invalid_review_blockers: DMS_CORE_CP211_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-211 cover exactly the 150 planned RP06 units from RP06.P03.M06.S01 through RP06.P04.M03.S08?",
      "Do the nine descriptor sections (five P03 API-surface micros and four P04 UI-surface micros) cover every planned row title as descriptor-only rows?",
      "Does CP00-211 avoid API handler execution, UI runtime execution, build runtime dispatch, golden case or Hermes packet runtime, Hermes runtime receipts, review/approval route execution, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, API response payloads, UI state payloads, unauthorized data, permission details, tenant policy details, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp211CloseoutHandoff() {
  return freezeCp211Validation({
    handoff_id: "CP00-211-to-CP00-212",
    from_pack_id: DMS_CORE_CP211_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP211_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP211_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP211_PACK_BINDING.range,
    open_scope: "Continue RP06.P04.M03.S09 onward with the remaining P04 UI primary slice rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP211_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp212CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp212Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp212Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-212 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP212_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-212");
  if (planPack?.risk_class !== DMS_CORE_CP212_PACK_BINDING.risk_class) errors.push("CP00-212 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP212_PACK_BINDING.unit_count) errors.push("CP00-212 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP212_PACK_BINDING.first_unit_id) errors.push("CP00-212 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP212_PACK_BINDING.last_unit_id) errors.push("CP00-212 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-212 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-212 must only include RP06 units");
  const summary = createDmsCoreCp212CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP212_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-212 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP212_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-212 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP212_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-212 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP212_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-212 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP212_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-212 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-212 ${microId} missing row ${title}`);
    }
  }
  return freezeCp212Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp212UiPrimarySliceTailDescriptor(
  descriptor = createDmsCoreCp212UiPrimarySliceTailDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.ui_primary_slice_tail_case_set ?? createDmsCoreCp212UiPrimarySliceTailCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp212UiPrimarySliceTailDescriptor") errors.push("CP00-212 descriptor type drift");
  if (descriptor.source_p03_closeout_p04_ui_foundation_descriptor !== "DmsCoreCp211P03CloseoutP04UiFoundationDescriptor") {
    errors.push("CP00-212 source p03 closeout p04 ui foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(DMS_CORE_CP212_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-212 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-212 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-212 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-212 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-212 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-212 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-212 ${microId} ${key} must not include raw payload`);
    }
  }
  const rows = sections["RP06.P04.M03"]?.rows ?? {};
  if (rows.permission_badge?.permission_decision_detail_included !== false) errors.push("CP00-212 permission badge must not expose decisions");
  if (rows.audit_hint_display?.audit_hint_detail_included !== false) errors.push("CP00-212 audit hint display must not expose detail");
  if (rows.error_message_copy?.validation_error_detail_included !== false) errors.push("CP00-212 error copy must not expose validation detail");
  if (rows.synthetic_fixture_binding?.real_client_data_loaded !== false) errors.push("CP00-212 must not load real client data");
  if (rows.build_smoke?.dispatches_build_runtime !== false) errors.push("CP00-212 must not dispatch build runtime");
  if (rows.hermes_ui_evidence?.emits_hermes_runtime_receipt !== false) errors.push("CP00-212 must not emit Hermes runtime receipts");
  if (DMS_CORE_CP212_NO_WRITE_ATTESTATION.executes_ui_runtime !== false) errors.push("CP00-212 must not execute UI runtime");
  if (DMS_CORE_CP212_NO_WRITE_ATTESTATION.dispatches_build_runtime !== false) errors.push("CP00-212 must not dispatch build runtime");
  if (DMS_CORE_CP212_NO_WRITE_ATTESTATION.exposes_ui_state_payload !== false) errors.push("CP00-212 must not expose UI state payload");
  if (DMS_CORE_CP212_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-212 must not promote Claude");
  if (DMS_CORE_CP212_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-212 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-212 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-212 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP212_PACK_BINDING.pack_id,
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-212 contract current_pack drift");
  }
  if (
    contractProjection?.ui_primary_slice_tail_descriptor?.descriptor &&
    contractProjection.ui_primary_slice_tail_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-212 contract ui_primary_slice_tail_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP212_PACK_BINDING.next_pack_id) errors.push("CP00-212 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP212_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-212 next subphase drift");
  }
  return freezeCp212Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp212HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp212UiPrimarySliceTailDescriptor(),
) {
  const coverage = validateDmsCoreCp212Coverage(planPack);
  const tail = validateDmsCoreCp212UiPrimarySliceTailDescriptor(descriptor, contractProjection);
  return freezeCp212Validation({
    evidence_packet: "H06.CP00-212.dms_core_ui_primary_slice_tail_descriptor",
    gate: DMS_CORE_CP212_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    ui_primary_slice_tail_valid: tail.valid,
    no_real_data: true,
    source_p03_closeout_p04_ui_foundation_pack_id: DMS_CORE_CP212_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP212_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP212_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP212_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && tail.valid,
  });
}

export function createDmsCoreCp212ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp212Coverage(planPack);
  const tail = validateDmsCoreCp212UiPrimarySliceTailDescriptor(createDmsCoreCp212UiPrimarySliceTailDescriptor(), {});
  return freezeCp212Validation({
    review_packet: "C06.CP00-212.dms_core_ui_primary_slice_tail_descriptor",
    gate: DMS_CORE_CP212_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP212_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    ui_primary_slice_tail_valid: tail.valid,
    invalid_review_blockers: DMS_CORE_CP212_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-212 cover exactly the 10 planned RP06 units from RP06.P04.M03.S09 through RP06.P04.M03.S18?",
      "Does the UI primary slice tail section cover permission badge, audit hint display, error message copy, responsive layouts, keyboard/focus behavior, visual density check, synthetic fixture binding, build smoke, and Hermes UI evidence as descriptor-only rows?",
      "Does CP00-212 avoid UI runtime execution, build runtime dispatch, API handler execution, Hermes runtime receipts, review/approval route execution, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, UI state payloads, permission details, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp212CloseoutHandoff() {
  return freezeCp212Validation({
    handoff_id: "CP00-212-to-CP00-213",
    from_pack_id: DMS_CORE_CP212_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP212_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP212_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP212_PACK_BINDING.range,
    open_scope: "Continue RP06.P04.M03.S19 onward with the remaining P04 primary slice rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP212_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp213CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp213Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp213Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-213 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP213_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-213");
  if (planPack?.risk_class !== DMS_CORE_CP213_PACK_BINDING.risk_class) errors.push("CP00-213 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP213_PACK_BINDING.unit_count) errors.push("CP00-213 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP213_PACK_BINDING.first_unit_id) errors.push("CP00-213 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP213_PACK_BINDING.last_unit_id) errors.push("CP00-213 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-213 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-213 must only include RP06 units");
  const summary = createDmsCoreCp213CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP213_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-213 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP213_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-213 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP213_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-213 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP213_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-213 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP213_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-213 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-213 ${microId} missing row ${title}`);
    }
  }
  return freezeCp213Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp213UiSecondarySliceBindingDescriptor(
  descriptor = createDmsCoreCp213UiSecondarySliceBindingDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.ui_secondary_slice_binding_case_set ?? createDmsCoreCp213UiSecondarySliceBindingCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp213UiSecondarySliceBindingDescriptor") errors.push("CP00-213 descriptor type drift");
  if (descriptor.source_ui_primary_slice_tail_descriptor !== "DmsCoreCp212UiPrimarySliceTailDescriptor") {
    errors.push("CP00-213 source ui primary slice tail descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(DMS_CORE_CP213_REQUIREMENTS.required_section_rows).length) errors.push("CP00-213 section count drift");
  for (const [microId, titles] of Object.entries(DMS_CORE_CP213_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-213 missing section ${microId}`);
      continue;
    }
    if (section.micro_title !== DMS_CORE_CP213_REQUIREMENTS.required_section_micro_titles[microId]) {
      errors.push(`CP00-213 ${microId} micro title drift`);
    }
    if (section.row_count !== titles.length) errors.push(`CP00-213 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-213 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-213 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-213 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-213 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-213 ${microId} ${key} must not include raw payload`);
      if (row.section_micro_phase_id !== microId) errors.push(`CP00-213 ${microId} ${key} section binding drift`);
    }
  }
  for (const sectionId of ["RP06.P04.M03", "RP06.P04.M04"]) {
    const rows = sections[sectionId]?.rows ?? {};
    if (rows.state_snapshot?.state_snapshot_payload_included !== false) errors.push(`CP00-213 ${sectionId} state snapshot must not include payload`);
    if (rows.no_unauthorized_count_leak?.unauthorized_count_included !== false) errors.push(`CP00-213 ${sectionId} must not include unauthorized counts`);
    if (rows.claude_ui_leak_prompt?.claude_final_approval_claimed !== false) errors.push(`CP00-213 ${sectionId} must not claim Claude final approval`);
  }
  for (const sectionId of ["RP06.P04.M04", "RP06.P04.M05"]) {
    const rows = sections[sectionId]?.rows ?? {};
    if (rows.denied_state?.permission_decision_detail_included !== false) errors.push(`CP00-213 ${sectionId} denied state must not expose permission decisions`);
    if (rows.permission_badge?.permission_decision_detail_included !== false) errors.push(`CP00-213 ${sectionId} permission badge must not expose decisions`);
    if (rows.audit_hint_display?.audit_hint_detail_included !== false) errors.push(`CP00-213 ${sectionId} audit hint display must not expose detail`);
    if (rows.primary_interaction?.writes_product_state !== false) errors.push(`CP00-213 ${sectionId} must not write product state`);
  }
  if (sections["RP06.P04.M04"]?.rows?.build_smoke?.dispatches_build_runtime !== false) errors.push("CP00-213 must not dispatch build runtime");
  if (sections["RP06.P04.M04"]?.rows?.hermes_ui_evidence?.emits_hermes_runtime_receipt !== false) errors.push("CP00-213 must not emit Hermes runtime receipts");
  if (DMS_CORE_CP213_NO_WRITE_ATTESTATION.exposes_unauthorized_count !== false) errors.push("CP00-213 must not expose unauthorized counts");
  if (DMS_CORE_CP213_NO_WRITE_ATTESTATION.exposes_state_snapshot_payload !== false) errors.push("CP00-213 must not expose state snapshot payload");
  if (DMS_CORE_CP213_NO_WRITE_ATTESTATION.executes_ui_runtime !== false) errors.push("CP00-213 must not execute UI runtime");
  if (DMS_CORE_CP213_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-213 must not promote Claude");
  if (DMS_CORE_CP213_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-213 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-213 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-213 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP213_PACK_BINDING.pack_id,
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-213 contract current_pack drift");
  }
  if (
    contractProjection?.ui_secondary_slice_binding_descriptor?.descriptor &&
    contractProjection.ui_secondary_slice_binding_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-213 contract ui_secondary_slice_binding_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP213_PACK_BINDING.next_pack_id) errors.push("CP00-213 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP213_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-213 next subphase drift");
  }
  return freezeCp213Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp213HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp213UiSecondarySliceBindingDescriptor(),
) {
  const coverage = validateDmsCoreCp213Coverage(planPack);
  const binding = validateDmsCoreCp213UiSecondarySliceBindingDescriptor(descriptor, contractProjection);
  return freezeCp213Validation({
    evidence_packet: "H06.CP00-213.dms_core_ui_secondary_slice_binding_descriptor",
    gate: DMS_CORE_CP213_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    ui_secondary_slice_binding_valid: binding.valid,
    no_real_data: true,
    source_ui_primary_slice_tail_pack_id: DMS_CORE_CP213_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP213_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP213_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP213_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && binding.valid,
  });
}

export function createDmsCoreCp213ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp213Coverage(planPack);
  const binding = validateDmsCoreCp213UiSecondarySliceBindingDescriptor(createDmsCoreCp213UiSecondarySliceBindingDescriptor(), {});
  return freezeCp213Validation({
    review_packet: "C06.CP00-213.dms_core_ui_secondary_slice_binding_descriptor",
    gate: DMS_CORE_CP213_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP213_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    ui_secondary_slice_binding_valid: binding.valid,
    invalid_review_blockers: DMS_CORE_CP213_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-213 cover exactly the 40 planned RP06 units from RP06.P04.M03.S19 through RP06.P04.M05.S14?",
      "Do the three descriptor sections (M03 primary slice tail, M04 secondary workflow slice, M05 permission and audit binding head) cover every planned row title as descriptor-only rows, including the state snapshot and no-unauthorized-count-leak rows?",
      "Does CP00-213 avoid UI runtime execution, build runtime dispatch, API handler execution, Hermes runtime receipts, review/approval route execution, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, UI state payloads, state snapshot payloads, unauthorized counts, permission details, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp213CloseoutHandoff() {
  return freezeCp213Validation({
    handoff_id: "CP00-213-to-CP00-214",
    from_pack_id: DMS_CORE_CP213_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP213_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP213_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP213_PACK_BINDING.range,
    open_scope: "Continue RP06.P04.M05.S15 onward with the remaining P04 permission/audit binding rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP213_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp214CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp214Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp214Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-214 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP214_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-214");
  if (planPack?.risk_class !== DMS_CORE_CP214_PACK_BINDING.risk_class) errors.push("CP00-214 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP214_PACK_BINDING.unit_count) errors.push("CP00-214 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP214_PACK_BINDING.first_unit_id) errors.push("CP00-214 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP214_PACK_BINDING.last_unit_id) errors.push("CP00-214 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-214 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-214 must only include RP06 units");
  const summary = createDmsCoreCp214CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP214_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-214 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP214_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-214 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP214_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-214 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP214_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-214 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP214_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-214 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-214 ${microId} missing row ${title}`);
    }
  }
  return freezeCp214Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp214UiBindingTailDescriptor(
  descriptor = createDmsCoreCp214UiBindingTailDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.ui_binding_tail_case_set ?? createDmsCoreCp214UiBindingTailCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp214UiBindingTailDescriptor") errors.push("CP00-214 descriptor type drift");
  if (descriptor.source_ui_secondary_slice_binding_descriptor !== "DmsCoreCp213UiSecondarySliceBindingDescriptor") {
    errors.push("CP00-214 source ui secondary slice binding descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(DMS_CORE_CP214_REQUIREMENTS.required_section_rows).length) errors.push("CP00-214 section count drift");
  for (const [microId, titles] of Object.entries(DMS_CORE_CP214_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-214 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-214 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-214 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-214 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-214 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-214 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-214 ${microId} ${key} must not include raw payload`);
    }
  }
  const m05 = sections["RP06.P04.M05"]?.rows ?? {};
  if (m05.state_snapshot?.state_snapshot_payload_included !== false) errors.push("CP00-214 state snapshot must not include payload");
  if (m05.no_unauthorized_count_leak?.unauthorized_count_included !== false) errors.push("CP00-214 must not include unauthorized counts");
  if (m05.build_smoke?.dispatches_build_runtime !== false) errors.push("CP00-214 must not dispatch build runtime");
  if (m05.hermes_ui_evidence?.emits_hermes_runtime_receipt !== false) errors.push("CP00-214 must not emit Hermes runtime receipts");
  if (m05.claude_ui_leak_prompt?.claude_final_approval_claimed !== false) errors.push("CP00-214 must not claim Claude final approval");
  if (m05.synthetic_fixture_binding?.real_client_data_loaded !== false) errors.push("CP00-214 must not load real client data");
  if (DMS_CORE_CP214_NO_WRITE_ATTESTATION.exposes_unauthorized_count !== false) errors.push("CP00-214 must not expose unauthorized counts");
  if (DMS_CORE_CP214_NO_WRITE_ATTESTATION.executes_ui_runtime !== false) errors.push("CP00-214 must not execute UI runtime");
  if (DMS_CORE_CP214_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-214 must not promote Claude");
  if (DMS_CORE_CP214_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-214 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-214 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-214 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP214_PACK_BINDING.pack_id,
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-214 contract current_pack drift");
  }
  if (
    contractProjection?.ui_binding_tail_descriptor?.descriptor &&
    contractProjection.ui_binding_tail_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-214 contract ui_binding_tail_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP214_PACK_BINDING.next_pack_id) errors.push("CP00-214 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP214_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-214 next subphase drift");
  }
  return freezeCp214Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp214HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp214UiBindingTailDescriptor(),
) {
  const coverage = validateDmsCoreCp214Coverage(planPack);
  const tail = validateDmsCoreCp214UiBindingTailDescriptor(descriptor, contractProjection);
  return freezeCp214Validation({
    evidence_packet: "H06.CP00-214.dms_core_ui_binding_tail_descriptor",
    gate: DMS_CORE_CP214_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    ui_binding_tail_valid: tail.valid,
    no_real_data: true,
    source_ui_secondary_slice_binding_pack_id: DMS_CORE_CP214_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP214_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP214_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP214_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && tail.valid,
  });
}

export function createDmsCoreCp214ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp214Coverage(planPack);
  const tail = validateDmsCoreCp214UiBindingTailDescriptor(createDmsCoreCp214UiBindingTailDescriptor(), {});
  return freezeCp214Validation({
    review_packet: "C06.CP00-214.dms_core_ui_binding_tail_descriptor",
    gate: DMS_CORE_CP214_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP214_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    ui_binding_tail_valid: tail.valid,
    invalid_review_blockers: DMS_CORE_CP214_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-214 cover exactly the 10 planned RP06 units from RP06.P04.M05.S15 through RP06.P04.M06.S02?",
      "Do the two descriptor sections (M05 permission/audit binding tail and M06 synthetic fixture head) cover every planned row title as descriptor-only rows?",
      "Does CP00-214 avoid UI runtime execution, build runtime dispatch, Hermes runtime receipts, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, state snapshot payloads, unauthorized counts, permission details, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp214CloseoutHandoff() {
  return freezeCp214Validation({
    handoff_id: "CP00-214-to-CP00-215",
    from_pack_id: DMS_CORE_CP214_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP214_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP214_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP214_PACK_BINDING.range,
    open_scope: "Continue RP06.P04.M06.S03 onward with the remaining P04 synthetic fixture rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP214_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp215CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp215Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp215Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-215 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP215_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-215");
  if (planPack?.risk_class !== DMS_CORE_CP215_PACK_BINDING.risk_class) errors.push("CP00-215 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP215_PACK_BINDING.unit_count) errors.push("CP00-215 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP215_PACK_BINDING.first_unit_id) errors.push("CP00-215 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP215_PACK_BINDING.last_unit_id) errors.push("CP00-215 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-215 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-215 must only include RP06 units");
  const summary = createDmsCoreCp215CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP215_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-215 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP215_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-215 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP215_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-215 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP215_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-215 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP215_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-215 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-215 ${microId} missing row ${title}`);
    }
  }
  return freezeCp215Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp215P04CloseoutP05FixtureFoundationDescriptor(
  descriptor = createDmsCoreCp215P04CloseoutP05FixtureFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_closeout_p05_fixture_foundation_case_set ?? createDmsCoreCp215P04CloseoutP05FixtureFoundationCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp215P04CloseoutP05FixtureFoundationDescriptor") errors.push("CP00-215 descriptor type drift");
  if (descriptor.source_ui_binding_tail_descriptor !== "DmsCoreCp214UiBindingTailDescriptor") {
    errors.push("CP00-215 source ui binding tail descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(DMS_CORE_CP215_REQUIREMENTS.required_section_rows).length) errors.push("CP00-215 section count drift");
  for (const [microId, titles] of Object.entries(DMS_CORE_CP215_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-215 missing section ${microId}`);
      continue;
    }
    if (section.micro_title !== DMS_CORE_CP215_REQUIREMENTS.required_section_micro_titles[microId]) {
      errors.push(`CP00-215 ${microId} micro title drift`);
    }
    if (section.row_count !== titles.length) errors.push(`CP00-215 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-215 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-215 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-215 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-215 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-215 ${microId} ${key} must not include raw payload`);
      if (row.section_micro_phase_id !== microId) errors.push(`CP00-215 ${microId} ${key} section binding drift`);
    }
  }
  for (const fixSection of ["RP06.P05.M00", "RP06.P05.M01", "RP06.P05.M02"]) {
    const rows = sections[fixSection]?.rows ?? {};
    if (rows.base_document_fixture?.real_client_data_loaded !== false) errors.push(`CP00-215 ${fixSection} must not load real client data`);
    if (rows.denied_case?.permission_decision_detail_included !== false) errors.push(`CP00-215 ${fixSection} denied case must not expose permission decisions`);
    if (rows.cross_tenant_case?.cross_tenant_access_allowed !== false) errors.push(`CP00-215 ${fixSection} must not allow cross-tenant access`);
    if (rows.missing_context_case?.validation_error_detail_included !== false) errors.push(`CP00-215 ${fixSection} must not expose validation detail`);
  }
  for (const extSection of ["RP06.P05.M01", "RP06.P05.M02"]) {
    const rows = sections[extSection]?.rows ?? {};
    if (rows.security_trimming_case?.unauthorized_data_omitted !== true) errors.push(`CP00-215 ${extSection} must omit unauthorized data`);
    if (rows.ai_retrieval_or_analytics_case?.dispatches_ai_runtime !== false) errors.push(`CP00-215 ${extSection} must not dispatch AI runtime`);
    if (rows.ai_retrieval_or_analytics_case?.ai_payload_included !== false) errors.push(`CP00-215 ${extSection} must not include AI payload`);
    if (rows.fixture_manifest?.fixture_payload_included !== false) errors.push(`CP00-215 ${extSection} fixture manifest must not include payload`);
  }
  const m01 = sections["RP06.P05.M01"]?.rows ?? {};
  if (m01.no_real_data_check?.real_client_data_loaded !== false) errors.push("CP00-215 no-real-data check drift");
  if (m01.hermes_fixture_evidence?.emits_hermes_runtime_receipt !== false) errors.push("CP00-215 must not emit Hermes runtime receipts");
  if (m01.claude_missing_test_prompt?.claude_final_approval_claimed !== false) errors.push("CP00-215 must not claim Claude final approval");
  if (DMS_CORE_CP215_NO_WRITE_ATTESTATION.dispatches_ai_runtime !== false) errors.push("CP00-215 must not dispatch AI runtime");
  if (DMS_CORE_CP215_NO_WRITE_ATTESTATION.exposes_ai_payload !== false) errors.push("CP00-215 must not expose AI payload");
  if (DMS_CORE_CP215_NO_WRITE_ATTESTATION.executes_ui_runtime !== false) errors.push("CP00-215 must not execute UI runtime");
  if (DMS_CORE_CP215_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-215 must not promote Claude");
  if (DMS_CORE_CP215_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-215 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-215 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-215 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP215_PACK_BINDING.pack_id,
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-215 contract current_pack drift");
  }
  if (
    contractProjection?.p04_closeout_p05_fixture_foundation_descriptor?.descriptor &&
    contractProjection.p04_closeout_p05_fixture_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-215 contract p04_closeout_p05_fixture_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP215_PACK_BINDING.next_pack_id) errors.push("CP00-215 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP215_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-215 next subphase drift");
  }
  return freezeCp215Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp215HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp215P04CloseoutP05FixtureFoundationDescriptor(),
) {
  const coverage = validateDmsCoreCp215Coverage(planPack);
  const foundation = validateDmsCoreCp215P04CloseoutP05FixtureFoundationDescriptor(descriptor, contractProjection);
  return freezeCp215Validation({
    evidence_packet: "H06.CP00-215.dms_core_p04_closeout_p05_fixture_foundation_descriptor",
    gate: DMS_CORE_CP215_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_closeout_p05_fixture_foundation_valid: foundation.valid,
    no_real_data: true,
    source_ui_binding_tail_pack_id: DMS_CORE_CP215_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP215_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP215_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP215_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createDmsCoreCp215ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp215Coverage(planPack);
  const foundation = validateDmsCoreCp215P04CloseoutP05FixtureFoundationDescriptor(createDmsCoreCp215P04CloseoutP05FixtureFoundationDescriptor(), {});
  return freezeCp215Validation({
    review_packet: "C06.CP00-215.dms_core_p04_closeout_p05_fixture_foundation_descriptor",
    gate: DMS_CORE_CP215_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP215_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_closeout_p05_fixture_foundation_valid: foundation.valid,
    invalid_review_blockers: DMS_CORE_CP215_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-215 cover exactly the 150 planned RP06 units from RP06.P04.M06.S03 through RP06.P05.M02.S14?",
      "Do the eight descriptor sections (five P04 UI cycle micros and three P05 fixture-case micros) cover every planned row title as descriptor-only rows, including base fixtures, golden/denied/cross-tenant/missing-context cases, security trimming, and the AI retrieval-or-analytics descriptor?",
      "Does CP00-215 avoid AI runtime dispatch, UI runtime execution, build runtime dispatch, API handler execution, Hermes runtime receipts, review/approval route execution, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, fixture payloads, AI payloads, unauthorized data or counts, permission details, tenant policy details, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp215CloseoutHandoff() {
  return freezeCp215Validation({
    handoff_id: "CP00-215-to-CP00-216",
    from_pack_id: DMS_CORE_CP215_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP215_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP215_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP215_PACK_BINDING.range,
    open_scope: "Continue RP06.P05.M02.S15 onward with the remaining P05 fixture case rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP215_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp216CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp216Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp216Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-216 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP216_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-216");
  if (planPack?.risk_class !== DMS_CORE_CP216_PACK_BINDING.risk_class) errors.push("CP00-216 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP216_PACK_BINDING.unit_count) errors.push("CP00-216 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP216_PACK_BINDING.first_unit_id) errors.push("CP00-216 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP216_PACK_BINDING.last_unit_id) errors.push("CP00-216 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-216 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-216 must only include RP06 units");
  const summary = createDmsCoreCp216CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP216_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-216 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP216_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-216 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP216_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-216 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP216_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-216 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP216_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-216 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-216 ${microId} missing row ${title}`);
    }
  }
  return freezeCp216Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp216FixtureCaseSliceDescriptor(
  descriptor = createDmsCoreCp216FixtureCaseSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.fixture_case_slice_case_set ?? createDmsCoreCp216FixtureCaseSliceCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp216FixtureCaseSliceDescriptor") errors.push("CP00-216 descriptor type drift");
  if (descriptor.source_p04_closeout_p05_fixture_foundation_descriptor !== "DmsCoreCp215P04CloseoutP05FixtureFoundationDescriptor") {
    errors.push("CP00-216 source p04 closeout p05 fixture foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(DMS_CORE_CP216_REQUIREMENTS.required_section_rows).length) errors.push("CP00-216 section count drift");
  for (const [microId, titles] of Object.entries(DMS_CORE_CP216_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-216 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-216 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-216 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-216 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-216 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-216 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-216 ${microId} ${key} must not include raw payload`);
    }
  }
  const m03 = sections["RP06.P05.M03"]?.rows ?? {};
  if (m03.stable_id_check?.id_drift_detected !== false) errors.push("CP00-216 stable ID drift detected");
  if (m03.stable_id_check?.stable_id_material_included !== false) errors.push("CP00-216 must not include stable ID material");
  if (m03.replay_command?.executes_replay_runtime !== false) errors.push("CP00-216 must not execute replay runtime");
  if (m03.cross_tenant_case?.cross_tenant_access_allowed !== false) errors.push("CP00-216 must not allow cross-tenant access");
  if (m03.ai_retrieval_or_analytics_case?.dispatches_ai_runtime !== false) errors.push("CP00-216 must not dispatch AI runtime");
  if (m03.no_real_data_check?.real_client_data_loaded !== false) errors.push("CP00-216 no-real-data check drift");
  if (m03.claude_missing_test_prompt?.claude_final_approval_claimed !== false) errors.push("CP00-216 must not claim Claude final approval");
  const m04 = sections["RP06.P05.M04"]?.rows ?? {};
  if (m04.security_trimming_case?.unauthorized_data_omitted !== true) errors.push("CP00-216 must omit unauthorized data");
  if (m04.denied_case?.permission_decision_detail_included !== false) errors.push("CP00-216 denied case must not expose permission decisions");
  if (DMS_CORE_CP216_NO_WRITE_ATTESTATION.executes_replay_runtime !== false) errors.push("CP00-216 must not execute replay runtime");
  if (DMS_CORE_CP216_NO_WRITE_ATTESTATION.exposes_stable_id_material !== false) errors.push("CP00-216 must not expose stable ID material");
  if (DMS_CORE_CP216_NO_WRITE_ATTESTATION.dispatches_ai_runtime !== false) errors.push("CP00-216 must not dispatch AI runtime");
  if (DMS_CORE_CP216_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-216 must not promote Claude");
  if (DMS_CORE_CP216_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-216 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-216 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-216 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP216_PACK_BINDING.pack_id,
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-216 contract current_pack drift");
  }
  if (
    contractProjection?.fixture_case_slice_descriptor?.descriptor &&
    contractProjection.fixture_case_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-216 contract fixture_case_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP216_PACK_BINDING.next_pack_id) errors.push("CP00-216 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP216_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-216 next subphase drift");
  }
  return freezeCp216Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp216HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp216FixtureCaseSliceDescriptor(),
) {
  const coverage = validateDmsCoreCp216Coverage(planPack);
  const slice = validateDmsCoreCp216FixtureCaseSliceDescriptor(descriptor, contractProjection);
  return freezeCp216Validation({
    evidence_packet: "H06.CP00-216.dms_core_fixture_case_slice_descriptor",
    gate: DMS_CORE_CP216_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    fixture_case_slice_valid: slice.valid,
    no_real_data: true,
    source_p04_closeout_p05_fixture_foundation_pack_id: DMS_CORE_CP216_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP216_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP216_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP216_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createDmsCoreCp216ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp216Coverage(planPack);
  const slice = validateDmsCoreCp216FixtureCaseSliceDescriptor(createDmsCoreCp216FixtureCaseSliceDescriptor(), {});
  return freezeCp216Validation({
    review_packet: "C06.CP00-216.dms_core_fixture_case_slice_descriptor",
    gate: DMS_CORE_CP216_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP216_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    fixture_case_slice_valid: slice.valid,
    invalid_review_blockers: DMS_CORE_CP216_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-216 cover exactly the 40 planned RP06 units from RP06.P05.M02.S15 through RP06.P05.M04.S12?",
      "Do the three descriptor sections (M02 type/shape tail, M03 primary implementation slice, M04 secondary workflow slice) cover every planned row title as descriptor-only rows, including the stable ID check and replay command rows?",
      "Does CP00-216 avoid replay runtime execution, AI runtime dispatch, UI runtime execution, Hermes runtime receipts, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, fixture payloads, stable ID material, unauthorized data, permission details, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp216CloseoutHandoff() {
  return freezeCp216Validation({
    handoff_id: "CP00-216-to-CP00-217",
    from_pack_id: DMS_CORE_CP216_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP216_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP216_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP216_PACK_BINDING.range,
    open_scope: "Continue RP06.P05.M04.S13 onward with the remaining P05 fixture case rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP216_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp217CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp217Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp217Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-217 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP217_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-217");
  if (planPack?.risk_class !== DMS_CORE_CP217_PACK_BINDING.risk_class) errors.push("CP00-217 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP217_PACK_BINDING.unit_count) errors.push("CP00-217 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP217_PACK_BINDING.first_unit_id) errors.push("CP00-217 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP217_PACK_BINDING.last_unit_id) errors.push("CP00-217 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-217 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-217 must only include RP06 units");
  const summary = createDmsCoreCp217CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP217_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-217 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP217_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-217 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP217_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-217 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP217_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-217 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP217_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-217 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-217 ${microId} missing row ${title}`);
    }
  }
  return freezeCp217Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp217FixtureBindingSliceDescriptor(
  descriptor = createDmsCoreCp217FixtureBindingSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.fixture_binding_slice_case_set ?? createDmsCoreCp217FixtureBindingSliceCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp217FixtureBindingSliceDescriptor") errors.push("CP00-217 descriptor type drift");
  if (descriptor.source_fixture_case_slice_descriptor !== "DmsCoreCp216FixtureCaseSliceDescriptor") {
    errors.push("CP00-217 source fixture case slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(DMS_CORE_CP217_REQUIREMENTS.required_section_rows).length) errors.push("CP00-217 section count drift");
  for (const [microId, titles] of Object.entries(DMS_CORE_CP217_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-217 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-217 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-217 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-217 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-217 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-217 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-217 ${microId} ${key} must not include raw payload`);
    }
  }
  const m05 = sections["RP06.P05.M05"]?.rows ?? {};
  if (m05.cross_tenant_case?.cross_tenant_access_allowed !== false) errors.push("CP00-217 must not allow cross-tenant access");
  if (m05.security_trimming_case?.unauthorized_data_omitted !== true) errors.push("CP00-217 must omit unauthorized data");
  if (m05.ai_retrieval_or_analytics_case?.dispatches_ai_runtime !== false) errors.push("CP00-217 must not dispatch AI runtime");
  if (m05.stable_id_check?.id_drift_detected !== false) errors.push("CP00-217 stable ID drift detected");
  if (m05.replay_command?.executes_replay_runtime !== false) errors.push("CP00-217 must not execute replay runtime");
  if (m05.denied_case?.permission_decision_detail_included !== false) errors.push("CP00-217 denied case must not expose permission decisions");
  const m04 = sections["RP06.P05.M04"]?.rows ?? {};
  if (m04.no_real_data_check?.real_client_data_loaded !== false) errors.push("CP00-217 no-real-data check drift");
  if (m04.claude_missing_test_prompt?.claude_final_approval_claimed !== false) errors.push("CP00-217 must not claim Claude final approval");
  if (m04.hermes_fixture_evidence?.emits_hermes_runtime_receipt !== false) errors.push("CP00-217 must not emit Hermes runtime receipts");
  if (DMS_CORE_CP217_NO_WRITE_ATTESTATION.executes_replay_runtime !== false) errors.push("CP00-217 must not execute replay runtime");
  if (DMS_CORE_CP217_NO_WRITE_ATTESTATION.dispatches_ai_runtime !== false) errors.push("CP00-217 must not dispatch AI runtime");
  if (DMS_CORE_CP217_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-217 must not promote Claude");
  if (DMS_CORE_CP217_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-217 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-217 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-217 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP217_PACK_BINDING.pack_id,
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-217 contract current_pack drift");
  }
  if (
    contractProjection?.fixture_binding_slice_descriptor?.descriptor &&
    contractProjection.fixture_binding_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-217 contract fixture_binding_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP217_PACK_BINDING.next_pack_id) errors.push("CP00-217 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP217_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-217 next subphase drift");
  }
  return freezeCp217Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp217HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp217FixtureBindingSliceDescriptor(),
) {
  const coverage = validateDmsCoreCp217Coverage(planPack);
  const slice = validateDmsCoreCp217FixtureBindingSliceDescriptor(descriptor, contractProjection);
  return freezeCp217Validation({
    evidence_packet: "H06.CP00-217.dms_core_fixture_binding_slice_descriptor",
    gate: DMS_CORE_CP217_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    fixture_binding_slice_valid: slice.valid,
    no_real_data: true,
    source_fixture_case_slice_pack_id: DMS_CORE_CP217_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP217_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP217_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP217_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createDmsCoreCp217ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp217Coverage(planPack);
  const slice = validateDmsCoreCp217FixtureBindingSliceDescriptor(createDmsCoreCp217FixtureBindingSliceDescriptor(), {});
  return freezeCp217Validation({
    review_packet: "C06.CP00-217.dms_core_fixture_binding_slice_descriptor",
    gate: DMS_CORE_CP217_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP217_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    fixture_binding_slice_valid: slice.valid,
    invalid_review_blockers: DMS_CORE_CP217_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-217 cover exactly the 40 planned RP06 units from RP06.P05.M04.S13 through RP06.P05.M06.S08?",
      "Do the three descriptor sections (M04 secondary workflow tail, M05 permission and audit binding cycle, M06 synthetic fixture head) cover every planned row title as descriptor-only rows?",
      "Does CP00-217 avoid replay runtime execution, AI runtime dispatch, Hermes runtime receipts, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, fixture payloads, stable ID material, unauthorized data, permission details, tenant policy details, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp217CloseoutHandoff() {
  return freezeCp217Validation({
    handoff_id: "CP00-217-to-CP00-218",
    from_pack_id: DMS_CORE_CP217_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP217_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP217_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP217_PACK_BINDING.range,
    open_scope: "Continue RP06.P05.M06.S09 onward with the remaining P05 fixture rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP217_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp218CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp218Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp218Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-218 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP218_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-218");
  if (planPack?.risk_class !== DMS_CORE_CP218_PACK_BINDING.risk_class) errors.push("CP00-218 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP218_PACK_BINDING.unit_count) errors.push("CP00-218 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP218_PACK_BINDING.first_unit_id) errors.push("CP00-218 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP218_PACK_BINDING.last_unit_id) errors.push("CP00-218 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-218 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-218 must only include RP06 units");
  const summary = createDmsCoreCp218CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP218_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-218 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP218_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-218 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP218_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-218 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP218_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-218 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP218_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-218 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-218 ${microId} missing row ${title}`);
    }
  }
  return freezeCp218Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp218FixtureSetTailDescriptor(
  descriptor = createDmsCoreCp218FixtureSetTailDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.fixture_set_tail_case_set ?? createDmsCoreCp218FixtureSetTailCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp218FixtureSetTailDescriptor") errors.push("CP00-218 descriptor type drift");
  if (descriptor.source_fixture_binding_slice_descriptor !== "DmsCoreCp217FixtureBindingSliceDescriptor") {
    errors.push("CP00-218 source fixture binding slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(DMS_CORE_CP218_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-218 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-218 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-218 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-218 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-218 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-218 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-218 ${microId} ${key} must not include raw payload`);
    }
  }
  const rows = sections["RP06.P05.M06"]?.rows ?? {};
  if (rows.cross_tenant_case?.cross_tenant_access_allowed !== false) errors.push("CP00-218 must not allow cross-tenant access");
  if (rows.security_trimming_case?.unauthorized_data_omitted !== true) errors.push("CP00-218 must omit unauthorized data");
  if (rows.ai_retrieval_or_analytics_case?.dispatches_ai_runtime !== false) errors.push("CP00-218 must not dispatch AI runtime");
  if (rows.fixture_manifest?.fixture_payload_included !== false) errors.push("CP00-218 fixture manifest must not include payload");
  if (rows.hermes_fixture_evidence?.emits_hermes_runtime_receipt !== false) errors.push("CP00-218 must not emit Hermes runtime receipts");
  if (rows.claude_missing_test_prompt?.claude_final_approval_claimed !== false) errors.push("CP00-218 must not claim Claude final approval");
  if (DMS_CORE_CP218_NO_WRITE_ATTESTATION.dispatches_ai_runtime !== false) errors.push("CP00-218 must not dispatch AI runtime");
  if (DMS_CORE_CP218_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-218 must not promote Claude");
  if (DMS_CORE_CP218_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-218 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-218 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-218 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP218_PACK_BINDING.pack_id,
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-218 contract current_pack drift");
  }
  if (
    contractProjection?.fixture_set_tail_descriptor?.descriptor &&
    contractProjection.fixture_set_tail_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-218 contract fixture_set_tail_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP218_PACK_BINDING.next_pack_id) errors.push("CP00-218 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP218_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-218 next subphase drift");
  }
  return freezeCp218Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp218HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp218FixtureSetTailDescriptor(),
) {
  const coverage = validateDmsCoreCp218Coverage(planPack);
  const tail = validateDmsCoreCp218FixtureSetTailDescriptor(descriptor, contractProjection);
  return freezeCp218Validation({
    evidence_packet: "H06.CP00-218.dms_core_fixture_set_tail_descriptor",
    gate: DMS_CORE_CP218_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    fixture_set_tail_valid: tail.valid,
    no_real_data: true,
    source_fixture_binding_slice_pack_id: DMS_CORE_CP218_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP218_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP218_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP218_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && tail.valid,
  });
}

export function createDmsCoreCp218ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp218Coverage(planPack);
  const tail = validateDmsCoreCp218FixtureSetTailDescriptor(createDmsCoreCp218FixtureSetTailDescriptor(), {});
  return freezeCp218Validation({
    review_packet: "C06.CP00-218.dms_core_fixture_set_tail_descriptor",
    gate: DMS_CORE_CP218_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP218_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    fixture_set_tail_valid: tail.valid,
    invalid_review_blockers: DMS_CORE_CP218_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-218 cover exactly the 10 planned RP06 units from RP06.P05.M06.S09 through RP06.P05.M06.S18?",
      "Does the fixture set tail section cover cross-tenant, missing-context, audit hint, security trimming, AI retrieval-or-analytics, fixture manifest, golden/failure test, Hermes fixture evidence, and Claude missing-test prompt rows as descriptor-only rows?",
      "Does CP00-218 avoid AI runtime dispatch, replay runtime, Hermes runtime receipts, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, fixture payloads, unauthorized data, permission details, tenant policy details, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp218CloseoutHandoff() {
  return freezeCp218Validation({
    handoff_id: "CP00-218-to-CP00-219",
    from_pack_id: DMS_CORE_CP218_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP218_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP218_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP218_PACK_BINDING.range,
    open_scope: "Continue RP06.P05.M06.S19 onward with the remaining P05 rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP218_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp219CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp219Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp219Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-219 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP219_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-219");
  if (planPack?.risk_class !== DMS_CORE_CP219_PACK_BINDING.risk_class) errors.push("CP00-219 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP219_PACK_BINDING.unit_count) errors.push("CP00-219 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP219_PACK_BINDING.first_unit_id) errors.push("CP00-219 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP219_PACK_BINDING.last_unit_id) errors.push("CP00-219 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-219 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-219 must only include RP06 units");
  const summary = createDmsCoreCp219CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP219_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-219 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP219_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-219 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP219_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-219 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP219_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-219 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP219_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-219 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-219 ${microId} missing row ${title}`);
    }
  }
  return freezeCp219Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp219P05CloseoutP06PermissionMatrixDescriptor(
  descriptor = createDmsCoreCp219P05CloseoutP06PermissionMatrixDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_closeout_p06_permission_matrix_case_set ?? createDmsCoreCp219P05CloseoutP06PermissionMatrixCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp219P05CloseoutP06PermissionMatrixDescriptor") errors.push("CP00-219 descriptor type drift");
  if (descriptor.source_fixture_set_tail_descriptor !== "DmsCoreCp218FixtureSetTailDescriptor") {
    errors.push("CP00-219 source fixture set tail descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(DMS_CORE_CP219_REQUIREMENTS.required_section_rows).length) errors.push("CP00-219 section count drift");
  for (const [microId, titles] of Object.entries(DMS_CORE_CP219_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-219 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-219 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-219 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-219 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-219 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-219 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-219 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const permSection of ["RP06.P06.M00", "RP06.P06.M01", "RP06.P06.M02"]) {
    const rows = sections[permSection]?.rows ?? {};
    if (rows.deny_over_allow_check?.deny_over_allow_enforced !== true) errors.push(`CP00-219 ${permSection} deny-over-allow drift`);
    if (rows.ethical_wall_interaction?.cross_wall_access_allowed !== false) errors.push(`CP00-219 ${permSection} must not allow cross-wall access`);
    if (rows.matched_rule_capture?.policy_rule_detail_included !== false) errors.push(`CP00-219 ${permSection} must not expose policy rule detail`);
    if (rows.permission_fixture?.permission_envelope_payload_included !== false) errors.push(`CP00-219 ${permSection} must not include permission envelope payload`);
    if (rows.ai_retrieval_decision_binding?.dispatches_ai_runtime !== false) errors.push(`CP00-219 ${permSection} must not dispatch AI runtime`);
    if (rows.export_download_decision_binding?.executes_file_download !== false) errors.push(`CP00-219 ${permSection} must not execute file download`);
    if (rows.share_decision_binding?.external_share_executed !== false) errors.push(`CP00-219 ${permSection} must not execute external share`);
    if (rows.approval_required_route?.human_approval_route_required_before_runtime !== true) errors.push(`CP00-219 ${permSection} human approval boundary missing`);
    if (rows.security_trimming_proof?.unauthorized_data_omitted !== true) errors.push(`CP00-219 ${permSection} must omit unauthorized data`);
    if (rows.denied_test?.permission_decision_detail_included !== false) errors.push(`CP00-219 ${permSection} denied test must not expose permission decisions`);
  }
  const m07 = sections["RP06.P05.M07"]?.rows ?? {};
  if (m07.cross_tenant_case?.cross_tenant_access_allowed !== false) errors.push("CP00-219 must not allow cross-tenant access");
  if (m07.no_real_data_check?.real_client_data_loaded !== false) errors.push("CP00-219 no-real-data check drift");
  if (DMS_CORE_CP219_NO_WRITE_ATTESTATION.enforces_deny_over_allow_descriptor !== true) errors.push("CP00-219 deny-over-allow attestation drift");
  if (DMS_CORE_CP219_NO_WRITE_ATTESTATION.allows_cross_wall_access !== false) errors.push("CP00-219 must not allow cross-wall access");
  if (DMS_CORE_CP219_NO_WRITE_ATTESTATION.exposes_policy_rule_detail !== false) errors.push("CP00-219 must not expose policy rule detail");
  if (DMS_CORE_CP219_NO_WRITE_ATTESTATION.exposes_permission_envelope_payload !== false) errors.push("CP00-219 must not expose permission envelope payload");
  if (DMS_CORE_CP219_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-219 must not promote Claude");
  if (DMS_CORE_CP219_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-219 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-219 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-219 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP219_PACK_BINDING.pack_id,
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-219 contract current_pack drift");
  }
  if (
    contractProjection?.p05_closeout_p06_permission_matrix_descriptor?.descriptor &&
    contractProjection.p05_closeout_p06_permission_matrix_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-219 contract p05_closeout_p06_permission_matrix_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP219_PACK_BINDING.next_pack_id) errors.push("CP00-219 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP219_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-219 next subphase drift");
  }
  return freezeCp219Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp219HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp219P05CloseoutP06PermissionMatrixDescriptor(),
) {
  const coverage = validateDmsCoreCp219Coverage(planPack);
  const matrix = validateDmsCoreCp219P05CloseoutP06PermissionMatrixDescriptor(descriptor, contractProjection);
  return freezeCp219Validation({
    evidence_packet: "H06.CP00-219.dms_core_p05_closeout_p06_permission_matrix_descriptor",
    gate: DMS_CORE_CP219_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_closeout_p06_permission_matrix_valid: matrix.valid,
    no_real_data: true,
    source_fixture_set_tail_pack_id: DMS_CORE_CP219_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP219_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP219_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP219_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && matrix.valid,
  });
}

export function createDmsCoreCp219ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp219Coverage(planPack);
  const matrix = validateDmsCoreCp219P05CloseoutP06PermissionMatrixDescriptor(createDmsCoreCp219P05CloseoutP06PermissionMatrixDescriptor(), {});
  return freezeCp219Validation({
    review_packet: "C06.CP00-219.dms_core_p05_closeout_p06_permission_matrix_descriptor",
    gate: DMS_CORE_CP219_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP219_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_closeout_p06_permission_matrix_valid: matrix.valid,
    invalid_review_blockers: DMS_CORE_CP219_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-219 cover exactly the 150 planned RP06 units from RP06.P05.M06.S19 through RP06.P06.M02.S20?",
      "Do the eight descriptor sections (five P05 fixture-case micros and three P06 permission-matrix micros) cover every planned row title as descriptor-only rows, including deny-over-allow checks, legal hold, ethical wall, object ACL interactions, and security trimming proofs?",
      "Does CP00-219 avoid permission runtime evaluation, file download/share execution, AI runtime dispatch, replay runtime, Hermes runtime receipts, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, permission envelope payloads, policy rule details, unauthorized data, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp219CloseoutHandoff() {
  return freezeCp219Validation({
    handoff_id: "CP00-219-to-CP00-220",
    from_pack_id: DMS_CORE_CP219_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP219_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP219_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP219_PACK_BINDING.range,
    open_scope: "Continue RP06.P06.M02.S21 onward with the remaining P06 permission matrix rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP219_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp220CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp220Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp220Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-220 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP220_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-220");
  if (planPack?.risk_class !== DMS_CORE_CP220_PACK_BINDING.risk_class) errors.push("CP00-220 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP220_PACK_BINDING.unit_count) errors.push("CP00-220 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP220_PACK_BINDING.first_unit_id) errors.push("CP00-220 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP220_PACK_BINDING.last_unit_id) errors.push("CP00-220 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-220 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-220 must only include RP06 units");
  const summary = createDmsCoreCp220CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP220_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-220 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP220_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-220 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP220_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-220 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP220_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-220 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP220_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-220 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-220 ${microId} missing row ${title}`);
    }
  }
  return freezeCp220Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp220PermissionMatrixSliceDescriptor(
  descriptor = createDmsCoreCp220PermissionMatrixSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.permission_matrix_slice_case_set ?? createDmsCoreCp220PermissionMatrixSliceCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp220PermissionMatrixSliceDescriptor") errors.push("CP00-220 descriptor type drift");
  if (descriptor.source_p05_closeout_p06_permission_matrix_descriptor !== "DmsCoreCp219P05CloseoutP06PermissionMatrixDescriptor") {
    errors.push("CP00-220 source p05 closeout p06 permission matrix descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(DMS_CORE_CP220_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-220 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-220 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-220 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-220 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-220 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-220 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-220 ${microId} ${key} must not include raw payload`);
    }
  }
  const m02 = sections["RP06.P06.M02"]?.rows ?? {};
  if (m02.cross_tenant_test?.cross_tenant_access_allowed !== false) errors.push("CP00-220 must not allow cross-tenant access");
  if (m02.leak_prevention_test?.leak_detected !== false) errors.push("CP00-220 leak prevention drift");
  if (m02.leak_prevention_test?.unauthorized_data_omitted !== true) errors.push("CP00-220 must omit unauthorized data");
  const m03 = sections["RP06.P06.M03"]?.rows ?? {};
  if (m03.ai_retrieval_decision_binding?.dispatches_ai_runtime !== false) errors.push("CP00-220 must not dispatch AI runtime");
  if (m03.export_download_decision_binding?.executes_file_download !== false) errors.push("CP00-220 must not execute file download");
  if (m03.share_decision_binding?.external_share_executed !== false) errors.push("CP00-220 must not execute external share");
  if (m03.audit_hint_fields?.audit_hint_detail_included !== false) errors.push("CP00-220 must not expose audit hint detail");
  if (DMS_CORE_CP220_NO_WRITE_ATTESTATION.leak_detected !== false) errors.push("CP00-220 leak attestation drift");
  if (DMS_CORE_CP220_NO_WRITE_ATTESTATION.enforces_deny_over_allow_descriptor !== true) errors.push("CP00-220 deny-over-allow drift");
  if (DMS_CORE_CP220_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-220 must not promote Claude");
  if (DMS_CORE_CP220_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-220 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-220 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-220 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP220_PACK_BINDING.pack_id,
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-220 contract current_pack drift");
  }
  if (
    contractProjection?.permission_matrix_slice_descriptor?.descriptor &&
    contractProjection.permission_matrix_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-220 contract permission_matrix_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP220_PACK_BINDING.next_pack_id) errors.push("CP00-220 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP220_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-220 next subphase drift");
  }
  return freezeCp220Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp220HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp220PermissionMatrixSliceDescriptor(),
) {
  const coverage = validateDmsCoreCp220Coverage(planPack);
  const slice = validateDmsCoreCp220PermissionMatrixSliceDescriptor(descriptor, contractProjection);
  return freezeCp220Validation({
    evidence_packet: "H06.CP00-220.dms_core_permission_matrix_slice_descriptor",
    gate: DMS_CORE_CP220_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    permission_matrix_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_closeout_p06_permission_matrix_pack_id: DMS_CORE_CP220_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP220_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP220_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP220_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createDmsCoreCp220ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp220Coverage(planPack);
  const slice = validateDmsCoreCp220PermissionMatrixSliceDescriptor(createDmsCoreCp220PermissionMatrixSliceDescriptor(), {});
  return freezeCp220Validation({
    review_packet: "C06.CP00-220.dms_core_permission_matrix_slice_descriptor",
    gate: DMS_CORE_CP220_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP220_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    permission_matrix_slice_valid: slice.valid,
    invalid_review_blockers: DMS_CORE_CP220_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-220 cover exactly the 10 planned RP06 units from RP06.P06.M02.S21 through RP06.P06.M03.S08?",
      "Do the two descriptor sections (M02 type/shape tail with cross-tenant and leak prevention tests, M03 primary slice head with permission matrix decision bindings) cover every planned row title as descriptor-only rows?",
      "Does CP00-220 avoid permission runtime evaluation, file download/share execution, AI runtime dispatch, leak exposure, Hermes runtime receipts, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, permission envelope payloads, policy rule details, unauthorized data, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp220CloseoutHandoff() {
  return freezeCp220Validation({
    handoff_id: "CP00-220-to-CP00-221",
    from_pack_id: DMS_CORE_CP220_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP220_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP220_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP220_PACK_BINDING.range,
    open_scope: "Continue RP06.P06.M03.S09 onward with the remaining P06 permission matrix slice rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP220_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp221CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp221Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp221Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-221 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP221_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-221");
  if (planPack?.risk_class !== DMS_CORE_CP221_PACK_BINDING.risk_class) errors.push("CP00-221 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP221_PACK_BINDING.unit_count) errors.push("CP00-221 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP221_PACK_BINDING.first_unit_id) errors.push("CP00-221 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP221_PACK_BINDING.last_unit_id) errors.push("CP00-221 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-221 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-221 must only include RP06 units");
  const summary = createDmsCoreCp221CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP221_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-221 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP221_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-221 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP221_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-221 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP221_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-221 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP221_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-221 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-221 ${microId} missing row ${title}`);
    }
  }
  return freezeCp221Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp221PermissionWorkflowSliceDescriptor(
  descriptor = createDmsCoreCp221PermissionWorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.permission_workflow_slice_case_set ?? createDmsCoreCp221PermissionWorkflowSliceCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp221PermissionWorkflowSliceDescriptor") errors.push("CP00-221 descriptor type drift");
  if (descriptor.source_permission_matrix_slice_descriptor !== "DmsCoreCp220PermissionMatrixSliceDescriptor") {
    errors.push("CP00-221 source permission matrix slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(DMS_CORE_CP221_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-221 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-221 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-221 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-221 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-221 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-221 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-221 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const sectionId of ["RP06.P06.M03", "RP06.P06.M04"]) {
    const rows = sections[sectionId]?.rows ?? {};
    if (rows.deny_over_allow_check?.deny_over_allow_enforced !== true) errors.push(`CP00-221 ${sectionId} deny-over-allow drift`);
    if (rows.ethical_wall_interaction?.cross_wall_access_allowed !== false) errors.push(`CP00-221 ${sectionId} must not allow cross-wall access`);
    if (rows.cross_tenant_test?.cross_tenant_access_allowed !== false) errors.push(`CP00-221 ${sectionId} must not allow cross-tenant access`);
    if (rows.leak_prevention_test?.leak_detected !== false) errors.push(`CP00-221 ${sectionId} leak prevention drift`);
    if (rows.hermes_security_evidence?.emits_hermes_runtime_receipt !== false) errors.push(`CP00-221 ${sectionId} must not emit Hermes runtime receipts`);
    if (rows.security_trimming_proof?.unauthorized_data_omitted !== true) errors.push(`CP00-221 ${sectionId} must omit unauthorized data`);
    if (rows.approval_required_route?.human_approval_route_required_before_runtime !== true) errors.push(`CP00-221 ${sectionId} human approval boundary missing`);
  }
  const m03 = sections["RP06.P06.M03"]?.rows ?? {};
  if (m03.claude_bypass_prompt?.permission_bypass_detected !== false) errors.push("CP00-221 permission bypass detected");
  if (m03.claude_bypass_prompt?.claude_final_approval_claimed !== false) errors.push("CP00-221 must not claim Claude final approval");
  if (m03.human_approval_note?.human_approval_route_required_before_runtime !== true) errors.push("CP00-221 human approval note drift");
  if (DMS_CORE_CP221_NO_WRITE_ATTESTATION.permission_bypass_detected !== false) errors.push("CP00-221 bypass attestation drift");
  if (DMS_CORE_CP221_NO_WRITE_ATTESTATION.leak_detected !== false) errors.push("CP00-221 leak attestation drift");
  if (DMS_CORE_CP221_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-221 must not promote Claude");
  if (DMS_CORE_CP221_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-221 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-221 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-221 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP221_PACK_BINDING.pack_id,
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-221 contract current_pack drift");
  }
  if (
    contractProjection?.permission_workflow_slice_descriptor?.descriptor &&
    contractProjection.permission_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-221 contract permission_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP221_PACK_BINDING.next_pack_id) errors.push("CP00-221 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP221_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-221 next subphase drift");
  }
  return freezeCp221Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp221HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp221PermissionWorkflowSliceDescriptor(),
) {
  const coverage = validateDmsCoreCp221Coverage(planPack);
  const slice = validateDmsCoreCp221PermissionWorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp221Validation({
    evidence_packet: "H06.CP00-221.dms_core_permission_workflow_slice_descriptor",
    gate: DMS_CORE_CP221_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    permission_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_permission_matrix_slice_pack_id: DMS_CORE_CP221_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP221_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP221_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP221_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createDmsCoreCp221ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp221Coverage(planPack);
  const slice = validateDmsCoreCp221PermissionWorkflowSliceDescriptor(createDmsCoreCp221PermissionWorkflowSliceDescriptor(), {});
  return freezeCp221Validation({
    review_packet: "C06.CP00-221.dms_core_permission_workflow_slice_descriptor",
    gate: DMS_CORE_CP221_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP221_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    permission_workflow_slice_valid: slice.valid,
    invalid_review_blockers: DMS_CORE_CP221_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-221 cover exactly the 40 planned RP06 units from RP06.P06.M03.S09 through RP06.P06.M04.S23?",
      "Do the two descriptor sections (M03 primary slice tail with Hermes security evidence, Claude bypass prompt, and human approval note, M04 secondary workflow slice) cover every planned row title as descriptor-only rows?",
      "Does CP00-221 avoid permission runtime evaluation, permission bypass, file download/share execution, AI runtime dispatch, leak exposure, Hermes runtime receipts, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, permission envelope payloads, policy rule details, unauthorized data, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp221CloseoutHandoff() {
  return freezeCp221Validation({
    handoff_id: "CP00-221-to-CP00-222",
    from_pack_id: DMS_CORE_CP221_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP221_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP221_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP221_PACK_BINDING.range,
    open_scope: "Continue RP06.P06.M04.S24 onward with the remaining P06 permission workflow rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP221_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp222CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp222Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp222Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-222 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP222_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-222");
  if (planPack?.risk_class !== DMS_CORE_CP222_PACK_BINDING.risk_class) errors.push("CP00-222 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP222_PACK_BINDING.unit_count) errors.push("CP00-222 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP222_PACK_BINDING.first_unit_id) errors.push("CP00-222 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP222_PACK_BINDING.last_unit_id) errors.push("CP00-222 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-222 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-222 must only include RP06 units");
  const summary = createDmsCoreCp222CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP222_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-222 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP222_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-222 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP222_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-222 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP222_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-222 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP222_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-222 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-222 ${microId} missing row ${title}`);
    }
  }
  return freezeCp222Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp222PermissionAuditBindingSliceDescriptor(
  descriptor = createDmsCoreCp222PermissionAuditBindingSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.permission_audit_binding_slice_case_set ?? createDmsCoreCp222PermissionAuditBindingSliceCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp222PermissionAuditBindingSliceDescriptor") errors.push("CP00-222 descriptor type drift");
  if (descriptor.source_permission_workflow_slice_descriptor !== "DmsCoreCp221PermissionWorkflowSliceDescriptor") {
    errors.push("CP00-222 source permission workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(DMS_CORE_CP222_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-222 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-222 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-222 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-222 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-222 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-222 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-222 ${microId} ${key} must not include raw payload`);
    }
  }
  const m05 = sections["RP06.P06.M05"]?.rows ?? {};
  if (m05.deny_over_allow_check?.deny_over_allow_enforced !== true) errors.push("CP00-222 deny-over-allow drift");
  if (m05.ethical_wall_interaction?.cross_wall_access_allowed !== false) errors.push("CP00-222 must not allow cross-wall access");
  if (m05.cross_tenant_test?.cross_tenant_access_allowed !== false) errors.push("CP00-222 must not allow cross-tenant access");
  if (m05.leak_prevention_test?.leak_detected !== false) errors.push("CP00-222 leak prevention drift");
  if (m05.claude_bypass_prompt?.permission_bypass_detected !== false) errors.push("CP00-222 permission bypass detected");
  if (m05.human_approval_note?.human_approval_route_required_before_runtime !== true) errors.push("CP00-222 human approval boundary missing");
  if (m05.hermes_security_evidence?.emits_hermes_runtime_receipt !== false) errors.push("CP00-222 must not emit Hermes runtime receipts");
  const m06 = sections["RP06.P06.M06"]?.rows ?? {};
  if (m06.ai_retrieval_decision_binding?.dispatches_ai_runtime !== false) errors.push("CP00-222 must not dispatch AI runtime");
  if (m06.export_download_decision_binding?.executes_file_download !== false) errors.push("CP00-222 must not execute file download");
  if (DMS_CORE_CP222_NO_WRITE_ATTESTATION.permission_bypass_detected !== false) errors.push("CP00-222 bypass attestation drift");
  if (DMS_CORE_CP222_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-222 must not promote Claude");
  if (DMS_CORE_CP222_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-222 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-222 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-222 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP222_PACK_BINDING.pack_id,
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-222 contract current_pack drift");
  }
  if (
    contractProjection?.permission_audit_binding_slice_descriptor?.descriptor &&
    contractProjection.permission_audit_binding_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-222 contract permission_audit_binding_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP222_PACK_BINDING.next_pack_id) errors.push("CP00-222 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP222_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-222 next subphase drift");
  }
  return freezeCp222Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp222HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp222PermissionAuditBindingSliceDescriptor(),
) {
  const coverage = validateDmsCoreCp222Coverage(planPack);
  const slice = validateDmsCoreCp222PermissionAuditBindingSliceDescriptor(descriptor, contractProjection);
  return freezeCp222Validation({
    evidence_packet: "H06.CP00-222.dms_core_permission_audit_binding_slice_descriptor",
    gate: DMS_CORE_CP222_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    permission_audit_binding_slice_valid: slice.valid,
    no_real_data: true,
    source_permission_workflow_slice_pack_id: DMS_CORE_CP222_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP222_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP222_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP222_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createDmsCoreCp222ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp222Coverage(planPack);
  const slice = validateDmsCoreCp222PermissionAuditBindingSliceDescriptor(createDmsCoreCp222PermissionAuditBindingSliceDescriptor(), {});
  return freezeCp222Validation({
    review_packet: "C06.CP00-222.dms_core_permission_audit_binding_slice_descriptor",
    gate: DMS_CORE_CP222_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP222_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    permission_audit_binding_slice_valid: slice.valid,
    invalid_review_blockers: DMS_CORE_CP222_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-222 cover exactly the 40 planned RP06 units from RP06.P06.M04.S24 through RP06.P06.M06.S13?",
      "Do the three descriptor sections (M04 workflow tail, M05 permission and audit binding cycle, M06 synthetic fixture head) cover every planned row title as descriptor-only rows?",
      "Does CP00-222 avoid permission runtime evaluation, permission bypass, file download/share execution, AI runtime dispatch, leak exposure, Hermes runtime receipts, state writes, idempotency persistence, lock acquisition, workflow persistence, raw payloads, permission envelope payloads, policy rule details, unauthorized data, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp222CloseoutHandoff() {
  return freezeCp222Validation({
    handoff_id: "CP00-222-to-CP00-223",
    from_pack_id: DMS_CORE_CP222_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP222_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP222_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP222_PACK_BINDING.range,
    open_scope: "Continue RP06.P06.M06.S14 onward with the remaining P06 synthetic fixture rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP222_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp223CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp223Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp223Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-223 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP223_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-223");
  if (planPack?.risk_class !== DMS_CORE_CP223_PACK_BINDING.risk_class) errors.push("CP00-223 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP223_PACK_BINDING.unit_count) errors.push("CP00-223 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP223_PACK_BINDING.first_unit_id) errors.push("CP00-223 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP223_PACK_BINDING.last_unit_id) errors.push("CP00-223 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-223 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-223 must only include RP06 units");
  const summary = createDmsCoreCp223CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP223_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-223 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP223_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-223 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP223_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-223 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP223_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-223 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP223_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-223 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-223 ${microId} missing row ${title}`);
    }
  }
  return freezeCp223Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp223P06CloseoutP07FailureFoundationDescriptor(
  descriptor = createDmsCoreCp223P06CloseoutP07FailureFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_closeout_p07_failure_foundation_case_set ?? createDmsCoreCp223P06CloseoutP07FailureFoundationCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp223P06CloseoutP07FailureFoundationDescriptor") errors.push("CP00-223 descriptor type drift");
  if (descriptor.source_permission_audit_binding_slice_descriptor !== "DmsCoreCp222PermissionAuditBindingSliceDescriptor") {
    errors.push("CP00-223 source permission audit binding slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(DMS_CORE_CP223_REQUIREMENTS.required_section_rows).length) errors.push("CP00-223 section count drift");
  for (const [microId, titles] of Object.entries(DMS_CORE_CP223_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-223 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-223 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-223 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-223 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-223 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-223 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-223 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const failSection of ["RP06.P07.M00", "RP06.P07.M01", "RP06.P07.M02"]) {
    const rows = sections[failSection]?.rows ?? {};
    if (rows.cross_tenant_failure?.cross_tenant_access_allowed !== false) errors.push(`CP00-223 ${failSection} must not allow cross-tenant access`);
    if (rows.permission_denied_failure?.permission_decision_detail_included !== false) errors.push(`CP00-223 ${failSection} must not expose permission decisions`);
    if (rows.ambiguous_rule_failure?.deny_over_allow_enforced !== true) errors.push(`CP00-223 ${failSection} ambiguous rule must deny-over-allow`);
    if (rows.lock_conflict_failure?.lock_token_included !== false) errors.push(`CP00-223 ${failSection} must not include lock tokens`);
    if (rows.retry_exhaustion_failure?.performs_retry_runtime !== false) errors.push(`CP00-223 ${failSection} must not perform retry runtime`);
  }
  for (const extSection of ["RP06.P07.M00", "RP06.P07.M01"]) {
    const rows = sections[extSection]?.rows ?? {};
    if (rows.rollback_expectation?.performs_rollback_runtime !== false) errors.push(`CP00-223 ${extSection} must not perform rollback runtime`);
    if (rows.compensation_expectation?.compensates_descriptor_steps_only !== true) errors.push(`CP00-223 ${extSection} compensation must be descriptor-only`);
    if (rows.blocked_claim_receipt?.blocked_claim_detail_included !== false) errors.push(`CP00-223 ${extSection} must not expose blocked-claim detail`);
    if (rows.failure_fixture?.real_client_data_loaded !== false) errors.push(`CP00-223 ${extSection} must not load real client data`);
    if (rows.hermes_failure_evidence?.emits_hermes_runtime_receipt !== false) errors.push(`CP00-223 ${extSection} must not emit Hermes runtime receipts`);
  }
  const m07 = sections["RP06.P06.M07"]?.rows ?? {};
  if (m07.claude_bypass_prompt?.permission_bypass_detected !== false) errors.push("CP00-223 permission bypass detected");
  if (m07.deny_over_allow_check?.deny_over_allow_enforced !== true) errors.push("CP00-223 deny-over-allow drift");
  if (DMS_CORE_CP223_NO_WRITE_ATTESTATION.exposes_failure_internal_state !== false) errors.push("CP00-223 must not expose failure internal state");
  if (DMS_CORE_CP223_NO_WRITE_ATTESTATION.performs_rollback_runtime !== false) errors.push("CP00-223 must not perform rollback runtime");
  if (DMS_CORE_CP223_NO_WRITE_ATTESTATION.performs_retry_runtime !== false) errors.push("CP00-223 must not perform retry runtime");
  if (DMS_CORE_CP223_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-223 must not promote Claude");
  if (DMS_CORE_CP223_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-223 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-223 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-223 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP223_PACK_BINDING.pack_id,
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-223 contract current_pack drift");
  }
  if (
    contractProjection?.p06_closeout_p07_failure_foundation_descriptor?.descriptor &&
    contractProjection.p06_closeout_p07_failure_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-223 contract p06_closeout_p07_failure_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP223_PACK_BINDING.next_pack_id) errors.push("CP00-223 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP223_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-223 next subphase drift");
  }
  return freezeCp223Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp223HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp223P06CloseoutP07FailureFoundationDescriptor(),
) {
  const coverage = validateDmsCoreCp223Coverage(planPack);
  const foundation = validateDmsCoreCp223P06CloseoutP07FailureFoundationDescriptor(descriptor, contractProjection);
  return freezeCp223Validation({
    evidence_packet: "H06.CP00-223.dms_core_p06_closeout_p07_failure_foundation_descriptor",
    gate: DMS_CORE_CP223_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_closeout_p07_failure_foundation_valid: foundation.valid,
    no_real_data: true,
    source_permission_audit_binding_slice_pack_id: DMS_CORE_CP223_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP223_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP223_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP223_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createDmsCoreCp223ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp223Coverage(planPack);
  const foundation = validateDmsCoreCp223P06CloseoutP07FailureFoundationDescriptor(createDmsCoreCp223P06CloseoutP07FailureFoundationDescriptor(), {});
  return freezeCp223Validation({
    review_packet: "C06.CP00-223.dms_core_p06_closeout_p07_failure_foundation_descriptor",
    gate: DMS_CORE_CP223_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP223_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_closeout_p07_failure_foundation_valid: foundation.valid,
    invalid_review_blockers: DMS_CORE_CP223_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-223 cover exactly the 150 planned RP06 units from RP06.P06.M06.S14 through RP06.P07.M02.S12?",
      "Do the eight descriptor sections (five P06 permission-matrix micros and three P07 failure-taxonomy micros) cover every planned row title as descriptor-only rows, including missing-context failures, cross-tenant and permission-denied failures, ambiguous rule deny-over-allow, lock conflict, retry exhaustion, rollback and compensation expectations, and blocked-claim receipts?",
      "Does CP00-223 avoid rollback/retry runtime, lock acquisition, permission runtime evaluation, permission bypass, AI runtime dispatch, Hermes runtime receipts, state writes, idempotency persistence, workflow persistence, raw payloads, failure internal state, lock tokens, blocked-claim details, permission envelope payloads, policy rule details, unauthorized data, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp223CloseoutHandoff() {
  return freezeCp223Validation({
    handoff_id: "CP00-223-to-CP00-224",
    from_pack_id: DMS_CORE_CP223_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP223_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP223_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP223_PACK_BINDING.range,
    open_scope: "Continue RP06.P07.M02.S13 onward with the remaining P07 failure taxonomy rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP223_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp224CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp224Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp224Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-224 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP224_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-224");
  if (planPack?.risk_class !== DMS_CORE_CP224_PACK_BINDING.risk_class) errors.push("CP00-224 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP224_PACK_BINDING.unit_count) errors.push("CP00-224 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP224_PACK_BINDING.first_unit_id) errors.push("CP00-224 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP224_PACK_BINDING.last_unit_id) errors.push("CP00-224 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-224 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-224 must only include RP06 units");
  const summary = createDmsCoreCp224CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP224_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-224 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP224_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-224 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP224_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-224 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP224_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-224 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP224_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-224 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-224 ${microId} missing row ${title}`);
    }
  }
  return freezeCp224Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp224FailureRecoverySliceDescriptor(
  descriptor = createDmsCoreCp224FailureRecoverySliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.failure_recovery_slice_case_set ?? createDmsCoreCp224FailureRecoverySliceCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp224FailureRecoverySliceDescriptor") errors.push("CP00-224 descriptor type drift");
  if (descriptor.source_p06_closeout_p07_failure_foundation_descriptor !== "DmsCoreCp223P06CloseoutP07FailureFoundationDescriptor") {
    errors.push("CP00-224 source p06 closeout p07 failure foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(DMS_CORE_CP224_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-224 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-224 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-224 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-224 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-224 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-224 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-224 ${microId} ${key} must not include raw payload`);
    }
  }
  const m03 = sections["RP06.P07.M03"]?.rows ?? {};
  if (m03.no_silent_success_check?.silent_success_detected !== false) errors.push("CP00-224 silent success detected");
  if (m03.no_data_leak_check?.leak_detected !== false) errors.push("CP00-224 data leak detected");
  if (m03.claude_edge_case_prompt?.claude_final_approval_claimed !== false) errors.push("CP00-224 must not claim Claude final approval");
  if (m03.human_escalation_note?.human_approval_route_required_before_runtime !== true) errors.push("CP00-224 human escalation boundary missing");
  if (m03.rollback_expectation?.performs_rollback_runtime !== false) errors.push("CP00-224 must not perform rollback runtime");
  if (m03.retry_exhaustion_failure?.performs_retry_runtime !== false) errors.push("CP00-224 must not perform retry runtime");
  if (m03.cross_tenant_failure?.cross_tenant_access_allowed !== false) errors.push("CP00-224 must not allow cross-tenant access");
  if (DMS_CORE_CP224_NO_WRITE_ATTESTATION.silent_success_detected !== false) errors.push("CP00-224 silent success attestation drift");
  if (DMS_CORE_CP224_NO_WRITE_ATTESTATION.leak_detected !== false) errors.push("CP00-224 leak attestation drift");
  if (DMS_CORE_CP224_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-224 must not promote Claude");
  if (DMS_CORE_CP224_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-224 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-224 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-224 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP224_PACK_BINDING.pack_id,
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-224 contract current_pack drift");
  }
  if (
    contractProjection?.failure_recovery_slice_descriptor?.descriptor &&
    contractProjection.failure_recovery_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-224 contract failure_recovery_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP224_PACK_BINDING.next_pack_id) errors.push("CP00-224 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP224_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-224 next subphase drift");
  }
  return freezeCp224Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp224HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp224FailureRecoverySliceDescriptor(),
) {
  const coverage = validateDmsCoreCp224Coverage(planPack);
  const slice = validateDmsCoreCp224FailureRecoverySliceDescriptor(descriptor, contractProjection);
  return freezeCp224Validation({
    evidence_packet: "H06.CP00-224.dms_core_failure_recovery_slice_descriptor",
    gate: DMS_CORE_CP224_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    failure_recovery_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_closeout_p07_failure_foundation_pack_id: DMS_CORE_CP224_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP224_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP224_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP224_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createDmsCoreCp224ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp224Coverage(planPack);
  const slice = validateDmsCoreCp224FailureRecoverySliceDescriptor(createDmsCoreCp224FailureRecoverySliceDescriptor(), {});
  return freezeCp224Validation({
    review_packet: "C06.CP00-224.dms_core_failure_recovery_slice_descriptor",
    gate: DMS_CORE_CP224_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP224_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    failure_recovery_slice_valid: slice.valid,
    invalid_review_blockers: DMS_CORE_CP224_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-224 cover exactly the 40 planned RP06 units from RP06.P07.M02.S13 through RP06.P07.M04.S05?",
      "Do the three descriptor sections (M02 type/shape tail with Claude edge-case prompt and human escalation note, M03 primary slice with no-silent-success and no-data-leak checks, M04 head) cover every planned row title as descriptor-only rows?",
      "Does CP00-224 avoid rollback/retry runtime, lock acquisition, silent success, data leaks, permission runtime evaluation, AI runtime dispatch, Hermes runtime receipts, state writes, idempotency persistence, workflow persistence, raw payloads, failure internal state, lock tokens, blocked-claim details, unauthorized data, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp224CloseoutHandoff() {
  return freezeCp224Validation({
    handoff_id: "CP00-224-to-CP00-225",
    from_pack_id: DMS_CORE_CP224_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP224_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP224_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP224_PACK_BINDING.range,
    open_scope: "Continue RP06.P07.M04.S06 onward with the remaining P07 failure recovery rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP224_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp225CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp225Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp225Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-225 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP225_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-225");
  if (planPack?.risk_class !== DMS_CORE_CP225_PACK_BINDING.risk_class) errors.push("CP00-225 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP225_PACK_BINDING.unit_count) errors.push("CP00-225 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP225_PACK_BINDING.first_unit_id) errors.push("CP00-225 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP225_PACK_BINDING.last_unit_id) errors.push("CP00-225 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-225 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-225 must only include RP06 units");
  const summary = createDmsCoreCp225CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP225_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-225 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP225_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-225 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP225_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-225 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP225_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-225 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP225_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-225 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-225 ${microId} missing row ${title}`);
    }
  }
  return freezeCp225Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp225FailureBindingSliceDescriptor(
  descriptor = createDmsCoreCp225FailureBindingSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.failure_binding_slice_case_set ?? createDmsCoreCp225FailureBindingSliceCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp225FailureBindingSliceDescriptor") errors.push("CP00-225 descriptor type drift");
  if (descriptor.source_failure_recovery_slice_descriptor !== "DmsCoreCp224FailureRecoverySliceDescriptor") {
    errors.push("CP00-225 source failure recovery slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(DMS_CORE_CP225_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-225 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-225 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-225 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-225 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-225 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-225 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-225 ${microId} ${key} must not include raw payload`);
    }
  }
  const m04 = sections["RP06.P07.M04"]?.rows ?? {};
  if (m04.no_silent_success_check?.silent_success_detected !== false) errors.push("CP00-225 silent success detected");
  if (m04.no_data_leak_check?.leak_detected !== false) errors.push("CP00-225 data leak detected");
  if (m04.cross_tenant_failure?.cross_tenant_access_allowed !== false) errors.push("CP00-225 must not allow cross-tenant access");
  if (m04.rollback_expectation?.performs_rollback_runtime !== false) errors.push("CP00-225 must not perform rollback runtime");
  const m05 = sections["RP06.P07.M05"]?.rows ?? {};
  if (m05.permission_denied_failure?.permission_decision_detail_included !== false) errors.push("CP00-225 must not expose permission decisions");
  if (m05.ambiguous_rule_failure?.deny_over_allow_enforced !== true) errors.push("CP00-225 ambiguous rule must deny-over-allow");
  if (m05.lock_conflict_failure?.lock_token_included !== false) errors.push("CP00-225 must not include lock tokens");
  if (m05.hermes_failure_evidence?.emits_hermes_runtime_receipt !== false) errors.push("CP00-225 must not emit Hermes runtime receipts");
  if (DMS_CORE_CP225_NO_WRITE_ATTESTATION.silent_success_detected !== false) errors.push("CP00-225 silent success attestation drift");
  if (DMS_CORE_CP225_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-225 must not promote Claude");
  if (DMS_CORE_CP225_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-225 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-225 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-225 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP225_PACK_BINDING.pack_id,
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-225 contract current_pack drift");
  }
  if (
    contractProjection?.failure_binding_slice_descriptor?.descriptor &&
    contractProjection.failure_binding_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-225 contract failure_binding_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP225_PACK_BINDING.next_pack_id) errors.push("CP00-225 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP225_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-225 next subphase drift");
  }
  return freezeCp225Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp225HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp225FailureBindingSliceDescriptor(),
) {
  const coverage = validateDmsCoreCp225Coverage(planPack);
  const slice = validateDmsCoreCp225FailureBindingSliceDescriptor(descriptor, contractProjection);
  return freezeCp225Validation({
    evidence_packet: "H06.CP00-225.dms_core_failure_binding_slice_descriptor",
    gate: DMS_CORE_CP225_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    failure_binding_slice_valid: slice.valid,
    no_real_data: true,
    source_failure_recovery_slice_pack_id: DMS_CORE_CP225_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP225_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP225_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP225_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createDmsCoreCp225ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp225Coverage(planPack);
  const slice = validateDmsCoreCp225FailureBindingSliceDescriptor(createDmsCoreCp225FailureBindingSliceDescriptor(), {});
  return freezeCp225Validation({
    review_packet: "C06.CP00-225.dms_core_failure_binding_slice_descriptor",
    gate: DMS_CORE_CP225_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP225_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    failure_binding_slice_valid: slice.valid,
    invalid_review_blockers: DMS_CORE_CP225_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-225 cover exactly the 40 planned RP06 units from RP06.P07.M04.S06 through RP06.P07.M05.S20?",
      "Do the two descriptor sections (M04 secondary workflow tail and M05 permission/audit binding head) cover every planned row title as descriptor-only rows?",
      "Does CP00-225 avoid rollback/retry runtime, lock acquisition, silent success, data leaks, permission runtime evaluation, AI runtime dispatch, Hermes runtime receipts, state writes, idempotency persistence, workflow persistence, raw payloads, failure internal state, lock tokens, blocked-claim details, unauthorized data, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp225CloseoutHandoff() {
  return freezeCp225Validation({
    handoff_id: "CP00-225-to-CP00-226",
    from_pack_id: DMS_CORE_CP225_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP225_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP225_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP225_PACK_BINDING.range,
    open_scope: "Continue RP06.P07.M05.S21 onward with the remaining P07 failure binding rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP225_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp226CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp226Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp226Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-226 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP226_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-226");
  if (planPack?.risk_class !== DMS_CORE_CP226_PACK_BINDING.risk_class) errors.push("CP00-226 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP226_PACK_BINDING.unit_count) errors.push("CP00-226 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP226_PACK_BINDING.first_unit_id) errors.push("CP00-226 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP226_PACK_BINDING.last_unit_id) errors.push("CP00-226 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-226 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-226 must only include RP06 units");
  const summary = createDmsCoreCp226CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP226_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-226 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP226_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-226 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP226_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-226 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP226_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-226 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP226_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-226 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-226 ${microId} missing row ${title}`);
    }
  }
  return freezeCp226Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp226FailureAuditTailDescriptor(
  descriptor = createDmsCoreCp226FailureAuditTailDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.failure_audit_tail_case_set ?? createDmsCoreCp226FailureAuditTailCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp226FailureAuditTailDescriptor") errors.push("CP00-226 descriptor type drift");
  if (descriptor.source_failure_binding_slice_descriptor !== "DmsCoreCp225FailureBindingSliceDescriptor") {
    errors.push("CP00-226 source failure binding slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(DMS_CORE_CP226_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-226 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-226 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-226 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-226 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-226 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-226 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-226 ${microId} ${key} must not include raw payload`);
    }
  }
  const m05 = sections["RP06.P07.M05"]?.rows ?? {};
  if (m05.no_silent_success_check?.silent_success_detected !== false) errors.push("CP00-226 silent success detected");
  if (m05.no_data_leak_check?.leak_detected !== false) errors.push("CP00-226 data leak detected");
  if (m05.claude_edge_case_prompt?.claude_final_approval_claimed !== false) errors.push("CP00-226 must not claim Claude final approval");
  if (m05.human_escalation_note?.human_approval_route_required_before_runtime !== true) errors.push("CP00-226 human escalation boundary missing");
  if (DMS_CORE_CP226_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-226 must not promote Claude");
  if (DMS_CORE_CP226_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-226 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-226 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-226 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP226_PACK_BINDING.pack_id,
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-226 contract current_pack drift");
  }
  if (
    contractProjection?.failure_audit_tail_descriptor?.descriptor &&
    contractProjection.failure_audit_tail_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-226 contract failure_audit_tail_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP226_PACK_BINDING.next_pack_id) errors.push("CP00-226 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP226_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-226 next subphase drift");
  }
  return freezeCp226Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp226HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp226FailureAuditTailDescriptor(),
) {
  const coverage = validateDmsCoreCp226Coverage(planPack);
  const tail = validateDmsCoreCp226FailureAuditTailDescriptor(descriptor, contractProjection);
  return freezeCp226Validation({
    evidence_packet: "H06.CP00-226.dms_core_failure_audit_tail_descriptor",
    gate: DMS_CORE_CP226_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    failure_audit_tail_valid: tail.valid,
    no_real_data: true,
    source_failure_binding_slice_pack_id: DMS_CORE_CP226_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP226_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP226_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP226_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && tail.valid,
  });
}

export function createDmsCoreCp226ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp226Coverage(planPack);
  const tail = validateDmsCoreCp226FailureAuditTailDescriptor(createDmsCoreCp226FailureAuditTailDescriptor(), {});
  return freezeCp226Validation({
    review_packet: "C06.CP00-226.dms_core_failure_audit_tail_descriptor",
    gate: DMS_CORE_CP226_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP226_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    failure_audit_tail_valid: tail.valid,
    invalid_review_blockers: DMS_CORE_CP226_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-226 cover exactly the 10 planned RP06 units from RP06.P07.M05.S21 through RP06.P07.M06.S05?",
      "Do the two descriptor sections (M05 permission/audit binding tail and M06 synthetic fixture head) cover every planned row title as descriptor-only rows?",
      "Does CP00-226 avoid rollback/retry runtime, lock acquisition, silent success, data leaks, permission runtime evaluation, AI runtime dispatch, Hermes runtime receipts, state writes, raw payloads, failure internal state, unauthorized data, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp226CloseoutHandoff() {
  return freezeCp226Validation({
    handoff_id: "CP00-226-to-CP00-227",
    from_pack_id: DMS_CORE_CP226_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP226_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP226_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP226_PACK_BINDING.range,
    open_scope: "Continue RP06.P07.M06.S06 onward with the remaining P07 fixture rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP226_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp227CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp227Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp227Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-227 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP227_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-227");
  if (planPack?.risk_class !== DMS_CORE_CP227_PACK_BINDING.risk_class) errors.push("CP00-227 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP227_PACK_BINDING.unit_count) errors.push("CP00-227 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP227_PACK_BINDING.first_unit_id) errors.push("CP00-227 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP227_PACK_BINDING.last_unit_id) errors.push("CP00-227 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-227 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-227 must only include RP06 units");
  const summary = createDmsCoreCp227CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP227_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-227 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP227_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-227 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP227_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-227 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP227_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-227 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP227_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-227 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-227 ${microId} missing row ${title}`);
    }
  }
  return freezeCp227Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp227P07CloseoutP08HermesReceiptDescriptor(
  descriptor = createDmsCoreCp227P07CloseoutP08HermesReceiptDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_closeout_p08_hermes_receipt_case_set ?? createDmsCoreCp227P07CloseoutP08HermesReceiptCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp227P07CloseoutP08HermesReceiptDescriptor") errors.push("CP00-227 descriptor type drift");
  if (descriptor.source_failure_audit_tail_descriptor !== "DmsCoreCp226FailureAuditTailDescriptor") {
    errors.push("CP00-227 source failure audit tail descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(DMS_CORE_CP227_REQUIREMENTS.required_section_rows).length) errors.push("CP00-227 section count drift");
  for (const [microId, titles] of Object.entries(DMS_CORE_CP227_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-227 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-227 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-227 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-227 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-227 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-227 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-227 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const herSection of ["RP06.P08.M00", "RP06.P08.M01", "RP06.P08.M02"]) {
    const rows = sections[herSection]?.rows ?? {};
    if (rows.hermes_command_matrix?.emits_hermes_runtime_receipt !== false) errors.push(`CP00-227 ${herSection} must not emit Hermes runtime receipts`);
    if (rows.command_result_receipt?.executes_command_runtime !== false) errors.push(`CP00-227 ${herSection} must not execute command runtime`);
    if (rows.permission_summary_receipt?.permission_decision_detail_included !== false) errors.push(`CP00-227 ${herSection} must not expose permission decisions`);
    if (rows.audit_summary_receipt?.audit_event_body_included !== false) errors.push(`CP00-227 ${herSection} must not expose audit bodies`);
    if (rows.no_real_data_receipt?.real_client_data_loaded !== false) errors.push(`CP00-227 ${herSection} must not load real data`);
    if (rows.claude_dependency_marker?.claude_final_approval_claimed !== false) errors.push(`CP00-227 ${herSection} must not claim Claude final approval`);
  }
  const m01 = sections["RP06.P08.M01"]?.rows ?? {};
  if (m01.human_approval_marker?.human_approval_route_required_before_runtime !== true) errors.push("CP00-227 human approval marker drift");
  if (m01.pass_semantics?.verdict_descriptor !== "PASS") errors.push("CP00-227 PASS semantics drift");
  if (m01.block_semantics?.blocks_closeout_descriptor !== true) errors.push("CP00-227 BLOCK semantics drift");
  if (m01.regression_receipt?.regression_detected !== false) errors.push("CP00-227 regression detected");
  const m07 = sections["RP06.P07.M07"]?.rows ?? {};
  if (m07.no_silent_success_check?.silent_success_detected !== false) errors.push("CP00-227 silent success detected");
  if (m07.cross_tenant_failure?.cross_tenant_access_allowed !== false) errors.push("CP00-227 must not allow cross-tenant access");
  if (DMS_CORE_CP227_NO_WRITE_ATTESTATION.regression_detected !== false) errors.push("CP00-227 regression attestation drift");
  if (DMS_CORE_CP227_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-227 must not promote Claude");
  if (DMS_CORE_CP227_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-227 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-227 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-227 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP227_PACK_BINDING.pack_id,
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-227 contract current_pack drift");
  }
  if (
    contractProjection?.p07_closeout_p08_hermes_receipt_descriptor?.descriptor &&
    contractProjection.p07_closeout_p08_hermes_receipt_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-227 contract p07_closeout_p08_hermes_receipt_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP227_PACK_BINDING.next_pack_id) errors.push("CP00-227 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP227_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-227 next subphase drift");
  }
  return freezeCp227Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp227HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp227P07CloseoutP08HermesReceiptDescriptor(),
) {
  const coverage = validateDmsCoreCp227Coverage(planPack);
  const receipt = validateDmsCoreCp227P07CloseoutP08HermesReceiptDescriptor(descriptor, contractProjection);
  return freezeCp227Validation({
    evidence_packet: "H06.CP00-227.dms_core_p07_closeout_p08_hermes_receipt_descriptor",
    gate: DMS_CORE_CP227_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_closeout_p08_hermes_receipt_valid: receipt.valid,
    no_real_data: true,
    source_failure_audit_tail_pack_id: DMS_CORE_CP227_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP227_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP227_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP227_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && receipt.valid,
  });
}

export function createDmsCoreCp227ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp227Coverage(planPack);
  const receipt = validateDmsCoreCp227P07CloseoutP08HermesReceiptDescriptor(createDmsCoreCp227P07CloseoutP08HermesReceiptDescriptor(), {});
  return freezeCp227Validation({
    review_packet: "C06.CP00-227.dms_core_p07_closeout_p08_hermes_receipt_descriptor",
    gate: DMS_CORE_CP227_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP227_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_closeout_p08_hermes_receipt_valid: receipt.valid,
    invalid_review_blockers: DMS_CORE_CP227_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-227 cover exactly the 150 planned RP06 units from RP06.P07.M06.S06 through RP06.P08.M02.S14?",
      "Do the eight descriptor sections (five P07 failure-taxonomy micros and three P08 Hermes-receipt micros) cover every planned row title as descriptor-only rows, including command matrices, evidence receipts, PASS/PASS_WITH_FINDINGS/BLOCK semantics, Claude dependency markers, and human approval markers?",
      "Does CP00-227 avoid Hermes runtime receipt emission, command runtime execution, rollback/retry runtime, lock acquisition, permission runtime evaluation, AI runtime dispatch, state writes, raw payloads, failure internal state, hermes packet bodies, unauthorized data, audit bodies, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp227CloseoutHandoff() {
  return freezeCp227Validation({
    handoff_id: "CP00-227-to-CP00-228",
    from_pack_id: DMS_CORE_CP227_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP227_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP227_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP227_PACK_BINDING.range,
    open_scope: "Continue RP06.P08.M02.S15 onward with the remaining P08 Hermes receipt rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP227_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp228CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp228Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp228Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-228 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP228_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-228");
  if (planPack?.risk_class !== DMS_CORE_CP228_PACK_BINDING.risk_class) errors.push("CP00-228 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP228_PACK_BINDING.unit_count) errors.push("CP00-228 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP228_PACK_BINDING.first_unit_id) errors.push("CP00-228 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP228_PACK_BINDING.last_unit_id) errors.push("CP00-228 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-228 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-228 must only include RP06 units");
  const summary = createDmsCoreCp228CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP228_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-228 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP228_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-228 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP228_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-228 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP228_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-228 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP228_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-228 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-228 ${microId} missing row ${title}`);
    }
  }
  return freezeCp228Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp228HermesReceiptSliceDescriptor(
  descriptor = createDmsCoreCp228HermesReceiptSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.hermes_receipt_slice_case_set ?? createDmsCoreCp228HermesReceiptSliceCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp228HermesReceiptSliceDescriptor") errors.push("CP00-228 descriptor type drift");
  if (descriptor.source_p07_closeout_p08_hermes_receipt_descriptor !== "DmsCoreCp227P07CloseoutP08HermesReceiptDescriptor") {
    errors.push("CP00-228 source p07 closeout p08 hermes receipt descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(DMS_CORE_CP228_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-228 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-228 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-228 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-228 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-228 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-228 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-228 ${microId} ${key} must not include raw payload`);
    }
  }
  const m03 = sections["RP06.P08.M03"]?.rows ?? {};
  if (m03.pass_semantics?.verdict_descriptor !== "PASS") errors.push("CP00-228 PASS semantics drift");
  if (m03.block_semantics?.blocks_closeout_descriptor !== true) errors.push("CP00-228 BLOCK semantics drift");
  if (m03.human_approval_marker?.human_approval_route_required_before_runtime !== true) errors.push("CP00-228 human approval marker drift");
  if (m03.claude_dependency_marker?.claude_final_approval_claimed !== false) errors.push("CP00-228 must not claim Claude final approval");
  if (m03.regression_receipt?.regression_detected !== false) errors.push("CP00-228 regression detected");
  if (m03.command_result_receipt?.executes_command_runtime !== false) errors.push("CP00-228 must not execute command runtime");
  if (m03.operator_summary?.hermes_packet_body_included !== false) errors.push("CP00-228 operator summary must not include Hermes packet body");
  if (DMS_CORE_CP228_NO_WRITE_ATTESTATION.regression_detected !== false) errors.push("CP00-228 regression attestation drift");
  if (DMS_CORE_CP228_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-228 must not promote Claude");
  if (DMS_CORE_CP228_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-228 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-228 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-228 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP228_PACK_BINDING.pack_id,
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-228 contract current_pack drift");
  }
  if (
    contractProjection?.hermes_receipt_slice_descriptor?.descriptor &&
    contractProjection.hermes_receipt_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-228 contract hermes_receipt_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP228_PACK_BINDING.next_pack_id) errors.push("CP00-228 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP228_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-228 next subphase drift");
  }
  return freezeCp228Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp228HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp228HermesReceiptSliceDescriptor(),
) {
  const coverage = validateDmsCoreCp228Coverage(planPack);
  const slice = validateDmsCoreCp228HermesReceiptSliceDescriptor(descriptor, contractProjection);
  return freezeCp228Validation({
    evidence_packet: "H06.CP00-228.dms_core_hermes_receipt_slice_descriptor",
    gate: DMS_CORE_CP228_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    hermes_receipt_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_closeout_p08_hermes_receipt_pack_id: DMS_CORE_CP228_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP228_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP228_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP228_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createDmsCoreCp228ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp228Coverage(planPack);
  const slice = validateDmsCoreCp228HermesReceiptSliceDescriptor(createDmsCoreCp228HermesReceiptSliceDescriptor(), {});
  return freezeCp228Validation({
    review_packet: "C06.CP00-228.dms_core_hermes_receipt_slice_descriptor",
    gate: DMS_CORE_CP228_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP228_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    hermes_receipt_slice_valid: slice.valid,
    invalid_review_blockers: DMS_CORE_CP228_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-228 cover exactly the 40 planned RP06 units from RP06.P08.M02.S15 through RP06.P08.M04.S12?",
      "Do the three descriptor sections (M02 type/shape tail, M03 primary slice with documentation update and operator summary, M04 head) cover every planned row title as descriptor-only rows?",
      "Does CP00-228 avoid Hermes runtime receipt emission, command runtime execution, regression, state writes, raw payloads, hermes packet bodies, permission decisions, audit bodies, fixture payloads, unauthorized data, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp228CloseoutHandoff() {
  return freezeCp228Validation({
    handoff_id: "CP00-228-to-CP00-229",
    from_pack_id: DMS_CORE_CP228_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP228_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP228_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP228_PACK_BINDING.range,
    open_scope: "Continue RP06.P08.M04.S13 onward with the remaining P08 Hermes receipt rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP228_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp229CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp229Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp229Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-229 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP229_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-229");
  if (planPack?.risk_class !== DMS_CORE_CP229_PACK_BINDING.risk_class) errors.push("CP00-229 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP229_PACK_BINDING.unit_count) errors.push("CP00-229 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP229_PACK_BINDING.first_unit_id) errors.push("CP00-229 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP229_PACK_BINDING.last_unit_id) errors.push("CP00-229 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-229 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-229 must only include RP06 units");
  const summary = createDmsCoreCp229CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP229_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-229 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP229_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-229 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP229_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-229 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP229_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-229 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP229_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-229 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-229 ${microId} missing row ${title}`);
    }
  }
  return freezeCp229Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp229HermesBindingSliceDescriptor(
  descriptor = createDmsCoreCp229HermesBindingSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.hermes_binding_slice_case_set ?? createDmsCoreCp229HermesBindingSliceCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp229HermesBindingSliceDescriptor") errors.push("CP00-229 descriptor type drift");
  if (descriptor.source_hermes_receipt_slice_descriptor !== "DmsCoreCp228HermesReceiptSliceDescriptor") {
    errors.push("CP00-229 source hermes receipt slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(DMS_CORE_CP229_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-229 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-229 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-229 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-229 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-229 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-229 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-229 ${microId} ${key} must not include raw payload`);
    }
  }
  const m05 = sections["RP06.P08.M05"]?.rows ?? {};
  if (m05.pass_semantics?.verdict_descriptor !== "PASS") errors.push("CP00-229 PASS semantics drift");
  if (m05.block_semantics?.blocks_closeout_descriptor !== true) errors.push("CP00-229 BLOCK semantics drift");
  if (m05.human_approval_marker?.human_approval_route_required_before_runtime !== true) errors.push("CP00-229 human approval marker drift");
  if (m05.claude_dependency_marker?.claude_final_approval_claimed !== false) errors.push("CP00-229 must not claim Claude final approval");
  if (m05.regression_receipt?.regression_detected !== false) errors.push("CP00-229 regression detected");
  if (m05.permission_summary_receipt?.permission_decision_detail_included !== false) errors.push("CP00-229 must not expose permission decisions");
  if (DMS_CORE_CP229_NO_WRITE_ATTESTATION.regression_detected !== false) errors.push("CP00-229 regression attestation drift");
  if (DMS_CORE_CP229_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-229 must not promote Claude");
  if (DMS_CORE_CP229_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-229 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-229 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-229 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP229_PACK_BINDING.pack_id,
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-229 contract current_pack drift");
  }
  if (
    contractProjection?.hermes_binding_slice_descriptor?.descriptor &&
    contractProjection.hermes_binding_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-229 contract hermes_binding_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP229_PACK_BINDING.next_pack_id) errors.push("CP00-229 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP229_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-229 next subphase drift");
  }
  return freezeCp229Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp229HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp229HermesBindingSliceDescriptor(),
) {
  const coverage = validateDmsCoreCp229Coverage(planPack);
  const slice = validateDmsCoreCp229HermesBindingSliceDescriptor(descriptor, contractProjection);
  return freezeCp229Validation({
    evidence_packet: "H06.CP00-229.dms_core_hermes_binding_slice_descriptor",
    gate: DMS_CORE_CP229_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    hermes_binding_slice_valid: slice.valid,
    no_real_data: true,
    source_hermes_receipt_slice_pack_id: DMS_CORE_CP229_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP229_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP229_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP229_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createDmsCoreCp229ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp229Coverage(planPack);
  const slice = validateDmsCoreCp229HermesBindingSliceDescriptor(createDmsCoreCp229HermesBindingSliceDescriptor(), {});
  return freezeCp229Validation({
    review_packet: "C06.CP00-229.dms_core_hermes_binding_slice_descriptor",
    gate: DMS_CORE_CP229_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP229_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    hermes_binding_slice_valid: slice.valid,
    invalid_review_blockers: DMS_CORE_CP229_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-229 cover exactly the 40 planned RP06 units from RP06.P08.M04.S13 through RP06.P08.M06.S08?",
      "Do the three descriptor sections (M04 secondary workflow tail, M05 permission/audit binding cycle, M06 synthetic fixture head) cover every planned row title as descriptor-only rows?",
      "Does CP00-229 avoid Hermes runtime receipt emission, command runtime execution, regression, state writes, raw payloads, hermes packet bodies, permission decisions, audit bodies, fixture payloads, unauthorized data, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp229CloseoutHandoff() {
  return freezeCp229Validation({
    handoff_id: "CP00-229-to-CP00-230",
    from_pack_id: DMS_CORE_CP229_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP229_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP229_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP229_PACK_BINDING.range,
    open_scope: "Continue RP06.P08.M06.S09 onward with the remaining P08 Hermes fixture rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP229_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp230CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp230Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp230Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-230 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP230_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-230");
  if (planPack?.risk_class !== DMS_CORE_CP230_PACK_BINDING.risk_class) errors.push("CP00-230 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP230_PACK_BINDING.unit_count) errors.push("CP00-230 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP230_PACK_BINDING.first_unit_id) errors.push("CP00-230 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP230_PACK_BINDING.last_unit_id) errors.push("CP00-230 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-230 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-230 must only include RP06 units");
  const summary = createDmsCoreCp230CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP230_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-230 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP230_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-230 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP230_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-230 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP230_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-230 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP230_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-230 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-230 ${microId} missing row ${title}`);
    }
  }
  return freezeCp230Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp230P08CloseoutP09ReviewGateDescriptor(
  descriptor = createDmsCoreCp230P08CloseoutP09ReviewGateDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_closeout_p09_review_gate_case_set ?? createDmsCoreCp230P08CloseoutP09ReviewGateCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp230P08CloseoutP09ReviewGateDescriptor") errors.push("CP00-230 descriptor type drift");
  if (descriptor.source_hermes_binding_slice_descriptor !== "DmsCoreCp229HermesBindingSliceDescriptor") {
    errors.push("CP00-230 source hermes binding slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(DMS_CORE_CP230_REQUIREMENTS.required_section_rows).length) errors.push("CP00-230 section count drift");
  for (const [microId, titles] of Object.entries(DMS_CORE_CP230_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-230 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-230 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-230 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-230 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-230 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-230 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-230 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const revSection of ["RP06.P09.M00", "RP06.P09.M01", "RP06.P09.M02", "RP06.P09.M03"]) {
    const rows = sections[revSection]?.rows ?? {};
    if (rows.permission_bypass_questions?.permission_bypass_detected !== false) errors.push(`CP00-230 ${revSection} permission bypass detected`);
    if (rows.ui_leak_questions?.leak_detected !== false) errors.push(`CP00-230 ${revSection} UI leak detected`);
    if (rows.audit_completeness_questions?.audit_event_body_included !== false) errors.push(`CP00-230 ${revSection} must not expose audit bodies`);
    if (!(rows.severity_taxonomy?.severity_levels ?? []).every((level, i) => ["P0", "P1", "P2", "P3"][i] === level)) {
      errors.push(`CP00-230 ${revSection} severity taxonomy drift`);
    }
    if (rows.go_no_go_verdict_format?.claude_final_approval_claimed !== false) errors.push(`CP00-230 ${revSection} must not claim Claude final approval`);
  }
  const m02 = sections["RP06.P09.M02"]?.rows ?? {};
  if (m02.human_approval_summary?.human_approval_route_required_before_runtime !== true) errors.push("CP00-230 human approval summary drift");
  if (m02.claude_review_packet?.read_only !== true) errors.push("CP00-230 Claude review packet must be read-only");
  if (m02.block_closeout_note?.blocks_closeout_descriptor !== true) errors.push("CP00-230 BLOCK closeout note drift");
  if (m02.command_rerun?.executes_command_runtime !== false) errors.push("CP00-230 must not execute command runtime");
  if (DMS_CORE_CP230_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-230 must not promote Claude");
  if (DMS_CORE_CP230_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-230 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-230 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-230 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP230_PACK_BINDING.pack_id,
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-230 contract current_pack drift");
  }
  if (
    contractProjection?.p08_closeout_p09_review_gate_descriptor?.descriptor &&
    contractProjection.p08_closeout_p09_review_gate_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-230 contract p08_closeout_p09_review_gate_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP230_PACK_BINDING.next_pack_id) errors.push("CP00-230 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP230_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-230 next subphase drift");
  }
  return freezeCp230Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp230HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp230P08CloseoutP09ReviewGateDescriptor(),
) {
  const coverage = validateDmsCoreCp230Coverage(planPack);
  const gate = validateDmsCoreCp230P08CloseoutP09ReviewGateDescriptor(descriptor, contractProjection);
  return freezeCp230Validation({
    evidence_packet: "H06.CP00-230.dms_core_p08_closeout_p09_review_gate_descriptor",
    gate: DMS_CORE_CP230_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_closeout_p09_review_gate_valid: gate.valid,
    no_real_data: true,
    source_hermes_binding_slice_pack_id: DMS_CORE_CP230_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP230_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP230_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP230_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && gate.valid,
  });
}

export function createDmsCoreCp230ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp230Coverage(planPack);
  const gate = validateDmsCoreCp230P08CloseoutP09ReviewGateDescriptor(createDmsCoreCp230P08CloseoutP09ReviewGateDescriptor(), {});
  return freezeCp230Validation({
    review_packet: "C06.CP00-230.dms_core_p08_closeout_p09_review_gate_descriptor",
    gate: DMS_CORE_CP230_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP230_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_closeout_p09_review_gate_valid: gate.valid,
    invalid_review_blockers: DMS_CORE_CP230_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-230 cover exactly the 150 planned RP06 units from RP06.P08.M06.S09 through RP06.P09.M03.S10?",
      "Do the nine descriptor sections (five P08 Hermes-receipt micros and four P09 review-gate micros) cover every planned row title as descriptor-only rows, including review question sets, risk registers, severity taxonomies, go/no-go verdict formats, finding routing maps, and closeout notes?",
      "Does CP00-230 avoid review runtime execution, Hermes runtime receipt emission, command runtime execution, permission bypass, UI leaks, state writes, raw payloads, hermes packet bodies, permission decisions, audit bodies, fixture payloads, unauthorized data, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp230CloseoutHandoff() {
  return freezeCp230Validation({
    handoff_id: "CP00-230-to-CP00-231",
    from_pack_id: DMS_CORE_CP230_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP230_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP230_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP230_PACK_BINDING.range,
    open_scope: "Continue RP06.P09.M03.S11 onward with the remaining P09 review-gate rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP230_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp231CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp231Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp231Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-231 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP231_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-231");
  if (planPack?.risk_class !== DMS_CORE_CP231_PACK_BINDING.risk_class) errors.push("CP00-231 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP231_PACK_BINDING.unit_count) errors.push("CP00-231 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP231_PACK_BINDING.first_unit_id) errors.push("CP00-231 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP231_PACK_BINDING.last_unit_id) errors.push("CP00-231 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-231 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-231 must only include RP06 units");
  const summary = createDmsCoreCp231CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP231_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-231 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP231_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-231 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP231_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-231 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP231_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-231 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP231_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-231 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-231 ${microId} missing row ${title}`);
    }
  }
  return freezeCp231Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp231ReviewGateSliceDescriptor(
  descriptor = createDmsCoreCp231ReviewGateSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.review_gate_slice_case_set ?? createDmsCoreCp231ReviewGateSliceCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp231ReviewGateSliceDescriptor") errors.push("CP00-231 descriptor type drift");
  if (descriptor.source_p08_closeout_p09_review_gate_descriptor !== "DmsCoreCp230P08CloseoutP09ReviewGateDescriptor") {
    errors.push("CP00-231 source p08 closeout p09 review gate descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(DMS_CORE_CP231_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-231 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-231 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-231 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-231 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-231 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-231 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-231 ${microId} ${key} must not include raw payload`);
    }
  }
  const m03 = sections["RP06.P09.M03"]?.rows ?? {};
  if (m03.review_receipt_placeholder?.review_receipt_descriptor_only !== true) errors.push("CP00-231 review receipt placeholder drift");
  if (m03.future_correction_slot?.future_correction_descriptor_only !== true) errors.push("CP00-231 future correction slot drift");
  if (m03.claude_review_packet?.read_only !== true) errors.push("CP00-231 Claude review packet must be read-only");
  if (m03.block_closeout_note?.blocks_closeout_descriptor !== true) errors.push("CP00-231 BLOCK closeout note drift");
  const m04 = sections["RP06.P09.M04"]?.rows ?? {};
  if (m04.permission_bypass_questions?.permission_bypass_detected !== false) errors.push("CP00-231 permission bypass detected");
  if (m04.ui_leak_questions?.leak_detected !== false) errors.push("CP00-231 UI leak detected");
  if (m04.go_no_go_verdict_format?.claude_final_approval_claimed !== false) errors.push("CP00-231 must not claim Claude final approval");
  if (m04.human_approval_summary?.human_approval_route_required_before_runtime !== true) errors.push("CP00-231 human approval summary drift");
  if (DMS_CORE_CP231_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-231 must not promote Claude");
  if (DMS_CORE_CP231_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-231 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-231 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-231 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      DMS_CORE_CP231_PACK_BINDING.pack_id,
      DMS_CORE_CP232_PACK_BINDING.pack_id,
      DMS_CORE_CP233_PACK_BINDING.pack_id,
      DMS_CORE_CP234_PACK_BINDING.pack_id,
    ].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-231 contract current_pack drift");
  }
  if (
    contractProjection?.review_gate_slice_descriptor?.descriptor &&
    contractProjection.review_gate_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-231 contract review_gate_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP231_PACK_BINDING.next_pack_id) errors.push("CP00-231 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP231_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-231 next subphase drift");
  }
  return freezeCp231Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp231HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp231ReviewGateSliceDescriptor(),
) {
  const coverage = validateDmsCoreCp231Coverage(planPack);
  const slice = validateDmsCoreCp231ReviewGateSliceDescriptor(descriptor, contractProjection);
  return freezeCp231Validation({
    evidence_packet: "H06.CP00-231.dms_core_review_gate_slice_descriptor",
    gate: DMS_CORE_CP231_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    review_gate_slice_valid: slice.valid,
    no_real_data: true,
    source_p08_closeout_p09_review_gate_pack_id: DMS_CORE_CP231_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP231_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP231_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP231_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createDmsCoreCp231ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp231Coverage(planPack);
  const slice = validateDmsCoreCp231ReviewGateSliceDescriptor(createDmsCoreCp231ReviewGateSliceDescriptor(), {});
  return freezeCp231Validation({
    review_packet: "C06.CP00-231.dms_core_review_gate_slice_descriptor",
    gate: DMS_CORE_CP231_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP231_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    review_gate_slice_valid: slice.valid,
    invalid_review_blockers: DMS_CORE_CP231_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-231 cover exactly the 40 planned RP06 units from RP06.P09.M03.S11 through RP06.P09.M05.S08?",
      "Do the three descriptor sections (M03 primary slice tail with review receipt placeholder and future correction slot, M04 secondary workflow cycle, M05 permission/audit binding head) cover every planned row title as descriptor-only rows?",
      "Does CP00-231 avoid review runtime execution, Hermes runtime receipt emission, command runtime execution, permission bypass, UI leaks, state writes, raw payloads, hermes packet bodies, permission decisions, audit bodies, unauthorized data, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp231CloseoutHandoff() {
  return freezeCp231Validation({
    handoff_id: "CP00-231-to-CP00-232",
    from_pack_id: DMS_CORE_CP231_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP231_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP231_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP231_PACK_BINDING.range,
    open_scope: "Continue RP06.P09.M05.S09 onward with the remaining P09 review-gate rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP231_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp232CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp232Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp232Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-232 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP232_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-232");
  if (planPack?.risk_class !== DMS_CORE_CP232_PACK_BINDING.risk_class) errors.push("CP00-232 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP232_PACK_BINDING.unit_count) errors.push("CP00-232 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP232_PACK_BINDING.first_unit_id) errors.push("CP00-232 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP232_PACK_BINDING.last_unit_id) errors.push("CP00-232 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-232 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-232 must only include RP06 units");
  const summary = createDmsCoreCp232CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP232_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-232 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP232_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-232 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP232_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-232 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP232_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-232 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP232_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-232 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-232 ${microId} missing row ${title}`);
    }
  }
  return freezeCp232Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp232ReviewAuditBindingDescriptor(
  descriptor = createDmsCoreCp232ReviewAuditBindingDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.review_audit_binding_case_set ?? createDmsCoreCp232ReviewAuditBindingCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp232ReviewAuditBindingDescriptor") errors.push("CP00-232 descriptor type drift");
  if (descriptor.source_review_gate_slice_descriptor !== "DmsCoreCp231ReviewGateSliceDescriptor") {
    errors.push("CP00-232 source review gate slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(DMS_CORE_CP232_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-232 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-232 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-232 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-232 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-232 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-232 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-232 ${microId} ${key} must not include raw payload`);
    }
  }
  const m05 = sections["RP06.P09.M05"]?.rows ?? {};
  if (!(m05.severity_taxonomy?.severity_levels ?? []).every((level, i) => ["P0", "P1", "P2", "P3"][i] === level)) {
    errors.push("CP00-232 severity taxonomy drift");
  }
  if (m05.go_no_go_verdict_format?.claude_final_approval_claimed !== false) errors.push("CP00-232 must not claim Claude final approval");
  if (m05.human_approval_summary?.human_approval_route_required_before_runtime !== true) errors.push("CP00-232 human approval summary drift");
  if (m05.claude_review_packet?.read_only !== true) errors.push("CP00-232 Claude review packet must be read-only");
  if (m05.block_closeout_note?.blocks_closeout_descriptor !== true) errors.push("CP00-232 BLOCK closeout note drift");
  if (DMS_CORE_CP232_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-232 must not promote Claude");
  if (DMS_CORE_CP232_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-232 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-232 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-232 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![DMS_CORE_CP232_PACK_BINDING.pack_id, DMS_CORE_CP233_PACK_BINDING.pack_id, DMS_CORE_CP234_PACK_BINDING.pack_id].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-232 contract current_pack drift");
  }
  if (
    contractProjection?.review_audit_binding_descriptor?.descriptor &&
    contractProjection.review_audit_binding_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-232 contract review_audit_binding_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP232_PACK_BINDING.next_pack_id) errors.push("CP00-232 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP232_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-232 next subphase drift");
  }
  return freezeCp232Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp232HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp232ReviewAuditBindingDescriptor(),
) {
  const coverage = validateDmsCoreCp232Coverage(planPack);
  const binding = validateDmsCoreCp232ReviewAuditBindingDescriptor(descriptor, contractProjection);
  return freezeCp232Validation({
    evidence_packet: "H06.CP00-232.dms_core_review_audit_binding_descriptor",
    gate: DMS_CORE_CP232_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    review_audit_binding_valid: binding.valid,
    no_real_data: true,
    source_review_gate_slice_pack_id: DMS_CORE_CP232_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP232_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP232_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP232_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && binding.valid,
  });
}

export function createDmsCoreCp232ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp232Coverage(planPack);
  const binding = validateDmsCoreCp232ReviewAuditBindingDescriptor(createDmsCoreCp232ReviewAuditBindingDescriptor(), {});
  return freezeCp232Validation({
    review_packet: "C06.CP00-232.dms_core_review_audit_binding_descriptor",
    gate: DMS_CORE_CP232_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP232_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    review_audit_binding_valid: binding.valid,
    invalid_review_blockers: DMS_CORE_CP232_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-232 cover exactly the 10 planned RP06 units from RP06.P09.M05.S09 through RP06.P09.M05.S18?",
      "Does the M05 permission/audit binding section cover every planned row title as descriptor-only rows, including severity taxonomy, go/no-go verdict format, finding routing map, human approval summary, Claude review packet, closeout criteria/notes, and next-RP dependency?",
      "Does CP00-232 avoid review runtime execution, command runtime execution, permission bypass, state writes, raw payloads, permission decisions, audit bodies, unauthorized data, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp232CloseoutHandoff() {
  return freezeCp232Validation({
    handoff_id: "CP00-232-to-CP00-233",
    from_pack_id: DMS_CORE_CP232_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP232_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP232_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP232_PACK_BINDING.range,
    open_scope: "Continue RP06.P09.M05.S19 onward with the remaining P09 review-gate rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP232_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp233CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp233Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp233Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-233 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP233_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-233");
  if (planPack?.risk_class !== DMS_CORE_CP233_PACK_BINDING.risk_class) errors.push("CP00-233 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP233_PACK_BINDING.unit_count) errors.push("CP00-233 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP233_PACK_BINDING.first_unit_id) errors.push("CP00-233 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP233_PACK_BINDING.last_unit_id) errors.push("CP00-233 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-233 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-233 must only include RP06 units");
  const summary = createDmsCoreCp233CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP233_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-233 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP233_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-233 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP233_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-233 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP233_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-233 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP233_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-233 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-233 ${microId} missing row ${title}`);
    }
  }
  return freezeCp233Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp233ReviewFixtureTailDescriptor(
  descriptor = createDmsCoreCp233ReviewFixtureTailDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.review_fixture_tail_case_set ?? createDmsCoreCp233ReviewFixtureTailCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp233ReviewFixtureTailDescriptor") errors.push("CP00-233 descriptor type drift");
  if (descriptor.source_review_audit_binding_descriptor !== "DmsCoreCp232ReviewAuditBindingDescriptor") {
    errors.push("CP00-233 source review audit binding descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(DMS_CORE_CP233_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-233 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-233 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-233 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-233 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-233 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-233 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-233 ${microId} ${key} must not include raw payload`);
    }
  }
  const m05 = sections["RP06.P09.M05"]?.rows ?? {};
  if (m05.review_receipt_placeholder?.review_receipt_descriptor_only !== true) errors.push("CP00-233 review receipt placeholder drift");
  if (m05.future_correction_slot?.future_correction_descriptor_only !== true) errors.push("CP00-233 future correction slot drift");
  if (m05.command_rerun?.executes_command_runtime !== false) errors.push("CP00-233 must not execute command runtime");
  const m06 = sections["RP06.P09.M06"]?.rows ?? {};
  if (m06.permission_bypass_questions?.permission_bypass_detected !== false) errors.push("CP00-233 permission bypass detected");
  if (m06.ui_leak_questions?.leak_detected !== false) errors.push("CP00-233 UI leak detected");
  if (m06.audit_completeness_questions?.audit_event_body_included !== false) errors.push("CP00-233 must not expose audit bodies");
  if (DMS_CORE_CP233_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-233 must not promote Claude");
  if (DMS_CORE_CP233_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-233 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-233 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-233 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![DMS_CORE_CP233_PACK_BINDING.pack_id, DMS_CORE_CP234_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-233 contract current_pack drift");
  }
  if (
    contractProjection?.review_fixture_tail_descriptor?.descriptor &&
    contractProjection.review_fixture_tail_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-233 contract review_fixture_tail_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP233_PACK_BINDING.next_pack_id) errors.push("CP00-233 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP233_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-233 next subphase drift");
  }
  return freezeCp233Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp233HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp233ReviewFixtureTailDescriptor(),
) {
  const coverage = validateDmsCoreCp233Coverage(planPack);
  const tail = validateDmsCoreCp233ReviewFixtureTailDescriptor(descriptor, contractProjection);
  return freezeCp233Validation({
    evidence_packet: "H06.CP00-233.dms_core_review_fixture_tail_descriptor",
    gate: DMS_CORE_CP233_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    review_fixture_tail_valid: tail.valid,
    no_real_data: true,
    source_review_audit_binding_pack_id: DMS_CORE_CP233_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP233_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP233_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP233_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && tail.valid,
  });
}

export function createDmsCoreCp233ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp233Coverage(planPack);
  const tail = validateDmsCoreCp233ReviewFixtureTailDescriptor(createDmsCoreCp233ReviewFixtureTailDescriptor(), {});
  return freezeCp233Validation({
    review_packet: "C06.CP00-233.dms_core_review_fixture_tail_descriptor",
    gate: DMS_CORE_CP233_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP233_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    review_fixture_tail_valid: tail.valid,
    invalid_review_blockers: DMS_CORE_CP233_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-233 cover exactly the 10 planned RP06 units from RP06.P09.M05.S19 through RP06.P09.M06.S06?",
      "Do the two descriptor sections (M05 permission/audit binding tail and M06 synthetic fixture head) cover every planned row title as descriptor-only rows?",
      "Does CP00-233 avoid review runtime execution, command runtime execution, permission bypass, UI leaks, state writes, raw payloads, permission decisions, audit bodies, unauthorized data, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp233CloseoutHandoff() {
  return freezeCp233Validation({
    handoff_id: "CP00-233-to-CP00-234",
    from_pack_id: DMS_CORE_CP233_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP233_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP233_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP233_PACK_BINDING.range,
    open_scope: "Continue RP06.P09.M06.S07 onward with the remaining P09 fixture rows and downstream micros while preserving descriptor-only DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP233_PACK_BINDING.production_ready_flag,
  });
}

export function createDmsCoreCp234CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp234Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateDmsCoreCp234Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-234 plan pack is required");
  if (planPack?.pack_id !== DMS_CORE_CP234_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-234");
  if (planPack?.risk_class !== DMS_CORE_CP234_PACK_BINDING.risk_class) errors.push("CP00-234 risk class drift");
  if (planPack?.unit_count !== DMS_CORE_CP234_PACK_BINDING.unit_count) errors.push("CP00-234 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== DMS_CORE_CP234_PACK_BINDING.first_unit_id) errors.push("CP00-234 first unit drift");
  if (unitIds.at(-1) !== DMS_CORE_CP234_PACK_BINDING.last_unit_id) errors.push("CP00-234 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-234 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP06")) errors.push("CP00-234 must only include RP06 units");
  const summary = createDmsCoreCp234CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(DMS_CORE_CP234_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-234 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(DMS_CORE_CP234_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-234 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(DMS_CORE_CP234_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-234 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(DMS_CORE_CP234_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-234 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(DMS_CORE_CP234_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-234 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-234 ${microId} missing row ${title}`);
    }
  }
  return freezeCp234Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateDmsCoreCp234P09ReviewCloseoutDescriptor(
  descriptor = createDmsCoreCp234P09ReviewCloseoutDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_review_closeout_case_set ?? createDmsCoreCp234P09ReviewCloseoutCaseSet();
  if (descriptor.descriptor !== "DmsCoreCp234P09ReviewCloseoutDescriptor") errors.push("CP00-234 descriptor type drift");
  if (descriptor.source_review_fixture_tail_descriptor !== "DmsCoreCp233ReviewFixtureTailDescriptor") {
    errors.push("CP00-234 source review fixture tail descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(DMS_CORE_CP234_REQUIREMENTS.required_section_rows).length) errors.push("CP00-234 section count drift");
  for (const [microId, titles] of Object.entries(DMS_CORE_CP234_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-234 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-234 ${microId} row count drift`);
    for (const title of titles) {
      const key = dmsCoreCp210RowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-234 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-234 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-234 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-234 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-234 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const revSection of ["RP06.P09.M07", "RP06.P09.M08", "RP06.P09.M09", "RP06.P09.M10"]) {
    const rows = sections[revSection]?.rows ?? {};
    if (rows.permission_bypass_questions?.permission_bypass_detected !== false) errors.push(`CP00-234 ${revSection} permission bypass detected`);
    if (rows.ui_leak_questions?.leak_detected !== false) errors.push(`CP00-234 ${revSection} UI leak detected`);
    if (!(rows.severity_taxonomy?.severity_levels ?? []).every((level, i) => ["P0", "P1", "P2", "P3"][i] === level)) {
      errors.push(`CP00-234 ${revSection} severity taxonomy drift`);
    }
    if (rows.go_no_go_verdict_format?.claude_final_approval_claimed !== false) errors.push(`CP00-234 ${revSection} must not claim Claude final approval`);
  }
  const m06 = sections["RP06.P09.M06"]?.rows ?? {};
  if (m06.documentation_update?.documentation_entry !== "packages/dms/README.md#cp00-234") errors.push("CP00-234 documentation anchor drift");
  if (m06.command_rerun?.executes_command_runtime !== false) errors.push("CP00-234 must not execute command runtime");
  if (m06.claude_review_packet?.read_only !== true) errors.push("CP00-234 Claude review packet must be read-only");
  const m07 = sections["RP06.P09.M07"]?.rows ?? {};
  if (m07.review_receipt_placeholder?.review_receipt_descriptor_only !== true) errors.push("CP00-234 review receipt placeholder drift");
  if (m07.future_correction_slot?.future_correction_descriptor_only !== true) errors.push("CP00-234 future correction slot drift");
  if (DMS_CORE_CP234_NO_WRITE_ATTESTATION.closes_rp06_p09_descriptor_scope !== true) errors.push("CP00-234 must close RP06.P09 descriptor scope");
  if (DMS_CORE_CP234_NO_WRITE_ATTESTATION.opens_rp07_runtime !== false) errors.push("CP00-234 must not open RP07 runtime");
  if (DMS_CORE_CP234_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-234 must not promote Claude");
  if (DMS_CORE_CP234_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-234 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-234 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-234 local validation trust boundary drift");
  }
  if (contractProjection?.current_pack?.pack_id && contractProjection.current_pack.pack_id !== DMS_CORE_CP234_PACK_BINDING.pack_id) {
    errors.push("CP00-234 contract current_pack drift");
  }
  if (
    contractProjection?.p09_review_closeout_descriptor?.descriptor &&
    contractProjection.p09_review_closeout_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-234 contract p09_review_closeout_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== DMS_CORE_CP234_PACK_BINDING.next_pack_id) errors.push("CP00-234 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== DMS_CORE_CP234_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-234 next subphase drift");
  }
  return freezeCp234Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createDmsCoreCp234HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createDmsCoreCp234P09ReviewCloseoutDescriptor(),
) {
  const coverage = validateDmsCoreCp234Coverage(planPack);
  const closeout = validateDmsCoreCp234P09ReviewCloseoutDescriptor(descriptor, contractProjection);
  return freezeCp234Validation({
    evidence_packet: "H06.CP00-234.dms_core_p09_review_closeout_descriptor",
    gate: DMS_CORE_CP234_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_review_closeout_valid: closeout.valid,
    no_real_data: true,
    source_review_fixture_tail_pack_id: DMS_CORE_CP234_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: DMS_CORE_CP234_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: DMS_CORE_CP234_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP234_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && closeout.valid,
  });
}

export function createDmsCoreCp234ClaudeReviewPacket(planPack) {
  const coverage = validateDmsCoreCp234Coverage(planPack);
  const closeout = validateDmsCoreCp234P09ReviewCloseoutDescriptor(createDmsCoreCp234P09ReviewCloseoutDescriptor(), {});
  return freezeCp234Validation({
    review_packet: "C06.CP00-234.dms_core_p09_review_closeout_descriptor",
    gate: DMS_CORE_CP234_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: DMS_CORE_CP234_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_review_closeout_valid: closeout.valid,
    invalid_review_blockers: DMS_CORE_CP234_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-234 cover exactly the 86 planned RP06 units from RP06.P09.M06.S07 through RP06.P09.M10.S10?",
      "Do the five descriptor sections (M06 fixture tail, M07 test/golden cycle with review receipt placeholder and future correction slot, M08 Hermes packet, M09 Claude review packet, M10 closeout head) cover every planned row title as descriptor-only rows?",
      "Does CP00-234 avoid review runtime execution, command runtime execution, permission bypass, UI leaks, RP07 runtime opening, state writes, raw payloads, hermes packet bodies, permission decisions, audit bodies, unauthorized data, object storage, OCR, search, email, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createDmsCoreCp234CloseoutHandoff() {
  return freezeCp234Validation({
    handoff_id: "CP00-234-to-CP00-235",
    from_pack_id: DMS_CORE_CP234_PACK_BINDING.pack_id,
    to_pack_id: DMS_CORE_CP234_PACK_BINDING.next_pack_id,
    next_subphase_id: DMS_CORE_CP234_PACK_BINDING.next_subphase_id,
    closed_scope: DMS_CORE_CP234_PACK_BINDING.range,
    open_scope: "RP06.P09 descriptor scope is closed; continue at RP07.P00.M00.S01 through the live queue while preserving descriptor-only boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: DMS_CORE_CP234_PACK_BINDING.production_ready_flag,
  });
}
