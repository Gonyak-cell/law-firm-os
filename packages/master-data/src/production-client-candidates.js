// The production deployment artifact imports client records only from the
// exact, signed migration inventory. No real client source is packaged in code.
export const AMIC_CURRENT_CLIENT_CANDIDATES = Object.freeze([]);
