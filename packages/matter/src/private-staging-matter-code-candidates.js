// Synthetic/private-local desktop staging replacement for the production
// matter candidate corpus. Keep the export contract, but never package the
// current-client/current-matter values in a desktop runtime.
export const AMIC_CURRENT_MATTER_CODES_SCHEMA_VERSION = "lawos.amic_matter_codes.v1";
export const AMIC_CURRENT_MATTER_CLIENTS = Object.freeze([]);
export const AMIC_CURRENT_MATTER_CODE_CANDIDATES = Object.freeze([]);
