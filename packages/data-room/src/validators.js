import {
  DATA_ROOM_VDR_CORE_CP610_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP610_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP610_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP611_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP611_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP611_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP612_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP612_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP612_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP613_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP613_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP613_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP614_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP614_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP614_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP615_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP615_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP615_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP616_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP616_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP616_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP617_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP617_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP617_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP618_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP618_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP618_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP619_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP619_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP619_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP620_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP620_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP620_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP621_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP621_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP621_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP622_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP622_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP622_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP623_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP623_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP623_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP624_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP624_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP624_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP625_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP625_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP625_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP626_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP626_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP626_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP627_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP627_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP627_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP628_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP628_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP628_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP629_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP629_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP629_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP630_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP630_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP630_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP631_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP631_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP631_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP632_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP632_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP632_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP633_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP633_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP633_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP634_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP634_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP634_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP635_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP635_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP635_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP636_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP636_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP636_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP637_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP637_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP637_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP638_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP638_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP638_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP639_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP639_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP639_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP640_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP640_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP640_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP641_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP641_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP641_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP642_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP642_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP642_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP643_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP643_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP643_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_CP644_NO_WRITE_ATTESTATION,
  DATA_ROOM_VDR_CORE_CP644_PACK_BINDING,
  DATA_ROOM_VDR_CORE_CP644_REQUIREMENTS,
  DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT,
} from "./registry.js";
import {
  createDataRoomVdrCoreCp644P09ReviewCloseoutTailDescriptor,
  createDataRoomVdrCoreCp643P08P09ReviewCloseoutBridgeDescriptor,
  createDataRoomVdrCoreCp642P08HermesEvidenceSyntheticFixtureTailDescriptor,
  createDataRoomVdrCoreCp641P08HermesEvidenceSecondaryPermissionFixtureBridgeDescriptor,
  createDataRoomVdrCoreCp640P08HermesEvidenceTypePrimarySecondaryBridgeDescriptor,
  createDataRoomVdrCoreCp639P07FixtureCloseoutP08HermesEvidenceBridgeDescriptor,
  createDataRoomVdrCoreCp638P07SecondaryTailPermissionAuditFixtureHeadDescriptor,
  createDataRoomVdrCoreCp637P07FailureRecoveryTypePrimarySecondaryBridgeDescriptor,
  createDataRoomVdrCoreCp636P06P07FixtureFailureBridgeDescriptor,
  createDataRoomVdrCoreCp635P06SyntheticFixtureHeadDescriptor,
  createDataRoomVdrCoreCp634P06PermissionAuditBindingTailDescriptor,
  createDataRoomVdrCoreCp633P06PermissionAuditBindingMiddleDescriptor,
  createDataRoomVdrCoreCp632P06SecondaryTailPermissionAuditBindingHeadDescriptor,
  createDataRoomVdrCoreCp631P06TypeShapeTailPrimarySecondaryBridgeDescriptor,
  createDataRoomVdrCoreCp630P05FixtureCloseoutP06PermissionMatrixBridgeDescriptor,
  createDataRoomVdrCoreCp626P05PrimaryImplementationHeadDescriptor,
  createDataRoomVdrCoreCp627P05PrimaryImplementationTailDescriptor,
  createDataRoomVdrCoreCp628P05SecondaryWorkflowPermissionAuditBridgeDescriptor,
  createDataRoomVdrCoreCp629P05PermissionAuditTailSyntheticFixtureHeadDescriptor,
  createDataRoomVdrCoreCp625P05FixtureTypeShapeTailDescriptor,
  createDataRoomVdrCoreCp624P05FixtureTypeShapeHeadDescriptor,
  createDataRoomVdrCoreCp623P05FixtureContractDraftTailDescriptor,
  createDataRoomVdrCoreCp622P05FixtureContractDraftDescriptor,
  createDataRoomVdrCoreCp621P04UiP05FixtureBridgeDescriptor,
  createDataRoomVdrCoreCp620P03ApiP04UiBridgeDescriptor,
  createDataRoomVdrCoreCp619P02ClaudeP03ApiInterfaceBridgeDescriptor,
  createDataRoomVdrCoreCp618P02TestHermesClaudeBridgeDescriptor,
  createDataRoomVdrCoreCp617P02TestGoldenCaseCoreDescriptor,
  createDataRoomVdrCoreCp616P02FixtureTailTestHeadDescriptor,
  createDataRoomVdrCoreCp615P02PermissionAuditFixtureHeadDescriptor,
  createDataRoomVdrCoreCp614P02PrimarySecondaryServiceDescriptor,
  createDataRoomVdrCoreCp613P01P02ServiceFoundationBridgeDescriptor,
  createDataRoomVdrCoreCp612P01SecondaryWorkflowPermissionAuditDescriptor,
  createDataRoomVdrCoreCp611P01ModelFoundationContinuationDescriptor,
  createDataRoomVdrCoreCp610ScopeContractFoundationDescriptor,
  dataRoomVdrCoreRowKey,
} from "./service.js";

function assertCondition(condition, message) {
  if (!condition) {
    throw new Error(message);
  }
}

export function createDataRoomVdrCoreCp610CoverageSummary(descriptor = createDataRoomVdrCoreCp610ScopeContractFoundationDescriptor()) {
  const caseSet = descriptor.scope_contract_foundation_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP610_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp610Coverage(descriptor = createDataRoomVdrCoreCp610ScopeContractFoundationDescriptor()) {
  const summary = createDataRoomVdrCoreCp610CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP610_REQUIREMENTS.expected_unit_count, "CP00-610 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP610_REQUIREMENTS.expected_section_count, "CP00-610 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP610_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.scope_contract_foundation_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-610 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP610_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP610_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp610ScopeContractFoundationDescriptor(
  descriptor = createDataRoomVdrCoreCp610ScopeContractFoundationDescriptor(),
) {
  validateDataRoomVdrCoreCp610Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp610ScopeContractFoundationDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP610_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP610_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp610CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp610HermesEvidencePacket(descriptor = createDataRoomVdrCoreCp610ScopeContractFoundationDescriptor()) {
  validateDataRoomVdrCoreCp610ScopeContractFoundationDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP610_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP610_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP610_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp610ClaudeReviewPacket(descriptor = createDataRoomVdrCoreCp610ScopeContractFoundationDescriptor()) {
  validateDataRoomVdrCoreCp610ScopeContractFoundationDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP610_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP610_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP610_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP610_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp610CloseoutHandoff(descriptor = createDataRoomVdrCoreCp610ScopeContractFoundationDescriptor()) {
  validateDataRoomVdrCoreCp610ScopeContractFoundationDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP610_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP610_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP610_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP610_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP610_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp611CoverageSummary(
  descriptor = createDataRoomVdrCoreCp611P01ModelFoundationContinuationDescriptor(),
) {
  const caseSet = descriptor.p01_model_foundation_continuation_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP611_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp611Coverage(
  descriptor = createDataRoomVdrCoreCp611P01ModelFoundationContinuationDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp611CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP611_REQUIREMENTS.expected_unit_count, "CP00-611 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP611_REQUIREMENTS.expected_section_count, "CP00-611 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP611_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p01_model_foundation_continuation_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-611 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP611_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP611_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp611P01ModelFoundationContinuationDescriptor(
  descriptor = createDataRoomVdrCoreCp611P01ModelFoundationContinuationDescriptor(),
) {
  validateDataRoomVdrCoreCp611Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp611P01ModelFoundationContinuationDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP611_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP611_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp611CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp611HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp611P01ModelFoundationContinuationDescriptor(),
) {
  validateDataRoomVdrCoreCp611P01ModelFoundationContinuationDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP611_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP611_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP611_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp611ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp611P01ModelFoundationContinuationDescriptor(),
) {
  validateDataRoomVdrCoreCp611P01ModelFoundationContinuationDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP611_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP611_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP611_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP611_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp611CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp611P01ModelFoundationContinuationDescriptor(),
) {
  validateDataRoomVdrCoreCp611P01ModelFoundationContinuationDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP611_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP611_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP611_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP611_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP611_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp612CoverageSummary(
  descriptor = createDataRoomVdrCoreCp612P01SecondaryWorkflowPermissionAuditDescriptor(),
) {
  const caseSet = descriptor.p01_secondary_workflow_permission_audit_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP612_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp612Coverage(
  descriptor = createDataRoomVdrCoreCp612P01SecondaryWorkflowPermissionAuditDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp612CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP612_REQUIREMENTS.expected_unit_count, "CP00-612 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP612_REQUIREMENTS.expected_section_count, "CP00-612 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP612_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p01_secondary_workflow_permission_audit_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-612 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP612_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP612_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp612P01SecondaryWorkflowPermissionAuditDescriptor(
  descriptor = createDataRoomVdrCoreCp612P01SecondaryWorkflowPermissionAuditDescriptor(),
) {
  validateDataRoomVdrCoreCp612Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp612P01SecondaryWorkflowPermissionAuditDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP612_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.loads_real_fixture_data === false, "descriptor must not load real fixture data");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP612_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp612CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp612HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp612P01SecondaryWorkflowPermissionAuditDescriptor(),
) {
  validateDataRoomVdrCoreCp612P01SecondaryWorkflowPermissionAuditDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP612_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP612_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP612_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp612ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp612P01SecondaryWorkflowPermissionAuditDescriptor(),
) {
  validateDataRoomVdrCoreCp612P01SecondaryWorkflowPermissionAuditDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP612_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP612_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP612_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP612_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp612CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp612P01SecondaryWorkflowPermissionAuditDescriptor(),
) {
  validateDataRoomVdrCoreCp612P01SecondaryWorkflowPermissionAuditDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP612_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP612_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP612_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP612_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP612_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp613CoverageSummary(
  descriptor = createDataRoomVdrCoreCp613P01P02ServiceFoundationBridgeDescriptor(),
) {
  const caseSet = descriptor.p01_p02_service_foundation_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP613_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp613Coverage(
  descriptor = createDataRoomVdrCoreCp613P01P02ServiceFoundationBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp613CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP613_REQUIREMENTS.expected_unit_count, "CP00-613 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP613_REQUIREMENTS.expected_section_count, "CP00-613 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP613_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p01_p02_service_foundation_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-613 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP613_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP613_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp613P01P02ServiceFoundationBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp613P01P02ServiceFoundationBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp613Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp613P01P02ServiceFoundationBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP613_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_api_handler === false, "descriptor must not execute API handlers");
  assertCondition(descriptor.executes_service_entrypoint_runtime === false, "descriptor must not execute service entrypoint runtime");
  assertCondition(descriptor.executes_request_normalization_runtime === false, "descriptor must not execute request normalization runtime");
  assertCondition(descriptor.executes_permission_precheck_runtime === false, "descriptor must not execute permission precheck runtime");
  assertCondition(descriptor.executes_audit_hint_runtime === false, "descriptor must not execute audit hint runtime");
  assertCondition(descriptor.executes_failure_recovery_runtime === false, "descriptor must not execute failure recovery runtime");
  assertCondition(descriptor.executes_integration_smoke_runtime === false, "descriptor must not execute integration smoke runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP613_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp613CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp613HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp613P01P02ServiceFoundationBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp613P01P02ServiceFoundationBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP613_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP613_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP613_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp613ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp613P01P02ServiceFoundationBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp613P01P02ServiceFoundationBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP613_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP613_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP613_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP613_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp613CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp613P01P02ServiceFoundationBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp613P01P02ServiceFoundationBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP613_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP613_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP613_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP613_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP613_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp614CoverageSummary(
  descriptor = createDataRoomVdrCoreCp614P02PrimarySecondaryServiceDescriptor(),
) {
  const caseSet = descriptor.p02_primary_secondary_service_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP614_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp614Coverage(
  descriptor = createDataRoomVdrCoreCp614P02PrimarySecondaryServiceDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp614CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP614_REQUIREMENTS.expected_unit_count, "CP00-614 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP614_REQUIREMENTS.expected_section_count, "CP00-614 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP614_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p02_primary_secondary_service_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-614 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP614_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP614_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp614P02PrimarySecondaryServiceDescriptor(
  descriptor = createDataRoomVdrCoreCp614P02PrimarySecondaryServiceDescriptor(),
) {
  validateDataRoomVdrCoreCp614Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp614P02PrimarySecondaryServiceDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP614_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_api_handler === false, "descriptor must not execute API handlers");
  assertCondition(descriptor.executes_service_entrypoint_runtime === false, "descriptor must not execute service entrypoint runtime");
  assertCondition(descriptor.executes_request_normalization_runtime === false, "descriptor must not execute request normalization runtime");
  assertCondition(descriptor.executes_permission_precheck_runtime === false, "descriptor must not execute permission precheck runtime");
  assertCondition(descriptor.executes_audit_hint_runtime === false, "descriptor must not execute audit hint runtime");
  assertCondition(descriptor.executes_primary_implementation_runtime === false, "descriptor must not execute primary implementation runtime");
  assertCondition(descriptor.executes_secondary_workflow_runtime === false, "descriptor must not execute secondary workflow runtime");
  assertCondition(descriptor.executes_failure_recovery_runtime === false, "descriptor must not execute failure recovery runtime");
  assertCondition(descriptor.executes_integration_smoke_runtime === false, "descriptor must not execute integration smoke runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP614_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp614CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp614HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp614P02PrimarySecondaryServiceDescriptor(),
) {
  validateDataRoomVdrCoreCp614P02PrimarySecondaryServiceDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP614_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP614_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP614_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp614ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp614P02PrimarySecondaryServiceDescriptor(),
) {
  validateDataRoomVdrCoreCp614P02PrimarySecondaryServiceDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP614_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP614_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP614_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP614_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp614CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp614P02PrimarySecondaryServiceDescriptor(),
) {
  validateDataRoomVdrCoreCp614P02PrimarySecondaryServiceDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP614_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP614_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP614_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP614_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP614_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp615CoverageSummary(
  descriptor = createDataRoomVdrCoreCp615P02PermissionAuditFixtureHeadDescriptor(),
) {
  const caseSet = descriptor.p02_permission_audit_fixture_head_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP615_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp615Coverage(
  descriptor = createDataRoomVdrCoreCp615P02PermissionAuditFixtureHeadDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp615CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP615_REQUIREMENTS.expected_unit_count, "CP00-615 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP615_REQUIREMENTS.expected_section_count, "CP00-615 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP615_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p02_permission_audit_fixture_head_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-615 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP615_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP615_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp615P02PermissionAuditFixtureHeadDescriptor(
  descriptor = createDataRoomVdrCoreCp615P02PermissionAuditFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp615Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp615P02PermissionAuditFixtureHeadDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP615_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_api_handler === false, "descriptor must not execute API handlers");
  assertCondition(descriptor.executes_service_entrypoint_runtime === false, "descriptor must not execute service entrypoint runtime");
  assertCondition(descriptor.executes_request_normalization_runtime === false, "descriptor must not execute request normalization runtime");
  assertCondition(descriptor.executes_permission_precheck_runtime === false, "descriptor must not execute permission precheck runtime");
  assertCondition(descriptor.executes_audit_hint_runtime === false, "descriptor must not execute audit hint runtime");
  assertCondition(descriptor.executes_primary_implementation_runtime === false, "descriptor must not execute primary implementation runtime");
  assertCondition(descriptor.executes_secondary_workflow_runtime === false, "descriptor must not execute secondary workflow runtime");
  assertCondition(descriptor.executes_permission_audit_binding_runtime === false, "descriptor must not execute permission audit binding runtime");
  assertCondition(descriptor.executes_fixture_head_runtime === false, "descriptor must not execute fixture head runtime");
  assertCondition(descriptor.executes_failure_recovery_runtime === false, "descriptor must not execute failure recovery runtime");
  assertCondition(descriptor.executes_integration_smoke_runtime === false, "descriptor must not execute integration smoke runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP615_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp615CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp615HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp615P02PermissionAuditFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp615P02PermissionAuditFixtureHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP615_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP615_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP615_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp615ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp615P02PermissionAuditFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp615P02PermissionAuditFixtureHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP615_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP615_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP615_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP615_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp615CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp615P02PermissionAuditFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp615P02PermissionAuditFixtureHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP615_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP615_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP615_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP615_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP615_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp616CoverageSummary(
  descriptor = createDataRoomVdrCoreCp616P02FixtureTailTestHeadDescriptor(),
) {
  const caseSet = descriptor.p02_fixture_tail_test_head_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP616_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp616Coverage(
  descriptor = createDataRoomVdrCoreCp616P02FixtureTailTestHeadDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp616CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP616_REQUIREMENTS.expected_unit_count, "CP00-616 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP616_REQUIREMENTS.expected_section_count, "CP00-616 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP616_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p02_fixture_tail_test_head_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-616 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP616_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP616_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp616P02FixtureTailTestHeadDescriptor(
  descriptor = createDataRoomVdrCoreCp616P02FixtureTailTestHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp616Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp616P02FixtureTailTestHeadDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP616_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_api_handler === false, "descriptor must not execute API handlers");
  assertCondition(descriptor.executes_service_entrypoint_runtime === false, "descriptor must not execute service entrypoint runtime");
  assertCondition(descriptor.executes_request_normalization_runtime === false, "descriptor must not execute request normalization runtime");
  assertCondition(descriptor.executes_unit_test_runtime_paths === false, "descriptor must not execute unit test runtime paths");
  assertCondition(descriptor.executes_failure_recovery_runtime === false, "descriptor must not execute failure recovery runtime");
  assertCondition(descriptor.executes_integration_smoke_runtime === false, "descriptor must not execute integration smoke runtime");
  assertCondition(descriptor.executes_fixture_tail_runtime === false, "descriptor must not execute fixture tail runtime");
  assertCondition(descriptor.executes_test_golden_case_runtime === false, "descriptor must not execute test golden case runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP616_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp616CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp616HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp616P02FixtureTailTestHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp616P02FixtureTailTestHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP616_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP616_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP616_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp616ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp616P02FixtureTailTestHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp616P02FixtureTailTestHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP616_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP616_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP616_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP616_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp616CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp616P02FixtureTailTestHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp616P02FixtureTailTestHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP616_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP616_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP616_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP616_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP616_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp617CoverageSummary(
  descriptor = createDataRoomVdrCoreCp617P02TestGoldenCaseCoreDescriptor(),
) {
  const caseSet = descriptor.p02_test_golden_case_core_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP617_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp617Coverage(
  descriptor = createDataRoomVdrCoreCp617P02TestGoldenCaseCoreDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp617CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP617_REQUIREMENTS.expected_unit_count, "CP00-617 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP617_REQUIREMENTS.expected_section_count, "CP00-617 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP617_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p02_test_golden_case_core_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-617 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP617_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP617_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp617P02TestGoldenCaseCoreDescriptor(
  descriptor = createDataRoomVdrCoreCp617P02TestGoldenCaseCoreDescriptor(),
) {
  validateDataRoomVdrCoreCp617Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp617P02TestGoldenCaseCoreDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP617_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_api_handler === false, "descriptor must not execute API handlers");
  assertCondition(descriptor.executes_service_entrypoint_runtime === false, "descriptor must not execute service entrypoint runtime");
  assertCondition(descriptor.executes_request_normalization_runtime === false, "descriptor must not execute request normalization runtime");
  assertCondition(descriptor.executes_permission_precheck_runtime === false, "descriptor must not execute permission precheck runtime");
  assertCondition(descriptor.executes_audit_hint_runtime === false, "descriptor must not execute audit hint runtime");
  assertCondition(descriptor.executes_primary_implementation_runtime === false, "descriptor must not execute primary implementation runtime");
  assertCondition(descriptor.executes_secondary_workflow_runtime === false, "descriptor must not execute secondary workflow runtime");
  assertCondition(descriptor.executes_state_transition_runtime === false, "descriptor must not execute state transition runtime");
  assertCondition(descriptor.executes_idempotency_runtime === false, "descriptor must not execute idempotency runtime");
  assertCondition(descriptor.executes_lock_runtime === false, "descriptor must not execute lock runtime");
  assertCondition(descriptor.executes_persistence_runtime === false, "descriptor must not execute persistence runtime");
  assertCondition(descriptor.executes_test_golden_case_runtime === false, "descriptor must not execute test golden case runtime");
  assertCondition(descriptor.executes_test_golden_case_core_runtime === false, "descriptor must not execute test golden case core runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP617_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp617CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp617HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp617P02TestGoldenCaseCoreDescriptor(),
) {
  validateDataRoomVdrCoreCp617P02TestGoldenCaseCoreDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP617_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP617_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP617_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp617ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp617P02TestGoldenCaseCoreDescriptor(),
) {
  validateDataRoomVdrCoreCp617P02TestGoldenCaseCoreDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP617_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP617_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP617_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP617_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp617CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp617P02TestGoldenCaseCoreDescriptor(),
) {
  validateDataRoomVdrCoreCp617P02TestGoldenCaseCoreDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP617_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP617_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP617_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP617_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP617_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp618CoverageSummary(
  descriptor = createDataRoomVdrCoreCp618P02TestHermesClaudeBridgeDescriptor(),
) {
  const caseSet = descriptor.p02_test_hermes_claude_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP618_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp618Coverage(
  descriptor = createDataRoomVdrCoreCp618P02TestHermesClaudeBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp618CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP618_REQUIREMENTS.expected_unit_count, "CP00-618 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP618_REQUIREMENTS.expected_section_count, "CP00-618 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP618_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p02_test_hermes_claude_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-618 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP618_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP618_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp618P02TestHermesClaudeBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp618P02TestHermesClaudeBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp618Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp618P02TestHermesClaudeBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP618_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_api_handler === false, "descriptor must not execute API handlers");
  assertCondition(descriptor.executes_service_entrypoint_runtime === false, "descriptor must not execute service entrypoint runtime");
  assertCondition(descriptor.executes_request_normalization_runtime === false, "descriptor must not execute request normalization runtime");
  assertCondition(descriptor.executes_permission_precheck_runtime === false, "descriptor must not execute permission precheck runtime");
  assertCondition(descriptor.executes_audit_hint_runtime === false, "descriptor must not execute audit hint runtime");
  assertCondition(descriptor.executes_primary_implementation_runtime === false, "descriptor must not execute primary implementation runtime");
  assertCondition(descriptor.executes_secondary_workflow_runtime === false, "descriptor must not execute secondary workflow runtime");
  assertCondition(descriptor.executes_state_transition_runtime === false, "descriptor must not execute state transition runtime");
  assertCondition(descriptor.executes_idempotency_runtime === false, "descriptor must not execute idempotency runtime");
  assertCondition(descriptor.executes_lock_runtime === false, "descriptor must not execute lock runtime");
  assertCondition(descriptor.executes_persistence_runtime === false, "descriptor must not execute persistence runtime");
  assertCondition(descriptor.executes_failure_recovery_runtime === false, "descriptor must not execute failure recovery runtime");
  assertCondition(descriptor.executes_integration_smoke_runtime === false, "descriptor must not execute integration smoke runtime");
  assertCondition(descriptor.executes_test_golden_case_runtime === false, "descriptor must not execute test golden case runtime");
  assertCondition(descriptor.executes_test_golden_case_tail_runtime === false, "descriptor must not execute test golden case tail runtime");
  assertCondition(descriptor.executes_hermes_evidence_packet_runtime === false, "descriptor must not execute Hermes evidence packet runtime");
  assertCondition(descriptor.executes_claude_review_packet_runtime === false, "descriptor must not execute Claude review packet runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP618_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp618CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp618HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp618P02TestHermesClaudeBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp618P02TestHermesClaudeBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP618_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP618_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP618_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp618ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp618P02TestHermesClaudeBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp618P02TestHermesClaudeBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP618_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP618_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP618_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP618_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp618CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp618P02TestHermesClaudeBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp618P02TestHermesClaudeBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP618_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP618_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP618_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP618_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP618_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp619CoverageSummary(
  descriptor = createDataRoomVdrCoreCp619P02ClaudeP03ApiInterfaceBridgeDescriptor(),
) {
  const caseSet = descriptor.p02_claude_p03_api_interface_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP619_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp619Coverage(
  descriptor = createDataRoomVdrCoreCp619P02ClaudeP03ApiInterfaceBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp619CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP619_REQUIREMENTS.expected_unit_count, "CP00-619 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP619_REQUIREMENTS.expected_section_count, "CP00-619 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP619_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p02_claude_p03_api_interface_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-619 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP619_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP619_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp619P02ClaudeP03ApiInterfaceBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp619P02ClaudeP03ApiInterfaceBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp619Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp619P02ClaudeP03ApiInterfaceBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP619_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_api_handler === false, "descriptor must not execute API handlers");
  assertCondition(descriptor.executes_service_entrypoint_runtime === false, "descriptor must not execute service entrypoint runtime");
  assertCondition(descriptor.executes_request_normalization_runtime === false, "descriptor must not execute request normalization runtime");
  assertCondition(descriptor.executes_permission_precheck_runtime === false, "descriptor must not execute permission precheck runtime");
  assertCondition(descriptor.executes_audit_hint_runtime === false, "descriptor must not execute audit hint runtime");
  assertCondition(descriptor.executes_primary_implementation_runtime === false, "descriptor must not execute primary implementation runtime");
  assertCondition(descriptor.executes_secondary_workflow_runtime === false, "descriptor must not execute secondary workflow runtime");
  assertCondition(descriptor.executes_state_transition_runtime === false, "descriptor must not execute state transition runtime");
  assertCondition(descriptor.executes_idempotency_runtime === false, "descriptor must not execute idempotency runtime");
  assertCondition(descriptor.executes_lock_runtime === false, "descriptor must not execute lock runtime");
  assertCondition(descriptor.executes_persistence_runtime === false, "descriptor must not execute persistence runtime");
  assertCondition(descriptor.executes_failure_recovery_runtime === false, "descriptor must not execute failure recovery runtime");
  assertCondition(descriptor.executes_integration_smoke_runtime === false, "descriptor must not execute integration smoke runtime");
  assertCondition(descriptor.executes_test_golden_case_runtime === false, "descriptor must not execute test golden case runtime");
  assertCondition(descriptor.executes_test_golden_case_tail_runtime === false, "descriptor must not execute test golden case tail runtime");
  assertCondition(descriptor.executes_hermes_evidence_packet_runtime === false, "descriptor must not execute Hermes evidence packet runtime");
  assertCondition(descriptor.executes_claude_review_packet_runtime === false, "descriptor must not execute Claude review packet runtime");
  assertCondition(descriptor.executes_closeout_handoff_runtime === false, "descriptor must not execute closeout handoff runtime");
  assertCondition(descriptor.executes_api_interface_runtime === false, "descriptor must not execute API/interface runtime");
  assertCondition(descriptor.executes_room_setup_api_runtime === false, "descriptor must not execute room setup API runtime");
  assertCondition(descriptor.executes_request_contract_runtime === false, "descriptor must not execute request contract runtime");
  assertCondition(descriptor.executes_response_contract_runtime === false, "descriptor must not execute response contract runtime");
  assertCondition(descriptor.executes_serialization_runtime === false, "descriptor must not execute serialization runtime");
  assertCondition(descriptor.executes_synthetic_fixture_runtime === false, "descriptor must not execute synthetic fixture runtime");
  assertCondition(descriptor.emits_hermes_api_runtime_evidence === false, "descriptor must not emit Hermes API runtime evidence");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP619_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp619CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp619HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp619P02ClaudeP03ApiInterfaceBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp619P02ClaudeP03ApiInterfaceBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP619_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP619_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP619_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp619ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp619P02ClaudeP03ApiInterfaceBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp619P02ClaudeP03ApiInterfaceBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP619_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP619_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP619_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP619_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp619CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp619P02ClaudeP03ApiInterfaceBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp619P02ClaudeP03ApiInterfaceBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP619_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP619_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP619_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP619_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP619_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp620CoverageSummary(
  descriptor = createDataRoomVdrCoreCp620P03ApiP04UiBridgeDescriptor(),
) {
  const caseSet = descriptor.p03_api_p04_ui_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP620_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp620Coverage(
  descriptor = createDataRoomVdrCoreCp620P03ApiP04UiBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp620CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP620_REQUIREMENTS.expected_unit_count, "CP00-620 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP620_REQUIREMENTS.expected_section_count, "CP00-620 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP620_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p03_api_p04_ui_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-620 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP620_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP620_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp620P03ApiP04UiBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp620P03ApiP04UiBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp620Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp620P03ApiP04UiBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP620_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_api_handler === false, "descriptor must not execute API handlers");
  assertCondition(descriptor.executes_api_interface_runtime === false, "descriptor must not execute API/interface runtime");
  assertCondition(descriptor.executes_room_setup_api_runtime === false, "descriptor must not execute room setup API runtime");
  assertCondition(descriptor.executes_request_contract_runtime === false, "descriptor must not execute request contract runtime");
  assertCondition(descriptor.executes_response_contract_runtime === false, "descriptor must not execute response contract runtime");
  assertCondition(descriptor.executes_serialization_runtime === false, "descriptor must not execute serialization runtime");
  assertCondition(descriptor.executes_synthetic_fixture_runtime === false, "descriptor must not execute synthetic fixture runtime");
  assertCondition(descriptor.emits_hermes_api_runtime_evidence === false, "descriptor must not emit Hermes API runtime evidence");
  assertCondition(descriptor.executes_ui_runtime === false, "descriptor must not execute UI runtime");
  assertCondition(descriptor.executes_ui_surface_runtime === false, "descriptor must not execute UI surface runtime");
  assertCondition(descriptor.executes_ui_data_dependency_runtime === false, "descriptor must not execute UI data dependency runtime");
  assertCondition(descriptor.executes_ui_interaction_runtime === false, "descriptor must not execute UI interaction runtime");
  assertCondition(descriptor.executes_responsive_layout_runtime === false, "descriptor must not execute responsive layout runtime");
  assertCondition(descriptor.executes_keyboard_focus_runtime === false, "descriptor must not execute keyboard/focus runtime");
  assertCondition(descriptor.executes_ui_synthetic_fixture_runtime === false, "descriptor must not execute UI synthetic fixture runtime");
  assertCondition(descriptor.executes_build_smoke_runtime === false, "descriptor must not execute build smoke runtime");
  assertCondition(descriptor.emits_hermes_ui_runtime_evidence === false, "descriptor must not emit Hermes UI runtime evidence");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP620_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp620CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp620HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp620P03ApiP04UiBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp620P03ApiP04UiBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP620_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP620_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP620_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp620ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp620P03ApiP04UiBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp620P03ApiP04UiBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP620_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP620_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP620_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP620_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp620CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp620P03ApiP04UiBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp620P03ApiP04UiBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP620_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP620_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP620_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP620_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP620_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp621CoverageSummary(
  descriptor = createDataRoomVdrCoreCp621P04UiP05FixtureBridgeDescriptor(),
) {
  const caseSet = descriptor.p04_ui_p05_fixture_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP621_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp621Coverage(
  descriptor = createDataRoomVdrCoreCp621P04UiP05FixtureBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp621CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP621_REQUIREMENTS.expected_unit_count, "CP00-621 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP621_REQUIREMENTS.expected_section_count, "CP00-621 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP621_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p04_ui_p05_fixture_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-621 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP621_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP621_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp621P04UiP05FixtureBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp621P04UiP05FixtureBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp621Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp621P04UiP05FixtureBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP621_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_ui_runtime === false, "descriptor must not execute UI runtime");
  assertCondition(descriptor.executes_ui_surface_runtime === false, "descriptor must not execute UI surface runtime");
  assertCondition(descriptor.executes_ui_interaction_runtime === false, "descriptor must not execute UI interaction runtime");
  assertCondition(descriptor.executes_ui_state_snapshot_runtime === false, "descriptor must not execute UI state snapshot runtime");
  assertCondition(descriptor.executes_unauthorized_count_query_runtime === false, "descriptor must not execute unauthorized-count query runtime");
  assertCondition(descriptor.executes_p05_fixture_foundation_runtime === false, "descriptor must not execute P05 fixture foundation runtime");
  assertCondition(descriptor.executes_golden_case_runtime === false, "descriptor must not execute golden case runtime");
  assertCondition(descriptor.executes_cross_tenant_fixture_runtime === false, "descriptor must not execute cross-tenant fixture runtime");
  assertCondition(descriptor.executes_ui_synthetic_fixture_runtime === false, "descriptor must not execute UI synthetic fixture runtime");
  assertCondition(descriptor.executes_build_smoke_runtime === false, "descriptor must not execute build smoke runtime");
  assertCondition(descriptor.emits_hermes_ui_runtime_evidence === false, "descriptor must not emit Hermes UI runtime evidence");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP621_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp621CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp621HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp621P04UiP05FixtureBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp621P04UiP05FixtureBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP621_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP621_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP621_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp621ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp621P04UiP05FixtureBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp621P04UiP05FixtureBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP621_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP621_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP621_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP621_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp621CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp621P04UiP05FixtureBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp621P04UiP05FixtureBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP621_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP621_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP621_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP621_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP621_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp622CoverageSummary(
  descriptor = createDataRoomVdrCoreCp622P05FixtureContractDraftDescriptor(),
) {
  const caseSet = descriptor.p05_fixture_contract_draft_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP622_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp622Coverage(
  descriptor = createDataRoomVdrCoreCp622P05FixtureContractDraftDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp622CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP622_REQUIREMENTS.expected_unit_count, "CP00-622 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP622_REQUIREMENTS.expected_section_count, "CP00-622 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP622_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p05_fixture_contract_draft_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-622 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP622_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP622_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp622P05FixtureContractDraftDescriptor(
  descriptor = createDataRoomVdrCoreCp622P05FixtureContractDraftDescriptor(),
) {
  validateDataRoomVdrCoreCp622Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp622P05FixtureContractDraftDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP622_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_ui_runtime === false, "descriptor must not execute UI runtime");
  assertCondition(descriptor.executes_ui_surface_runtime === false, "descriptor must not execute UI surface runtime");
  assertCondition(descriptor.executes_ui_interaction_runtime === false, "descriptor must not execute UI interaction runtime");
  assertCondition(descriptor.executes_ui_state_snapshot_runtime === false, "descriptor must not execute UI state snapshot runtime");
  assertCondition(descriptor.executes_unauthorized_count_query_runtime === false, "descriptor must not execute unauthorized-count query runtime");
  assertCondition(descriptor.executes_p05_fixture_foundation_runtime === false, "descriptor must not execute P05 fixture foundation runtime");
  assertCondition(descriptor.executes_p05_fixture_contract_draft_runtime === false, "descriptor must not execute P05 fixture contract draft runtime");
  assertCondition(descriptor.executes_fixture_manifest_runtime === false, "descriptor must not execute fixture manifest runtime");
  assertCondition(descriptor.executes_golden_case_runtime === false, "descriptor must not execute golden case runtime");
  assertCondition(descriptor.executes_cross_tenant_fixture_runtime === false, "descriptor must not execute cross-tenant fixture runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP622_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp622CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp622HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp622P05FixtureContractDraftDescriptor(),
) {
  validateDataRoomVdrCoreCp622P05FixtureContractDraftDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP622_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP622_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP622_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp622ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp622P05FixtureContractDraftDescriptor(),
) {
  validateDataRoomVdrCoreCp622P05FixtureContractDraftDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP622_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP622_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP622_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP622_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp622CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp622P05FixtureContractDraftDescriptor(),
) {
  validateDataRoomVdrCoreCp622P05FixtureContractDraftDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP622_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP622_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP622_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP622_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP622_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp623CoverageSummary(
  descriptor = createDataRoomVdrCoreCp623P05FixtureContractDraftTailDescriptor(),
) {
  const caseSet = descriptor.p05_fixture_contract_draft_tail_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP623_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp623Coverage(
  descriptor = createDataRoomVdrCoreCp623P05FixtureContractDraftTailDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp623CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP623_REQUIREMENTS.expected_unit_count, "CP00-623 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP623_REQUIREMENTS.expected_section_count, "CP00-623 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP623_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p05_fixture_contract_draft_tail_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-623 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP623_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP623_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp623P05FixtureContractDraftTailDescriptor(
  descriptor = createDataRoomVdrCoreCp623P05FixtureContractDraftTailDescriptor(),
) {
  validateDataRoomVdrCoreCp623Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp623P05FixtureContractDraftTailDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP623_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_ui_runtime === false, "descriptor must not execute UI runtime");
  assertCondition(descriptor.executes_p05_fixture_foundation_runtime === false, "descriptor must not execute P05 fixture foundation runtime");
  assertCondition(descriptor.executes_p05_fixture_contract_draft_runtime === false, "descriptor must not execute P05 fixture contract draft runtime");
  assertCondition(descriptor.executes_p05_fixture_contract_draft_tail_runtime === false, "descriptor must not execute P05 fixture contract draft tail runtime");
  assertCondition(descriptor.executes_fixture_manifest_runtime === false, "descriptor must not execute fixture manifest runtime");
  assertCondition(descriptor.executes_fixture_test_runtime === false, "descriptor must not execute fixture test runtime");
  assertCondition(descriptor.executes_ai_retrieval_runtime === false, "descriptor must not execute AI retrieval runtime");
  assertCondition(descriptor.executes_golden_case_runtime === false, "descriptor must not execute golden case runtime");
  assertCondition(descriptor.executes_cross_tenant_fixture_runtime === false, "descriptor must not execute cross-tenant fixture runtime");
  assertCondition(descriptor.emits_hermes_fixture_runtime_evidence === false, "descriptor must not emit Hermes fixture runtime evidence");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP623_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp623CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp623HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp623P05FixtureContractDraftTailDescriptor(),
) {
  validateDataRoomVdrCoreCp623P05FixtureContractDraftTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP623_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP623_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP623_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp623ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp623P05FixtureContractDraftTailDescriptor(),
) {
  validateDataRoomVdrCoreCp623P05FixtureContractDraftTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP623_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP623_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP623_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP623_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp623CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp623P05FixtureContractDraftTailDescriptor(),
) {
  validateDataRoomVdrCoreCp623P05FixtureContractDraftTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP623_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP623_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP623_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP623_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP623_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp624CoverageSummary(
  descriptor = createDataRoomVdrCoreCp624P05FixtureTypeShapeHeadDescriptor(),
) {
  const caseSet = descriptor.p05_fixture_type_shape_head_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP624_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp624Coverage(
  descriptor = createDataRoomVdrCoreCp624P05FixtureTypeShapeHeadDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp624CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP624_REQUIREMENTS.expected_unit_count, "CP00-624 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP624_REQUIREMENTS.expected_section_count, "CP00-624 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP624_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p05_fixture_type_shape_head_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-624 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP624_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP624_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp624P05FixtureTypeShapeHeadDescriptor(
  descriptor = createDataRoomVdrCoreCp624P05FixtureTypeShapeHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp624Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp624P05FixtureTypeShapeHeadDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP624_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_ui_runtime === false, "descriptor must not execute UI runtime");
  assertCondition(descriptor.executes_p05_fixture_foundation_runtime === false, "descriptor must not execute P05 fixture foundation runtime");
  assertCondition(descriptor.executes_p05_fixture_contract_draft_runtime === false, "descriptor must not execute P05 fixture contract draft runtime");
  assertCondition(descriptor.executes_p05_fixture_contract_draft_tail_runtime === false, "descriptor must not execute P05 fixture contract draft tail runtime");
  assertCondition(descriptor.executes_p05_fixture_type_shape_runtime === false, "descriptor must not execute P05 fixture type/shape runtime");
  assertCondition(descriptor.executes_base_fixture_shape_runtime === false, "descriptor must not execute base fixture shape runtime");
  assertCondition(descriptor.executes_golden_case_type_shape_runtime === false, "descriptor must not execute golden case type/shape runtime");
  assertCondition(descriptor.executes_review_required_type_shape_runtime === false, "descriptor must not execute review-required type/shape runtime");
  assertCondition(descriptor.executes_denied_or_cross_tenant_type_shape_runtime === false, "descriptor must not execute denied or cross-tenant type/shape runtime");
  assertCondition(descriptor.executes_fixture_manifest_runtime === false, "descriptor must not execute fixture manifest runtime");
  assertCondition(descriptor.executes_fixture_test_runtime === false, "descriptor must not execute fixture test runtime");
  assertCondition(descriptor.executes_ai_retrieval_runtime === false, "descriptor must not execute AI retrieval runtime");
  assertCondition(descriptor.executes_golden_case_runtime === false, "descriptor must not execute golden case runtime");
  assertCondition(descriptor.executes_cross_tenant_fixture_runtime === false, "descriptor must not execute cross-tenant fixture runtime");
  assertCondition(descriptor.emits_hermes_fixture_runtime_evidence === false, "descriptor must not emit Hermes fixture runtime evidence");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP624_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp624CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp624HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp624P05FixtureTypeShapeHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp624P05FixtureTypeShapeHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP624_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP624_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP624_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp624ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp624P05FixtureTypeShapeHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp624P05FixtureTypeShapeHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP624_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP624_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP624_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP624_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp624CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp624P05FixtureTypeShapeHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp624P05FixtureTypeShapeHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP624_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP624_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP624_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP624_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP624_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp625CoverageSummary(
  descriptor = createDataRoomVdrCoreCp625P05FixtureTypeShapeTailDescriptor(),
) {
  const caseSet = descriptor.p05_fixture_type_shape_tail_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP625_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp625Coverage(
  descriptor = createDataRoomVdrCoreCp625P05FixtureTypeShapeTailDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp625CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP625_REQUIREMENTS.expected_unit_count, "CP00-625 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP625_REQUIREMENTS.expected_section_count, "CP00-625 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP625_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p05_fixture_type_shape_tail_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-625 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP625_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP625_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp625P05FixtureTypeShapeTailDescriptor(
  descriptor = createDataRoomVdrCoreCp625P05FixtureTypeShapeTailDescriptor(),
) {
  validateDataRoomVdrCoreCp625Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp625P05FixtureTypeShapeTailDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP625_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_ui_runtime === false, "descriptor must not execute UI runtime");
  assertCondition(descriptor.executes_p05_fixture_foundation_runtime === false, "descriptor must not execute P05 fixture foundation runtime");
  assertCondition(descriptor.executes_p05_fixture_contract_draft_runtime === false, "descriptor must not execute P05 fixture contract draft runtime");
  assertCondition(descriptor.executes_p05_fixture_contract_draft_tail_runtime === false, "descriptor must not execute P05 fixture contract draft tail runtime");
  assertCondition(descriptor.executes_p05_fixture_type_shape_runtime === false, "descriptor must not execute P05 fixture type/shape runtime");
  assertCondition(descriptor.executes_p05_fixture_type_shape_tail_runtime === false, "descriptor must not execute P05 fixture type/shape tail runtime");
  assertCondition(descriptor.executes_type_shape_ai_retrieval_runtime === false, "descriptor must not execute type/shape AI retrieval runtime");
  assertCondition(descriptor.executes_type_shape_fixture_test_runtime === false, "descriptor must not execute type/shape fixture test runtime");
  assertCondition(descriptor.emits_type_shape_hermes_fixture_runtime_evidence === false, "descriptor must not emit type/shape Hermes fixture runtime evidence");
  assertCondition(descriptor.executes_base_fixture_shape_runtime === false, "descriptor must not execute base fixture shape runtime");
  assertCondition(descriptor.executes_golden_case_type_shape_runtime === false, "descriptor must not execute golden case type/shape runtime");
  assertCondition(descriptor.executes_review_required_type_shape_runtime === false, "descriptor must not execute review-required type/shape runtime");
  assertCondition(descriptor.executes_denied_or_cross_tenant_type_shape_runtime === false, "descriptor must not execute denied or cross-tenant type/shape runtime");
  assertCondition(descriptor.executes_fixture_manifest_runtime === false, "descriptor must not execute fixture manifest runtime");
  assertCondition(descriptor.executes_fixture_test_runtime === false, "descriptor must not execute fixture test runtime");
  assertCondition(descriptor.executes_ai_retrieval_runtime === false, "descriptor must not execute AI retrieval runtime");
  assertCondition(descriptor.executes_golden_case_runtime === false, "descriptor must not execute golden case runtime");
  assertCondition(descriptor.executes_cross_tenant_fixture_runtime === false, "descriptor must not execute cross-tenant fixture runtime");
  assertCondition(descriptor.emits_hermes_fixture_runtime_evidence === false, "descriptor must not emit Hermes fixture runtime evidence");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP625_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp625CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp625HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp625P05FixtureTypeShapeTailDescriptor(),
) {
  validateDataRoomVdrCoreCp625P05FixtureTypeShapeTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP625_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP625_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP625_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp625ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp625P05FixtureTypeShapeTailDescriptor(),
) {
  validateDataRoomVdrCoreCp625P05FixtureTypeShapeTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP625_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP625_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP625_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP625_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp625CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp625P05FixtureTypeShapeTailDescriptor(),
) {
  validateDataRoomVdrCoreCp625P05FixtureTypeShapeTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP625_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP625_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP625_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP625_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP625_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp626CoverageSummary(
  descriptor = createDataRoomVdrCoreCp626P05PrimaryImplementationHeadDescriptor(),
) {
  const caseSet = descriptor.p05_primary_implementation_head_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP626_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp626Coverage(
  descriptor = createDataRoomVdrCoreCp626P05PrimaryImplementationHeadDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp626CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP626_REQUIREMENTS.expected_unit_count, "CP00-626 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP626_REQUIREMENTS.expected_section_count, "CP00-626 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP626_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p05_primary_implementation_head_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-626 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP626_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP626_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp626P05PrimaryImplementationHeadDescriptor(
  descriptor = createDataRoomVdrCoreCp626P05PrimaryImplementationHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp626Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp626P05PrimaryImplementationHeadDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP626_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_ui_runtime === false, "descriptor must not execute UI runtime");
  assertCondition(descriptor.executes_p05_fixture_foundation_runtime === false, "descriptor must not execute P05 fixture foundation runtime");
  assertCondition(descriptor.executes_p05_fixture_contract_draft_runtime === false, "descriptor must not execute P05 fixture contract draft runtime");
  assertCondition(descriptor.executes_p05_fixture_contract_draft_tail_runtime === false, "descriptor must not execute P05 fixture contract draft tail runtime");
  assertCondition(descriptor.executes_p05_fixture_type_shape_runtime === false, "descriptor must not execute P05 fixture type/shape runtime");
  assertCondition(descriptor.executes_p05_fixture_type_shape_tail_runtime === false, "descriptor must not execute P05 fixture type/shape tail runtime");
  assertCondition(descriptor.executes_p05_primary_implementation_runtime === false, "descriptor must not execute P05 primary implementation runtime");
  assertCondition(descriptor.executes_primary_fixture_runtime === false, "descriptor must not execute primary fixture runtime");
  assertCondition(descriptor.executes_primary_golden_case_runtime === false, "descriptor must not execute primary golden case runtime");
  assertCondition(descriptor.executes_primary_review_required_runtime === false, "descriptor must not execute primary review-required runtime");
  assertCondition(descriptor.executes_primary_denied_or_cross_tenant_runtime === false, "descriptor must not execute primary denied or cross-tenant runtime");
  assertCondition(descriptor.executes_base_fixture_shape_runtime === false, "descriptor must not execute base fixture shape runtime");
  assertCondition(descriptor.executes_golden_case_type_shape_runtime === false, "descriptor must not execute golden case type/shape runtime");
  assertCondition(descriptor.executes_review_required_type_shape_runtime === false, "descriptor must not execute review-required type/shape runtime");
  assertCondition(descriptor.executes_denied_or_cross_tenant_type_shape_runtime === false, "descriptor must not execute denied or cross-tenant type/shape runtime");
  assertCondition(descriptor.executes_fixture_manifest_runtime === false, "descriptor must not execute fixture manifest runtime");
  assertCondition(descriptor.executes_fixture_test_runtime === false, "descriptor must not execute fixture test runtime");
  assertCondition(descriptor.executes_ai_retrieval_runtime === false, "descriptor must not execute AI retrieval runtime");
  assertCondition(descriptor.executes_golden_case_runtime === false, "descriptor must not execute golden case runtime");
  assertCondition(descriptor.executes_cross_tenant_fixture_runtime === false, "descriptor must not execute cross-tenant fixture runtime");
  assertCondition(descriptor.emits_hermes_fixture_runtime_evidence === false, "descriptor must not emit Hermes fixture runtime evidence");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP626_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp626CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp626HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp626P05PrimaryImplementationHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp626P05PrimaryImplementationHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP626_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP626_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP626_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp626ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp626P05PrimaryImplementationHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp626P05PrimaryImplementationHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP626_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP626_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP626_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP626_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp626CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp626P05PrimaryImplementationHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp626P05PrimaryImplementationHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP626_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP626_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP626_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP626_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP626_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp627CoverageSummary(
  descriptor = createDataRoomVdrCoreCp627P05PrimaryImplementationTailDescriptor(),
) {
  const caseSet = descriptor.p05_primary_implementation_tail_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP627_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp627Coverage(
  descriptor = createDataRoomVdrCoreCp627P05PrimaryImplementationTailDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp627CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP627_REQUIREMENTS.expected_unit_count, "CP00-627 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP627_REQUIREMENTS.expected_section_count, "CP00-627 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP627_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p05_primary_implementation_tail_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-627 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP627_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP627_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp627P05PrimaryImplementationTailDescriptor(
  descriptor = createDataRoomVdrCoreCp627P05PrimaryImplementationTailDescriptor(),
) {
  validateDataRoomVdrCoreCp627Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp627P05PrimaryImplementationTailDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP627_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_ui_runtime === false, "descriptor must not execute UI runtime");
  assertCondition(descriptor.executes_p05_fixture_foundation_runtime === false, "descriptor must not execute P05 fixture foundation runtime");
  assertCondition(descriptor.executes_p05_fixture_contract_draft_runtime === false, "descriptor must not execute P05 fixture contract draft runtime");
  assertCondition(descriptor.executes_p05_fixture_contract_draft_tail_runtime === false, "descriptor must not execute P05 fixture contract draft tail runtime");
  assertCondition(descriptor.executes_p05_fixture_type_shape_runtime === false, "descriptor must not execute P05 fixture type/shape runtime");
  assertCondition(descriptor.executes_p05_fixture_type_shape_tail_runtime === false, "descriptor must not execute P05 fixture type/shape tail runtime");
  assertCondition(descriptor.executes_p05_primary_implementation_runtime === false, "descriptor must not execute P05 primary implementation runtime");
  assertCondition(descriptor.executes_p05_primary_implementation_tail_runtime === false, "descriptor must not execute P05 primary implementation tail runtime");
  assertCondition(descriptor.executes_primary_fixture_runtime === false, "descriptor must not execute primary fixture runtime");
  assertCondition(descriptor.executes_primary_golden_case_runtime === false, "descriptor must not execute primary golden case runtime");
  assertCondition(descriptor.executes_primary_review_required_runtime === false, "descriptor must not execute primary review-required runtime");
  assertCondition(descriptor.executes_primary_denied_or_cross_tenant_runtime === false, "descriptor must not execute primary denied or cross-tenant runtime");
  assertCondition(descriptor.executes_primary_ai_retrieval_runtime === false, "descriptor must not execute primary AI retrieval runtime");
  assertCondition(descriptor.executes_primary_fixture_test_runtime === false, "descriptor must not execute primary fixture test runtime");
  assertCondition(descriptor.emits_primary_hermes_fixture_runtime_evidence === false, "descriptor must not emit primary Hermes fixture runtime evidence");
  assertCondition(descriptor.executes_base_fixture_shape_runtime === false, "descriptor must not execute base fixture shape runtime");
  assertCondition(descriptor.executes_golden_case_type_shape_runtime === false, "descriptor must not execute golden case type/shape runtime");
  assertCondition(descriptor.executes_review_required_type_shape_runtime === false, "descriptor must not execute review-required type/shape runtime");
  assertCondition(descriptor.executes_denied_or_cross_tenant_type_shape_runtime === false, "descriptor must not execute denied or cross-tenant type/shape runtime");
  assertCondition(descriptor.executes_fixture_manifest_runtime === false, "descriptor must not execute fixture manifest runtime");
  assertCondition(descriptor.executes_fixture_test_runtime === false, "descriptor must not execute fixture test runtime");
  assertCondition(descriptor.executes_ai_retrieval_runtime === false, "descriptor must not execute AI retrieval runtime");
  assertCondition(descriptor.executes_golden_case_runtime === false, "descriptor must not execute golden case runtime");
  assertCondition(descriptor.executes_cross_tenant_fixture_runtime === false, "descriptor must not execute cross-tenant fixture runtime");
  assertCondition(descriptor.emits_hermes_fixture_runtime_evidence === false, "descriptor must not emit Hermes fixture runtime evidence");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP627_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp627CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp627HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp627P05PrimaryImplementationTailDescriptor(),
) {
  validateDataRoomVdrCoreCp627P05PrimaryImplementationTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP627_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP627_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP627_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp627ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp627P05PrimaryImplementationTailDescriptor(),
) {
  validateDataRoomVdrCoreCp627P05PrimaryImplementationTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP627_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP627_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP627_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP627_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp627CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp627P05PrimaryImplementationTailDescriptor(),
) {
  validateDataRoomVdrCoreCp627P05PrimaryImplementationTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP627_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP627_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP627_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP627_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP627_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp628CoverageSummary(
  descriptor = createDataRoomVdrCoreCp628P05SecondaryWorkflowPermissionAuditBridgeDescriptor(),
) {
  const caseSet = descriptor.p05_secondary_workflow_permission_audit_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP628_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp628Coverage(
  descriptor = createDataRoomVdrCoreCp628P05SecondaryWorkflowPermissionAuditBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp628CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP628_REQUIREMENTS.expected_unit_count, "CP00-628 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP628_REQUIREMENTS.expected_section_count, "CP00-628 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP628_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p05_secondary_workflow_permission_audit_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-628 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP628_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP628_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp628P05SecondaryWorkflowPermissionAuditBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp628P05SecondaryWorkflowPermissionAuditBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp628Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp628P05SecondaryWorkflowPermissionAuditBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP628_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_ui_runtime === false, "descriptor must not execute UI runtime");
  assertCondition(descriptor.executes_p05_primary_implementation_runtime === false, "descriptor must not execute P05 primary implementation runtime");
  assertCondition(descriptor.executes_p05_primary_implementation_tail_runtime === false, "descriptor must not execute P05 primary implementation tail runtime");
  assertCondition(descriptor.executes_p05_primary_stable_id_runtime === false, "descriptor must not execute P05 primary stable ID runtime");
  assertCondition(descriptor.executes_p05_primary_replay_runtime === false, "descriptor must not execute P05 primary replay runtime");
  assertCondition(descriptor.executes_p05_secondary_workflow_runtime === false, "descriptor must not execute P05 secondary workflow runtime");
  assertCondition(descriptor.executes_p05_permission_audit_binding_runtime === false, "descriptor must not execute P05 permission/audit binding runtime");
  assertCondition(descriptor.executes_secondary_fixture_runtime === false, "descriptor must not execute secondary fixture runtime");
  assertCondition(descriptor.executes_permission_audit_fixture_runtime === false, "descriptor must not execute permission/audit fixture runtime");
  assertCondition(descriptor.executes_primary_fixture_runtime === false, "descriptor must not execute primary fixture runtime");
  assertCondition(descriptor.executes_primary_golden_case_runtime === false, "descriptor must not execute primary golden case runtime");
  assertCondition(descriptor.executes_primary_review_required_runtime === false, "descriptor must not execute primary review-required runtime");
  assertCondition(descriptor.executes_primary_denied_or_cross_tenant_runtime === false, "descriptor must not execute primary denied or cross-tenant runtime");
  assertCondition(descriptor.executes_primary_ai_retrieval_runtime === false, "descriptor must not execute primary AI retrieval runtime");
  assertCondition(descriptor.executes_primary_fixture_test_runtime === false, "descriptor must not execute primary fixture test runtime");
  assertCondition(descriptor.emits_primary_hermes_fixture_runtime_evidence === false, "descriptor must not emit primary Hermes fixture runtime evidence");
  assertCondition(descriptor.executes_p05_fixture_foundation_runtime === false, "descriptor must not execute P05 fixture foundation runtime");
  assertCondition(descriptor.executes_p05_fixture_contract_draft_runtime === false, "descriptor must not execute P05 fixture contract draft runtime");
  assertCondition(descriptor.executes_p05_fixture_contract_draft_tail_runtime === false, "descriptor must not execute P05 fixture contract draft tail runtime");
  assertCondition(descriptor.executes_p05_fixture_type_shape_runtime === false, "descriptor must not execute P05 fixture type/shape runtime");
  assertCondition(descriptor.executes_p05_fixture_type_shape_tail_runtime === false, "descriptor must not execute P05 fixture type/shape tail runtime");
  assertCondition(descriptor.executes_base_fixture_shape_runtime === false, "descriptor must not execute base fixture shape runtime");
  assertCondition(descriptor.executes_golden_case_type_shape_runtime === false, "descriptor must not execute golden case type/shape runtime");
  assertCondition(descriptor.executes_review_required_type_shape_runtime === false, "descriptor must not execute review-required type/shape runtime");
  assertCondition(descriptor.executes_denied_or_cross_tenant_type_shape_runtime === false, "descriptor must not execute denied or cross-tenant type/shape runtime");
  assertCondition(descriptor.executes_fixture_manifest_runtime === false, "descriptor must not execute fixture manifest runtime");
  assertCondition(descriptor.executes_fixture_test_runtime === false, "descriptor must not execute fixture test runtime");
  assertCondition(descriptor.executes_ai_retrieval_runtime === false, "descriptor must not execute AI retrieval runtime");
  assertCondition(descriptor.executes_golden_case_runtime === false, "descriptor must not execute golden case runtime");
  assertCondition(descriptor.executes_cross_tenant_fixture_runtime === false, "descriptor must not execute cross-tenant fixture runtime");
  assertCondition(descriptor.emits_hermes_fixture_runtime_evidence === false, "descriptor must not emit Hermes fixture runtime evidence");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP628_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp628CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp628HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp628P05SecondaryWorkflowPermissionAuditBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp628P05SecondaryWorkflowPermissionAuditBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP628_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP628_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP628_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp628ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp628P05SecondaryWorkflowPermissionAuditBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp628P05SecondaryWorkflowPermissionAuditBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP628_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP628_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP628_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP628_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp628CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp628P05SecondaryWorkflowPermissionAuditBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp628P05SecondaryWorkflowPermissionAuditBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP628_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP628_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP628_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP628_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP628_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp629CoverageSummary(
  descriptor = createDataRoomVdrCoreCp629P05PermissionAuditTailSyntheticFixtureHeadDescriptor(),
) {
  const caseSet = descriptor.p05_permission_audit_tail_synthetic_fixture_head_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP629_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp629Coverage(
  descriptor = createDataRoomVdrCoreCp629P05PermissionAuditTailSyntheticFixtureHeadDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp629CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP629_REQUIREMENTS.expected_unit_count, "CP00-629 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP629_REQUIREMENTS.expected_section_count, "CP00-629 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP629_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p05_permission_audit_tail_synthetic_fixture_head_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-629 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP629_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP629_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp629P05PermissionAuditTailSyntheticFixtureHeadDescriptor(
  descriptor = createDataRoomVdrCoreCp629P05PermissionAuditTailSyntheticFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp629Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp629P05PermissionAuditTailSyntheticFixtureHeadDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP629_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p05_permission_audit_tail_runtime === false, "descriptor must not execute P05 permission/audit tail runtime");
  assertCondition(descriptor.executes_p05_synthetic_fixture_head_runtime === false, "descriptor must not execute P05 synthetic fixture head runtime");
  assertCondition(descriptor.executes_synthetic_fixture_head_runtime === false, "descriptor must not execute synthetic fixture head runtime");
  assertCondition(descriptor.executes_p05_primary_stable_id_runtime === false, "descriptor must not execute P05 primary stable ID runtime");
  assertCondition(descriptor.executes_p05_primary_replay_runtime === false, "descriptor must not execute P05 primary replay runtime");
  assertCondition(descriptor.executes_p05_secondary_workflow_runtime === false, "descriptor must not execute P05 secondary workflow runtime");
  assertCondition(descriptor.executes_p05_permission_audit_binding_runtime === false, "descriptor must not execute P05 permission/audit binding runtime");
  assertCondition(descriptor.executes_secondary_fixture_runtime === false, "descriptor must not execute secondary fixture runtime");
  assertCondition(descriptor.executes_permission_audit_fixture_runtime === false, "descriptor must not execute permission/audit fixture runtime");
  assertCondition(descriptor.executes_p05_primary_implementation_runtime === false, "descriptor must not execute P05 primary implementation runtime");
  assertCondition(descriptor.executes_p05_primary_implementation_tail_runtime === false, "descriptor must not execute P05 primary implementation tail runtime");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP629_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp629CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp629HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp629P05PermissionAuditTailSyntheticFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp629P05PermissionAuditTailSyntheticFixtureHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP629_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP629_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP629_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp629ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp629P05PermissionAuditTailSyntheticFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp629P05PermissionAuditTailSyntheticFixtureHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP629_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP629_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP629_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP629_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp629CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp629P05PermissionAuditTailSyntheticFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp629P05PermissionAuditTailSyntheticFixtureHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP629_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP629_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP629_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP629_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP629_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp630CoverageSummary(
  descriptor = createDataRoomVdrCoreCp630P05FixtureCloseoutP06PermissionMatrixBridgeDescriptor(),
) {
  const caseSet = descriptor.p05_fixture_closeout_p06_permission_matrix_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP630_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp630Coverage(
  descriptor = createDataRoomVdrCoreCp630P05FixtureCloseoutP06PermissionMatrixBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp630CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP630_REQUIREMENTS.expected_unit_count, "CP00-630 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP630_REQUIREMENTS.expected_section_count, "CP00-630 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP630_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p05_fixture_closeout_p06_permission_matrix_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-630 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP630_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP630_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp630P05FixtureCloseoutP06PermissionMatrixBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp630P05FixtureCloseoutP06PermissionMatrixBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp630Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp630P05FixtureCloseoutP06PermissionMatrixBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP630_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p05_fixture_closeout_runtime === false, "descriptor must not execute P05 fixture closeout runtime");
  assertCondition(descriptor.executes_p06_permission_matrix_runtime === false, "descriptor must not execute P06 permission matrix runtime");
  assertCondition(descriptor.executes_p06_decision_binding_runtime === false, "descriptor must not execute P06 decision binding runtime");
  assertCondition(descriptor.executes_p06_security_interaction_runtime === false, "descriptor must not execute P06 security interaction runtime");
  assertCondition(descriptor.executes_p06_permission_fixture_runtime === false, "descriptor must not execute P06 permission fixture runtime");
  assertCondition(descriptor.executes_p05_permission_audit_tail_runtime === false, "descriptor must not execute P05 permission/audit tail runtime");
  assertCondition(descriptor.executes_p05_synthetic_fixture_head_runtime === false, "descriptor must not execute P05 synthetic fixture head runtime");
  assertCondition(descriptor.executes_synthetic_fixture_head_runtime === false, "descriptor must not execute synthetic fixture head runtime");
  assertCondition(descriptor.executes_p05_primary_stable_id_runtime === false, "descriptor must not execute P05 primary stable ID runtime");
  assertCondition(descriptor.executes_p05_primary_replay_runtime === false, "descriptor must not execute P05 primary replay runtime");
  assertCondition(descriptor.executes_p05_secondary_workflow_runtime === false, "descriptor must not execute P05 secondary workflow runtime");
  assertCondition(descriptor.executes_p05_permission_audit_binding_runtime === false, "descriptor must not execute P05 permission/audit binding runtime");
  assertCondition(descriptor.executes_secondary_fixture_runtime === false, "descriptor must not execute secondary fixture runtime");
  assertCondition(descriptor.executes_permission_audit_fixture_runtime === false, "descriptor must not execute permission/audit fixture runtime");
  assertCondition(descriptor.executes_p05_primary_implementation_runtime === false, "descriptor must not execute P05 primary implementation runtime");
  assertCondition(descriptor.executes_p05_primary_implementation_tail_runtime === false, "descriptor must not execute P05 primary implementation tail runtime");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP630_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp630CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp630HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp630P05FixtureCloseoutP06PermissionMatrixBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp630P05FixtureCloseoutP06PermissionMatrixBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP630_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP630_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP630_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp630ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp630P05FixtureCloseoutP06PermissionMatrixBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp630P05FixtureCloseoutP06PermissionMatrixBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP630_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP630_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP630_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP630_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp630CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp630P05FixtureCloseoutP06PermissionMatrixBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp630P05FixtureCloseoutP06PermissionMatrixBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP630_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP630_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP630_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP630_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP630_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp631CoverageSummary(
  descriptor = createDataRoomVdrCoreCp631P06TypeShapeTailPrimarySecondaryBridgeDescriptor(),
) {
  const caseSet = descriptor.p06_type_shape_tail_primary_secondary_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP631_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp631Coverage(
  descriptor = createDataRoomVdrCoreCp631P06TypeShapeTailPrimarySecondaryBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp631CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP631_REQUIREMENTS.expected_unit_count, "CP00-631 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP631_REQUIREMENTS.expected_section_count, "CP00-631 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP631_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p06_type_shape_tail_primary_secondary_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-631 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP631_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP631_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp631P06TypeShapeTailPrimarySecondaryBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp631P06TypeShapeTailPrimarySecondaryBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp631Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp631P06TypeShapeTailPrimarySecondaryBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP631_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p06_type_shape_test_tail_runtime === false, "descriptor must not execute P06 type/shape test tail runtime");
  assertCondition(descriptor.executes_p06_primary_implementation_runtime === false, "descriptor must not execute P06 primary implementation runtime");
  assertCondition(descriptor.executes_p06_secondary_workflow_head_runtime === false, "descriptor must not execute P06 secondary workflow head runtime");
  assertCondition(descriptor.executes_p06_cross_tenant_test_runtime === false, "descriptor must not execute P06 cross-tenant test runtime");
  assertCondition(descriptor.executes_p06_leak_prevention_test_runtime === false, "descriptor must not execute P06 leak prevention test runtime");
  assertCondition(descriptor.executes_p06_permission_matrix_runtime === false, "descriptor must not execute P06 permission matrix runtime");
  assertCondition(descriptor.executes_p06_decision_binding_runtime === false, "descriptor must not execute P06 decision binding runtime");
  assertCondition(descriptor.executes_p06_security_interaction_runtime === false, "descriptor must not execute P06 security interaction runtime");
  assertCondition(descriptor.executes_p06_permission_fixture_runtime === false, "descriptor must not execute P06 permission fixture runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP631_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp631CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp631HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp631P06TypeShapeTailPrimarySecondaryBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp631P06TypeShapeTailPrimarySecondaryBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP631_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP631_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP631_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp631ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp631P06TypeShapeTailPrimarySecondaryBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp631P06TypeShapeTailPrimarySecondaryBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP631_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP631_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP631_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP631_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp631CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp631P06TypeShapeTailPrimarySecondaryBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp631P06TypeShapeTailPrimarySecondaryBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP631_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP631_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP631_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP631_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP631_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp632CoverageSummary(
  descriptor = createDataRoomVdrCoreCp632P06SecondaryTailPermissionAuditBindingHeadDescriptor(),
) {
  const caseSet = descriptor.p06_secondary_tail_permission_audit_binding_head_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP632_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp632Coverage(
  descriptor = createDataRoomVdrCoreCp632P06SecondaryTailPermissionAuditBindingHeadDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp632CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP632_REQUIREMENTS.expected_unit_count, "CP00-632 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP632_REQUIREMENTS.expected_section_count, "CP00-632 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP632_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p06_secondary_tail_permission_audit_binding_head_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-632 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP632_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP632_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp632P06SecondaryTailPermissionAuditBindingHeadDescriptor(
  descriptor = createDataRoomVdrCoreCp632P06SecondaryTailPermissionAuditBindingHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp632Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp632P06SecondaryTailPermissionAuditBindingHeadDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP632_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p06_secondary_workflow_tail_runtime === false, "descriptor must not execute P06 secondary workflow tail runtime");
  assertCondition(descriptor.executes_p06_permission_audit_binding_head_runtime === false, "descriptor must not execute P06 permission/audit binding head runtime");
  assertCondition(descriptor.executes_p06_approval_route_runtime === false, "descriptor must not execute P06 approval route runtime");
  assertCondition(descriptor.executes_p06_security_trimming_runtime === false, "descriptor must not execute P06 security trimming runtime");
  assertCondition(descriptor.executes_p06_audit_event_runtime === false, "descriptor must not execute P06 audit event runtime");
  assertCondition(descriptor.executes_p06_cross_tenant_test_runtime === false, "descriptor must not execute P06 cross-tenant test runtime");
  assertCondition(descriptor.executes_p06_leak_prevention_test_runtime === false, "descriptor must not execute P06 leak prevention test runtime");
  assertCondition(descriptor.executes_p06_permission_matrix_runtime === false, "descriptor must not execute P06 permission matrix runtime");
  assertCondition(descriptor.executes_p06_decision_binding_runtime === false, "descriptor must not execute P06 decision binding runtime");
  assertCondition(descriptor.executes_p06_security_interaction_runtime === false, "descriptor must not execute P06 security interaction runtime");
  assertCondition(descriptor.executes_p06_permission_fixture_runtime === false, "descriptor must not execute P06 permission fixture runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP632_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp632CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp632HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp632P06SecondaryTailPermissionAuditBindingHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp632P06SecondaryTailPermissionAuditBindingHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP632_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP632_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP632_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp632ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp632P06SecondaryTailPermissionAuditBindingHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp632P06SecondaryTailPermissionAuditBindingHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP632_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP632_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP632_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP632_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp632CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp632P06SecondaryTailPermissionAuditBindingHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp632P06SecondaryTailPermissionAuditBindingHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP632_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP632_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP632_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP632_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP632_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp633CoverageSummary(
  descriptor = createDataRoomVdrCoreCp633P06PermissionAuditBindingMiddleDescriptor(),
) {
  const caseSet = descriptor.p06_permission_audit_binding_middle_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP633_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp633Coverage(
  descriptor = createDataRoomVdrCoreCp633P06PermissionAuditBindingMiddleDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp633CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP633_REQUIREMENTS.expected_unit_count, "CP00-633 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP633_REQUIREMENTS.expected_section_count, "CP00-633 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP633_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p06_permission_audit_binding_middle_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-633 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP633_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP633_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp633P06PermissionAuditBindingMiddleDescriptor(
  descriptor = createDataRoomVdrCoreCp633P06PermissionAuditBindingMiddleDescriptor(),
) {
  validateDataRoomVdrCoreCp633Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp633P06PermissionAuditBindingMiddleDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP633_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p06_permission_audit_binding_middle_runtime === false, "descriptor must not execute P06 permission/audit binding middle runtime");
  assertCondition(descriptor.executes_p06_decision_binding_middle_runtime === false, "descriptor must not execute P06 decision binding middle runtime");
  assertCondition(descriptor.executes_p06_audit_hint_runtime === false, "descriptor must not execute P06 audit hint runtime");
  assertCondition(descriptor.executes_p06_matched_rule_capture_runtime === false, "descriptor must not execute P06 matched rule runtime");
  assertCondition(descriptor.executes_p06_deny_over_allow_runtime === false, "descriptor must not execute P06 deny-over-allow runtime");
  assertCondition(descriptor.executes_p06_legal_hold_interaction_runtime === false, "descriptor must not execute P06 legal-hold interaction runtime");
  assertCondition(descriptor.executes_p06_ethical_wall_interaction_runtime === false, "descriptor must not execute P06 ethical-wall interaction runtime");
  assertCondition(descriptor.executes_p06_permission_matrix_runtime === false, "descriptor must not execute P06 permission matrix runtime");
  assertCondition(descriptor.executes_p06_decision_binding_runtime === false, "descriptor must not execute P06 decision binding runtime");
  assertCondition(descriptor.executes_p06_security_interaction_runtime === false, "descriptor must not execute P06 security interaction runtime");
  assertCondition(descriptor.executes_p06_permission_fixture_runtime === false, "descriptor must not execute P06 permission fixture runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP633_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp633CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp633HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp633P06PermissionAuditBindingMiddleDescriptor(),
) {
  validateDataRoomVdrCoreCp633P06PermissionAuditBindingMiddleDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP633_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP633_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP633_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp633ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp633P06PermissionAuditBindingMiddleDescriptor(),
) {
  validateDataRoomVdrCoreCp633P06PermissionAuditBindingMiddleDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP633_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP633_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP633_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP633_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp633CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp633P06PermissionAuditBindingMiddleDescriptor(),
) {
  validateDataRoomVdrCoreCp633P06PermissionAuditBindingMiddleDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP633_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP633_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP633_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP633_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP633_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp634CoverageSummary(
  descriptor = createDataRoomVdrCoreCp634P06PermissionAuditBindingTailDescriptor(),
) {
  const caseSet = descriptor.p06_permission_audit_binding_tail_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP634_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp634Coverage(
  descriptor = createDataRoomVdrCoreCp634P06PermissionAuditBindingTailDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp634CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP634_REQUIREMENTS.expected_unit_count, "CP00-634 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP634_REQUIREMENTS.expected_section_count, "CP00-634 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP634_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p06_permission_audit_binding_tail_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-634 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP634_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP634_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp634P06PermissionAuditBindingTailDescriptor(
  descriptor = createDataRoomVdrCoreCp634P06PermissionAuditBindingTailDescriptor(),
) {
  validateDataRoomVdrCoreCp634Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp634P06PermissionAuditBindingTailDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP634_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p06_permission_audit_binding_tail_runtime === false, "descriptor must not execute P06 permission/audit binding tail runtime");
  assertCondition(descriptor.executes_p06_object_acl_interaction_runtime === false, "descriptor must not execute P06 object ACL interaction runtime");
  assertCondition(descriptor.executes_p06_review_required_route_runtime === false, "descriptor must not execute P06 review-required route runtime");
  assertCondition(descriptor.executes_p06_approval_required_route_runtime === false, "descriptor must not execute P06 approval-required route runtime");
  assertCondition(descriptor.executes_p06_security_trimming_proof_runtime === false, "descriptor must not execute P06 security trimming proof runtime");
  assertCondition(descriptor.executes_p06_audit_event_expectation_runtime === false, "descriptor must not execute P06 audit event expectation runtime");
  assertCondition(descriptor.executes_p06_permission_fixture_runtime === false, "descriptor must not execute P06 permission fixture runtime");
  assertCondition(descriptor.executes_p06_permission_test_runtime === false, "descriptor must not execute P06 permission test runtime");
  assertCondition(descriptor.executes_p06_permission_audit_binding_middle_runtime === false, "descriptor must not execute P06 permission/audit binding middle runtime");
  assertCondition(descriptor.executes_p06_decision_binding_middle_runtime === false, "descriptor must not execute P06 decision binding middle runtime");
  assertCondition(descriptor.executes_p06_permission_matrix_runtime === false, "descriptor must not execute P06 permission matrix runtime");
  assertCondition(descriptor.executes_p06_decision_binding_runtime === false, "descriptor must not execute P06 decision binding runtime");
  assertCondition(descriptor.executes_p06_security_interaction_runtime === false, "descriptor must not execute P06 security interaction runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP634_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp634CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp634HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp634P06PermissionAuditBindingTailDescriptor(),
) {
  validateDataRoomVdrCoreCp634P06PermissionAuditBindingTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP634_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP634_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP634_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp634ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp634P06PermissionAuditBindingTailDescriptor(),
) {
  validateDataRoomVdrCoreCp634P06PermissionAuditBindingTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP634_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP634_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP634_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP634_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp634CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp634P06PermissionAuditBindingTailDescriptor(),
) {
  validateDataRoomVdrCoreCp634P06PermissionAuditBindingTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP634_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP634_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP634_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP634_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP634_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp635CoverageSummary(
  descriptor = createDataRoomVdrCoreCp635P06SyntheticFixtureHeadDescriptor(),
) {
  const caseSet = descriptor.p06_synthetic_fixture_head_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP635_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp635Coverage(
  descriptor = createDataRoomVdrCoreCp635P06SyntheticFixtureHeadDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp635CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP635_REQUIREMENTS.expected_unit_count, "CP00-635 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP635_REQUIREMENTS.expected_section_count, "CP00-635 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP635_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p06_synthetic_fixture_head_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-635 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP635_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP635_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp635P06SyntheticFixtureHeadDescriptor(
  descriptor = createDataRoomVdrCoreCp635P06SyntheticFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp635Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp635P06SyntheticFixtureHeadDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP635_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p06_synthetic_fixture_head_runtime === false, "descriptor must not execute P06 synthetic fixture head runtime");
  assertCondition(descriptor.executes_p06_synthetic_permission_matrix_runtime === false, "descriptor must not execute P06 synthetic permission matrix runtime");
  assertCondition(descriptor.executes_p06_synthetic_decision_binding_runtime === false, "descriptor must not execute P06 synthetic decision binding runtime");
  assertCondition(descriptor.executes_p06_synthetic_audit_hint_runtime === false, "descriptor must not execute P06 synthetic audit hint runtime");
  assertCondition(descriptor.executes_p06_synthetic_matched_rule_runtime === false, "descriptor must not execute P06 synthetic matched-rule runtime");
  assertCondition(descriptor.executes_p06_synthetic_deny_over_allow_runtime === false, "descriptor must not execute P06 synthetic deny-over-allow runtime");
  assertCondition(descriptor.executes_p06_permission_audit_binding_tail_runtime === false, "descriptor must not execute P06 permission/audit binding tail runtime");
  assertCondition(descriptor.executes_p06_permission_audit_binding_middle_runtime === false, "descriptor must not execute P06 permission/audit binding middle runtime");
  assertCondition(descriptor.executes_p06_permission_matrix_runtime === false, "descriptor must not execute P06 permission matrix runtime");
  assertCondition(descriptor.executes_p06_decision_binding_runtime === false, "descriptor must not execute P06 decision binding runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP635_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp635CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp635HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp635P06SyntheticFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp635P06SyntheticFixtureHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP635_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP635_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP635_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp635ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp635P06SyntheticFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp635P06SyntheticFixtureHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP635_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP635_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP635_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP635_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp635CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp635P06SyntheticFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp635P06SyntheticFixtureHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP635_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP635_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP635_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP635_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP635_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp636CoverageSummary(
  descriptor = createDataRoomVdrCoreCp636P06P07FixtureFailureBridgeDescriptor(),
) {
  const caseSet = descriptor.p06_p07_fixture_failure_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP636_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp636Coverage(
  descriptor = createDataRoomVdrCoreCp636P06P07FixtureFailureBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp636CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP636_REQUIREMENTS.expected_unit_count, "CP00-636 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP636_REQUIREMENTS.expected_section_count, "CP00-636 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP636_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p06_p07_fixture_failure_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-636 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP636_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP636_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp636P06P07FixtureFailureBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp636P06P07FixtureFailureBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp636Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp636P06P07FixtureFailureBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP636_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p06_synthetic_fixture_head_runtime === false, "descriptor must not execute P06 synthetic fixture head runtime");
  assertCondition(descriptor.executes_p06_synthetic_fixture_tail_runtime === false, "descriptor must not execute P06 synthetic fixture tail runtime");
  assertCondition(descriptor.executes_p06_synthetic_permission_matrix_runtime === false, "descriptor must not execute P06 synthetic permission matrix runtime");
  assertCondition(descriptor.executes_p06_synthetic_decision_binding_runtime === false, "descriptor must not execute P06 synthetic decision binding runtime");
  assertCondition(descriptor.executes_p06_synthetic_audit_hint_runtime === false, "descriptor must not execute P06 synthetic audit hint runtime");
  assertCondition(descriptor.executes_p06_synthetic_matched_rule_runtime === false, "descriptor must not execute P06 synthetic matched-rule runtime");
  assertCondition(descriptor.executes_p06_synthetic_deny_over_allow_runtime === false, "descriptor must not execute P06 synthetic deny-over-allow runtime");
  assertCondition(descriptor.executes_p06_permission_audit_binding_tail_runtime === false, "descriptor must not execute P06 permission/audit binding tail runtime");
  assertCondition(descriptor.executes_p06_permission_audit_binding_middle_runtime === false, "descriptor must not execute P06 permission/audit binding middle runtime");
  assertCondition(descriptor.executes_p06_permission_matrix_runtime === false, "descriptor must not execute P06 permission matrix runtime");
  assertCondition(descriptor.executes_p06_decision_binding_runtime === false, "descriptor must not execute P06 decision binding runtime");
  assertCondition(descriptor.executes_p06_test_golden_case_runtime === false, "descriptor must not execute P06 test/golden case runtime");
  assertCondition(descriptor.executes_p06_hermes_evidence_packet_runtime === false, "descriptor must not execute P06 Hermes evidence packet runtime");
  assertCondition(descriptor.executes_p06_claude_review_packet_runtime === false, "descriptor must not execute P06 Claude review packet runtime");
  assertCondition(descriptor.executes_p06_closeout_handoff_runtime === false, "descriptor must not execute P06 closeout handoff runtime");
  assertCondition(descriptor.executes_p07_failure_recovery_scope_runtime === false, "descriptor must not execute P07 failure recovery scope runtime");
  assertCondition(descriptor.executes_p07_failure_recovery_contract_runtime === false, "descriptor must not execute P07 failure recovery contract runtime");
  assertCondition(descriptor.executes_p07_failure_recovery_type_shape_runtime === false, "descriptor must not execute P07 failure recovery type/shape runtime");
  assertCondition(descriptor.executes_p07_failure_runtime === false, "descriptor must not execute P07 failure runtime");
  assertCondition(descriptor.executes_p07_compensation_runtime === false, "descriptor must not execute P07 compensation runtime");
  assertCondition(descriptor.executes_p07_failure_test_runtime === false, "descriptor must not execute P07 failure test runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP636_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp636CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp636HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp636P06P07FixtureFailureBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp636P06P07FixtureFailureBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP636_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP636_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP636_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp636ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp636P06P07FixtureFailureBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp636P06P07FixtureFailureBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP636_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP636_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP636_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP636_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp636CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp636P06P07FixtureFailureBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp636P06P07FixtureFailureBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP636_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP636_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP636_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP636_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP636_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp637CoverageSummary(
  descriptor = createDataRoomVdrCoreCp637P07FailureRecoveryTypePrimarySecondaryBridgeDescriptor(),
) {
  const caseSet = descriptor.p07_failure_recovery_type_primary_secondary_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP637_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp637Coverage(
  descriptor = createDataRoomVdrCoreCp637P07FailureRecoveryTypePrimarySecondaryBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp637CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP637_REQUIREMENTS.expected_unit_count, "CP00-637 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP637_REQUIREMENTS.expected_section_count, "CP00-637 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP637_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p07_failure_recovery_type_primary_secondary_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-637 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP637_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP637_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp637P07FailureRecoveryTypePrimarySecondaryBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp637P07FailureRecoveryTypePrimarySecondaryBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp637Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp637P07FailureRecoveryTypePrimarySecondaryBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP637_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p07_failure_recovery_scope_runtime === false, "descriptor must not execute P07 failure recovery scope runtime");
  assertCondition(descriptor.executes_p07_failure_recovery_contract_runtime === false, "descriptor must not execute P07 failure recovery contract runtime");
  assertCondition(descriptor.executes_p07_failure_recovery_type_shape_runtime === false, "descriptor must not execute P07 failure recovery type/shape runtime");
  assertCondition(descriptor.executes_p07_failure_recovery_type_tail_runtime === false, "descriptor must not execute P07 failure recovery type-tail runtime");
  assertCondition(descriptor.executes_p07_primary_implementation_runtime === false, "descriptor must not execute P07 primary implementation runtime");
  assertCondition(descriptor.executes_p07_secondary_workflow_head_runtime === false, "descriptor must not execute P07 secondary workflow head runtime");
  assertCondition(descriptor.executes_p07_failure_runtime === false, "descriptor must not execute P07 failure runtime");
  assertCondition(descriptor.executes_p07_compensation_runtime === false, "descriptor must not execute P07 compensation runtime");
  assertCondition(descriptor.executes_p07_failure_test_runtime === false, "descriptor must not execute P07 failure test runtime");
  assertCondition(descriptor.executes_p07_failure_recovery_edge_prompt_runtime === false, "descriptor must not execute P07 failure recovery edge prompt runtime");
  assertCondition(descriptor.executes_p07_human_escalation_runtime === false, "descriptor must not execute P07 human escalation runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP637_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp637CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp637HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp637P07FailureRecoveryTypePrimarySecondaryBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp637P07FailureRecoveryTypePrimarySecondaryBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP637_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP637_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP637_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp637ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp637P07FailureRecoveryTypePrimarySecondaryBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp637P07FailureRecoveryTypePrimarySecondaryBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP637_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP637_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP637_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP637_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp637CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp637P07FailureRecoveryTypePrimarySecondaryBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp637P07FailureRecoveryTypePrimarySecondaryBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP637_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP637_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP637_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP637_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP637_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp638CoverageSummary(
  descriptor = createDataRoomVdrCoreCp638P07SecondaryTailPermissionAuditFixtureHeadDescriptor(),
) {
  const caseSet = descriptor.p07_secondary_tail_permission_audit_fixture_head_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP638_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp638Coverage(
  descriptor = createDataRoomVdrCoreCp638P07SecondaryTailPermissionAuditFixtureHeadDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp638CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP638_REQUIREMENTS.expected_unit_count, "CP00-638 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP638_REQUIREMENTS.expected_section_count, "CP00-638 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP638_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p07_secondary_tail_permission_audit_fixture_head_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-638 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP638_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP638_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp638P07SecondaryTailPermissionAuditFixtureHeadDescriptor(
  descriptor = createDataRoomVdrCoreCp638P07SecondaryTailPermissionAuditFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp638Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp638P07SecondaryTailPermissionAuditFixtureHeadDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP638_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p07_failure_recovery_scope_runtime === false, "descriptor must not execute P07 failure recovery scope runtime");
  assertCondition(descriptor.executes_p07_failure_recovery_contract_runtime === false, "descriptor must not execute P07 failure recovery contract runtime");
  assertCondition(descriptor.executes_p07_failure_recovery_type_shape_runtime === false, "descriptor must not execute P07 failure recovery type/shape runtime");
  assertCondition(descriptor.executes_p07_failure_recovery_type_tail_runtime === false, "descriptor must not execute P07 failure recovery type-tail runtime");
  assertCondition(descriptor.executes_p07_primary_implementation_runtime === false, "descriptor must not execute P07 primary implementation runtime");
  assertCondition(descriptor.executes_p07_secondary_workflow_head_runtime === false, "descriptor must not execute P07 secondary workflow head runtime");
  assertCondition(descriptor.executes_p07_secondary_workflow_tail_runtime === false, "descriptor must not execute P07 secondary workflow tail runtime");
  assertCondition(descriptor.executes_p07_permission_audit_binding_runtime === false, "descriptor must not execute P07 permission/audit binding runtime");
  assertCondition(descriptor.executes_p07_synthetic_fixture_head_runtime === false, "descriptor must not execute P07 synthetic fixture head runtime");
  assertCondition(descriptor.executes_p07_failure_runtime === false, "descriptor must not execute P07 failure runtime");
  assertCondition(descriptor.executes_p07_compensation_runtime === false, "descriptor must not execute P07 compensation runtime");
  assertCondition(descriptor.executes_p07_failure_test_runtime === false, "descriptor must not execute P07 failure test runtime");
  assertCondition(descriptor.executes_p07_failure_recovery_edge_prompt_runtime === false, "descriptor must not execute P07 failure recovery edge prompt runtime");
  assertCondition(descriptor.executes_p07_human_escalation_runtime === false, "descriptor must not execute P07 human escalation runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP638_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp638CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp638HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp638P07SecondaryTailPermissionAuditFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp638P07SecondaryTailPermissionAuditFixtureHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP638_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP638_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP638_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp638ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp638P07SecondaryTailPermissionAuditFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp638P07SecondaryTailPermissionAuditFixtureHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP638_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP638_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP638_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP638_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp638CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp638P07SecondaryTailPermissionAuditFixtureHeadDescriptor(),
) {
  validateDataRoomVdrCoreCp638P07SecondaryTailPermissionAuditFixtureHeadDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP638_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP638_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP638_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP638_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP638_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp639CoverageSummary(
  descriptor = createDataRoomVdrCoreCp639P07FixtureCloseoutP08HermesEvidenceBridgeDescriptor(),
) {
  const caseSet = descriptor.p07_fixture_closeout_p08_hermes_evidence_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP639_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp639Coverage(
  descriptor = createDataRoomVdrCoreCp639P07FixtureCloseoutP08HermesEvidenceBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp639CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP639_REQUIREMENTS.expected_unit_count, "CP00-639 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP639_REQUIREMENTS.expected_section_count, "CP00-639 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP639_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p07_fixture_closeout_p08_hermes_evidence_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-639 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP639_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP639_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp639P07FixtureCloseoutP08HermesEvidenceBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp639P07FixtureCloseoutP08HermesEvidenceBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp639Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp639P07FixtureCloseoutP08HermesEvidenceBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP639_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p07_failure_runtime === false, "descriptor must not execute P07 failure runtime");
  assertCondition(descriptor.executes_p07_compensation_runtime === false, "descriptor must not execute P07 compensation runtime");
  assertCondition(descriptor.executes_p07_failure_test_runtime === false, "descriptor must not execute P07 failure test runtime");
  assertCondition(descriptor.executes_p07_synthetic_fixture_tail_runtime === false, "descriptor must not execute P07 synthetic fixture tail runtime");
  assertCondition(descriptor.executes_p07_test_golden_case_runtime === false, "descriptor must not execute P07 test/golden case runtime");
  assertCondition(descriptor.executes_p07_hermes_evidence_packet_runtime === false, "descriptor must not execute P07 Hermes evidence packet runtime");
  assertCondition(descriptor.executes_p07_claude_review_packet_runtime === false, "descriptor must not execute P07 Claude review packet runtime");
  assertCondition(descriptor.executes_p07_closeout_handoff_runtime === false, "descriptor must not execute P07 closeout handoff runtime");
  assertCondition(descriptor.executes_p08_hermes_evidence_scope_runtime === false, "descriptor must not execute P08 Hermes evidence scope runtime");
  assertCondition(descriptor.executes_p08_hermes_evidence_contract_runtime === false, "descriptor must not execute P08 Hermes evidence contract runtime");
  assertCondition(descriptor.executes_p08_hermes_evidence_type_shape_head_runtime === false, "descriptor must not execute P08 Hermes evidence type-shape head runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP639_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp639CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp639HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp639P07FixtureCloseoutP08HermesEvidenceBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp639P07FixtureCloseoutP08HermesEvidenceBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP639_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP639_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP639_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp639ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp639P07FixtureCloseoutP08HermesEvidenceBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp639P07FixtureCloseoutP08HermesEvidenceBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP639_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP639_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP639_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP639_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp639CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp639P07FixtureCloseoutP08HermesEvidenceBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp639P07FixtureCloseoutP08HermesEvidenceBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP639_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP639_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP639_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP639_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP639_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp640CoverageSummary(
  descriptor = createDataRoomVdrCoreCp640P08HermesEvidenceTypePrimarySecondaryBridgeDescriptor(),
) {
  const caseSet = descriptor.p08_hermes_evidence_type_primary_secondary_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP640_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp640Coverage(
  descriptor = createDataRoomVdrCoreCp640P08HermesEvidenceTypePrimarySecondaryBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp640CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP640_REQUIREMENTS.expected_unit_count, "CP00-640 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP640_REQUIREMENTS.expected_section_count, "CP00-640 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP640_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p08_hermes_evidence_type_primary_secondary_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-640 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP640_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP640_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp640P08HermesEvidenceTypePrimarySecondaryBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp640P08HermesEvidenceTypePrimarySecondaryBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp640Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp640P08HermesEvidenceTypePrimarySecondaryBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP640_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p08_hermes_evidence_scope_runtime === false, "descriptor must not execute P08 Hermes evidence scope runtime");
  assertCondition(descriptor.executes_p08_hermes_evidence_contract_runtime === false, "descriptor must not execute P08 Hermes evidence contract runtime");
  assertCondition(descriptor.executes_p08_hermes_evidence_type_shape_head_runtime === false, "descriptor must not execute P08 Hermes evidence type-shape head runtime");
  assertCondition(descriptor.executes_p08_hermes_evidence_type_shape_tail_runtime === false, "descriptor must not execute P08 Hermes evidence type-shape tail runtime");
  assertCondition(descriptor.executes_p08_hermes_evidence_primary_implementation_runtime === false, "descriptor must not execute P08 Hermes evidence primary implementation runtime");
  assertCondition(descriptor.executes_p08_hermes_evidence_secondary_workflow_head_runtime === false, "descriptor must not execute P08 Hermes evidence secondary workflow head runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP640_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp640CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp640HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp640P08HermesEvidenceTypePrimarySecondaryBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp640P08HermesEvidenceTypePrimarySecondaryBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP640_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP640_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP640_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp640ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp640P08HermesEvidenceTypePrimarySecondaryBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp640P08HermesEvidenceTypePrimarySecondaryBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP640_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP640_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP640_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP640_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp640CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp640P08HermesEvidenceTypePrimarySecondaryBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp640P08HermesEvidenceTypePrimarySecondaryBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP640_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP640_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP640_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP640_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP640_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp641CoverageSummary(
  descriptor = createDataRoomVdrCoreCp641P08HermesEvidenceSecondaryPermissionFixtureBridgeDescriptor(),
) {
  const caseSet = descriptor.p08_hermes_evidence_secondary_permission_fixture_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP641_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp641Coverage(
  descriptor = createDataRoomVdrCoreCp641P08HermesEvidenceSecondaryPermissionFixtureBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp641CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP641_REQUIREMENTS.expected_unit_count, "CP00-641 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP641_REQUIREMENTS.expected_section_count, "CP00-641 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP641_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p08_hermes_evidence_secondary_permission_fixture_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-641 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP641_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP641_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp641P08HermesEvidenceSecondaryPermissionFixtureBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp641P08HermesEvidenceSecondaryPermissionFixtureBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp641Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp641P08HermesEvidenceSecondaryPermissionFixtureBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP641_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p08_hermes_evidence_secondary_workflow_tail_runtime === false, "descriptor must not execute P08 Hermes evidence secondary workflow tail runtime");
  assertCondition(descriptor.executes_p08_hermes_evidence_permission_audit_binding_runtime === false, "descriptor must not execute P08 Hermes evidence permission/audit binding runtime");
  assertCondition(descriptor.executes_p08_hermes_evidence_synthetic_fixture_head_runtime === false, "descriptor must not execute P08 Hermes evidence synthetic fixture head runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP641_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp641CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp641HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp641P08HermesEvidenceSecondaryPermissionFixtureBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp641P08HermesEvidenceSecondaryPermissionFixtureBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP641_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP641_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP641_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp641ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp641P08HermesEvidenceSecondaryPermissionFixtureBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp641P08HermesEvidenceSecondaryPermissionFixtureBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP641_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP641_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP641_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP641_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp641CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp641P08HermesEvidenceSecondaryPermissionFixtureBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp641P08HermesEvidenceSecondaryPermissionFixtureBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP641_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP641_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP641_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP641_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP641_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp642CoverageSummary(
  descriptor = createDataRoomVdrCoreCp642P08HermesEvidenceSyntheticFixtureTailDescriptor(),
) {
  const caseSet = descriptor.p08_hermes_evidence_synthetic_fixture_tail_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP642_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp642Coverage(
  descriptor = createDataRoomVdrCoreCp642P08HermesEvidenceSyntheticFixtureTailDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp642CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP642_REQUIREMENTS.expected_unit_count, "CP00-642 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP642_REQUIREMENTS.expected_section_count, "CP00-642 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP642_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p08_hermes_evidence_synthetic_fixture_tail_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-642 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP642_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP642_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp642P08HermesEvidenceSyntheticFixtureTailDescriptor(
  descriptor = createDataRoomVdrCoreCp642P08HermesEvidenceSyntheticFixtureTailDescriptor(),
) {
  validateDataRoomVdrCoreCp642Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp642P08HermesEvidenceSyntheticFixtureTailDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP642_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p08_hermes_evidence_synthetic_fixture_tail_runtime === false, "descriptor must not execute P08 Hermes evidence synthetic fixture tail runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP642_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp642CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp642HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp642P08HermesEvidenceSyntheticFixtureTailDescriptor(),
) {
  validateDataRoomVdrCoreCp642P08HermesEvidenceSyntheticFixtureTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP642_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP642_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP642_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp642ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp642P08HermesEvidenceSyntheticFixtureTailDescriptor(),
) {
  validateDataRoomVdrCoreCp642P08HermesEvidenceSyntheticFixtureTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP642_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP642_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP642_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP642_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp642CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp642P08HermesEvidenceSyntheticFixtureTailDescriptor(),
) {
  validateDataRoomVdrCoreCp642P08HermesEvidenceSyntheticFixtureTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP642_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP642_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP642_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP642_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP642_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp643CoverageSummary(
  descriptor = createDataRoomVdrCoreCp643P08P09ReviewCloseoutBridgeDescriptor(),
) {
  const caseSet = descriptor.p08_p09_review_closeout_bridge_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP643_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp643Coverage(
  descriptor = createDataRoomVdrCoreCp643P08P09ReviewCloseoutBridgeDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp643CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP643_REQUIREMENTS.expected_unit_count, "CP00-643 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP643_REQUIREMENTS.expected_section_count, "CP00-643 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP643_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p08_p09_review_closeout_bridge_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-643 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP643_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP643_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp643P08P09ReviewCloseoutBridgeDescriptor(
  descriptor = createDataRoomVdrCoreCp643P08P09ReviewCloseoutBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp643Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp643P08P09ReviewCloseoutBridgeDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP643_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p08_hermes_evidence_synthetic_fixture_documentation_tail_runtime === false, "descriptor must not execute P08 Hermes evidence synthetic fixture documentation tail runtime");
  assertCondition(descriptor.executes_p08_test_golden_case_runtime === false, "descriptor must not execute P08 test/golden runtime");
  assertCondition(descriptor.executes_p08_hermes_evidence_packet_runtime === false, "descriptor must not execute P08 Hermes evidence packet runtime");
  assertCondition(descriptor.executes_p08_claude_review_packet_runtime === false, "descriptor must not execute P08 Claude review packet runtime");
  assertCondition(descriptor.executes_p08_closeout_handoff_runtime === false, "descriptor must not execute P08 closeout handoff runtime");
  assertCondition(descriptor.executes_p09_review_scope_runtime === false, "descriptor must not execute P09 review scope runtime");
  assertCondition(descriptor.executes_p09_review_contract_runtime === false, "descriptor must not execute P09 review contract runtime");
  assertCondition(descriptor.executes_p09_review_type_shape_runtime === false, "descriptor must not execute P09 review type/shape runtime");
  assertCondition(descriptor.executes_p09_review_primary_implementation_runtime === false, "descriptor must not execute P09 review primary implementation runtime");
  assertCondition(descriptor.executes_p09_review_secondary_workflow_head_runtime === false, "descriptor must not execute P09 review secondary workflow head runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP643_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp643CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp643HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp643P08P09ReviewCloseoutBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp643P08P09ReviewCloseoutBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP643_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP643_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP643_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp643ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp643P08P09ReviewCloseoutBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp643P08P09ReviewCloseoutBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP643_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP643_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP643_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP643_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp643CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp643P08P09ReviewCloseoutBridgeDescriptor(),
) {
  validateDataRoomVdrCoreCp643P08P09ReviewCloseoutBridgeDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP643_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP643_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP643_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP643_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP643_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}

export function createDataRoomVdrCoreCp644CoverageSummary(
  descriptor = createDataRoomVdrCoreCp644P09ReviewCloseoutTailDescriptor(),
) {
  const caseSet = descriptor.p09_review_closeout_tail_case_set;
  const micro_counts = {};
  const deliverable_counts = {};
  let covered_units = 0;

  for (const [microId, section] of Object.entries(caseSet.sections)) {
    micro_counts[microId] = section.row_count;
    covered_units += section.row_count;
    for (const row of Object.values(section.rows)) {
      deliverable_counts[row.deliverable_type] = (deliverable_counts[row.deliverable_type] ?? 0) + 1;
    }
  }

  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP644_PACK_BINDING.pack_id,
    covered_units,
    section_count: caseSet.section_count,
    micro_counts: Object.freeze(micro_counts),
    deliverable_counts: Object.freeze(deliverable_counts),
  });
}

export function validateDataRoomVdrCoreCp644Coverage(
  descriptor = createDataRoomVdrCoreCp644P09ReviewCloseoutTailDescriptor(),
) {
  const summary = createDataRoomVdrCoreCp644CoverageSummary(descriptor);
  assertCondition(summary.covered_units === DATA_ROOM_VDR_CORE_CP644_REQUIREMENTS.expected_unit_count, "CP00-644 covered unit count mismatch");
  assertCondition(summary.section_count === DATA_ROOM_VDR_CORE_CP644_REQUIREMENTS.expected_section_count, "CP00-644 section count mismatch");

  for (const [microId, rows] of Object.entries(DATA_ROOM_VDR_CORE_CP644_REQUIREMENTS.required_section_rows)) {
    const section = descriptor.p09_review_closeout_tail_case_set.sections[microId];
    assertCondition(Boolean(section), `missing CP00-644 section: ${microId}`);
    assertCondition(section.row_count === rows.length, `row count mismatch for ${microId}`);
    assertCondition(section.micro_title === DATA_ROOM_VDR_CORE_CP644_REQUIREMENTS.required_section_micro_titles[microId], `micro title mismatch for ${microId}`);
    const expectedKeys = rows.map((title) => dataRoomVdrCoreRowKey(title));
    assertCondition(JSON.stringify(section.row_keys) === JSON.stringify(expectedKeys), `row key mismatch for ${microId}`);
  }

  for (const [deliverableType, expectedCount] of Object.entries(DATA_ROOM_VDR_CORE_CP644_REQUIREMENTS.expected_deliverable_counts)) {
    assertCondition(summary.deliverable_counts[deliverableType] === expectedCount, `deliverable count mismatch for ${deliverableType}`);
  }

  return summary;
}

export function validateDataRoomVdrCoreCp644P09ReviewCloseoutTailDescriptor(
  descriptor = createDataRoomVdrCoreCp644P09ReviewCloseoutTailDescriptor(),
) {
  validateDataRoomVdrCoreCp644Coverage(descriptor);
  assertCondition(descriptor.descriptor === "DataRoomVdrCoreCp644P09ReviewCloseoutTailDescriptor", "descriptor name mismatch");
  assertCondition(descriptor.pack_binding.pack_id === DATA_ROOM_VDR_CORE_CP644_PACK_BINDING.pack_id, "pack binding mismatch");
  assertCondition(descriptor.program_contract.program_id === DATA_ROOM_VDR_CORE_PROGRAM_CONTRACT.program_id, "program contract mismatch");
  assertCondition(descriptor.synthetic_only === true, "descriptor must be synthetic only");
  assertCondition(descriptor.no_real_data === true, "descriptor must exclude real deal data");
  assertCondition(descriptor.writes_product_state === false, "descriptor must not write product state");
  assertCondition(descriptor.dispatches_data_room_runtime === false, "descriptor must not dispatch data-room runtime");
  assertCondition(descriptor.dispatches_vdr_runtime === false, "descriptor must not dispatch VDR runtime");
  assertCondition(descriptor.dispatches_rfi_runtime === false, "descriptor must not dispatch RFI runtime");
  assertCondition(descriptor.dispatches_cp_runtime === false, "descriptor must not dispatch CP runtime");
  assertCondition(descriptor.dispatches_closing_binder_runtime === false, "descriptor must not dispatch closing binder runtime");
  assertCondition(descriptor.dispatches_access_analytics_runtime === false, "descriptor must not dispatch access analytics runtime");
  assertCondition(descriptor.evaluates_runtime_permission === false, "descriptor must not evaluate runtime permission");
  assertCondition(descriptor.writes_permission_decision === false, "descriptor must not write permission decisions");
  assertCondition(descriptor.writes_audit_event === false, "descriptor must not write audit events");
  assertCondition(descriptor.appends_audit_trace === false, "descriptor must not append audit traces");
  assertCondition(descriptor.reads_object_storage === false, "descriptor must not read object storage");
  assertCondition(descriptor.writes_object_storage === false, "descriptor must not write object storage");
  assertCondition(descriptor.emits_hermes_runtime_receipt === false, "descriptor must not emit Hermes runtime receipts");
  assertCondition(descriptor.executes_p09_review_secondary_workflow_tail_runtime === false, "descriptor must not execute P09 review secondary workflow tail runtime");
  assertCondition(descriptor.executes_p09_review_permission_audit_binding_runtime === false, "descriptor must not execute P09 review permission/audit binding runtime");
  assertCondition(descriptor.executes_p09_review_synthetic_fixture_runtime === false, "descriptor must not execute P09 review synthetic fixture runtime");
  assertCondition(descriptor.executes_p09_review_test_golden_case_runtime === false, "descriptor must not execute P09 review test/golden runtime");
  assertCondition(descriptor.executes_p09_review_hermes_evidence_packet_runtime === false, "descriptor must not execute P09 review Hermes evidence packet runtime");
  assertCondition(descriptor.executes_p09_review_claude_review_packet_runtime === false, "descriptor must not execute P09 review Claude review packet runtime");
  assertCondition(descriptor.executes_p09_review_closeout_handoff_runtime === false, "descriptor must not execute P09 review closeout handoff runtime");
  assertCondition(descriptor.authority_boundary.claude_is_final_approval === false, "Claude cannot be final approval");
  assertCondition(descriptor.authority_boundary.local_validation_claims_enterprise_trust === false, "local validation cannot claim enterprise trust");
  assertCondition(descriptor.authority_boundary.human_final_approval_required_for_runtime_opening === true, "runtime opening requires human approval");
  return Object.freeze({
    ok: true,
    pack_id: DATA_ROOM_VDR_CORE_CP644_PACK_BINDING.pack_id,
    production_ready_candidate: true,
    coverage: createDataRoomVdrCoreCp644CoverageSummary(descriptor),
  });
}

export function createDataRoomVdrCoreCp644HermesEvidencePacket(
  descriptor = createDataRoomVdrCoreCp644P09ReviewCloseoutTailDescriptor(),
) {
  validateDataRoomVdrCoreCp644P09ReviewCloseoutTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP644_PACK_BINDING.pack_id,
    hermes_gate: DATA_ROOM_VDR_CORE_CP644_PACK_BINDING.hermes_gate,
    evidence_packet: descriptor.hermes_packet.evidence_packet,
    receipt_shape: descriptor.hermes_packet.receipt_shape,
    descriptor_only: true,
    emits_hermes_runtime_receipt: false,
    no_write_attestation: DATA_ROOM_VDR_CORE_CP644_NO_WRITE_ATTESTATION,
  });
}

export function createDataRoomVdrCoreCp644ClaudeReviewPacket(
  descriptor = createDataRoomVdrCoreCp644P09ReviewCloseoutTailDescriptor(),
) {
  validateDataRoomVdrCoreCp644P09ReviewCloseoutTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP644_PACK_BINDING.pack_id,
    claude_gate: DATA_ROOM_VDR_CORE_CP644_PACK_BINDING.claude_gate,
    review_packet: descriptor.claude_packet.review_packet,
    allowed_tools: DATA_ROOM_VDR_CORE_CP644_REQUIREMENTS.allowed_claude_tools,
    invalid_review_blockers: DATA_ROOM_VDR_CORE_CP644_REQUIREMENTS.forbidden_review_evidence,
    read_only: true,
    claude_is_final_approval: false,
    source_inspection_basis: "read_tools_used",
  });
}

export function createDataRoomVdrCoreCp644CloseoutHandoff(
  descriptor = createDataRoomVdrCoreCp644P09ReviewCloseoutTailDescriptor(),
) {
  validateDataRoomVdrCoreCp644P09ReviewCloseoutTailDescriptor(descriptor);
  return Object.freeze({
    pack_id: DATA_ROOM_VDR_CORE_CP644_PACK_BINDING.pack_id,
    from_pack_id: DATA_ROOM_VDR_CORE_CP644_PACK_BINDING.pack_id,
    to_pack_id: DATA_ROOM_VDR_CORE_CP644_PACK_BINDING.next_pack_id,
    next_subphase_id: DATA_ROOM_VDR_CORE_CP644_PACK_BINDING.next_subphase_id,
    upstream_pack_id: DATA_ROOM_VDR_CORE_CP644_PACK_BINDING.upstream_pack_id,
    runtime_opened: false,
    handoff_descriptor_only: true,
    open_scope: descriptor.closeout_handoff.open_scope,
  });
}
