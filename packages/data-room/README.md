# @law-firm-os/data-room

RP20 "Data Room And VDR" descriptor-only program package (M&A room, RFI, CP, closing binder, access analytics). Gates H20/C20, upstream RP19. CP00-610 does not open data-room runtime, document serving, secure link runtime, CP/RFI workflow runtime, closing binder export, access analytics runtime, object storage, audit writes, permission decision writes, Hermes runtime receipts, real deal data, Claude final approval, or enterprise trust claims from local validation.

## CP00-610

CP00-610 bootstraps the RP20 program: the program contract (`contracts/data-room-vdr-core-contract.json`), this package, and the `rp20:data-room-vdr-core:validate` gate, covering the full RP20.P00 scope foundation cycle (M00-M10: scope inventory, acceptance gate definition, non-goal boundary, target file map, contract schema outline, ownership note, matter-first trace note, permission baseline note, audit baseline note, synthetic data policy, risk register row, blocked-claim rule, Hermes preflight fields, Claude review prompts, human approval note, closeout handoff, dependency list, downstream RP routing, command matrix, receipt shape) and the RP20.P01 model foundation head through `RP20.P01.M02.S08` as fourteen descriptor-only sections over 150 planned rows.

## CP00-611

CP00-611 continues the RP20.P01 model foundation from `RP20.P01.M02.S09` through `RP20.P01.M04.S06`, covering optional/state/validation/fixture/serialization/public export/test/Hermes/Claude/closeout rows for the tail of Type And Shape Definition, the full Primary Implementation Slice model section, and the head of Secondary Workflow Slice. It remains descriptor-only: no VDR runtime, model runtime, validation runtime, fixture runtime, test runtime, object storage access, audit writes, or real deal data.

## CP00-612

CP00-612 continues the RP20.P01 descriptor queue from `RP20.P01.M04.S07` through `RP20.P01.M06.S04`, covering the tail of Secondary Workflow Slice, the full Permission And Audit Binding model section, and the head of Synthetic Fixture Set. It remains descriptor-only: no Data Room or VDR runtime, no RFI/CP/closing binder/access analytics dispatch, no permission decision writes, no audit trace appends, no fixture runtime, no object storage access, no Hermes runtime receipt, and no real deal data.

## CP00-613

CP00-613 continues from `RP20.P01.M06.S05` through `RP20.P02.M02.S22`, closing the P01 synthetic fixture/test/Hermes/Claude/closeout descriptor tail and bridging into the first P02 service foundation rows. It remains descriptor-only: no service entrypoint runtime, request normalization runtime, permission precheck runtime, audit hint runtime, persistence, lock, idempotency, failure recovery, integration smoke, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, Hermes runtime receipt, or real deal data behavior is opened.

## CP00-614

CP00-614 continues the P02 service descriptor lane from `RP20.P02.M03.S01` through `RP20.P02.M04.S18`, covering the full Primary Implementation Slice and the pre-test Secondary Workflow Slice rows. It remains descriptor-only: no service entrypoint runtime, request normalization runtime, primary implementation runtime, secondary workflow runtime, permission precheck runtime, audit hint runtime, persistence, lock, idempotency, failure recovery, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, Hermes runtime receipt, or real deal data behavior is opened.

## CP00-615

CP00-615 continues from `RP20.P02.M04.S19` through `RP20.P02.M06.S14`, closing the Secondary Workflow Slice service test tail, covering the full Permission And Audit Binding service descriptor, and opening the Synthetic Fixture Set head as descriptors only. It does not open service entrypoint runtime, request normalization runtime, permission/audit binding runtime, fixture head runtime, integration smoke runtime, persistence, lock, idempotency, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, Hermes runtime receipt, or real deal data behavior.

## CP00-616

CP00-616 covers the Risk A bridge from `RP20.P02.M06.S15` through `RP20.P02.M07.S02`, closing the Synthetic Fixture Set test/failure-recovery tail and opening the Test And Golden Case Set with descriptor-only service entrypoint and request normalization rows. It does not open fixture tail runtime, test/golden-case runtime, service entrypoint runtime, request normalization runtime, unit test runtime paths, integration smoke runtime, failure recovery runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, Hermes runtime receipt, or real deal data behavior.

## CP00-617

CP00-617 continues the Risk A Test And Golden Case Set from `RP20.P02.M07.S03` through `RP20.P02.M07.S12`, covering tenant boundary, matter trace, permission/audit precheck, primary/secondary path, state transition, idempotency, lock, and persistence rows as descriptors only. It does not open test/golden-case core runtime, permission precheck runtime, audit hint runtime, primary or secondary implementation runtime, state transition runtime, idempotency runtime, lock runtime, persistence runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, Hermes runtime receipt, object storage access, audit writes, permission decision writes, or real deal data behavior.

## CP00-618

CP00-618 covers the Risk B bridge from `RP20.P02.M07.S13` through `RP20.P02.M09.S08`, closing the Test And Golden Case Set tail, covering the full Hermes Evidence Packet descriptor rows, and opening the Claude Review Packet head through secondary workflow path. It remains descriptor-only: no test/golden-case tail runtime, Hermes evidence packet runtime, Claude review packet runtime, service entrypoint runtime, permission/audit runtime, failure recovery runtime, integration smoke runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, Hermes runtime receipt, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-619

CP00-619 covers the Risk C bridge from `RP20.P02.M09.S09` through `RP20.P03.M06.S12`, closing the Claude Review Packet and Closeout And Next Handoff descriptor rows, then opening the P03 API/interface descriptor lane through the Synthetic Fixture Set invalid-request test. It remains descriptor-only: no API handler runtime, API/interface runtime, room setup API runtime, request/response contract runtime, serialization runtime, synthetic fixture runtime, Hermes API runtime evidence, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-620

CP00-620 covers the Risk C bridge from `RP20.P03.M06.S13` through `RP20.P04.M03.S20`, closing the P03 API/interface fixture tail, test/golden-case, Hermes, Claude, and handoff descriptor rows, then opening the P04 UI descriptor lane through Primary Implementation Slice closeout handoff. It remains descriptor-only: no API/interface runtime, UI runtime, UI surface runtime, data-dependency runtime, UI interaction runtime, responsive layout runtime, keyboard/focus runtime, UI synthetic fixture runtime, build smoke runtime, Hermes UI runtime evidence, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-621

CP00-621 covers the Risk C bridge from `RP20.P04.M04.S01` through `RP20.P05.M00.S10`, closing the P04 UI secondary workflow, permission/audit, synthetic fixture, test/golden-case, Hermes, Claude, and handoff descriptor rows, then opening the P05 fixture foundation head. It remains descriptor-only: no UI runtime, UI surface runtime, UI interaction runtime, UI state snapshot runtime, unauthorized-count query runtime, P05 fixture foundation runtime, golden-case runtime, cross-tenant fixture runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-622

CP00-622 covers the Risk A `RP20.P05.M01.S01-RP20.P05.M01.S10` fixture contract draft head: base tenant/user/matter/document fixtures, primary and secondary golden cases, review-required, denied, cross-tenant, and missing-context cases. It remains descriptor-only: no P05 fixture foundation runtime, fixture contract draft runtime, fixture manifest runtime, golden-case runtime, cross-tenant fixture runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-623

CP00-623 covers the Risk A `RP20.P05.M01.S11-RP20.P05.M01.S20` fixture contract draft tail: audit hint, security trimming, AI retrieval/analytics, fixture manifest, golden/failure tests, Hermes fixture evidence, Claude missing-test prompt, closeout handoff, and no-real-data check rows. It remains descriptor-only: no P05 fixture contract draft tail runtime, AI retrieval runtime, fixture test runtime, Hermes fixture runtime evidence, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-624

CP00-624 covers the Risk A `RP20.P05.M02.S01-RP20.P05.M02.S10` fixture type-and-shape head: base tenant/user/matter/document fixtures, primary and secondary golden cases, review-required, denied, cross-tenant, and missing-context cases. It remains descriptor-only: no P05 fixture type/shape runtime, base fixture shape runtime, golden-case type/shape runtime, review-required type/shape runtime, denied or cross-tenant type/shape runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-625

CP00-625 covers the Risk A `RP20.P05.M02.S11-RP20.P05.M02.S20` fixture type-and-shape tail: audit hint, security trimming, AI retrieval/analytics, fixture manifest, golden/failure tests, Hermes fixture evidence, Claude missing-test prompt, closeout handoff, and no-real-data check rows. It remains descriptor-only: no P05 fixture type/shape tail runtime, type-shape AI retrieval runtime, type-shape fixture test runtime, type-shape Hermes fixture runtime evidence, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-626

CP00-626 covers the Risk A `RP20.P05.M03.S01-RP20.P05.M03.S10` primary implementation head: base tenant/user/matter/document fixtures, primary and secondary golden cases, review-required, denied, cross-tenant, and missing-context cases. It remains descriptor-only: no P05 primary implementation runtime, primary fixture runtime, primary golden-case runtime, primary review-required runtime, primary denied or cross-tenant runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-627

CP00-627 covers the Risk A `RP20.P05.M03.S11-RP20.P05.M03.S20` primary implementation tail: audit hint, security trimming, AI retrieval or analytics, fixture manifest, golden/failure tests, Hermes fixture evidence, Claude missing-test prompt, closeout handoff, and no-real-data check rows. It remains descriptor-only: no P05 primary implementation tail runtime, primary AI retrieval runtime, primary fixture test runtime, primary Hermes fixture runtime evidence, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-628

CP00-628 covers the Risk B `RP20.P05.M03.S21-RP20.P05.M05.S16` secondary workflow and permission/audit bridge: primary stable ID and replay checks, all `RP20.P05.M04` secondary workflow rows, and the first 16 `RP20.P05.M05` permission/audit rows. It remains descriptor-only: no P05 stable ID, replay, secondary workflow, permission/audit binding, secondary fixture, permission/audit fixture, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-629

CP00-629 covers the Risk A `RP20.P05.M05.S17-RP20.P05.M06.S04` permission/audit tail and synthetic fixture head: Hermes fixture evidence, Claude missing-test prompt, closeout handoff, no-real-data, stable ID, replay, and the first four synthetic fixture rows. It remains descriptor-only: no P05 permission/audit tail runtime, synthetic fixture head runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-630

CP00-630 covers the Risk C `RP20.P05.M06.S05-RP20.P06.M02.S18` fixture closeout and P06 permission matrix bridge: the remaining synthetic fixture rows, the full P05 test/golden, Hermes, Claude, and closeout sections, then P06 scope inventory, contract draft, and type/shape rows through permission fixture. It remains descriptor-only: no P05 fixture closeout runtime, P06 permission matrix runtime, P06 decision binding runtime, P06 security interaction runtime, P06 permission fixture runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-631

CP00-631 covers the Risk B `RP20.P06.M02.S19-RP20.P06.M04.S14` type/shape tail and primary/secondary bridge: allowed, denied, cross-tenant, and leak-prevention tests for type/shape; the full P06 primary implementation permission matrix rows; and the secondary workflow head through review-required route. It remains descriptor-only: no P06 type/shape test-tail runtime, P06 primary implementation runtime, P06 secondary workflow head runtime, P06 cross-tenant test runtime, P06 leak-prevention test runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-632

CP00-632 covers the Risk A `RP20.P06.M04.S15-RP20.P06.M05.S02` secondary workflow tail and permission/audit binding head: approval-required route, security trimming proof, audit event expectation, permission fixture, allowed/denied/cross-tenant/leak-prevention tests, permission matrix row, and view decision binding. It remains descriptor-only: no P06 secondary workflow tail runtime, permission/audit binding head runtime, approval route runtime, security trimming runtime, audit event runtime, cross-tenant test runtime, leak-prevention test runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-633

CP00-633 covers the Risk A `RP20.P06.M05.S03-RP20.P06.M05.S12` permission/audit binding middle: search, mutation, export/download, share, and AI retrieval decision bindings; audit hint fields; matched rule capture; deny-over-allow check; legal hold interaction; and ethical wall interaction. It remains descriptor-only: no P06 permission/audit binding middle runtime, decision binding middle runtime, audit hint runtime, matched-rule capture runtime, deny-over-allow runtime, legal-hold interaction runtime, ethical-wall interaction runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-634

CP00-634 covers the Risk A `RP20.P06.M05.S13-RP20.P06.M05.S22` permission/audit binding tail: object ACL interaction, review-required and approval-required routes, security trimming proof, audit event expectation, permission fixture, and allowed/denied/cross-tenant/leak-prevention tests. It remains descriptor-only: no P06 permission/audit binding tail runtime, object ACL interaction runtime, review-required route runtime, approval-required route runtime, security trimming proof runtime, audit event expectation runtime, permission fixture runtime, permission test runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-635

CP00-635 covers the Risk A `RP20.P06.M06.S01-RP20.P06.M06.S10` synthetic fixture head: permission matrix row; view, search, mutation, export/download, share, and AI retrieval decision bindings; audit hint fields; matched rule capture; and deny-over-allow check. It remains descriptor-only: no P06 synthetic fixture head runtime, synthetic permission matrix runtime, synthetic decision binding runtime, synthetic audit hint runtime, synthetic matched-rule runtime, synthetic deny-over-allow runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-636

CP00-636 covers the Risk C `RP20.P06.M06.S11-RP20.P07.M02.S12` P06/P07 fixture failure bridge: the remaining P06 synthetic fixture rows, P06 test/golden, Hermes evidence, Claude review, and closeout rows, plus P07 failure recovery scope, contract, and type/shape head rows through retry exhaustion failure. It remains descriptor-only: no P06 synthetic fixture tail runtime, P06 test/golden runtime, P06 Hermes or Claude packet runtime, P06 closeout runtime, P07 failure recovery scope/contract/type-shape runtime, P07 failure runtime, P07 compensation runtime, P07 failure test runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-637

CP00-637 covers the Risk B `RP20.P07.M02.S13-RP20.P07.M04.S08` P07 failure recovery continuation: type/shape tail rollback, compensation, blocked-claim, fixture, test, audit, Hermes, Claude edge-case, and human escalation rows; the full primary implementation failure recovery row set; and the secondary workflow head through permission denied failure. It remains descriptor-only: no P07 failure recovery type-tail runtime, primary implementation runtime, secondary workflow head runtime, edge-prompt runtime, human escalation runtime, failure runtime, compensation runtime, failure test runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-638

CP00-638 covers the Risk B `RP20.P07.M04.S09-RP20.P07.M06.S04` P07 failure recovery continuation: the secondary workflow tail from ambiguous rule failure through human escalation note, the full permission and audit binding failure-recovery row set, and the synthetic fixture head through missing Matter failure. It remains descriptor-only: no P07 secondary workflow tail runtime, permission/audit binding runtime, synthetic fixture head runtime, failure runtime, compensation runtime, failure test runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-639

CP00-639 covers the Risk C `RP20.P07.M06.S05-RP20.P08.M02.S16` P07/P08 bridge: the remaining P07 synthetic fixture failure-recovery rows, P07 test, Hermes, Claude, and closeout rows, plus the P08 Hermes evidence scope, contract, and type/shape head rows. It remains descriptor-only: no P07 synthetic fixture tail runtime, test/golden runtime, Hermes evidence packet runtime, Claude review packet runtime, closeout handoff runtime, P08 Hermes evidence scope/contract/type-shape runtime, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-640

CP00-640 covers the Risk B `RP20.P08.M02.S17-RP20.P08.M04.S14` P08 Hermes evidence continuation: the type/shape tail rows, the full primary implementation evidence row set including documentation and operator summary, and the secondary workflow head through BLOCK semantics. It remains descriptor-only: no P08 Hermes evidence type/shape tail runtime, primary implementation runtime, secondary workflow head runtime, command execution runtime, Hermes runtime receipt, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-641

CP00-641 covers the Risk B `RP20.P08.M04.S15-RP20.P08.M06.S10` P08 Hermes evidence bridge: the secondary workflow tail, the full permission/audit binding evidence row set, and the synthetic fixture head through Claude dependency marker. It remains descriptor-only: no P08 Hermes evidence secondary workflow tail runtime, permission/audit binding runtime, synthetic fixture head runtime, runtime permission evaluation, audit writes, Hermes runtime receipt, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, permission decision writes, or real deal data behavior is opened.

## CP00-642

CP00-642 covers the Risk A `RP20.P08.M06.S11-RP20.P08.M06.S20` P08 Hermes evidence synthetic fixture tail: human approval marker, PASS/PASS_WITH_FINDINGS/BLOCK semantics, evidence template, validation command check, harness boundary note, closeout handoff, regression receipt, and next gate readiness. It remains descriptor-only: no P08 Hermes evidence synthetic fixture tail runtime, review verdict runtime, Hermes runtime receipt, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-643

CP00-643 covers the Risk C `RP20.P08.M06.S21-RP20.P09.M04.S12` P08/P09 review closeout bridge: P08 synthetic fixture documentation/operator rows, P08 test/golden, Hermes evidence, Claude review, and closeout packet rows, plus P09 review scope, contract, type/shape, primary implementation, and secondary workflow head rows through human approval summary. It remains descriptor-only: no P08 documentation tail, test/golden, Hermes packet, Claude packet, closeout handoff, P09 review scope/contract/type-shape/primary/secondary runtime, Hermes runtime receipt, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, or real deal data behavior is opened.

## CP00-644

CP00-644 covers the Risk C `RP20.P09.M04.S13-RP20.P09.M10.S10` P09 review closeout tail: the remaining secondary workflow review rows, permission/audit binding review packet, synthetic fixture review rows, test/golden review rows, Hermes evidence review rows, Claude review rows, and closeout/next handoff rows through go/no-go verdict format. It remains descriptor-only: no P09 secondary tail, permission/audit, synthetic fixture, test/golden, Hermes evidence, Claude review, closeout handoff, Data Room, VDR, RFI/CP/closing binder/access analytics dispatch, object storage access, audit writes, permission decision writes, Hermes runtime receipt, or real deal data behavior is opened.
