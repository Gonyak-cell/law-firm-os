import {
  CLIENT_PORTAL_CORE_CP583_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP583_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP583_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP584_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP584_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP584_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP585_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP585_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP585_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP586_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP586_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP586_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP587_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP587_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP587_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP588_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP588_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP588_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP589_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP589_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP589_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP590_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP590_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP590_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP591_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP591_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP591_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP592_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP592_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP592_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP593_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP593_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP593_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP594_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP594_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP594_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP595_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP595_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP595_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP596_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP596_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP596_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP597_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP597_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP597_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP598_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP598_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP598_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP599_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP599_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP599_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP600_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP600_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP600_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP601_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP601_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP601_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP602_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP602_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP602_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP603_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP603_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP603_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP604_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP604_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP604_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP605_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP605_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP605_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP606_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP606_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP606_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP607_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP607_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP607_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP608_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP608_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP608_REQUIREMENTS,
  CLIENT_PORTAL_CORE_CP609_NO_WRITE_ATTESTATION,
  CLIENT_PORTAL_CORE_CP609_PACK_BINDING,
  CLIENT_PORTAL_CORE_CP609_REQUIREMENTS,
} from "./registry.js";
import {
  createClientPortalCoreCp583ScopeContractFoundationCaseSet,
  createClientPortalCoreCp583ScopeContractFoundationDescriptor,
  createClientPortalCoreCp584P01CloseoutP02FoundationCaseSet,
  createClientPortalCoreCp584P01CloseoutP02FoundationDescriptor,
  createClientPortalCoreCp585P02WorkflowPermissionSliceCaseSet,
  createClientPortalCoreCp585P02WorkflowPermissionSliceDescriptor,
  createClientPortalCoreCp586P02FixtureSliceCaseSet,
  createClientPortalCoreCp586P02FixtureSliceDescriptor,
  createClientPortalCoreCp587P02CloseoutP03FoundationCaseSet,
  createClientPortalCoreCp587P02CloseoutP03FoundationDescriptor,
  createClientPortalCoreCp588P03PermissionSliceCaseSet,
  createClientPortalCoreCp588P03PermissionSliceDescriptor,
  createClientPortalCoreCp589P03PermissionFixtureSliceCaseSet,
  createClientPortalCoreCp589P03PermissionFixtureSliceDescriptor,
  createClientPortalCoreCp590P03CloseoutP04FoundationCaseSet,
  createClientPortalCoreCp590P03CloseoutP04FoundationDescriptor,
  createClientPortalCoreCp591P04PermissionSliceCaseSet,
  createClientPortalCoreCp591P04PermissionSliceDescriptor,
  createClientPortalCoreCp592P04PermissionFixtureSliceCaseSet,
  createClientPortalCoreCp592P04PermissionFixtureSliceDescriptor,
  createClientPortalCoreCp593P04CloseoutP05FoundationCaseSet,
  createClientPortalCoreCp593P04CloseoutP05FoundationDescriptor,
  createClientPortalCoreCp594P05WorkflowPermissionSliceCaseSet,
  createClientPortalCoreCp594P05WorkflowPermissionSliceDescriptor,
  createClientPortalCoreCp595P05CloseoutP06FoundationCaseSet,
  createClientPortalCoreCp595P05CloseoutP06FoundationDescriptor,
  createClientPortalCoreCp596P06ImplementationSliceCaseSet,
  createClientPortalCoreCp596P06ImplementationSliceDescriptor,
  createClientPortalCoreCp597P06WorkflowPermissionSliceCaseSet,
  createClientPortalCoreCp597P06WorkflowPermissionSliceDescriptor,
  createClientPortalCoreCp598P06PermissionFixtureSliceCaseSet,
  createClientPortalCoreCp598P06PermissionFixtureSliceDescriptor,
  createClientPortalCoreCp599P06CloseoutP07FoundationCaseSet,
  createClientPortalCoreCp599P06CloseoutP07FoundationDescriptor,
  createClientPortalCoreCp600P07ImplementationSliceCaseSet,
  createClientPortalCoreCp600P07ImplementationSliceDescriptor,
  createClientPortalCoreCp601P07WorkflowPermissionSliceCaseSet,
  createClientPortalCoreCp601P07WorkflowPermissionSliceDescriptor,
  createClientPortalCoreCp602P07PermissionFixtureSliceCaseSet,
  createClientPortalCoreCp602P07PermissionFixtureSliceDescriptor,
  createClientPortalCoreCp603P07CloseoutP08FoundationCaseSet,
  createClientPortalCoreCp603P07CloseoutP08FoundationDescriptor,
  createClientPortalCoreCp604P08WorkflowPermissionSliceCaseSet,
  createClientPortalCoreCp604P08WorkflowPermissionSliceDescriptor,
  createClientPortalCoreCp605P08PermissionFixtureSliceCaseSet,
  createClientPortalCoreCp605P08PermissionFixtureSliceDescriptor,
  createClientPortalCoreCp606P08FixtureSliceCaseSet,
  createClientPortalCoreCp606P08FixtureSliceDescriptor,
  createClientPortalCoreCp607P08CloseoutP09FoundationCaseSet,
  createClientPortalCoreCp607P08CloseoutP09FoundationDescriptor,
  createClientPortalCoreCp608P09PermissionAuditFixtureBridgeCaseSet,
  createClientPortalCoreCp608P09PermissionAuditFixtureBridgeDescriptor,
  createClientPortalCoreCp609P09ReviewEvidenceCloseoutBridgeCaseSet,
  createClientPortalCoreCp609P09ReviewEvidenceCloseoutBridgeDescriptor,
  clientPortalCoreRowKey,
} from "./service.js";

function countBy(units, field) {
  const counts = {};
  for (const unit of units) {
    const key = unit?.[field];
    if (key === undefined) continue;
    counts[key] = (counts[key] ?? 0) + 1;
  }
  return Object.freeze(counts);
}

function freezeCp583Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP583_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP583_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp583CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp583Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp583Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-583 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP583_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-583");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP583_PACK_BINDING.risk_class) errors.push("CP00-583 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP583_PACK_BINDING.unit_count) errors.push("CP00-583 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP583_PACK_BINDING.first_unit_id) errors.push("CP00-583 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP583_PACK_BINDING.last_unit_id) errors.push("CP00-583 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-583 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-583 must only include RP19 units");
  const summary = createClientPortalCoreCp583CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP583_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-583 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP583_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-583 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP583_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-583 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP583_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-583 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP583_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-583 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-583 ${microId} missing row ${title}`);
    }
  }
  return freezeCp583Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp583ScopeContractFoundationDescriptor(
  descriptor = createClientPortalCoreCp583ScopeContractFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.scope_contract_foundation_case_set ?? createClientPortalCoreCp583ScopeContractFoundationCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp583ScopeContractFoundationDescriptor") errors.push("CP00-583 descriptor type drift");
  if (descriptor.program_contract?.program_id !== "RP19") errors.push("CP00-583 program id drift");
  if (descriptor.program_contract?.upstream_program_id !== "RP18") errors.push("CP00-583 upstream program drift");
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP583_REQUIREMENTS.required_section_rows).length) errors.push("CP00-583 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP583_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-583 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-583 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-583 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-583 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-583 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-583 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-583 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(CLIENT_PORTAL_CORE_CP583_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.non_goal_boundary && rows.non_goal_boundary.client_portal_runtime_opened !== false) {
      errors.push(`CP00-583 ${microId} must not open client portal runtime`);
    }
    if (rows.permission_baseline_note && rows.permission_baseline_note.permission_decision_detail_included !== false) {
      errors.push(`CP00-583 ${microId} permission baseline must not expose decisions`);
    }
    if (rows.permission_baseline_note && rows.permission_baseline_note.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-583 ${microId} permission baseline must deny cross-tenant access`);
    }
    if (rows.audit_baseline_note && rows.audit_baseline_note.audit_event_body_included !== false) {
      errors.push(`CP00-583 ${microId} audit baseline must not expose event bodies`);
    }
    if (rows.synthetic_data_policy && rows.synthetic_data_policy.real_client_data_loaded !== false) {
      errors.push(`CP00-583 ${microId} synthetic data policy drift`);
    }
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-583 ${microId} tenant scope must deny cross-tenant access`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-583 ${microId} matter trace must be required`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-583 ${microId} state transition map must not write state`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-583 ${microId} fixture model must not load real data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-583 ${microId} must not execute test runtime`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-583 ${microId} must not emit Hermes receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-583 ${microId} must not claim Claude final approval`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP583_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-583 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP583_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-583 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-583 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-583 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP583_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP584_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP585_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP586_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP587_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP588_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP589_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP590_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP591_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP592_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-583 contract current_pack drift");
  }
  if (
    contractProjection?.scope_contract_foundation_descriptor?.descriptor &&
    contractProjection.scope_contract_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-583 contract scope_contract_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP583_PACK_BINDING.next_pack_id) errors.push("CP00-583 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP583_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-583 next subphase drift");
  }
  return freezeCp583Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp583HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp583ScopeContractFoundationDescriptor(),
) {
  const coverage = validateClientPortalCoreCp583Coverage(planPack);
  const foundation = validateClientPortalCoreCp583ScopeContractFoundationDescriptor(descriptor, contractProjection);
  return freezeCp583Validation({
    evidence_packet: "H19.CP00-583.client_portal_core_scope_contract_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP583_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    scope_contract_foundation_valid: foundation.valid,
    no_real_data: true,
    source_ai_legal_workflows_core_pack_id: CLIENT_PORTAL_CORE_CP583_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP583_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP583_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP583_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createClientPortalCoreCp583ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp583Coverage(planPack);
  const foundation = validateClientPortalCoreCp583ScopeContractFoundationDescriptor(createClientPortalCoreCp583ScopeContractFoundationDescriptor(), {});
  return freezeCp583Validation({
    review_packet: "C19.CP00-583.client_portal_core_scope_contract_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP583_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP583_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    scope_contract_foundation_valid: foundation.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP583_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-583 cover exactly the 150 planned RP19 units from RP19.P00.M00.S01 through RP19.P01.M05.S20?",
      "Do the twenty-one descriptor sections (P00 M00-M10 scope foundation cycle and P01 M00-M08 model foundation cycle with package directory layout, primary entity identifier, tenant scope field, matter trace reference, lifecycle status enum, ownership metadata, reference relationship map, field registries, state transition map, validation helper, fixture model, serialization shape, public export, model tests, Hermes model summary, Claude model review prompt, and closeout handoff) cover every planned row title as descriptor-only rows?",
      "Does CP00-583 avoid client-portal/secure-link/client-review/watermark runtime, runtime permission evaluation, audit event writes, state writes, object storage, command runtime execution, Hermes runtime receipts, real data, permission decision leaks, audit event body leaks, fixture payload leaks, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp583CloseoutHandoff() {
  return freezeCp583Validation({
    handoff_id: "CP00-583-to-CP00-584",
    from_pack_id: CLIENT_PORTAL_CORE_CP583_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP583_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP583_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP583_PACK_BINDING.range,
    open_scope: "Continue RP19.P01.M06.S01 onward with the synthetic fixture rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP583_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp584Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP584_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP584_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp584CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp584Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp584Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-584 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP584_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-584");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP584_PACK_BINDING.risk_class) errors.push("CP00-584 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP584_PACK_BINDING.unit_count) errors.push("CP00-584 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP584_PACK_BINDING.first_unit_id) errors.push("CP00-584 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP584_PACK_BINDING.last_unit_id) errors.push("CP00-584 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-584 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-584 must only include RP19 units");
  const summary = createClientPortalCoreCp584CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP584_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-584 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP584_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-584 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP584_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-584 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP584_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-584 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP584_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-584 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-584 ${microId} missing row ${title}`);
    }
  }
  return freezeCp584Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp584P01CloseoutP02FoundationDescriptor(
  descriptor = createClientPortalCoreCp584P01CloseoutP02FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p01_closeout_p02_foundation_case_set ?? createClientPortalCoreCp584P01CloseoutP02FoundationCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp584P01CloseoutP02FoundationDescriptor") errors.push("CP00-584 descriptor type drift");
  if (descriptor.source_scope_contract_foundation_descriptor !== "ClientPortalCoreCp583ScopeContractFoundationDescriptor") {
    errors.push("CP00-584 source scope contract foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP584_REQUIREMENTS.required_section_rows).length) errors.push("CP00-584 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP584_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-584 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-584 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-584 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-584 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-584 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-584 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-584 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(CLIENT_PORTAL_CORE_CP584_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-584 ${microId} must not allow cross-tenant access`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-584 ${microId} matter trace requirement drift`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-584 ${microId} must not write state transitions`);
    }
    if (rows.validation_helper && rows.validation_helper.validation_error_detail_included !== false) {
      errors.push(`CP00-584 ${microId} must not expose validation error details`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-584 ${microId} must not load real fixture data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-584 ${microId} must not execute test runtime`);
    }
    if (rows.invalid_reference_test && rows.invalid_reference_test.expected_outcome !== "rejected_customer_safe") {
      errors.push(`CP00-584 ${microId} invalid reference outcome drift`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-584 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-584 ${microId} must not claim Claude final approval`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP584_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-584 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP584_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-584 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-584 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-584 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP584_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP585_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP586_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP587_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP588_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP589_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP590_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP591_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP592_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-584 contract current_pack drift");
  }
  if (
    contractProjection?.p01_closeout_p02_foundation_descriptor?.descriptor &&
    contractProjection.p01_closeout_p02_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-584 contract p01_closeout_p02_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP584_PACK_BINDING.next_pack_id) errors.push("CP00-584 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP584_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-584 next subphase drift");
  }
  return freezeCp584Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp584HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp584P01CloseoutP02FoundationDescriptor(),
) {
  const coverage = validateClientPortalCoreCp584Coverage(planPack);
  const slice = validateClientPortalCoreCp584P01CloseoutP02FoundationDescriptor(descriptor, contractProjection);
  return freezeCp584Validation({
    evidence_packet: "H19.CP00-584.client_portal_core_p01_closeout_p02_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP584_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p01_closeout_p02_foundation_valid: slice.valid,
    no_real_data: true,
    source_scope_contract_foundation_pack_id: CLIENT_PORTAL_CORE_CP584_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP584_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP584_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP584_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp584ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp584Coverage(planPack);
  const slice = validateClientPortalCoreCp584P01CloseoutP02FoundationDescriptor(createClientPortalCoreCp584P01CloseoutP02FoundationDescriptor(), {});
  return freezeCp584Validation({
    review_packet: "C19.CP00-584.client_portal_core_p01_closeout_p02_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP584_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP584_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p01_closeout_p02_foundation_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP584_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-584 cover exactly the 150 planned RP19 units from RP19.P01.M06.S01 through RP19.P02.M04.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-584 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp584CloseoutHandoff() {
  return freezeCp584Validation({
    handoff_id: "CP00-584-to-CP00-585",
    from_pack_id: CLIENT_PORTAL_CORE_CP584_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP584_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP584_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP584_PACK_BINDING.range,
    open_scope: "Continue RP19.P02.M04.S07 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP584_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp585Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP585_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP585_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp585CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp585Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp585Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-585 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP585_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-585");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP585_PACK_BINDING.risk_class) errors.push("CP00-585 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP585_PACK_BINDING.unit_count) errors.push("CP00-585 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP585_PACK_BINDING.first_unit_id) errors.push("CP00-585 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP585_PACK_BINDING.last_unit_id) errors.push("CP00-585 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-585 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-585 must only include RP19 units");
  const summary = createClientPortalCoreCp585CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP585_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-585 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP585_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-585 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP585_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-585 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP585_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-585 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP585_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-585 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-585 ${microId} missing row ${title}`);
    }
  }
  return freezeCp585Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp585P02WorkflowPermissionSliceDescriptor(
  descriptor = createClientPortalCoreCp585P02WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_workflow_permission_slice_case_set ?? createClientPortalCoreCp585P02WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp585P02WorkflowPermissionSliceDescriptor") errors.push("CP00-585 descriptor type drift");
  if (descriptor.source_p01_closeout_p02_foundation_descriptor !== "ClientPortalCoreCp584P01CloseoutP02FoundationDescriptor") {
    errors.push("CP00-585 source p01 closeout p02 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP585_REQUIREMENTS.required_section_rows).length) errors.push("CP00-585 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP585_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-585 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-585 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-585 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-585 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-585 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-585 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-585 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP585_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-585 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP585_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-585 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-585 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-585 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP585_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP586_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP587_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP588_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP589_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP590_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP591_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP592_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-585 contract current_pack drift");
  }
  if (
    contractProjection?.p02_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p02_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-585 contract p02_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP585_PACK_BINDING.next_pack_id) errors.push("CP00-585 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP585_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-585 next subphase drift");
  }
  return freezeCp585Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp585HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp585P02WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp585Coverage(planPack);
  const slice = validateClientPortalCoreCp585P02WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp585Validation({
    evidence_packet: "H19.CP00-585.client_portal_core_p02_workflow_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP585_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p01_closeout_p02_foundation_pack_id: CLIENT_PORTAL_CORE_CP585_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP585_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP585_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP585_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp585ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp585Coverage(planPack);
  const slice = validateClientPortalCoreCp585P02WorkflowPermissionSliceDescriptor(createClientPortalCoreCp585P02WorkflowPermissionSliceDescriptor(), {});
  return freezeCp585Validation({
    review_packet: "C19.CP00-585.client_portal_core_p02_workflow_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP585_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP585_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP585_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-585 cover exactly the 40 planned RP19 units from RP19.P02.M04.S07 through RP19.P02.M06.S02?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-585 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp585CloseoutHandoff() {
  return freezeCp585Validation({
    handoff_id: "CP00-585-to-CP00-586",
    from_pack_id: CLIENT_PORTAL_CORE_CP585_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP585_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP585_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP585_PACK_BINDING.range,
    open_scope: "Continue RP19.P02.M06.S03 onward with the remaining synthetic fixture rows while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP585_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp586Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP586_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP586_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp586CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp586Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp586Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-586 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP586_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-586");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP586_PACK_BINDING.risk_class) errors.push("CP00-586 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP586_PACK_BINDING.unit_count) errors.push("CP00-586 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP586_PACK_BINDING.first_unit_id) errors.push("CP00-586 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP586_PACK_BINDING.last_unit_id) errors.push("CP00-586 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-586 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-586 must only include RP19 units");
  const summary = createClientPortalCoreCp586CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP586_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-586 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP586_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-586 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP586_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-586 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP586_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-586 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP586_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-586 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-586 ${microId} missing row ${title}`);
    }
  }
  return freezeCp586Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp586P02FixtureSliceDescriptor(
  descriptor = createClientPortalCoreCp586P02FixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_fixture_slice_case_set ?? createClientPortalCoreCp586P02FixtureSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp586P02FixtureSliceDescriptor") errors.push("CP00-586 descriptor type drift");
  if (descriptor.source_p02_workflow_permission_slice_descriptor !== "ClientPortalCoreCp585P02WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-586 source p02 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP586_REQUIREMENTS.required_section_rows).length) errors.push("CP00-586 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP586_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-586 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-586 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-586 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-586 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-586 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-586 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-586 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP586_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-586 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP586_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-586 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-586 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-586 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP586_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP587_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP588_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP589_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP590_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP591_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP592_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-586 contract current_pack drift");
  }
  if (
    contractProjection?.p02_fixture_slice_descriptor?.descriptor &&
    contractProjection.p02_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-586 contract p02_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP586_PACK_BINDING.next_pack_id) errors.push("CP00-586 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP586_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-586 next subphase drift");
  }
  return freezeCp586Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp586HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp586P02FixtureSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp586Coverage(planPack);
  const slice = validateClientPortalCoreCp586P02FixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp586Validation({
    evidence_packet: "H19.CP00-586.client_portal_core_p02_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP586_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_workflow_permission_slice_pack_id: CLIENT_PORTAL_CORE_CP586_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP586_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP586_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP586_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp586ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp586Coverage(planPack);
  const slice = validateClientPortalCoreCp586P02FixtureSliceDescriptor(createClientPortalCoreCp586P02FixtureSliceDescriptor(), {});
  return freezeCp586Validation({
    review_packet: "C19.CP00-586.client_portal_core_p02_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP586_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP586_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_fixture_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP586_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-586 cover exactly the 10 planned RP19 units from RP19.P02.M06.S03 through RP19.P02.M06.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-586 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp586CloseoutHandoff() {
  return freezeCp586Validation({
    handoff_id: "CP00-586-to-CP00-587",
    from_pack_id: CLIENT_PORTAL_CORE_CP586_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP586_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP586_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP586_PACK_BINDING.range,
    open_scope: "Continue RP19.P02.M06.S13 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP586_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp587Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP587_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP587_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp587CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp587Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp587Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-587 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP587_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-587");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP587_PACK_BINDING.risk_class) errors.push("CP00-587 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP587_PACK_BINDING.unit_count) errors.push("CP00-587 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP587_PACK_BINDING.first_unit_id) errors.push("CP00-587 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP587_PACK_BINDING.last_unit_id) errors.push("CP00-587 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-587 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-587 must only include RP19 units");
  const summary = createClientPortalCoreCp587CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP587_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-587 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP587_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-587 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP587_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-587 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP587_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-587 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP587_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-587 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-587 ${microId} missing row ${title}`);
    }
  }
  return freezeCp587Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp587P02CloseoutP03FoundationDescriptor(
  descriptor = createClientPortalCoreCp587P02CloseoutP03FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_closeout_p03_foundation_case_set ?? createClientPortalCoreCp587P02CloseoutP03FoundationCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp587P02CloseoutP03FoundationDescriptor") errors.push("CP00-587 descriptor type drift");
  if (descriptor.source_p02_fixture_slice_descriptor !== "ClientPortalCoreCp586P02FixtureSliceDescriptor") {
    errors.push("CP00-587 source p02 fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP587_REQUIREMENTS.required_section_rows).length) errors.push("CP00-587 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP587_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-587 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-587 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-587 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-587 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-587 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-587 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-587 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP587_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-587 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP587_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-587 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-587 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-587 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP587_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP588_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP589_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP590_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP591_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP592_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-587 contract current_pack drift");
  }
  if (
    contractProjection?.p02_closeout_p03_foundation_descriptor?.descriptor &&
    contractProjection.p02_closeout_p03_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-587 contract p02_closeout_p03_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP587_PACK_BINDING.next_pack_id) errors.push("CP00-587 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP587_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-587 next subphase drift");
  }
  return freezeCp587Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp587HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp587P02CloseoutP03FoundationDescriptor(),
) {
  const coverage = validateClientPortalCoreCp587Coverage(planPack);
  const slice = validateClientPortalCoreCp587P02CloseoutP03FoundationDescriptor(descriptor, contractProjection);
  return freezeCp587Validation({
    evidence_packet: "H19.CP00-587.client_portal_core_p02_closeout_p03_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP587_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_closeout_p03_foundation_valid: slice.valid,
    no_real_data: true,
    source_p02_fixture_slice_pack_id: CLIENT_PORTAL_CORE_CP587_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP587_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP587_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP587_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp587ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp587Coverage(planPack);
  const slice = validateClientPortalCoreCp587P02CloseoutP03FoundationDescriptor(createClientPortalCoreCp587P02CloseoutP03FoundationDescriptor(), {});
  return freezeCp587Validation({
    review_packet: "C19.CP00-587.client_portal_core_p02_closeout_p03_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP587_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP587_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_closeout_p03_foundation_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP587_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-587 cover exactly the 150 planned RP19 units from RP19.P02.M06.S13 through RP19.P03.M05.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-587 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp587CloseoutHandoff() {
  return freezeCp587Validation({
    handoff_id: "CP00-587-to-CP00-588",
    from_pack_id: CLIENT_PORTAL_CORE_CP587_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP587_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP587_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP587_PACK_BINDING.range,
    open_scope: "Continue RP19.P03.M05.S07 onward with the remaining permission and audit binding rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP587_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp588Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP588_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP588_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp588CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp588Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp588Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-588 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP588_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-588");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP588_PACK_BINDING.risk_class) errors.push("CP00-588 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP588_PACK_BINDING.unit_count) errors.push("CP00-588 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP588_PACK_BINDING.first_unit_id) errors.push("CP00-588 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP588_PACK_BINDING.last_unit_id) errors.push("CP00-588 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-588 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-588 must only include RP19 units");
  const summary = createClientPortalCoreCp588CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP588_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-588 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP588_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-588 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP588_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-588 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP588_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-588 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP588_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-588 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-588 ${microId} missing row ${title}`);
    }
  }
  return freezeCp588Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp588P03PermissionSliceDescriptor(
  descriptor = createClientPortalCoreCp588P03PermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_permission_slice_case_set ?? createClientPortalCoreCp588P03PermissionSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp588P03PermissionSliceDescriptor") errors.push("CP00-588 descriptor type drift");
  if (descriptor.source_p02_closeout_p03_foundation_descriptor !== "ClientPortalCoreCp587P02CloseoutP03FoundationDescriptor") {
    errors.push("CP00-588 source p02 closeout p03 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP588_REQUIREMENTS.required_section_rows).length) errors.push("CP00-588 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP588_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-588 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-588 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-588 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-588 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-588 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-588 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-588 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP588_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-588 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP588_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-588 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-588 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-588 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP588_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP589_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP590_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP591_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP592_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-588 contract current_pack drift");
  }
  if (
    contractProjection?.p03_permission_slice_descriptor?.descriptor &&
    contractProjection.p03_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-588 contract p03_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP588_PACK_BINDING.next_pack_id) errors.push("CP00-588 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP588_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-588 next subphase drift");
  }
  return freezeCp588Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp588HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp588P03PermissionSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp588Coverage(planPack);
  const slice = validateClientPortalCoreCp588P03PermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp588Validation({
    evidence_packet: "H19.CP00-588.client_portal_core_p03_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP588_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_closeout_p03_foundation_pack_id: CLIENT_PORTAL_CORE_CP588_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP588_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP588_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP588_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp588ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp588Coverage(planPack);
  const slice = validateClientPortalCoreCp588P03PermissionSliceDescriptor(createClientPortalCoreCp588P03PermissionSliceDescriptor(), {});
  return freezeCp588Validation({
    review_packet: "C19.CP00-588.client_portal_core_p03_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP588_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP588_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_permission_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP588_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-588 cover exactly the 10 planned RP19 units from RP19.P03.M05.S07 through RP19.P03.M05.S16?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-588 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp588CloseoutHandoff() {
  return freezeCp588Validation({
    handoff_id: "CP00-588-to-CP00-589",
    from_pack_id: CLIENT_PORTAL_CORE_CP588_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP588_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP588_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP588_PACK_BINDING.range,
    open_scope: "Continue RP19.P03.M05.S17 onward with the remaining permission and audit binding rows and the synthetic fixture micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP588_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp589Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP589_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP589_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp589CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp589Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp589Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-589 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP589_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-589");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP589_PACK_BINDING.risk_class) errors.push("CP00-589 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP589_PACK_BINDING.unit_count) errors.push("CP00-589 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP589_PACK_BINDING.first_unit_id) errors.push("CP00-589 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP589_PACK_BINDING.last_unit_id) errors.push("CP00-589 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-589 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-589 must only include RP19 units");
  const summary = createClientPortalCoreCp589CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP589_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-589 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP589_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-589 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP589_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-589 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP589_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-589 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP589_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-589 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-589 ${microId} missing row ${title}`);
    }
  }
  return freezeCp589Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp589P03PermissionFixtureSliceDescriptor(
  descriptor = createClientPortalCoreCp589P03PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_permission_fixture_slice_case_set ?? createClientPortalCoreCp589P03PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp589P03PermissionFixtureSliceDescriptor") errors.push("CP00-589 descriptor type drift");
  if (descriptor.source_p03_permission_slice_descriptor !== "ClientPortalCoreCp588P03PermissionSliceDescriptor") {
    errors.push("CP00-589 source p03 permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP589_REQUIREMENTS.required_section_rows).length) errors.push("CP00-589 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP589_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-589 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-589 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-589 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-589 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-589 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-589 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-589 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP589_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-589 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP589_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-589 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-589 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-589 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP589_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP590_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP591_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP592_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-589 contract current_pack drift");
  }
  if (
    contractProjection?.p03_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p03_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-589 contract p03_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP589_PACK_BINDING.next_pack_id) errors.push("CP00-589 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP589_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-589 next subphase drift");
  }
  return freezeCp589Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp589HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp589P03PermissionFixtureSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp589Coverage(planPack);
  const slice = validateClientPortalCoreCp589P03PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp589Validation({
    evidence_packet: "H19.CP00-589.client_portal_core_p03_permission_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP589_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p03_permission_slice_pack_id: CLIENT_PORTAL_CORE_CP589_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP589_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP589_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP589_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp589ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp589Coverage(planPack);
  const slice = validateClientPortalCoreCp589P03PermissionFixtureSliceDescriptor(createClientPortalCoreCp589P03PermissionFixtureSliceDescriptor(), {});
  return freezeCp589Validation({
    review_packet: "C19.CP00-589.client_portal_core_p03_permission_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP589_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP589_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP589_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-589 cover exactly the 10 planned RP19 units from RP19.P03.M05.S17 through RP19.P03.M06.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-589 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp589CloseoutHandoff() {
  return freezeCp589Validation({
    handoff_id: "CP00-589-to-CP00-590",
    from_pack_id: CLIENT_PORTAL_CORE_CP589_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP589_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP589_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP589_PACK_BINDING.range,
    open_scope: "Continue RP19.P03.M06.S07 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP589_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp590Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP590_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP590_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp590CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp590Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp590Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-590 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP590_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-590");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP590_PACK_BINDING.risk_class) errors.push("CP00-590 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP590_PACK_BINDING.unit_count) errors.push("CP00-590 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP590_PACK_BINDING.first_unit_id) errors.push("CP00-590 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP590_PACK_BINDING.last_unit_id) errors.push("CP00-590 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-590 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-590 must only include RP19 units");
  const summary = createClientPortalCoreCp590CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP590_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-590 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP590_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-590 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP590_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-590 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP590_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-590 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP590_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-590 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-590 ${microId} missing row ${title}`);
    }
  }
  return freezeCp590Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp590P03CloseoutP04FoundationDescriptor(
  descriptor = createClientPortalCoreCp590P03CloseoutP04FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_closeout_p04_foundation_case_set ?? createClientPortalCoreCp590P03CloseoutP04FoundationCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp590P03CloseoutP04FoundationDescriptor") errors.push("CP00-590 descriptor type drift");
  if (descriptor.source_p03_permission_fixture_slice_descriptor !== "ClientPortalCoreCp589P03PermissionFixtureSliceDescriptor") {
    errors.push("CP00-590 source p03 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP590_REQUIREMENTS.required_section_rows).length) errors.push("CP00-590 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP590_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-590 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-590 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-590 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-590 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-590 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-590 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-590 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP590_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-590 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP590_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-590 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-590 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-590 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP590_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP591_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP592_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-590 contract current_pack drift");
  }
  if (
    contractProjection?.p03_closeout_p04_foundation_descriptor?.descriptor &&
    contractProjection.p03_closeout_p04_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-590 contract p03_closeout_p04_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP590_PACK_BINDING.next_pack_id) errors.push("CP00-590 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP590_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-590 next subphase drift");
  }
  return freezeCp590Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp590HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp590P03CloseoutP04FoundationDescriptor(),
) {
  const coverage = validateClientPortalCoreCp590Coverage(planPack);
  const slice = validateClientPortalCoreCp590P03CloseoutP04FoundationDescriptor(descriptor, contractProjection);
  return freezeCp590Validation({
    evidence_packet: "H19.CP00-590.client_portal_core_p03_closeout_p04_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP590_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_closeout_p04_foundation_valid: slice.valid,
    no_real_data: true,
    source_p03_permission_fixture_slice_pack_id: CLIENT_PORTAL_CORE_CP590_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP590_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP590_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP590_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp590ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp590Coverage(planPack);
  const slice = validateClientPortalCoreCp590P03CloseoutP04FoundationDescriptor(createClientPortalCoreCp590P03CloseoutP04FoundationDescriptor(), {});
  return freezeCp590Validation({
    review_packet: "C19.CP00-590.client_portal_core_p03_closeout_p04_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP590_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP590_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_closeout_p04_foundation_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP590_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-590 cover exactly the 150 planned RP19 units from RP19.P03.M06.S07 through RP19.P04.M05.S07?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-590 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp590CloseoutHandoff() {
  return freezeCp590Validation({
    handoff_id: "CP00-590-to-CP00-591",
    from_pack_id: CLIENT_PORTAL_CORE_CP590_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP590_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP590_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP590_PACK_BINDING.range,
    open_scope: "Continue RP19.P04.M05.S08 onward with the remaining permission and audit binding rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP590_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp591Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP591_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP591_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp591CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp591Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp591Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-591 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP591_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-591");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP591_PACK_BINDING.risk_class) errors.push("CP00-591 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP591_PACK_BINDING.unit_count) errors.push("CP00-591 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP591_PACK_BINDING.first_unit_id) errors.push("CP00-591 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP591_PACK_BINDING.last_unit_id) errors.push("CP00-591 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-591 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-591 must only include RP19 units");
  const summary = createClientPortalCoreCp591CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP591_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-591 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP591_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-591 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP591_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-591 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP591_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-591 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP591_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-591 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-591 ${microId} missing row ${title}`);
    }
  }
  return freezeCp591Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp591P04PermissionSliceDescriptor(
  descriptor = createClientPortalCoreCp591P04PermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_permission_slice_case_set ?? createClientPortalCoreCp591P04PermissionSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp591P04PermissionSliceDescriptor") errors.push("CP00-591 descriptor type drift");
  if (descriptor.source_p03_closeout_p04_foundation_descriptor !== "ClientPortalCoreCp590P03CloseoutP04FoundationDescriptor") {
    errors.push("CP00-591 source p03 closeout p04 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP591_REQUIREMENTS.required_section_rows).length) errors.push("CP00-591 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP591_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-591 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-591 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-591 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-591 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-591 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-591 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-591 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP591_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-591 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP591_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-591 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-591 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-591 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP591_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP592_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-591 contract current_pack drift");
  }
  if (
    contractProjection?.p04_permission_slice_descriptor?.descriptor &&
    contractProjection.p04_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-591 contract p04_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP591_PACK_BINDING.next_pack_id) errors.push("CP00-591 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP591_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-591 next subphase drift");
  }
  return freezeCp591Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp591HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp591P04PermissionSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp591Coverage(planPack);
  const slice = validateClientPortalCoreCp591P04PermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp591Validation({
    evidence_packet: "H19.CP00-591.client_portal_core_p04_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP591_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p03_closeout_p04_foundation_pack_id: CLIENT_PORTAL_CORE_CP591_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP591_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP591_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP591_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp591ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp591Coverage(planPack);
  const slice = validateClientPortalCoreCp591P04PermissionSliceDescriptor(createClientPortalCoreCp591P04PermissionSliceDescriptor(), {});
  return freezeCp591Validation({
    review_packet: "C19.CP00-591.client_portal_core_p04_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP591_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP591_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_permission_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP591_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-591 cover exactly the 10 planned RP19 units from RP19.P04.M05.S08 through RP19.P04.M05.S17?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-591 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp591CloseoutHandoff() {
  return freezeCp591Validation({
    handoff_id: "CP00-591-to-CP00-592",
    from_pack_id: CLIENT_PORTAL_CORE_CP591_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP591_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP591_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP591_PACK_BINDING.range,
    open_scope: "Continue RP19.P04.M05.S18 onward with the remaining permission and audit binding rows and the synthetic fixture micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP591_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp592Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP592_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP592_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp592CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp592Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp592Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-592 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP592_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-592");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP592_PACK_BINDING.risk_class) errors.push("CP00-592 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP592_PACK_BINDING.unit_count) errors.push("CP00-592 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP592_PACK_BINDING.first_unit_id) errors.push("CP00-592 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP592_PACK_BINDING.last_unit_id) errors.push("CP00-592 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-592 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-592 must only include RP19 units");
  const summary = createClientPortalCoreCp592CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP592_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-592 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP592_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-592 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP592_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-592 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP592_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-592 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP592_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-592 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-592 ${microId} missing row ${title}`);
    }
  }
  return freezeCp592Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp592P04PermissionFixtureSliceDescriptor(
  descriptor = createClientPortalCoreCp592P04PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_permission_fixture_slice_case_set ?? createClientPortalCoreCp592P04PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp592P04PermissionFixtureSliceDescriptor") errors.push("CP00-592 descriptor type drift");
  if (descriptor.source_p04_permission_slice_descriptor !== "ClientPortalCoreCp591P04PermissionSliceDescriptor") {
    errors.push("CP00-592 source p04 permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP592_REQUIREMENTS.required_section_rows).length) errors.push("CP00-592 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP592_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-592 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-592 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-592 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-592 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-592 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-592 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-592 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP592_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-592 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP592_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-592 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-592 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-592 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP592_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-592 contract current_pack drift");
  }
  if (
    contractProjection?.p04_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p04_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-592 contract p04_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP592_PACK_BINDING.next_pack_id) errors.push("CP00-592 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP592_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-592 next subphase drift");
  }
  return freezeCp592Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp592HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp592P04PermissionFixtureSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp592Coverage(planPack);
  const slice = validateClientPortalCoreCp592P04PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp592Validation({
    evidence_packet: "H19.CP00-592.client_portal_core_p04_permission_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP592_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p04_permission_slice_pack_id: CLIENT_PORTAL_CORE_CP592_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP592_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP592_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP592_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp592ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp592Coverage(planPack);
  const slice = validateClientPortalCoreCp592P04PermissionFixtureSliceDescriptor(createClientPortalCoreCp592P04PermissionFixtureSliceDescriptor(), {});
  return freezeCp592Validation({
    review_packet: "C19.CP00-592.client_portal_core_p04_permission_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP592_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP592_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP592_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-592 cover exactly the 10 planned RP19 units from RP19.P04.M05.S18 through RP19.P04.M06.S05?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-592 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp592CloseoutHandoff() {
  return freezeCp592Validation({
    handoff_id: "CP00-592-to-CP00-593",
    from_pack_id: CLIENT_PORTAL_CORE_CP592_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP592_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP592_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP592_PACK_BINDING.range,
    open_scope: "Continue RP19.P04.M06.S06 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP592_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp593Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP593_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp593CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp593Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp593Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-593 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-593");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP593_PACK_BINDING.risk_class) errors.push("CP00-593 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP593_PACK_BINDING.unit_count) errors.push("CP00-593 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP593_PACK_BINDING.first_unit_id) errors.push("CP00-593 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP593_PACK_BINDING.last_unit_id) errors.push("CP00-593 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-593 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-593 must only include RP19 units");
  const summary = createClientPortalCoreCp593CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP593_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-593 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP593_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-593 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP593_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-593 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP593_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-593 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP593_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-593 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-593 ${microId} missing row ${title}`);
    }
  }
  return freezeCp593Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp593P04CloseoutP05FoundationDescriptor(
  descriptor = createClientPortalCoreCp593P04CloseoutP05FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_closeout_p05_foundation_case_set ?? createClientPortalCoreCp593P04CloseoutP05FoundationCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp593P04CloseoutP05FoundationDescriptor") errors.push("CP00-593 descriptor type drift");
  if (descriptor.source_p04_permission_fixture_slice_descriptor !== "ClientPortalCoreCp592P04PermissionFixtureSliceDescriptor") {
    errors.push("CP00-593 source p04 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP593_REQUIREMENTS.required_section_rows).length) errors.push("CP00-593 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP593_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-593 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-593 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-593 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-593 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-593 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-593 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-593 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP593_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-593 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP593_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-593 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-593 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-593 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-593 contract current_pack drift");
  }
  if (
    contractProjection?.p04_closeout_p05_foundation_descriptor?.descriptor &&
    contractProjection.p04_closeout_p05_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-593 contract p04_closeout_p05_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP593_PACK_BINDING.next_pack_id) errors.push("CP00-593 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP593_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-593 next subphase drift");
  }
  return freezeCp593Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp593HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp593P04CloseoutP05FoundationDescriptor(),
) {
  const coverage = validateClientPortalCoreCp593Coverage(planPack);
  const slice = validateClientPortalCoreCp593P04CloseoutP05FoundationDescriptor(descriptor, contractProjection);
  return freezeCp593Validation({
    evidence_packet: "H19.CP00-593.client_portal_core_p04_closeout_p05_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP593_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_closeout_p05_foundation_valid: slice.valid,
    no_real_data: true,
    source_p04_permission_fixture_slice_pack_id: CLIENT_PORTAL_CORE_CP593_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP593_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP593_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP593_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp593ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp593Coverage(planPack);
  const slice = validateClientPortalCoreCp593P04CloseoutP05FoundationDescriptor(createClientPortalCoreCp593P04CloseoutP05FoundationDescriptor(), {});
  return freezeCp593Validation({
    review_packet: "C19.CP00-593.client_portal_core_p04_closeout_p05_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP593_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP593_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_closeout_p05_foundation_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP593_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-593 cover exactly the 150 planned RP19 units from RP19.P04.M06.S06 through RP19.P05.M04.S07?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-593 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp593CloseoutHandoff() {
  return freezeCp593Validation({
    handoff_id: "CP00-593-to-CP00-594",
    from_pack_id: CLIENT_PORTAL_CORE_CP593_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP593_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP593_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP593_PACK_BINDING.range,
    open_scope: "Continue RP19.P05.M04.S08 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP593_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp594Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP594_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp594CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp594Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp594Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-594 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-594");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP594_PACK_BINDING.risk_class) errors.push("CP00-594 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP594_PACK_BINDING.unit_count) errors.push("CP00-594 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP594_PACK_BINDING.first_unit_id) errors.push("CP00-594 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP594_PACK_BINDING.last_unit_id) errors.push("CP00-594 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-594 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-594 must only include RP19 units");
  const summary = createClientPortalCoreCp594CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP594_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-594 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP594_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-594 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP594_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-594 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP594_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-594 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP594_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-594 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-594 ${microId} missing row ${title}`);
    }
  }
  return freezeCp594Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp594P05WorkflowPermissionSliceDescriptor(
  descriptor = createClientPortalCoreCp594P05WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_workflow_permission_slice_case_set ?? createClientPortalCoreCp594P05WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp594P05WorkflowPermissionSliceDescriptor") errors.push("CP00-594 descriptor type drift");
  if (descriptor.source_p04_closeout_p05_foundation_descriptor !== "ClientPortalCoreCp593P04CloseoutP05FoundationDescriptor") {
    errors.push("CP00-594 source p04 closeout p05 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP594_REQUIREMENTS.required_section_rows).length) errors.push("CP00-594 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP594_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-594 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-594 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-594 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-594 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-594 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-594 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-594 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP594_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-594 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP594_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-594 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-594 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-594 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-594 contract current_pack drift");
  }
  if (
    contractProjection?.p05_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p05_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-594 contract p05_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP594_PACK_BINDING.next_pack_id) errors.push("CP00-594 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP594_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-594 next subphase drift");
  }
  return freezeCp594Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp594HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp594P05WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp594Coverage(planPack);
  const slice = validateClientPortalCoreCp594P05WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp594Validation({
    evidence_packet: "H19.CP00-594.client_portal_core_p05_workflow_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP594_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p04_closeout_p05_foundation_pack_id: CLIENT_PORTAL_CORE_CP594_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP594_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP594_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP594_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp594ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp594Coverage(planPack);
  const slice = validateClientPortalCoreCp594P05WorkflowPermissionSliceDescriptor(createClientPortalCoreCp594P05WorkflowPermissionSliceDescriptor(), {});
  return freezeCp594Validation({
    review_packet: "C19.CP00-594.client_portal_core_p05_workflow_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP594_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP594_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP594_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-594 cover exactly the 40 planned RP19 units from RP19.P05.M04.S08 through RP19.P05.M06.S05?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-594 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp594CloseoutHandoff() {
  return freezeCp594Validation({
    handoff_id: "CP00-594-to-CP00-595",
    from_pack_id: CLIENT_PORTAL_CORE_CP594_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP594_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP594_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP594_PACK_BINDING.range,
    open_scope: "Continue RP19.P05.M06.S06 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP594_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp595Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP595_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp595CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp595Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp595Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-595 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-595");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP595_PACK_BINDING.risk_class) errors.push("CP00-595 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP595_PACK_BINDING.unit_count) errors.push("CP00-595 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP595_PACK_BINDING.first_unit_id) errors.push("CP00-595 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP595_PACK_BINDING.last_unit_id) errors.push("CP00-595 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-595 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-595 must only include RP19 units");
  const summary = createClientPortalCoreCp595CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP595_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-595 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP595_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-595 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP595_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-595 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP595_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-595 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP595_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-595 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-595 ${microId} missing row ${title}`);
    }
  }
  return freezeCp595Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp595P05CloseoutP06FoundationDescriptor(
  descriptor = createClientPortalCoreCp595P05CloseoutP06FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_closeout_p06_foundation_case_set ?? createClientPortalCoreCp595P05CloseoutP06FoundationCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp595P05CloseoutP06FoundationDescriptor") errors.push("CP00-595 descriptor type drift");
  if (descriptor.source_p05_workflow_permission_slice_descriptor !== "ClientPortalCoreCp594P05WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-595 source p05 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP595_REQUIREMENTS.required_section_rows).length) errors.push("CP00-595 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP595_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-595 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-595 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-595 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-595 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-595 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-595 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-595 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP595_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-595 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP595_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-595 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-595 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-595 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-595 contract current_pack drift");
  }
  if (
    contractProjection?.p05_closeout_p06_foundation_descriptor?.descriptor &&
    contractProjection.p05_closeout_p06_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-595 contract p05_closeout_p06_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP595_PACK_BINDING.next_pack_id) errors.push("CP00-595 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP595_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-595 next subphase drift");
  }
  return freezeCp595Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp595HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp595P05CloseoutP06FoundationDescriptor(),
) {
  const coverage = validateClientPortalCoreCp595Coverage(planPack);
  const slice = validateClientPortalCoreCp595P05CloseoutP06FoundationDescriptor(descriptor, contractProjection);
  return freezeCp595Validation({
    evidence_packet: "H19.CP00-595.client_portal_core_p05_closeout_p06_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP595_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_closeout_p06_foundation_valid: slice.valid,
    no_real_data: true,
    source_p05_workflow_permission_slice_pack_id: CLIENT_PORTAL_CORE_CP595_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP595_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP595_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP595_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp595ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp595Coverage(planPack);
  const slice = validateClientPortalCoreCp595P05CloseoutP06FoundationDescriptor(createClientPortalCoreCp595P05CloseoutP06FoundationDescriptor(), {});
  return freezeCp595Validation({
    review_packet: "C19.CP00-595.client_portal_core_p05_closeout_p06_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP595_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP595_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_closeout_p06_foundation_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP595_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-595 cover exactly the 150 planned RP19 units from RP19.P05.M06.S06 through RP19.P06.M03.S14?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-595 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp595CloseoutHandoff() {
  return freezeCp595Validation({
    handoff_id: "CP00-595-to-CP00-596",
    from_pack_id: CLIENT_PORTAL_CORE_CP595_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP595_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP595_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP595_PACK_BINDING.range,
    open_scope: "Continue RP19.P06.M03.S15 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP595_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp596Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP596_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp596CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp596Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp596Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-596 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-596");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP596_PACK_BINDING.risk_class) errors.push("CP00-596 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP596_PACK_BINDING.unit_count) errors.push("CP00-596 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP596_PACK_BINDING.first_unit_id) errors.push("CP00-596 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP596_PACK_BINDING.last_unit_id) errors.push("CP00-596 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-596 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-596 must only include RP19 units");
  const summary = createClientPortalCoreCp596CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP596_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-596 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP596_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-596 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP596_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-596 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP596_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-596 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP596_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-596 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-596 ${microId} missing row ${title}`);
    }
  }
  return freezeCp596Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp596P06ImplementationSliceDescriptor(
  descriptor = createClientPortalCoreCp596P06ImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_implementation_slice_case_set ?? createClientPortalCoreCp596P06ImplementationSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp596P06ImplementationSliceDescriptor") errors.push("CP00-596 descriptor type drift");
  if (descriptor.source_p05_closeout_p06_foundation_descriptor !== "ClientPortalCoreCp595P05CloseoutP06FoundationDescriptor") {
    errors.push("CP00-596 source p05 closeout p06 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP596_REQUIREMENTS.required_section_rows).length) errors.push("CP00-596 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP596_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-596 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-596 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-596 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-596 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-596 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-596 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-596 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP596_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-596 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP596_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-596 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-596 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-596 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-596 contract current_pack drift");
  }
  if (
    contractProjection?.p06_implementation_slice_descriptor?.descriptor &&
    contractProjection.p06_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-596 contract p06_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP596_PACK_BINDING.next_pack_id) errors.push("CP00-596 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP596_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-596 next subphase drift");
  }
  return freezeCp596Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp596HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp596P06ImplementationSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp596Coverage(planPack);
  const slice = validateClientPortalCoreCp596P06ImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp596Validation({
    evidence_packet: "H19.CP00-596.client_portal_core_p06_implementation_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP596_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_closeout_p06_foundation_pack_id: CLIENT_PORTAL_CORE_CP596_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP596_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP596_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP596_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp596ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp596Coverage(planPack);
  const slice = validateClientPortalCoreCp596P06ImplementationSliceDescriptor(createClientPortalCoreCp596P06ImplementationSliceDescriptor(), {});
  return freezeCp596Validation({
    review_packet: "C19.CP00-596.client_portal_core_p06_implementation_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP596_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP596_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_implementation_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP596_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-596 cover exactly the 10 planned RP19 units from RP19.P06.M03.S15 through RP19.P06.M04.S02?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-596 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp596CloseoutHandoff() {
  return freezeCp596Validation({
    handoff_id: "CP00-596-to-CP00-597",
    from_pack_id: CLIENT_PORTAL_CORE_CP596_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP596_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP596_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP596_PACK_BINDING.range,
    open_scope: "Continue RP19.P06.M04.S03 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP596_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp597Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP597_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp597CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp597Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp597Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-597 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-597");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP597_PACK_BINDING.risk_class) errors.push("CP00-597 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP597_PACK_BINDING.unit_count) errors.push("CP00-597 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP597_PACK_BINDING.first_unit_id) errors.push("CP00-597 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP597_PACK_BINDING.last_unit_id) errors.push("CP00-597 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-597 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-597 must only include RP19 units");
  const summary = createClientPortalCoreCp597CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP597_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-597 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP597_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-597 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP597_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-597 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP597_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-597 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP597_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-597 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-597 ${microId} missing row ${title}`);
    }
  }
  return freezeCp597Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp597P06WorkflowPermissionSliceDescriptor(
  descriptor = createClientPortalCoreCp597P06WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_workflow_permission_slice_case_set ?? createClientPortalCoreCp597P06WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp597P06WorkflowPermissionSliceDescriptor") errors.push("CP00-597 descriptor type drift");
  if (descriptor.source_p06_implementation_slice_descriptor !== "ClientPortalCoreCp596P06ImplementationSliceDescriptor") {
    errors.push("CP00-597 source p06 implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP597_REQUIREMENTS.required_section_rows).length) errors.push("CP00-597 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP597_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-597 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-597 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-597 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-597 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-597 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-597 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-597 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP597_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-597 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP597_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-597 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-597 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-597 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-597 contract current_pack drift");
  }
  if (
    contractProjection?.p06_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p06_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-597 contract p06_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP597_PACK_BINDING.next_pack_id) errors.push("CP00-597 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP597_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-597 next subphase drift");
  }
  return freezeCp597Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp597HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp597P06WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp597Coverage(planPack);
  const slice = validateClientPortalCoreCp597P06WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp597Validation({
    evidence_packet: "H19.CP00-597.client_portal_core_p06_workflow_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP597_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_implementation_slice_pack_id: CLIENT_PORTAL_CORE_CP597_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP597_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP597_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP597_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp597ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp597Coverage(planPack);
  const slice = validateClientPortalCoreCp597P06WorkflowPermissionSliceDescriptor(createClientPortalCoreCp597P06WorkflowPermissionSliceDescriptor(), {});
  return freezeCp597Validation({
    review_packet: "C19.CP00-597.client_portal_core_p06_workflow_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP597_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP597_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP597_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-597 cover exactly the 40 planned RP19 units from RP19.P06.M04.S03 through RP19.P06.M05.S20?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-597 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp597CloseoutHandoff() {
  return freezeCp597Validation({
    handoff_id: "CP00-597-to-CP00-598",
    from_pack_id: CLIENT_PORTAL_CORE_CP597_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP597_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP597_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP597_PACK_BINDING.range,
    open_scope: "Continue RP19.P06.M05.S21 onward with the remaining permission and audit binding rows and the synthetic fixture micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP597_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp598Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP598_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp598CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp598Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp598Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-598 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-598");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP598_PACK_BINDING.risk_class) errors.push("CP00-598 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP598_PACK_BINDING.unit_count) errors.push("CP00-598 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP598_PACK_BINDING.first_unit_id) errors.push("CP00-598 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP598_PACK_BINDING.last_unit_id) errors.push("CP00-598 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-598 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-598 must only include RP19 units");
  const summary = createClientPortalCoreCp598CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP598_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-598 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP598_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-598 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP598_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-598 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP598_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-598 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP598_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-598 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-598 ${microId} missing row ${title}`);
    }
  }
  return freezeCp598Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp598P06PermissionFixtureSliceDescriptor(
  descriptor = createClientPortalCoreCp598P06PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_permission_fixture_slice_case_set ?? createClientPortalCoreCp598P06PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp598P06PermissionFixtureSliceDescriptor") errors.push("CP00-598 descriptor type drift");
  if (descriptor.source_p06_workflow_permission_slice_descriptor !== "ClientPortalCoreCp597P06WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-598 source p06 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP598_REQUIREMENTS.required_section_rows).length) errors.push("CP00-598 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP598_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-598 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-598 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-598 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-598 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-598 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-598 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-598 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP598_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-598 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP598_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-598 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-598 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-598 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-598 contract current_pack drift");
  }
  if (
    contractProjection?.p06_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p06_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-598 contract p06_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP598_PACK_BINDING.next_pack_id) errors.push("CP00-598 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP598_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-598 next subphase drift");
  }
  return freezeCp598Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp598HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp598P06PermissionFixtureSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp598Coverage(planPack);
  const slice = validateClientPortalCoreCp598P06PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp598Validation({
    evidence_packet: "H19.CP00-598.client_portal_core_p06_permission_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP598_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_workflow_permission_slice_pack_id: CLIENT_PORTAL_CORE_CP598_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP598_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP598_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP598_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp598ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp598Coverage(planPack);
  const slice = validateClientPortalCoreCp598P06PermissionFixtureSliceDescriptor(createClientPortalCoreCp598P06PermissionFixtureSliceDescriptor(), {});
  return freezeCp598Validation({
    review_packet: "C19.CP00-598.client_portal_core_p06_permission_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP598_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP598_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP598_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-598 cover exactly the 10 planned RP19 units from RP19.P06.M05.S21 through RP19.P06.M06.S08?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-598 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp598CloseoutHandoff() {
  return freezeCp598Validation({
    handoff_id: "CP00-598-to-CP00-599",
    from_pack_id: CLIENT_PORTAL_CORE_CP598_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP598_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP598_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP598_PACK_BINDING.range,
    open_scope: "Continue RP19.P06.M06.S09 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP598_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp599Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP599_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp599CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp599Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp599Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-599 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-599");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP599_PACK_BINDING.risk_class) errors.push("CP00-599 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP599_PACK_BINDING.unit_count) errors.push("CP00-599 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP599_PACK_BINDING.first_unit_id) errors.push("CP00-599 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP599_PACK_BINDING.last_unit_id) errors.push("CP00-599 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-599 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-599 must only include RP19 units");
  const summary = createClientPortalCoreCp599CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP599_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-599 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP599_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-599 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP599_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-599 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP599_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-599 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP599_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-599 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-599 ${microId} missing row ${title}`);
    }
  }
  return freezeCp599Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp599P06CloseoutP07FoundationDescriptor(
  descriptor = createClientPortalCoreCp599P06CloseoutP07FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_closeout_p07_foundation_case_set ?? createClientPortalCoreCp599P06CloseoutP07FoundationCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp599P06CloseoutP07FoundationDescriptor") errors.push("CP00-599 descriptor type drift");
  if (descriptor.source_p06_permission_fixture_slice_descriptor !== "ClientPortalCoreCp598P06PermissionFixtureSliceDescriptor") {
    errors.push("CP00-599 source p06 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP599_REQUIREMENTS.required_section_rows).length) errors.push("CP00-599 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP599_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-599 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-599 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-599 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-599 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-599 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-599 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-599 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP599_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-599 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP599_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-599 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-599 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-599 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-599 contract current_pack drift");
  }
  if (
    contractProjection?.p06_closeout_p07_foundation_descriptor?.descriptor &&
    contractProjection.p06_closeout_p07_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-599 contract p06_closeout_p07_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP599_PACK_BINDING.next_pack_id) errors.push("CP00-599 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP599_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-599 next subphase drift");
  }
  return freezeCp599Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp599HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp599P06CloseoutP07FoundationDescriptor(),
) {
  const coverage = validateClientPortalCoreCp599Coverage(planPack);
  const slice = validateClientPortalCoreCp599P06CloseoutP07FoundationDescriptor(descriptor, contractProjection);
  return freezeCp599Validation({
    evidence_packet: "H19.CP00-599.client_portal_core_p06_closeout_p07_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP599_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_closeout_p07_foundation_valid: slice.valid,
    no_real_data: true,
    source_p06_permission_fixture_slice_pack_id: CLIENT_PORTAL_CORE_CP599_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP599_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP599_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP599_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp599ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp599Coverage(planPack);
  const slice = validateClientPortalCoreCp599P06CloseoutP07FoundationDescriptor(createClientPortalCoreCp599P06CloseoutP07FoundationDescriptor(), {});
  return freezeCp599Validation({
    review_packet: "C19.CP00-599.client_portal_core_p06_closeout_p07_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP599_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP599_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_closeout_p07_foundation_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP599_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-599 cover exactly the 150 planned RP19 units from RP19.P06.M06.S09 through RP19.P07.M03.S10?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-599 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp599CloseoutHandoff() {
  return freezeCp599Validation({
    handoff_id: "CP00-599-to-CP00-600",
    from_pack_id: CLIENT_PORTAL_CORE_CP599_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP599_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP599_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP599_PACK_BINDING.range,
    open_scope: "Continue RP19.P07.M03.S11 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP599_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp600Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP600_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp600CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp600Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp600Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-600 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-600");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP600_PACK_BINDING.risk_class) errors.push("CP00-600 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP600_PACK_BINDING.unit_count) errors.push("CP00-600 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP600_PACK_BINDING.first_unit_id) errors.push("CP00-600 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP600_PACK_BINDING.last_unit_id) errors.push("CP00-600 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-600 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-600 must only include RP19 units");
  const summary = createClientPortalCoreCp600CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP600_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-600 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP600_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-600 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP600_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-600 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP600_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-600 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP600_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-600 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-600 ${microId} missing row ${title}`);
    }
  }
  return freezeCp600Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp600P07ImplementationSliceDescriptor(
  descriptor = createClientPortalCoreCp600P07ImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_implementation_slice_case_set ?? createClientPortalCoreCp600P07ImplementationSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp600P07ImplementationSliceDescriptor") errors.push("CP00-600 descriptor type drift");
  if (descriptor.source_p06_closeout_p07_foundation_descriptor !== "ClientPortalCoreCp599P06CloseoutP07FoundationDescriptor") {
    errors.push("CP00-600 source p06 closeout p07 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP600_REQUIREMENTS.required_section_rows).length) errors.push("CP00-600 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP600_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-600 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-600 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-600 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-600 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-600 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-600 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-600 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP600_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-600 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP600_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-600 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-600 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-600 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-600 contract current_pack drift");
  }
  if (
    contractProjection?.p07_implementation_slice_descriptor?.descriptor &&
    contractProjection.p07_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-600 contract p07_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP600_PACK_BINDING.next_pack_id) errors.push("CP00-600 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP600_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-600 next subphase drift");
  }
  return freezeCp600Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp600HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp600P07ImplementationSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp600Coverage(planPack);
  const slice = validateClientPortalCoreCp600P07ImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp600Validation({
    evidence_packet: "H19.CP00-600.client_portal_core_p07_implementation_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP600_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_closeout_p07_foundation_pack_id: CLIENT_PORTAL_CORE_CP600_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP600_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP600_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP600_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp600ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp600Coverage(planPack);
  const slice = validateClientPortalCoreCp600P07ImplementationSliceDescriptor(createClientPortalCoreCp600P07ImplementationSliceDescriptor(), {});
  return freezeCp600Validation({
    review_packet: "C19.CP00-600.client_portal_core_p07_implementation_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP600_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP600_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_implementation_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP600_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-600 cover exactly the 10 planned RP19 units from RP19.P07.M03.S11 through RP19.P07.M03.S20?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-600 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp600CloseoutHandoff() {
  return freezeCp600Validation({
    handoff_id: "CP00-600-to-CP00-601",
    from_pack_id: CLIENT_PORTAL_CORE_CP600_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP600_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP600_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP600_PACK_BINDING.range,
    open_scope: "Continue RP19.P07.M03.S21 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP600_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp601Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP601_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp601CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp601Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp601Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-601 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-601");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP601_PACK_BINDING.risk_class) errors.push("CP00-601 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP601_PACK_BINDING.unit_count) errors.push("CP00-601 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP601_PACK_BINDING.first_unit_id) errors.push("CP00-601 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP601_PACK_BINDING.last_unit_id) errors.push("CP00-601 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-601 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-601 must only include RP19 units");
  const summary = createClientPortalCoreCp601CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP601_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-601 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP601_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-601 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP601_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-601 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP601_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-601 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP601_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-601 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-601 ${microId} missing row ${title}`);
    }
  }
  return freezeCp601Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp601P07WorkflowPermissionSliceDescriptor(
  descriptor = createClientPortalCoreCp601P07WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_workflow_permission_slice_case_set ?? createClientPortalCoreCp601P07WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp601P07WorkflowPermissionSliceDescriptor") errors.push("CP00-601 descriptor type drift");
  if (descriptor.source_p07_implementation_slice_descriptor !== "ClientPortalCoreCp600P07ImplementationSliceDescriptor") {
    errors.push("CP00-601 source p07 implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP601_REQUIREMENTS.required_section_rows).length) errors.push("CP00-601 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP601_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-601 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-601 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-601 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-601 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-601 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-601 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-601 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP601_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-601 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP601_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-601 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-601 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-601 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-601 contract current_pack drift");
  }
  if (
    contractProjection?.p07_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p07_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-601 contract p07_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP601_PACK_BINDING.next_pack_id) errors.push("CP00-601 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP601_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-601 next subphase drift");
  }
  return freezeCp601Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp601HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp601P07WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp601Coverage(planPack);
  const slice = validateClientPortalCoreCp601P07WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp601Validation({
    evidence_packet: "H19.CP00-601.client_portal_core_p07_workflow_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP601_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_implementation_slice_pack_id: CLIENT_PORTAL_CORE_CP601_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP601_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP601_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP601_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp601ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp601Coverage(planPack);
  const slice = validateClientPortalCoreCp601P07WorkflowPermissionSliceDescriptor(createClientPortalCoreCp601P07WorkflowPermissionSliceDescriptor(), {});
  return freezeCp601Validation({
    review_packet: "C19.CP00-601.client_portal_core_p07_workflow_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP601_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP601_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP601_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-601 cover exactly the 40 planned RP19 units from RP19.P07.M03.S21 through RP19.P07.M05.S16?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-601 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp601CloseoutHandoff() {
  return freezeCp601Validation({
    handoff_id: "CP00-601-to-CP00-602",
    from_pack_id: CLIENT_PORTAL_CORE_CP601_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP601_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP601_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP601_PACK_BINDING.range,
    open_scope: "Continue RP19.P07.M05.S17 onward with the remaining permission and audit binding rows and the synthetic fixture micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP601_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp602Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP602_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp602CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp602Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp602Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-602 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-602");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP602_PACK_BINDING.risk_class) errors.push("CP00-602 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP602_PACK_BINDING.unit_count) errors.push("CP00-602 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP602_PACK_BINDING.first_unit_id) errors.push("CP00-602 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP602_PACK_BINDING.last_unit_id) errors.push("CP00-602 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-602 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-602 must only include RP19 units");
  const summary = createClientPortalCoreCp602CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP602_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-602 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP602_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-602 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP602_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-602 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP602_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-602 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP602_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-602 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-602 ${microId} missing row ${title}`);
    }
  }
  return freezeCp602Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp602P07PermissionFixtureSliceDescriptor(
  descriptor = createClientPortalCoreCp602P07PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_permission_fixture_slice_case_set ?? createClientPortalCoreCp602P07PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp602P07PermissionFixtureSliceDescriptor") errors.push("CP00-602 descriptor type drift");
  if (descriptor.source_p07_workflow_permission_slice_descriptor !== "ClientPortalCoreCp601P07WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-602 source p07 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP602_REQUIREMENTS.required_section_rows).length) errors.push("CP00-602 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP602_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-602 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-602 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-602 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-602 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-602 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-602 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-602 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP602_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-602 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP602_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-602 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-602 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-602 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-602 contract current_pack drift");
  }
  if (
    contractProjection?.p07_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p07_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-602 contract p07_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP602_PACK_BINDING.next_pack_id) errors.push("CP00-602 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP602_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-602 next subphase drift");
  }
  return freezeCp602Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp602HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp602P07PermissionFixtureSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp602Coverage(planPack);
  const slice = validateClientPortalCoreCp602P07PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp602Validation({
    evidence_packet: "H19.CP00-602.client_portal_core_p07_permission_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP602_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_workflow_permission_slice_pack_id: CLIENT_PORTAL_CORE_CP602_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP602_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP602_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP602_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp602ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp602Coverage(planPack);
  const slice = validateClientPortalCoreCp602P07PermissionFixtureSliceDescriptor(createClientPortalCoreCp602P07PermissionFixtureSliceDescriptor(), {});
  return freezeCp602Validation({
    review_packet: "C19.CP00-602.client_portal_core_p07_permission_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP602_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP602_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP602_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-602 cover exactly the 10 planned RP19 units from RP19.P07.M05.S17 through RP19.P07.M06.S04?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-602 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp602CloseoutHandoff() {
  return freezeCp602Validation({
    handoff_id: "CP00-602-to-CP00-603",
    from_pack_id: CLIENT_PORTAL_CORE_CP602_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP602_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP602_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP602_PACK_BINDING.range,
    open_scope: "Continue RP19.P07.M06.S05 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP602_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp603Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP603_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp603CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp603Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp603Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-603 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-603");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP603_PACK_BINDING.risk_class) errors.push("CP00-603 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP603_PACK_BINDING.unit_count) errors.push("CP00-603 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP603_PACK_BINDING.first_unit_id) errors.push("CP00-603 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP603_PACK_BINDING.last_unit_id) errors.push("CP00-603 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-603 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-603 must only include RP19 units");
  const summary = createClientPortalCoreCp603CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP603_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-603 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP603_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-603 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP603_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-603 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP603_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-603 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP603_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-603 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-603 ${microId} missing row ${title}`);
    }
  }
  return freezeCp603Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp603P07CloseoutP08FoundationDescriptor(
  descriptor = createClientPortalCoreCp603P07CloseoutP08FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_closeout_p08_foundation_case_set ?? createClientPortalCoreCp603P07CloseoutP08FoundationCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp603P07CloseoutP08FoundationDescriptor") errors.push("CP00-603 descriptor type drift");
  if (descriptor.source_p07_permission_fixture_slice_descriptor !== "ClientPortalCoreCp602P07PermissionFixtureSliceDescriptor") {
    errors.push("CP00-603 source p07 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP603_REQUIREMENTS.required_section_rows).length) errors.push("CP00-603 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP603_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-603 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-603 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-603 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-603 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-603 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-603 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-603 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP603_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-603 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP603_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-603 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-603 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-603 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-603 contract current_pack drift");
  }
  if (
    contractProjection?.p07_closeout_p08_foundation_descriptor?.descriptor &&
    contractProjection.p07_closeout_p08_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-603 contract p07_closeout_p08_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP603_PACK_BINDING.next_pack_id) errors.push("CP00-603 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP603_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-603 next subphase drift");
  }
  return freezeCp603Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp603HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp603P07CloseoutP08FoundationDescriptor(),
) {
  const coverage = validateClientPortalCoreCp603Coverage(planPack);
  const slice = validateClientPortalCoreCp603P07CloseoutP08FoundationDescriptor(descriptor, contractProjection);
  return freezeCp603Validation({
    evidence_packet: "H19.CP00-603.client_portal_core_p07_closeout_p08_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP603_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_closeout_p08_foundation_valid: slice.valid,
    no_real_data: true,
    source_p07_permission_fixture_slice_pack_id: CLIENT_PORTAL_CORE_CP603_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP603_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP603_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP603_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp603ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp603Coverage(planPack);
  const slice = validateClientPortalCoreCp603P07CloseoutP08FoundationDescriptor(createClientPortalCoreCp603P07CloseoutP08FoundationDescriptor(), {});
  return freezeCp603Validation({
    review_packet: "C19.CP00-603.client_portal_core_p07_closeout_p08_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP603_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP603_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_closeout_p08_foundation_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP603_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-603 cover exactly the 150 planned RP19 units from RP19.P07.M06.S05 through RP19.P08.M03.S21?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-603 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp603CloseoutHandoff() {
  return freezeCp603Validation({
    handoff_id: "CP00-603-to-CP00-604",
    from_pack_id: CLIENT_PORTAL_CORE_CP603_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP603_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP603_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP603_PACK_BINDING.range,
    open_scope: "Continue RP19.P08.M03.S22 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP603_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp604Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP604_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp604CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp604Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp604Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-604 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-604");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP604_PACK_BINDING.risk_class) errors.push("CP00-604 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP604_PACK_BINDING.unit_count) errors.push("CP00-604 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP604_PACK_BINDING.first_unit_id) errors.push("CP00-604 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP604_PACK_BINDING.last_unit_id) errors.push("CP00-604 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-604 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-604 must only include RP19 units");
  const summary = createClientPortalCoreCp604CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP604_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-604 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP604_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-604 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP604_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-604 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP604_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-604 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP604_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-604 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-604 ${microId} missing row ${title}`);
    }
  }
  return freezeCp604Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp604P08WorkflowPermissionSliceDescriptor(
  descriptor = createClientPortalCoreCp604P08WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_workflow_permission_slice_case_set ?? createClientPortalCoreCp604P08WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp604P08WorkflowPermissionSliceDescriptor") errors.push("CP00-604 descriptor type drift");
  if (descriptor.source_p07_closeout_p08_foundation_descriptor !== "ClientPortalCoreCp603P07CloseoutP08FoundationDescriptor") {
    errors.push("CP00-604 source p07 closeout p08 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP604_REQUIREMENTS.required_section_rows).length) errors.push("CP00-604 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP604_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-604 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-604 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-604 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-604 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-604 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-604 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-604 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP604_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-604 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP604_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-604 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-604 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-604 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-604 contract current_pack drift");
  }
  if (
    contractProjection?.p08_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p08_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-604 contract p08_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP604_PACK_BINDING.next_pack_id) errors.push("CP00-604 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP604_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-604 next subphase drift");
  }
  return freezeCp604Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp604HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp604P08WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp604Coverage(planPack);
  const slice = validateClientPortalCoreCp604P08WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp604Validation({
    evidence_packet: "H19.CP00-604.client_portal_core_p08_workflow_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP604_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_closeout_p08_foundation_pack_id: CLIENT_PORTAL_CORE_CP604_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP604_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP604_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP604_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp604ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp604Coverage(planPack);
  const slice = validateClientPortalCoreCp604P08WorkflowPermissionSliceDescriptor(createClientPortalCoreCp604P08WorkflowPermissionSliceDescriptor(), {});
  return freezeCp604Validation({
    review_packet: "C19.CP00-604.client_portal_core_p08_workflow_permission_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP604_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP604_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP604_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-604 cover exactly the 40 planned RP19 units from RP19.P08.M03.S22 through RP19.P08.M05.S19?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-604 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp604CloseoutHandoff() {
  return freezeCp604Validation({
    handoff_id: "CP00-604-to-CP00-605",
    from_pack_id: CLIENT_PORTAL_CORE_CP604_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP604_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP604_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP604_PACK_BINDING.range,
    open_scope: "Continue RP19.P08.M05.S20 onward with the remaining permission and audit binding rows and the synthetic fixture micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP604_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp605Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP605_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp605CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp605Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp605Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-605 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-605");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP605_PACK_BINDING.risk_class) errors.push("CP00-605 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP605_PACK_BINDING.unit_count) errors.push("CP00-605 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP605_PACK_BINDING.first_unit_id) errors.push("CP00-605 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP605_PACK_BINDING.last_unit_id) errors.push("CP00-605 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-605 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-605 must only include RP19 units");
  const summary = createClientPortalCoreCp605CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP605_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-605 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP605_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-605 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP605_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-605 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP605_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-605 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP605_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-605 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-605 ${microId} missing row ${title}`);
    }
  }
  return freezeCp605Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp605P08PermissionFixtureSliceDescriptor(
  descriptor = createClientPortalCoreCp605P08PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_permission_fixture_slice_case_set ?? createClientPortalCoreCp605P08PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp605P08PermissionFixtureSliceDescriptor") errors.push("CP00-605 descriptor type drift");
  if (descriptor.source_p08_workflow_permission_slice_descriptor !== "ClientPortalCoreCp604P08WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-605 source p08 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP605_REQUIREMENTS.required_section_rows).length) errors.push("CP00-605 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP605_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-605 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-605 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-605 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-605 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-605 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-605 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-605 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP605_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-605 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP605_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-605 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-605 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-605 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-605 contract current_pack drift");
  }
  if (
    contractProjection?.p08_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p08_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-605 contract p08_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP605_PACK_BINDING.next_pack_id) errors.push("CP00-605 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP605_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-605 next subphase drift");
  }
  return freezeCp605Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp605HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp605P08PermissionFixtureSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp605Coverage(planPack);
  const slice = validateClientPortalCoreCp605P08PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp605Validation({
    evidence_packet: "H19.CP00-605.client_portal_core_p08_permission_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP605_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p08_workflow_permission_slice_pack_id: CLIENT_PORTAL_CORE_CP605_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP605_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP605_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP605_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp605ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp605Coverage(planPack);
  const slice = validateClientPortalCoreCp605P08PermissionFixtureSliceDescriptor(createClientPortalCoreCp605P08PermissionFixtureSliceDescriptor(), {});
  return freezeCp605Validation({
    review_packet: "C19.CP00-605.client_portal_core_p08_permission_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP605_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP605_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP605_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-605 cover exactly the 10 planned RP19 units from RP19.P08.M05.S20 through RP19.P08.M06.S07?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-605 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp605CloseoutHandoff() {
  return freezeCp605Validation({
    handoff_id: "CP00-605-to-CP00-606",
    from_pack_id: CLIENT_PORTAL_CORE_CP605_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP605_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP605_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP605_PACK_BINDING.range,
    open_scope: "Continue RP19.P08.M06.S08 onward with the remaining synthetic fixture rows while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP605_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp606Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP606_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp606CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp606Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp606Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-606 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-606");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP606_PACK_BINDING.risk_class) errors.push("CP00-606 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP606_PACK_BINDING.unit_count) errors.push("CP00-606 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP606_PACK_BINDING.first_unit_id) errors.push("CP00-606 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP606_PACK_BINDING.last_unit_id) errors.push("CP00-606 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-606 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-606 must only include RP19 units");
  const summary = createClientPortalCoreCp606CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP606_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-606 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP606_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-606 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP606_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-606 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP606_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-606 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP606_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-606 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-606 ${microId} missing row ${title}`);
    }
  }
  return freezeCp606Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp606P08FixtureSliceDescriptor(
  descriptor = createClientPortalCoreCp606P08FixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_fixture_slice_case_set ?? createClientPortalCoreCp606P08FixtureSliceCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp606P08FixtureSliceDescriptor") errors.push("CP00-606 descriptor type drift");
  if (descriptor.source_p08_permission_fixture_slice_descriptor !== "ClientPortalCoreCp605P08PermissionFixtureSliceDescriptor") {
    errors.push("CP00-606 source p08 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP606_REQUIREMENTS.required_section_rows).length) errors.push("CP00-606 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP606_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-606 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-606 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-606 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-606 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-606 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-606 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-606 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP606_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-606 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP606_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-606 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-606 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-606 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-606 contract current_pack drift");
  }
  if (
    contractProjection?.p08_fixture_slice_descriptor?.descriptor &&
    contractProjection.p08_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-606 contract p08_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP606_PACK_BINDING.next_pack_id) errors.push("CP00-606 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP606_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-606 next subphase drift");
  }
  return freezeCp606Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp606HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp606P08FixtureSliceDescriptor(),
) {
  const coverage = validateClientPortalCoreCp606Coverage(planPack);
  const slice = validateClientPortalCoreCp606P08FixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp606Validation({
    evidence_packet: "H19.CP00-606.client_portal_core_p08_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP606_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p08_permission_fixture_slice_pack_id: CLIENT_PORTAL_CORE_CP606_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP606_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP606_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP606_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createClientPortalCoreCp606ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp606Coverage(planPack);
  const slice = validateClientPortalCoreCp606P08FixtureSliceDescriptor(createClientPortalCoreCp606P08FixtureSliceDescriptor(), {});
  return freezeCp606Validation({
    review_packet: "C19.CP00-606.client_portal_core_p08_fixture_slice_descriptor",
    gate: CLIENT_PORTAL_CORE_CP606_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP606_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_fixture_slice_valid: slice.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP606_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-606 cover exactly the 10 planned RP19 units from RP19.P08.M06.S08 through RP19.P08.M06.S17?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared client portal guards applied?",
      "Does CP00-606 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp606CloseoutHandoff() {
  return freezeCp606Validation({
    handoff_id: "CP00-606-to-CP00-607",
    from_pack_id: CLIENT_PORTAL_CORE_CP606_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP606_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP606_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP606_PACK_BINDING.range,
    open_scope: "Continue RP19.P08.M06.S18 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP606_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp607Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP607_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp607CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp607Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp607Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-607 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-607");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP607_PACK_BINDING.risk_class) errors.push("CP00-607 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP607_PACK_BINDING.unit_count) errors.push("CP00-607 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP607_PACK_BINDING.first_unit_id) errors.push("CP00-607 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP607_PACK_BINDING.last_unit_id) errors.push("CP00-607 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-607 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-607 must only include RP19 units");
  const summary = createClientPortalCoreCp607CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP607_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-607 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP607_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-607 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP607_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-607 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP607_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-607 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP607_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-607 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-607 ${microId} missing row ${title}`);
    }
  }
  return freezeCp607Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp607P08CloseoutP09FoundationDescriptor(
  descriptor = createClientPortalCoreCp607P08CloseoutP09FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet =
    descriptor.p08_closeout_p09_foundation_case_set ?? createClientPortalCoreCp607P08CloseoutP09FoundationCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp607P08CloseoutP09FoundationDescriptor") errors.push("CP00-607 descriptor type drift");
  if (descriptor.source_p08_fixture_slice_descriptor !== "ClientPortalCoreCp606P08FixtureSliceDescriptor") {
    errors.push("CP00-607 source p08 fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP607_REQUIREMENTS.required_section_rows).length) errors.push("CP00-607 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP607_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-607 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-607 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-607 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-607 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-607 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-607 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-607 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP607_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-607 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP607_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-607 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-607 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-607 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-607 contract current_pack drift");
  }
  if (
    contractProjection?.p08_closeout_p09_foundation_descriptor?.descriptor &&
    contractProjection.p08_closeout_p09_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-607 contract p08_closeout_p09_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP607_PACK_BINDING.next_pack_id) errors.push("CP00-607 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP607_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-607 next subphase drift");
  }
  return freezeCp607Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp607HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp607P08CloseoutP09FoundationDescriptor(),
) {
  const coverage = validateClientPortalCoreCp607Coverage(planPack);
  const foundation = validateClientPortalCoreCp607P08CloseoutP09FoundationDescriptor(descriptor, contractProjection);
  return freezeCp607Validation({
    evidence_packet: "H19.CP00-607.client_portal_core_p08_closeout_p09_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP607_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_closeout_p09_foundation_valid: foundation.valid,
    no_real_data: true,
    source_p08_fixture_slice_pack_id: CLIENT_PORTAL_CORE_CP607_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP607_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP607_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP607_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createClientPortalCoreCp607ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp607Coverage(planPack);
  const foundation = validateClientPortalCoreCp607P08CloseoutP09FoundationDescriptor(
    createClientPortalCoreCp607P08CloseoutP09FoundationDescriptor(),
    {},
  );
  return freezeCp607Validation({
    review_packet: "C19.CP00-607.client_portal_core_p08_closeout_p09_foundation_descriptor",
    gate: CLIENT_PORTAL_CORE_CP607_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP607_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_closeout_p09_foundation_valid: foundation.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP607_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-607 cover exactly the 150 planned RP19 units from RP19.P08.M06.S18 through RP19.P09.M05.S17?",
      "Do the eleven descriptor sections cover every planned row title as descriptor-only rows with the shared client portal guards applied?",
      "Does CP00-607 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp607CloseoutHandoff() {
  return freezeCp607Validation({
    handoff_id: "CP00-607-to-CP00-608",
    from_pack_id: CLIENT_PORTAL_CORE_CP607_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP607_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP607_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP607_PACK_BINDING.range,
    open_scope: "Continue RP19.P09.M05.S18 onward with the remaining permission and audit binding rows and downstream micros while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP607_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp608Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP608_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp608CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp608Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp608Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-608 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-608");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP608_PACK_BINDING.risk_class) errors.push("CP00-608 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP608_PACK_BINDING.unit_count) errors.push("CP00-608 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP608_PACK_BINDING.first_unit_id) errors.push("CP00-608 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP608_PACK_BINDING.last_unit_id) errors.push("CP00-608 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-608 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-608 must only include RP19 units");
  const summary = createClientPortalCoreCp608CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP608_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-608 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP608_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-608 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP608_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-608 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP608_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-608 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP608_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-608 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-608 ${microId} missing row ${title}`);
    }
  }
  return freezeCp608Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp608P09PermissionAuditFixtureBridgeDescriptor(
  descriptor = createClientPortalCoreCp608P09PermissionAuditFixtureBridgeDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet =
    descriptor.p09_permission_audit_fixture_bridge_case_set ?? createClientPortalCoreCp608P09PermissionAuditFixtureBridgeCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp608P09PermissionAuditFixtureBridgeDescriptor") errors.push("CP00-608 descriptor type drift");
  if (descriptor.source_p08_closeout_p09_foundation_descriptor !== "ClientPortalCoreCp607P08CloseoutP09FoundationDescriptor") {
    errors.push("CP00-608 source p08 closeout p09 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP608_REQUIREMENTS.required_section_rows).length) errors.push("CP00-608 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP608_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-608 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-608 ${microId} row count drift`);
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-608 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-608 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-608 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-608 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-608 ${microId} ${key} must not include raw payload`);
    }
  }
  if (CLIENT_PORTAL_CORE_CP608_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-608 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP608_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-608 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-608 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-608 local validation trust boundary drift");
  }
  if (contractProjection?.current_pack?.pack_id && ![CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id, CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)) {
    errors.push("CP00-608 contract current_pack drift");
  }
  if (
    contractProjection?.p09_permission_audit_fixture_bridge_descriptor?.descriptor &&
    contractProjection.p09_permission_audit_fixture_bridge_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-608 contract p09_permission_audit_fixture_bridge_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP608_PACK_BINDING.next_pack_id) errors.push("CP00-608 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP608_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-608 next subphase drift");
  }
  return freezeCp608Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp608HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp608P09PermissionAuditFixtureBridgeDescriptor(),
) {
  const coverage = validateClientPortalCoreCp608Coverage(planPack);
  const bridge = validateClientPortalCoreCp608P09PermissionAuditFixtureBridgeDescriptor(descriptor, contractProjection);
  return freezeCp608Validation({
    evidence_packet: "H19.CP00-608.client_portal_core_p09_permission_audit_fixture_bridge_descriptor",
    gate: CLIENT_PORTAL_CORE_CP608_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_permission_audit_fixture_bridge_valid: bridge.valid,
    no_real_data: true,
    source_p08_closeout_p09_foundation_pack_id: CLIENT_PORTAL_CORE_CP608_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP608_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP608_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP608_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && bridge.valid,
  });
}

export function createClientPortalCoreCp608ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp608Coverage(planPack);
  const bridge = validateClientPortalCoreCp608P09PermissionAuditFixtureBridgeDescriptor(
    createClientPortalCoreCp608P09PermissionAuditFixtureBridgeDescriptor(),
    {},
  );
  return freezeCp608Validation({
    review_packet: "C19.CP00-608.client_portal_core_p09_permission_audit_fixture_bridge_descriptor",
    gate: CLIENT_PORTAL_CORE_CP608_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP608_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_permission_audit_fixture_bridge_valid: bridge.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP608_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-608 cover exactly the 10 planned RP19 units from RP19.P09.M05.S18 through RP19.P09.M06.S07?",
      "Do the two descriptor sections cover every planned row title as descriptor-only rows with the shared client portal guards applied?",
      "Does CP00-608 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp608CloseoutHandoff() {
  return freezeCp608Validation({
    handoff_id: "CP00-608-to-CP00-609",
    from_pack_id: CLIENT_PORTAL_CORE_CP608_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP608_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP608_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP608_PACK_BINDING.range,
    open_scope: "Continue RP19.P09.M06.S08 onward with the remaining synthetic fixture rows and downstream review closeout rows while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP608_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp609Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id,
    no_write_attestation: CLIENT_PORTAL_CORE_CP609_NO_WRITE_ATTESTATION,
  });
}

export function createClientPortalCoreCp609CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp609Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateClientPortalCoreCp609Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-609 plan pack is required");
  if (planPack?.pack_id !== CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-609");
  if (planPack?.risk_class !== CLIENT_PORTAL_CORE_CP609_PACK_BINDING.risk_class) errors.push("CP00-609 risk class drift");
  if (planPack?.unit_count !== CLIENT_PORTAL_CORE_CP609_PACK_BINDING.unit_count) errors.push("CP00-609 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== CLIENT_PORTAL_CORE_CP609_PACK_BINDING.first_unit_id) errors.push("CP00-609 first unit drift");
  if (unitIds.at(-1) !== CLIENT_PORTAL_CORE_CP609_PACK_BINDING.last_unit_id) errors.push("CP00-609 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-609 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP19")) errors.push("CP00-609 must only include RP19 units");
  const summary = createClientPortalCoreCp609CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(CLIENT_PORTAL_CORE_CP609_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push("CP00-609 " + deliverable + " distribution drift");
  }
  for (const [phase, count] of Object.entries(CLIENT_PORTAL_CORE_CP609_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push("CP00-609 " + phase + " distribution drift");
  }
  for (const [microPhase, count] of Object.entries(CLIENT_PORTAL_CORE_CP609_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push("CP00-609 " + microPhase + " distribution drift");
  }
  for (const [microTitle, count] of Object.entries(CLIENT_PORTAL_CORE_CP609_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push("CP00-609 " + microTitle + " distribution drift");
  }
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP609_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push("CP00-609 " + microId + " row count drift");
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push("CP00-609 " + microId + " missing row " + title);
    }
  }
  return freezeCp609Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateClientPortalCoreCp609P09ReviewEvidenceCloseoutBridgeDescriptor(
  descriptor = createClientPortalCoreCp609P09ReviewEvidenceCloseoutBridgeDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet =
    descriptor.p09_review_evidence_closeout_bridge_case_set ?? createClientPortalCoreCp609P09ReviewEvidenceCloseoutBridgeCaseSet();
  if (descriptor.descriptor !== "ClientPortalCoreCp609P09ReviewEvidenceCloseoutBridgeDescriptor") errors.push("CP00-609 descriptor type drift");
  if (descriptor.source_p09_permission_audit_fixture_bridge_descriptor !== "ClientPortalCoreCp608P09PermissionAuditFixtureBridgeDescriptor") {
    errors.push("CP00-609 source p09 permission audit fixture bridge descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(CLIENT_PORTAL_CORE_CP609_REQUIREMENTS.required_section_rows).length) errors.push("CP00-609 section count drift");
  for (const [microId, titles] of Object.entries(CLIENT_PORTAL_CORE_CP609_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push("CP00-609 missing section " + microId);
      continue;
    }
    if (section.row_count !== titles.length) errors.push("CP00-609 " + microId + " row count drift");
    for (const title of titles) {
      const key = clientPortalCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push("CP00-609 " + microId + " missing row " + key);
        continue;
      }
      if (row.descriptor_only !== true) errors.push("CP00-609 " + microId + " " + key + " must be descriptor-only");
      if (row.runtime_execution !== false) errors.push("CP00-609 " + microId + " " + key + " must not execute runtime");
      if (row.customer_safe_errors_only !== true) errors.push("CP00-609 " + microId + " " + key + " must be customer-safe");
      if (row.raw_payload_included !== false) errors.push("CP00-609 " + microId + " " + key + " must not include raw payload");
    }
  }
  if (CLIENT_PORTAL_CORE_CP609_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-609 must not promote Claude");
  if (CLIENT_PORTAL_CORE_CP609_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-609 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-609 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-609 local validation trust boundary drift");
  }
  if (contractProjection?.current_pack?.pack_id && contractProjection.current_pack.pack_id !== CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id) {
    errors.push("CP00-609 contract current_pack drift");
  }
  if (
    contractProjection?.p09_review_evidence_closeout_bridge_descriptor?.descriptor &&
    contractProjection.p09_review_evidence_closeout_bridge_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-609 contract p09_review_evidence_closeout_bridge_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== CLIENT_PORTAL_CORE_CP609_PACK_BINDING.next_pack_id) errors.push("CP00-609 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== CLIENT_PORTAL_CORE_CP609_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-609 next subphase drift");
  }
  return freezeCp609Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createClientPortalCoreCp609HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createClientPortalCoreCp609P09ReviewEvidenceCloseoutBridgeDescriptor(),
) {
  const coverage = validateClientPortalCoreCp609Coverage(planPack);
  const bridge = validateClientPortalCoreCp609P09ReviewEvidenceCloseoutBridgeDescriptor(descriptor, contractProjection);
  return freezeCp609Validation({
    evidence_packet: "H19.CP00-609.client_portal_core_p09_review_evidence_closeout_bridge_descriptor",
    gate: CLIENT_PORTAL_CORE_CP609_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_review_evidence_closeout_bridge_valid: bridge.valid,
    no_real_data: true,
    source_p09_permission_audit_fixture_bridge_pack_id: CLIENT_PORTAL_CORE_CP609_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: CLIENT_PORTAL_CORE_CP609_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: CLIENT_PORTAL_CORE_CP609_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP609_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && bridge.valid,
  });
}

export function createClientPortalCoreCp609ClaudeReviewPacket(planPack) {
  const coverage = validateClientPortalCoreCp609Coverage(planPack);
  const bridge = validateClientPortalCoreCp609P09ReviewEvidenceCloseoutBridgeDescriptor(
    createClientPortalCoreCp609P09ReviewEvidenceCloseoutBridgeDescriptor(),
    {},
  );
  return freezeCp609Validation({
    review_packet: "C19.CP00-609.client_portal_core_p09_review_evidence_closeout_bridge_descriptor",
    gate: CLIENT_PORTAL_CORE_CP609_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: CLIENT_PORTAL_CORE_CP609_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_review_evidence_closeout_bridge_valid: bridge.valid,
    invalid_review_blockers: CLIENT_PORTAL_CORE_CP609_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-609 cover exactly the 65 planned RP19 units from RP19.P09.M06.S08 through RP19.P09.M10.S04?",
      "Do the five descriptor sections cover every planned row title as descriptor-only rows with the shared client portal guards applied?",
      "Does CP00-609 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack preserve the handoff boundary to RP20 without claiming client portal runtime or enterprise trust?",
    ]),
  });
}

export function createClientPortalCoreCp609CloseoutHandoff() {
  return freezeCp609Validation({
    handoff_id: "CP00-609-to-CP00-610",
    from_pack_id: CLIENT_PORTAL_CORE_CP609_PACK_BINDING.pack_id,
    to_pack_id: CLIENT_PORTAL_CORE_CP609_PACK_BINDING.next_pack_id,
    next_subphase_id: CLIENT_PORTAL_CORE_CP609_PACK_BINDING.next_subphase_id,
    closed_scope: CLIENT_PORTAL_CORE_CP609_PACK_BINDING.range,
    open_scope: "Continue RP20.P00.M00.S01 onward with the next release program scope inventory while preserving descriptor-only client portal boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: CLIENT_PORTAL_CORE_CP609_PACK_BINDING.production_ready_flag,
  });
}
