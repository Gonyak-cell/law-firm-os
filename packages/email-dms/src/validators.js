import {
  EMAIL_DMS_CORE_CP272_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP272_PACK_BINDING,
  EMAIL_DMS_CORE_CP272_REQUIREMENTS,
  EMAIL_DMS_CORE_CP273_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP273_PACK_BINDING,
  EMAIL_DMS_CORE_CP273_REQUIREMENTS,
  EMAIL_DMS_CORE_CP274_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP274_PACK_BINDING,
  EMAIL_DMS_CORE_CP274_REQUIREMENTS,
  EMAIL_DMS_CORE_CP275_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP275_PACK_BINDING,
  EMAIL_DMS_CORE_CP275_REQUIREMENTS,
  EMAIL_DMS_CORE_CP276_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP276_PACK_BINDING,
  EMAIL_DMS_CORE_CP276_REQUIREMENTS,
  EMAIL_DMS_CORE_CP277_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP277_PACK_BINDING,
  EMAIL_DMS_CORE_CP277_REQUIREMENTS,
  EMAIL_DMS_CORE_CP278_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP278_PACK_BINDING,
  EMAIL_DMS_CORE_CP278_REQUIREMENTS,
  EMAIL_DMS_CORE_CP279_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP279_PACK_BINDING,
  EMAIL_DMS_CORE_CP279_REQUIREMENTS,
  EMAIL_DMS_CORE_CP280_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP280_PACK_BINDING,
  EMAIL_DMS_CORE_CP280_REQUIREMENTS,
  EMAIL_DMS_CORE_CP281_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP281_PACK_BINDING,
  EMAIL_DMS_CORE_CP281_REQUIREMENTS,
  EMAIL_DMS_CORE_CP282_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP282_PACK_BINDING,
  EMAIL_DMS_CORE_CP282_REQUIREMENTS,
  EMAIL_DMS_CORE_CP283_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP283_PACK_BINDING,
  EMAIL_DMS_CORE_CP283_REQUIREMENTS,
  EMAIL_DMS_CORE_CP284_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP284_PACK_BINDING,
  EMAIL_DMS_CORE_CP284_REQUIREMENTS,
  EMAIL_DMS_CORE_CP285_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP285_PACK_BINDING,
  EMAIL_DMS_CORE_CP285_REQUIREMENTS,
  EMAIL_DMS_CORE_CP286_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP286_PACK_BINDING,
  EMAIL_DMS_CORE_CP286_REQUIREMENTS,
  EMAIL_DMS_CORE_CP287_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP287_PACK_BINDING,
  EMAIL_DMS_CORE_CP287_REQUIREMENTS,
  EMAIL_DMS_CORE_CP288_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP288_PACK_BINDING,
  EMAIL_DMS_CORE_CP288_REQUIREMENTS,
  EMAIL_DMS_CORE_CP289_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP289_PACK_BINDING,
  EMAIL_DMS_CORE_CP289_REQUIREMENTS,
  EMAIL_DMS_CORE_CP290_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP290_PACK_BINDING,
  EMAIL_DMS_CORE_CP290_REQUIREMENTS,
  EMAIL_DMS_CORE_CP291_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP291_PACK_BINDING,
  EMAIL_DMS_CORE_CP291_REQUIREMENTS,
  EMAIL_DMS_CORE_CP292_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP292_PACK_BINDING,
  EMAIL_DMS_CORE_CP292_REQUIREMENTS,
  EMAIL_DMS_CORE_CP293_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP293_PACK_BINDING,
  EMAIL_DMS_CORE_CP293_REQUIREMENTS,
  EMAIL_DMS_CORE_CP294_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP294_PACK_BINDING,
  EMAIL_DMS_CORE_CP294_REQUIREMENTS,
  EMAIL_DMS_CORE_CP295_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP295_PACK_BINDING,
  EMAIL_DMS_CORE_CP295_REQUIREMENTS,
  EMAIL_DMS_CORE_CP296_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP296_PACK_BINDING,
  EMAIL_DMS_CORE_CP296_REQUIREMENTS,
  EMAIL_DMS_CORE_CP297_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP297_PACK_BINDING,
  EMAIL_DMS_CORE_CP297_REQUIREMENTS,
  EMAIL_DMS_CORE_CP298_NO_WRITE_ATTESTATION,
  EMAIL_DMS_CORE_CP298_PACK_BINDING,
  EMAIL_DMS_CORE_CP298_REQUIREMENTS,
} from "./registry.js";
import {
  createEmailDmsCoreCp272ScopeContractFoundationCaseSet,
  createEmailDmsCoreCp272ScopeContractFoundationDescriptor,
  createEmailDmsCoreCp273ModelStorageSliceCaseSet,
  createEmailDmsCoreCp273ModelStorageSliceDescriptor,
  createEmailDmsCoreCp274ModelBindingSliceCaseSet,
  createEmailDmsCoreCp274ModelBindingSliceDescriptor,
  createEmailDmsCoreCp275P01CloseoutP02ServiceFoundationCaseSet,
  createEmailDmsCoreCp275P01CloseoutP02ServiceFoundationDescriptor,
  createEmailDmsCoreCp276ServiceSliceCaseSet,
  createEmailDmsCoreCp276ServiceSliceDescriptor,
  createEmailDmsCoreCp277ServiceBindingSliceCaseSet,
  createEmailDmsCoreCp277ServiceBindingSliceDescriptor,
  createEmailDmsCoreCp278ServiceFixtureTailCaseSet,
  createEmailDmsCoreCp278ServiceFixtureTailDescriptor,
  createEmailDmsCoreCp279ServiceGoldenMidCaseSet,
  createEmailDmsCoreCp279ServiceGoldenMidDescriptor,
  createEmailDmsCoreCp280GoldenHermesSliceCaseSet,
  createEmailDmsCoreCp280GoldenHermesSliceDescriptor,
  createEmailDmsCoreCp281P02CloseoutP03InterfaceFoundationCaseSet,
  createEmailDmsCoreCp281P02CloseoutP03InterfaceFoundationDescriptor,
  createEmailDmsCoreCp282P03CloseoutP04UiFoundationCaseSet,
  createEmailDmsCoreCp282P03CloseoutP04UiFoundationDescriptor,
  createEmailDmsCoreCp283UiWorkflowSliceCaseSet,
  createEmailDmsCoreCp283UiWorkflowSliceDescriptor,
  createEmailDmsCoreCp284UiBindingTailCaseSet,
  createEmailDmsCoreCp284UiBindingTailDescriptor,
  createEmailDmsCoreCp285P04CloseoutP05FixtureFoundationCaseSet,
  createEmailDmsCoreCp285P04CloseoutP05FixtureFoundationDescriptor,
  createEmailDmsCoreCp286FixtureSliceCaseSet,
  createEmailDmsCoreCp286FixtureSliceDescriptor,
  createEmailDmsCoreCp287P05CloseoutP06PermissionFoundationCaseSet,
  createEmailDmsCoreCp287P05CloseoutP06PermissionFoundationDescriptor,
  createEmailDmsCoreCp288PermissionSliceCaseSet,
  createEmailDmsCoreCp288PermissionSliceDescriptor,
  createEmailDmsCoreCp289PermissionBindingSliceCaseSet,
  createEmailDmsCoreCp289PermissionBindingSliceDescriptor,
  createEmailDmsCoreCp290P06CloseoutP07FailureFoundationCaseSet,
  createEmailDmsCoreCp290P06CloseoutP07FailureFoundationDescriptor,
  createEmailDmsCoreCp291FailureSliceCaseSet,
  createEmailDmsCoreCp291FailureSliceDescriptor,
  createEmailDmsCoreCp292FailureBindingSliceCaseSet,
  createEmailDmsCoreCp292FailureBindingSliceDescriptor,
  createEmailDmsCoreCp293P07CloseoutP08HermesFoundationCaseSet,
  createEmailDmsCoreCp293P07CloseoutP08HermesFoundationDescriptor,
  createEmailDmsCoreCp294HermesSliceCaseSet,
  createEmailDmsCoreCp294HermesSliceDescriptor,
  createEmailDmsCoreCp295HermesBindingSliceCaseSet,
  createEmailDmsCoreCp295HermesBindingSliceDescriptor,
  createEmailDmsCoreCp296P08CloseoutP09ReviewFoundationCaseSet,
  createEmailDmsCoreCp296P08CloseoutP09ReviewFoundationDescriptor,
  createEmailDmsCoreCp297ReviewSliceCaseSet,
  createEmailDmsCoreCp297ReviewSliceDescriptor,
  createEmailDmsCoreCp298P09CloseoutCaseSet,
  createEmailDmsCoreCp298P09CloseoutDescriptor,
  emailDmsCoreRowKey,
} from "./service.js";

function countBy(units, field) {
  const counts = {};
  for (const unit of units) {
    const key = unit?.[field];
    if (key === undefined || key === null) continue;
    counts[key] = (counts[key] ?? 0) + 1;
  }
  return Object.freeze(counts);
}

function freezeCp272Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP272_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP272_NO_WRITE_ATTESTATION,
  });
}

function freezeCp273Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP273_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP273_NO_WRITE_ATTESTATION,
  });
}

function freezeCp274Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP274_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP274_NO_WRITE_ATTESTATION,
  });
}

function freezeCp275Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP275_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP275_NO_WRITE_ATTESTATION,
  });
}

function freezeCp276Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP276_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP276_NO_WRITE_ATTESTATION,
  });
}

function freezeCp277Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP277_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP277_NO_WRITE_ATTESTATION,
  });
}

function freezeCp278Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP278_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP278_NO_WRITE_ATTESTATION,
  });
}

function freezeCp279Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP279_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP279_NO_WRITE_ATTESTATION,
  });
}

function freezeCp280Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP280_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP280_NO_WRITE_ATTESTATION,
  });
}

function freezeCp281Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP281_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP281_NO_WRITE_ATTESTATION,
  });
}

function freezeCp282Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP282_NO_WRITE_ATTESTATION,
  });
}

function freezeCp283Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP283_NO_WRITE_ATTESTATION,
  });
}

function freezeCp284Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP284_NO_WRITE_ATTESTATION,
  });
}

function freezeCp285Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP285_NO_WRITE_ATTESTATION,
  });
}

function freezeCp286Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP286_NO_WRITE_ATTESTATION,
  });
}

function freezeCp287Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP287_NO_WRITE_ATTESTATION,
  });
}

function freezeCp288Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP288_NO_WRITE_ATTESTATION,
  });
}

function freezeCp289Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP289_NO_WRITE_ATTESTATION,
  });
}

function freezeCp290Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP290_NO_WRITE_ATTESTATION,
  });
}

function freezeCp291Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP291_NO_WRITE_ATTESTATION,
  });
}

function freezeCp292Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP292_NO_WRITE_ATTESTATION,
  });
}

function freezeCp293Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP293_NO_WRITE_ATTESTATION,
  });
}

function freezeCp294Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP294_NO_WRITE_ATTESTATION,
  });
}

function freezeCp295Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP295_NO_WRITE_ATTESTATION,
  });
}

function freezeCp296Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP296_NO_WRITE_ATTESTATION,
  });
}

function freezeCp297Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP297_NO_WRITE_ATTESTATION,
  });
}

function freezeCp298Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    no_write_attestation: EMAIL_DMS_CORE_CP298_NO_WRITE_ATTESTATION,
  });
}

export function createEmailDmsCoreCp272CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp272Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp272Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-272 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP272_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-272");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP272_PACK_BINDING.risk_class) errors.push("CP00-272 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP272_PACK_BINDING.unit_count) errors.push("CP00-272 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP272_PACK_BINDING.first_unit_id) errors.push("CP00-272 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP272_PACK_BINDING.last_unit_id) errors.push("CP00-272 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-272 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-272 must only include RP08 units");
  const summary = createEmailDmsCoreCp272CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP272_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-272 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP272_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-272 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP272_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-272 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP272_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-272 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP272_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-272 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-272 ${microId} missing row ${title}`);
    }
  }
  return freezeCp272Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp272ScopeContractFoundationDescriptor(
  descriptor = createEmailDmsCoreCp272ScopeContractFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.scope_contract_foundation_case_set ?? createEmailDmsCoreCp272ScopeContractFoundationCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp272ScopeContractFoundationDescriptor") errors.push("CP00-272 descriptor type drift");
  if (descriptor.program_contract?.program_id !== "RP08") errors.push("CP00-272 program drift");
  if (descriptor.program_contract?.upstream_program_id !== "RP07") errors.push("CP00-272 upstream program drift");
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP272_REQUIREMENTS.required_section_rows).length) errors.push("CP00-272 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP272_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-272 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-272 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-272 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-272 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-272 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-272 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-272 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const scopeSection of ["RP08.P00.M00", "RP08.P00.M01", "RP08.P00.M10"]) {
    const rows = sections[scopeSection]?.rows ?? {};
    if (rows.non_goal_boundary?.email_runtime_opened !== false) errors.push(`CP00-272 ${scopeSection} must not open email runtime`);
    if (rows.non_goal_boundary?.office_native_runtime_opened !== false) errors.push(`CP00-272 ${scopeSection} must not open Office native runtime`);
    if (rows.non_goal_boundary?.sync_runtime_opened !== false) errors.push(`CP00-272 ${scopeSection} must not open sync runtime`);
    if (rows.non_goal_boundary?.filing_runtime_opened !== false) errors.push(`CP00-272 ${scopeSection} must not open filing runtime`);
    if (rows.acceptance_gate_definition?.hermes_gate !== "H08") errors.push(`CP00-272 ${scopeSection} Hermes gate drift`);
    if (rows.acceptance_gate_definition?.claude_gate !== "C08") errors.push(`CP00-272 ${scopeSection} Claude gate drift`);
  }
  for (const fullSection of ["RP08.P00.M03", "RP08.P00.M05", "RP08.P00.M07"]) {
    const rows = sections[fullSection]?.rows ?? {};
    if (rows.permission_baseline_note?.deny_over_allow_enforced !== true) errors.push(`CP00-272 ${fullSection} deny-over-allow drift`);
    if (rows.permission_baseline_note?.cross_tenant_access_allowed !== false) errors.push(`CP00-272 ${fullSection} must not allow cross-tenant access`);
    if (rows.audit_baseline_note?.audit_event_body_included !== false) errors.push(`CP00-272 ${fullSection} must not expose audit bodies`);
    if (rows.synthetic_data_policy?.real_client_data_loaded !== false) errors.push(`CP00-272 ${fullSection} must not load real data`);
    if (rows.blocked_claim_rule?.blocked_claim_detail_included !== false) errors.push(`CP00-272 ${fullSection} must not expose blocked-claim detail`);
    if (rows.claude_review_prompts?.claude_final_approval_claimed !== false) errors.push(`CP00-272 ${fullSection} must not claim Claude final approval`);
    if (rows.human_approval_note?.human_approval_route_required_before_runtime !== true) errors.push(`CP00-272 ${fullSection} human approval boundary missing`);
    if (rows.command_matrix?.executes_command_runtime !== false) errors.push(`CP00-272 ${fullSection} must not execute command runtime`);
  }
  for (const modelSection of ["RP08.P01.M00", "RP08.P01.M01", "RP08.P01.M02"]) {
    const rows = sections[modelSection]?.rows ?? {};
    if (rows.tenant_scope_field?.cross_tenant_access_allowed !== false) errors.push(`CP00-272 ${modelSection} tenant scope drift`);
    if (rows.matter_trace_reference?.matter_trace_required !== true) errors.push(`CP00-272 ${modelSection} matter trace drift`);
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-272 ${modelSection} must not write state transitions`);
    }
  }
  if (EMAIL_DMS_CORE_CP272_NO_WRITE_ATTESTATION.dispatches_email_runtime !== false) errors.push("CP00-272 must not dispatch email runtime");
  if (EMAIL_DMS_CORE_CP272_NO_WRITE_ATTESTATION.dispatches_office_native_runtime !== false) errors.push("CP00-272 must not dispatch Office native runtime");
  if (EMAIL_DMS_CORE_CP272_NO_WRITE_ATTESTATION.dispatches_sync_runtime !== false) errors.push("CP00-272 must not dispatch sync runtime");
  if (EMAIL_DMS_CORE_CP272_NO_WRITE_ATTESTATION.dispatches_filing_runtime !== false) errors.push("CP00-272 must not dispatch filing runtime");
  if (EMAIL_DMS_CORE_CP272_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-272 must not promote Claude");
  if (EMAIL_DMS_CORE_CP272_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-272 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-272 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-272 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP272_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP273_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP274_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP275_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP276_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP277_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP278_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP279_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP280_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP281_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-272 contract current_pack drift");
  }
  if (
    contractProjection?.scope_contract_foundation_descriptor?.descriptor &&
    contractProjection.scope_contract_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-272 contract scope_contract_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP272_PACK_BINDING.next_pack_id) errors.push("CP00-272 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP272_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-272 next subphase drift");
  }
  return freezeCp272Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp272HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp272ScopeContractFoundationDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp272Coverage(planPack);
  const foundation = validateEmailDmsCoreCp272ScopeContractFoundationDescriptor(descriptor, contractProjection);
  return freezeCp272Validation({
    evidence_packet: "H08.CP00-272.email_dms_core_scope_contract_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP272_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    scope_contract_foundation_valid: foundation.valid,
    no_real_data: true,
    source_search_core_pack_id: EMAIL_DMS_CORE_CP272_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP272_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP272_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP272_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createEmailDmsCoreCp272ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp272Coverage(planPack);
  const foundation = validateEmailDmsCoreCp272ScopeContractFoundationDescriptor(createEmailDmsCoreCp272ScopeContractFoundationDescriptor(), {});
  return freezeCp272Validation({
    review_packet: "C08.CP00-272.email_dms_core_scope_contract_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP272_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP272_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    scope_contract_foundation_valid: foundation.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP272_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-272 cover exactly the 150 planned RP08 units from RP08.P00.M00.S01 through RP08.P01.M02.S08?",
      "Do the fourteen descriptor sections (eleven RP08.P00 scope-and-contract micros and three RP08.P01 model/storage foundation micros) cover every planned row title as descriptor-only rows, including scope inventories, acceptance gates (H08/C08), non-goal boundaries, target file maps, baseline notes, synthetic data policies, risk registers, blocked-claim rules, Hermes preflight fields, Claude review prompts, human approval notes, dependency lists, command matrices, receipt shapes, package layout, entity identifiers, tenant scope, matter traces, lifecycle enums, and field registries?",
      "Does CP00-272 avoid email/Office-native/sync/filing runtime dispatch, permission runtime evaluation, audit event writes, state writes, object storage, command runtime execution, Hermes runtime receipts, raw payloads, permission decisions, audit bodies, blocked-claim details, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp272CloseoutHandoff() {
  return freezeCp272Validation({
    handoff_id: "CP00-272-to-CP00-273",
    from_pack_id: EMAIL_DMS_CORE_CP272_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP272_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP272_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP272_PACK_BINDING.range,
    open_scope: "Continue RP08.P01.M02.S09 onward with the remaining model/storage foundation rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP272_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp273CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp273Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp273Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-273 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP273_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-273");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP273_PACK_BINDING.risk_class) errors.push("CP00-273 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP273_PACK_BINDING.unit_count) errors.push("CP00-273 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP273_PACK_BINDING.first_unit_id) errors.push("CP00-273 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP273_PACK_BINDING.last_unit_id) errors.push("CP00-273 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-273 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-273 must only include RP08 units");
  const summary = createEmailDmsCoreCp273CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP273_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-273 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP273_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-273 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP273_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-273 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP273_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-273 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP273_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-273 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-273 ${microId} missing row ${title}`);
    }
  }
  return freezeCp273Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp273ModelStorageSliceDescriptor(
  descriptor = createEmailDmsCoreCp273ModelStorageSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.model_storage_slice_case_set ?? createEmailDmsCoreCp273ModelStorageSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp273ModelStorageSliceDescriptor") errors.push("CP00-273 descriptor type drift");
  if (descriptor.source_scope_contract_foundation_descriptor !== "EmailDmsCoreCp272ScopeContractFoundationDescriptor") {
    errors.push("CP00-273 source scope contract foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP273_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-273 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-273 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-273 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-273 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-273 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-273 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-273 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP273_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-273 ${microId} tenant scope drift`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-273 ${microId} matter trace drift`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-273 ${microId} must not write state transitions`);
    }
    if (rows.validation_helper && rows.validation_helper.validation_error_detail_included !== false) {
      errors.push(`CP00-273 ${microId} must not expose validation error detail`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-273 ${microId} must not load real data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-273 ${microId} must not execute test runtime`);
    }
    if (rows.ownership_drift_test && rows.ownership_drift_test.ownership_drift_detected !== false) {
      errors.push(`CP00-273 ${microId} ownership drift detected`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-273 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-273 ${microId} must not claim Claude final approval`);
    }
  }
  if (EMAIL_DMS_CORE_CP273_NO_WRITE_ATTESTATION.ownership_drift_detected !== false) errors.push("CP00-273 ownership drift attestation drift");
  if (EMAIL_DMS_CORE_CP273_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-273 must not promote Claude");
  if (EMAIL_DMS_CORE_CP273_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-273 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-273 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-273 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP273_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP274_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP275_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP276_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP277_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP278_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP279_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP280_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP281_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-273 contract current_pack drift");
  }
  if (
    contractProjection?.model_storage_slice_descriptor?.descriptor &&
    contractProjection.model_storage_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-273 contract model_storage_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP273_PACK_BINDING.next_pack_id) errors.push("CP00-273 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP273_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-273 next subphase drift");
  }
  return freezeCp273Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp273HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp273ModelStorageSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp273Coverage(planPack);
  const slice = validateEmailDmsCoreCp273ModelStorageSliceDescriptor(descriptor, contractProjection);
  return freezeCp273Validation({
    evidence_packet: "H08.CP00-273.email_dms_core_model_storage_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP273_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    model_storage_slice_valid: slice.valid,
    no_real_data: true,
    source_scope_contract_foundation_pack_id: EMAIL_DMS_CORE_CP273_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP273_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP273_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP273_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp273ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp273Coverage(planPack);
  const slice = validateEmailDmsCoreCp273ModelStorageSliceDescriptor(createEmailDmsCoreCp273ModelStorageSliceDescriptor(), {});
  return freezeCp273Validation({
    review_packet: "C08.CP00-273.email_dms_core_model_storage_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP273_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP273_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    model_storage_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP273_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-273 cover exactly the 40 planned RP08 units from RP08.P01.M02.S09 through RP08.P01.M04.S06?",
      "Do the three descriptor sections (M02 type/shape tail with validation helper through closeout handoff, M03 primary model/storage cycle with documentation entry and index export check, M04 secondary workflow head) cover every planned row title as descriptor-only rows, with the shared model guards applied across every section that carries them?",
      "Does CP00-273 avoid email/Office-native/sync/filing runtime dispatch, ownership drift, unit-test runtime paths, state writes, object storage, command runtime execution, Hermes runtime receipts, raw payloads, validation error details, fixture payloads, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp273CloseoutHandoff() {
  return freezeCp273Validation({
    handoff_id: "CP00-273-to-CP00-274",
    from_pack_id: EMAIL_DMS_CORE_CP273_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP273_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP273_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP273_PACK_BINDING.range,
    open_scope: "Continue RP08.P01.M04.S07 onward with the remaining model/storage rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP273_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp274CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp274Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp274Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-274 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP274_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-274");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP274_PACK_BINDING.risk_class) errors.push("CP00-274 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP274_PACK_BINDING.unit_count) errors.push("CP00-274 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP274_PACK_BINDING.first_unit_id) errors.push("CP00-274 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP274_PACK_BINDING.last_unit_id) errors.push("CP00-274 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-274 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-274 must only include RP08 units");
  const summary = createEmailDmsCoreCp274CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP274_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-274 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP274_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-274 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP274_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-274 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP274_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-274 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP274_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-274 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-274 ${microId} missing row ${title}`);
    }
  }
  return freezeCp274Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp274ModelBindingSliceDescriptor(
  descriptor = createEmailDmsCoreCp274ModelBindingSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.model_binding_slice_case_set ?? createEmailDmsCoreCp274ModelBindingSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp274ModelBindingSliceDescriptor") errors.push("CP00-274 descriptor type drift");
  if (descriptor.source_model_storage_slice_descriptor !== "EmailDmsCoreCp273ModelStorageSliceDescriptor") {
    errors.push("CP00-274 source model storage slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP274_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-274 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-274 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-274 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-274 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-274 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-274 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-274 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP274_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-274 ${microId} tenant scope drift`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-274 ${microId} matter trace drift`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-274 ${microId} must not write state transitions`);
    }
    if (rows.validation_helper && rows.validation_helper.validation_error_detail_included !== false) {
      errors.push(`CP00-274 ${microId} must not expose validation error detail`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-274 ${microId} must not load real data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-274 ${microId} must not execute test runtime`);
    }
    if (rows.ownership_drift_test && rows.ownership_drift_test.ownership_drift_detected !== false) {
      errors.push(`CP00-274 ${microId} ownership drift detected`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-274 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-274 ${microId} must not claim Claude final approval`);
    }
  }
  if (EMAIL_DMS_CORE_CP274_NO_WRITE_ATTESTATION.ownership_drift_detected !== false) errors.push("CP00-274 ownership drift attestation drift");
  if (EMAIL_DMS_CORE_CP274_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-274 must not promote Claude");
  if (EMAIL_DMS_CORE_CP274_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-274 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-274 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-274 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP274_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP275_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP276_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP277_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP278_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP279_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP280_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP281_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-274 contract current_pack drift");
  }
  if (
    contractProjection?.model_binding_slice_descriptor?.descriptor &&
    contractProjection.model_binding_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-274 contract model_binding_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP274_PACK_BINDING.next_pack_id) errors.push("CP00-274 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP274_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-274 next subphase drift");
  }
  return freezeCp274Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp274HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp274ModelBindingSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp274Coverage(planPack);
  const slice = validateEmailDmsCoreCp274ModelBindingSliceDescriptor(descriptor, contractProjection);
  return freezeCp274Validation({
    evidence_packet: "H08.CP00-274.email_dms_core_model_binding_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP274_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    model_binding_slice_valid: slice.valid,
    no_real_data: true,
    source_model_storage_slice_pack_id: EMAIL_DMS_CORE_CP274_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP274_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP274_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP274_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp274ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp274Coverage(planPack);
  const slice = validateEmailDmsCoreCp274ModelBindingSliceDescriptor(createEmailDmsCoreCp274ModelBindingSliceDescriptor(), {});
  return freezeCp274Validation({
    review_packet: "C08.CP00-274.email_dms_core_model_binding_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP274_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP274_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    model_binding_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP274_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-274 cover exactly the 40 planned RP08 units from RP08.P01.M04.S07 through RP08.P01.M06.S04?",
      "Do the three descriptor sections (M04 secondary workflow tail, M05 permission/audit binding model cycle with documentation entry and index export check, M06 synthetic fixture head) cover every planned row title as descriptor-only rows, with the shared model guards applied across every section that carries them?",
      "Does CP00-274 avoid email/Office-native/sync/filing runtime dispatch, ownership drift, unit-test runtime paths, state writes, object storage, command runtime execution, Hermes runtime receipts, raw payloads, validation error details, fixture payloads, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp274CloseoutHandoff() {
  return freezeCp274Validation({
    handoff_id: "CP00-274-to-CP00-275",
    from_pack_id: EMAIL_DMS_CORE_CP274_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP274_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP274_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP274_PACK_BINDING.range,
    open_scope: "Continue RP08.P01.M06.S05 onward with the remaining model fixture rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP274_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp275CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp275Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp275Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-275 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP275_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-275");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP275_PACK_BINDING.risk_class) errors.push("CP00-275 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP275_PACK_BINDING.unit_count) errors.push("CP00-275 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP275_PACK_BINDING.first_unit_id) errors.push("CP00-275 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP275_PACK_BINDING.last_unit_id) errors.push("CP00-275 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-275 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-275 must only include RP08 units");
  const summary = createEmailDmsCoreCp275CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP275_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-275 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP275_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-275 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP275_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-275 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP275_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-275 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP275_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-275 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-275 ${microId} missing row ${title}`);
    }
  }
  return freezeCp275Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp275P01CloseoutP02ServiceFoundationDescriptor(
  descriptor = createEmailDmsCoreCp275P01CloseoutP02ServiceFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p01_closeout_p02_service_foundation_case_set ?? createEmailDmsCoreCp275P01CloseoutP02ServiceFoundationCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp275P01CloseoutP02ServiceFoundationDescriptor") errors.push("CP00-275 descriptor type drift");
  if (descriptor.source_model_binding_slice_descriptor !== "EmailDmsCoreCp274ModelBindingSliceDescriptor") {
    errors.push("CP00-275 source model binding slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP275_REQUIREMENTS.required_section_rows).length) errors.push("CP00-275 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP275_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-275 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-275 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-275 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-275 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-275 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-275 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-275 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP275_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-275 ${microId} tenant scope drift`);
    }
    if (rows.tenant_boundary_precheck && rows.tenant_boundary_precheck.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-275 ${microId} must not allow cross-tenant access`);
    }
    if (rows.permission_precheck && rows.permission_precheck.deny_over_allow_enforced !== true) {
      errors.push(`CP00-275 ${microId} deny-over-allow drift`);
    }
    if (rows.audit_hint_precheck && rows.audit_hint_precheck.audit_hint_detail_included !== false) {
      errors.push(`CP00-275 ${microId} must not expose audit hints`);
    }
    if (rows.state_transition_enforcement && rows.state_transition_enforcement.writes_state_transition !== false) {
      errors.push(`CP00-275 ${microId} must not write state transitions`);
    }
    if (rows.lock_acquisition_rule && rows.lock_acquisition_rule.acquires_runtime_lock !== false) {
      errors.push(`CP00-275 ${microId} must not acquire locks`);
    }
    if (rows.persistence_boundary && rows.persistence_boundary.creates_database_rows !== false) {
      errors.push(`CP00-275 ${microId} must not create database rows`);
    }
    if (rows.validation_error_mapping && rows.validation_error_mapping.validation_error_detail_included !== false) {
      errors.push(`CP00-275 ${microId} must not expose validation error detail`);
    }
    if (rows.blocked_claim_output && rows.blocked_claim_output.blocked_claim_detail_included !== false) {
      errors.push(`CP00-275 ${microId} must not expose blocked claims`);
    }
    if (rows.rollback_behavior && rows.rollback_behavior.performs_rollback_runtime !== false) {
      errors.push(`CP00-275 ${microId} must not perform rollback runtime`);
    }
    if (rows.retry_behavior && rows.retry_behavior.performs_retry_runtime !== false) {
      errors.push(`CP00-275 ${microId} must not perform retry runtime`);
    }
    if (rows.ownership_drift_test && rows.ownership_drift_test.ownership_drift_detected !== false) {
      errors.push(`CP00-275 ${microId} ownership drift detected`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-275 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-275 ${microId} must not claim Claude final approval`);
    }
  }
  if (EMAIL_DMS_CORE_CP275_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-275 must not promote Claude");
  if (EMAIL_DMS_CORE_CP275_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-275 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-275 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-275 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP275_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP276_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP277_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP278_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP279_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP280_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP281_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-275 contract current_pack drift");
  }
  if (
    contractProjection?.p01_closeout_p02_service_foundation_descriptor?.descriptor &&
    contractProjection.p01_closeout_p02_service_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-275 contract p01_closeout_p02_service_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP275_PACK_BINDING.next_pack_id) errors.push("CP00-275 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP275_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-275 next subphase drift");
  }
  return freezeCp275Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp275HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp275P01CloseoutP02ServiceFoundationDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp275Coverage(planPack);
  const foundation = validateEmailDmsCoreCp275P01CloseoutP02ServiceFoundationDescriptor(descriptor, contractProjection);
  return freezeCp275Validation({
    evidence_packet: "H08.CP00-275.email_dms_core_p01_closeout_p02_service_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP275_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p01_closeout_p02_service_foundation_valid: foundation.valid,
    no_real_data: true,
    source_model_binding_slice_pack_id: EMAIL_DMS_CORE_CP275_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP275_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP275_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP275_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createEmailDmsCoreCp275ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp275Coverage(planPack);
  const foundation = validateEmailDmsCoreCp275P01CloseoutP02ServiceFoundationDescriptor(createEmailDmsCoreCp275P01CloseoutP02ServiceFoundationDescriptor(), {});
  return freezeCp275Validation({
    review_packet: "C08.CP00-275.email_dms_core_p01_closeout_p02_service_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP275_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP275_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p01_closeout_p02_service_foundation_valid: foundation.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP275_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-275 cover exactly the 150 planned RP08 units from RP08.P01.M06.S05 through RP08.P02.M02.S22?",
      "Do the eight descriptor sections (five P01 model micros and three P02 service foundation micros) cover every planned row title as descriptor-only rows, including service entrypoint contracts, request normalization, tenant/matter/permission/audit prechecks, primary and secondary paths, state transition enforcement, idempotency key handling, lock acquisition rules, persistence boundaries, validation error mapping, review/approval routing, blocked-claim outputs, rollback/retry behavior, and happy/denied/review/integration test rows?",
      "Does CP00-275 avoid email/Office-native/sync/filing runtime dispatch, review/approval route runtime, rollback/retry runtime, lock acquisition, idempotency persistence, state writes, object storage, command runtime execution, Hermes runtime receipts, raw payloads, lock tokens, blocked-claim details, validation error details, permission decisions, audit hints, fixture payloads, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp275CloseoutHandoff() {
  return freezeCp275Validation({
    handoff_id: "CP00-275-to-CP00-276",
    from_pack_id: EMAIL_DMS_CORE_CP275_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP275_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP275_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP275_PACK_BINDING.range,
    open_scope: "RP08.P01 descriptor scope is closed; continue RP08.P02.M03.S01 onward with the remaining service rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP275_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp276CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp276Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp276Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-276 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP276_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-276");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP276_PACK_BINDING.risk_class) errors.push("CP00-276 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP276_PACK_BINDING.unit_count) errors.push("CP00-276 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP276_PACK_BINDING.first_unit_id) errors.push("CP00-276 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP276_PACK_BINDING.last_unit_id) errors.push("CP00-276 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-276 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-276 must only include RP08 units");
  const summary = createEmailDmsCoreCp276CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP276_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-276 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP276_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-276 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP276_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-276 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP276_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-276 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP276_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-276 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-276 ${microId} missing row ${title}`);
    }
  }
  return freezeCp276Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp276ServiceSliceDescriptor(
  descriptor = createEmailDmsCoreCp276ServiceSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.service_slice_case_set ?? createEmailDmsCoreCp276ServiceSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp276ServiceSliceDescriptor") errors.push("CP00-276 descriptor type drift");
  if (descriptor.source_p01_closeout_p02_service_foundation_descriptor !== "EmailDmsCoreCp275P01CloseoutP02ServiceFoundationDescriptor") {
    errors.push("CP00-276 source p01 closeout p02 service foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP276_REQUIREMENTS.required_section_rows).length) errors.push("CP00-276 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP276_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-276 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-276 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-276 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-276 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-276 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-276 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-276 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP276_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_boundary_precheck && rows.tenant_boundary_precheck.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-276 ${microId} must not allow cross-tenant access`);
    }
    if (rows.matter_trace_precheck && rows.matter_trace_precheck.matter_trace_required !== true) {
      errors.push(`CP00-276 ${microId} matter trace drift`);
    }
    if (rows.permission_precheck && rows.permission_precheck.deny_over_allow_enforced !== true) {
      errors.push(`CP00-276 ${microId} deny-over-allow drift`);
    }
    if (rows.audit_hint_precheck && rows.audit_hint_precheck.audit_hint_detail_included !== false) {
      errors.push(`CP00-276 ${microId} must not expose audit hints`);
    }
    if (rows.state_transition_enforcement && rows.state_transition_enforcement.writes_state_transition !== false) {
      errors.push(`CP00-276 ${microId} must not write state transitions`);
    }
    if (rows.idempotency_key_handling && rows.idempotency_key_handling.persists_idempotency_key !== false) {
      errors.push(`CP00-276 ${microId} must not persist idempotency keys`);
    }
    if (rows.lock_acquisition_rule && rows.lock_acquisition_rule.acquires_runtime_lock !== false) {
      errors.push(`CP00-276 ${microId} must not acquire locks`);
    }
    if (rows.persistence_boundary && rows.persistence_boundary.creates_database_rows !== false) {
      errors.push(`CP00-276 ${microId} must not create database rows`);
    }
    if (rows.validation_error_mapping && rows.validation_error_mapping.validation_error_detail_included !== false) {
      errors.push(`CP00-276 ${microId} must not expose validation error detail`);
    }
    if (rows.blocked_claim_output && rows.blocked_claim_output.blocked_claim_detail_included !== false) {
      errors.push(`CP00-276 ${microId} must not expose blocked claims`);
    }
    if (rows.rollback_behavior && rows.rollback_behavior.performs_rollback_runtime !== false) {
      errors.push(`CP00-276 ${microId} must not perform rollback runtime`);
    }
    if (rows.retry_behavior && rows.retry_behavior.performs_retry_runtime !== false) {
      errors.push(`CP00-276 ${microId} must not perform retry runtime`);
    }
    if (rows.integration_smoke_case && rows.integration_smoke_case.dispatches_integration_smoke_runtime !== false) {
      errors.push(`CP00-276 ${microId} must not dispatch integration smoke runtime`);
    }
  }
  if (EMAIL_DMS_CORE_CP276_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-276 must not promote Claude");
  if (EMAIL_DMS_CORE_CP276_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-276 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-276 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-276 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP276_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP277_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP278_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP279_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP280_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP281_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-276 contract current_pack drift");
  }
  if (
    contractProjection?.service_slice_descriptor?.descriptor &&
    contractProjection.service_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-276 contract service_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP276_PACK_BINDING.next_pack_id) errors.push("CP00-276 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP276_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-276 next subphase drift");
  }
  return freezeCp276Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp276HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp276ServiceSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp276Coverage(planPack);
  const slice = validateEmailDmsCoreCp276ServiceSliceDescriptor(descriptor, contractProjection);
  return freezeCp276Validation({
    evidence_packet: "H08.CP00-276.email_dms_core_service_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP276_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    service_slice_valid: slice.valid,
    no_real_data: true,
    source_p01_closeout_p02_service_foundation_pack_id: EMAIL_DMS_CORE_CP276_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP276_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP276_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP276_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp276ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp276Coverage(planPack);
  const slice = validateEmailDmsCoreCp276ServiceSliceDescriptor(createEmailDmsCoreCp276ServiceSliceDescriptor(), {});
  return freezeCp276Validation({
    review_packet: "C08.CP00-276.email_dms_core_service_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP276_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP276_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    service_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP276_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-276 cover exactly the 40 planned RP08 units from RP08.P02.M03.S01 through RP08.P02.M04.S18?",
      "Do the two descriptor sections (M03 primary implementation cycle with happy/denied/review/integration test rows, M04 secondary workflow slice through retry behavior) cover every planned row title as descriptor-only rows, with the shared service guards applied across every section that carries them?",
      "Does CP00-276 avoid email/Office-native/sync/filing runtime dispatch, review/approval route runtime, rollback/retry runtime, lock acquisition, idempotency persistence, state writes, object storage, command runtime execution, Hermes runtime receipts, raw payloads, lock tokens, blocked-claim details, validation error details, permission decisions, audit hints, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp276CloseoutHandoff() {
  return freezeCp276Validation({
    handoff_id: "CP00-276-to-CP00-277",
    from_pack_id: EMAIL_DMS_CORE_CP276_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP276_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP276_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP276_PACK_BINDING.range,
    open_scope: "Continue RP08.P02.M04.S19 onward with the remaining service workflow rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP276_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp277CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp277Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp277Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-277 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP277_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-277");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP277_PACK_BINDING.risk_class) errors.push("CP00-277 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP277_PACK_BINDING.unit_count) errors.push("CP00-277 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP277_PACK_BINDING.first_unit_id) errors.push("CP00-277 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP277_PACK_BINDING.last_unit_id) errors.push("CP00-277 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-277 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-277 must only include RP08 units");
  const summary = createEmailDmsCoreCp277CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP277_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-277 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP277_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-277 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP277_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-277 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP277_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-277 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP277_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-277 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-277 ${microId} missing row ${title}`);
    }
  }
  return freezeCp277Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp277ServiceBindingSliceDescriptor(
  descriptor = createEmailDmsCoreCp277ServiceBindingSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.service_binding_slice_case_set ?? createEmailDmsCoreCp277ServiceBindingSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp277ServiceBindingSliceDescriptor") errors.push("CP00-277 descriptor type drift");
  if (descriptor.source_service_slice_descriptor !== "EmailDmsCoreCp276ServiceSliceDescriptor") {
    errors.push("CP00-277 source service slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP277_REQUIREMENTS.required_section_rows).length) errors.push("CP00-277 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP277_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-277 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-277 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-277 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-277 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-277 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-277 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-277 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP277_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_boundary_precheck && rows.tenant_boundary_precheck.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-277 ${microId} must not allow cross-tenant access`);
    }
    if (rows.matter_trace_precheck && rows.matter_trace_precheck.matter_trace_required !== true) {
      errors.push(`CP00-277 ${microId} matter trace drift`);
    }
    if (rows.permission_precheck && rows.permission_precheck.deny_over_allow_enforced !== true) {
      errors.push(`CP00-277 ${microId} deny-over-allow drift`);
    }
    if (rows.audit_hint_precheck && rows.audit_hint_precheck.audit_hint_detail_included !== false) {
      errors.push(`CP00-277 ${microId} must not expose audit hints`);
    }
    if (rows.state_transition_enforcement && rows.state_transition_enforcement.writes_state_transition !== false) {
      errors.push(`CP00-277 ${microId} must not write state transitions`);
    }
    if (rows.idempotency_key_handling && rows.idempotency_key_handling.persists_idempotency_key !== false) {
      errors.push(`CP00-277 ${microId} must not persist idempotency keys`);
    }
    if (rows.lock_acquisition_rule && rows.lock_acquisition_rule.acquires_runtime_lock !== false) {
      errors.push(`CP00-277 ${microId} must not acquire locks`);
    }
    if (rows.persistence_boundary && rows.persistence_boundary.creates_database_rows !== false) {
      errors.push(`CP00-277 ${microId} must not create database rows`);
    }
    if (rows.validation_error_mapping && rows.validation_error_mapping.validation_error_detail_included !== false) {
      errors.push(`CP00-277 ${microId} must not expose validation error detail`);
    }
    if (rows.blocked_claim_output && rows.blocked_claim_output.blocked_claim_detail_included !== false) {
      errors.push(`CP00-277 ${microId} must not expose blocked claims`);
    }
    if (rows.rollback_behavior && rows.rollback_behavior.performs_rollback_runtime !== false) {
      errors.push(`CP00-277 ${microId} must not perform rollback runtime`);
    }
    if (rows.retry_behavior && rows.retry_behavior.performs_retry_runtime !== false) {
      errors.push(`CP00-277 ${microId} must not perform retry runtime`);
    }
    if (rows.unit_test_happy_path && rows.unit_test_happy_path.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-277 ${microId} must not execute test runtime`);
    }
    if (rows.integration_smoke_case && rows.integration_smoke_case.dispatches_integration_smoke_runtime !== false) {
      errors.push(`CP00-277 ${microId} must not dispatch integration smoke runtime`);
    }
  }
  if (EMAIL_DMS_CORE_CP277_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-277 must not promote Claude");
  if (EMAIL_DMS_CORE_CP277_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-277 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-277 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-277 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP277_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP278_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP279_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP280_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP281_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-277 contract current_pack drift");
  }
  if (
    contractProjection?.service_binding_slice_descriptor?.descriptor &&
    contractProjection.service_binding_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-277 contract service_binding_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP277_PACK_BINDING.next_pack_id) errors.push("CP00-277 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP277_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-277 next subphase drift");
  }
  return freezeCp277Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp277HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp277ServiceBindingSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp277Coverage(planPack);
  const slice = validateEmailDmsCoreCp277ServiceBindingSliceDescriptor(descriptor, contractProjection);
  return freezeCp277Validation({
    evidence_packet: "H08.CP00-277.email_dms_core_service_binding_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP277_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    service_binding_slice_valid: slice.valid,
    no_real_data: true,
    source_service_slice_pack_id: EMAIL_DMS_CORE_CP277_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP277_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP277_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP277_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp277ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp277Coverage(planPack);
  const slice = validateEmailDmsCoreCp277ServiceBindingSliceDescriptor(createEmailDmsCoreCp277ServiceBindingSliceDescriptor(), {});
  return freezeCp277Validation({
    review_packet: "C08.CP00-277.email_dms_core_service_binding_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP277_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP277_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    service_binding_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP277_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-277 cover exactly the 40 planned RP08 units from RP08.P02.M04.S19 through RP08.P02.M06.S14?",
      "Do the three descriptor sections (M04 secondary workflow test tail, M05 permission/audit binding service cycle, M06 synthetic fixture head through review-required routing) cover every planned row title as descriptor-only rows, with the shared service guards applied across every section that carries them?",
      "Does CP00-277 avoid email/Office-native/sync/filing runtime dispatch, review/approval route runtime, rollback/retry runtime, lock acquisition, idempotency persistence, state writes, object storage, command runtime execution, Hermes runtime receipts, raw payloads, lock tokens, blocked-claim details, validation error details, permission decisions, audit hints, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp277CloseoutHandoff() {
  return freezeCp277Validation({
    handoff_id: "CP00-277-to-CP00-278",
    from_pack_id: EMAIL_DMS_CORE_CP277_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP277_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP277_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP277_PACK_BINDING.range,
    open_scope: "Continue RP08.P02.M06.S15 onward with the remaining service fixture rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP277_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp278CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp278Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp278Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-278 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP278_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-278");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP278_PACK_BINDING.risk_class) errors.push("CP00-278 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP278_PACK_BINDING.unit_count) errors.push("CP00-278 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP278_PACK_BINDING.first_unit_id) errors.push("CP00-278 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP278_PACK_BINDING.last_unit_id) errors.push("CP00-278 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-278 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-278 must only include RP08 units");
  const summary = createEmailDmsCoreCp278CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP278_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-278 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP278_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-278 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP278_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-278 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP278_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-278 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP278_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-278 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-278 ${microId} missing row ${title}`);
    }
  }
  return freezeCp278Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp278ServiceFixtureTailDescriptor(
  descriptor = createEmailDmsCoreCp278ServiceFixtureTailDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.service_fixture_tail_case_set ?? createEmailDmsCoreCp278ServiceFixtureTailCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp278ServiceFixtureTailDescriptor") errors.push("CP00-278 descriptor type drift");
  if (descriptor.source_service_binding_slice_descriptor !== "EmailDmsCoreCp277ServiceBindingSliceDescriptor") {
    errors.push("CP00-278 source service binding slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP278_REQUIREMENTS.required_section_rows).length) errors.push("CP00-278 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP278_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-278 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-278 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-278 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-278 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-278 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-278 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-278 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP278_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.blocked_claim_output && rows.blocked_claim_output.blocked_claim_detail_included !== false) {
      errors.push(`CP00-278 ${microId} must not expose blocked claims`);
    }
    if (rows.rollback_behavior && rows.rollback_behavior.performs_rollback_runtime !== false) {
      errors.push(`CP00-278 ${microId} must not perform rollback runtime`);
    }
    if (rows.retry_behavior && rows.retry_behavior.performs_retry_runtime !== false) {
      errors.push(`CP00-278 ${microId} must not perform retry runtime`);
    }
    if (rows.unit_test_happy_path && rows.unit_test_happy_path.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-278 ${microId} must not execute test runtime`);
    }
    if (rows.integration_smoke_case && rows.integration_smoke_case.dispatches_integration_smoke_runtime !== false) {
      errors.push(`CP00-278 ${microId} must not dispatch integration smoke runtime`);
    }
    if (rows.service_entrypoint_contract && rows.service_entrypoint_contract.service_contract_descriptor_only !== true) {
      errors.push(`CP00-278 ${microId} service contract drift`);
    }
  }
  if (EMAIL_DMS_CORE_CP278_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-278 must not promote Claude");
  if (EMAIL_DMS_CORE_CP278_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-278 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-278 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-278 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP278_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP279_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP280_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP281_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-278 contract current_pack drift");
  }
  if (
    contractProjection?.service_fixture_tail_descriptor?.descriptor &&
    contractProjection.service_fixture_tail_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-278 contract service_fixture_tail_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP278_PACK_BINDING.next_pack_id) errors.push("CP00-278 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP278_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-278 next subphase drift");
  }
  return freezeCp278Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp278HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp278ServiceFixtureTailDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp278Coverage(planPack);
  const tail = validateEmailDmsCoreCp278ServiceFixtureTailDescriptor(descriptor, contractProjection);
  return freezeCp278Validation({
    evidence_packet: "H08.CP00-278.email_dms_core_service_fixture_tail_descriptor",
    gate: EMAIL_DMS_CORE_CP278_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    service_fixture_tail_valid: tail.valid,
    no_real_data: true,
    source_service_binding_slice_pack_id: EMAIL_DMS_CORE_CP278_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP278_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP278_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP278_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && tail.valid,
  });
}

export function createEmailDmsCoreCp278ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp278Coverage(planPack);
  const tail = validateEmailDmsCoreCp278ServiceFixtureTailDescriptor(createEmailDmsCoreCp278ServiceFixtureTailDescriptor(), {});
  return freezeCp278Validation({
    review_packet: "C08.CP00-278.email_dms_core_service_fixture_tail_descriptor",
    gate: EMAIL_DMS_CORE_CP278_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP278_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    service_fixture_tail_valid: tail.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP278_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-278 cover exactly the 10 planned RP08 units from RP08.P02.M06.S15 through RP08.P02.M07.S02?",
      "Do the two descriptor sections (M06 synthetic fixture tail through integration smoke case, M07 test/golden head) cover every planned row title as descriptor-only rows, with the shared service guards applied across every section that carries them?",
      "Does CP00-278 avoid email/Office-native/sync/filing runtime dispatch, rollback/retry runtime, test runtime paths, state writes, object storage, command runtime execution, Hermes runtime receipts, raw payloads, blocked-claim details, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp278CloseoutHandoff() {
  return freezeCp278Validation({
    handoff_id: "CP00-278-to-CP00-279",
    from_pack_id: EMAIL_DMS_CORE_CP278_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP278_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP278_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP278_PACK_BINDING.range,
    open_scope: "Continue RP08.P02.M07.S03 onward with the remaining service test/golden rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP278_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp279CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp279Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp279Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-279 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP279_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-279");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP279_PACK_BINDING.risk_class) errors.push("CP00-279 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP279_PACK_BINDING.unit_count) errors.push("CP00-279 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP279_PACK_BINDING.first_unit_id) errors.push("CP00-279 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP279_PACK_BINDING.last_unit_id) errors.push("CP00-279 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-279 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-279 must only include RP08 units");
  const summary = createEmailDmsCoreCp279CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP279_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-279 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP279_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-279 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP279_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-279 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP279_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-279 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP279_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-279 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-279 ${microId} missing row ${title}`);
    }
  }
  return freezeCp279Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp279ServiceGoldenMidDescriptor(
  descriptor = createEmailDmsCoreCp279ServiceGoldenMidDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.service_golden_mid_case_set ?? createEmailDmsCoreCp279ServiceGoldenMidCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp279ServiceGoldenMidDescriptor") errors.push("CP00-279 descriptor type drift");
  if (descriptor.source_service_fixture_tail_descriptor !== "EmailDmsCoreCp278ServiceFixtureTailDescriptor") {
    errors.push("CP00-279 source service fixture tail descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP279_REQUIREMENTS.required_section_rows).length) errors.push("CP00-279 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP279_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-279 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-279 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-279 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-279 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-279 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-279 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-279 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP279_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_boundary_precheck && rows.tenant_boundary_precheck.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-279 ${microId} must not allow cross-tenant access`);
    }
    if (rows.matter_trace_precheck && rows.matter_trace_precheck.matter_trace_required !== true) {
      errors.push(`CP00-279 ${microId} matter trace drift`);
    }
    if (rows.permission_precheck && rows.permission_precheck.deny_over_allow_enforced !== true) {
      errors.push(`CP00-279 ${microId} deny-over-allow drift`);
    }
    if (rows.audit_hint_precheck && rows.audit_hint_precheck.audit_hint_detail_included !== false) {
      errors.push(`CP00-279 ${microId} must not expose audit hints`);
    }
    if (rows.state_transition_enforcement && rows.state_transition_enforcement.writes_state_transition !== false) {
      errors.push(`CP00-279 ${microId} must not write state transitions`);
    }
    if (rows.idempotency_key_handling && rows.idempotency_key_handling.persists_idempotency_key !== false) {
      errors.push(`CP00-279 ${microId} must not persist idempotency keys`);
    }
    if (rows.lock_acquisition_rule && rows.lock_acquisition_rule.acquires_runtime_lock !== false) {
      errors.push(`CP00-279 ${microId} must not acquire locks`);
    }
    if (rows.persistence_boundary && rows.persistence_boundary.creates_database_rows !== false) {
      errors.push(`CP00-279 ${microId} must not create database rows`);
    }
  }
  if (EMAIL_DMS_CORE_CP279_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-279 must not promote Claude");
  if (EMAIL_DMS_CORE_CP279_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-279 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-279 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-279 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP279_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP280_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP281_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-279 contract current_pack drift");
  }
  if (
    contractProjection?.service_golden_mid_descriptor?.descriptor &&
    contractProjection.service_golden_mid_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-279 contract service_golden_mid_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP279_PACK_BINDING.next_pack_id) errors.push("CP00-279 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP279_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-279 next subphase drift");
  }
  return freezeCp279Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp279HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp279ServiceGoldenMidDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp279Coverage(planPack);
  const mid = validateEmailDmsCoreCp279ServiceGoldenMidDescriptor(descriptor, contractProjection);
  return freezeCp279Validation({
    evidence_packet: "H08.CP00-279.email_dms_core_service_golden_mid_descriptor",
    gate: EMAIL_DMS_CORE_CP279_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    service_golden_mid_valid: mid.valid,
    no_real_data: true,
    source_service_fixture_tail_pack_id: EMAIL_DMS_CORE_CP279_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP279_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP279_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP279_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && mid.valid,
  });
}

export function createEmailDmsCoreCp279ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp279Coverage(planPack);
  const mid = validateEmailDmsCoreCp279ServiceGoldenMidDescriptor(createEmailDmsCoreCp279ServiceGoldenMidDescriptor(), {});
  return freezeCp279Validation({
    review_packet: "C08.CP00-279.email_dms_core_service_golden_mid_descriptor",
    gate: EMAIL_DMS_CORE_CP279_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP279_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    service_golden_mid_valid: mid.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP279_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-279 cover exactly the 10 planned RP08 units from RP08.P02.M07.S03 through RP08.P02.M07.S12?",
      "Does the single M07 test/golden descriptor section (tenant/matter/permission/audit prechecks, primary/secondary paths, state transition enforcement, idempotency key handling, lock acquisition rule, persistence boundary) cover every planned row title as descriptor-only rows, with the shared service guards applied?",
      "Does CP00-279 avoid email/Office-native/sync/filing runtime dispatch, lock acquisition, idempotency persistence, state writes, object storage, command runtime execution, Hermes runtime receipts, raw payloads, permission decisions, audit hints, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp279CloseoutHandoff() {
  return freezeCp279Validation({
    handoff_id: "CP00-279-to-CP00-280",
    from_pack_id: EMAIL_DMS_CORE_CP279_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP279_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP279_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP279_PACK_BINDING.range,
    open_scope: "Continue RP08.P02.M07.S13 onward with the remaining test/golden rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP279_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp280CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp280Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp280Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-280 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP280_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-280");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP280_PACK_BINDING.risk_class) errors.push("CP00-280 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP280_PACK_BINDING.unit_count) errors.push("CP00-280 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP280_PACK_BINDING.first_unit_id) errors.push("CP00-280 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP280_PACK_BINDING.last_unit_id) errors.push("CP00-280 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-280 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-280 must only include RP08 units");
  const summary = createEmailDmsCoreCp280CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP280_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-280 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP280_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-280 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP280_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-280 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP280_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-280 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP280_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-280 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-280 ${microId} missing row ${title}`);
    }
  }
  return freezeCp280Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp280GoldenHermesSliceDescriptor(
  descriptor = createEmailDmsCoreCp280GoldenHermesSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.golden_hermes_slice_case_set ?? createEmailDmsCoreCp280GoldenHermesSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp280GoldenHermesSliceDescriptor") errors.push("CP00-280 descriptor type drift");
  if (descriptor.source_service_golden_mid_descriptor !== "EmailDmsCoreCp279ServiceGoldenMidDescriptor") {
    errors.push("CP00-280 source service golden mid descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP280_REQUIREMENTS.required_section_rows).length) errors.push("CP00-280 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP280_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-280 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-280 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-280 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-280 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-280 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-280 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-280 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP280_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_boundary_precheck && rows.tenant_boundary_precheck.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-280 ${microId} must not allow cross-tenant access`);
    }
    if (rows.matter_trace_precheck && rows.matter_trace_precheck.matter_trace_required !== true) {
      errors.push(`CP00-280 ${microId} matter trace drift`);
    }
    if (rows.permission_precheck && rows.permission_precheck.deny_over_allow_enforced !== true) {
      errors.push(`CP00-280 ${microId} deny-over-allow drift`);
    }
    if (rows.audit_hint_precheck && rows.audit_hint_precheck.audit_hint_detail_included !== false) {
      errors.push(`CP00-280 ${microId} must not expose audit hints`);
    }
    if (rows.state_transition_enforcement && rows.state_transition_enforcement.writes_state_transition !== false) {
      errors.push(`CP00-280 ${microId} must not write state transitions`);
    }
    if (rows.idempotency_key_handling && rows.idempotency_key_handling.persists_idempotency_key !== false) {
      errors.push(`CP00-280 ${microId} must not persist idempotency keys`);
    }
    if (rows.lock_acquisition_rule && rows.lock_acquisition_rule.acquires_runtime_lock !== false) {
      errors.push(`CP00-280 ${microId} must not acquire locks`);
    }
    if (rows.persistence_boundary && rows.persistence_boundary.creates_database_rows !== false) {
      errors.push(`CP00-280 ${microId} must not create database rows`);
    }
    if (rows.validation_error_mapping && rows.validation_error_mapping.validation_error_detail_included !== false) {
      errors.push(`CP00-280 ${microId} must not expose validation error detail`);
    }
    if (rows.blocked_claim_output && rows.blocked_claim_output.blocked_claim_detail_included !== false) {
      errors.push(`CP00-280 ${microId} must not expose blocked claims`);
    }
    if (rows.rollback_behavior && rows.rollback_behavior.performs_rollback_runtime !== false) {
      errors.push(`CP00-280 ${microId} must not perform rollback runtime`);
    }
    if (rows.retry_behavior && rows.retry_behavior.performs_retry_runtime !== false) {
      errors.push(`CP00-280 ${microId} must not perform retry runtime`);
    }
    if (rows.unit_test_happy_path && rows.unit_test_happy_path.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-280 ${microId} must not execute test runtime`);
    }
    if (rows.integration_smoke_case && rows.integration_smoke_case.dispatches_integration_smoke_runtime !== false) {
      errors.push(`CP00-280 ${microId} must not dispatch integration smoke runtime`);
    }
  }
  if (EMAIL_DMS_CORE_CP280_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-280 must not promote Claude");
  if (EMAIL_DMS_CORE_CP280_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-280 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-280 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-280 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP280_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP281_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-280 contract current_pack drift");
  }
  if (
    contractProjection?.golden_hermes_slice_descriptor?.descriptor &&
    contractProjection.golden_hermes_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-280 contract golden_hermes_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP280_PACK_BINDING.next_pack_id) errors.push("CP00-280 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP280_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-280 next subphase drift");
  }
  return freezeCp280Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp280HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp280GoldenHermesSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp280Coverage(planPack);
  const slice = validateEmailDmsCoreCp280GoldenHermesSliceDescriptor(descriptor, contractProjection);
  return freezeCp280Validation({
    evidence_packet: "H08.CP00-280.email_dms_core_golden_hermes_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP280_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    golden_hermes_slice_valid: slice.valid,
    no_real_data: true,
    source_service_golden_mid_pack_id: EMAIL_DMS_CORE_CP280_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP280_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP280_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP280_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp280ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp280Coverage(planPack);
  const slice = validateEmailDmsCoreCp280GoldenHermesSliceDescriptor(createEmailDmsCoreCp280GoldenHermesSliceDescriptor(), {});
  return freezeCp280Validation({
    review_packet: "C08.CP00-280.email_dms_core_golden_hermes_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP280_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP280_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    golden_hermes_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP280_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-280 cover exactly the 40 planned RP08 units from RP08.P02.M07.S13 through RP08.P02.M09.S08?",
      "Do the three descriptor sections (M07 test/golden tail with happy/denied/review/integration test rows, M08 Hermes evidence packet service cycle, M09 Claude review packet head) cover every planned row title as descriptor-only rows, with the shared service guards applied across every section that carries them?",
      "Does CP00-280 avoid email/Office-native/sync/filing runtime dispatch, rollback/retry runtime, lock acquisition, idempotency persistence, state writes, object storage, command runtime execution, Hermes runtime receipts, raw payloads, lock tokens, blocked-claim details, validation error details, permission decisions, audit hints, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp280CloseoutHandoff() {
  return freezeCp280Validation({
    handoff_id: "CP00-280-to-CP00-281",
    from_pack_id: EMAIL_DMS_CORE_CP280_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP280_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP280_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP280_PACK_BINDING.range,
    open_scope: "Continue RP08.P02.M09.S09 onward with the remaining Claude review packet rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP280_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp281CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp281Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp281Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-281 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP281_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-281");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP281_PACK_BINDING.risk_class) errors.push("CP00-281 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP281_PACK_BINDING.unit_count) errors.push("CP00-281 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP281_PACK_BINDING.first_unit_id) errors.push("CP00-281 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP281_PACK_BINDING.last_unit_id) errors.push("CP00-281 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-281 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-281 must only include RP08 units");
  const summary = createEmailDmsCoreCp281CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP281_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-281 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP281_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-281 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP281_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-281 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP281_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-281 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP281_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-281 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-281 ${microId} missing row ${title}`);
    }
  }
  return freezeCp281Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp281P02CloseoutP03InterfaceFoundationDescriptor(
  descriptor = createEmailDmsCoreCp281P02CloseoutP03InterfaceFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_closeout_p03_interface_foundation_case_set ?? createEmailDmsCoreCp281P02CloseoutP03InterfaceFoundationCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp281P02CloseoutP03InterfaceFoundationDescriptor") errors.push("CP00-281 descriptor type drift");
  if (descriptor.source_golden_hermes_slice_descriptor !== "EmailDmsCoreCp280GoldenHermesSliceDescriptor") {
    errors.push("CP00-281 source golden hermes slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP281_REQUIREMENTS.required_section_rows).length) errors.push("CP00-281 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP281_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-281 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-281 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-281 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-281 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-281 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-281 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-281 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP281_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.permission_annotation && rows.permission_annotation.deny_over_allow_enforced !== true) {
      errors.push(`CP00-281 ${microId} deny-over-allow drift`);
    }
    if (rows.permission_annotation && rows.permission_annotation.permission_decision_detail_included !== false) {
      errors.push(`CP00-281 ${microId} must not expose permission decisions`);
    }
    if (rows.audit_annotation && rows.audit_annotation.audit_hint_detail_included !== false) {
      errors.push(`CP00-281 ${microId} must not expose audit hints`);
    }
    if (rows.pagination_or_filtering_contract && rows.pagination_or_filtering_contract.no_unauthorized_count_leak !== true) {
      errors.push(`CP00-281 ${microId} must not leak unauthorized counts`);
    }
    if (rows.unauthorized_data_omission && rows.unauthorized_data_omission.unauthorized_data_omitted !== true) {
      errors.push(`CP00-281 ${microId} unauthorized data omission drift`);
    }
    if (rows.api_fixture && rows.api_fixture.real_client_data_loaded !== false) {
      errors.push(`CP00-281 ${microId} must not load real data`);
    }
    if (rows.contract_test && rows.contract_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-281 ${microId} must not execute test runtime`);
    }
    if (rows.hermes_api_evidence && rows.hermes_api_evidence.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-281 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_interface_prompt && rows.claude_interface_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-281 ${microId} must not claim Claude final approval`);
    }
    if (rows.command_rerun && rows.command_rerun.executes_command_runtime !== false) {
      errors.push(`CP00-281 ${microId} command rerun must not execute runtime`);
    }
    if (rows.schema_drift_check && rows.schema_drift_check.schema_drift_detected !== false) {
      errors.push(`CP00-281 ${microId} schema drift detected`);
    }
    if (rows.backward_compatibility_check && rows.backward_compatibility_check.backward_compatibility_broken !== false) {
      errors.push(`CP00-281 ${microId} backward compatibility broken`);
    }
    if (rows.tenant_boundary_precheck && rows.tenant_boundary_precheck.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-281 ${microId} must not allow cross-tenant access`);
    }
    if (rows.permission_precheck && rows.permission_precheck.deny_over_allow_enforced !== true) {
      errors.push(`CP00-281 ${microId} service deny-over-allow drift`);
    }
    if (rows.state_transition_enforcement && rows.state_transition_enforcement.writes_state_transition !== false) {
      errors.push(`CP00-281 ${microId} must not write state transitions`);
    }
    if (rows.lock_acquisition_rule && rows.lock_acquisition_rule.acquires_runtime_lock !== false) {
      errors.push(`CP00-281 ${microId} must not acquire locks`);
    }
    if (rows.persistence_boundary && rows.persistence_boundary.creates_database_rows !== false) {
      errors.push(`CP00-281 ${microId} must not create database rows`);
    }
    if (rows.rollback_behavior && rows.rollback_behavior.performs_rollback_runtime !== false) {
      errors.push(`CP00-281 ${microId} must not perform rollback runtime`);
    }
    if (rows.retry_behavior && rows.retry_behavior.performs_retry_runtime !== false) {
      errors.push(`CP00-281 ${microId} must not perform retry runtime`);
    }
    if (rows.integration_smoke_case && rows.integration_smoke_case.dispatches_integration_smoke_runtime !== false) {
      errors.push(`CP00-281 ${microId} must not dispatch integration smoke runtime`);
    }
  }
  if (EMAIL_DMS_CORE_CP281_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-281 must not promote Claude");
  if (EMAIL_DMS_CORE_CP281_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-281 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-281 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-281 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP281_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-281 contract current_pack drift");
  }
  if (
    contractProjection?.p02_closeout_p03_interface_foundation_descriptor?.descriptor &&
    contractProjection.p02_closeout_p03_interface_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-281 contract p02_closeout_p03_interface_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP281_PACK_BINDING.next_pack_id) errors.push("CP00-281 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP281_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-281 next subphase drift");
  }
  return freezeCp281Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp281HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp281P02CloseoutP03InterfaceFoundationDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp281Coverage(planPack);
  const foundation = validateEmailDmsCoreCp281P02CloseoutP03InterfaceFoundationDescriptor(descriptor, contractProjection);
  return freezeCp281Validation({
    evidence_packet: "H08.CP00-281.email_dms_core_p02_closeout_p03_interface_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP281_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_closeout_p03_interface_foundation_valid: foundation.valid,
    no_real_data: true,
    source_golden_hermes_slice_pack_id: EMAIL_DMS_CORE_CP281_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP281_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP281_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP281_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createEmailDmsCoreCp281ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp281Coverage(planPack);
  const foundation = validateEmailDmsCoreCp281P02CloseoutP03InterfaceFoundationDescriptor(createEmailDmsCoreCp281P02CloseoutP03InterfaceFoundationDescriptor(), {});
  return freezeCp281Validation({
    review_packet: "C08.CP00-281.email_dms_core_p02_closeout_p03_interface_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP281_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP281_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_closeout_p03_interface_foundation_valid: foundation.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP281_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-281 cover exactly the 150 planned RP08 units from RP08.P02.M09.S09 through RP08.P03.M06.S12?",
      "Do the nine descriptor sections (M09 Claude review packet tail, M10 closeout handoff service cycle, and seven P03 interface micros with public export maps, request/response contracts, error code taxonomies, permission/audit annotations, pagination contracts, serialization guards, unauthorized data omission, API fixtures, contract/invalid/denied tests, Hermes API evidence, Claude interface prompts, documentation examples, versioning notes, downstream consumer notes, command reruns, and schema drift/backward compatibility checks) cover every planned row title as descriptor-only rows?",
      "Does CP00-281 avoid email/Office-native/sync/filing runtime dispatch, API handler execution, schema drift, backward compatibility breaks, rollback/retry runtime, lock acquisition, idempotency persistence, state writes, object storage, command runtime execution, Hermes runtime receipts, raw payloads, unauthorized counts, fixture payloads, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp281CloseoutHandoff() {
  return freezeCp281Validation({
    handoff_id: "CP00-281-to-CP00-282",
    from_pack_id: EMAIL_DMS_CORE_CP281_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP281_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP281_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP281_PACK_BINDING.range,
    open_scope: "RP08.P02 descriptor scope is closed; continue RP08.P03.M06.S13 onward with the remaining interface fixture rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP281_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp282CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp282Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp282Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-282 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-282");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP282_PACK_BINDING.risk_class) errors.push("CP00-282 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP282_PACK_BINDING.unit_count) errors.push("CP00-282 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP282_PACK_BINDING.first_unit_id) errors.push("CP00-282 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP282_PACK_BINDING.last_unit_id) errors.push("CP00-282 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-282 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-282 must only include RP08 units");
  const summary = createEmailDmsCoreCp282CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP282_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-282 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP282_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-282 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP282_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-282 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP282_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-282 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP282_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-282 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-282 ${microId} missing row ${title}`);
    }
  }
  return freezeCp282Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp282P03CloseoutP04UiFoundationDescriptor(
  descriptor = createEmailDmsCoreCp282P03CloseoutP04UiFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_closeout_p04_ui_foundation_case_set ?? createEmailDmsCoreCp282P03CloseoutP04UiFoundationCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp282P03CloseoutP04UiFoundationDescriptor") errors.push("CP00-282 descriptor type drift");
  if (descriptor.source_p02_closeout_p03_interface_foundation_descriptor !== "EmailDmsCoreCp281P02CloseoutP03InterfaceFoundationDescriptor") {
    errors.push("CP00-282 source p02 closeout p03 interface foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP282_REQUIREMENTS.required_section_rows).length) errors.push("CP00-282 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP282_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-282 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-282 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-282 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-282 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-282 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-282 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-282 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP282_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.permission_annotation && rows.permission_annotation.deny_over_allow_enforced !== true) {
      errors.push(`CP00-282 ${microId} deny-over-allow drift`);
    }
    if (rows.pagination_or_filtering_contract && rows.pagination_or_filtering_contract.no_unauthorized_count_leak !== true) {
      errors.push(`CP00-282 ${microId} must not leak unauthorized counts`);
    }
    if (rows.unauthorized_data_omission && rows.unauthorized_data_omission.unauthorized_data_omitted !== true) {
      errors.push(`CP00-282 ${microId} unauthorized data omission drift`);
    }
    if (rows.api_fixture && rows.api_fixture.real_client_data_loaded !== false) {
      errors.push(`CP00-282 ${microId} must not load real data`);
    }
    if (rows.hermes_api_evidence && rows.hermes_api_evidence.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-282 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_interface_prompt && rows.claude_interface_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-282 ${microId} must not claim Claude final approval`);
    }
    if (rows.command_rerun && rows.command_rerun.executes_command_runtime !== false) {
      errors.push(`CP00-282 ${microId} command rerun must not execute runtime`);
    }
    if (rows.schema_drift_check && rows.schema_drift_check.schema_drift_detected !== false) {
      errors.push(`CP00-282 ${microId} schema drift detected`);
    }
    if (rows.backward_compatibility_check && rows.backward_compatibility_check.backward_compatibility_broken !== false) {
      errors.push(`CP00-282 ${microId} backward compatibility broken`);
    }
    if (rows.denied_state && rows.denied_state.permission_decision_detail_included !== false) {
      errors.push(`CP00-282 ${microId} denied state must not expose permission decisions`);
    }
    if (rows.empty_state && rows.empty_state.no_unauthorized_count_leak !== true) {
      errors.push(`CP00-282 ${microId} empty state must not leak counts`);
    }
    if (rows.permission_badge && rows.permission_badge.permission_decision_detail_included !== false) {
      errors.push(`CP00-282 ${microId} permission badge must not expose decisions`);
    }
    if (rows.audit_hint_display && rows.audit_hint_display.audit_hint_detail_included !== false) {
      errors.push(`CP00-282 ${microId} audit hint display must not expose hints`);
    }
    if (rows.error_message_copy && rows.error_message_copy.validation_error_detail_included !== false) {
      errors.push(`CP00-282 ${microId} error copy must not expose validation detail`);
    }
    if (rows.synthetic_fixture_binding && rows.synthetic_fixture_binding.real_client_data_loaded !== false) {
      errors.push(`CP00-282 ${microId} fixture binding must not load real data`);
    }
    if (rows.build_smoke && rows.build_smoke.executes_ui_runtime !== false) {
      errors.push(`CP00-282 ${microId} build smoke must not execute UI runtime`);
    }
    if (rows.hermes_ui_evidence && rows.hermes_ui_evidence.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-282 ${microId} must not emit Hermes UI receipts`);
    }
    if (rows.claude_ui_leak_prompt && rows.claude_ui_leak_prompt.leak_detected !== false) {
      errors.push(`CP00-282 ${microId} UI leak detected`);
    }
  }
  if (EMAIL_DMS_CORE_CP282_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-282 must not promote Claude");
  if (EMAIL_DMS_CORE_CP282_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-282 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-282 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-282 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-282 contract current_pack drift");
  }
  if (
    contractProjection?.p03_closeout_p04_ui_foundation_descriptor?.descriptor &&
    contractProjection.p03_closeout_p04_ui_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-282 contract p03_closeout_p04_ui_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP282_PACK_BINDING.next_pack_id) errors.push("CP00-282 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP282_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-282 next subphase drift");
  }
  return freezeCp282Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp282HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp282P03CloseoutP04UiFoundationDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp282Coverage(planPack);
  const foundation = validateEmailDmsCoreCp282P03CloseoutP04UiFoundationDescriptor(descriptor, contractProjection);
  return freezeCp282Validation({
    evidence_packet: "H08.CP00-282.email_dms_core_p03_closeout_p04_ui_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP282_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_closeout_p04_ui_foundation_valid: foundation.valid,
    no_real_data: true,
    source_p02_closeout_p03_interface_foundation_pack_id: EMAIL_DMS_CORE_CP282_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP282_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP282_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP282_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createEmailDmsCoreCp282ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp282Coverage(planPack);
  const foundation = validateEmailDmsCoreCp282P03CloseoutP04UiFoundationDescriptor(createEmailDmsCoreCp282P03CloseoutP04UiFoundationDescriptor(), {});
  return freezeCp282Validation({
    review_packet: "C08.CP00-282.email_dms_core_p03_closeout_p04_ui_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP282_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP282_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_closeout_p04_ui_foundation_valid: foundation.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP282_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-282 cover exactly the 150 planned RP08 units from RP08.P03.M06.S13 through RP08.P04.M03.S20?",
      "Do the nine descriptor sections (five P03 interface micros closing the phase and four P04 UI micros with surface inventories, data dependency maps, loading/empty/denied/review states, interactions, permission badges, audit hint displays, error copy, responsive layouts, keyboard/focus behavior, visual density checks, synthetic fixture bindings, build smokes, Hermes UI evidence, Claude UI leak prompts, and closeout handoffs) cover every planned row title as descriptor-only rows?",
      "Does CP00-282 avoid email/Office-native/sync/filing runtime dispatch, UI runtime execution, API handler execution, schema drift, backward compatibility breaks, permission decision leaks, audit hint leaks, unauthorized count leaks, validation error detail leaks, fixture payload leaks, state writes, object storage, command runtime execution, Hermes runtime receipts, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp282CloseoutHandoff() {
  return freezeCp282Validation({
    handoff_id: "CP00-282-to-CP00-283",
    from_pack_id: EMAIL_DMS_CORE_CP282_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP282_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP282_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP282_PACK_BINDING.range,
    open_scope: "RP08.P03 descriptor scope is closed; continue RP08.P04.M03.S21 onward with the remaining UI rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP282_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp283CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp283Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp283Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-283 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-283");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP283_PACK_BINDING.risk_class) errors.push("CP00-283 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP283_PACK_BINDING.unit_count) errors.push("CP00-283 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP283_PACK_BINDING.first_unit_id) errors.push("CP00-283 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP283_PACK_BINDING.last_unit_id) errors.push("CP00-283 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-283 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-283 must only include RP08 units");
  const summary = createEmailDmsCoreCp283CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP283_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-283 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP283_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-283 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP283_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-283 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP283_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-283 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP283_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-283 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-283 ${microId} missing row ${title}`);
    }
  }
  return freezeCp283Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp283UiWorkflowSliceDescriptor(
  descriptor = createEmailDmsCoreCp283UiWorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.ui_workflow_slice_case_set ?? createEmailDmsCoreCp283UiWorkflowSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp283UiWorkflowSliceDescriptor") errors.push("CP00-283 descriptor type drift");
  if (descriptor.source_p03_closeout_p04_ui_foundation_descriptor !== "EmailDmsCoreCp282P03CloseoutP04UiFoundationDescriptor") {
    errors.push("CP00-283 source p03 closeout p04 ui foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP283_REQUIREMENTS.required_section_rows).length) errors.push("CP00-283 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP283_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-283 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-283 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-283 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-283 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-283 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-283 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-283 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP283_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.denied_state && rows.denied_state.permission_decision_detail_included !== false) {
      errors.push(`CP00-283 ${microId} denied state must not expose permission decisions`);
    }
    if (rows.empty_state && rows.empty_state.no_unauthorized_count_leak !== true) {
      errors.push(`CP00-283 ${microId} empty state must not leak counts`);
    }
    if (rows.permission_badge && rows.permission_badge.permission_decision_detail_included !== false) {
      errors.push(`CP00-283 ${microId} permission badge must not expose decisions`);
    }
    if (rows.audit_hint_display && rows.audit_hint_display.audit_hint_detail_included !== false) {
      errors.push(`CP00-283 ${microId} audit hint display must not expose hints`);
    }
    if (rows.error_message_copy && rows.error_message_copy.validation_error_detail_included !== false) {
      errors.push(`CP00-283 ${microId} error copy must not expose validation detail`);
    }
    if (rows.synthetic_fixture_binding && rows.synthetic_fixture_binding.real_client_data_loaded !== false) {
      errors.push(`CP00-283 ${microId} fixture binding must not load real data`);
    }
    if (rows.build_smoke && rows.build_smoke.executes_ui_runtime !== false) {
      errors.push(`CP00-283 ${microId} build smoke must not execute UI runtime`);
    }
    if (rows.hermes_ui_evidence && rows.hermes_ui_evidence.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-283 ${microId} must not emit Hermes UI receipts`);
    }
    if (rows.claude_ui_leak_prompt && rows.claude_ui_leak_prompt.leak_detected !== false) {
      errors.push(`CP00-283 ${microId} UI leak detected`);
    }
    if (rows.state_snapshot && rows.state_snapshot.state_snapshot_descriptor_only !== true) {
      errors.push(`CP00-283 ${microId} state snapshot drift`);
    }
    if (rows.no_unauthorized_count_leak && rows.no_unauthorized_count_leak.no_unauthorized_count_leak !== true) {
      errors.push(`CP00-283 ${microId} unauthorized count leak`);
    }
  }
  if (EMAIL_DMS_CORE_CP283_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-283 must not promote Claude");
  if (EMAIL_DMS_CORE_CP283_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-283 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-283 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-283 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-283 contract current_pack drift");
  }
  if (
    contractProjection?.ui_workflow_slice_descriptor?.descriptor &&
    contractProjection.ui_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-283 contract ui_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP283_PACK_BINDING.next_pack_id) errors.push("CP00-283 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP283_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-283 next subphase drift");
  }
  return freezeCp283Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp283HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp283UiWorkflowSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp283Coverage(planPack);
  const slice = validateEmailDmsCoreCp283UiWorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp283Validation({
    evidence_packet: "H08.CP00-283.email_dms_core_ui_workflow_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP283_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    ui_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_p03_closeout_p04_ui_foundation_pack_id: EMAIL_DMS_CORE_CP283_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP283_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP283_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP283_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp283ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp283Coverage(planPack);
  const slice = validateEmailDmsCoreCp283UiWorkflowSliceDescriptor(createEmailDmsCoreCp283UiWorkflowSliceDescriptor(), {});
  return freezeCp283Validation({
    review_packet: "C08.CP00-283.email_dms_core_ui_workflow_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP283_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP283_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    ui_workflow_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP283_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-283 cover exactly the 40 planned RP08 units from RP08.P04.M03.S21 through RP08.P04.M05.S16?",
      "Do the three descriptor sections (M03 primary slice tail with state snapshot and no-unauthorized-count-leak rows, M04 secondary workflow UI cycle, M05 permission/audit binding head) cover every planned row title as descriptor-only rows, with the shared UI guards applied across every section that carries them?",
      "Does CP00-283 avoid email/Office-native/sync/filing runtime dispatch, UI runtime execution, permission decision leaks, audit hint leaks, unauthorized count leaks, validation error detail leaks, fixture payload leaks, state writes, object storage, command runtime execution, Hermes runtime receipts, raw payloads, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp283CloseoutHandoff() {
  return freezeCp283Validation({
    handoff_id: "CP00-283-to-CP00-284",
    from_pack_id: EMAIL_DMS_CORE_CP283_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP283_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP283_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP283_PACK_BINDING.range,
    open_scope: "Continue RP08.P04.M05.S17 onward with the remaining UI permission/audit binding rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP283_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp284CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp284Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp284Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-284 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-284");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP284_PACK_BINDING.risk_class) errors.push("CP00-284 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP284_PACK_BINDING.unit_count) errors.push("CP00-284 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP284_PACK_BINDING.first_unit_id) errors.push("CP00-284 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP284_PACK_BINDING.last_unit_id) errors.push("CP00-284 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-284 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-284 must only include RP08 units");
  const summary = createEmailDmsCoreCp284CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP284_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-284 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP284_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-284 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP284_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-284 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP284_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-284 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP284_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-284 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-284 ${microId} missing row ${title}`);
    }
  }
  return freezeCp284Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp284UiBindingTailDescriptor(
  descriptor = createEmailDmsCoreCp284UiBindingTailDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.ui_binding_tail_case_set ?? createEmailDmsCoreCp284UiBindingTailCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp284UiBindingTailDescriptor") errors.push("CP00-284 descriptor type drift");
  if (descriptor.source_ui_workflow_slice_descriptor !== "EmailDmsCoreCp283UiWorkflowSliceDescriptor") {
    errors.push("CP00-284 source ui workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP284_REQUIREMENTS.required_section_rows).length) errors.push("CP00-284 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP284_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-284 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-284 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-284 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-284 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-284 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-284 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-284 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP284_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.build_smoke && rows.build_smoke.executes_ui_runtime !== false) {
      errors.push(`CP00-284 ${microId} build smoke must not execute UI runtime`);
    }
    if (rows.hermes_ui_evidence && rows.hermes_ui_evidence.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-284 ${microId} must not emit Hermes UI receipts`);
    }
    if (rows.claude_ui_leak_prompt && rows.claude_ui_leak_prompt.leak_detected !== false) {
      errors.push(`CP00-284 ${microId} UI leak detected`);
    }
    if (rows.state_snapshot && rows.state_snapshot.state_snapshot_descriptor_only !== true) {
      errors.push(`CP00-284 ${microId} state snapshot drift`);
    }
    if (rows.no_unauthorized_count_leak && rows.no_unauthorized_count_leak.no_unauthorized_count_leak !== true) {
      errors.push(`CP00-284 ${microId} unauthorized count leak`);
    }
    if (rows.denied_state && rows.denied_state.permission_decision_detail_included !== false) {
      errors.push(`CP00-284 ${microId} denied state must not expose permission decisions`);
    }
    if (rows.empty_state && rows.empty_state.no_unauthorized_count_leak !== true) {
      errors.push(`CP00-284 ${microId} empty state must not leak counts`);
    }
  }
  if (EMAIL_DMS_CORE_CP284_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-284 must not promote Claude");
  if (EMAIL_DMS_CORE_CP284_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-284 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-284 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-284 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-284 contract current_pack drift");
  }
  if (
    contractProjection?.ui_binding_tail_descriptor?.descriptor &&
    contractProjection.ui_binding_tail_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-284 contract ui_binding_tail_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP284_PACK_BINDING.next_pack_id) errors.push("CP00-284 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP284_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-284 next subphase drift");
  }
  return freezeCp284Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp284HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp284UiBindingTailDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp284Coverage(planPack);
  const tail = validateEmailDmsCoreCp284UiBindingTailDescriptor(descriptor, contractProjection);
  return freezeCp284Validation({
    evidence_packet: "H08.CP00-284.email_dms_core_ui_binding_tail_descriptor",
    gate: EMAIL_DMS_CORE_CP284_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    ui_binding_tail_valid: tail.valid,
    no_real_data: true,
    source_ui_workflow_slice_pack_id: EMAIL_DMS_CORE_CP284_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP284_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP284_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP284_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && tail.valid,
  });
}

export function createEmailDmsCoreCp284ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp284Coverage(planPack);
  const tail = validateEmailDmsCoreCp284UiBindingTailDescriptor(createEmailDmsCoreCp284UiBindingTailDescriptor(), {});
  return freezeCp284Validation({
    review_packet: "C08.CP00-284.email_dms_core_ui_binding_tail_descriptor",
    gate: EMAIL_DMS_CORE_CP284_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP284_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    ui_binding_tail_valid: tail.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP284_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-284 cover exactly the 10 planned RP08 units from RP08.P04.M05.S17 through RP08.P04.M06.S04?",
      "Do the two descriptor sections (M05 permission/audit binding tail with build smoke, Hermes UI evidence, Claude UI leak prompt, closeout handoff, state snapshot, and no-unauthorized-count-leak rows; M06 synthetic fixture head) cover every planned row title as descriptor-only rows, with the shared UI guards applied across every section that carries them?",
      "Does CP00-284 avoid email/Office-native/sync/filing runtime dispatch, UI runtime execution, permission decision leaks, audit hint leaks, unauthorized count leaks, fixture payload leaks, state writes, object storage, command runtime execution, Hermes runtime receipts, raw payloads, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp284CloseoutHandoff() {
  return freezeCp284Validation({
    handoff_id: "CP00-284-to-CP00-285",
    from_pack_id: EMAIL_DMS_CORE_CP284_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP284_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP284_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP284_PACK_BINDING.range,
    open_scope: "Continue RP08.P04.M06.S05 onward with the remaining UI fixture rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP284_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp285CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp285Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp285Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-285 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-285");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP285_PACK_BINDING.risk_class) errors.push("CP00-285 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP285_PACK_BINDING.unit_count) errors.push("CP00-285 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP285_PACK_BINDING.first_unit_id) errors.push("CP00-285 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP285_PACK_BINDING.last_unit_id) errors.push("CP00-285 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-285 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-285 must only include RP08 units");
  const summary = createEmailDmsCoreCp285CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP285_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-285 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP285_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-285 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP285_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-285 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP285_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-285 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP285_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-285 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-285 ${microId} missing row ${title}`);
    }
  }
  return freezeCp285Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp285P04CloseoutP05FixtureFoundationDescriptor(
  descriptor = createEmailDmsCoreCp285P04CloseoutP05FixtureFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_closeout_p05_fixture_foundation_case_set ?? createEmailDmsCoreCp285P04CloseoutP05FixtureFoundationCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp285P04CloseoutP05FixtureFoundationDescriptor") errors.push("CP00-285 descriptor type drift");
  if (descriptor.source_ui_binding_tail_descriptor !== "EmailDmsCoreCp284UiBindingTailDescriptor") {
    errors.push("CP00-285 source ui binding tail descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP285_REQUIREMENTS.required_section_rows).length) errors.push("CP00-285 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP285_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-285 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-285 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-285 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-285 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-285 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-285 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-285 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP285_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.denied_state && rows.denied_state.permission_decision_detail_included !== false) {
      errors.push(`CP00-285 ${microId} denied state must not expose permission decisions`);
    }
    if (rows.empty_state && rows.empty_state.no_unauthorized_count_leak !== true) {
      errors.push(`CP00-285 ${microId} empty state must not leak counts`);
    }
    if (rows.build_smoke && rows.build_smoke.executes_ui_runtime !== false) {
      errors.push(`CP00-285 ${microId} build smoke must not execute UI runtime`);
    }
    if (rows.claude_ui_leak_prompt && rows.claude_ui_leak_prompt.leak_detected !== false) {
      errors.push(`CP00-285 ${microId} UI leak detected`);
    }
    if (rows.no_unauthorized_count_leak && rows.no_unauthorized_count_leak.no_unauthorized_count_leak !== true) {
      errors.push(`CP00-285 ${microId} unauthorized count leak`);
    }
    if (rows.base_tenant_fixture && rows.base_tenant_fixture.real_client_data_loaded !== false) {
      errors.push(`CP00-285 ${microId} tenant fixture must not load real data`);
    }
    if (rows.denied_case && rows.denied_case.permission_decision_detail_included !== false) {
      errors.push(`CP00-285 ${microId} denied case must not expose permission decisions`);
    }
    if (rows.cross_tenant_case && rows.cross_tenant_case.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-285 ${microId} cross-tenant case must deny access`);
    }
    if (rows.audit_hint_case && rows.audit_hint_case.audit_hint_detail_included !== false) {
      errors.push(`CP00-285 ${microId} audit hint case must not expose hints`);
    }
    if (rows.security_trimming_case && rows.security_trimming_case.unauthorized_data_omitted !== true) {
      errors.push(`CP00-285 ${microId} security trimming drift`);
    }
    if (rows.ai_retrieval_or_analytics_case && rows.ai_retrieval_or_analytics_case.dispatches_ai_runtime !== false) {
      errors.push(`CP00-285 ${microId} must not dispatch AI runtime`);
    }
    if (rows.golden_test && rows.golden_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-285 ${microId} must not execute test runtime`);
    }
    if (rows.hermes_fixture_evidence && rows.hermes_fixture_evidence.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-285 ${microId} must not emit Hermes fixture receipts`);
    }
    if (rows.claude_missing_test_prompt && rows.claude_missing_test_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-285 ${microId} must not claim Claude final approval`);
    }
    if (rows.no_real_data_check && rows.no_real_data_check.real_client_data_loaded !== false) {
      errors.push(`CP00-285 ${microId} no-real-data check drift`);
    }
  }
  if (EMAIL_DMS_CORE_CP285_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-285 must not promote Claude");
  if (EMAIL_DMS_CORE_CP285_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-285 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-285 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-285 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-285 contract current_pack drift");
  }
  if (
    contractProjection?.p04_closeout_p05_fixture_foundation_descriptor?.descriptor &&
    contractProjection.p04_closeout_p05_fixture_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-285 contract p04_closeout_p05_fixture_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP285_PACK_BINDING.next_pack_id) errors.push("CP00-285 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP285_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-285 next subphase drift");
  }
  return freezeCp285Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp285HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp285P04CloseoutP05FixtureFoundationDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp285Coverage(planPack);
  const foundation = validateEmailDmsCoreCp285P04CloseoutP05FixtureFoundationDescriptor(descriptor, contractProjection);
  return freezeCp285Validation({
    evidence_packet: "H08.CP00-285.email_dms_core_p04_closeout_p05_fixture_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP285_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_closeout_p05_fixture_foundation_valid: foundation.valid,
    no_real_data: true,
    source_ui_binding_tail_pack_id: EMAIL_DMS_CORE_CP285_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP285_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP285_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP285_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createEmailDmsCoreCp285ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp285Coverage(planPack);
  const foundation = validateEmailDmsCoreCp285P04CloseoutP05FixtureFoundationDescriptor(createEmailDmsCoreCp285P04CloseoutP05FixtureFoundationDescriptor(), {});
  return freezeCp285Validation({
    review_packet: "C08.CP00-285.email_dms_core_p04_closeout_p05_fixture_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP285_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP285_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_closeout_p05_fixture_foundation_valid: foundation.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP285_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-285 cover exactly the 150 planned RP08 units from RP08.P04.M06.S05 through RP08.P05.M03.S08?",
      "Do the nine descriptor sections (five P04 UI micros closing the phase and four P05 fixture micros with base tenant/user/matter/document fixtures, golden/review/denied/cross-tenant/missing-context cases, audit hint cases, security trimming cases, AI retrieval/analytics cases, fixture manifests, golden/failure tests, Hermes fixture evidence, Claude missing-test prompts, closeout handoffs, and no-real-data checks) cover every planned row title as descriptor-only rows?",
      "Does CP00-285 avoid email/Office-native/sync/filing runtime dispatch, UI runtime execution, AI runtime dispatch, real fixture data loading, permission decision leaks, audit hint leaks, unauthorized count leaks, fixture payload leaks, state writes, object storage, command runtime execution, Hermes runtime receipts, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp285CloseoutHandoff() {
  return freezeCp285Validation({
    handoff_id: "CP00-285-to-CP00-286",
    from_pack_id: EMAIL_DMS_CORE_CP285_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP285_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP285_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP285_PACK_BINDING.range,
    open_scope: "RP08.P04 descriptor scope is closed; continue RP08.P05.M03.S09 onward with the remaining fixture rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP285_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp286CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp286Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp286Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-286 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-286");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP286_PACK_BINDING.risk_class) errors.push("CP00-286 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP286_PACK_BINDING.unit_count) errors.push("CP00-286 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP286_PACK_BINDING.first_unit_id) errors.push("CP00-286 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP286_PACK_BINDING.last_unit_id) errors.push("CP00-286 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-286 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-286 must only include RP08 units");
  const summary = createEmailDmsCoreCp286CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP286_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-286 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP286_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-286 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP286_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-286 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP286_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-286 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP286_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-286 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-286 ${microId} missing row ${title}`);
    }
  }
  return freezeCp286Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp286FixtureSliceDescriptor(
  descriptor = createEmailDmsCoreCp286FixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.fixture_slice_case_set ?? createEmailDmsCoreCp286FixtureSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp286FixtureSliceDescriptor") errors.push("CP00-286 descriptor type drift");
  if (descriptor.source_p04_closeout_p05_fixture_foundation_descriptor !== "EmailDmsCoreCp285P04CloseoutP05FixtureFoundationDescriptor") {
    errors.push("CP00-286 source p04 closeout p05 fixture foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP286_REQUIREMENTS.required_section_rows).length) errors.push("CP00-286 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP286_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-286 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-286 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-286 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-286 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-286 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-286 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-286 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP286_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.cross_tenant_case && rows.cross_tenant_case.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-286 ${microId} cross-tenant case must deny access`);
    }
    if (rows.audit_hint_case && rows.audit_hint_case.audit_hint_detail_included !== false) {
      errors.push(`CP00-286 ${microId} audit hint case must not expose hints`);
    }
    if (rows.security_trimming_case && rows.security_trimming_case.unauthorized_data_omitted !== true) {
      errors.push(`CP00-286 ${microId} security trimming drift`);
    }
    if (rows.ai_retrieval_or_analytics_case && rows.ai_retrieval_or_analytics_case.dispatches_ai_runtime !== false) {
      errors.push(`CP00-286 ${microId} must not dispatch AI runtime`);
    }
    if (rows.fixture_manifest && rows.fixture_manifest.fixture_manifest_descriptor_only !== true) {
      errors.push(`CP00-286 ${microId} fixture manifest drift`);
    }
    if (rows.golden_test && rows.golden_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-286 ${microId} must not execute test runtime`);
    }
    if (rows.failure_test && rows.failure_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-286 ${microId} failure test must not execute runtime`);
    }
    if (rows.hermes_fixture_evidence && rows.hermes_fixture_evidence.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-286 ${microId} must not emit Hermes fixture receipts`);
    }
    if (rows.claude_missing_test_prompt && rows.claude_missing_test_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-286 ${microId} must not claim Claude final approval`);
    }
  }
  if (EMAIL_DMS_CORE_CP286_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-286 must not promote Claude");
  if (EMAIL_DMS_CORE_CP286_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-286 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-286 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-286 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-286 contract current_pack drift");
  }
  if (
    contractProjection?.fixture_slice_descriptor?.descriptor &&
    contractProjection.fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-286 contract fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP286_PACK_BINDING.next_pack_id) errors.push("CP00-286 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP286_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-286 next subphase drift");
  }
  return freezeCp286Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp286HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp286FixtureSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp286Coverage(planPack);
  const slice = validateEmailDmsCoreCp286FixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp286Validation({
    evidence_packet: "H08.CP00-286.email_dms_core_fixture_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP286_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p04_closeout_p05_fixture_foundation_pack_id: EMAIL_DMS_CORE_CP286_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP286_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP286_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP286_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp286ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp286Coverage(planPack);
  const slice = validateEmailDmsCoreCp286FixtureSliceDescriptor(createEmailDmsCoreCp286FixtureSliceDescriptor(), {});
  return freezeCp286Validation({
    review_packet: "C08.CP00-286.email_dms_core_fixture_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP286_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP286_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    fixture_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP286_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-286 cover exactly the 10 planned RP08 units from RP08.P05.M03.S09 through RP08.P05.M03.S18?",
      "Does the single M03 primary implementation slice section (cross-tenant case, missing context case, audit hint case, security trimming case, AI retrieval/analytics case, fixture manifest, golden/failure tests, Hermes fixture evidence, Claude missing-test prompt) cover every planned row title as descriptor-only rows, with the shared fixture guards applied?",
      "Does CP00-286 avoid email/Office-native/sync/filing runtime dispatch, AI runtime dispatch, real fixture data loading, test runtime paths, permission decision leaks, audit hint leaks, fixture payload leaks, state writes, object storage, command runtime execution, Hermes runtime receipts, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp286CloseoutHandoff() {
  return freezeCp286Validation({
    handoff_id: "CP00-286-to-CP00-287",
    from_pack_id: EMAIL_DMS_CORE_CP286_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP286_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP286_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP286_PACK_BINDING.range,
    open_scope: "Continue RP08.P05.M03.S19 onward with the remaining fixture rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP286_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp287CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp287Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp287Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-287 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-287");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP287_PACK_BINDING.risk_class) errors.push("CP00-287 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP287_PACK_BINDING.unit_count) errors.push("CP00-287 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP287_PACK_BINDING.first_unit_id) errors.push("CP00-287 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP287_PACK_BINDING.last_unit_id) errors.push("CP00-287 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-287 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-287 must only include RP08 units");
  const summary = createEmailDmsCoreCp287CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP287_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-287 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP287_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-287 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP287_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-287 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP287_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-287 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP287_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-287 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-287 ${microId} missing row ${title}`);
    }
  }
  return freezeCp287Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp287P05CloseoutP06PermissionFoundationDescriptor(
  descriptor = createEmailDmsCoreCp287P05CloseoutP06PermissionFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_closeout_p06_permission_foundation_case_set ?? createEmailDmsCoreCp287P05CloseoutP06PermissionFoundationCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp287P05CloseoutP06PermissionFoundationDescriptor") errors.push("CP00-287 descriptor type drift");
  if (descriptor.source_fixture_slice_descriptor !== "EmailDmsCoreCp286FixtureSliceDescriptor") {
    errors.push("CP00-287 source fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP287_REQUIREMENTS.required_section_rows).length) errors.push("CP00-287 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP287_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-287 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-287 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-287 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-287 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-287 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-287 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-287 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP287_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.cross_tenant_case && rows.cross_tenant_case.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-287 ${microId} cross-tenant case must deny access`);
    }
    if (rows.security_trimming_case && rows.security_trimming_case.unauthorized_data_omitted !== true) {
      errors.push(`CP00-287 ${microId} security trimming drift`);
    }
    if (rows.ai_retrieval_or_analytics_case && rows.ai_retrieval_or_analytics_case.dispatches_ai_runtime !== false) {
      errors.push(`CP00-287 ${microId} must not dispatch AI runtime`);
    }
    if (rows.golden_test && rows.golden_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-287 ${microId} must not execute test runtime`);
    }
    if (rows.hermes_fixture_evidence && rows.hermes_fixture_evidence.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-287 ${microId} must not emit Hermes fixture receipts`);
    }
    if (rows.claude_missing_test_prompt && rows.claude_missing_test_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-287 ${microId} must not claim Claude final approval`);
    }
    if (rows.no_real_data_check && rows.no_real_data_check.real_client_data_loaded !== false) {
      errors.push(`CP00-287 ${microId} no-real-data check drift`);
    }
    if (rows.stable_id_check && rows.stable_id_check.stable_ids_used !== true) {
      errors.push(`CP00-287 ${microId} stable ID drift`);
    }
    if (rows.replay_command && rows.replay_command.executes_command_runtime !== false) {
      errors.push(`CP00-287 ${microId} replay command must not execute runtime`);
    }
    if (rows.permission_matrix_row && rows.permission_matrix_row.deny_over_allow_enforced !== true) {
      errors.push(`CP00-287 ${microId} permission matrix deny-over-allow drift`);
    }
    for (const bindingKey of ["view_decision_binding", "search_decision_binding", "mutation_decision_binding", "export_download_decision_binding", "share_decision_binding"]) {
      if (rows[bindingKey] && rows[bindingKey].permission_decision_detail_included !== false) {
        errors.push(`CP00-287 ${microId} ${bindingKey} must not expose permission decisions`);
      }
      if (rows[bindingKey] && rows[bindingKey].deny_over_allow_enforced !== true) {
        errors.push(`CP00-287 ${microId} ${bindingKey} deny-over-allow drift`);
      }
    }
  }
  if (EMAIL_DMS_CORE_CP287_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-287 must not promote Claude");
  if (EMAIL_DMS_CORE_CP287_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-287 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-287 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-287 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-287 contract current_pack drift");
  }
  if (
    contractProjection?.p05_closeout_p06_permission_foundation_descriptor?.descriptor &&
    contractProjection.p05_closeout_p06_permission_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-287 contract p05_closeout_p06_permission_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP287_PACK_BINDING.next_pack_id) errors.push("CP00-287 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP287_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-287 next subphase drift");
  }
  return freezeCp287Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp287HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp287P05CloseoutP06PermissionFoundationDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp287Coverage(planPack);
  const foundation = validateEmailDmsCoreCp287P05CloseoutP06PermissionFoundationDescriptor(descriptor, contractProjection);
  return freezeCp287Validation({
    evidence_packet: "H08.CP00-287.email_dms_core_p05_closeout_p06_permission_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP287_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_closeout_p06_permission_foundation_valid: foundation.valid,
    no_real_data: true,
    source_fixture_slice_pack_id: EMAIL_DMS_CORE_CP287_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP287_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP287_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP287_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createEmailDmsCoreCp287ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp287Coverage(planPack);
  const foundation = validateEmailDmsCoreCp287P05CloseoutP06PermissionFoundationDescriptor(createEmailDmsCoreCp287P05CloseoutP06PermissionFoundationDescriptor(), {});
  return freezeCp287Validation({
    review_packet: "C08.CP00-287.email_dms_core_p05_closeout_p06_permission_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP287_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP287_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_closeout_p06_permission_foundation_valid: foundation.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP287_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-287 cover exactly the 150 planned RP08 units from RP08.P05.M03.S19 through RP08.P06.M00.S06?",
      "Do the nine descriptor sections (M03 fixture tail with closeout handoff, no-real-data, stable-ID, and replay rows; M04-M08 full fixture cycles with stable-ID and replay rows; M09 Claude review packet; M10 closeout head; and the P06 M00 permission matrix head with view/search/mutation/export/share decision bindings) cover every planned row title as descriptor-only rows, with the shared fixture and permission guards applied across every section that carries them?",
      "Does CP00-287 avoid email/Office-native/sync/filing runtime dispatch, AI runtime dispatch, runtime permission evaluation, real fixture data loading, test runtime paths, permission decision leaks, audit hint leaks, fixture payload leaks, state writes, object storage, command runtime execution, Hermes runtime receipts, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp287CloseoutHandoff() {
  return freezeCp287Validation({
    handoff_id: "CP00-287-to-CP00-288",
    from_pack_id: EMAIL_DMS_CORE_CP287_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP287_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP287_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP287_PACK_BINDING.range,
    open_scope: "RP08.P05 descriptor scope is closed; continue RP08.P06.M00.S07 onward with the remaining permission rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP287_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp288CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp288Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp288Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-288 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-288");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP288_PACK_BINDING.risk_class) errors.push("CP00-288 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP288_PACK_BINDING.unit_count) errors.push("CP00-288 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP288_PACK_BINDING.first_unit_id) errors.push("CP00-288 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP288_PACK_BINDING.last_unit_id) errors.push("CP00-288 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-288 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-288 must only include RP08 units");
  const summary = createEmailDmsCoreCp288CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP288_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-288 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP288_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-288 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP288_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-288 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP288_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-288 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP288_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-288 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-288 ${microId} missing row ${title}`);
    }
  }
  return freezeCp288Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp288PermissionSliceDescriptor(
  descriptor = createEmailDmsCoreCp288PermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.permission_slice_case_set ?? createEmailDmsCoreCp288PermissionSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp288PermissionSliceDescriptor") errors.push("CP00-288 descriptor type drift");
  if (descriptor.source_p05_closeout_p06_permission_foundation_descriptor !== "EmailDmsCoreCp287P05CloseoutP06PermissionFoundationDescriptor") {
    errors.push("CP00-288 source p05 closeout p06 permission foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP288_REQUIREMENTS.required_section_rows).length) errors.push("CP00-288 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP288_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-288 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-288 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-288 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-288 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-288 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-288 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-288 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP288_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.permission_matrix_row && rows.permission_matrix_row.deny_over_allow_enforced !== true) {
      errors.push(`CP00-288 ${microId} permission matrix deny-over-allow drift`);
    }
    for (const bindingKey of ["view_decision_binding", "search_decision_binding", "mutation_decision_binding", "export_download_decision_binding", "share_decision_binding", "ai_retrieval_decision_binding"]) {
      if (rows[bindingKey] && rows[bindingKey].permission_decision_detail_included !== false) {
        errors.push(`CP00-288 ${microId} ${bindingKey} must not expose permission decisions`);
      }
      if (rows[bindingKey] && rows[bindingKey].deny_over_allow_enforced !== true) {
        errors.push(`CP00-288 ${microId} ${bindingKey} deny-over-allow drift`);
      }
    }
    if (rows.audit_hint_fields && rows.audit_hint_fields.audit_hint_detail_included !== false) {
      errors.push(`CP00-288 ${microId} audit hint fields must not expose hints`);
    }
    if (rows.deny_over_allow_check && rows.deny_over_allow_check.deny_over_allow_enforced !== true) {
      errors.push(`CP00-288 ${microId} deny-over-allow check drift`);
    }
    if (rows.security_trimming_proof && rows.security_trimming_proof.unauthorized_data_omitted !== true) {
      errors.push(`CP00-288 ${microId} security trimming drift`);
    }
    if (rows.audit_event_expectation && rows.audit_event_expectation.writes_audit_event !== false) {
      errors.push(`CP00-288 ${microId} must not write audit events`);
    }
    if (rows.permission_fixture && rows.permission_fixture.real_client_data_loaded !== false) {
      errors.push(`CP00-288 ${microId} permission fixture must not load real data`);
    }
    if (rows.allowed_test && rows.allowed_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-288 ${microId} must not execute test runtime`);
    }
    if (rows.cross_tenant_test && rows.cross_tenant_test.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-288 ${microId} cross-tenant test must deny access`);
    }
    if (rows.leak_prevention_test && rows.leak_prevention_test.leak_detected !== false) {
      errors.push(`CP00-288 ${microId} leak detected`);
    }
  }
  if (EMAIL_DMS_CORE_CP288_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-288 must not promote Claude");
  if (EMAIL_DMS_CORE_CP288_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-288 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-288 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-288 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-288 contract current_pack drift");
  }
  if (
    contractProjection?.permission_slice_descriptor?.descriptor &&
    contractProjection.permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-288 contract permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP288_PACK_BINDING.next_pack_id) errors.push("CP00-288 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP288_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-288 next subphase drift");
  }
  return freezeCp288Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp288HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp288PermissionSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp288Coverage(planPack);
  const slice = validateEmailDmsCoreCp288PermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp288Validation({
    evidence_packet: "H08.CP00-288.email_dms_core_permission_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP288_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_closeout_p06_permission_foundation_pack_id: EMAIL_DMS_CORE_CP288_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP288_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP288_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP288_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp288ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp288Coverage(planPack);
  const slice = validateEmailDmsCoreCp288PermissionSliceDescriptor(createEmailDmsCoreCp288PermissionSliceDescriptor(), {});
  return freezeCp288Validation({
    review_packet: "C08.CP00-288.email_dms_core_permission_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP288_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP288_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    permission_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP288_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-288 cover exactly the 150 planned RP08 units from RP08.P06.M00.S07 through RP08.P06.M07.S06?",
      "Do the eight descriptor sections (M00 scope inventory tail, M01 contract draft, M02-M06 full permission cycles with cross-tenant and leak-prevention tests, M07 test/golden head) cover every planned row title as descriptor-only rows, including permission matrix rows, view/search/mutation/export/share/AI-retrieval decision bindings, audit hint fields, matched rule captures, deny-over-allow checks, legal hold/ethical wall/object ACL interactions, review/approval routes, security trimming proofs, audit event expectations, permission fixtures, and allowed/denied tests?",
      "Does CP00-288 avoid email/Office-native/sync/filing runtime dispatch, AI runtime dispatch, runtime permission evaluation, audit event writes, permission decision leaks, audit hint leaks, fixture payload leaks, cross-tenant access, state writes, object storage, command runtime execution, Hermes runtime receipts, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp288CloseoutHandoff() {
  return freezeCp288Validation({
    handoff_id: "CP00-288-to-CP00-289",
    from_pack_id: EMAIL_DMS_CORE_CP288_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP288_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP288_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP288_PACK_BINDING.range,
    open_scope: "Continue RP08.P06.M07.S07 onward with the remaining permission test/golden rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP288_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp289CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp289Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp289Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-289 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-289");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP289_PACK_BINDING.risk_class) errors.push("CP00-289 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP289_PACK_BINDING.unit_count) errors.push("CP00-289 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP289_PACK_BINDING.first_unit_id) errors.push("CP00-289 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP289_PACK_BINDING.last_unit_id) errors.push("CP00-289 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-289 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-289 must only include RP08 units");
  const summary = createEmailDmsCoreCp289CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP289_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-289 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP289_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-289 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP289_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-289 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP289_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-289 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP289_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-289 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-289 ${microId} missing row ${title}`);
    }
  }
  return freezeCp289Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp289PermissionBindingSliceDescriptor(
  descriptor = createEmailDmsCoreCp289PermissionBindingSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.permission_binding_slice_case_set ?? createEmailDmsCoreCp289PermissionBindingSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp289PermissionBindingSliceDescriptor") errors.push("CP00-289 descriptor type drift");
  if (descriptor.source_permission_slice_descriptor !== "EmailDmsCoreCp288PermissionSliceDescriptor") {
    errors.push("CP00-289 source permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP289_REQUIREMENTS.required_section_rows).length) errors.push("CP00-289 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP289_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-289 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-289 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-289 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-289 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-289 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-289 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-289 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP289_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.permission_matrix_row && rows.permission_matrix_row.deny_over_allow_enforced !== true) {
      errors.push(`CP00-289 ${microId} permission matrix deny-over-allow drift`);
    }
    for (const bindingKey of ["view_decision_binding", "search_decision_binding", "mutation_decision_binding", "export_download_decision_binding", "share_decision_binding", "ai_retrieval_decision_binding"]) {
      if (rows[bindingKey] && rows[bindingKey].permission_decision_detail_included !== false) {
        errors.push(`CP00-289 ${microId} ${bindingKey} must not expose permission decisions`);
      }
      if (rows[bindingKey] && rows[bindingKey].deny_over_allow_enforced !== true) {
        errors.push(`CP00-289 ${microId} ${bindingKey} deny-over-allow drift`);
      }
    }
    if (rows.audit_hint_fields && rows.audit_hint_fields.audit_hint_detail_included !== false) {
      errors.push(`CP00-289 ${microId} audit hint fields must not expose hints`);
    }
    if (rows.deny_over_allow_check && rows.deny_over_allow_check.deny_over_allow_enforced !== true) {
      errors.push(`CP00-289 ${microId} deny-over-allow check drift`);
    }
    if (rows.security_trimming_proof && rows.security_trimming_proof.unauthorized_data_omitted !== true) {
      errors.push(`CP00-289 ${microId} security trimming drift`);
    }
    if (rows.audit_event_expectation && rows.audit_event_expectation.writes_audit_event !== false) {
      errors.push(`CP00-289 ${microId} must not write audit events`);
    }
    if (rows.permission_fixture && rows.permission_fixture.real_client_data_loaded !== false) {
      errors.push(`CP00-289 ${microId} permission fixture must not load real data`);
    }
    if (rows.allowed_test && rows.allowed_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-289 ${microId} must not execute test runtime`);
    }
    if (rows.cross_tenant_test && rows.cross_tenant_test.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-289 ${microId} cross-tenant test must deny access`);
    }
    if (rows.leak_prevention_test && rows.leak_prevention_test.leak_detected !== false) {
      errors.push(`CP00-289 ${microId} leak detected`);
    }
  }
  if (EMAIL_DMS_CORE_CP289_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-289 must not promote Claude");
  if (EMAIL_DMS_CORE_CP289_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-289 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-289 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-289 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-289 contract current_pack drift");
  }
  if (
    contractProjection?.permission_binding_slice_descriptor?.descriptor &&
    contractProjection.permission_binding_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-289 contract permission_binding_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP289_PACK_BINDING.next_pack_id) errors.push("CP00-289 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP289_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-289 next subphase drift");
  }
  return freezeCp289Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp289HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp289PermissionBindingSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp289Coverage(planPack);
  const slice = validateEmailDmsCoreCp289PermissionBindingSliceDescriptor(descriptor, contractProjection);
  return freezeCp289Validation({
    evidence_packet: "H08.CP00-289.email_dms_core_permission_binding_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP289_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    permission_binding_slice_valid: slice.valid,
    no_real_data: true,
    source_permission_slice_pack_id: EMAIL_DMS_CORE_CP289_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP289_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP289_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP289_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp289ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp289Coverage(planPack);
  const slice = validateEmailDmsCoreCp289PermissionBindingSliceDescriptor(createEmailDmsCoreCp289PermissionBindingSliceDescriptor(), {});
  return freezeCp289Validation({
    review_packet: "C08.CP00-289.email_dms_core_permission_binding_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP289_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP289_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    permission_binding_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP289_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-289 cover exactly the 40 planned RP08 units from RP08.P06.M07.S07 through RP08.P06.M09.S02?",
      "Do the three descriptor sections (M07 test/golden tail from AI retrieval decision binding through leak prevention test, M08 full Hermes evidence permission cycle, M09 Claude review packet head) cover every planned row title as descriptor-only rows, with the shared permission guards applied across every section that carries them?",
      "Does CP00-289 avoid runtime permission evaluation, audit event writes, AI runtime dispatch, permission decision leaks, audit hint leaks, fixture payload leaks, cross-tenant access, test runtime paths, state writes, object storage, command runtime execution, Hermes runtime receipts, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp289CloseoutHandoff() {
  return freezeCp289Validation({
    handoff_id: "CP00-289-to-CP00-290",
    from_pack_id: EMAIL_DMS_CORE_CP289_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP289_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP289_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP289_PACK_BINDING.range,
    open_scope: "Continue RP08.P06.M09.S03 onward with the remaining Claude review packet rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP289_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp290CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp290Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp290Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-290 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-290");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP290_PACK_BINDING.risk_class) errors.push("CP00-290 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP290_PACK_BINDING.unit_count) errors.push("CP00-290 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP290_PACK_BINDING.first_unit_id) errors.push("CP00-290 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP290_PACK_BINDING.last_unit_id) errors.push("CP00-290 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-290 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-290 must only include RP08 units");
  const summary = createEmailDmsCoreCp290CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP290_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-290 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP290_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-290 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP290_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-290 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP290_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-290 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP290_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-290 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-290 ${microId} missing row ${title}`);
    }
  }
  return freezeCp290Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp290P06CloseoutP07FailureFoundationDescriptor(
  descriptor = createEmailDmsCoreCp290P06CloseoutP07FailureFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_closeout_p07_failure_foundation_case_set ?? createEmailDmsCoreCp290P06CloseoutP07FailureFoundationCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp290P06CloseoutP07FailureFoundationDescriptor") errors.push("CP00-290 descriptor type drift");
  if (descriptor.source_permission_binding_slice_descriptor !== "EmailDmsCoreCp289PermissionBindingSliceDescriptor") {
    errors.push("CP00-290 source permission binding slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP290_REQUIREMENTS.required_section_rows).length) errors.push("CP00-290 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP290_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-290 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-290 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-290 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-290 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-290 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-290 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-290 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP290_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.cross_tenant_failure && rows.cross_tenant_failure.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-290 ${microId} cross-tenant failure must deny access`);
    }
    if (rows.permission_denied_failure && rows.permission_denied_failure.permission_decision_detail_included !== false) {
      errors.push(`CP00-290 ${microId} permission denied failure must not expose decisions`);
    }
    if (rows.ambiguous_rule_failure && rows.ambiguous_rule_failure.deny_over_allow_enforced !== true) {
      errors.push(`CP00-290 ${microId} ambiguous rule failure deny-over-allow drift`);
    }
    if (rows.lock_conflict_failure && rows.lock_conflict_failure.acquires_runtime_lock !== false) {
      errors.push(`CP00-290 ${microId} lock conflict failure must not acquire locks`);
    }
    if (rows.retry_exhaustion_failure && rows.retry_exhaustion_failure.performs_retry_runtime !== false) {
      errors.push(`CP00-290 ${microId} retry exhaustion failure must not retry`);
    }
    if (rows.rollback_expectation && rows.rollback_expectation.performs_rollback_runtime !== false) {
      errors.push(`CP00-290 ${microId} rollback expectation must not perform rollback`);
    }
    if (rows.blocked_claim_receipt && rows.blocked_claim_receipt.blocked_claim_detail_included !== false) {
      errors.push(`CP00-290 ${microId} blocked-claim receipt must not expose details`);
    }
    if (rows.failure_fixture && rows.failure_fixture.real_client_data_loaded !== false) {
      errors.push(`CP00-290 ${microId} failure fixture must not load real data`);
    }
    if (rows.failure_unit_test && rows.failure_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-290 ${microId} must not execute test runtime`);
    }
    if (rows.failure_integration_smoke && rows.failure_integration_smoke.dispatches_integration_smoke_runtime !== false) {
      errors.push(`CP00-290 ${microId} must not dispatch integration smoke runtime`);
    }
    if (rows.audit_failure_hint && rows.audit_failure_hint.audit_hint_detail_included !== false) {
      errors.push(`CP00-290 ${microId} audit failure hint must not expose hints`);
    }
    if (rows.hermes_failure_evidence && rows.hermes_failure_evidence.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-290 ${microId} must not emit Hermes failure receipts`);
    }
    if (rows.claude_edge_case_prompt && rows.claude_edge_case_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-290 ${microId} must not claim Claude final approval`);
    }
    if (rows.human_escalation_note && rows.human_escalation_note.human_approval_route_required_before_runtime !== true) {
      errors.push(`CP00-290 ${microId} human escalation boundary missing`);
    }
    if (rows.permission_matrix_row && rows.permission_matrix_row.deny_over_allow_enforced !== true) {
      errors.push(`CP00-290 ${microId} permission matrix deny-over-allow drift`);
    }
    if (rows.cross_tenant_test && rows.cross_tenant_test.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-290 ${microId} cross-tenant test must deny access`);
    }
    if (rows.leak_prevention_test && rows.leak_prevention_test.leak_detected !== false) {
      errors.push(`CP00-290 ${microId} leak detected`);
    }
    if (rows.audit_event_expectation && rows.audit_event_expectation.writes_audit_event !== false) {
      errors.push(`CP00-290 ${microId} must not write audit events`);
    }
  }
  if (EMAIL_DMS_CORE_CP290_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-290 must not promote Claude");
  if (EMAIL_DMS_CORE_CP290_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-290 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-290 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-290 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-290 contract current_pack drift");
  }
  if (
    contractProjection?.p06_closeout_p07_failure_foundation_descriptor?.descriptor &&
    contractProjection.p06_closeout_p07_failure_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-290 contract p06_closeout_p07_failure_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP290_PACK_BINDING.next_pack_id) errors.push("CP00-290 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP290_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-290 next subphase drift");
  }
  return freezeCp290Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp290HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp290P06CloseoutP07FailureFoundationDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp290Coverage(planPack);
  const foundation = validateEmailDmsCoreCp290P06CloseoutP07FailureFoundationDescriptor(descriptor, contractProjection);
  return freezeCp290Validation({
    evidence_packet: "H08.CP00-290.email_dms_core_p06_closeout_p07_failure_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP290_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_closeout_p07_failure_foundation_valid: foundation.valid,
    no_real_data: true,
    source_permission_binding_slice_pack_id: EMAIL_DMS_CORE_CP290_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP290_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP290_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP290_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createEmailDmsCoreCp290ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp290Coverage(planPack);
  const foundation = validateEmailDmsCoreCp290P06CloseoutP07FailureFoundationDescriptor(createEmailDmsCoreCp290P06CloseoutP07FailureFoundationDescriptor(), {});
  return freezeCp290Validation({
    review_packet: "C08.CP00-290.email_dms_core_p06_closeout_p07_failure_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP290_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP290_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_closeout_p07_failure_foundation_valid: foundation.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP290_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-290 cover exactly the 150 planned RP08 units from RP08.P06.M09.S03 through RP08.P07.M05.S04?",
      "Do the eight descriptor sections (M09 Claude review packet tail, M10 closeout permission cycle, and six P07 failure micros with failure taxonomies, missing tenant/actor/Matter/resource failures, unknown action/cross-tenant/permission-denied/ambiguous-rule/stale-reference failures, lock conflict and retry exhaustion failures, rollback/compensation expectations, blocked-claim receipts, failure fixtures/tests, audit failure hints, Hermes failure evidence, Claude edge-case prompts, and human escalation notes) cover every planned row title as descriptor-only rows?",
      "Does CP00-290 avoid rollback/retry runtime, lock acquisition, runtime permission evaluation, audit event writes, permission decision leaks, blocked-claim detail leaks, audit hint leaks, fixture payload leaks, cross-tenant access, test runtime paths, state writes, object storage, command runtime execution, Hermes runtime receipts, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp290CloseoutHandoff() {
  return freezeCp290Validation({
    handoff_id: "CP00-290-to-CP00-291",
    from_pack_id: EMAIL_DMS_CORE_CP290_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP290_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP290_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP290_PACK_BINDING.range,
    open_scope: "RP08.P06 descriptor scope is closed; continue RP08.P07.M05.S05 onward with the remaining failure rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP290_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp291CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp291Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp291Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-291 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-291");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP291_PACK_BINDING.risk_class) errors.push("CP00-291 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP291_PACK_BINDING.unit_count) errors.push("CP00-291 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP291_PACK_BINDING.first_unit_id) errors.push("CP00-291 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP291_PACK_BINDING.last_unit_id) errors.push("CP00-291 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-291 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-291 must only include RP08 units");
  const summary = createEmailDmsCoreCp291CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP291_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-291 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP291_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-291 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP291_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-291 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP291_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-291 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP291_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-291 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-291 ${microId} missing row ${title}`);
    }
  }
  return freezeCp291Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp291FailureSliceDescriptor(
  descriptor = createEmailDmsCoreCp291FailureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.failure_slice_case_set ?? createEmailDmsCoreCp291FailureSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp291FailureSliceDescriptor") errors.push("CP00-291 descriptor type drift");
  if (descriptor.source_p06_closeout_p07_failure_foundation_descriptor !== "EmailDmsCoreCp290P06CloseoutP07FailureFoundationDescriptor") {
    errors.push("CP00-291 source p06 closeout p07 failure foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP291_REQUIREMENTS.required_section_rows).length) errors.push("CP00-291 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP291_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-291 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-291 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-291 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-291 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-291 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-291 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-291 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP291_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.cross_tenant_failure && rows.cross_tenant_failure.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-291 ${microId} cross-tenant failure must deny access`);
    }
    if (rows.permission_denied_failure && rows.permission_denied_failure.permission_decision_detail_included !== false) {
      errors.push(`CP00-291 ${microId} permission denied failure must not expose decisions`);
    }
    if (rows.ambiguous_rule_failure && rows.ambiguous_rule_failure.deny_over_allow_enforced !== true) {
      errors.push(`CP00-291 ${microId} ambiguous rule failure deny-over-allow drift`);
    }
    if (rows.lock_conflict_failure && rows.lock_conflict_failure.acquires_runtime_lock !== false) {
      errors.push(`CP00-291 ${microId} lock conflict failure must not acquire locks`);
    }
    if (rows.retry_exhaustion_failure && rows.retry_exhaustion_failure.performs_retry_runtime !== false) {
      errors.push(`CP00-291 ${microId} retry exhaustion failure must not retry`);
    }
    if (rows.rollback_expectation && rows.rollback_expectation.performs_rollback_runtime !== false) {
      errors.push(`CP00-291 ${microId} rollback expectation must not perform rollback`);
    }
    if (rows.compensation_expectation && rows.compensation_expectation.compensation_descriptor_only !== true) {
      errors.push(`CP00-291 ${microId} compensation expectation drift`);
    }
  }
  if (EMAIL_DMS_CORE_CP291_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-291 must not promote Claude");
  if (EMAIL_DMS_CORE_CP291_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-291 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-291 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-291 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-291 contract current_pack drift");
  }
  if (
    contractProjection?.failure_slice_descriptor?.descriptor &&
    contractProjection.failure_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-291 contract failure_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP291_PACK_BINDING.next_pack_id) errors.push("CP00-291 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP291_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-291 next subphase drift");
  }
  return freezeCp291Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp291HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp291FailureSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp291Coverage(planPack);
  const slice = validateEmailDmsCoreCp291FailureSliceDescriptor(descriptor, contractProjection);
  return freezeCp291Validation({
    evidence_packet: "H08.CP00-291.email_dms_core_failure_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP291_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    failure_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_closeout_p07_failure_foundation_pack_id: EMAIL_DMS_CORE_CP291_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP291_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP291_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP291_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp291ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp291Coverage(planPack);
  const slice = validateEmailDmsCoreCp291FailureSliceDescriptor(createEmailDmsCoreCp291FailureSliceDescriptor(), {});
  return freezeCp291Validation({
    review_packet: "C08.CP00-291.email_dms_core_failure_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP291_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP291_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    failure_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP291_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-291 cover exactly the 10 planned RP08 units from RP08.P07.M05.S05 through RP08.P07.M05.S14?",
      "Does the single M05 permission/audit binding failure section (missing resource, unknown action, cross-tenant, permission denied, ambiguous rule, stale reference, lock conflict, retry exhaustion failures, rollback and compensation expectations) cover every planned row title as descriptor-only rows, with the shared failure guards applied?",
      "Does CP00-291 avoid rollback/retry runtime, lock acquisition, runtime permission evaluation, permission decision leaks, blocked-claim detail leaks, cross-tenant access, state writes, object storage, command runtime execution, Hermes runtime receipts, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp291CloseoutHandoff() {
  return freezeCp291Validation({
    handoff_id: "CP00-291-to-CP00-292",
    from_pack_id: EMAIL_DMS_CORE_CP291_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP291_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP291_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP291_PACK_BINDING.range,
    open_scope: "Continue RP08.P07.M05.S15 onward with the remaining failure binding rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP291_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp292CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp292Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp292Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-292 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-292");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP292_PACK_BINDING.risk_class) errors.push("CP00-292 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP292_PACK_BINDING.unit_count) errors.push("CP00-292 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP292_PACK_BINDING.first_unit_id) errors.push("CP00-292 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP292_PACK_BINDING.last_unit_id) errors.push("CP00-292 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-292 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-292 must only include RP08 units");
  const summary = createEmailDmsCoreCp292CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP292_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-292 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP292_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-292 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP292_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-292 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP292_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-292 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP292_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-292 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-292 ${microId} missing row ${title}`);
    }
  }
  return freezeCp292Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp292FailureBindingSliceDescriptor(
  descriptor = createEmailDmsCoreCp292FailureBindingSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.failure_binding_slice_case_set ?? createEmailDmsCoreCp292FailureBindingSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp292FailureBindingSliceDescriptor") errors.push("CP00-292 descriptor type drift");
  if (descriptor.source_failure_slice_descriptor !== "EmailDmsCoreCp291FailureSliceDescriptor") {
    errors.push("CP00-292 source failure slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP292_REQUIREMENTS.required_section_rows).length) errors.push("CP00-292 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP292_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-292 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-292 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-292 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-292 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-292 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-292 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-292 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP292_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.blocked_claim_receipt && rows.blocked_claim_receipt.blocked_claim_detail_included !== false) {
      errors.push(`CP00-292 ${microId} blocked-claim receipt must not expose details`);
    }
    if (rows.failure_fixture && rows.failure_fixture.real_client_data_loaded !== false) {
      errors.push(`CP00-292 ${microId} failure fixture must not load real data`);
    }
    if (rows.failure_unit_test && rows.failure_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-292 ${microId} must not execute test runtime`);
    }
    if (rows.failure_integration_smoke && rows.failure_integration_smoke.dispatches_integration_smoke_runtime !== false) {
      errors.push(`CP00-292 ${microId} must not dispatch integration smoke runtime`);
    }
    if (rows.audit_failure_hint && rows.audit_failure_hint.audit_hint_detail_included !== false) {
      errors.push(`CP00-292 ${microId} audit failure hint must not expose hints`);
    }
    if (rows.hermes_failure_evidence && rows.hermes_failure_evidence.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-292 ${microId} must not emit Hermes failure receipts`);
    }
    if (rows.claude_edge_case_prompt && rows.claude_edge_case_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-292 ${microId} must not claim Claude final approval`);
    }
    if (rows.human_escalation_note && rows.human_escalation_note.human_approval_route_required_before_runtime !== true) {
      errors.push(`CP00-292 ${microId} human escalation boundary missing`);
    }
    if (rows.missing_tenant_failure && rows.missing_tenant_failure.expected_outcome !== "rejected_customer_safe") {
      errors.push(`CP00-292 ${microId} missing tenant failure outcome drift`);
    }
  }
  if (EMAIL_DMS_CORE_CP292_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-292 must not promote Claude");
  if (EMAIL_DMS_CORE_CP292_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-292 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-292 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-292 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-292 contract current_pack drift");
  }
  if (
    contractProjection?.failure_binding_slice_descriptor?.descriptor &&
    contractProjection.failure_binding_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-292 contract failure_binding_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP292_PACK_BINDING.next_pack_id) errors.push("CP00-292 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP292_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-292 next subphase drift");
  }
  return freezeCp292Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp292HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp292FailureBindingSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp292Coverage(planPack);
  const slice = validateEmailDmsCoreCp292FailureBindingSliceDescriptor(descriptor, contractProjection);
  return freezeCp292Validation({
    evidence_packet: "H08.CP00-292.email_dms_core_failure_binding_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP292_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    failure_binding_slice_valid: slice.valid,
    no_real_data: true,
    source_failure_slice_pack_id: EMAIL_DMS_CORE_CP292_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP292_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP292_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP292_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp292ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp292Coverage(planPack);
  const slice = validateEmailDmsCoreCp292FailureBindingSliceDescriptor(createEmailDmsCoreCp292FailureBindingSliceDescriptor(), {});
  return freezeCp292Validation({
    review_packet: "C08.CP00-292.email_dms_core_failure_binding_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP292_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP292_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    failure_binding_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP292_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-292 cover exactly the 10 planned RP08 units from RP08.P07.M05.S15 through RP08.P07.M06.S02?",
      "Do the two descriptor sections (M05 permission/audit binding failure tail with blocked-claim receipt, failure fixture, failure unit test, failure integration smoke, audit failure hint, Hermes failure evidence, Claude edge-case prompt, and human escalation note; M06 synthetic fixture head with failure taxonomy and missing tenant failure) cover every planned row title as descriptor-only rows, with the shared failure guards applied?",
      "Does CP00-292 avoid rollback/retry runtime, test runtime paths, blocked-claim detail leaks, audit hint leaks, fixture payload leaks, state writes, object storage, command runtime execution, Hermes runtime receipts, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp292CloseoutHandoff() {
  return freezeCp292Validation({
    handoff_id: "CP00-292-to-CP00-293",
    from_pack_id: EMAIL_DMS_CORE_CP292_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP292_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP292_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP292_PACK_BINDING.range,
    open_scope: "Continue RP08.P07.M06.S03 onward with the remaining failure fixture rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP292_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp293CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp293Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp293Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-293 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-293");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP293_PACK_BINDING.risk_class) errors.push("CP00-293 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP293_PACK_BINDING.unit_count) errors.push("CP00-293 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP293_PACK_BINDING.first_unit_id) errors.push("CP00-293 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP293_PACK_BINDING.last_unit_id) errors.push("CP00-293 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-293 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-293 must only include RP08 units");
  const summary = createEmailDmsCoreCp293CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP293_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-293 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP293_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-293 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP293_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-293 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP293_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-293 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP293_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-293 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-293 ${microId} missing row ${title}`);
    }
  }
  return freezeCp293Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp293P07CloseoutP08HermesFoundationDescriptor(
  descriptor = createEmailDmsCoreCp293P07CloseoutP08HermesFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_closeout_p08_hermes_foundation_case_set ?? createEmailDmsCoreCp293P07CloseoutP08HermesFoundationCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp293P07CloseoutP08HermesFoundationDescriptor") errors.push("CP00-293 descriptor type drift");
  if (descriptor.source_failure_binding_slice_descriptor !== "EmailDmsCoreCp292FailureBindingSliceDescriptor") {
    errors.push("CP00-293 source failure binding slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP293_REQUIREMENTS.required_section_rows).length) errors.push("CP00-293 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP293_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-293 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-293 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-293 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-293 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-293 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-293 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-293 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP293_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.cross_tenant_failure && rows.cross_tenant_failure.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-293 ${microId} cross-tenant failure must deny access`);
    }
    if (rows.lock_conflict_failure && rows.lock_conflict_failure.acquires_runtime_lock !== false) {
      errors.push(`CP00-293 ${microId} lock conflict failure must not acquire locks`);
    }
    if (rows.rollback_expectation && rows.rollback_expectation.performs_rollback_runtime !== false) {
      errors.push(`CP00-293 ${microId} rollback expectation must not perform rollback`);
    }
    if (rows.blocked_claim_receipt && rows.blocked_claim_receipt.blocked_claim_detail_included !== false) {
      errors.push(`CP00-293 ${microId} blocked-claim receipt must not expose details`);
    }
    if (rows.hermes_failure_evidence && rows.hermes_failure_evidence.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-293 ${microId} must not emit Hermes failure receipts`);
    }
    if (rows.claude_edge_case_prompt && rows.claude_edge_case_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-293 ${microId} must not claim Claude final approval`);
    }
    if (rows.human_escalation_note && rows.human_escalation_note.human_approval_route_required_before_runtime !== true) {
      errors.push(`CP00-293 ${microId} human escalation boundary missing`);
    }
    if (rows.hermes_command_matrix && rows.hermes_command_matrix.executes_command_runtime !== false) {
      errors.push(`CP00-293 ${microId} Hermes command matrix must not execute runtime`);
    }
    if (rows.command_result_receipt && rows.command_result_receipt.executes_command_runtime !== false) {
      errors.push(`CP00-293 ${microId} command result receipt must not execute runtime`);
    }
    if (rows.fixture_summary_receipt && rows.fixture_summary_receipt.fixture_payload_included !== false) {
      errors.push(`CP00-293 ${microId} fixture summary receipt must not include payloads`);
    }
    if (rows.permission_summary_receipt && rows.permission_summary_receipt.permission_decision_detail_included !== false) {
      errors.push(`CP00-293 ${microId} permission summary receipt must not expose decisions`);
    }
    if (rows.audit_summary_receipt && rows.audit_summary_receipt.audit_hint_detail_included !== false) {
      errors.push(`CP00-293 ${microId} audit summary receipt must not expose hints`);
    }
    if (rows.no_real_data_receipt && rows.no_real_data_receipt.real_client_data_loaded !== false) {
      errors.push(`CP00-293 ${microId} no-real-data receipt drift`);
    }
    if (rows.claude_dependency_marker && rows.claude_dependency_marker.claude_final_approval_claimed !== false) {
      errors.push(`CP00-293 ${microId} Claude dependency marker must not claim final approval`);
    }
    if (rows.human_approval_marker && rows.human_approval_marker.human_approval_route_required_before_runtime !== true) {
      errors.push(`CP00-293 ${microId} human approval marker boundary missing`);
    }
    if (rows.validation_command_check && rows.validation_command_check.executes_command_runtime !== false) {
      errors.push(`CP00-293 ${microId} validation command check must not execute runtime`);
    }
  }
  if (EMAIL_DMS_CORE_CP293_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-293 must not promote Claude");
  if (EMAIL_DMS_CORE_CP293_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-293 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-293 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-293 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-293 contract current_pack drift");
  }
  if (
    contractProjection?.p07_closeout_p08_hermes_foundation_descriptor?.descriptor &&
    contractProjection.p07_closeout_p08_hermes_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-293 contract p07_closeout_p08_hermes_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP293_PACK_BINDING.next_pack_id) errors.push("CP00-293 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP293_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-293 next subphase drift");
  }
  return freezeCp293Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp293HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp293P07CloseoutP08HermesFoundationDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp293Coverage(planPack);
  const foundation = validateEmailDmsCoreCp293P07CloseoutP08HermesFoundationDescriptor(descriptor, contractProjection);
  return freezeCp293Validation({
    evidence_packet: "H08.CP00-293.email_dms_core_p07_closeout_p08_hermes_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP293_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_closeout_p08_hermes_foundation_valid: foundation.valid,
    no_real_data: true,
    source_failure_binding_slice_pack_id: EMAIL_DMS_CORE_CP293_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP293_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP293_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP293_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createEmailDmsCoreCp293ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp293Coverage(planPack);
  const foundation = validateEmailDmsCoreCp293P07CloseoutP08HermesFoundationDescriptor(createEmailDmsCoreCp293P07CloseoutP08HermesFoundationDescriptor(), {});
  return freezeCp293Validation({
    review_packet: "C08.CP00-293.email_dms_core_p07_closeout_p08_hermes_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP293_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP293_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_closeout_p08_hermes_foundation_valid: foundation.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP293_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-293 cover exactly the 150 planned RP08 units from RP08.P07.M06.S03 through RP08.P08.M02.S14?",
      "Do the eight descriptor sections (M06 fixture tail, M07-M09 full failure cycles, M10 closeout failure head, and three P08 Hermes micros with command matrices, evidence field lists, changed-file/command-result/fixture-summary/blocked-claim/permission-summary/audit-summary/no-real-data receipts, Claude dependency markers, human approval markers, and PASS/PASS_WITH_FINDINGS/BLOCK semantics) cover every planned row title as descriptor-only rows?",
      "Does CP00-293 avoid Hermes runtime receipts, command runtime execution, rollback/retry runtime, lock acquisition, permission decision leaks, blocked-claim detail leaks, audit hint leaks, fixture payload leaks, cross-tenant access, state writes, object storage, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp293CloseoutHandoff() {
  return freezeCp293Validation({
    handoff_id: "CP00-293-to-CP00-294",
    from_pack_id: EMAIL_DMS_CORE_CP293_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP293_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP293_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP293_PACK_BINDING.range,
    open_scope: "RP08.P07 descriptor scope is closed; continue RP08.P08.M02.S15 onward with the remaining Hermes receipt rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP293_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp294CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp294Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp294Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-294 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-294");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP294_PACK_BINDING.risk_class) errors.push("CP00-294 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP294_PACK_BINDING.unit_count) errors.push("CP00-294 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP294_PACK_BINDING.first_unit_id) errors.push("CP00-294 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP294_PACK_BINDING.last_unit_id) errors.push("CP00-294 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-294 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-294 must only include RP08 units");
  const summary = createEmailDmsCoreCp294CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP294_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-294 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP294_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-294 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP294_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-294 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP294_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-294 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP294_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-294 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-294 ${microId} missing row ${title}`);
    }
  }
  return freezeCp294Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp294HermesSliceDescriptor(
  descriptor = createEmailDmsCoreCp294HermesSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.hermes_slice_case_set ?? createEmailDmsCoreCp294HermesSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp294HermesSliceDescriptor") errors.push("CP00-294 descriptor type drift");
  if (descriptor.source_p07_closeout_p08_hermes_foundation_descriptor !== "EmailDmsCoreCp293P07CloseoutP08HermesFoundationDescriptor") {
    errors.push("CP00-294 source p07 closeout p08 hermes foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP294_REQUIREMENTS.required_section_rows).length) errors.push("CP00-294 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP294_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-294 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-294 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-294 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-294 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-294 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-294 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-294 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP294_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.hermes_command_matrix && rows.hermes_command_matrix.executes_command_runtime !== false) {
      errors.push(`CP00-294 ${microId} Hermes command matrix must not execute runtime`);
    }
    if (rows.command_result_receipt && rows.command_result_receipt.executes_command_runtime !== false) {
      errors.push(`CP00-294 ${microId} command result receipt must not execute runtime`);
    }
    if (rows.fixture_summary_receipt && rows.fixture_summary_receipt.fixture_payload_included !== false) {
      errors.push(`CP00-294 ${microId} fixture summary receipt must not include payloads`);
    }
    if (rows.blocked_claim_receipt && rows.blocked_claim_receipt.blocked_claim_detail_included !== false) {
      errors.push(`CP00-294 ${microId} blocked-claim receipt must not expose details`);
    }
    if (rows.permission_summary_receipt && rows.permission_summary_receipt.permission_decision_detail_included !== false) {
      errors.push(`CP00-294 ${microId} permission summary receipt must not expose decisions`);
    }
    if (rows.audit_summary_receipt && rows.audit_summary_receipt.audit_hint_detail_included !== false) {
      errors.push(`CP00-294 ${microId} audit summary receipt must not expose hints`);
    }
    if (rows.no_real_data_receipt && rows.no_real_data_receipt.real_client_data_loaded !== false) {
      errors.push(`CP00-294 ${microId} no-real-data receipt drift`);
    }
    if (rows.claude_dependency_marker && rows.claude_dependency_marker.claude_final_approval_claimed !== false) {
      errors.push(`CP00-294 ${microId} Claude dependency marker must not claim final approval`);
    }
    if (rows.human_approval_marker && rows.human_approval_marker.human_approval_route_required_before_runtime !== true) {
      errors.push(`CP00-294 ${microId} human approval marker boundary missing`);
    }
    if (rows.validation_command_check && rows.validation_command_check.executes_command_runtime !== false) {
      errors.push(`CP00-294 ${microId} validation command check must not execute runtime`);
    }
  }
  if (EMAIL_DMS_CORE_CP294_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-294 must not promote Claude");
  if (EMAIL_DMS_CORE_CP294_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-294 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-294 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-294 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-294 contract current_pack drift");
  }
  if (
    contractProjection?.hermes_slice_descriptor?.descriptor &&
    contractProjection.hermes_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-294 contract hermes_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP294_PACK_BINDING.next_pack_id) errors.push("CP00-294 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP294_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-294 next subphase drift");
  }
  return freezeCp294Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp294HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp294HermesSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp294Coverage(planPack);
  const slice = validateEmailDmsCoreCp294HermesSliceDescriptor(descriptor, contractProjection);
  return freezeCp294Validation({
    evidence_packet: "H08.CP00-294.email_dms_core_hermes_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP294_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    hermes_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_closeout_p08_hermes_foundation_pack_id: EMAIL_DMS_CORE_CP294_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP294_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP294_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP294_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp294ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp294Coverage(planPack);
  const slice = validateEmailDmsCoreCp294HermesSliceDescriptor(createEmailDmsCoreCp294HermesSliceDescriptor(), {});
  return freezeCp294Validation({
    review_packet: "C08.CP00-294.email_dms_core_hermes_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP294_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP294_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    hermes_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP294_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-294 cover exactly the 40 planned RP08 units from RP08.P08.M02.S15 through RP08.P08.M04.S12?",
      "Do the three descriptor sections (M02 type/shape tail with evidence templates, validation command checks, harness boundary notes, closeout handoffs, regression receipts, and next-gate readiness; M03 full Hermes cycle with documentation update and operator summary; M04 secondary workflow head) cover every planned row title as descriptor-only rows, with the shared Hermes guards applied across every section that carries them?",
      "Does CP00-294 avoid Hermes runtime receipts, command runtime execution, permission decision leaks, blocked-claim detail leaks, audit hint leaks, fixture payload leaks, state writes, object storage, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp294CloseoutHandoff() {
  return freezeCp294Validation({
    handoff_id: "CP00-294-to-CP00-295",
    from_pack_id: EMAIL_DMS_CORE_CP294_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP294_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP294_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP294_PACK_BINDING.range,
    open_scope: "Continue RP08.P08.M04.S13 onward with the remaining Hermes receipt rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP294_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp295CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp295Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp295Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-295 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-295");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP295_PACK_BINDING.risk_class) errors.push("CP00-295 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP295_PACK_BINDING.unit_count) errors.push("CP00-295 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP295_PACK_BINDING.first_unit_id) errors.push("CP00-295 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP295_PACK_BINDING.last_unit_id) errors.push("CP00-295 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-295 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-295 must only include RP08 units");
  const summary = createEmailDmsCoreCp295CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP295_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-295 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP295_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-295 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP295_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-295 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP295_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-295 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP295_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-295 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-295 ${microId} missing row ${title}`);
    }
  }
  return freezeCp295Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp295HermesBindingSliceDescriptor(
  descriptor = createEmailDmsCoreCp295HermesBindingSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.hermes_binding_slice_case_set ?? createEmailDmsCoreCp295HermesBindingSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp295HermesBindingSliceDescriptor") errors.push("CP00-295 descriptor type drift");
  if (descriptor.source_hermes_slice_descriptor !== "EmailDmsCoreCp294HermesSliceDescriptor") {
    errors.push("CP00-295 source hermes slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP295_REQUIREMENTS.required_section_rows).length) errors.push("CP00-295 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP295_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-295 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-295 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-295 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-295 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-295 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-295 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-295 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP295_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.hermes_command_matrix && rows.hermes_command_matrix.executes_command_runtime !== false) {
      errors.push(`CP00-295 ${microId} Hermes command matrix must not execute runtime`);
    }
    if (rows.command_result_receipt && rows.command_result_receipt.executes_command_runtime !== false) {
      errors.push(`CP00-295 ${microId} command result receipt must not execute runtime`);
    }
    if (rows.fixture_summary_receipt && rows.fixture_summary_receipt.fixture_payload_included !== false) {
      errors.push(`CP00-295 ${microId} fixture summary receipt must not include payloads`);
    }
    if (rows.blocked_claim_receipt && rows.blocked_claim_receipt.blocked_claim_detail_included !== false) {
      errors.push(`CP00-295 ${microId} blocked-claim receipt must not expose details`);
    }
    if (rows.permission_summary_receipt && rows.permission_summary_receipt.permission_decision_detail_included !== false) {
      errors.push(`CP00-295 ${microId} permission summary receipt must not expose decisions`);
    }
    if (rows.audit_summary_receipt && rows.audit_summary_receipt.audit_hint_detail_included !== false) {
      errors.push(`CP00-295 ${microId} audit summary receipt must not expose hints`);
    }
    if (rows.no_real_data_receipt && rows.no_real_data_receipt.real_client_data_loaded !== false) {
      errors.push(`CP00-295 ${microId} no-real-data receipt drift`);
    }
    if (rows.claude_dependency_marker && rows.claude_dependency_marker.claude_final_approval_claimed !== false) {
      errors.push(`CP00-295 ${microId} Claude dependency marker must not claim final approval`);
    }
    if (rows.human_approval_marker && rows.human_approval_marker.human_approval_route_required_before_runtime !== true) {
      errors.push(`CP00-295 ${microId} human approval marker boundary missing`);
    }
    if (rows.validation_command_check && rows.validation_command_check.executes_command_runtime !== false) {
      errors.push(`CP00-295 ${microId} validation command check must not execute runtime`);
    }
  }
  if (EMAIL_DMS_CORE_CP295_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-295 must not promote Claude");
  if (EMAIL_DMS_CORE_CP295_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-295 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-295 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-295 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![
      EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
      EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    ].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-295 contract current_pack drift");
  }
  if (
    contractProjection?.hermes_binding_slice_descriptor?.descriptor &&
    contractProjection.hermes_binding_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-295 contract hermes_binding_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP295_PACK_BINDING.next_pack_id) errors.push("CP00-295 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP295_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-295 next subphase drift");
  }
  return freezeCp295Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp295HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp295HermesBindingSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp295Coverage(planPack);
  const slice = validateEmailDmsCoreCp295HermesBindingSliceDescriptor(descriptor, contractProjection);
  return freezeCp295Validation({
    evidence_packet: "H08.CP00-295.email_dms_core_hermes_binding_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP295_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    hermes_binding_slice_valid: slice.valid,
    no_real_data: true,
    source_hermes_slice_pack_id: EMAIL_DMS_CORE_CP295_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP295_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP295_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP295_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp295ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp295Coverage(planPack);
  const slice = validateEmailDmsCoreCp295HermesBindingSliceDescriptor(createEmailDmsCoreCp295HermesBindingSliceDescriptor(), {});
  return freezeCp295Validation({
    review_packet: "C08.CP00-295.email_dms_core_hermes_binding_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP295_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP295_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    hermes_binding_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP295_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-295 cover exactly the 40 planned RP08 units from RP08.P08.M04.S13 through RP08.P08.M06.S08?",
      "Do the three descriptor sections (M04 secondary workflow tail from PASS_WITH_FINDINGS semantics through operator summary, M05 full permission/audit binding Hermes cycle, M06 synthetic fixture head through audit summary receipt) cover every planned row title as descriptor-only rows, with the shared Hermes guards applied across every section that carries them?",
      "Does CP00-295 avoid Hermes runtime receipts, command runtime execution, permission decision leaks, blocked-claim detail leaks, audit hint leaks, fixture payload leaks, state writes, object storage, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp295CloseoutHandoff() {
  return freezeCp295Validation({
    handoff_id: "CP00-295-to-CP00-296",
    from_pack_id: EMAIL_DMS_CORE_CP295_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP295_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP295_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP295_PACK_BINDING.range,
    open_scope: "Continue RP08.P08.M06.S09 onward with the remaining Hermes fixture rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP295_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp296CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp296Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp296Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-296 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-296");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP296_PACK_BINDING.risk_class) errors.push("CP00-296 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP296_PACK_BINDING.unit_count) errors.push("CP00-296 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP296_PACK_BINDING.first_unit_id) errors.push("CP00-296 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP296_PACK_BINDING.last_unit_id) errors.push("CP00-296 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-296 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-296 must only include RP08 units");
  const summary = createEmailDmsCoreCp296CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP296_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-296 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP296_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-296 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP296_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-296 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP296_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-296 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP296_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-296 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-296 ${microId} missing row ${title}`);
    }
  }
  return freezeCp296Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp296P08CloseoutP09ReviewFoundationDescriptor(
  descriptor = createEmailDmsCoreCp296P08CloseoutP09ReviewFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_closeout_p09_review_foundation_case_set ?? createEmailDmsCoreCp296P08CloseoutP09ReviewFoundationCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp296P08CloseoutP09ReviewFoundationDescriptor") errors.push("CP00-296 descriptor type drift");
  if (descriptor.source_hermes_binding_slice_descriptor !== "EmailDmsCoreCp295HermesBindingSliceDescriptor") {
    errors.push("CP00-296 source hermes binding slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP296_REQUIREMENTS.required_section_rows).length) errors.push("CP00-296 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP296_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-296 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-296 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-296 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-296 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-296 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-296 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-296 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP296_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.hermes_command_matrix && rows.hermes_command_matrix.executes_command_runtime !== false) {
      errors.push(`CP00-296 ${microId} Hermes command matrix must not execute runtime`);
    }
    if (rows.permission_summary_receipt && rows.permission_summary_receipt.permission_decision_detail_included !== false) {
      errors.push(`CP00-296 ${microId} permission summary receipt must not expose decisions`);
    }
    if (rows.no_real_data_receipt && rows.no_real_data_receipt.real_client_data_loaded !== false) {
      errors.push(`CP00-296 ${microId} no-real-data receipt drift`);
    }
    if (rows.claude_dependency_marker && rows.claude_dependency_marker.claude_final_approval_claimed !== false) {
      errors.push(`CP00-296 ${microId} Claude dependency marker must not claim final approval`);
    }
    if (rows.human_approval_marker && rows.human_approval_marker.human_approval_route_required_before_runtime !== true) {
      errors.push(`CP00-296 ${microId} human approval marker boundary missing`);
    }
    if (rows.permission_bypass_questions && rows.permission_bypass_questions.permission_bypass_detected !== false) {
      errors.push(`CP00-296 ${microId} permission bypass detected`);
    }
    if (rows.ui_leak_questions && rows.ui_leak_questions.leak_detected !== false) {
      errors.push(`CP00-296 ${microId} UI leak detected`);
    }
    if (rows.go_no_go_verdict_format && rows.go_no_go_verdict_format.claude_final_approval_claimed !== false) {
      errors.push(`CP00-296 ${microId} go/no-go must not claim Claude final approval`);
    }
    if (rows.human_approval_summary && rows.human_approval_summary.human_final_approval_required !== true) {
      errors.push(`CP00-296 ${microId} human approval summary drift`);
    }
    if (rows.claude_review_packet && rows.claude_review_packet.claude_final_approval_claimed !== false) {
      errors.push(`CP00-296 ${microId} review packet must not claim Claude final approval`);
    }
    if (rows.command_rerun && rows.command_rerun.executes_command_runtime !== false) {
      errors.push(`CP00-296 ${microId} command rerun must not execute runtime`);
    }
  }
  if (EMAIL_DMS_CORE_CP296_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-296 must not promote Claude");
  if (EMAIL_DMS_CORE_CP296_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-296 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-296 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-296 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id, EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id, EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id].includes(
      contractProjection.current_pack.pack_id,
    )
  ) {
    errors.push("CP00-296 contract current_pack drift");
  }
  if (
    contractProjection?.p08_closeout_p09_review_foundation_descriptor?.descriptor &&
    contractProjection.p08_closeout_p09_review_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-296 contract p08_closeout_p09_review_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP296_PACK_BINDING.next_pack_id) errors.push("CP00-296 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP296_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-296 next subphase drift");
  }
  return freezeCp296Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp296HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp296P08CloseoutP09ReviewFoundationDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp296Coverage(planPack);
  const foundation = validateEmailDmsCoreCp296P08CloseoutP09ReviewFoundationDescriptor(descriptor, contractProjection);
  return freezeCp296Validation({
    evidence_packet: "H08.CP00-296.email_dms_core_p08_closeout_p09_review_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP296_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_closeout_p09_review_foundation_valid: foundation.valid,
    no_real_data: true,
    source_hermes_binding_slice_pack_id: EMAIL_DMS_CORE_CP296_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP296_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP296_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP296_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createEmailDmsCoreCp296ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp296Coverage(planPack);
  const foundation = validateEmailDmsCoreCp296P08CloseoutP09ReviewFoundationDescriptor(createEmailDmsCoreCp296P08CloseoutP09ReviewFoundationDescriptor(), {});
  return freezeCp296Validation({
    review_packet: "C08.CP00-296.email_dms_core_p08_closeout_p09_review_foundation_descriptor",
    gate: EMAIL_DMS_CORE_CP296_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP296_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_closeout_p09_review_foundation_valid: foundation.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP296_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-296 cover exactly the 150 planned RP08 units from RP08.P08.M06.S09 through RP08.P09.M03.S22?",
      "Do the nine descriptor sections (M06 fixture tail, M07-M08 full Hermes cycles, M09 Claude review packet, M10 closeout head, and four P09 review micros with architecture/security/permission-bypass/audit-completeness/missing-test/UI-leak/downstream-readiness questions, risk registers, severity taxonomies, go/no-go verdict formats, finding routing maps, human approval summaries, Claude review packets, closeout criteria, PASS/PASS_WITH_FINDINGS/BLOCK closeout notes, next RP dependencies, review receipt placeholders, and future correction slots) cover every planned row title as descriptor-only rows?",
      "Does CP00-296 avoid Hermes runtime receipts, command runtime execution, permission bypass, UI leaks, permission decision leaks, blocked-claim detail leaks, audit hint leaks, fixture payload leaks, state writes, object storage, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp296CloseoutHandoff() {
  return freezeCp296Validation({
    handoff_id: "CP00-296-to-CP00-297",
    from_pack_id: EMAIL_DMS_CORE_CP296_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP296_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP296_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP296_PACK_BINDING.range,
    open_scope: "RP08.P08 descriptor scope is closed; continue RP08.P09.M04.S01 onward with the remaining review rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP296_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp297CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp297Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp297Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-297 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-297");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP297_PACK_BINDING.risk_class) errors.push("CP00-297 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP297_PACK_BINDING.unit_count) errors.push("CP00-297 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP297_PACK_BINDING.first_unit_id) errors.push("CP00-297 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP297_PACK_BINDING.last_unit_id) errors.push("CP00-297 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-297 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-297 must only include RP08 units");
  const summary = createEmailDmsCoreCp297CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP297_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-297 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP297_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-297 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP297_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-297 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP297_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-297 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP297_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-297 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-297 ${microId} missing row ${title}`);
    }
  }
  return freezeCp297Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp297ReviewSliceDescriptor(
  descriptor = createEmailDmsCoreCp297ReviewSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.review_slice_case_set ?? createEmailDmsCoreCp297ReviewSliceCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp297ReviewSliceDescriptor") errors.push("CP00-297 descriptor type drift");
  if (descriptor.source_p08_closeout_p09_review_foundation_descriptor !== "EmailDmsCoreCp296P08CloseoutP09ReviewFoundationDescriptor") {
    errors.push("CP00-297 source p08 closeout p09 review foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP297_REQUIREMENTS.required_section_rows).length) errors.push("CP00-297 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP297_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-297 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-297 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-297 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-297 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-297 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-297 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-297 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP297_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.permission_bypass_questions && rows.permission_bypass_questions.permission_bypass_detected !== false) {
      errors.push(`CP00-297 ${microId} permission bypass detected`);
    }
    if (rows.ui_leak_questions && rows.ui_leak_questions.leak_detected !== false) {
      errors.push(`CP00-297 ${microId} UI leak detected`);
    }
    if (rows.go_no_go_verdict_format && rows.go_no_go_verdict_format.claude_final_approval_claimed !== false) {
      errors.push(`CP00-297 ${microId} go/no-go must not claim Claude final approval`);
    }
    if (rows.human_approval_summary && rows.human_approval_summary.human_final_approval_required !== true) {
      errors.push(`CP00-297 ${microId} human approval summary drift`);
    }
    if (rows.claude_review_packet && rows.claude_review_packet.claude_final_approval_claimed !== false) {
      errors.push(`CP00-297 ${microId} review packet must not claim Claude final approval`);
    }
    if (rows.command_rerun && rows.command_rerun.executes_command_runtime !== false) {
      errors.push(`CP00-297 ${microId} command rerun must not execute runtime`);
    }
  }
  if (EMAIL_DMS_CORE_CP297_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-297 must not promote Claude");
  if (EMAIL_DMS_CORE_CP297_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-297 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-297 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-297 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id, EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-297 contract current_pack drift");
  }
  if (
    contractProjection?.review_slice_descriptor?.descriptor &&
    contractProjection.review_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-297 contract review_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP297_PACK_BINDING.next_pack_id) errors.push("CP00-297 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP297_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-297 next subphase drift");
  }
  return freezeCp297Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp297HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp297ReviewSliceDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp297Coverage(planPack);
  const slice = validateEmailDmsCoreCp297ReviewSliceDescriptor(descriptor, contractProjection);
  return freezeCp297Validation({
    evidence_packet: "H08.CP00-297.email_dms_core_review_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP297_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    review_slice_valid: slice.valid,
    no_real_data: true,
    source_p08_closeout_p09_review_foundation_pack_id: EMAIL_DMS_CORE_CP297_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP297_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP297_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP297_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createEmailDmsCoreCp297ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp297Coverage(planPack);
  const slice = validateEmailDmsCoreCp297ReviewSliceDescriptor(createEmailDmsCoreCp297ReviewSliceDescriptor(), {});
  return freezeCp297Validation({
    review_packet: "C08.CP00-297.email_dms_core_review_slice_descriptor",
    gate: EMAIL_DMS_CORE_CP297_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP297_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    review_slice_valid: slice.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP297_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-297 cover exactly the 40 planned RP08 units from RP08.P09.M04.S01 through RP08.P09.M05.S20?",
      "Do the two descriptor sections (M04 secondary workflow review cycle, M05 permission/audit binding review cycle) cover every planned row title as descriptor-only rows, with the shared review guards applied across every section that carries them?",
      "Does CP00-297 avoid permission bypass, UI leaks, command runtime execution, Hermes runtime receipts, permission decision leaks, blocked-claim detail leaks, audit hint leaks, state writes, object storage, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp297CloseoutHandoff() {
  return freezeCp297Validation({
    handoff_id: "CP00-297-to-CP00-298",
    from_pack_id: EMAIL_DMS_CORE_CP297_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP297_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP297_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP297_PACK_BINDING.range,
    open_scope: "Continue RP08.P09.M06.S01 onward with the remaining review fixture rows and downstream micros while preserving descriptor-only email/DMS boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP297_PACK_BINDING.production_ready_flag,
  });
}

export function createEmailDmsCoreCp298CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp298Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validateEmailDmsCoreCp298Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-298 plan pack is required");
  if (planPack?.pack_id !== EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-298");
  if (planPack?.risk_class !== EMAIL_DMS_CORE_CP298_PACK_BINDING.risk_class) errors.push("CP00-298 risk class drift");
  if (planPack?.unit_count !== EMAIL_DMS_CORE_CP298_PACK_BINDING.unit_count) errors.push("CP00-298 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== EMAIL_DMS_CORE_CP298_PACK_BINDING.first_unit_id) errors.push("CP00-298 first unit drift");
  if (unitIds.at(-1) !== EMAIL_DMS_CORE_CP298_PACK_BINDING.last_unit_id) errors.push("CP00-298 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-298 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP08")) errors.push("CP00-298 must only include RP08 units");
  const summary = createEmailDmsCoreCp298CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(EMAIL_DMS_CORE_CP298_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-298 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(EMAIL_DMS_CORE_CP298_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-298 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(EMAIL_DMS_CORE_CP298_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-298 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(EMAIL_DMS_CORE_CP298_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-298 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP298_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-298 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-298 ${microId} missing row ${title}`);
    }
  }
  return freezeCp298Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validateEmailDmsCoreCp298P09CloseoutDescriptor(
  descriptor = createEmailDmsCoreCp298P09CloseoutDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_closeout_case_set ?? createEmailDmsCoreCp298P09CloseoutCaseSet();
  if (descriptor.descriptor !== "EmailDmsCoreCp298P09CloseoutDescriptor") errors.push("CP00-298 descriptor type drift");
  if (descriptor.source_review_slice_descriptor !== "EmailDmsCoreCp297ReviewSliceDescriptor") {
    errors.push("CP00-298 source review slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(EMAIL_DMS_CORE_CP298_REQUIREMENTS.required_section_rows).length) errors.push("CP00-298 section count drift");
  for (const [microId, titles] of Object.entries(EMAIL_DMS_CORE_CP298_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-298 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-298 ${microId} row count drift`);
    for (const title of titles) {
      const key = emailDmsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-298 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-298 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-298 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-298 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-298 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(EMAIL_DMS_CORE_CP298_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.permission_bypass_questions && rows.permission_bypass_questions.permission_bypass_detected !== false) {
      errors.push(`CP00-298 ${microId} permission bypass detected`);
    }
    if (rows.ui_leak_questions && rows.ui_leak_questions.leak_detected !== false) {
      errors.push(`CP00-298 ${microId} UI leak detected`);
    }
    if (rows.go_no_go_verdict_format && rows.go_no_go_verdict_format.claude_final_approval_claimed !== false) {
      errors.push(`CP00-298 ${microId} go/no-go must not claim Claude final approval`);
    }
    if (rows.human_approval_summary && rows.human_approval_summary.human_final_approval_required !== true) {
      errors.push(`CP00-298 ${microId} human approval summary drift`);
    }
    if (rows.claude_review_packet && rows.claude_review_packet.claude_final_approval_claimed !== false) {
      errors.push(`CP00-298 ${microId} review packet must not claim Claude final approval`);
    }
    if (rows.command_rerun && rows.command_rerun.executes_command_runtime !== false) {
      errors.push(`CP00-298 ${microId} command rerun must not execute runtime`);
    }
  }
  if (EMAIL_DMS_CORE_CP298_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-298 must not promote Claude");
  if (EMAIL_DMS_CORE_CP298_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-298 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-298 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-298 local validation trust boundary drift");
  }
  if (contractProjection?.current_pack?.pack_id && contractProjection.current_pack.pack_id !== EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id) {
    errors.push("CP00-298 contract current_pack drift");
  }
  if (
    contractProjection?.p09_closeout_descriptor?.descriptor &&
    contractProjection.p09_closeout_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-298 contract p09_closeout_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== EMAIL_DMS_CORE_CP298_PACK_BINDING.next_pack_id) errors.push("CP00-298 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== EMAIL_DMS_CORE_CP298_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-298 next subphase drift");
  }
  return freezeCp298Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createEmailDmsCoreCp298HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createEmailDmsCoreCp298P09CloseoutDescriptor(),
) {
  const coverage = validateEmailDmsCoreCp298Coverage(planPack);
  const closeout = validateEmailDmsCoreCp298P09CloseoutDescriptor(descriptor, contractProjection);
  return freezeCp298Validation({
    evidence_packet: "H08.CP00-298.email_dms_core_p09_closeout_descriptor",
    gate: EMAIL_DMS_CORE_CP298_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_closeout_valid: closeout.valid,
    no_real_data: true,
    source_review_slice_pack_id: EMAIL_DMS_CORE_CP298_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: EMAIL_DMS_CORE_CP298_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: EMAIL_DMS_CORE_CP298_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP298_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && closeout.valid,
  });
}

export function createEmailDmsCoreCp298ClaudeReviewPacket(planPack) {
  const coverage = validateEmailDmsCoreCp298Coverage(planPack);
  const closeout = validateEmailDmsCoreCp298P09CloseoutDescriptor(createEmailDmsCoreCp298P09CloseoutDescriptor(), {});
  return freezeCp298Validation({
    review_packet: "C08.CP00-298.email_dms_core_p09_closeout_descriptor",
    gate: EMAIL_DMS_CORE_CP298_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: EMAIL_DMS_CORE_CP298_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_closeout_valid: closeout.valid,
    invalid_review_blockers: EMAIL_DMS_CORE_CP298_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-298 cover exactly the 92 planned RP08 units from RP08.P09.M06.S01 through RP08.P09.M10.S10?",
      "Do the five descriptor sections (M06 fixture review cycle, M07 test/golden cycle with review receipt placeholder and future correction slot, M08 Hermes packet cycle, M09 Claude review packet cycle, M10 closeout head) cover every planned row title as descriptor-only rows, closing the RP08.P09 review phase and the RP08 program descriptor scope?",
      "Does CP00-298 avoid permission bypass, UI leaks, command runtime execution, Hermes runtime receipts, permission decision leaks, blocked-claim detail leaks, audit hint leaks, state writes, object storage, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createEmailDmsCoreCp298CloseoutHandoff() {
  return freezeCp298Validation({
    handoff_id: "CP00-298-to-CP00-299",
    from_pack_id: EMAIL_DMS_CORE_CP298_PACK_BINDING.pack_id,
    to_pack_id: EMAIL_DMS_CORE_CP298_PACK_BINDING.next_pack_id,
    next_subphase_id: EMAIL_DMS_CORE_CP298_PACK_BINDING.next_subphase_id,
    closed_scope: EMAIL_DMS_CORE_CP298_PACK_BINDING.range,
    open_scope: "RP08 descriptor scope is fully closed; CP00-299 opens the next program (RP09) from RP09.P00.M00.S01 with a fresh program bootstrap while preserving descriptor-only boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: EMAIL_DMS_CORE_CP298_PACK_BINDING.production_ready_flag,
  });
}
