import {
  PAYMENTS_CORE_CP392_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP392_PACK_BINDING,
  PAYMENTS_CORE_CP392_REQUIREMENTS,
  PAYMENTS_CORE_CP393_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP393_PACK_BINDING,
  PAYMENTS_CORE_CP393_REQUIREMENTS,
  PAYMENTS_CORE_CP394_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP394_PACK_BINDING,
  PAYMENTS_CORE_CP394_REQUIREMENTS,
  PAYMENTS_CORE_CP395_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP395_PACK_BINDING,
  PAYMENTS_CORE_CP395_REQUIREMENTS,
  PAYMENTS_CORE_CP396_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP396_PACK_BINDING,
  PAYMENTS_CORE_CP396_REQUIREMENTS,
  PAYMENTS_CORE_CP397_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP397_PACK_BINDING,
  PAYMENTS_CORE_CP397_REQUIREMENTS,
  PAYMENTS_CORE_CP398_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP398_PACK_BINDING,
  PAYMENTS_CORE_CP398_REQUIREMENTS,
  PAYMENTS_CORE_CP399_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP399_PACK_BINDING,
  PAYMENTS_CORE_CP399_REQUIREMENTS,
  PAYMENTS_CORE_CP400_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP400_PACK_BINDING,
  PAYMENTS_CORE_CP400_REQUIREMENTS,
  PAYMENTS_CORE_CP401_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP401_PACK_BINDING,
  PAYMENTS_CORE_CP401_REQUIREMENTS,
  PAYMENTS_CORE_CP402_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP402_PACK_BINDING,
  PAYMENTS_CORE_CP402_REQUIREMENTS,
  PAYMENTS_CORE_CP403_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP403_PACK_BINDING,
  PAYMENTS_CORE_CP403_REQUIREMENTS,
  PAYMENTS_CORE_CP404_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP404_PACK_BINDING,
  PAYMENTS_CORE_CP404_REQUIREMENTS,
  PAYMENTS_CORE_CP405_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP405_PACK_BINDING,
  PAYMENTS_CORE_CP405_REQUIREMENTS,
  PAYMENTS_CORE_CP406_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP406_PACK_BINDING,
  PAYMENTS_CORE_CP406_REQUIREMENTS,
  PAYMENTS_CORE_CP407_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP407_PACK_BINDING,
  PAYMENTS_CORE_CP407_REQUIREMENTS,
  PAYMENTS_CORE_CP408_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP408_PACK_BINDING,
  PAYMENTS_CORE_CP408_REQUIREMENTS,
  PAYMENTS_CORE_CP409_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP409_PACK_BINDING,
  PAYMENTS_CORE_CP409_REQUIREMENTS,
  PAYMENTS_CORE_CP410_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP410_PACK_BINDING,
  PAYMENTS_CORE_CP410_REQUIREMENTS,
  PAYMENTS_CORE_CP411_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP411_PACK_BINDING,
  PAYMENTS_CORE_CP411_REQUIREMENTS,
  PAYMENTS_CORE_CP412_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP412_PACK_BINDING,
  PAYMENTS_CORE_CP412_REQUIREMENTS,
  PAYMENTS_CORE_CP413_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP413_PACK_BINDING,
  PAYMENTS_CORE_CP413_REQUIREMENTS,
  PAYMENTS_CORE_CP414_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP414_PACK_BINDING,
  PAYMENTS_CORE_CP414_REQUIREMENTS,
  PAYMENTS_CORE_CP415_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP415_PACK_BINDING,
  PAYMENTS_CORE_CP415_REQUIREMENTS,
  PAYMENTS_CORE_CP416_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP416_PACK_BINDING,
  PAYMENTS_CORE_CP416_REQUIREMENTS,
  PAYMENTS_CORE_CP417_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP417_PACK_BINDING,
  PAYMENTS_CORE_CP417_REQUIREMENTS,
  PAYMENTS_CORE_CP418_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP418_PACK_BINDING,
  PAYMENTS_CORE_CP418_REQUIREMENTS,
  PAYMENTS_CORE_CP419_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP419_PACK_BINDING,
  PAYMENTS_CORE_CP419_REQUIREMENTS,
  PAYMENTS_CORE_CP420_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP420_PACK_BINDING,
  PAYMENTS_CORE_CP420_REQUIREMENTS,
  PAYMENTS_CORE_CP421_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP421_PACK_BINDING,
  PAYMENTS_CORE_CP421_REQUIREMENTS,
  PAYMENTS_CORE_CP422_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP422_PACK_BINDING,
  PAYMENTS_CORE_CP422_REQUIREMENTS,
  PAYMENTS_CORE_CP423_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP423_PACK_BINDING,
  PAYMENTS_CORE_CP423_REQUIREMENTS,
  PAYMENTS_CORE_CP424_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP424_PACK_BINDING,
  PAYMENTS_CORE_CP424_REQUIREMENTS,
  PAYMENTS_CORE_CP425_NO_WRITE_ATTESTATION,
  PAYMENTS_CORE_CP425_PACK_BINDING,
  PAYMENTS_CORE_CP425_REQUIREMENTS,
} from "./registry.js";
import {
  createPaymentsCoreCp392ScopeContractFoundationCaseSet,
  createPaymentsCoreCp392ScopeContractFoundationDescriptor,
  createPaymentsCoreCp393P01CloseoutP02FoundationCaseSet,
  createPaymentsCoreCp393P01CloseoutP02FoundationDescriptor,
  createPaymentsCoreCp394P02WorkflowPermissionSliceCaseSet,
  createPaymentsCoreCp394P02WorkflowPermissionSliceDescriptor,
  createPaymentsCoreCp395P02FixtureSliceCaseSet,
  createPaymentsCoreCp395P02FixtureSliceDescriptor,
  createPaymentsCoreCp396P02CloseoutP03FoundationCaseSet,
  createPaymentsCoreCp396P02CloseoutP03FoundationDescriptor,
  createPaymentsCoreCp397P03PermissionSliceCaseSet,
  createPaymentsCoreCp397P03PermissionSliceDescriptor,
  createPaymentsCoreCp398P03PermissionFixtureSliceCaseSet,
  createPaymentsCoreCp398P03PermissionFixtureSliceDescriptor,
  createPaymentsCoreCp399P03FixtureSliceCaseSet,
  createPaymentsCoreCp399P03FixtureSliceDescriptor,
  createPaymentsCoreCp400P03CloseoutP04FoundationCaseSet,
  createPaymentsCoreCp400P03CloseoutP04FoundationDescriptor,
  createPaymentsCoreCp401P04PermissionFixtureSliceCaseSet,
  createPaymentsCoreCp401P04PermissionFixtureSliceDescriptor,
  createPaymentsCoreCp402P04CloseoutP05FoundationCaseSet,
  createPaymentsCoreCp402P04CloseoutP05FoundationDescriptor,
  createPaymentsCoreCp403P05CloseoutP06FoundationCaseSet,
  createPaymentsCoreCp403P05CloseoutP06FoundationDescriptor,
  createPaymentsCoreCp404P06ContractImplementationSliceCaseSet,
  createPaymentsCoreCp404P06ContractImplementationSliceDescriptor,
  createPaymentsCoreCp405P06ImplementationWorkflowSliceCaseSet,
  createPaymentsCoreCp405P06ImplementationWorkflowSliceDescriptor,
  createPaymentsCoreCp406P06PermissionSliceCaseSet,
  createPaymentsCoreCp406P06PermissionSliceDescriptor,
  createPaymentsCoreCp407P06PermissionFixtureSliceCaseSet,
  createPaymentsCoreCp407P06PermissionFixtureSliceDescriptor,
  createPaymentsCoreCp408P06CloseoutP07FoundationCaseSet,
  createPaymentsCoreCp408P06CloseoutP07FoundationDescriptor,
  createPaymentsCoreCp409P07ImplementationSliceCaseSet,
  createPaymentsCoreCp409P07ImplementationSliceDescriptor,
  createPaymentsCoreCp410P07WorkflowPermissionSliceCaseSet,
  createPaymentsCoreCp410P07WorkflowPermissionSliceDescriptor,
  createPaymentsCoreCp411P07PermissionSliceCaseSet,
  createPaymentsCoreCp411P07PermissionSliceDescriptor,
  createPaymentsCoreCp412P07CloseoutP08FoundationCaseSet,
  createPaymentsCoreCp412P07CloseoutP08FoundationDescriptor,
  createPaymentsCoreCp413P08ImplementationWorkflowSliceCaseSet,
  createPaymentsCoreCp413P08ImplementationWorkflowSliceDescriptor,
  createPaymentsCoreCp414P08PermissionFixtureSliceCaseSet,
  createPaymentsCoreCp414P08PermissionFixtureSliceDescriptor,
  createPaymentsCoreCp415P08CloseoutP09FoundationCaseSet,
  createPaymentsCoreCp415P08CloseoutP09FoundationDescriptor,
  createPaymentsCoreCp416P09PermissionSliceCaseSet,
  createPaymentsCoreCp416P09PermissionSliceDescriptor,
  createPaymentsCoreCp417P09PermissionFixtureSliceCaseSet,
  createPaymentsCoreCp417P09PermissionFixtureSliceDescriptor,
  createPaymentsCoreCp418P09FixtureSliceCaseSet,
  createPaymentsCoreCp418P09FixtureSliceDescriptor,
  createPaymentsCoreCp419P09FixtureTestSliceCaseSet,
  createPaymentsCoreCp419P09FixtureTestSliceDescriptor,
  createPaymentsCoreCp420P09TestSliceCaseSet,
  createPaymentsCoreCp420P09TestSliceDescriptor,
  createPaymentsCoreCp421P09TestHermesSliceCaseSet,
  createPaymentsCoreCp421P09TestHermesSliceDescriptor,
  createPaymentsCoreCp422P09HermesSliceCaseSet,
  createPaymentsCoreCp422P09HermesSliceDescriptor,
  createPaymentsCoreCp423P09HermesReviewSliceCaseSet,
  createPaymentsCoreCp423P09HermesReviewSliceDescriptor,
  createPaymentsCoreCp424P09ReviewCloseoutSliceCaseSet,
  createPaymentsCoreCp424P09ReviewCloseoutSliceDescriptor,
  createPaymentsCoreCp425P09CloseoutHandoffSliceCaseSet,
  createPaymentsCoreCp425P09CloseoutHandoffSliceDescriptor,
  paymentsCoreRowKey,
} from "./service.js";

function countBy(units, field) {
  const counts = {};
  for (const unit of units) {
    const key = unit?.[field];
    if (key === undefined) continue;
    counts[key] = (counts[key] ?? 0) + 1;
  }
  return Object.freeze(counts);
}

function freezeCp392Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP392_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP392_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp392CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp392Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp392Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-392 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP392_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-392");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP392_PACK_BINDING.risk_class) errors.push("CP00-392 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP392_PACK_BINDING.unit_count) errors.push("CP00-392 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP392_PACK_BINDING.first_unit_id) errors.push("CP00-392 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP392_PACK_BINDING.last_unit_id) errors.push("CP00-392 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-392 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-392 must only include RP13 units");
  const summary = createPaymentsCoreCp392CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP392_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-392 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP392_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-392 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP392_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-392 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP392_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-392 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP392_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-392 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-392 ${microId} missing row ${title}`);
    }
  }
  return freezeCp392Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp392ScopeContractFoundationDescriptor(
  descriptor = createPaymentsCoreCp392ScopeContractFoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.scope_contract_foundation_case_set ?? createPaymentsCoreCp392ScopeContractFoundationCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp392ScopeContractFoundationDescriptor") errors.push("CP00-392 descriptor type drift");
  if (descriptor.program_contract?.program_id !== "RP13") errors.push("CP00-392 program id drift");
  if (descriptor.program_contract?.upstream_program_id !== "RP12") errors.push("CP00-392 upstream program drift");
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP392_REQUIREMENTS.required_section_rows).length) errors.push("CP00-392 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP392_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-392 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-392 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-392 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-392 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-392 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-392 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-392 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(PAYMENTS_CORE_CP392_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.non_goal_boundary && rows.non_goal_boundary.payments_runtime_opened !== false) {
      errors.push(`CP00-392 ${microId} must not open payments runtime`);
    }
    if (rows.permission_baseline_note && rows.permission_baseline_note.permission_decision_detail_included !== false) {
      errors.push(`CP00-392 ${microId} permission baseline must not expose decisions`);
    }
    if (rows.permission_baseline_note && rows.permission_baseline_note.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-392 ${microId} permission baseline must deny cross-tenant access`);
    }
    if (rows.audit_baseline_note && rows.audit_baseline_note.audit_event_body_included !== false) {
      errors.push(`CP00-392 ${microId} audit baseline must not expose event bodies`);
    }
    if (rows.synthetic_data_policy && rows.synthetic_data_policy.real_client_data_loaded !== false) {
      errors.push(`CP00-392 ${microId} synthetic data policy drift`);
    }
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-392 ${microId} tenant scope must deny cross-tenant access`);
    }
    if (rows.matter_trace_reference && rows.matter_trace_reference.matter_trace_required !== true) {
      errors.push(`CP00-392 ${microId} matter trace must be required`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-392 ${microId} state transition map must not write state`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-392 ${microId} fixture model must not load real data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-392 ${microId} must not execute test runtime`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-392 ${microId} must not emit Hermes receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-392 ${microId} must not claim Claude final approval`);
    }
  }
  if (PAYMENTS_CORE_CP392_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-392 must not promote Claude");
  if (PAYMENTS_CORE_CP392_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-392 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-392 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-392 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP392_PACK_BINDING.pack_id, PAYMENTS_CORE_CP393_PACK_BINDING.pack_id, PAYMENTS_CORE_CP394_PACK_BINDING.pack_id, PAYMENTS_CORE_CP395_PACK_BINDING.pack_id, PAYMENTS_CORE_CP396_PACK_BINDING.pack_id, PAYMENTS_CORE_CP397_PACK_BINDING.pack_id, PAYMENTS_CORE_CP398_PACK_BINDING.pack_id, PAYMENTS_CORE_CP399_PACK_BINDING.pack_id, PAYMENTS_CORE_CP400_PACK_BINDING.pack_id, PAYMENTS_CORE_CP401_PACK_BINDING.pack_id, PAYMENTS_CORE_CP402_PACK_BINDING.pack_id, PAYMENTS_CORE_CP403_PACK_BINDING.pack_id, PAYMENTS_CORE_CP404_PACK_BINDING.pack_id, PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-392 contract current_pack drift");
  }
  if (
    contractProjection?.scope_contract_foundation_descriptor?.descriptor &&
    contractProjection.scope_contract_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-392 contract scope_contract_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP392_PACK_BINDING.next_pack_id) errors.push("CP00-392 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP392_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-392 next subphase drift");
  }
  return freezeCp392Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp392HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp392ScopeContractFoundationDescriptor(),
) {
  const coverage = validatePaymentsCoreCp392Coverage(planPack);
  const foundation = validatePaymentsCoreCp392ScopeContractFoundationDescriptor(descriptor, contractProjection);
  return freezeCp392Validation({
    evidence_packet: "H13.CP00-392.payments_core_scope_contract_foundation_descriptor",
    gate: PAYMENTS_CORE_CP392_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    scope_contract_foundation_valid: foundation.valid,
    no_real_data: true,
    source_billing_core_pack_id: PAYMENTS_CORE_CP392_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP392_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP392_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP392_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && foundation.valid,
  });
}

export function createPaymentsCoreCp392ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp392Coverage(planPack);
  const foundation = validatePaymentsCoreCp392ScopeContractFoundationDescriptor(createPaymentsCoreCp392ScopeContractFoundationDescriptor(), {});
  return freezeCp392Validation({
    review_packet: "C13.CP00-392.payments_core_scope_contract_foundation_descriptor",
    gate: PAYMENTS_CORE_CP392_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP392_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    scope_contract_foundation_valid: foundation.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP392_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-392 cover exactly the 150 planned RP13 units from RP13.P00.M00.S01 through RP13.P01.M05.S20?",
      "Do the twenty-one descriptor sections (P00 M00-M10 scope foundation cycle and P01 M00-M08 model foundation cycle with package directory layout, primary entity identifier, tenant scope field, matter trace reference, lifecycle status enum, ownership metadata, reference relationship map, field registries, state transition map, validation helper, fixture model, serialization shape, public export, model tests, Hermes model summary, Claude model review prompt, and closeout handoff) cover every planned row title as descriptor-only rows?",
      "Does CP00-392 avoid payments/payment-matching/ar-aging/journal-entry runtime, runtime permission evaluation, audit event writes, state writes, object storage, command runtime execution, Hermes runtime receipts, real data, permission decision leaks, audit event body leaks, fixture payload leaks, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp392CloseoutHandoff() {
  return freezeCp392Validation({
    handoff_id: "CP00-392-to-CP00-393",
    from_pack_id: PAYMENTS_CORE_CP392_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP392_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP392_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP392_PACK_BINDING.range,
    open_scope: "Continue RP13.P01.M06.S01 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP392_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp393Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP393_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP393_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp393CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp393Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp393Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-393 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP393_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-393");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP393_PACK_BINDING.risk_class) errors.push("CP00-393 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP393_PACK_BINDING.unit_count) errors.push("CP00-393 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP393_PACK_BINDING.first_unit_id) errors.push("CP00-393 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP393_PACK_BINDING.last_unit_id) errors.push("CP00-393 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-393 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-393 must only include RP13 units");
  const summary = createPaymentsCoreCp393CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP393_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-393 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP393_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-393 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP393_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-393 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP393_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-393 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP393_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-393 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-393 ${microId} missing row ${title}`);
    }
  }
  return freezeCp393Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp393P01CloseoutP02FoundationDescriptor(
  descriptor = createPaymentsCoreCp393P01CloseoutP02FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p01_closeout_p02_foundation_case_set ?? createPaymentsCoreCp393P01CloseoutP02FoundationCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp393P01CloseoutP02FoundationDescriptor") errors.push("CP00-393 descriptor type drift");
  if (descriptor.source_scope_contract_foundation_descriptor !== "PaymentsCoreCp392ScopeContractFoundationDescriptor") {
    errors.push("CP00-393 source scope contract foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP393_REQUIREMENTS.required_section_rows).length) errors.push("CP00-393 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP393_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-393 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-393 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-393 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-393 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-393 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-393 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-393 ${microId} ${key} must not include raw payload`);
    }
  }
  for (const [microId] of Object.entries(PAYMENTS_CORE_CP393_REQUIREMENTS.required_section_rows)) {
    const rows = sections[microId]?.rows ?? {};
    if (rows.tenant_scope_field && rows.tenant_scope_field.cross_tenant_access_allowed !== false) {
      errors.push(`CP00-393 ${microId} must not allow cross-tenant access`);
    }
    if (rows.state_transition_map && rows.state_transition_map.writes_state_transition !== false) {
      errors.push(`CP00-393 ${microId} must not write state transitions`);
    }
    if (rows.validation_helper && rows.validation_helper.validation_error_detail_included !== false) {
      errors.push(`CP00-393 ${microId} must not expose validation error details`);
    }
    if (rows.fixture_model && rows.fixture_model.real_client_data_loaded !== false) {
      errors.push(`CP00-393 ${microId} must not load real fixture data`);
    }
    if (rows.model_unit_test && rows.model_unit_test.executes_unit_test_runtime_paths !== false) {
      errors.push(`CP00-393 ${microId} must not execute test runtime`);
    }
    if (rows.invalid_reference_test && rows.invalid_reference_test.expected_outcome !== "rejected_customer_safe") {
      errors.push(`CP00-393 ${microId} invalid reference outcome drift`);
    }
    if (rows.hermes_model_summary && rows.hermes_model_summary.emits_hermes_runtime_receipt !== false) {
      errors.push(`CP00-393 ${microId} must not emit Hermes runtime receipts`);
    }
    if (rows.claude_model_review_prompt && rows.claude_model_review_prompt.claude_final_approval_claimed !== false) {
      errors.push(`CP00-393 ${microId} must not claim Claude final approval`);
    }
  }
  if (PAYMENTS_CORE_CP393_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-393 must not promote Claude");
  if (PAYMENTS_CORE_CP393_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-393 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-393 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-393 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP393_PACK_BINDING.pack_id, PAYMENTS_CORE_CP394_PACK_BINDING.pack_id, PAYMENTS_CORE_CP395_PACK_BINDING.pack_id, PAYMENTS_CORE_CP396_PACK_BINDING.pack_id, PAYMENTS_CORE_CP397_PACK_BINDING.pack_id, PAYMENTS_CORE_CP398_PACK_BINDING.pack_id, PAYMENTS_CORE_CP399_PACK_BINDING.pack_id, PAYMENTS_CORE_CP400_PACK_BINDING.pack_id, PAYMENTS_CORE_CP401_PACK_BINDING.pack_id, PAYMENTS_CORE_CP402_PACK_BINDING.pack_id, PAYMENTS_CORE_CP403_PACK_BINDING.pack_id, PAYMENTS_CORE_CP404_PACK_BINDING.pack_id, PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-393 contract current_pack drift");
  }
  if (
    contractProjection?.p01_closeout_p02_foundation_descriptor?.descriptor &&
    contractProjection.p01_closeout_p02_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-393 contract p01_closeout_p02_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP393_PACK_BINDING.next_pack_id) errors.push("CP00-393 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP393_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-393 next subphase drift");
  }
  return freezeCp393Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp393HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp393P01CloseoutP02FoundationDescriptor(),
) {
  const coverage = validatePaymentsCoreCp393Coverage(planPack);
  const slice = validatePaymentsCoreCp393P01CloseoutP02FoundationDescriptor(descriptor, contractProjection);
  return freezeCp393Validation({
    evidence_packet: "H13.CP00-393.payments_core_p01_closeout_p02_foundation_descriptor",
    gate: PAYMENTS_CORE_CP393_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p01_closeout_p02_foundation_valid: slice.valid,
    no_real_data: true,
    source_scope_contract_foundation_pack_id: PAYMENTS_CORE_CP393_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP393_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP393_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP393_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp393ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp393Coverage(planPack);
  const slice = validatePaymentsCoreCp393P01CloseoutP02FoundationDescriptor(createPaymentsCoreCp393P01CloseoutP02FoundationDescriptor(), {});
  return freezeCp393Validation({
    review_packet: "C13.CP00-393.payments_core_p01_closeout_p02_foundation_descriptor",
    gate: PAYMENTS_CORE_CP393_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP393_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p01_closeout_p02_foundation_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP393_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-393 cover exactly the 150 planned RP13 units from RP13.P01.M06.S01 through RP13.P02.M04.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-393 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp393CloseoutHandoff() {
  return freezeCp393Validation({
    handoff_id: "CP00-393-to-CP00-394",
    from_pack_id: PAYMENTS_CORE_CP393_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP393_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP393_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP393_PACK_BINDING.range,
    open_scope: "Continue RP13.P02.M04.S07 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP393_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp394Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP394_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP394_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp394CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp394Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp394Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-394 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP394_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-394");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP394_PACK_BINDING.risk_class) errors.push("CP00-394 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP394_PACK_BINDING.unit_count) errors.push("CP00-394 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP394_PACK_BINDING.first_unit_id) errors.push("CP00-394 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP394_PACK_BINDING.last_unit_id) errors.push("CP00-394 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-394 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-394 must only include RP13 units");
  const summary = createPaymentsCoreCp394CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP394_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-394 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP394_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-394 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP394_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-394 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP394_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-394 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP394_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-394 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-394 ${microId} missing row ${title}`);
    }
  }
  return freezeCp394Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp394P02WorkflowPermissionSliceDescriptor(
  descriptor = createPaymentsCoreCp394P02WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_workflow_permission_slice_case_set ?? createPaymentsCoreCp394P02WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp394P02WorkflowPermissionSliceDescriptor") errors.push("CP00-394 descriptor type drift");
  if (descriptor.source_p01_closeout_p02_foundation_descriptor !== "PaymentsCoreCp393P01CloseoutP02FoundationDescriptor") {
    errors.push("CP00-394 source p01 closeout p02 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP394_REQUIREMENTS.required_section_rows).length) errors.push("CP00-394 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP394_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-394 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-394 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-394 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-394 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-394 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-394 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-394 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP394_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-394 must not promote Claude");
  if (PAYMENTS_CORE_CP394_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-394 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-394 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-394 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP394_PACK_BINDING.pack_id, PAYMENTS_CORE_CP395_PACK_BINDING.pack_id, PAYMENTS_CORE_CP396_PACK_BINDING.pack_id, PAYMENTS_CORE_CP397_PACK_BINDING.pack_id, PAYMENTS_CORE_CP398_PACK_BINDING.pack_id, PAYMENTS_CORE_CP399_PACK_BINDING.pack_id, PAYMENTS_CORE_CP400_PACK_BINDING.pack_id, PAYMENTS_CORE_CP401_PACK_BINDING.pack_id, PAYMENTS_CORE_CP402_PACK_BINDING.pack_id, PAYMENTS_CORE_CP403_PACK_BINDING.pack_id, PAYMENTS_CORE_CP404_PACK_BINDING.pack_id, PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-394 contract current_pack drift");
  }
  if (
    contractProjection?.p02_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p02_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-394 contract p02_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP394_PACK_BINDING.next_pack_id) errors.push("CP00-394 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP394_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-394 next subphase drift");
  }
  return freezeCp394Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp394HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp394P02WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp394Coverage(planPack);
  const slice = validatePaymentsCoreCp394P02WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp394Validation({
    evidence_packet: "H13.CP00-394.payments_core_p02_workflow_permission_slice_descriptor",
    gate: PAYMENTS_CORE_CP394_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p01_closeout_p02_foundation_pack_id: PAYMENTS_CORE_CP394_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP394_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP394_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP394_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp394ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp394Coverage(planPack);
  const slice = validatePaymentsCoreCp394P02WorkflowPermissionSliceDescriptor(createPaymentsCoreCp394P02WorkflowPermissionSliceDescriptor(), {});
  return freezeCp394Validation({
    review_packet: "C13.CP00-394.payments_core_p02_workflow_permission_slice_descriptor",
    gate: PAYMENTS_CORE_CP394_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP394_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP394_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-394 cover exactly the 40 planned RP13 units from RP13.P02.M04.S07 through RP13.P02.M06.S02?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-394 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp394CloseoutHandoff() {
  return freezeCp394Validation({
    handoff_id: "CP00-394-to-CP00-395",
    from_pack_id: PAYMENTS_CORE_CP394_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP394_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP394_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP394_PACK_BINDING.range,
    open_scope: "Continue RP13.P02.M06.S03 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP394_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp395Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP395_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP395_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp395CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp395Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp395Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-395 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP395_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-395");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP395_PACK_BINDING.risk_class) errors.push("CP00-395 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP395_PACK_BINDING.unit_count) errors.push("CP00-395 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP395_PACK_BINDING.first_unit_id) errors.push("CP00-395 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP395_PACK_BINDING.last_unit_id) errors.push("CP00-395 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-395 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-395 must only include RP13 units");
  const summary = createPaymentsCoreCp395CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP395_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-395 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP395_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-395 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP395_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-395 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP395_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-395 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP395_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-395 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-395 ${microId} missing row ${title}`);
    }
  }
  return freezeCp395Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp395P02FixtureSliceDescriptor(
  descriptor = createPaymentsCoreCp395P02FixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_fixture_slice_case_set ?? createPaymentsCoreCp395P02FixtureSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp395P02FixtureSliceDescriptor") errors.push("CP00-395 descriptor type drift");
  if (descriptor.source_p02_workflow_permission_slice_descriptor !== "PaymentsCoreCp394P02WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-395 source p02 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP395_REQUIREMENTS.required_section_rows).length) errors.push("CP00-395 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP395_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-395 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-395 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-395 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-395 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-395 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-395 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-395 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP395_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-395 must not promote Claude");
  if (PAYMENTS_CORE_CP395_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-395 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-395 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-395 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP395_PACK_BINDING.pack_id, PAYMENTS_CORE_CP396_PACK_BINDING.pack_id, PAYMENTS_CORE_CP397_PACK_BINDING.pack_id, PAYMENTS_CORE_CP398_PACK_BINDING.pack_id, PAYMENTS_CORE_CP399_PACK_BINDING.pack_id, PAYMENTS_CORE_CP400_PACK_BINDING.pack_id, PAYMENTS_CORE_CP401_PACK_BINDING.pack_id, PAYMENTS_CORE_CP402_PACK_BINDING.pack_id, PAYMENTS_CORE_CP403_PACK_BINDING.pack_id, PAYMENTS_CORE_CP404_PACK_BINDING.pack_id, PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-395 contract current_pack drift");
  }
  if (
    contractProjection?.p02_fixture_slice_descriptor?.descriptor &&
    contractProjection.p02_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-395 contract p02_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP395_PACK_BINDING.next_pack_id) errors.push("CP00-395 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP395_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-395 next subphase drift");
  }
  return freezeCp395Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp395HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp395P02FixtureSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp395Coverage(planPack);
  const slice = validatePaymentsCoreCp395P02FixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp395Validation({
    evidence_packet: "H13.CP00-395.payments_core_p02_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP395_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_workflow_permission_slice_pack_id: PAYMENTS_CORE_CP395_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP395_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP395_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP395_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp395ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp395Coverage(planPack);
  const slice = validatePaymentsCoreCp395P02FixtureSliceDescriptor(createPaymentsCoreCp395P02FixtureSliceDescriptor(), {});
  return freezeCp395Validation({
    review_packet: "C13.CP00-395.payments_core_p02_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP395_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP395_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_fixture_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP395_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-395 cover exactly the 10 planned RP13 units from RP13.P02.M06.S03 through RP13.P02.M06.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-395 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp395CloseoutHandoff() {
  return freezeCp395Validation({
    handoff_id: "CP00-395-to-CP00-396",
    from_pack_id: PAYMENTS_CORE_CP395_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP395_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP395_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP395_PACK_BINDING.range,
    open_scope: "Continue RP13.P02.M06.S13 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP395_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp396Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP396_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP396_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp396CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp396Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp396Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-396 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP396_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-396");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP396_PACK_BINDING.risk_class) errors.push("CP00-396 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP396_PACK_BINDING.unit_count) errors.push("CP00-396 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP396_PACK_BINDING.first_unit_id) errors.push("CP00-396 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP396_PACK_BINDING.last_unit_id) errors.push("CP00-396 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-396 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-396 must only include RP13 units");
  const summary = createPaymentsCoreCp396CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP396_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-396 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP396_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-396 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP396_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-396 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP396_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-396 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP396_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-396 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-396 ${microId} missing row ${title}`);
    }
  }
  return freezeCp396Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp396P02CloseoutP03FoundationDescriptor(
  descriptor = createPaymentsCoreCp396P02CloseoutP03FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p02_closeout_p03_foundation_case_set ?? createPaymentsCoreCp396P02CloseoutP03FoundationCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp396P02CloseoutP03FoundationDescriptor") errors.push("CP00-396 descriptor type drift");
  if (descriptor.source_p02_fixture_slice_descriptor !== "PaymentsCoreCp395P02FixtureSliceDescriptor") {
    errors.push("CP00-396 source p02 fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP396_REQUIREMENTS.required_section_rows).length) errors.push("CP00-396 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP396_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-396 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-396 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-396 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-396 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-396 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-396 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-396 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP396_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-396 must not promote Claude");
  if (PAYMENTS_CORE_CP396_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-396 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-396 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-396 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP396_PACK_BINDING.pack_id, PAYMENTS_CORE_CP397_PACK_BINDING.pack_id, PAYMENTS_CORE_CP398_PACK_BINDING.pack_id, PAYMENTS_CORE_CP399_PACK_BINDING.pack_id, PAYMENTS_CORE_CP400_PACK_BINDING.pack_id, PAYMENTS_CORE_CP401_PACK_BINDING.pack_id, PAYMENTS_CORE_CP402_PACK_BINDING.pack_id, PAYMENTS_CORE_CP403_PACK_BINDING.pack_id, PAYMENTS_CORE_CP404_PACK_BINDING.pack_id, PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-396 contract current_pack drift");
  }
  if (
    contractProjection?.p02_closeout_p03_foundation_descriptor?.descriptor &&
    contractProjection.p02_closeout_p03_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-396 contract p02_closeout_p03_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP396_PACK_BINDING.next_pack_id) errors.push("CP00-396 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP396_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-396 next subphase drift");
  }
  return freezeCp396Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp396HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp396P02CloseoutP03FoundationDescriptor(),
) {
  const coverage = validatePaymentsCoreCp396Coverage(planPack);
  const slice = validatePaymentsCoreCp396P02CloseoutP03FoundationDescriptor(descriptor, contractProjection);
  return freezeCp396Validation({
    evidence_packet: "H13.CP00-396.payments_core_p02_closeout_p03_foundation_descriptor",
    gate: PAYMENTS_CORE_CP396_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p02_closeout_p03_foundation_valid: slice.valid,
    no_real_data: true,
    source_p02_fixture_slice_pack_id: PAYMENTS_CORE_CP396_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP396_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP396_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP396_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp396ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp396Coverage(planPack);
  const slice = validatePaymentsCoreCp396P02CloseoutP03FoundationDescriptor(createPaymentsCoreCp396P02CloseoutP03FoundationDescriptor(), {});
  return freezeCp396Validation({
    review_packet: "C13.CP00-396.payments_core_p02_closeout_p03_foundation_descriptor",
    gate: PAYMENTS_CORE_CP396_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP396_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p02_closeout_p03_foundation_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP396_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-396 cover exactly the 150 planned RP13 units from RP13.P02.M06.S13 through RP13.P03.M05.S04?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-396 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp396CloseoutHandoff() {
  return freezeCp396Validation({
    handoff_id: "CP00-396-to-CP00-397",
    from_pack_id: PAYMENTS_CORE_CP396_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP396_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP396_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP396_PACK_BINDING.range,
    open_scope: "Continue RP13.P03.M05.S05 onward with the remaining permission and audit binding rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP396_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp397Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP397_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP397_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp397CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp397Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp397Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-397 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP397_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-397");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP397_PACK_BINDING.risk_class) errors.push("CP00-397 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP397_PACK_BINDING.unit_count) errors.push("CP00-397 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP397_PACK_BINDING.first_unit_id) errors.push("CP00-397 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP397_PACK_BINDING.last_unit_id) errors.push("CP00-397 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-397 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-397 must only include RP13 units");
  const summary = createPaymentsCoreCp397CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP397_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-397 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP397_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-397 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP397_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-397 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP397_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-397 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP397_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-397 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-397 ${microId} missing row ${title}`);
    }
  }
  return freezeCp397Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp397P03PermissionSliceDescriptor(
  descriptor = createPaymentsCoreCp397P03PermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_permission_slice_case_set ?? createPaymentsCoreCp397P03PermissionSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp397P03PermissionSliceDescriptor") errors.push("CP00-397 descriptor type drift");
  if (descriptor.source_p02_closeout_p03_foundation_descriptor !== "PaymentsCoreCp396P02CloseoutP03FoundationDescriptor") {
    errors.push("CP00-397 source p02 closeout p03 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP397_REQUIREMENTS.required_section_rows).length) errors.push("CP00-397 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP397_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-397 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-397 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-397 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-397 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-397 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-397 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-397 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP397_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-397 must not promote Claude");
  if (PAYMENTS_CORE_CP397_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-397 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-397 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-397 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP397_PACK_BINDING.pack_id, PAYMENTS_CORE_CP398_PACK_BINDING.pack_id, PAYMENTS_CORE_CP399_PACK_BINDING.pack_id, PAYMENTS_CORE_CP400_PACK_BINDING.pack_id, PAYMENTS_CORE_CP401_PACK_BINDING.pack_id, PAYMENTS_CORE_CP402_PACK_BINDING.pack_id, PAYMENTS_CORE_CP403_PACK_BINDING.pack_id, PAYMENTS_CORE_CP404_PACK_BINDING.pack_id, PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-397 contract current_pack drift");
  }
  if (
    contractProjection?.p03_permission_slice_descriptor?.descriptor &&
    contractProjection.p03_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-397 contract p03_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP397_PACK_BINDING.next_pack_id) errors.push("CP00-397 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP397_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-397 next subphase drift");
  }
  return freezeCp397Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp397HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp397P03PermissionSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp397Coverage(planPack);
  const slice = validatePaymentsCoreCp397P03PermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp397Validation({
    evidence_packet: "H13.CP00-397.payments_core_p03_permission_slice_descriptor",
    gate: PAYMENTS_CORE_CP397_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p02_closeout_p03_foundation_pack_id: PAYMENTS_CORE_CP397_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP397_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP397_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP397_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp397ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp397Coverage(planPack);
  const slice = validatePaymentsCoreCp397P03PermissionSliceDescriptor(createPaymentsCoreCp397P03PermissionSliceDescriptor(), {});
  return freezeCp397Validation({
    review_packet: "C13.CP00-397.payments_core_p03_permission_slice_descriptor",
    gate: PAYMENTS_CORE_CP397_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP397_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_permission_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP397_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-397 cover exactly the 10 planned RP13 units from RP13.P03.M05.S05 through RP13.P03.M05.S14?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-397 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp397CloseoutHandoff() {
  return freezeCp397Validation({
    handoff_id: "CP00-397-to-CP00-398",
    from_pack_id: PAYMENTS_CORE_CP397_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP397_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP397_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP397_PACK_BINDING.range,
    open_scope: "Continue RP13.P03.M05.S15 onward with the remaining permission and audit binding rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP397_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp398Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP398_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP398_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp398CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp398Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp398Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-398 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP398_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-398");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP398_PACK_BINDING.risk_class) errors.push("CP00-398 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP398_PACK_BINDING.unit_count) errors.push("CP00-398 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP398_PACK_BINDING.first_unit_id) errors.push("CP00-398 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP398_PACK_BINDING.last_unit_id) errors.push("CP00-398 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-398 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-398 must only include RP13 units");
  const summary = createPaymentsCoreCp398CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP398_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-398 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP398_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-398 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP398_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-398 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP398_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-398 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP398_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-398 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-398 ${microId} missing row ${title}`);
    }
  }
  return freezeCp398Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp398P03PermissionFixtureSliceDescriptor(
  descriptor = createPaymentsCoreCp398P03PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_permission_fixture_slice_case_set ?? createPaymentsCoreCp398P03PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp398P03PermissionFixtureSliceDescriptor") errors.push("CP00-398 descriptor type drift");
  if (descriptor.source_p03_permission_slice_descriptor !== "PaymentsCoreCp397P03PermissionSliceDescriptor") {
    errors.push("CP00-398 source p03 permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP398_REQUIREMENTS.required_section_rows).length) errors.push("CP00-398 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP398_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-398 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-398 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-398 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-398 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-398 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-398 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-398 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP398_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-398 must not promote Claude");
  if (PAYMENTS_CORE_CP398_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-398 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-398 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-398 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP398_PACK_BINDING.pack_id, PAYMENTS_CORE_CP399_PACK_BINDING.pack_id, PAYMENTS_CORE_CP400_PACK_BINDING.pack_id, PAYMENTS_CORE_CP401_PACK_BINDING.pack_id, PAYMENTS_CORE_CP402_PACK_BINDING.pack_id, PAYMENTS_CORE_CP403_PACK_BINDING.pack_id, PAYMENTS_CORE_CP404_PACK_BINDING.pack_id, PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-398 contract current_pack drift");
  }
  if (
    contractProjection?.p03_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p03_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-398 contract p03_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP398_PACK_BINDING.next_pack_id) errors.push("CP00-398 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP398_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-398 next subphase drift");
  }
  return freezeCp398Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp398HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp398P03PermissionFixtureSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp398Coverage(planPack);
  const slice = validatePaymentsCoreCp398P03PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp398Validation({
    evidence_packet: "H13.CP00-398.payments_core_p03_permission_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP398_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p03_permission_slice_pack_id: PAYMENTS_CORE_CP398_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP398_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP398_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP398_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp398ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp398Coverage(planPack);
  const slice = validatePaymentsCoreCp398P03PermissionFixtureSliceDescriptor(createPaymentsCoreCp398P03PermissionFixtureSliceDescriptor(), {});
  return freezeCp398Validation({
    review_packet: "C13.CP00-398.payments_core_p03_permission_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP398_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP398_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP398_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-398 cover exactly the 10 planned RP13 units from RP13.P03.M05.S15 through RP13.P03.M06.S04?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-398 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp398CloseoutHandoff() {
  return freezeCp398Validation({
    handoff_id: "CP00-398-to-CP00-399",
    from_pack_id: PAYMENTS_CORE_CP398_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP398_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP398_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP398_PACK_BINDING.range,
    open_scope: "Continue RP13.P03.M06.S05 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP398_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp399Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP399_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP399_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp399CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp399Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp399Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-399 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP399_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-399");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP399_PACK_BINDING.risk_class) errors.push("CP00-399 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP399_PACK_BINDING.unit_count) errors.push("CP00-399 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP399_PACK_BINDING.first_unit_id) errors.push("CP00-399 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP399_PACK_BINDING.last_unit_id) errors.push("CP00-399 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-399 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-399 must only include RP13 units");
  const summary = createPaymentsCoreCp399CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP399_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-399 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP399_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-399 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP399_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-399 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP399_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-399 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP399_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-399 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-399 ${microId} missing row ${title}`);
    }
  }
  return freezeCp399Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp399P03FixtureSliceDescriptor(
  descriptor = createPaymentsCoreCp399P03FixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_fixture_slice_case_set ?? createPaymentsCoreCp399P03FixtureSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp399P03FixtureSliceDescriptor") errors.push("CP00-399 descriptor type drift");
  if (descriptor.source_p03_permission_fixture_slice_descriptor !== "PaymentsCoreCp398P03PermissionFixtureSliceDescriptor") {
    errors.push("CP00-399 source p03 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP399_REQUIREMENTS.required_section_rows).length) errors.push("CP00-399 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP399_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-399 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-399 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-399 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-399 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-399 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-399 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-399 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP399_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-399 must not promote Claude");
  if (PAYMENTS_CORE_CP399_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-399 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-399 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-399 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP399_PACK_BINDING.pack_id, PAYMENTS_CORE_CP400_PACK_BINDING.pack_id, PAYMENTS_CORE_CP401_PACK_BINDING.pack_id, PAYMENTS_CORE_CP402_PACK_BINDING.pack_id, PAYMENTS_CORE_CP403_PACK_BINDING.pack_id, PAYMENTS_CORE_CP404_PACK_BINDING.pack_id, PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-399 contract current_pack drift");
  }
  if (
    contractProjection?.p03_fixture_slice_descriptor?.descriptor &&
    contractProjection.p03_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-399 contract p03_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP399_PACK_BINDING.next_pack_id) errors.push("CP00-399 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP399_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-399 next subphase drift");
  }
  return freezeCp399Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp399HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp399P03FixtureSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp399Coverage(planPack);
  const slice = validatePaymentsCoreCp399P03FixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp399Validation({
    evidence_packet: "H13.CP00-399.payments_core_p03_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP399_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p03_permission_fixture_slice_pack_id: PAYMENTS_CORE_CP399_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP399_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP399_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP399_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp399ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp399Coverage(planPack);
  const slice = validatePaymentsCoreCp399P03FixtureSliceDescriptor(createPaymentsCoreCp399P03FixtureSliceDescriptor(), {});
  return freezeCp399Validation({
    review_packet: "C13.CP00-399.payments_core_p03_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP399_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP399_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_fixture_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP399_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-399 cover exactly the 10 planned RP13 units from RP13.P03.M06.S05 through RP13.P03.M06.S14?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-399 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp399CloseoutHandoff() {
  return freezeCp399Validation({
    handoff_id: "CP00-399-to-CP00-400",
    from_pack_id: PAYMENTS_CORE_CP399_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP399_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP399_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP399_PACK_BINDING.range,
    open_scope: "Continue RP13.P03.M06.S15 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP399_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp400Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP400_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP400_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp400CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp400Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp400Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-400 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP400_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-400");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP400_PACK_BINDING.risk_class) errors.push("CP00-400 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP400_PACK_BINDING.unit_count) errors.push("CP00-400 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP400_PACK_BINDING.first_unit_id) errors.push("CP00-400 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP400_PACK_BINDING.last_unit_id) errors.push("CP00-400 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-400 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-400 must only include RP13 units");
  const summary = createPaymentsCoreCp400CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP400_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-400 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP400_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-400 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP400_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-400 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP400_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-400 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP400_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-400 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-400 ${microId} missing row ${title}`);
    }
  }
  return freezeCp400Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp400P03CloseoutP04FoundationDescriptor(
  descriptor = createPaymentsCoreCp400P03CloseoutP04FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p03_closeout_p04_foundation_case_set ?? createPaymentsCoreCp400P03CloseoutP04FoundationCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp400P03CloseoutP04FoundationDescriptor") errors.push("CP00-400 descriptor type drift");
  if (descriptor.source_p03_fixture_slice_descriptor !== "PaymentsCoreCp399P03FixtureSliceDescriptor") {
    errors.push("CP00-400 source p03 fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP400_REQUIREMENTS.required_section_rows).length) errors.push("CP00-400 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP400_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-400 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-400 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-400 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-400 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-400 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-400 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-400 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP400_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-400 must not promote Claude");
  if (PAYMENTS_CORE_CP400_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-400 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-400 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-400 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP400_PACK_BINDING.pack_id, PAYMENTS_CORE_CP401_PACK_BINDING.pack_id, PAYMENTS_CORE_CP402_PACK_BINDING.pack_id, PAYMENTS_CORE_CP403_PACK_BINDING.pack_id, PAYMENTS_CORE_CP404_PACK_BINDING.pack_id, PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-400 contract current_pack drift");
  }
  if (
    contractProjection?.p03_closeout_p04_foundation_descriptor?.descriptor &&
    contractProjection.p03_closeout_p04_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-400 contract p03_closeout_p04_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP400_PACK_BINDING.next_pack_id) errors.push("CP00-400 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP400_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-400 next subphase drift");
  }
  return freezeCp400Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp400HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp400P03CloseoutP04FoundationDescriptor(),
) {
  const coverage = validatePaymentsCoreCp400Coverage(planPack);
  const slice = validatePaymentsCoreCp400P03CloseoutP04FoundationDescriptor(descriptor, contractProjection);
  return freezeCp400Validation({
    evidence_packet: "H13.CP00-400.payments_core_p03_closeout_p04_foundation_descriptor",
    gate: PAYMENTS_CORE_CP400_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p03_closeout_p04_foundation_valid: slice.valid,
    no_real_data: true,
    source_p03_fixture_slice_pack_id: PAYMENTS_CORE_CP400_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP400_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP400_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP400_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp400ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp400Coverage(planPack);
  const slice = validatePaymentsCoreCp400P03CloseoutP04FoundationDescriptor(createPaymentsCoreCp400P03CloseoutP04FoundationDescriptor(), {});
  return freezeCp400Validation({
    review_packet: "C13.CP00-400.payments_core_p03_closeout_p04_foundation_descriptor",
    gate: PAYMENTS_CORE_CP400_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP400_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p03_closeout_p04_foundation_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP400_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-400 cover exactly the 150 planned RP13 units from RP13.P03.M06.S15 through RP13.P04.M05.S15?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-400 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp400CloseoutHandoff() {
  return freezeCp400Validation({
    handoff_id: "CP00-400-to-CP00-401",
    from_pack_id: PAYMENTS_CORE_CP400_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP400_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP400_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP400_PACK_BINDING.range,
    open_scope: "Continue RP13.P04.M05.S16 onward with the remaining permission and audit binding rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP400_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp401Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP401_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP401_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp401CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp401Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp401Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-401 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP401_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-401");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP401_PACK_BINDING.risk_class) errors.push("CP00-401 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP401_PACK_BINDING.unit_count) errors.push("CP00-401 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP401_PACK_BINDING.first_unit_id) errors.push("CP00-401 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP401_PACK_BINDING.last_unit_id) errors.push("CP00-401 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-401 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-401 must only include RP13 units");
  const summary = createPaymentsCoreCp401CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP401_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-401 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP401_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-401 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP401_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-401 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP401_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-401 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP401_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-401 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-401 ${microId} missing row ${title}`);
    }
  }
  return freezeCp401Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp401P04PermissionFixtureSliceDescriptor(
  descriptor = createPaymentsCoreCp401P04PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_permission_fixture_slice_case_set ?? createPaymentsCoreCp401P04PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp401P04PermissionFixtureSliceDescriptor") errors.push("CP00-401 descriptor type drift");
  if (descriptor.source_p03_closeout_p04_foundation_descriptor !== "PaymentsCoreCp400P03CloseoutP04FoundationDescriptor") {
    errors.push("CP00-401 source p03 closeout p04 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP401_REQUIREMENTS.required_section_rows).length) errors.push("CP00-401 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP401_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-401 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-401 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-401 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-401 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-401 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-401 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-401 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP401_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-401 must not promote Claude");
  if (PAYMENTS_CORE_CP401_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-401 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-401 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-401 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP401_PACK_BINDING.pack_id, PAYMENTS_CORE_CP402_PACK_BINDING.pack_id, PAYMENTS_CORE_CP403_PACK_BINDING.pack_id, PAYMENTS_CORE_CP404_PACK_BINDING.pack_id, PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-401 contract current_pack drift");
  }
  if (
    contractProjection?.p04_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p04_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-401 contract p04_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP401_PACK_BINDING.next_pack_id) errors.push("CP00-401 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP401_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-401 next subphase drift");
  }
  return freezeCp401Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp401HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp401P04PermissionFixtureSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp401Coverage(planPack);
  const slice = validatePaymentsCoreCp401P04PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp401Validation({
    evidence_packet: "H13.CP00-401.payments_core_p04_permission_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP401_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p03_closeout_p04_foundation_pack_id: PAYMENTS_CORE_CP401_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP401_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP401_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP401_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp401ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp401Coverage(planPack);
  const slice = validatePaymentsCoreCp401P04PermissionFixtureSliceDescriptor(createPaymentsCoreCp401P04PermissionFixtureSliceDescriptor(), {});
  return freezeCp401Validation({
    review_packet: "C13.CP00-401.payments_core_p04_permission_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP401_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP401_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP401_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-401 cover exactly the 10 planned RP13 units from RP13.P04.M05.S16 through RP13.P04.M06.S03?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-401 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp401CloseoutHandoff() {
  return freezeCp401Validation({
    handoff_id: "CP00-401-to-CP00-402",
    from_pack_id: PAYMENTS_CORE_CP401_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP401_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP401_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP401_PACK_BINDING.range,
    open_scope: "Continue RP13.P04.M06.S04 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP401_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp402Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP402_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP402_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp402CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp402Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp402Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-402 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP402_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-402");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP402_PACK_BINDING.risk_class) errors.push("CP00-402 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP402_PACK_BINDING.unit_count) errors.push("CP00-402 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP402_PACK_BINDING.first_unit_id) errors.push("CP00-402 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP402_PACK_BINDING.last_unit_id) errors.push("CP00-402 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-402 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-402 must only include RP13 units");
  const summary = createPaymentsCoreCp402CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP402_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-402 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP402_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-402 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP402_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-402 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP402_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-402 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP402_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-402 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-402 ${microId} missing row ${title}`);
    }
  }
  return freezeCp402Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp402P04CloseoutP05FoundationDescriptor(
  descriptor = createPaymentsCoreCp402P04CloseoutP05FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p04_closeout_p05_foundation_case_set ?? createPaymentsCoreCp402P04CloseoutP05FoundationCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp402P04CloseoutP05FoundationDescriptor") errors.push("CP00-402 descriptor type drift");
  if (descriptor.source_p04_permission_fixture_slice_descriptor !== "PaymentsCoreCp401P04PermissionFixtureSliceDescriptor") {
    errors.push("CP00-402 source p04 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP402_REQUIREMENTS.required_section_rows).length) errors.push("CP00-402 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP402_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-402 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-402 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-402 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-402 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-402 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-402 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-402 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP402_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-402 must not promote Claude");
  if (PAYMENTS_CORE_CP402_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-402 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-402 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-402 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP402_PACK_BINDING.pack_id, PAYMENTS_CORE_CP403_PACK_BINDING.pack_id, PAYMENTS_CORE_CP404_PACK_BINDING.pack_id, PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-402 contract current_pack drift");
  }
  if (
    contractProjection?.p04_closeout_p05_foundation_descriptor?.descriptor &&
    contractProjection.p04_closeout_p05_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-402 contract p04_closeout_p05_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP402_PACK_BINDING.next_pack_id) errors.push("CP00-402 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP402_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-402 next subphase drift");
  }
  return freezeCp402Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp402HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp402P04CloseoutP05FoundationDescriptor(),
) {
  const coverage = validatePaymentsCoreCp402Coverage(planPack);
  const slice = validatePaymentsCoreCp402P04CloseoutP05FoundationDescriptor(descriptor, contractProjection);
  return freezeCp402Validation({
    evidence_packet: "H13.CP00-402.payments_core_p04_closeout_p05_foundation_descriptor",
    gate: PAYMENTS_CORE_CP402_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p04_closeout_p05_foundation_valid: slice.valid,
    no_real_data: true,
    source_p04_permission_fixture_slice_pack_id: PAYMENTS_CORE_CP402_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP402_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP402_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP402_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp402ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp402Coverage(planPack);
  const slice = validatePaymentsCoreCp402P04CloseoutP05FoundationDescriptor(createPaymentsCoreCp402P04CloseoutP05FoundationDescriptor(), {});
  return freezeCp402Validation({
    review_packet: "C13.CP00-402.payments_core_p04_closeout_p05_foundation_descriptor",
    gate: PAYMENTS_CORE_CP402_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP402_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p04_closeout_p05_foundation_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP402_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-402 cover exactly the 150 planned RP13 units from RP13.P04.M06.S04 through RP13.P05.M04.S05?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-402 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp402CloseoutHandoff() {
  return freezeCp402Validation({
    handoff_id: "CP00-402-to-CP00-403",
    from_pack_id: PAYMENTS_CORE_CP402_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP402_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP402_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP402_PACK_BINDING.range,
    open_scope: "Continue RP13.P05.M04.S06 onward with the remaining secondary workflow rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP402_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp403Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP403_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP403_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp403CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp403Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp403Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-403 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP403_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-403");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP403_PACK_BINDING.risk_class) errors.push("CP00-403 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP403_PACK_BINDING.unit_count) errors.push("CP00-403 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP403_PACK_BINDING.first_unit_id) errors.push("CP00-403 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP403_PACK_BINDING.last_unit_id) errors.push("CP00-403 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-403 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-403 must only include RP13 units");
  const summary = createPaymentsCoreCp403CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP403_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-403 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP403_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-403 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP403_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-403 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP403_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-403 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP403_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-403 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-403 ${microId} missing row ${title}`);
    }
  }
  return freezeCp403Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp403P05CloseoutP06FoundationDescriptor(
  descriptor = createPaymentsCoreCp403P05CloseoutP06FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p05_closeout_p06_foundation_case_set ?? createPaymentsCoreCp403P05CloseoutP06FoundationCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp403P05CloseoutP06FoundationDescriptor") errors.push("CP00-403 descriptor type drift");
  if (descriptor.source_p04_closeout_p05_foundation_descriptor !== "PaymentsCoreCp402P04CloseoutP05FoundationDescriptor") {
    errors.push("CP00-403 source p04 closeout p05 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP403_REQUIREMENTS.required_section_rows).length) errors.push("CP00-403 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP403_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-403 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-403 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-403 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-403 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-403 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-403 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-403 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP403_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-403 must not promote Claude");
  if (PAYMENTS_CORE_CP403_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-403 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-403 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-403 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP403_PACK_BINDING.pack_id, PAYMENTS_CORE_CP404_PACK_BINDING.pack_id, PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-403 contract current_pack drift");
  }
  if (
    contractProjection?.p05_closeout_p06_foundation_descriptor?.descriptor &&
    contractProjection.p05_closeout_p06_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-403 contract p05_closeout_p06_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP403_PACK_BINDING.next_pack_id) errors.push("CP00-403 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP403_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-403 next subphase drift");
  }
  return freezeCp403Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp403HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp403P05CloseoutP06FoundationDescriptor(),
) {
  const coverage = validatePaymentsCoreCp403Coverage(planPack);
  const slice = validatePaymentsCoreCp403P05CloseoutP06FoundationDescriptor(descriptor, contractProjection);
  return freezeCp403Validation({
    evidence_packet: "H13.CP00-403.payments_core_p05_closeout_p06_foundation_descriptor",
    gate: PAYMENTS_CORE_CP403_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p05_closeout_p06_foundation_valid: slice.valid,
    no_real_data: true,
    source_p04_closeout_p05_foundation_pack_id: PAYMENTS_CORE_CP403_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP403_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP403_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP403_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp403ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp403Coverage(planPack);
  const slice = validatePaymentsCoreCp403P05CloseoutP06FoundationDescriptor(createPaymentsCoreCp403P05CloseoutP06FoundationDescriptor(), {});
  return freezeCp403Validation({
    review_packet: "C13.CP00-403.payments_core_p05_closeout_p06_foundation_descriptor",
    gate: PAYMENTS_CORE_CP403_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP403_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p05_closeout_p06_foundation_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP403_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-403 cover exactly the 150 planned RP13 units from RP13.P05.M04.S06 through RP13.P06.M01.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-403 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp403CloseoutHandoff() {
  return freezeCp403Validation({
    handoff_id: "CP00-403-to-CP00-404",
    from_pack_id: PAYMENTS_CORE_CP403_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP403_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP403_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP403_PACK_BINDING.range,
    open_scope: "Continue RP13.P06.M01.S13 onward with the remaining contract draft rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP403_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp404Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP404_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP404_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp404CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp404Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp404Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-404 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP404_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-404");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP404_PACK_BINDING.risk_class) errors.push("CP00-404 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP404_PACK_BINDING.unit_count) errors.push("CP00-404 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP404_PACK_BINDING.first_unit_id) errors.push("CP00-404 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP404_PACK_BINDING.last_unit_id) errors.push("CP00-404 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-404 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-404 must only include RP13 units");
  const summary = createPaymentsCoreCp404CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP404_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-404 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP404_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-404 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP404_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-404 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP404_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-404 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP404_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-404 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-404 ${microId} missing row ${title}`);
    }
  }
  return freezeCp404Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp404P06ContractImplementationSliceDescriptor(
  descriptor = createPaymentsCoreCp404P06ContractImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_contract_implementation_slice_case_set ?? createPaymentsCoreCp404P06ContractImplementationSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp404P06ContractImplementationSliceDescriptor") errors.push("CP00-404 descriptor type drift");
  if (descriptor.source_p05_closeout_p06_foundation_descriptor !== "PaymentsCoreCp403P05CloseoutP06FoundationDescriptor") {
    errors.push("CP00-404 source p05 closeout p06 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP404_REQUIREMENTS.required_section_rows).length) errors.push("CP00-404 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP404_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-404 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-404 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-404 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-404 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-404 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-404 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-404 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP404_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-404 must not promote Claude");
  if (PAYMENTS_CORE_CP404_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-404 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-404 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-404 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP404_PACK_BINDING.pack_id, PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-404 contract current_pack drift");
  }
  if (
    contractProjection?.p06_contract_implementation_slice_descriptor?.descriptor &&
    contractProjection.p06_contract_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-404 contract p06_contract_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP404_PACK_BINDING.next_pack_id) errors.push("CP00-404 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP404_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-404 next subphase drift");
  }
  return freezeCp404Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp404HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp404P06ContractImplementationSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp404Coverage(planPack);
  const slice = validatePaymentsCoreCp404P06ContractImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp404Validation({
    evidence_packet: "H13.CP00-404.payments_core_p06_contract_implementation_slice_descriptor",
    gate: PAYMENTS_CORE_CP404_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_contract_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_p05_closeout_p06_foundation_pack_id: PAYMENTS_CORE_CP404_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP404_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP404_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP404_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp404ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp404Coverage(planPack);
  const slice = validatePaymentsCoreCp404P06ContractImplementationSliceDescriptor(createPaymentsCoreCp404P06ContractImplementationSliceDescriptor(), {});
  return freezeCp404Validation({
    review_packet: "C13.CP00-404.payments_core_p06_contract_implementation_slice_descriptor",
    gate: PAYMENTS_CORE_CP404_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP404_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_contract_implementation_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP404_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-404 cover exactly the 40 planned RP13 units from RP13.P06.M01.S13 through RP13.P06.M03.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-404 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp404CloseoutHandoff() {
  return freezeCp404Validation({
    handoff_id: "CP00-404-to-CP00-405",
    from_pack_id: PAYMENTS_CORE_CP404_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP404_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP404_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP404_PACK_BINDING.range,
    open_scope: "Continue RP13.P06.M03.S13 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP404_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp405Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP405_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP405_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp405CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp405Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp405Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-405 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP405_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-405");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP405_PACK_BINDING.risk_class) errors.push("CP00-405 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP405_PACK_BINDING.unit_count) errors.push("CP00-405 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP405_PACK_BINDING.first_unit_id) errors.push("CP00-405 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP405_PACK_BINDING.last_unit_id) errors.push("CP00-405 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-405 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-405 must only include RP13 units");
  const summary = createPaymentsCoreCp405CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP405_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-405 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP405_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-405 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP405_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-405 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP405_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-405 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP405_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-405 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-405 ${microId} missing row ${title}`);
    }
  }
  return freezeCp405Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp405P06ImplementationWorkflowSliceDescriptor(
  descriptor = createPaymentsCoreCp405P06ImplementationWorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_implementation_workflow_slice_case_set ?? createPaymentsCoreCp405P06ImplementationWorkflowSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp405P06ImplementationWorkflowSliceDescriptor") errors.push("CP00-405 descriptor type drift");
  if (descriptor.source_p06_contract_implementation_slice_descriptor !== "PaymentsCoreCp404P06ContractImplementationSliceDescriptor") {
    errors.push("CP00-405 source p06 contract implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP405_REQUIREMENTS.required_section_rows).length) errors.push("CP00-405 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP405_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-405 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-405 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-405 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-405 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-405 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-405 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-405 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP405_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-405 must not promote Claude");
  if (PAYMENTS_CORE_CP405_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-405 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-405 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-405 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP405_PACK_BINDING.pack_id, PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-405 contract current_pack drift");
  }
  if (
    contractProjection?.p06_implementation_workflow_slice_descriptor?.descriptor &&
    contractProjection.p06_implementation_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-405 contract p06_implementation_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP405_PACK_BINDING.next_pack_id) errors.push("CP00-405 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP405_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-405 next subphase drift");
  }
  return freezeCp405Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp405HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp405P06ImplementationWorkflowSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp405Coverage(planPack);
  const slice = validatePaymentsCoreCp405P06ImplementationWorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp405Validation({
    evidence_packet: "H13.CP00-405.payments_core_p06_implementation_workflow_slice_descriptor",
    gate: PAYMENTS_CORE_CP405_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_implementation_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_contract_implementation_slice_pack_id: PAYMENTS_CORE_CP405_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP405_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP405_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP405_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp405ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp405Coverage(planPack);
  const slice = validatePaymentsCoreCp405P06ImplementationWorkflowSliceDescriptor(createPaymentsCoreCp405P06ImplementationWorkflowSliceDescriptor(), {});
  return freezeCp405Validation({
    review_packet: "C13.CP00-405.payments_core_p06_implementation_workflow_slice_descriptor",
    gate: PAYMENTS_CORE_CP405_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP405_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_implementation_workflow_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP405_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-405 cover exactly the 40 planned RP13 units from RP13.P06.M03.S13 through RP13.P06.M05.S08?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-405 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp405CloseoutHandoff() {
  return freezeCp405Validation({
    handoff_id: "CP00-405-to-CP00-406",
    from_pack_id: PAYMENTS_CORE_CP405_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP405_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP405_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP405_PACK_BINDING.range,
    open_scope: "Continue RP13.P06.M05.S09 onward with the remaining permission and audit binding rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP405_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp406Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP406_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP406_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp406CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp406Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp406Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-406 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP406_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-406");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP406_PACK_BINDING.risk_class) errors.push("CP00-406 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP406_PACK_BINDING.unit_count) errors.push("CP00-406 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP406_PACK_BINDING.first_unit_id) errors.push("CP00-406 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP406_PACK_BINDING.last_unit_id) errors.push("CP00-406 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-406 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-406 must only include RP13 units");
  const summary = createPaymentsCoreCp406CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP406_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-406 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP406_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-406 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP406_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-406 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP406_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-406 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP406_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-406 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-406 ${microId} missing row ${title}`);
    }
  }
  return freezeCp406Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp406P06PermissionSliceDescriptor(
  descriptor = createPaymentsCoreCp406P06PermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_permission_slice_case_set ?? createPaymentsCoreCp406P06PermissionSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp406P06PermissionSliceDescriptor") errors.push("CP00-406 descriptor type drift");
  if (descriptor.source_p06_implementation_workflow_slice_descriptor !== "PaymentsCoreCp405P06ImplementationWorkflowSliceDescriptor") {
    errors.push("CP00-406 source p06 implementation workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP406_REQUIREMENTS.required_section_rows).length) errors.push("CP00-406 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP406_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-406 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-406 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-406 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-406 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-406 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-406 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-406 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP406_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-406 must not promote Claude");
  if (PAYMENTS_CORE_CP406_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-406 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-406 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-406 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP406_PACK_BINDING.pack_id, PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-406 contract current_pack drift");
  }
  if (
    contractProjection?.p06_permission_slice_descriptor?.descriptor &&
    contractProjection.p06_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-406 contract p06_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP406_PACK_BINDING.next_pack_id) errors.push("CP00-406 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP406_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-406 next subphase drift");
  }
  return freezeCp406Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp406HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp406P06PermissionSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp406Coverage(planPack);
  const slice = validatePaymentsCoreCp406P06PermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp406Validation({
    evidence_packet: "H13.CP00-406.payments_core_p06_permission_slice_descriptor",
    gate: PAYMENTS_CORE_CP406_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_implementation_workflow_slice_pack_id: PAYMENTS_CORE_CP406_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP406_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP406_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP406_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp406ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp406Coverage(planPack);
  const slice = validatePaymentsCoreCp406P06PermissionSliceDescriptor(createPaymentsCoreCp406P06PermissionSliceDescriptor(), {});
  return freezeCp406Validation({
    review_packet: "C13.CP00-406.payments_core_p06_permission_slice_descriptor",
    gate: PAYMENTS_CORE_CP406_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP406_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_permission_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP406_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-406 cover exactly the 10 planned RP13 units from RP13.P06.M05.S09 through RP13.P06.M05.S18?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-406 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp406CloseoutHandoff() {
  return freezeCp406Validation({
    handoff_id: "CP00-406-to-CP00-407",
    from_pack_id: PAYMENTS_CORE_CP406_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP406_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP406_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP406_PACK_BINDING.range,
    open_scope: "Continue RP13.P06.M05.S19 onward with the remaining permission and audit binding rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP406_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp407Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP407_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP407_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp407CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp407Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp407Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-407 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP407_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-407");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP407_PACK_BINDING.risk_class) errors.push("CP00-407 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP407_PACK_BINDING.unit_count) errors.push("CP00-407 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP407_PACK_BINDING.first_unit_id) errors.push("CP00-407 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP407_PACK_BINDING.last_unit_id) errors.push("CP00-407 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-407 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-407 must only include RP13 units");
  const summary = createPaymentsCoreCp407CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP407_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-407 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP407_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-407 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP407_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-407 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP407_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-407 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP407_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-407 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-407 ${microId} missing row ${title}`);
    }
  }
  return freezeCp407Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp407P06PermissionFixtureSliceDescriptor(
  descriptor = createPaymentsCoreCp407P06PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_permission_fixture_slice_case_set ?? createPaymentsCoreCp407P06PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp407P06PermissionFixtureSliceDescriptor") errors.push("CP00-407 descriptor type drift");
  if (descriptor.source_p06_permission_slice_descriptor !== "PaymentsCoreCp406P06PermissionSliceDescriptor") {
    errors.push("CP00-407 source p06 permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP407_REQUIREMENTS.required_section_rows).length) errors.push("CP00-407 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP407_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-407 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-407 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-407 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-407 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-407 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-407 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-407 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP407_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-407 must not promote Claude");
  if (PAYMENTS_CORE_CP407_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-407 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-407 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-407 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP407_PACK_BINDING.pack_id, PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-407 contract current_pack drift");
  }
  if (
    contractProjection?.p06_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p06_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-407 contract p06_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP407_PACK_BINDING.next_pack_id) errors.push("CP00-407 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP407_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-407 next subphase drift");
  }
  return freezeCp407Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp407HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp407P06PermissionFixtureSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp407Coverage(planPack);
  const slice = validatePaymentsCoreCp407P06PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp407Validation({
    evidence_packet: "H13.CP00-407.payments_core_p06_permission_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP407_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_permission_slice_pack_id: PAYMENTS_CORE_CP407_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP407_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP407_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP407_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp407ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp407Coverage(planPack);
  const slice = validatePaymentsCoreCp407P06PermissionFixtureSliceDescriptor(createPaymentsCoreCp407P06PermissionFixtureSliceDescriptor(), {});
  return freezeCp407Validation({
    review_packet: "C13.CP00-407.payments_core_p06_permission_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP407_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP407_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP407_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-407 cover exactly the 10 planned RP13 units from RP13.P06.M05.S19 through RP13.P06.M06.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-407 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp407CloseoutHandoff() {
  return freezeCp407Validation({
    handoff_id: "CP00-407-to-CP00-408",
    from_pack_id: PAYMENTS_CORE_CP407_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP407_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP407_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP407_PACK_BINDING.range,
    open_scope: "Continue RP13.P06.M06.S07 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP407_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp408Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP408_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP408_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp408CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp408Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp408Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-408 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP408_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-408");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP408_PACK_BINDING.risk_class) errors.push("CP00-408 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP408_PACK_BINDING.unit_count) errors.push("CP00-408 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP408_PACK_BINDING.first_unit_id) errors.push("CP00-408 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP408_PACK_BINDING.last_unit_id) errors.push("CP00-408 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-408 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-408 must only include RP13 units");
  const summary = createPaymentsCoreCp408CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP408_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-408 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP408_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-408 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP408_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-408 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP408_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-408 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP408_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-408 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-408 ${microId} missing row ${title}`);
    }
  }
  return freezeCp408Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp408P06CloseoutP07FoundationDescriptor(
  descriptor = createPaymentsCoreCp408P06CloseoutP07FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p06_closeout_p07_foundation_case_set ?? createPaymentsCoreCp408P06CloseoutP07FoundationCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp408P06CloseoutP07FoundationDescriptor") errors.push("CP00-408 descriptor type drift");
  if (descriptor.source_p06_permission_fixture_slice_descriptor !== "PaymentsCoreCp407P06PermissionFixtureSliceDescriptor") {
    errors.push("CP00-408 source p06 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP408_REQUIREMENTS.required_section_rows).length) errors.push("CP00-408 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP408_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-408 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-408 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-408 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-408 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-408 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-408 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-408 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP408_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-408 must not promote Claude");
  if (PAYMENTS_CORE_CP408_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-408 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-408 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-408 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP408_PACK_BINDING.pack_id, PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-408 contract current_pack drift");
  }
  if (
    contractProjection?.p06_closeout_p07_foundation_descriptor?.descriptor &&
    contractProjection.p06_closeout_p07_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-408 contract p06_closeout_p07_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP408_PACK_BINDING.next_pack_id) errors.push("CP00-408 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP408_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-408 next subphase drift");
  }
  return freezeCp408Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp408HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp408P06CloseoutP07FoundationDescriptor(),
) {
  const coverage = validatePaymentsCoreCp408Coverage(planPack);
  const slice = validatePaymentsCoreCp408P06CloseoutP07FoundationDescriptor(descriptor, contractProjection);
  return freezeCp408Validation({
    evidence_packet: "H13.CP00-408.payments_core_p06_closeout_p07_foundation_descriptor",
    gate: PAYMENTS_CORE_CP408_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p06_closeout_p07_foundation_valid: slice.valid,
    no_real_data: true,
    source_p06_permission_fixture_slice_pack_id: PAYMENTS_CORE_CP408_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP408_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP408_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP408_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp408ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp408Coverage(planPack);
  const slice = validatePaymentsCoreCp408P06CloseoutP07FoundationDescriptor(createPaymentsCoreCp408P06CloseoutP07FoundationDescriptor(), {});
  return freezeCp408Validation({
    review_packet: "C13.CP00-408.payments_core_p06_closeout_p07_foundation_descriptor",
    gate: PAYMENTS_CORE_CP408_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP408_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p06_closeout_p07_foundation_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP408_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-408 cover exactly the 150 planned RP13 units from RP13.P06.M06.S07 through RP13.P07.M03.S06?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-408 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp408CloseoutHandoff() {
  return freezeCp408Validation({
    handoff_id: "CP00-408-to-CP00-409",
    from_pack_id: PAYMENTS_CORE_CP408_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP408_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP408_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP408_PACK_BINDING.range,
    open_scope: "Continue RP13.P07.M03.S07 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP408_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp409Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP409_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP409_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp409CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp409Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp409Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-409 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP409_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-409");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP409_PACK_BINDING.risk_class) errors.push("CP00-409 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP409_PACK_BINDING.unit_count) errors.push("CP00-409 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP409_PACK_BINDING.first_unit_id) errors.push("CP00-409 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP409_PACK_BINDING.last_unit_id) errors.push("CP00-409 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-409 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-409 must only include RP13 units");
  const summary = createPaymentsCoreCp409CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP409_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-409 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP409_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-409 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP409_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-409 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP409_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-409 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP409_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-409 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-409 ${microId} missing row ${title}`);
    }
  }
  return freezeCp409Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp409P07ImplementationSliceDescriptor(
  descriptor = createPaymentsCoreCp409P07ImplementationSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_implementation_slice_case_set ?? createPaymentsCoreCp409P07ImplementationSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp409P07ImplementationSliceDescriptor") errors.push("CP00-409 descriptor type drift");
  if (descriptor.source_p06_closeout_p07_foundation_descriptor !== "PaymentsCoreCp408P06CloseoutP07FoundationDescriptor") {
    errors.push("CP00-409 source p06 closeout p07 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP409_REQUIREMENTS.required_section_rows).length) errors.push("CP00-409 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP409_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-409 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-409 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-409 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-409 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-409 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-409 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-409 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP409_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-409 must not promote Claude");
  if (PAYMENTS_CORE_CP409_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-409 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-409 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-409 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP409_PACK_BINDING.pack_id, PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-409 contract current_pack drift");
  }
  if (
    contractProjection?.p07_implementation_slice_descriptor?.descriptor &&
    contractProjection.p07_implementation_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-409 contract p07_implementation_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP409_PACK_BINDING.next_pack_id) errors.push("CP00-409 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP409_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-409 next subphase drift");
  }
  return freezeCp409Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp409HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp409P07ImplementationSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp409Coverage(planPack);
  const slice = validatePaymentsCoreCp409P07ImplementationSliceDescriptor(descriptor, contractProjection);
  return freezeCp409Validation({
    evidence_packet: "H13.CP00-409.payments_core_p07_implementation_slice_descriptor",
    gate: PAYMENTS_CORE_CP409_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_implementation_slice_valid: slice.valid,
    no_real_data: true,
    source_p06_closeout_p07_foundation_pack_id: PAYMENTS_CORE_CP409_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP409_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP409_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP409_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp409ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp409Coverage(planPack);
  const slice = validatePaymentsCoreCp409P07ImplementationSliceDescriptor(createPaymentsCoreCp409P07ImplementationSliceDescriptor(), {});
  return freezeCp409Validation({
    review_packet: "C13.CP00-409.payments_core_p07_implementation_slice_descriptor",
    gate: PAYMENTS_CORE_CP409_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP409_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_implementation_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP409_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-409 cover exactly the 10 planned RP13 units from RP13.P07.M03.S07 through RP13.P07.M03.S16?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-409 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp409CloseoutHandoff() {
  return freezeCp409Validation({
    handoff_id: "CP00-409-to-CP00-410",
    from_pack_id: PAYMENTS_CORE_CP409_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP409_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP409_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP409_PACK_BINDING.range,
    open_scope: "Continue RP13.P07.M03.S17 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP409_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp410Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP410_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP410_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp410CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp410Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp410Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-410 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP410_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-410");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP410_PACK_BINDING.risk_class) errors.push("CP00-410 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP410_PACK_BINDING.unit_count) errors.push("CP00-410 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP410_PACK_BINDING.first_unit_id) errors.push("CP00-410 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP410_PACK_BINDING.last_unit_id) errors.push("CP00-410 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-410 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-410 must only include RP13 units");
  const summary = createPaymentsCoreCp410CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP410_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-410 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP410_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-410 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP410_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-410 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP410_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-410 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP410_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-410 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-410 ${microId} missing row ${title}`);
    }
  }
  return freezeCp410Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp410P07WorkflowPermissionSliceDescriptor(
  descriptor = createPaymentsCoreCp410P07WorkflowPermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_workflow_permission_slice_case_set ?? createPaymentsCoreCp410P07WorkflowPermissionSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp410P07WorkflowPermissionSliceDescriptor") errors.push("CP00-410 descriptor type drift");
  if (descriptor.source_p07_implementation_slice_descriptor !== "PaymentsCoreCp409P07ImplementationSliceDescriptor") {
    errors.push("CP00-410 source p07 implementation slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP410_REQUIREMENTS.required_section_rows).length) errors.push("CP00-410 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP410_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-410 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-410 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-410 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-410 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-410 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-410 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-410 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP410_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-410 must not promote Claude");
  if (PAYMENTS_CORE_CP410_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-410 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-410 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-410 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP410_PACK_BINDING.pack_id, PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-410 contract current_pack drift");
  }
  if (
    contractProjection?.p07_workflow_permission_slice_descriptor?.descriptor &&
    contractProjection.p07_workflow_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-410 contract p07_workflow_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP410_PACK_BINDING.next_pack_id) errors.push("CP00-410 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP410_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-410 next subphase drift");
  }
  return freezeCp410Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp410HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp410P07WorkflowPermissionSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp410Coverage(planPack);
  const slice = validatePaymentsCoreCp410P07WorkflowPermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp410Validation({
    evidence_packet: "H13.CP00-410.payments_core_p07_workflow_permission_slice_descriptor",
    gate: PAYMENTS_CORE_CP410_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_workflow_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_implementation_slice_pack_id: PAYMENTS_CORE_CP410_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP410_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP410_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP410_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp410ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp410Coverage(planPack);
  const slice = validatePaymentsCoreCp410P07WorkflowPermissionSliceDescriptor(createPaymentsCoreCp410P07WorkflowPermissionSliceDescriptor(), {});
  return freezeCp410Validation({
    review_packet: "C13.CP00-410.payments_core_p07_workflow_permission_slice_descriptor",
    gate: PAYMENTS_CORE_CP410_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP410_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_workflow_permission_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP410_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-410 cover exactly the 40 planned RP13 units from RP13.P07.M03.S17 through RP13.P07.M05.S12?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-410 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp410CloseoutHandoff() {
  return freezeCp410Validation({
    handoff_id: "CP00-410-to-CP00-411",
    from_pack_id: PAYMENTS_CORE_CP410_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP410_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP410_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP410_PACK_BINDING.range,
    open_scope: "Continue RP13.P07.M05.S13 onward with the remaining permission and audit binding rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP410_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp411Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP411_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP411_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp411CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp411Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp411Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-411 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP411_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-411");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP411_PACK_BINDING.risk_class) errors.push("CP00-411 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP411_PACK_BINDING.unit_count) errors.push("CP00-411 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP411_PACK_BINDING.first_unit_id) errors.push("CP00-411 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP411_PACK_BINDING.last_unit_id) errors.push("CP00-411 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-411 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-411 must only include RP13 units");
  const summary = createPaymentsCoreCp411CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP411_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-411 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP411_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-411 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP411_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-411 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP411_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-411 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP411_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-411 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-411 ${microId} missing row ${title}`);
    }
  }
  return freezeCp411Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp411P07PermissionSliceDescriptor(
  descriptor = createPaymentsCoreCp411P07PermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_permission_slice_case_set ?? createPaymentsCoreCp411P07PermissionSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp411P07PermissionSliceDescriptor") errors.push("CP00-411 descriptor type drift");
  if (descriptor.source_p07_workflow_permission_slice_descriptor !== "PaymentsCoreCp410P07WorkflowPermissionSliceDescriptor") {
    errors.push("CP00-411 source p07 workflow permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP411_REQUIREMENTS.required_section_rows).length) errors.push("CP00-411 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP411_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-411 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-411 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-411 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-411 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-411 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-411 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-411 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP411_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-411 must not promote Claude");
  if (PAYMENTS_CORE_CP411_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-411 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-411 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-411 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP411_PACK_BINDING.pack_id, PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-411 contract current_pack drift");
  }
  if (
    contractProjection?.p07_permission_slice_descriptor?.descriptor &&
    contractProjection.p07_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-411 contract p07_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP411_PACK_BINDING.next_pack_id) errors.push("CP00-411 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP411_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-411 next subphase drift");
  }
  return freezeCp411Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp411HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp411P07PermissionSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp411Coverage(planPack);
  const slice = validatePaymentsCoreCp411P07PermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp411Validation({
    evidence_packet: "H13.CP00-411.payments_core_p07_permission_slice_descriptor",
    gate: PAYMENTS_CORE_CP411_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_workflow_permission_slice_pack_id: PAYMENTS_CORE_CP411_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP411_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP411_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP411_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp411ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp411Coverage(planPack);
  const slice = validatePaymentsCoreCp411P07PermissionSliceDescriptor(createPaymentsCoreCp411P07PermissionSliceDescriptor(), {});
  return freezeCp411Validation({
    review_packet: "C13.CP00-411.payments_core_p07_permission_slice_descriptor",
    gate: PAYMENTS_CORE_CP411_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP411_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_permission_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP411_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-411 cover exactly the 10 planned RP13 units from RP13.P07.M05.S13 through RP13.P07.M05.S22?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-411 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp411CloseoutHandoff() {
  return freezeCp411Validation({
    handoff_id: "CP00-411-to-CP00-412",
    from_pack_id: PAYMENTS_CORE_CP411_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP411_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP411_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP411_PACK_BINDING.range,
    open_scope: "Continue RP13.P07.M06.S01 onward with the remaining permission and audit binding rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP411_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp412Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP412_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP412_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp412CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp412Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp412Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-412 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP412_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-412");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP412_PACK_BINDING.risk_class) errors.push("CP00-412 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP412_PACK_BINDING.unit_count) errors.push("CP00-412 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP412_PACK_BINDING.first_unit_id) errors.push("CP00-412 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP412_PACK_BINDING.last_unit_id) errors.push("CP00-412 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-412 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-412 must only include RP13 units");
  const summary = createPaymentsCoreCp412CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP412_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-412 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP412_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-412 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP412_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-412 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP412_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-412 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP412_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-412 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-412 ${microId} missing row ${title}`);
    }
  }
  return freezeCp412Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp412P07CloseoutP08FoundationDescriptor(
  descriptor = createPaymentsCoreCp412P07CloseoutP08FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p07_closeout_p08_foundation_case_set ?? createPaymentsCoreCp412P07CloseoutP08FoundationCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp412P07CloseoutP08FoundationDescriptor") errors.push("CP00-412 descriptor type drift");
  if (descriptor.source_p07_permission_slice_descriptor !== "PaymentsCoreCp411P07PermissionSliceDescriptor") {
    errors.push("CP00-412 source p07 permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP412_REQUIREMENTS.required_section_rows).length) errors.push("CP00-412 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP412_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-412 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-412 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-412 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-412 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-412 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-412 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-412 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP412_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-412 must not promote Claude");
  if (PAYMENTS_CORE_CP412_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-412 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-412 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-412 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP412_PACK_BINDING.pack_id, PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-412 contract current_pack drift");
  }
  if (
    contractProjection?.p07_closeout_p08_foundation_descriptor?.descriptor &&
    contractProjection.p07_closeout_p08_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-412 contract p07_closeout_p08_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP412_PACK_BINDING.next_pack_id) errors.push("CP00-412 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP412_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-412 next subphase drift");
  }
  return freezeCp412Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp412HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp412P07CloseoutP08FoundationDescriptor(),
) {
  const coverage = validatePaymentsCoreCp412Coverage(planPack);
  const slice = validatePaymentsCoreCp412P07CloseoutP08FoundationDescriptor(descriptor, contractProjection);
  return freezeCp412Validation({
    evidence_packet: "H13.CP00-412.payments_core_p07_closeout_p08_foundation_descriptor",
    gate: PAYMENTS_CORE_CP412_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p07_closeout_p08_foundation_valid: slice.valid,
    no_real_data: true,
    source_p07_permission_slice_pack_id: PAYMENTS_CORE_CP412_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP412_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP412_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP412_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp412ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp412Coverage(planPack);
  const slice = validatePaymentsCoreCp412P07CloseoutP08FoundationDescriptor(createPaymentsCoreCp412P07CloseoutP08FoundationDescriptor(), {});
  return freezeCp412Validation({
    review_packet: "C13.CP00-412.payments_core_p07_closeout_p08_foundation_descriptor",
    gate: PAYMENTS_CORE_CP412_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP412_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p07_closeout_p08_foundation_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP412_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-412 cover exactly the 150 planned RP13 units from RP13.P07.M06.S01 through RP13.P08.M03.S15?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-412 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp412CloseoutHandoff() {
  return freezeCp412Validation({
    handoff_id: "CP00-412-to-CP00-413",
    from_pack_id: PAYMENTS_CORE_CP412_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP412_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP412_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP412_PACK_BINDING.range,
    open_scope: "Continue RP13.P08.M03.S16 onward with the remaining primary implementation rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP412_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp413Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP413_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP413_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp413CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp413Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp413Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-413 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP413_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-413");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP413_PACK_BINDING.risk_class) errors.push("CP00-413 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP413_PACK_BINDING.unit_count) errors.push("CP00-413 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP413_PACK_BINDING.first_unit_id) errors.push("CP00-413 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP413_PACK_BINDING.last_unit_id) errors.push("CP00-413 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-413 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-413 must only include RP13 units");
  const summary = createPaymentsCoreCp413CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP413_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-413 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP413_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-413 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP413_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-413 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP413_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-413 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP413_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-413 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-413 ${microId} missing row ${title}`);
    }
  }
  return freezeCp413Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp413P08ImplementationWorkflowSliceDescriptor(
  descriptor = createPaymentsCoreCp413P08ImplementationWorkflowSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_implementation_workflow_slice_case_set ?? createPaymentsCoreCp413P08ImplementationWorkflowSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp413P08ImplementationWorkflowSliceDescriptor") errors.push("CP00-413 descriptor type drift");
  if (descriptor.source_p07_closeout_p08_foundation_descriptor !== "PaymentsCoreCp412P07CloseoutP08FoundationDescriptor") {
    errors.push("CP00-413 source p07 closeout p08 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP413_REQUIREMENTS.required_section_rows).length) errors.push("CP00-413 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP413_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-413 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-413 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-413 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-413 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-413 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-413 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-413 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP413_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-413 must not promote Claude");
  if (PAYMENTS_CORE_CP413_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-413 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-413 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-413 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP413_PACK_BINDING.pack_id, PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-413 contract current_pack drift");
  }
  if (
    contractProjection?.p08_implementation_workflow_slice_descriptor?.descriptor &&
    contractProjection.p08_implementation_workflow_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-413 contract p08_implementation_workflow_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP413_PACK_BINDING.next_pack_id) errors.push("CP00-413 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP413_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-413 next subphase drift");
  }
  return freezeCp413Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp413HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp413P08ImplementationWorkflowSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp413Coverage(planPack);
  const slice = validatePaymentsCoreCp413P08ImplementationWorkflowSliceDescriptor(descriptor, contractProjection);
  return freezeCp413Validation({
    evidence_packet: "H13.CP00-413.payments_core_p08_implementation_workflow_slice_descriptor",
    gate: PAYMENTS_CORE_CP413_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_implementation_workflow_slice_valid: slice.valid,
    no_real_data: true,
    source_p07_closeout_p08_foundation_pack_id: PAYMENTS_CORE_CP413_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP413_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP413_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP413_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp413ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp413Coverage(planPack);
  const slice = validatePaymentsCoreCp413P08ImplementationWorkflowSliceDescriptor(createPaymentsCoreCp413P08ImplementationWorkflowSliceDescriptor(), {});
  return freezeCp413Validation({
    review_packet: "C13.CP00-413.payments_core_p08_implementation_workflow_slice_descriptor",
    gate: PAYMENTS_CORE_CP413_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP413_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_implementation_workflow_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP413_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-413 cover exactly the 40 planned RP13 units from RP13.P08.M03.S16 through RP13.P08.M05.S13?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-413 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp413CloseoutHandoff() {
  return freezeCp413Validation({
    handoff_id: "CP00-413-to-CP00-414",
    from_pack_id: PAYMENTS_CORE_CP413_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP413_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP413_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP413_PACK_BINDING.range,
    open_scope: "Continue RP13.P08.M05.S14 onward with the remaining permission and audit binding rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP413_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp414Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP414_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP414_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp414CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp414Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp414Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-414 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP414_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-414");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP414_PACK_BINDING.risk_class) errors.push("CP00-414 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP414_PACK_BINDING.unit_count) errors.push("CP00-414 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP414_PACK_BINDING.first_unit_id) errors.push("CP00-414 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP414_PACK_BINDING.last_unit_id) errors.push("CP00-414 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-414 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-414 must only include RP13 units");
  const summary = createPaymentsCoreCp414CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP414_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-414 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP414_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-414 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP414_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-414 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP414_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-414 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP414_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-414 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-414 ${microId} missing row ${title}`);
    }
  }
  return freezeCp414Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp414P08PermissionFixtureSliceDescriptor(
  descriptor = createPaymentsCoreCp414P08PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_permission_fixture_slice_case_set ?? createPaymentsCoreCp414P08PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp414P08PermissionFixtureSliceDescriptor") errors.push("CP00-414 descriptor type drift");
  if (descriptor.source_p08_implementation_workflow_slice_descriptor !== "PaymentsCoreCp413P08ImplementationWorkflowSliceDescriptor") {
    errors.push("CP00-414 source p08 implementation workflow slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP414_REQUIREMENTS.required_section_rows).length) errors.push("CP00-414 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP414_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-414 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-414 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-414 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-414 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-414 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-414 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-414 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP414_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-414 must not promote Claude");
  if (PAYMENTS_CORE_CP414_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-414 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-414 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-414 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP414_PACK_BINDING.pack_id, PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-414 contract current_pack drift");
  }
  if (
    contractProjection?.p08_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p08_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-414 contract p08_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP414_PACK_BINDING.next_pack_id) errors.push("CP00-414 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP414_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-414 next subphase drift");
  }
  return freezeCp414Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp414HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp414P08PermissionFixtureSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp414Coverage(planPack);
  const slice = validatePaymentsCoreCp414P08PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp414Validation({
    evidence_packet: "H13.CP00-414.payments_core_p08_permission_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP414_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p08_implementation_workflow_slice_pack_id: PAYMENTS_CORE_CP414_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP414_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP414_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP414_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp414ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp414Coverage(planPack);
  const slice = validatePaymentsCoreCp414P08PermissionFixtureSliceDescriptor(createPaymentsCoreCp414P08PermissionFixtureSliceDescriptor(), {});
  return freezeCp414Validation({
    review_packet: "C13.CP00-414.payments_core_p08_permission_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP414_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP414_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP414_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-414 cover exactly the 10 planned RP13 units from RP13.P08.M05.S14 through RP13.P08.M06.S01?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-414 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp414CloseoutHandoff() {
  return freezeCp414Validation({
    handoff_id: "CP00-414-to-CP00-415",
    from_pack_id: PAYMENTS_CORE_CP414_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP414_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP414_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP414_PACK_BINDING.range,
    open_scope: "Continue RP13.P08.M06.S02 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP414_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp415Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP415_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP415_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp415CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp415Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp415Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-415 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP415_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-415");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP415_PACK_BINDING.risk_class) errors.push("CP00-415 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP415_PACK_BINDING.unit_count) errors.push("CP00-415 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP415_PACK_BINDING.first_unit_id) errors.push("CP00-415 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP415_PACK_BINDING.last_unit_id) errors.push("CP00-415 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-415 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-415 must only include RP13 units");
  const summary = createPaymentsCoreCp415CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP415_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-415 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP415_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-415 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP415_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-415 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP415_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-415 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP415_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-415 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-415 ${microId} missing row ${title}`);
    }
  }
  return freezeCp415Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp415P08CloseoutP09FoundationDescriptor(
  descriptor = createPaymentsCoreCp415P08CloseoutP09FoundationDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p08_closeout_p09_foundation_case_set ?? createPaymentsCoreCp415P08CloseoutP09FoundationCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp415P08CloseoutP09FoundationDescriptor") errors.push("CP00-415 descriptor type drift");
  if (descriptor.source_p08_permission_fixture_slice_descriptor !== "PaymentsCoreCp414P08PermissionFixtureSliceDescriptor") {
    errors.push("CP00-415 source p08 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP415_REQUIREMENTS.required_section_rows).length) errors.push("CP00-415 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP415_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-415 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-415 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-415 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-415 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-415 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-415 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-415 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP415_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-415 must not promote Claude");
  if (PAYMENTS_CORE_CP415_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-415 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-415 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-415 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP415_PACK_BINDING.pack_id, PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-415 contract current_pack drift");
  }
  if (
    contractProjection?.p08_closeout_p09_foundation_descriptor?.descriptor &&
    contractProjection.p08_closeout_p09_foundation_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-415 contract p08_closeout_p09_foundation_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP415_PACK_BINDING.next_pack_id) errors.push("CP00-415 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP415_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-415 next subphase drift");
  }
  return freezeCp415Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp415HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp415P08CloseoutP09FoundationDescriptor(),
) {
  const coverage = validatePaymentsCoreCp415Coverage(planPack);
  const slice = validatePaymentsCoreCp415P08CloseoutP09FoundationDescriptor(descriptor, contractProjection);
  return freezeCp415Validation({
    evidence_packet: "H13.CP00-415.payments_core_p08_closeout_p09_foundation_descriptor",
    gate: PAYMENTS_CORE_CP415_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p08_closeout_p09_foundation_valid: slice.valid,
    no_real_data: true,
    source_p08_permission_fixture_slice_pack_id: PAYMENTS_CORE_CP415_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP415_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP415_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP415_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp415ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp415Coverage(planPack);
  const slice = validatePaymentsCoreCp415P08CloseoutP09FoundationDescriptor(createPaymentsCoreCp415P08CloseoutP09FoundationDescriptor(), {});
  return freezeCp415Validation({
    review_packet: "C13.CP00-415.payments_core_p08_closeout_p09_foundation_descriptor",
    gate: PAYMENTS_CORE_CP415_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP415_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p08_closeout_p09_foundation_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP415_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-415 cover exactly the 150 planned RP13 units from RP13.P08.M06.S02 through RP13.P09.M05.S01?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-415 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp415CloseoutHandoff() {
  return freezeCp415Validation({
    handoff_id: "CP00-415-to-CP00-416",
    from_pack_id: PAYMENTS_CORE_CP415_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP415_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP415_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP415_PACK_BINDING.range,
    open_scope: "Continue RP13.P09.M05.S02 onward with the remaining permission and audit binding rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP415_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp416Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP416_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP416_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp416CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp416Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp416Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-416 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP416_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-416");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP416_PACK_BINDING.risk_class) errors.push("CP00-416 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP416_PACK_BINDING.unit_count) errors.push("CP00-416 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP416_PACK_BINDING.first_unit_id) errors.push("CP00-416 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP416_PACK_BINDING.last_unit_id) errors.push("CP00-416 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-416 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-416 must only include RP13 units");
  const summary = createPaymentsCoreCp416CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP416_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-416 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP416_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-416 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP416_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-416 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP416_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-416 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP416_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-416 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-416 ${microId} missing row ${title}`);
    }
  }
  return freezeCp416Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp416P09PermissionSliceDescriptor(
  descriptor = createPaymentsCoreCp416P09PermissionSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_permission_slice_case_set ?? createPaymentsCoreCp416P09PermissionSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp416P09PermissionSliceDescriptor") errors.push("CP00-416 descriptor type drift");
  if (descriptor.source_p08_closeout_p09_foundation_descriptor !== "PaymentsCoreCp415P08CloseoutP09FoundationDescriptor") {
    errors.push("CP00-416 source p08 closeout p09 foundation descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP416_REQUIREMENTS.required_section_rows).length) errors.push("CP00-416 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP416_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-416 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-416 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-416 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-416 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-416 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-416 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-416 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP416_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-416 must not promote Claude");
  if (PAYMENTS_CORE_CP416_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-416 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-416 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-416 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP416_PACK_BINDING.pack_id, PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-416 contract current_pack drift");
  }
  if (
    contractProjection?.p09_permission_slice_descriptor?.descriptor &&
    contractProjection.p09_permission_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-416 contract p09_permission_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP416_PACK_BINDING.next_pack_id) errors.push("CP00-416 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP416_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-416 next subphase drift");
  }
  return freezeCp416Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp416HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp416P09PermissionSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp416Coverage(planPack);
  const slice = validatePaymentsCoreCp416P09PermissionSliceDescriptor(descriptor, contractProjection);
  return freezeCp416Validation({
    evidence_packet: "H13.CP00-416.payments_core_p09_permission_slice_descriptor",
    gate: PAYMENTS_CORE_CP416_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_permission_slice_valid: slice.valid,
    no_real_data: true,
    source_p08_closeout_p09_foundation_pack_id: PAYMENTS_CORE_CP416_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP416_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP416_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP416_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp416ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp416Coverage(planPack);
  const slice = validatePaymentsCoreCp416P09PermissionSliceDescriptor(createPaymentsCoreCp416P09PermissionSliceDescriptor(), {});
  return freezeCp416Validation({
    review_packet: "C13.CP00-416.payments_core_p09_permission_slice_descriptor",
    gate: PAYMENTS_CORE_CP416_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP416_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_permission_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP416_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-416 cover exactly the 10 planned RP13 units from RP13.P09.M05.S02 through RP13.P09.M05.S11?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-416 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp416CloseoutHandoff() {
  return freezeCp416Validation({
    handoff_id: "CP00-416-to-CP00-417",
    from_pack_id: PAYMENTS_CORE_CP416_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP416_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP416_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP416_PACK_BINDING.range,
    open_scope: "Continue RP13.P09.M05.S12 onward with the remaining permission and audit binding rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP416_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp417Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP417_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP417_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp417CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp417Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp417Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-417 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP417_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-417");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP417_PACK_BINDING.risk_class) errors.push("CP00-417 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP417_PACK_BINDING.unit_count) errors.push("CP00-417 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP417_PACK_BINDING.first_unit_id) errors.push("CP00-417 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP417_PACK_BINDING.last_unit_id) errors.push("CP00-417 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-417 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-417 must only include RP13 units");
  const summary = createPaymentsCoreCp417CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP417_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-417 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP417_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-417 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP417_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-417 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP417_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-417 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP417_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-417 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-417 ${microId} missing row ${title}`);
    }
  }
  return freezeCp417Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp417P09PermissionFixtureSliceDescriptor(
  descriptor = createPaymentsCoreCp417P09PermissionFixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_permission_fixture_slice_case_set ?? createPaymentsCoreCp417P09PermissionFixtureSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp417P09PermissionFixtureSliceDescriptor") errors.push("CP00-417 descriptor type drift");
  if (descriptor.source_p09_permission_slice_descriptor !== "PaymentsCoreCp416P09PermissionSliceDescriptor") {
    errors.push("CP00-417 source p09 permission slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP417_REQUIREMENTS.required_section_rows).length) errors.push("CP00-417 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP417_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-417 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-417 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-417 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-417 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-417 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-417 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-417 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP417_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-417 must not promote Claude");
  if (PAYMENTS_CORE_CP417_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-417 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-417 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-417 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP417_PACK_BINDING.pack_id, PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-417 contract current_pack drift");
  }
  if (
    contractProjection?.p09_permission_fixture_slice_descriptor?.descriptor &&
    contractProjection.p09_permission_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-417 contract p09_permission_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP417_PACK_BINDING.next_pack_id) errors.push("CP00-417 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP417_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-417 next subphase drift");
  }
  return freezeCp417Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp417HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp417P09PermissionFixtureSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp417Coverage(planPack);
  const slice = validatePaymentsCoreCp417P09PermissionFixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp417Validation({
    evidence_packet: "H13.CP00-417.payments_core_p09_permission_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP417_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_permission_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_permission_slice_pack_id: PAYMENTS_CORE_CP417_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP417_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP417_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP417_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp417ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp417Coverage(planPack);
  const slice = validatePaymentsCoreCp417P09PermissionFixtureSliceDescriptor(createPaymentsCoreCp417P09PermissionFixtureSliceDescriptor(), {});
  return freezeCp417Validation({
    review_packet: "C13.CP00-417.payments_core_p09_permission_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP417_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP417_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_permission_fixture_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP417_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-417 cover exactly the 10 planned RP13 units from RP13.P09.M05.S12 through RP13.P09.M06.S01?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-417 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp417CloseoutHandoff() {
  return freezeCp417Validation({
    handoff_id: "CP00-417-to-CP00-418",
    from_pack_id: PAYMENTS_CORE_CP417_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP417_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP417_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP417_PACK_BINDING.range,
    open_scope: "Continue RP13.P09.M06.S02 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP417_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp418Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP418_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP418_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp418CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp418Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp418Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-418 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP418_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-418");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP418_PACK_BINDING.risk_class) errors.push("CP00-418 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP418_PACK_BINDING.unit_count) errors.push("CP00-418 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP418_PACK_BINDING.first_unit_id) errors.push("CP00-418 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP418_PACK_BINDING.last_unit_id) errors.push("CP00-418 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-418 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-418 must only include RP13 units");
  const summary = createPaymentsCoreCp418CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP418_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-418 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP418_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-418 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP418_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-418 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP418_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-418 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP418_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-418 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-418 ${microId} missing row ${title}`);
    }
  }
  return freezeCp418Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp418P09FixtureSliceDescriptor(
  descriptor = createPaymentsCoreCp418P09FixtureSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_fixture_slice_case_set ?? createPaymentsCoreCp418P09FixtureSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp418P09FixtureSliceDescriptor") errors.push("CP00-418 descriptor type drift");
  if (descriptor.source_p09_permission_fixture_slice_descriptor !== "PaymentsCoreCp417P09PermissionFixtureSliceDescriptor") {
    errors.push("CP00-418 source p09 permission fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP418_REQUIREMENTS.required_section_rows).length) errors.push("CP00-418 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP418_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-418 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-418 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-418 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-418 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-418 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-418 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-418 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP418_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-418 must not promote Claude");
  if (PAYMENTS_CORE_CP418_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-418 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-418 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-418 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP418_PACK_BINDING.pack_id, PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-418 contract current_pack drift");
  }
  if (
    contractProjection?.p09_fixture_slice_descriptor?.descriptor &&
    contractProjection.p09_fixture_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-418 contract p09_fixture_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP418_PACK_BINDING.next_pack_id) errors.push("CP00-418 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP418_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-418 next subphase drift");
  }
  return freezeCp418Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp418HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp418P09FixtureSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp418Coverage(planPack);
  const slice = validatePaymentsCoreCp418P09FixtureSliceDescriptor(descriptor, contractProjection);
  return freezeCp418Validation({
    evidence_packet: "H13.CP00-418.payments_core_p09_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP418_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_fixture_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_permission_fixture_slice_pack_id: PAYMENTS_CORE_CP418_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP418_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP418_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP418_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp418ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp418Coverage(planPack);
  const slice = validatePaymentsCoreCp418P09FixtureSliceDescriptor(createPaymentsCoreCp418P09FixtureSliceDescriptor(), {});
  return freezeCp418Validation({
    review_packet: "C13.CP00-418.payments_core_p09_fixture_slice_descriptor",
    gate: PAYMENTS_CORE_CP418_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP418_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_fixture_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP418_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-418 cover exactly the 10 planned RP13 units from RP13.P09.M06.S02 through RP13.P09.M06.S11?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-418 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp418CloseoutHandoff() {
  return freezeCp418Validation({
    handoff_id: "CP00-418-to-CP00-419",
    from_pack_id: PAYMENTS_CORE_CP418_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP418_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP418_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP418_PACK_BINDING.range,
    open_scope: "Continue RP13.P09.M06.S12 onward with the remaining synthetic fixture rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP418_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp419Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP419_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP419_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp419CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp419Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp419Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-419 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP419_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-419");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP419_PACK_BINDING.risk_class) errors.push("CP00-419 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP419_PACK_BINDING.unit_count) errors.push("CP00-419 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP419_PACK_BINDING.first_unit_id) errors.push("CP00-419 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP419_PACK_BINDING.last_unit_id) errors.push("CP00-419 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-419 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-419 must only include RP13 units");
  const summary = createPaymentsCoreCp419CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP419_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-419 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP419_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-419 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP419_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-419 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP419_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-419 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP419_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-419 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-419 ${microId} missing row ${title}`);
    }
  }
  return freezeCp419Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp419P09FixtureTestSliceDescriptor(
  descriptor = createPaymentsCoreCp419P09FixtureTestSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_fixture_test_slice_case_set ?? createPaymentsCoreCp419P09FixtureTestSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp419P09FixtureTestSliceDescriptor") errors.push("CP00-419 descriptor type drift");
  if (descriptor.source_p09_fixture_slice_descriptor !== "PaymentsCoreCp418P09FixtureSliceDescriptor") {
    errors.push("CP00-419 source p09 fixture slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP419_REQUIREMENTS.required_section_rows).length) errors.push("CP00-419 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP419_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-419 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-419 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-419 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-419 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-419 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-419 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-419 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP419_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-419 must not promote Claude");
  if (PAYMENTS_CORE_CP419_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-419 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-419 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-419 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP419_PACK_BINDING.pack_id, PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-419 contract current_pack drift");
  }
  if (
    contractProjection?.p09_fixture_test_slice_descriptor?.descriptor &&
    contractProjection.p09_fixture_test_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-419 contract p09_fixture_test_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP419_PACK_BINDING.next_pack_id) errors.push("CP00-419 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP419_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-419 next subphase drift");
  }
  return freezeCp419Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp419HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp419P09FixtureTestSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp419Coverage(planPack);
  const slice = validatePaymentsCoreCp419P09FixtureTestSliceDescriptor(descriptor, contractProjection);
  return freezeCp419Validation({
    evidence_packet: "H13.CP00-419.payments_core_p09_fixture_test_slice_descriptor",
    gate: PAYMENTS_CORE_CP419_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_fixture_test_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_fixture_slice_pack_id: PAYMENTS_CORE_CP419_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP419_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP419_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP419_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp419ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp419Coverage(planPack);
  const slice = validatePaymentsCoreCp419P09FixtureTestSliceDescriptor(createPaymentsCoreCp419P09FixtureTestSliceDescriptor(), {});
  return freezeCp419Validation({
    review_packet: "C13.CP00-419.payments_core_p09_fixture_test_slice_descriptor",
    gate: PAYMENTS_CORE_CP419_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP419_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_fixture_test_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP419_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-419 cover exactly the 10 planned RP13 units from RP13.P09.M06.S12 through RP13.P09.M07.S01?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-419 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp419CloseoutHandoff() {
  return freezeCp419Validation({
    handoff_id: "CP00-419-to-CP00-420",
    from_pack_id: PAYMENTS_CORE_CP419_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP419_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP419_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP419_PACK_BINDING.range,
    open_scope: "Continue RP13.P09.M07.S02 onward with the remaining test and golden case rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP419_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp420Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP420_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP420_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp420CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp420Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp420Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-420 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP420_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-420");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP420_PACK_BINDING.risk_class) errors.push("CP00-420 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP420_PACK_BINDING.unit_count) errors.push("CP00-420 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP420_PACK_BINDING.first_unit_id) errors.push("CP00-420 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP420_PACK_BINDING.last_unit_id) errors.push("CP00-420 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-420 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-420 must only include RP13 units");
  const summary = createPaymentsCoreCp420CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP420_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-420 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP420_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-420 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP420_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-420 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP420_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-420 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP420_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-420 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-420 ${microId} missing row ${title}`);
    }
  }
  return freezeCp420Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp420P09TestSliceDescriptor(
  descriptor = createPaymentsCoreCp420P09TestSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_test_slice_case_set ?? createPaymentsCoreCp420P09TestSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp420P09TestSliceDescriptor") errors.push("CP00-420 descriptor type drift");
  if (descriptor.source_p09_fixture_test_slice_descriptor !== "PaymentsCoreCp419P09FixtureTestSliceDescriptor") {
    errors.push("CP00-420 source p09 fixture test slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP420_REQUIREMENTS.required_section_rows).length) errors.push("CP00-420 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP420_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-420 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-420 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-420 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-420 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-420 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-420 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-420 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP420_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-420 must not promote Claude");
  if (PAYMENTS_CORE_CP420_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-420 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-420 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-420 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP420_PACK_BINDING.pack_id, PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-420 contract current_pack drift");
  }
  if (
    contractProjection?.p09_test_slice_descriptor?.descriptor &&
    contractProjection.p09_test_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-420 contract p09_test_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP420_PACK_BINDING.next_pack_id) errors.push("CP00-420 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP420_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-420 next subphase drift");
  }
  return freezeCp420Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp420HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp420P09TestSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp420Coverage(planPack);
  const slice = validatePaymentsCoreCp420P09TestSliceDescriptor(descriptor, contractProjection);
  return freezeCp420Validation({
    evidence_packet: "H13.CP00-420.payments_core_p09_test_slice_descriptor",
    gate: PAYMENTS_CORE_CP420_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_test_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_fixture_test_slice_pack_id: PAYMENTS_CORE_CP420_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP420_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP420_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP420_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp420ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp420Coverage(planPack);
  const slice = validatePaymentsCoreCp420P09TestSliceDescriptor(createPaymentsCoreCp420P09TestSliceDescriptor(), {});
  return freezeCp420Validation({
    review_packet: "C13.CP00-420.payments_core_p09_test_slice_descriptor",
    gate: PAYMENTS_CORE_CP420_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP420_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_test_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP420_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-420 cover exactly the 10 planned RP13 units from RP13.P09.M07.S02 through RP13.P09.M07.S11?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-420 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp420CloseoutHandoff() {
  return freezeCp420Validation({
    handoff_id: "CP00-420-to-CP00-421",
    from_pack_id: PAYMENTS_CORE_CP420_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP420_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP420_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP420_PACK_BINDING.range,
    open_scope: "Continue RP13.P09.M07.S12 onward with the remaining test and golden case rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP420_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp421Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP421_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP421_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp421CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp421Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp421Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-421 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP421_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-421");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP421_PACK_BINDING.risk_class) errors.push("CP00-421 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP421_PACK_BINDING.unit_count) errors.push("CP00-421 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP421_PACK_BINDING.first_unit_id) errors.push("CP00-421 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP421_PACK_BINDING.last_unit_id) errors.push("CP00-421 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-421 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-421 must only include RP13 units");
  const summary = createPaymentsCoreCp421CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP421_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-421 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP421_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-421 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP421_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-421 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP421_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-421 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP421_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-421 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-421 ${microId} missing row ${title}`);
    }
  }
  return freezeCp421Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp421P09TestHermesSliceDescriptor(
  descriptor = createPaymentsCoreCp421P09TestHermesSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_test_hermes_slice_case_set ?? createPaymentsCoreCp421P09TestHermesSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp421P09TestHermesSliceDescriptor") errors.push("CP00-421 descriptor type drift");
  if (descriptor.source_p09_test_slice_descriptor !== "PaymentsCoreCp420P09TestSliceDescriptor") {
    errors.push("CP00-421 source p09 test slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP421_REQUIREMENTS.required_section_rows).length) errors.push("CP00-421 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP421_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-421 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-421 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-421 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-421 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-421 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-421 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-421 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP421_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-421 must not promote Claude");
  if (PAYMENTS_CORE_CP421_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-421 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-421 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-421 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP421_PACK_BINDING.pack_id, PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-421 contract current_pack drift");
  }
  if (
    contractProjection?.p09_test_hermes_slice_descriptor?.descriptor &&
    contractProjection.p09_test_hermes_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-421 contract p09_test_hermes_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP421_PACK_BINDING.next_pack_id) errors.push("CP00-421 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP421_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-421 next subphase drift");
  }
  return freezeCp421Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp421HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp421P09TestHermesSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp421Coverage(planPack);
  const slice = validatePaymentsCoreCp421P09TestHermesSliceDescriptor(descriptor, contractProjection);
  return freezeCp421Validation({
    evidence_packet: "H13.CP00-421.payments_core_p09_test_hermes_slice_descriptor",
    gate: PAYMENTS_CORE_CP421_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_test_hermes_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_test_slice_pack_id: PAYMENTS_CORE_CP421_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP421_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP421_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP421_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp421ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp421Coverage(planPack);
  const slice = validatePaymentsCoreCp421P09TestHermesSliceDescriptor(createPaymentsCoreCp421P09TestHermesSliceDescriptor(), {});
  return freezeCp421Validation({
    review_packet: "C13.CP00-421.payments_core_p09_test_hermes_slice_descriptor",
    gate: PAYMENTS_CORE_CP421_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP421_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_test_hermes_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP421_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-421 cover exactly the 10 planned RP13 units from RP13.P09.M07.S12 through RP13.P09.M08.S01?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-421 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp421CloseoutHandoff() {
  return freezeCp421Validation({
    handoff_id: "CP00-421-to-CP00-422",
    from_pack_id: PAYMENTS_CORE_CP421_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP421_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP421_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP421_PACK_BINDING.range,
    open_scope: "Continue RP13.P09.M08.S02 onward with the remaining Hermes evidence rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP421_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp422Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP422_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP422_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp422CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp422Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp422Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-422 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP422_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-422");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP422_PACK_BINDING.risk_class) errors.push("CP00-422 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP422_PACK_BINDING.unit_count) errors.push("CP00-422 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP422_PACK_BINDING.first_unit_id) errors.push("CP00-422 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP422_PACK_BINDING.last_unit_id) errors.push("CP00-422 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-422 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-422 must only include RP13 units");
  const summary = createPaymentsCoreCp422CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP422_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-422 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP422_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-422 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP422_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-422 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP422_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-422 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP422_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-422 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-422 ${microId} missing row ${title}`);
    }
  }
  return freezeCp422Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp422P09HermesSliceDescriptor(
  descriptor = createPaymentsCoreCp422P09HermesSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_hermes_slice_case_set ?? createPaymentsCoreCp422P09HermesSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp422P09HermesSliceDescriptor") errors.push("CP00-422 descriptor type drift");
  if (descriptor.source_p09_test_hermes_slice_descriptor !== "PaymentsCoreCp421P09TestHermesSliceDescriptor") {
    errors.push("CP00-422 source p09 test hermes slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP422_REQUIREMENTS.required_section_rows).length) errors.push("CP00-422 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP422_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-422 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-422 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-422 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-422 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-422 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-422 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-422 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP422_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-422 must not promote Claude");
  if (PAYMENTS_CORE_CP422_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-422 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-422 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-422 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP422_PACK_BINDING.pack_id, PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-422 contract current_pack drift");
  }
  if (
    contractProjection?.p09_hermes_slice_descriptor?.descriptor &&
    contractProjection.p09_hermes_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-422 contract p09_hermes_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP422_PACK_BINDING.next_pack_id) errors.push("CP00-422 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP422_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-422 next subphase drift");
  }
  return freezeCp422Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp422HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp422P09HermesSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp422Coverage(planPack);
  const slice = validatePaymentsCoreCp422P09HermesSliceDescriptor(descriptor, contractProjection);
  return freezeCp422Validation({
    evidence_packet: "H13.CP00-422.payments_core_p09_hermes_slice_descriptor",
    gate: PAYMENTS_CORE_CP422_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_hermes_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_test_hermes_slice_pack_id: PAYMENTS_CORE_CP422_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP422_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP422_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP422_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp422ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp422Coverage(planPack);
  const slice = validatePaymentsCoreCp422P09HermesSliceDescriptor(createPaymentsCoreCp422P09HermesSliceDescriptor(), {});
  return freezeCp422Validation({
    review_packet: "C13.CP00-422.payments_core_p09_hermes_slice_descriptor",
    gate: PAYMENTS_CORE_CP422_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP422_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_hermes_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP422_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-422 cover exactly the 10 planned RP13 units from RP13.P09.M08.S02 through RP13.P09.M08.S11?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-422 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp422CloseoutHandoff() {
  return freezeCp422Validation({
    handoff_id: "CP00-422-to-CP00-423",
    from_pack_id: PAYMENTS_CORE_CP422_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP422_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP422_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP422_PACK_BINDING.range,
    open_scope: "Continue RP13.P09.M08.S12 onward with the remaining Hermes evidence rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP422_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp423Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP423_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP423_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp423CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp423Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp423Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-423 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP423_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-423");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP423_PACK_BINDING.risk_class) errors.push("CP00-423 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP423_PACK_BINDING.unit_count) errors.push("CP00-423 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP423_PACK_BINDING.first_unit_id) errors.push("CP00-423 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP423_PACK_BINDING.last_unit_id) errors.push("CP00-423 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-423 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-423 must only include RP13 units");
  const summary = createPaymentsCoreCp423CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP423_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-423 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP423_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-423 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP423_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-423 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP423_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-423 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP423_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-423 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-423 ${microId} missing row ${title}`);
    }
  }
  return freezeCp423Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp423P09HermesReviewSliceDescriptor(
  descriptor = createPaymentsCoreCp423P09HermesReviewSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_hermes_review_slice_case_set ?? createPaymentsCoreCp423P09HermesReviewSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp423P09HermesReviewSliceDescriptor") errors.push("CP00-423 descriptor type drift");
  if (descriptor.source_p09_hermes_slice_descriptor !== "PaymentsCoreCp422P09HermesSliceDescriptor") {
    errors.push("CP00-423 source p09 hermes slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP423_REQUIREMENTS.required_section_rows).length) errors.push("CP00-423 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP423_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-423 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-423 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-423 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-423 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-423 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-423 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-423 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP423_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-423 must not promote Claude");
  if (PAYMENTS_CORE_CP423_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-423 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-423 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-423 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP423_PACK_BINDING.pack_id, PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-423 contract current_pack drift");
  }
  if (
    contractProjection?.p09_hermes_review_slice_descriptor?.descriptor &&
    contractProjection.p09_hermes_review_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-423 contract p09_hermes_review_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP423_PACK_BINDING.next_pack_id) errors.push("CP00-423 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP423_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-423 next subphase drift");
  }
  return freezeCp423Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp423HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp423P09HermesReviewSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp423Coverage(planPack);
  const slice = validatePaymentsCoreCp423P09HermesReviewSliceDescriptor(descriptor, contractProjection);
  return freezeCp423Validation({
    evidence_packet: "H13.CP00-423.payments_core_p09_hermes_review_slice_descriptor",
    gate: PAYMENTS_CORE_CP423_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_hermes_review_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_hermes_slice_pack_id: PAYMENTS_CORE_CP423_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP423_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP423_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP423_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp423ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp423Coverage(planPack);
  const slice = validatePaymentsCoreCp423P09HermesReviewSliceDescriptor(createPaymentsCoreCp423P09HermesReviewSliceDescriptor(), {});
  return freezeCp423Validation({
    review_packet: "C13.CP00-423.payments_core_p09_hermes_review_slice_descriptor",
    gate: PAYMENTS_CORE_CP423_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP423_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_hermes_review_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP423_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-423 cover exactly the 10 planned RP13 units from RP13.P09.M08.S12 through RP13.P09.M09.S01?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-423 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp423CloseoutHandoff() {
  return freezeCp423Validation({
    handoff_id: "CP00-423-to-CP00-424",
    from_pack_id: PAYMENTS_CORE_CP423_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP423_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP423_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP423_PACK_BINDING.range,
    open_scope: "Continue RP13.P09.M09.S02 onward with the remaining Claude review rows and downstream micros while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP423_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp424Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP424_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP424_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp424CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp424Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp424Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-424 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP424_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-424");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP424_PACK_BINDING.risk_class) errors.push("CP00-424 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP424_PACK_BINDING.unit_count) errors.push("CP00-424 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP424_PACK_BINDING.first_unit_id) errors.push("CP00-424 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP424_PACK_BINDING.last_unit_id) errors.push("CP00-424 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-424 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-424 must only include RP13 units");
  const summary = createPaymentsCoreCp424CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP424_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-424 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP424_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-424 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP424_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-424 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP424_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-424 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP424_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-424 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-424 ${microId} missing row ${title}`);
    }
  }
  return freezeCp424Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp424P09ReviewCloseoutSliceDescriptor(
  descriptor = createPaymentsCoreCp424P09ReviewCloseoutSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_review_closeout_slice_case_set ?? createPaymentsCoreCp424P09ReviewCloseoutSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp424P09ReviewCloseoutSliceDescriptor") errors.push("CP00-424 descriptor type drift");
  if (descriptor.source_p09_hermes_review_slice_descriptor !== "PaymentsCoreCp423P09HermesReviewSliceDescriptor") {
    errors.push("CP00-424 source p09 hermes review slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP424_REQUIREMENTS.required_section_rows).length) errors.push("CP00-424 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP424_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-424 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-424 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-424 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-424 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-424 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-424 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-424 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP424_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-424 must not promote Claude");
  if (PAYMENTS_CORE_CP424_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-424 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-424 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-424 local validation trust boundary drift");
  }
  if (
    contractProjection?.current_pack?.pack_id &&
    ![PAYMENTS_CORE_CP424_PACK_BINDING.pack_id, PAYMENTS_CORE_CP425_PACK_BINDING.pack_id].includes(contractProjection.current_pack.pack_id)
  ) {
    errors.push("CP00-424 contract current_pack drift");
  }
  if (
    contractProjection?.p09_review_closeout_slice_descriptor?.descriptor &&
    contractProjection.p09_review_closeout_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-424 contract p09_review_closeout_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP424_PACK_BINDING.next_pack_id) errors.push("CP00-424 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP424_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-424 next subphase drift");
  }
  return freezeCp424Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp424HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp424P09ReviewCloseoutSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp424Coverage(planPack);
  const slice = validatePaymentsCoreCp424P09ReviewCloseoutSliceDescriptor(descriptor, contractProjection);
  return freezeCp424Validation({
    evidence_packet: "H13.CP00-424.payments_core_p09_review_closeout_slice_descriptor",
    gate: PAYMENTS_CORE_CP424_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_review_closeout_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_hermes_review_slice_pack_id: PAYMENTS_CORE_CP424_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP424_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP424_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP424_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp424ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp424Coverage(planPack);
  const slice = validatePaymentsCoreCp424P09ReviewCloseoutSliceDescriptor(createPaymentsCoreCp424P09ReviewCloseoutSliceDescriptor(), {});
  return freezeCp424Validation({
    review_packet: "C13.CP00-424.payments_core_p09_review_closeout_slice_descriptor",
    gate: PAYMENTS_CORE_CP424_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP424_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_review_closeout_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP424_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-424 cover exactly the 10 planned RP13 units from RP13.P09.M09.S02 through RP13.P09.M10.S03?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-424 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp424CloseoutHandoff() {
  return freezeCp424Validation({
    handoff_id: "CP00-424-to-CP00-425",
    from_pack_id: PAYMENTS_CORE_CP424_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP424_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP424_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP424_PACK_BINDING.range,
    open_scope: "Continue RP13.P09.M10.S04 onward with the remaining closeout handoff rows while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP424_PACK_BINDING.production_ready_flag,
  });
}

function freezeCp425Validation(result) {
  return Object.freeze({
    ...result,
    pack_id: PAYMENTS_CORE_CP425_PACK_BINDING.pack_id,
    no_write_attestation: PAYMENTS_CORE_CP425_NO_WRITE_ATTESTATION,
  });
}

export function createPaymentsCoreCp425CoverageSummary(planPack) {
  const units = planPack?.included_units ?? [];
  return freezeCp425Validation({
    first_unit_id: units.at(0)?.id,
    last_unit_id: units.at(-1)?.id,
    unit_count: units.length,
    by_deliverable: countBy(units, "deliverable_type"),
    by_phase: countBy(units, "phase_id"),
    by_micro_phase: countBy(units, "source_micro_phase_id"),
    by_micro_title: countBy(units, "micro_title"),
    unit_titles: Object.freeze([...new Set(units.map((unit) => unit.title))]),
  });
}

export function validatePaymentsCoreCp425Coverage(planPack) {
  const errors = [];
  if (!planPack) errors.push("CP00-425 plan pack is required");
  if (planPack?.pack_id !== PAYMENTS_CORE_CP425_PACK_BINDING.pack_id) errors.push("plan pack must be CP00-425");
  if (planPack?.risk_class !== PAYMENTS_CORE_CP425_PACK_BINDING.risk_class) errors.push("CP00-425 risk class drift");
  if (planPack?.unit_count !== PAYMENTS_CORE_CP425_PACK_BINDING.unit_count) errors.push("CP00-425 unit count drift");
  const unitIds = (planPack?.included_units ?? []).map((unit) => unit.id);
  if (unitIds[0] !== PAYMENTS_CORE_CP425_PACK_BINDING.first_unit_id) errors.push("CP00-425 first unit drift");
  if (unitIds.at(-1) !== PAYMENTS_CORE_CP425_PACK_BINDING.last_unit_id) errors.push("CP00-425 last unit drift");
  if (new Set(unitIds).size !== unitIds.length) errors.push("CP00-425 duplicate unit IDs");
  if ((planPack?.included_units ?? []).some((unit) => unit.program_id !== "RP13")) errors.push("CP00-425 must only include RP13 units");
  const summary = createPaymentsCoreCp425CoverageSummary(planPack);
  for (const [deliverable, count] of Object.entries(PAYMENTS_CORE_CP425_REQUIREMENTS.deliverable_counts)) {
    if (summary.by_deliverable[deliverable] !== count) errors.push(`CP00-425 ${deliverable} distribution drift`);
  }
  for (const [phase, count] of Object.entries(PAYMENTS_CORE_CP425_REQUIREMENTS.phase_counts)) {
    if (summary.by_phase[phase] !== count) errors.push(`CP00-425 ${phase} distribution drift`);
  }
  for (const [microPhase, count] of Object.entries(PAYMENTS_CORE_CP425_REQUIREMENTS.micro_phase_row_counts)) {
    if (summary.by_micro_phase[microPhase] !== count) errors.push(`CP00-425 ${microPhase} distribution drift`);
  }
  for (const [microTitle, count] of Object.entries(PAYMENTS_CORE_CP425_REQUIREMENTS.micro_title_row_counts)) {
    if (summary.by_micro_title[microTitle] !== count) errors.push(`CP00-425 ${microTitle} distribution drift`);
  }
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP425_REQUIREMENTS.required_section_rows)) {
    const sectionUnits = (planPack?.included_units ?? []).filter((unit) => unit.source_micro_phase_id === microId);
    if (sectionUnits.length !== titles.length) errors.push(`CP00-425 ${microId} row count drift`);
    const sectionTitles = sectionUnits.map((unit) => unit.title);
    for (const title of titles) {
      if (!sectionTitles.includes(title)) errors.push(`CP00-425 ${microId} missing row ${title}`);
    }
  }
  return freezeCp425Validation({ valid: errors.length === 0, errors: Object.freeze(errors), summary });
}

export function validatePaymentsCoreCp425P09CloseoutHandoffSliceDescriptor(
  descriptor = createPaymentsCoreCp425P09CloseoutHandoffSliceDescriptor(),
  contractProjection = {},
) {
  const errors = [];
  const caseSet = descriptor.p09_closeout_handoff_slice_case_set ?? createPaymentsCoreCp425P09CloseoutHandoffSliceCaseSet();
  if (descriptor.descriptor !== "PaymentsCoreCp425P09CloseoutHandoffSliceDescriptor") errors.push("CP00-425 descriptor type drift");
  if (descriptor.source_p09_review_closeout_slice_descriptor !== "PaymentsCoreCp424P09ReviewCloseoutSliceDescriptor") {
    errors.push("CP00-425 source p09 review closeout slice descriptor drift");
  }
  const sections = caseSet.sections ?? {};
  if (caseSet.section_count !== Object.keys(PAYMENTS_CORE_CP425_REQUIREMENTS.required_section_rows).length) errors.push("CP00-425 section count drift");
  for (const [microId, titles] of Object.entries(PAYMENTS_CORE_CP425_REQUIREMENTS.required_section_rows)) {
    const section = sections[microId];
    if (!section) {
      errors.push(`CP00-425 missing section ${microId}`);
      continue;
    }
    if (section.row_count !== titles.length) errors.push(`CP00-425 ${microId} row count drift`);
    for (const title of titles) {
      const key = paymentsCoreRowKey(title);
      const row = section.rows?.[key];
      if (!row) {
        errors.push(`CP00-425 ${microId} missing row ${key}`);
        continue;
      }
      if (row.descriptor_only !== true) errors.push(`CP00-425 ${microId} ${key} must be descriptor-only`);
      if (row.runtime_execution !== false) errors.push(`CP00-425 ${microId} ${key} must not execute runtime`);
      if (row.customer_safe_errors_only !== true) errors.push(`CP00-425 ${microId} ${key} must be customer-safe`);
      if (row.raw_payload_included !== false) errors.push(`CP00-425 ${microId} ${key} must not include raw payload`);
    }
  }
  if (PAYMENTS_CORE_CP425_NO_WRITE_ATTESTATION.promotes_claude_to_final_approval !== false) errors.push("CP00-425 must not promote Claude");
  if (PAYMENTS_CORE_CP425_NO_WRITE_ATTESTATION.claims_enterprise_trust_from_local_validation !== false) {
    errors.push("CP00-425 must not claim enterprise trust");
  }
  if (descriptor.authority_boundary?.claude_is_final_approval !== false) errors.push("CP00-425 Claude authority boundary drift");
  if (descriptor.authority_boundary?.local_validation_claims_enterprise_trust !== false) {
    errors.push("CP00-425 local validation trust boundary drift");
  }
  if (contractProjection?.current_pack?.pack_id && contractProjection.current_pack.pack_id !== PAYMENTS_CORE_CP425_PACK_BINDING.pack_id) {
    errors.push("CP00-425 contract current_pack drift");
  }
  if (
    contractProjection?.p09_closeout_handoff_slice_descriptor?.descriptor &&
    contractProjection.p09_closeout_handoff_slice_descriptor.descriptor !== descriptor.descriptor
  ) {
    errors.push("CP00-425 contract p09_closeout_handoff_slice_descriptor drift");
  }
  if (descriptor.closeout_handoff?.to_pack_id !== PAYMENTS_CORE_CP425_PACK_BINDING.next_pack_id) errors.push("CP00-425 handoff target drift");
  if (descriptor.closeout_handoff?.next_subphase_id !== PAYMENTS_CORE_CP425_PACK_BINDING.next_subphase_id) {
    errors.push("CP00-425 next subphase drift");
  }
  return freezeCp425Validation({ valid: errors.length === 0, errors: Object.freeze(errors) });
}

export function createPaymentsCoreCp425HermesEvidencePacket(
  planPack,
  contractProjection = {},
  descriptor = createPaymentsCoreCp425P09CloseoutHandoffSliceDescriptor(),
) {
  const coverage = validatePaymentsCoreCp425Coverage(planPack);
  const slice = validatePaymentsCoreCp425P09CloseoutHandoffSliceDescriptor(descriptor, contractProjection);
  return freezeCp425Validation({
    evidence_packet: "H13.CP00-425.payments_core_p09_closeout_handoff_slice_descriptor",
    gate: PAYMENTS_CORE_CP425_PACK_BINDING.hermes_gate,
    coverage_valid: coverage.valid,
    p09_closeout_handoff_slice_valid: slice.valid,
    no_real_data: true,
    source_p09_review_closeout_slice_pack_id: PAYMENTS_CORE_CP425_PACK_BINDING.upstream_pack_id,
    required_no_leak_guards: PAYMENTS_CORE_CP425_REQUIREMENTS.required_no_leak_guards,
    next_pack_id: PAYMENTS_CORE_CP425_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP425_PACK_BINDING.next_subphase_id,
    production_ready_candidate: coverage.valid && slice.valid,
  });
}

export function createPaymentsCoreCp425ClaudeReviewPacket(planPack) {
  const coverage = validatePaymentsCoreCp425Coverage(planPack);
  const slice = validatePaymentsCoreCp425P09CloseoutHandoffSliceDescriptor(createPaymentsCoreCp425P09CloseoutHandoffSliceDescriptor(), {});
  return freezeCp425Validation({
    review_packet: "C13.CP00-425.payments_core_p09_closeout_handoff_slice_descriptor",
    gate: PAYMENTS_CORE_CP425_PACK_BINDING.claude_gate,
    required_model: "claude-opus-4-8",
    required_effort: "max",
    read_only: true,
    source_inspection_basis: "read_tools_used",
    allowed_tools: PAYMENTS_CORE_CP425_REQUIREMENTS.allowed_claude_tools,
    coverage_valid: coverage.valid,
    p09_closeout_handoff_slice_valid: slice.valid,
    invalid_review_blockers: PAYMENTS_CORE_CP425_REQUIREMENTS.forbidden_review_evidence,
    questions: Object.freeze([
      "Does CP00-425 cover exactly the 1 planned RP13 units from RP13.P09.M10.S04 through RP13.P09.M10.S04?",
      "Do the descriptor sections cover every planned row title as a descriptor-only row with the shared payments guards applied?",
      "Does CP00-425 avoid state writes, permission bypass, validation error leaks, real fixture data, test runtime paths, Hermes runtime receipts, cross-tenant access, unauthorized data, Citation Ledger, and Loop execution?",
      "Does the pack avoid treating Claude or local validation as final approval or enterprise trust?",
    ]),
  });
}

export function createPaymentsCoreCp425CloseoutHandoff() {
  return freezeCp425Validation({
    handoff_id: "CP00-425-to-CP00-426",
    from_pack_id: PAYMENTS_CORE_CP425_PACK_BINDING.pack_id,
    to_pack_id: PAYMENTS_CORE_CP425_PACK_BINDING.next_pack_id,
    next_subphase_id: PAYMENTS_CORE_CP425_PACK_BINDING.next_subphase_id,
    closed_scope: PAYMENTS_CORE_CP425_PACK_BINDING.range,
    open_scope: "Continue RP14.P00.M00.S01 onward with the next program while preserving descriptor-only payments boundaries until downstream packs explicitly open runtime behavior.",
    production_ready_flag: PAYMENTS_CORE_CP425_PACK_BINDING.production_ready_flag,
  });
}
